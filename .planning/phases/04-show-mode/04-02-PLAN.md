---
phase: 04-show-mode
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/core/src/search/search-catalog.ts
  - packages/core/src/config.ts
  - packages/core/src/index.ts
  - packages/core/test/search-catalog.test.ts
autonomous: true
requirements: [SHOW-04]
user_setup: []
must_haves:
  truths:
    - "A pure, DOM-free searchCatalog fuzzy-matches a known song by name (SHOW-04)"
    - "The search tolerates a one-character typo and returns the intended song"
    - "An empty query returns an empty result list (no crash, no whole-catalog dump)"
  artifacts:
    - path: "packages/core/src/search/search-catalog.ts"
      provides: "toCatalog + makeCatalogSearcher fuse.js wrapper over MatrixNode[]"
      exports: ["toCatalog", "makeCatalogSearcher"]
    - path: "packages/core/src/index.ts"
      provides: "barrel export of the search API"
      contains: "search-catalog"
  key_links:
    - from: "packages/core/src/search/search-catalog.ts"
      to: "fuse.js"
      via: "new Fuse(catalog, {...})"
      pattern: "new Fuse"
    - from: "packages/core/src/search/search-catalog.ts"
      to: "packages/core/src/config.ts"
      via: "search threshold/distance tunables"
      pattern: "config\\.search"
---

<objective>
Add the one genuinely new piece of core domain logic this phase needs: `searchCatalog` — a pure, zero-DOM fuzzy search over the 264-song catalog (the matrix nodes), wrapping fuse.js behind a swappable core function per CLAUDE.md. This lives in `packages/core` (not app) so the SHOW-04 search is Node-testable and the miss path can be built against it in a later wave.

Purpose: The miss path ("misses as fast as hits") is the fastest-must-be path in Show Mode; its matching engine belongs in the pure core with its own unit test, decoupled from any UI.
Output: `packages/core/src/search/search-catalog.ts`, a `search` block in core config, a barrel export, and a RED→GREEN test. No app files touched (fully parallel with plan 04-01).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-show-mode/04-RESEARCH.md
@.planning/phases/04-show-mode/04-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: searchCatalog — fuse.js fuzzy song search in core</name>
  <files>packages/core/src/search/search-catalog.ts, packages/core/src/config.ts, packages/core/src/index.ts, packages/core/test/search-catalog.test.ts</files>
  <read_first>
    - .planning/phases/04-show-mode/04-RESEARCH.md §Pattern 4 (the concrete fuse.js config + toCatalog/makeCatalogSearcher shapes + core-purity rationale) and §Package Legitimacy Audit (fuse.js CITED+VERIFIED, no checkpoint)
    - .planning/phases/04-show-mode/04-PATTERNS.md "search-catalog.ts" section (tuning-tags.ts copy-from idiom: module JSDoc, `.ts` import extensions, co-located type exports, config-under-`as const`)
    - packages/core/src/ingest/tuning-tags.ts (the exact pure-core-module analog to mirror)
    - packages/core/test/tuning-tags.test.ts (exact test-structure analog: describe/it/expect, `.ts` imports, small fixture with known expected output)
    - packages/core/src/config.ts (the `as const` object + JSDoc-per-key idiom to extend with a `search` block)
    - packages/core/src/index.ts (barrel grouped-export-with-JSDoc pattern) and packages/core/src/domain/types.ts:104 (MatrixNode fields)
  </read_first>
  <behavior>
    - Test 1: `makeCatalogSearcher(toCatalog(nodes))("Rattlesnake")` returns a result list with the exact song ranked first.
    - Test 2: a one-character typo (e.g. "Ratlesnake") still returns the intended song among results (threshold tolerance).
    - Test 3: an empty / whitespace-only query returns `[]` (never the full catalog).
    - Test 4: `toCatalog(nodes)` projects `MatrixNode[]` → `{ songId, songName }[]` losslessly for those two fields.
  </behavior>
  <action>
    Follow RESEARCH §Pattern 4 and PATTERNS.md "search-catalog.ts" analog (tuning-tags.ts idiom). Install fuse.js@7.4.2 into the core workspace ONLY (`npm i -w @guezzer/core fuse.js@7.4.2` or the pnpm --filter equivalent). Create `search/search-catalog.ts` opening with a JSDoc block stating purity + DOM-freedom (fuse.js has zero DOM deps — keeps core's ES2023/no-React purity). Import `MatrixNode` from `../domain/types.ts` with the `.ts` extension. Export: interface `CatalogEntry { songId; songName }`, interface `SearchResult { songId; songName; score }`, `toCatalog(nodes: MatrixNode[]): CatalogEntry[]`, and `makeCatalogSearcher(catalog: CatalogEntry[])` returning a `(query: string) => SearchResult[]` that builds the Fuse index once and queries many times; empty/whitespace query short-circuits to `[]`. Fuse options: keys `["songName"]`, `ignoreLocation: true`, `includeScore: true`, `threshold` + `distance` sourced from a new `search` block in `packages/core/src/config.ts` (`threshold: 0.4`, `distance: 100`) added under the existing `as const` object with JSDoc-per-key (RESEARCH A1 — tunable/swappable). Add the barrel export to `packages/core/src/index.ts` as a new grouped block with a rationale comment: `export { makeCatalogSearcher, toCatalog, type CatalogEntry, type SearchResult } from "./search/search-catalog.ts";`. Write search-catalog.test.ts under `packages/core/test/` mirroring tuning-tags.test.ts (describe/it/expect from vitest, `.ts` imports, small fixture node array with known expected output) — runs automatically in the `node` project. RED first (assert the four behaviors), then GREEN.
  </action>
  <verify>
    <automated>npx vitest run packages/core -t "searchCatalog"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run packages/core -t "searchCatalog"` passes (exact match ranked first, typo tolerated, empty→[], toCatalog projection).
    - Source assertion: search-catalog.ts imports no React/DOM symbol; core `package.json` gains only `fuse.js` at 7.4.2.
    - Source assertion: fuse options `threshold`/`distance` read from `config.search` (no inline literals).
    - Source assertion: the barrel `packages/core/src/index.ts` re-exports `makeCatalogSearcher`, `toCatalog`, `CatalogEntry`, `SearchResult`.
  </acceptance_criteria>
  <done>searchCatalog fuzzy-matches a known song, tolerates a typo, and returns [] for empty input from the Node project; fuse.js in core only; core config + barrel updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user query → fuse.js | Bounded search string; passed only to fuse.js over 264 in-memory strings — never to DOM, eval, regex, or network |
| npm registry → core deps | fuse.js supply chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Denial of Service | searchCatalog input | mitigate | fuse.js over 264 items is bounded; no user-supplied regex, no network; empty query short-circuits. (ASVS V5) |
| T-04-SC | Tampering | fuse.js install | mitigate | fuse.js is `[CITED: CLAUDE.md locked stack]` + `[VERIFIED: npm registry, 7.4.2 pub 2026-06-05]` per RESEARCH Package Legitimacy Audit (Approved, not [ASSUMED]/[SUS]/[SLOP]) — no blocking human checkpoint required. Verify the pinned version after install (lockfile shows 7.4.2). |
</threat_model>

<verification>
- `npx vitest run packages/core -t "searchCatalog"` green.
- Core purity intact: search-catalog.ts imports no React/DOM; core `package.json` gains only fuse.js.
</verification>

<success_criteria>
searchCatalog fuzzy-matches a known song, tolerates a typo, and returns [] for empty input from the Node test project; fuse.js installed into core only; barrel + core config updated.
</success_criteria>

## Artifacts this phase produces (this plan)

- **Core exports:** `searchCatalog` API — `toCatalog(nodes)`, `makeCatalogSearcher(catalog)`, types `CatalogEntry`, `SearchResult` (from `@guezzer/core` barrel)
- **Config:** `config.search.{threshold, distance}` in `packages/core/src/config.ts`
- **Dependency:** `fuse.js@7.4.2` added to `packages/core`
- **Files:** `packages/core/src/search/search-catalog.ts`, `packages/core/test/search-catalog.test.ts`

<output>
Create `.planning/phases/04-show-mode/04-02-SUMMARY.md` when done.
</output>
