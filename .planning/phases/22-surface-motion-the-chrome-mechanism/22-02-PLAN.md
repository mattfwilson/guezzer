---
phase: 22-surface-motion-the-chrome-mechanism
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - packages/app/src/components/a11y/useFocusTrap.ts
  - packages/app/src/components/Sheet.tsx
  - packages/app/test/sheet.a11y.test.tsx
  - packages/app/test/sheet.motion.test.tsx
  - packages/app/test/sheet.closeStart.test.tsx
autonomous: true
requirements: [SHEET-01, SHEET-02]

must_haves:
  truths:
    - "The instant a sheet close is requested, the background is no longer inert and focus is back on the trigger"
    - "The instant a sheet close is requested, the exiting card is aria-hidden and neither card nor scrim is a pointer target"
    - "A tap on the exiting scrim during the ~200ms window does not re-close — it reaches the real background"
    - "The sheet card slides down while the scrim fades out, in parallel, and only then leaves the DOM"
    - "A sheet with initialFocusRef focuses that element after the enter animation completes, never on exit, and never not at all"
    - "Re-opening a sheet that was mid-exit restores it fully — no stale aria-hidden, no stale pointer-events"
    - "Closing the bottom sheet of a stack leaves the background inert while the top sheet is still open"
    - "Reverting the one exit commit leaves a green, enter-only build"
  artifacts:
    - path: "packages/app/src/components/a11y/useFocusTrap.ts"
      provides: "layout-timed teardown, idempotent release, and a returned focusInitialTarget() for enter-END focus"
      exports: ["useFocusTrap"]
    - path: "packages/app/src/components/Sheet.tsx"
      provides: "enter-END initialFocusRef wiring (task 1) + exit variants and the presence-derived close-start contract (task 2)"
      contains: "useIsPresent"
    - path: "packages/app/test/sheet.closeStart.test.tsx"
      provides: "D-20 close-start contract, asserted against the REAL motion library while the exiting subtree is still mounted"
  key_links:
    - from: "packages/app/src/components/Sheet.tsx"
      to: "motion/react useIsPresent"
      via: "aria-hidden, pointer-events, scrim onClick and the onAnimationComplete guard all derive from presence, never from open"
      pattern: "useIsPresent\\(\\)"
    - from: "packages/app/src/components/Sheet.tsx"
      to: "packages/app/src/components/a11y/useFocusTrap.ts"
      via: "focusInitialTarget() called from onAnimationComplete when present, with a duration fallback timer"
      pattern: "focusInitialTarget"
---

<objective>
Close SHEET-02: everything except DOM removal fires at close-**START**. Release the background
`inert`, restore focus to the trigger, `aria-hidden` the exiting sheet, and make both sheet and scrim
non-interactive — all in the same tick `onClose` is requested. Only `AnimatePresence`'s DOM removal
waits for the ~200ms animation.

This is the phase's single hardest invariant and its one timing rule (D-19 for sheets, D-08 for
chrome — if two different rules appear, one of them is wrong).

Output: a re-timed `useFocusTrap` with its D-27 enter-END focus wiring, the `exit` variants plus the
presence-derived close-start contract (the atomic, revertible commit — D-17), and
`sheet.closeStart.test.tsx`, which keeps the contract true on every commit thereafter (D-20).

**Backable-out (D-18/D-17):** Task 2's implementation and Task 3's contract test land in **one**
commit, so a single `git revert` removes the exit behaviour *and* every assertion that depends on an
exit window, leaving a clean **enter-only** build that still typechecks and passes the suite — the
degraded ship SHEET-02 explicitly sanctions. Task 3 proves that on a scratch branch before the plan
closes. No runtime kill-switch and no feature flag: a flag ships both code paths, both need testing,
and the un-animated path rots unexercised until the night it matters.
</objective>

## Task boundaries — read before starting, they are load-bearing

**Task 1 must land a self-consistent, GREEN state.** Splitting `initialFocusRef` out of
`useFocusTrap`'s activation without also wiring `focusInitialTarget()` would leave that focus move
performed by nobody, and the shipped synchronous assertion in
`packages/app/test/sheet.a11y.test.tsx` (`initialFocusRef receives focus on open…`, around line 122)
would go red with no path back — D-27 defers that focus to enter-END, so a synchronous assertion can
never pass again. Therefore Task 1 owns **both** halves: the hook split **and** the enter-END wiring in
`Sheet`, plus converting that one shipped assertion to `await waitFor(...)`. That is not weakening the
test — the target element is unchanged; only the moment the contract promises focus has moved, per
D-27.

**Task 2 + Task 3 are ONE commit.** Task 2 writes the implementation; Task 3 writes
`sheet.closeStart.test.tsx` and folds it in with `git commit --amend --no-edit`. If the test file
landed in its own commit, reverting the exit would leave five cases asserting an exit window that no
longer exists — and its case-1 anti-vacuity check (`document.body.contains(dialog)`) would fail first.
The revert must be one command with a green result.

## ⚠ Pitfall 14 — the defect this plan exists to avoid. Read before writing a line.

**An exiting `AnimatePresence` child renders from a FROZEN React element.** `AnimatePresence` keeps
exiting children in its own `renderedChildren` state — the elements exactly as they were last created
**while present**. Motion's own docs say it verbatim: exiting children "cannot receive prop updates
because when a component is removed from the React tree … its props can no longer be updated."

So anything computed in `Sheet`'s render from the `open` prop —
`aria-hidden={open ? undefined : true}`, `pointerEvents: open ? undefined : "none"`,
`onClick={open ? onClose : undefined}`, `onAnimationComplete={() => { if (open) … }}` — is baked in at
`open === true` and **stays that way for the entire exit window**. D-19 items 3 and 4 silently never
happen, and **every test written against the open state still passes.**

Probe P2 in `22-RESEARCH.md` reproduced exactly this. Probe P1, with the identical structure but the
values derived from `useIsPresent()` inside the child, produced `aria-hidden="true"` and
`pointer-events: none` **synchronously**.

**Rule for this plan: every presence-dependent value lives inside `SheetSurface` and derives from
`useIsPresent()`. Nothing derives from `open` except `useFocusTrap`'s and `useDialogDismiss`'s
`active` argument — and those are correct precisely because `Sheet` itself is not the frozen
element.** `useIsPresent()` also self-corrects on re-open (D-22's interrupt-and-reverse), because
`PresenceChild` re-creates its context value whenever `isPresent` flips and React re-renders every
consumer of that context.

**Corollary for testing:** a hand-rolled `AnimatePresence` double that re-renders `children` fresh
would show the attributes flipping correctly **even against the broken prop-derived code**, i.e. it
would make the D-20 test pass against the defect. That is worse than no test. Task 3 uses the real
library.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-surface-motion-the-chrome-mechanism/22-CONTEXT.md
@.planning/phases/22-surface-motion-the-chrome-mechanism/22-RESEARCH.md
@.planning/phases/22-surface-motion-the-chrome-mechanism/22-PATTERNS.md
@.planning/phases/22-surface-motion-the-chrome-mechanism/22-UI-SPEC.md
@.planning/phases/22-surface-motion-the-chrome-mechanism/22-VALIDATION.md
@.planning/phases/22-surface-motion-the-chrome-mechanism/22-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-time useFocusTrap AND wire its enter-END focus, in one green step</name>
  <read_first>
    - packages/app/src/components/a11y/useFocusTrap.ts (the whole file — the `useEffect` with deps `[active, ref, initialFocusRef]`, the initial-focus line `(initialFocusRef?.current ?? focusables()[0] ?? container).focus()`, and the cleanup that removes the listener, calls `setRootInert(false)` and restores focus)
    - packages/app/src/components/a11y/inertRoot.ts (the ref-counted `setRootInert` — it guards underflow only at zero)
    - packages/app/src/components/Sheet.tsx (as restructured by plan 22-01 — the `useFocusTrap(contentRef, { active: open && modal, initialFocusRef })` call and the `SheetSurface` child, which currently receives no `focusInitialTarget`)
    - packages/app/test/sheet.a11y.test.tsx (the "close restores focus to the trigger and clears inert" case; the **"initialFocusRef receives focus on open instead of the first focusable"** case around line 122, which renders a `Harness` and asserts `document.activeElement` synchronously; and the "stacked modals: one Escape closes only the topmost; inert needs two clears" case)
    - .planning/phases/22-surface-motion-the-chrome-mechanism/22-RESEARCH.md (§Pattern 3 including its fallback-timer paragraph, §Pitfall 4a, §Pitfall 5, §Pitfall 12)
  </read_first>
  <action>
**A. Three changes to `packages/app/src/components/a11y/useFocusTrap.ts`.** This is a **re-timing, not
a rewrite** — the teardown already fires at close-start today, because React runs an effect's cleanup
whenever a dep changes, not only on unmount, and `Sheet` passes `active: open && modal`, derived from
`open` and never from presence. Probe P1 confirmed it empirically.

1. **Switch `useEffect` to `useLayoutEffect`,** so its destroy runs synchronously inside the commit's
   mutation phase rather than after paint. That is the safer contract to write down for the ordering
   Pitfall 4a describes (focus restore must complete before `aria-hidden` lands on the exiting card).
2. **Guard the release with a per-instance `releasedRef`.** `setRootInert(false)` decrements a shared
   ref count and guards underflow only at zero. With two sheets open the count is 2; if the release
   ever runs twice for the same hook instance the count drops 2 → 0 while the other sheet is still
   open, and the background becomes interactive underneath an open modal. Add a `useRef<boolean>` set
   `false` on activate and `true` on release, and make the release a no-op when already `true`.
3. **Split the `initialFocusRef` focus out of activation (D-27) and return it.** Change the return type
   from `void` to `{ focusInitialTarget: () => void }`. On activate, focus
   `focusables()[0] ?? container` — **never** `initialFocusRef`; that keeps focus inside the trap from
   frame 0, so a keyboard/VoiceOver user is not parked on a now-`inert` trigger for 200ms.
   `focusInitialTarget()` focuses `initialFocusRef.current` if one was supplied, is a no-op otherwise,
   and is safe to call more than once. Make the returned function referentially stable (`useCallback`
   or a ref-backed closure) so `SheetSurface` can pass it into a `motion` callback prop without
   churning identity every render.

Add a source comment recording **the invariant to preserve**: `active` is a function of `open`, never
of "is this still in the DOM". Making the hook presence-driven would defeat the whole close-start
contract.

**B. Wire the enter-END focus in `packages/app/src/components/Sheet.tsx`,** so nothing is left
performed-by-nobody:
  - `Sheet` destructures `const { focusInitialTarget } = useFocusTrap(...)` and passes it to
    `SheetSurface`.
  - `SheetSurface` calls `const isPresent = useIsPresent();` (from `motion/react`) and puts
    `onAnimationComplete={() => { if (isPresent) focusInitialTarget(); }}` on the **card**
    `motion.div`. **Guard on presence, not on `open`** — an `if (open)` guard is itself frozen on an
    exiting element (§Pitfall 14) and would yank focus back into a sheet that is already gone. Probe P4
    confirmed the enter completion reports `isPresent === true`, so presence is a sound discriminator.
    (There is no `exit` variant yet, so no exit completion can fire; writing the guard correctly now
    means Task 2 does not have to revisit it.)
  - **Pair the callback with a fallback timer.** In `SheetSurface`, start a
    `setTimeout(config.ui.motion.SHEET_DURATION_MS)` on mount that calls `focusInitialTarget()` if the
    surface is still present, and clear it from `onAnimationComplete` and on unmount. If the animation
    is interrupted, cancelled or never scheduled — a background tab, a future
    `MotionConfig skipAnimations` — the completion callback may never fire and the text input would
    never be focused. Focusing an already-focused element is a no-op, so a double-fire is harmless; a
    never-fire strands the user in a sheet they cannot type into.
  - Comment the D-27 rationale: `SettingsView`'s name input and `PinSheet`'s label input open the iOS
    keyboard, and focusing a translating element races two layout changes on the platform where this
    app's viewport math has already misfired. Accepted cost: ~200ms before the user can type.

**C. Amend `packages/app/test/sheet.a11y.test.tsx`** — three deliberate edits:
  - **Convert the `initialFocusRef` case to the D-27 contract.** Its assertion is currently synchronous
    (`render(<Harness/>)` then `expect(document.activeElement).toBe(…)`), which encodes focus-at-open.
    Change it to `await waitFor(() => expect(input).toHaveFocus())` and rename the case to say focus
    arrives **after the enter completes**. Add a line asserting the input is **not** focused
    synchronously on open, so the case still distinguishes the two contracts rather than merely
    becoming laxer. The target element is unchanged — only the promised moment moved (D-27). This file
    deliberately does **not** mock `motion/react`, so the real rAF fallback drives the completion; the
    fallback timer added in step B guarantees it even if the animation is skipped.
  - **Add the stacked-release case (Pitfall 5).** The shipped "stacked modals" case only closes a
    *fully closed* stacked sheet. Add a case that opens sheet A and sheet B, closes the **bottom** one
    (A) while B stays open, and asserts `#app-content` is **still** inert. Then close B and assert
    inert clears.
  - **Extend the limits header** with two notes: (i) `expect(appContent()!.inert).toBe(true)` passes
    only because it reads back the JS **expando** that `inertRoot.ts` writes — probe P8 confirmed jsdom
    29.1.1 implements no `inert`, so no attribute exists and `document.querySelector("[inert]")` finds
    nothing; React's JSX `inert` prop (used by Phase-22 chrome) *does* render a real attribute, and the
    two assertions are not equivalent. (ii) After Task 2's `exit` props land, an exiting sheet stays in
    the DOM for ~200ms; every case here asserts synchronously after `rerender`, which is exactly the
    close-start moment and stays correct — but any future case that queries *after* a close must
    `await waitForElementToBeRemoved`.

The hook's 16 consumer files are unaffected — nothing but `Sheet.tsx` calls it.
  </action>
  <verify>
    <automated>npx vitest run --project @guezzer/app packages/app/test/sheet.a11y.test.tsx packages/app/test/sheet.motion.test.tsx && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `useFocusTrap` is declared with `useLayoutEffect`, not `useEffect`, and returns an object with a `focusInitialTarget: () => void` member (return type no longer `void`).
    - The activation path no longer references `initialFocusRef` — a source read shows it used only inside `focusInitialTarget`.
    - The release path is guarded by a per-instance ref such that calling it twice performs exactly one `setRootInert(false)`.
    - `Sheet.tsx` passes `focusInitialTarget` into `SheetSurface`, and `SheetSurface` calls it from `onAnimationComplete` under an `if (isPresent)` guard **and** from a `config.ui.motion.SHEET_DURATION_MS` fallback timer cleared on completion and on unmount.
    - `packages/app/test/sheet.a11y.test.tsx`'s `initialFocusRef` case uses `await waitFor(` and also asserts the input is **not** focused synchronously on open.
    - `packages/app/test/sheet.a11y.test.tsx` gains a case named for closing the **bottom** sheet of an open stack, asserting `appContent()!.inert === true` after that close and `false` only after the top sheet also closes.
    - `npx vitest run --project @guezzer/app packages/app/test/sheet.a11y.test.tsx` exits **0** — this task leaves no red test and no deferred failure.
    - `npx tsc -b` exits 0.
  </acceptance_criteria>
  <done>The a11y hook fires its teardown in the mutation phase, cannot double-release the shared inert counter, and its initial-focus move is performed at enter-END by a caller that actually calls it — with the suite green.</done>
</task>

<task type="auto">
  <name>Task 2: The atomic exit commit — exit variants + the presence-derived close-start contract</name>
  <read_first>
    - packages/app/src/components/Sheet.tsx (the whole file as left by Task 1 — `Sheet`, `SheetSurface`, the sibling scrim/card `motion.div`s, `useIsPresent()`, the `onAnimationComplete` guard and the fallback timer)
    - packages/app/src/components/a11y/useFocusTrap.ts (as re-timed in Task 1)
    - .planning/phases/22-surface-motion-the-chrome-mechanism/22-RESEARCH.md (§Pitfall 14, §Pitfall 4, and §Code Examples "The restructured `Sheet`" — copy that structure; it was probed live in this repo)
    - .planning/phases/22-surface-motion-the-chrome-mechanism/22-UI-SPEC.md (§Sheet Timing Contract — the close-START table rows 1-4 and the close-END row)
  </read_first>
  <action>
Write the implementation and commit it with the message
`feat(22-02): sheet exit animation + close-start teardown (SHEET-02)`. **Task 3 amends this commit** —
do not squash it into anything else and do not commit anything unrelated alongside it.

All of the following goes inside **`SheetSurface`**. Nothing here may be computed in `Sheet`'s render
from `open` (§Pitfall 14).

1. **Add `exit` variants** to both siblings:
   - scrim: `exit={{ opacity: 0 }}`.
   - bottom-sheet card, motion allowed: `exit={{ y: "100%" }}` — **translate only**, matching its enter
     (the card's opacity is never animated; the scrim owns the cross-fade).
   - bottom-sheet card, reduced motion: `exit={{ opacity: 0 }}`.
   - fullscreen card: `exit={{ opacity: 0 }}` in both motion modes (D-26).
   Card and scrim run **in parallel with the same duration** — never staged. A scrim that lags the card
   leaves a painted scrim after the sheet is gone, which would make `pointer-events: none` on the first
   exit frame a subtle, easily-regressed rule instead of a structural one.
   Interruption needs no special case (D-22): `AnimatePresence` reverses from the current position, and
   because every close-start value derives from `useIsPresent()`, re-opening mid-exit **self-corrects** —
   there is no stale attribute to undo.

2. **Ease flips with presence:**
   `const ease = isPresent ? config.ui.motion.SHEET_EASE_ENTER : config.ui.motion.SHEET_EASE_EXIT;`
   used on both siblings' `transition`, alongside the shared
   `duration: config.ui.motion.SHEET_DURATION_MS / 1000`. No numeric literal in a transition (D-25).

3. **D-19 items 3 and 4, presence-derived:** build one `closing` object —
   `"aria-hidden": isPresent ? undefined : true` and
   `style: { pointerEvents: isPresent ? undefined : "none" }` — and spread it onto **both** the card and
   the scrim (the scrim already has a literal `aria-hidden="true"`, so it needs only the style half).
   Comment that these must never be derived from `open`.

4. **Neutralise the scrim tap while exiting (Pitfall 4b):** `onClick={isPresent ? onClose : undefined}`.
   `fireEvent.click` in `@testing-library/dom` dispatches directly and **ignores**
   `pointer-events: none` (only `user-event` enforces it, and it is not installed and must not be
   installed), so dropping the handler is what makes the contract provable in jsdom, while the inline
   `pointer-events: none` is what makes it true in a real browser. Two mechanisms, one contract — say so
   in a comment.

Then extend `packages/app/test/sheet.motion.test.tsx` (created in 22-01) with the exit half of
SHEET-01, using its existing pass-through mock: the bottom-sheet card's `data-exit` has `y === "100%"`
and **no** `opacity` key with motion allowed, and `opacity` with no `y` under reduced motion; the
fullscreen variant's `data-exit` never contains `y`; the scrim's `data-exit` is `{ opacity: 0 }`. (The
mocked `useIsPresent` should return `true` there — that file proves prop shape, not timing.)
  </action>
  <verify>
    <automated>npx vitest run --project @guezzer/app packages/app/test/sheet.motion.test.tsx packages/app/test/sheet.a11y.test.tsx packages/app/test/layerOrder.test.tsx && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `Sheet.tsx` contains `exit=` on both siblings in both variants, and both transitions read `config.ui.motion.SHEET_DURATION_MS` with an `ease` chosen by `isPresent`.
    - Every occurrence of `aria-hidden`, `pointerEvents`, the scrim `onClick` and the `onAnimationComplete` guard reads `isPresent`; **grepping `Sheet.tsx` for `open ?` returns no match inside `SheetSurface`**, and `SheetSurface` receives no `open` prop at all.
    - `packages/app/test/sheet.motion.test.tsx` gains the four exit-prop cases and exits 0.
    - `packages/app/test/sheet.a11y.test.tsx` and `packages/app/test/layerOrder.test.tsx` both exit 0.
    - `npx tsc -b` exits 0.
    - `git log -1 --pretty=%s` reads `feat(22-02): sheet exit animation + close-start teardown (SHEET-02)` and `git log -1 --name-only` lists only `packages/app/src/components/Sheet.tsx` and `packages/app/test/sheet.motion.test.tsx` — Task 3 adds the third file by amend.
  </acceptance_criteria>
  <done>Sheets slide out and cross-fade out in parallel; the a11y teardown is derived from presence so it actually fires; re-opening mid-exit self-corrects.</done>
</task>

<task type="auto">
  <name>Task 3: sheet.closeStart.test.tsx against the REAL library, folded into the exit commit, with a revert dry-run</name>
  <read_first>
    - .planning/phases/22-surface-motion-the-chrome-mechanism/22-RESEARCH.md (§Code Examples "The D-20 close-start test — use the **real** `motion`, not a hand-rolled double" — reproduce that test's structure and its anti-vacuity ordering; and probes P1/P3)
    - packages/app/test/sheet.a11y.test.tsx (lines ~40-58 — `mountAppContent()` and the `afterEach` cleanup; and the `rerender` open→closed idiom this test extends)
    - packages/app/src/components/Sheet.tsx (the file under test, as completed in Task 2)
    - .planning/phases/22-surface-motion-the-chrome-mechanism/22-VALIDATION.md (§Correction — the D-20 test uses the real library; the hand-rolled double is retracted)
  </read_first>
  <action>
**A. Create `packages/app/test/sheet.closeStart.test.tsx`.**

**Use the real `motion` library. Do not mock `motion/react` in this file at all.** Two mocks are both
wrong here and the file header must say so:
  - the shipped pass-through double (`AnimatePresence: ({children}) => children`) unmounts the exiting
    child instantly, so every assertion passes **vacuously**;
  - a hand-rolled "retaining" double that re-renders `children` fresh would show `aria-hidden` and
    `pointer-events` flipping correctly **even if production derived them from `open`** — it would make
    this test pass against the §Pitfall 14 defect, which is worse than no test.
Probe P3 verified the real library works end to end under this repo's jsdom: `Element.prototype.animate`
is undefined so motion runs its rAF fallback, the exiting node is retained synchronously and removed
after the duration. **No fake timers** — they desynchronise that loop.

Cases:
  1. **Close-start contract (D-20).** Mount `#app-content` with a trigger, focus it, render a modal
     `<Sheet open backdrop>`, capture the `dialog` node and the scrim node (query the scrim by its
     `bg-black/50` class), then `rerender` with `open={false}` and assert **synchronously**, in this
     order:
       - **anti-vacuity FIRST:** `document.body.contains(dialog) === true`. Without this the five
         assertions below pass trivially and prove nothing.
       - `appContent.inert === false` (D-19 #1)
       - `document.activeElement === trigger` (D-19 #2)
       - `dialog.getAttribute("aria-hidden") === "true"` (D-19 #3 — the assertion that catches Pitfall 14)
       - `dialog.style.pointerEvents === "none"` **and** `scrim.style.pointerEvents === "none"` (D-19 #4)
       - `fireEvent.click(scrim)` then `expect(onClose).not.toHaveBeenCalled()` (Pitfall 4b)
     Then `await waitForElementToBeRemoved(() => screen.queryByRole("dialog"), { timeout: 2000 })` —
     D-19 #6, the only thing that waits.
  2. **`aria-hidden` never lands on the focused subtree (Pitfall 4a).** In the same close-start window,
     assert `dialog.contains(document.activeElement) === false`.
  3. **Re-open mid-exit self-corrects (D-22).** Close, and before removal completes `rerender` with
     `open` back to `true`; assert the dialog has **no** `aria-hidden` attribute and an empty
     `style.pointerEvents`, and that `#app-content.inert` is `true` again.
  4. **`initialFocusRef` fires at enter-END, never on exit (D-27, Pitfall 12).** Render a sheet with an
     `initialFocusRef` on an `<input>`. Assert that synchronously on open the input is **not** focused.
     Then `await waitFor(() => expect(input).toHaveFocus())`. Close, and assert focus ends on the
     trigger and the input is not re-focused.
  5. **Fullscreen variant has no scrim to de-target but still hides itself.** Close a
     `variant="fullscreen"` sheet and assert, before removal, `aria-hidden="true"` and
     `pointerEvents === "none"` on the dialog, and that no `.bg-black\\/50` element exists.

**Do not assert intermediate geometry.** Probe P1 showed motion writes the exit target
(`translateY(100%)`) into inline style essentially immediately under jsdom, so the transform value is
not a trustworthy proxy for visual progress. Assert attributes, presence and removal only — say this in
the file header, along with the honest limit that real 200ms touch timing on a device is
`22-HUMAN-UAT.md` test 2 (plan 22-09).

**B. Fold this file into Task 2's commit.** Stage it and run `git commit --amend --no-edit`, so the
resulting single commit — subject `feat(22-02): sheet exit animation + close-start teardown (SHEET-02)` —
contains `packages/app/src/components/Sheet.tsx`, `packages/app/test/sheet.motion.test.tsx` **and**
`packages/app/test/sheet.closeStart.test.tsx`. This is the D-17/D-18 revert unit: the exit behaviour and
every assertion that depends on an exit window must leave together, or the sanctioned enter-only ship is
a red build.

**C. Prove the revert on a scratch branch.** Create a throwaway branch at HEAD, `git revert --no-edit`
that one commit, then run `npx vitest run --project @guezzer/app` and `npx tsc -b`. Both must pass —
`sheet.closeStart.test.tsx` should be **gone** (removed by the revert), `sheet.a11y.test.tsx`'s
`waitFor` initialFocusRef case should still pass (Task 1's enter-END wiring survives the revert), and
`sheet.motion.test.tsx` should be back to its enter-only cases. Record the observed exit codes in the
plan summary, then delete the scratch branch.

Record in the summary that **three artifacts outside this commit also have to go in a real revert.**
`22-02-SUMMARY.md` is the document actually read on a device night when the enter-only fallback is being
considered, so it must not understate the work:

  1. `packages/app/test/dexView.test.tsx` — the
     `describe("fullscreen sheet exit window (reverts with the 22-02 exit commit)", …)` block (plan 22-04).
  2. `packages/app/test/trailNodeSheet.test.tsx` — the
     `describe("bottom-sheet exit window (reverts with the 22-02 exit commit)", …)` block (plan 22-10).
  3. `packages/app/test/songRow.test.tsx` — the same-named `bottom-sheet exit window …` block (plan 22-10).

All three assert an exit window that the revert removes. They correspond exactly to steps **(b)** and
**(c)** of plan 22-09's revert procedure 1, which is the authoritative copy and which re-runs the dry-run
with all three deletions applied. Cross-reference it by name from the summary rather than restating the
steps a second time.
  </action>
  <verify>
    <automated>npx vitest run --project @guezzer/app packages/app/test/sheet.closeStart.test.tsx && git log -1 --name-only --pretty=format:"%s" | grep -q "sheet.closeStart.test.tsx" && echo "closeStart test is inside the exit commit"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --project @guezzer/app packages/app/test/sheet.closeStart.test.tsx` exits 0 with at least 5 passing cases.
    - The file contains **no** `vi.mock("motion/react"` and no `vi.useFakeTimers()`.
    - Case 1's first assertion after `rerender` is the `document.body.contains(dialog)` anti-vacuity check, and all six sub-conditions are in a single test body.
    - Case 3 asserts the **absence** of `aria-hidden` after re-open, not merely that the dialog is present.
    - The file contains no assertion on `style.transform` or any other geometry value.
    - `git log -1 --name-only` shows exactly three files in the exit commit: `packages/app/src/components/Sheet.tsx`, `packages/app/test/sheet.motion.test.tsx`, `packages/app/test/sheet.closeStart.test.tsx`.
    - The scratch-branch revert dry-run is recorded in the summary with the actual `vitest` and `tsc` exit codes, both 0.
    - The summary names all **three** out-of-commit artifacts (`dexView.test.tsx`, `trailNodeSheet.test.tsx`, `songRow.test.tsx`) with their `describe`-block names and points at plan 22-09 procedure 1 steps (b) and (c) — it must not say "one artifact".
    - `npx vitest run --project @guezzer/app` (the whole app project) exits 0 on the real branch.
  </acceptance_criteria>
  <done>The close-start contract is asserted on every commit against the real library, and the sanctioned enter-only fallback is a single verified `git revert` rather than a hope.</done>
</task>

</tasks>

## Artifacts this phase produces

Created by **this plan**:

| Kind | Symbol / path |
|------|----------------|
| Hook return value | `useFocusTrap(...)` now returns `{ focusInitialTarget: () => void }` (was `void`) |
| Internal binding | `isPresent` (from `useIsPresent()`) inside `SheetSurface`, and the `closing` prop bundle derived from it |
| Internal binding | the `SHEET_DURATION_MS` fallback timer in `SheetSurface` |
| Test file | `packages/app/test/sheet.closeStart.test.tsx` |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | No network call, no persistence, no user-supplied data path, no new dependency. The change is entirely a re-timing of DOM lifecycle code that already ships. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03 | Denial of Service (availability) | `useFocusTrap` / `inertRoot` — a double release un-inerts the background underneath an open modal, or an unreleased count leaves the whole app permanently inert | mitigate | Per-instance `releasedRef` makes release idempotent; the extended stacked-modals case in `sheet.a11y.test.tsx` closes the **bottom** sheet of an open stack and asserts the background stays inert — the exact case a single-sheet test cannot see. |
| T-22-04 | Elevation of Privilege (interaction with a control the UI claims is gone) | the exiting sheet card and scrim | mitigate | `pointer-events: none` is never relied on alone: it is paired with `aria-hidden` on the card and with dropping the scrim's `onClick`, and **all three derive from `useIsPresent()`**, not from the frozen `open` prop (§Pitfall 14) — otherwise none of them would fire at all. Asserted by Task 3 case 1, anti-vacuity check first. |
| T-22-05 | Denial of Service (at a venue) | a tap swallowed by a leaving sheet during the ~200ms exit | mitigate | D-20's six-part assertion runs on every commit **against the real library**. The real-touch case is device-verified in plan 22-09. If the device check fails, one `git revert` — proven green on a scratch branch in Task 3 — leaves the sanctioned enter-only ship. |
| T-22-21 | Denial of Service (availability) | `initialFocusRef` focus that never fires because `onAnimationComplete` never runs (background tab, interrupted animation) | mitigate | A `SHEET_DURATION_MS` fallback timer calls `focusInitialTarget()` and is cleared by whichever path fires first. Focusing an already-focused element is a no-op; a never-fire strands the user in a text-entry sheet they cannot type into. |
| T-22-35 | Denial of Service (a red build on the night it matters) | the sanctioned enter-only revert leaving orphaned tests that assert a removed exit window | mitigate | The exit implementation and `sheet.closeStart.test.tsx` land in **one** commit, and Task 3 proves that single revert green on a scratch branch. The **three** out-of-commit artifacts (`dexView.test.tsx` from plan 22-04; `trailNodeSheet.test.tsx` and `songRow.test.tsx` from plan 22-10) are named in this plan's summary and are steps (b) and (c) of plan 22-09's revert procedure 1, whose own dry-run applies all three deletions. |
| T-22-SC | Tampering | npm/pip/cargo installs | accept | No packages installed. `@testing-library/user-event` is explicitly **not** to be installed (zero-new-deps constraint); the `pointer-events` contract is proven by the style string plus the dropped handler instead. |
</threat_model>

<verification>
- `npx vitest run --project @guezzer/app` green.
- `npx tsc -b` exits 0.
- `git log -1 --name-only` shows the exit commit carrying exactly three files.
- The scratch-branch revert dry-run passed and is recorded in the summary, together with all **three** out-of-commit artifacts (`dexView.test.tsx`, `trailNodeSheet.test.tsx`, `songRow.test.tsx`) cross-referenced to plan 22-09 procedure 1 steps (b) and (c).
- A source read of `Sheet.tsx` confirms `SheetSurface` receives no `open` prop.
</verification>

<success_criteria>
- Background not inert, focus on the trigger, card `aria-hidden`, card + scrim `pointer-events: none`, and a scrim tap not re-closing — all true, verifiably, before the exit animation finishes.
- Sheets slide down and cross-fade out in parallel over `config.ui.motion.SHEET_DURATION_MS`, then leave the DOM.
- Re-opening mid-exit restores a fully interactive sheet with no stale attributes.
- `initialFocusRef` focus happens exactly once, at enter-END, and never on exit — with a fallback so it never fails to happen at all.
- Reverting one commit yields an enter-only build that typechecks and passes the suite, **verified**, not assumed.
</success_criteria>

<output>
Create `.planning/phases/22-surface-motion-the-chrome-mechanism/22-02-SUMMARY.md` when done.
</output>
