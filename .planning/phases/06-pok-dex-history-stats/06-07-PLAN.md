---
phase: 06-pok-dex-history-stats
plan: 07
type: execute
wave: 5
depends_on: ["06-05", "06-06"]
files_modified:
  - packages/core/src/dex/search-archive.ts
  - packages/core/src/dex/recent-shows.ts
  - packages/core/src/index.ts
  - packages/app/src/dex/ArchiveBrowser.tsx
  - packages/app/src/dex/DexView.tsx
  - packages/app/src/dex/useDexStats.ts
  - packages/app/src/config.ts
  - packages/core/test/dex/search-archive.test.ts
  - packages/core/test/dex/recent-shows.test.ts
  - packages/app/test/archiveBrowser.test.tsx
autonomous: true
requirements: [DEX-02, DEX-03]
must_haves:
  truths:
    - "From the Dex Shows segment (or the empty-state CTA) the user opens Mark attended shows: year sections newest-first plus fuzzy search over date/venue/city of the bundled 738-show archive, fully offline (D-09/D-10)"
    - "Tapping an unmarked show marks it in one tap and credits its full setlist as sightings — every dex number recomputes live; '+{n} songs caught' flashes inline (D-11)"
    - "Unmarking is one tap behind a confirm dialog; live-tracked shows are not unmarkable (they render as already-marked, D-12 + Pitfall 6)"
    - "When online, 'Search kglw.net for newer shows' fetches post-corpus shows politely (validated, filtered, never retried); marking one persists its setlist to the archiveShows cache so it survives reload (D-09 + Pitfall 5)"
    - "Offline, the fallback row is hidden and replaced by the muted 'Newer shows need a connection.' note — the corpus path is fully functional"
  artifacts:
    - path: "packages/core/src/dex/search-archive.ts"
      provides: "makeArchiveSearcher (fuse.js) + groupShowsByYear pure helpers"
      contains: "makeArchiveSearcher"
    - path: "packages/core/src/dex/recent-shows.ts"
      provides: "fetchRecentShows tolerant online fallback (zod + assertFilterApplied)"
      contains: "assertFilterApplied"
    - path: "packages/app/src/dex/ArchiveBrowser.tsx"
      provides: "Mark attended shows full-screen browser"
      contains: "markShowAttended"
  key_links:
    - from: "packages/app/src/dex/ArchiveBrowser.tsx"
      to: "packages/app/src/db/db.ts markShowAttended/unmarkShowAttended"
      via: "one-tap mark / confirm-gated unmark"
      pattern: "unmarkShowAttended"
    - from: "packages/core/src/dex/recent-shows.ts"
      to: "packages/core/src/ingest/validate.ts"
      via: "assertFilterApplied after artist_id===1 filter (D-09 etiquette)"
      pattern: "assertFilterApplied"
    - from: "packages/app/src/dex/useDexStats.ts"
      to: "db.archiveShows"
      via: "liveQuery read feeding deriveDex's archiveShows cache input"
      pattern: "archiveShows"
---

<objective>
The retroactive attendance slice (DEX-02, D-09..D-12): pure core archive search (fuse.js `searchCatalog` idiom) + year browsing, the polite online fallback for post-corpus shows (`pollLatest` tolerance + census zod schemas + `assertFilterApplied`), and the full-screen ArchiveBrowser wired to the 06-06 mark/unmark helpers — reachable from the Dex Shows segment.

User story slice: *As a fan who's been to shows before this app existed, I want to mark past shows from the archive and get full-setlist credit instantly, so that my dex reflects my whole history.* One tap marks (D-11), one confirmed tap unmarks (D-12), and every count recomputes by derivation — nothing stored.

Purpose: the bundled archive ends 2025-12-13 (RESEARCH-verified, a real 7-month gap today); the online fallback + archiveShows cache (06-06) is what keeps summer-2026 shows creditable. Etiquette for the browser fetch is behavioral (RESEARCH Pitfall 9): user-initiated, one GET per session per year, soft-fail-never-retry.
Output: two core modules + ArchiveBrowser + DexView/useDexStats wiring + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-pok-dex-history-stats/06-CONTEXT.md
@.planning/phases/06-pok-dex-history-stats/06-RESEARCH.md
@.planning/phases/06-pok-dex-history-stats/06-PATTERNS.md
@.planning/phases/06-pok-dex-history-stats/06-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Core archive search + year browse (D-10)</name>
  <files>packages/core/src/dex/search-archive.ts, packages/core/src/index.ts, packages/core/test/dex/search-archive.test.ts</files>
  <read_first>
    - packages/core/src/search/search-catalog.ts (the build-once/query-many fuse.js factory to copy nearly wholesale, lines ~49-68: empty query short-circuits to [])
    - packages/core/src/dex/archive-types.ts (ArchiveShow input shape)
    - packages/core/src/config.ts (config.dex.archiveSearch tunables from 06-01)
  </read_first>
  <behavior>
    - makeArchiveSearcher(shows) returns a query fn: fuzzy over date, venue, city; empty/whitespace query → [] (never a whole-archive dump); results carry the full ArchiveShow + score.
    - "red rocks" matches Red Rocks shows despite case; "2022-10" matches shows in that month (date substring tolerance).
    - groupShowsByYear(shows) returns [{ year, shows }] newest year first, shows within a year newest first.
  </behavior>
  <action>
    Create `packages/core/src/dex/search-archive.ts` exporting `makeArchiveSearcher(shows: ArchiveShow[]): (query: string) => ArchiveSearchHit[]` (Fuse over keys ["date", "venue", "city"], threshold/distance/ignoreLocation from `config.dex.archiveSearch`, includeScore) and `groupShowsByYear(shows: ArchiveShow[]): Array<{ year: number; shows: ArchiveShow[] }>` (plain sort/group — year browse is NOT fuse, per the pattern map). Export `ArchiveSearchHit` ({ show: ArchiveShow; score: number | undefined }). Barrel-export all three. Tests with a ~8-show synthetic archive: venue match, city match, date-fragment match, case insensitivity, empty-query short circuit, year grouping order (newest first at both levels).
  </action>
  <verify>
    <automated>npx vitest run --project @guezzer/core test/dex/search-archive.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All six fixture behaviors asserted and green.
    - No tunable literal in search-archive.ts (threshold/distance read from config — grep confirms `archiveSearch`).
    - Full core suite green.
  </acceptance_criteria>
  <done>Finding a show is a pure, offline, config-tuned function over the bundled archive.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Online recent-show fallback (D-09, Pitfall 9)</name>
  <files>packages/core/src/dex/recent-shows.ts, packages/core/src/index.ts, packages/core/test/dex/recent-shows.test.ts</files>
  <read_first>
    - packages/core/src/live/poll-latest.ts (the injected-deps + tolerant-never-throw shape to copy: lines ~33-43 PollDeps, ~52-93 soft-fail policy)
    - packages/core/src/cli/fetch-corpus.ts (endpoint shape `setlists/showyear/{year}.json` lines ~149-155 + the zod row validation flow)
    - packages/core/src/ingest/validate.ts (assertFilterApplied lines ~15-30 — reuse, don't reimplement) and packages/core/src/ingest/api-types.ts (the census setlist row schema the responses match)
    - packages/core/test/poll-latest.test.ts / fetch.test.ts (injected deps.fetch test idiom)
    - 06-RESEARCH.md §Code Examples "Online recent-show fallback" + Pitfall 9 (browser UA header is inert; etiquette is behavioral) + Assumption A1
  </read_first>
  <behavior>
    - fetchRecentShows(year, deps) GETs `${config.apiBase}/setlists/showyear/{year}.json`, validates rows with the existing census zod schema, filters artist_id === 1, then assertFilterApplied — and returns shows GROUPED into the ArchiveShow shape (id, date, venue, city, sets with ordered songIds).
    - Non-OK response, network reject, timeout, malformed JSON, or body.error → [] (soft-fail, never throw, never retry).
    - Malformed rows are skipped individually (console.debug), valid rows survive.
    - Only shows dated AFTER a supplied sinceDate (the archive's latestShowDate) are returned — the fallback never duplicates corpus shows.
  </behavior>
  <action>
    Create `packages/core/src/dex/recent-shows.ts` exporting `interface RecentShowsDeps { fetch: typeof globalThis.fetch }` and `fetchRecentShows(year: number, sinceDate: string, deps: RecentShowsDeps = defaultDeps): Promise<ArchiveShow[]>` following pollLatest's exact failure policy (try/catch → [], `if (!res.ok) return []`, `AbortSignal.timeout(config.fetchTimeoutMs)`; keep the UA header line for Node/test symmetry knowing browsers drop it — the REAL etiquette is the caller contract documented in the module header: user-initiated only, session-cached, never retried). Validate each raw row with the existing ingest setlist-row zod schema (import — do not redeclare), filter `artist_id === 1`, call `assertFilterApplied(validated, endpoint, { field: "showyear", expected: year })` inside the try (an assert failure is a soft [] too — tolerant tier), group rows by show into `ArchiveShow` (reuse/adapt the normalizer's set-structure logic minimally: setnumber → "1"|"2"|"e" vocabulary, position order), and drop shows with date ≤ sinceDate. Barrel-export. Tests with injected fake fetch: happy path (2 shows grouped with correct sets), non-OK → [], reject → [], foreign-artist rows filtered, filter-assert failure → [], sinceDate boundary exclusion.
  </action>
  <verify>
    <automated>npx vitest run --project @guezzer/core test/dex/recent-shows.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All six injected-fetch scenarios green; no test performs real network I/O.
    - grep confirms recent-shows.ts imports assertFilterApplied from ingest/validate (no reimplementation) and contains no retry loop.
    - Returned shows validate structurally as ArchiveShow (parse through archiveShowSchema in a test).
    - Full core suite green.
  </acceptance_criteria>
  <done>Post-corpus shows are fetchable once, politely, validated and filtered — or silently absent, never an error loop.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ArchiveBrowser + Dex wiring (D-11/D-12, Pitfall 6)</name>
  <files>packages/app/src/dex/ArchiveBrowser.tsx, packages/app/src/dex/DexView.tsx, packages/app/src/dex/useDexStats.ts, packages/app/src/config.ts, packages/app/test/archiveBrowser.test.tsx</files>
  <read_first>
    - packages/app/src/show/SearchSheet.tsx (full-screen overlay to copy: role="dialog" aria-modal, ≥16px input, memoized core searcher lines ~53-57, min-h-11 result rows)
    - packages/app/src/live/useOnlineStatus.ts (online gating, as used in ShowView ~line 84)
    - packages/app/src/db/db.ts (markShowAttended/unmarkShowAttended from 06-06; attendedShows/trackedShows reads for marked-state)
    - packages/app/src/dex/useDexStats.ts + DexView.tsx (06-05 — the hook gains the archiveShows read; the Shows segment gains the CTA)
    - 06-UI-SPEC.md §Layout 2 (archive browser contract) + §Copywriting (exact strings: "Mark attended shows", "Search by date, venue, or city", "+{n} songs caught", "No shows match.", "The bundled archive covers through {corpusDate}.", "Search kglw.net for newer shows", "Newer shows need a connection.", "Can't reach kglw.net.", unmark dialog copy) + §Destructive actions
  </read_first>
  <behavior>
    - Shows segment header (and the dex empty state) shows the "Mark attended shows" CTA opening ArchiveBrowser full-screen.
    - Year sections newest-first, most recent expanded, others collapsed (ChevronDown/Up); search field filters via the core searcher.
    - Rows: date + venue, city; trailing hollow circle (unmarked) or green CircleCheck (marked). Already-attended detection checks BOTH sources: attendedShows by show_id AND trackedShows (bound showId match, else date match — Pitfall 6; prefer show_id).
    - Tap unmarked row → markShowAttended + inline "+{n} songs caught" flash (n = the show's setlist size); every dex stat updates via liveQuery with no manual refresh.
    - Tap a retro-marked row's state control → "Unmark this show?" confirm dialog (Unmark destructive red / Cancel); live-tracked rows render marked but expose NO unmark control.
    - Online: a "Search kglw.net for newer shows" row appears; tapping fetches years after latestShowDate once per session (in-memory cache), renders results in the same row UI; marking one calls markShowAttended WITH cachedSetlist. Failure → "Can't reach kglw.net." note. Offline: the row is replaced by "Newer shows need a connection."
  </behavior>
  <action>
    Create `packages/app/src/dex/ArchiveBrowser.tsx` per the SearchSheet idiom: memoized `makeArchiveSearcher(archive.shows)` + `groupShowsByYear`, controlled search input (text-base ≥16px), collapsible year sections (Label muted headers with tabular-nums year + show count), result rows ≥44px rendering strings as React text. Marked-state via useLiveQuery over attendedShows + trackedShows with the dual-source rule above. Mark flow: `markShowAttended({ show_id: show.id, showDate: show.date })` for corpus/archive shows; for fallback-fetched shows pass `cachedSetlist` built from the fetched ArchiveShow (songName resolved from archive.songs map or the fetched rows). Inline flash: transient row state showing config copy "+{n} songs caught" (CircleCheck green). Unmark: confirm dialog per the EndShowDialog sheet idiom with exact UI-SPEC copy, destructive `#EF4444` Unmark button calling `unmarkShowAttended`; suppress the control for tracked-only rows (they are history records — UI-SPEC destructive table). Online fallback: `useOnlineStatus`; on tap, `fetchRecentShows(currentYear, archive.latestShowDate)` (and Jan/Dec edge: also fetch latestShowDate's year when it differs) with a session-scoped in-memory cache (module-level Map keyed by year — one GET per year per session); loading → results → soft-failure note. Wire the CTA into DexView's Shows segment header + the dex empty-state CTA (06-05 left both spots). Extend useDexStats: add `useLiveQuery(() => db.archiveShows.toArray())` and pass it into deriveDex's archiveShows input. Add `config.copy.archive` block with every string above. Tests (fake-indexeddb + mocked @archive fixture + vi.mock of recent-shows): year order + collapse default, search filtering, one-tap mark writes attendedShows and the row flips, unmark confirm deletes, tracked-row shows marked with no unmark control, offline hides the fallback row and shows the note, fallback mark passes cachedSetlist through to db (archiveShows row present).
  </action>
  <verify>
    <automated>npx vitest run --project @guezzer/app test/archiveBrowser.test.tsx</automated>
    <human-check>End-of-phase device gate: mark a real past show on-device; dex header counts jump instantly; airplane-mode browse still works.</human-check>
  </verify>
  <acceptance_criteria>
    - archiveBrowser.test.tsx passes all seven scenarios above.
    - Marking a fixture show changes useDexStats-derived counts in the same render cycle chain (liveQuery reactivity asserted end-to-end in a test).
    - grep confirms ArchiveBrowser renders venue/city via React text (no dangerouslySetInnerHTML) and contains no fetch retry.
    - All archive copy strings live in config.copy.archive (spot-check two).
    - Full app suite green.
  </acceptance_criteria>
  <done>The user can mark any show they ever attended — bundled archive offline, recent shows online — with one tap, full-setlist credit, and free unmark.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kglw.net showyear responses → app state | Live external rows enter the mark flow and (on mark) the DB cache |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-17 | Spoofing/Tampering | silent-filter-ignore returning foreign-band rows | mitigate | artist_id === 1 filter + assertFilterApplied (existing guard, mandatory per D-09); zod row validation via existing census schemas |
| T-06-18 | Injection (XSS) | fetched venue/song strings | mitigate | React text rendering only; strings pass through zod string schemas; persisted rows conform to strictObject archiveShowRow at export time |
| T-06-19 | Denial of service (API abuse) | fallback fetch | mitigate | User-initiated only, one GET per year per session (in-memory cache), soft-fail-never-retry (pollLatest policy) |
| T-06-20 | Tampering (double count) | tracked + retro same night | mitigate | Dual-source marked-state rendering + deriveDex dedupe (06-03, fixture-pinned); show_id preferred over date matching |
| T-06-SC | Tampering (supply chain) | package installs | mitigate | No new dependencies in this plan |
</threat_model>

<verification>
- `npx vitest run` (both projects) green; `npx tsc --noEmit` green.
- Dev-server spot check: mark/unmark round-trip updates the dex header live.
</verification>

<success_criteria>
- DEX-02: full archive searchable by date/venue offline, keyed by stable show_id; recent shows reachable online.
- DEX-03/D-11/D-12: full-setlist credit on one tap, derivation-only counts, free confirmed unmark.
- D-09 etiquette: validated, filtered, asserted, session-cached, never retried.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 07)

New symbols (exclude from source-drift verification — created here):
- `makeArchiveSearcher`, `groupShowsByYear`, `ArchiveSearchHit` — `packages/core/src/dex/search-archive.ts`
- `fetchRecentShows`, `RecentShowsDeps` — `packages/core/src/dex/recent-shows.ts`
- `ArchiveBrowser` component — `packages/app/src/dex/ArchiveBrowser.tsx`
- `config.copy.archive.*` block — `packages/app/src/config.ts`
- Barrel re-exports — `packages/core/src/index.ts`
- New tests: `search-archive.test.ts`, `recent-shows.test.ts`, `archiveBrowser.test.tsx`
</artifacts_produced>

<output>
Create `.planning/phases/06-pok-dex-history-stats/06-07-SUMMARY.md` when done
</output>
