---
phase: 20-presence-interactions
plan: 05
type: execute
wave: 5
depends_on: [20-04]
files_modified: []
autonomous: false
requirements: [PRES-01, PRES-02, PRES-05, PRES-07]

must_haves:
  truths:
    - "Two signed-in devices see each other's online dot appear on connect and disappear on disconnect (present-now only, never a stale-green lie)"
    - "A friend's coarse activity label updates live across devices as they switch tabs / start a tracked show (`At a show 🎸`)"
    - "A broadcast wave and a targeted wave each pop the correct toast on the peer (targeted reads `waved at you`; a wave targeted at a third party is NOT shown)"
    - "A burst of waves reads as distinct one-at-a-time pops, not a clobber"
    - "When a device goes offline, its friend dots hide and its You row reads `offline`; reconnect restores presence"
  artifacts: []
  key_links: []
---

<objective>
Verify the ephemeral presence + interaction layer live across two real devices — the only way to prove Realtime presence join/leave, cross-device activity propagation, and targeted/broadcast wave delivery (PRES-01/02/05/07). This mirrors the Phase-19 19-04 two-device UAT discipline; unit/component tests already cover the pure logic, but realtime cross-device behavior is inherently a device check.

Purpose: confirm the residency-night payoff ("who's at the gig right now" + waves) actually works between two friends before relying on it during the run.
Output: an on-device pass recorded by the owner; no code unless a loop-breaking blocker is found.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-presence-interactions/20-CONTEXT.md
@.planning/phases/20-presence-interactions/20-UI-SPEC.md
@.planning/phases/20-presence-interactions/20-04-PLAN.md
</context>

## Artifacts this plan produces

- No new symbols. This plan is a human verification gate over the surfaces shipped in 20-01…20-04. Device-under-test notes go into `20-05-SUMMARY.md` / `20-HUMAN-UAT.md`.

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: [BLOCKING] Two-device live presence + wave verification</name>
  <files>none (build + verification-only)</files>
  <read_first>
    - $HOME/.claude/projects/C--Users-mattf-git-guezzer/memory/device-uat-hosting.md (serve over an HTTPS cloudflared tunnel with --http-host-header localhost for iOS)
    - .planning/phases/19-shared-dex-progress/19-04-PLAN.md (the prior two-device UAT harness + serve pattern to reuse verbatim)
    - .planning/phases/20-presence-interactions/20-UI-SPEC.md (the interaction/behavior contract to verify against)
  </read_first>
  <what-built>
    Realtime presence dots + coarse activity labels on the Friends screen, app-wide wave/reaction toasts (targeted + broadcast), and the offline dot-hide / self-offline behavior — all on the single `gizz-room` channel, ephemeral (never persisted).
  </what-built>
  <action>
    Executor: run the full automated suite (`npm test -- --run`) as the regression backstop, produce the production build (`npm run build --workspace packages/app`), and serve `packages/app/dist` over the HTTPS cloudflared tunnel per MEMORY `device-uat-hosting` (include `--http-host-header localhost` so an installed iOS PWA reaches it in a secure context). Give the owner the tunnel URL and confirm two devices can each sign in as DISTINCT seeded identities (Phase-18 roster) so presence keys differ. Present the how-to-verify checklist, pause for the human verdict, and record PASS or the concrete gap list in the SUMMARY. Do NOT make code changes here — reported gaps feed `/gsd-plan-phase 20 --gaps`, not an in-place fix.
  </action>
  <how-to-verify>
    Sign device A and device B in as two distinct seeded friends. Open the GizzDex → Friends segment on both.
    1. PRES-01 present-now: With both online, confirm A sees B's row with a green online dot (and B sees A's). Force-quit / background B — confirm B's dot disappears on A within a few seconds (present-now only, no "recently seen").
    2. PRES-04 live activity: On B, switch tabs (LiveGizz / GizzVerse / GizzMap / GizzGames) — confirm A's row label for B updates to match. Start a tracked show on B (log an opener) — confirm A's label for B upgrades to `At a show 🎸`. Background B's tab — confirm A shows `idle`.
    3. PRES-02/05 waves: From A's Friends header palette, pick 👋 → Everyone — confirm B sees `A 👋` (broadcast). Pick 🔥 → B specifically — confirm B sees `A waved at you 🔥` with the "to you" mark, and that a wave A targets at a THIRD identity is NOT shown on B. Open FriendDetail(B) on A and use `Wave at {name}` — confirm the pre-targeted toast lands on B.
    4. D-10 burst: Send 4–5 waves quickly from A — confirm B shows distinct one-at-a-time pops (not one clobbered toast), and that the toast never blocks a tap on B (the live-log loop stays usable).
    5. D-16/17 offline: Put A in airplane mode — confirm A's friend dots + activity labels all hide and A's You row reads `offline`; restore connectivity — confirm presence returns and A re-appears online to B.
    6. PRES-03 ephemeral: Confirm nothing about presence/waves persisted — no missed-wave replay when B was offline, no last-seen timestamp.
    Report PASS, or a list of concrete gaps (which step, what went wrong).
  </how-to-verify>
  <verify>Human confirms all six checks on two devices; verdict recorded in the SUMMARY.</verify>
  <done>Owner has run all six checks across two devices and returned PASS or a concrete gap list.</done>
  <resume-signal>Type "approved" if all six checks pass, or describe the failing check (device, step, observed vs expected) so a `/gsd-plan-phase 20 --gaps` closure plan can be scoped.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| peer device → local device | Live cross-device presence/wave traffic over `gizz-room` — the on-device gate confirms the read-boundary validation (validateWave, targeting) holds against real peer traffic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-05 | Access Control (V4) | targeted wave delivery | mitigate | UAT step 3 explicitly confirms a wave targeted at a third identity is NOT shown on B (receiver enforces `to===null || to===me`). |
| T-20-11 | Spoofing / stale-green lie | offline dot-hide | mitigate | UAT step 5 confirms an offline device shows NO green dots (honest absence, D-16/17). |
| T-20-08 | Persistence leak (PRES-03) | ephemerality | mitigate | UAT step 6 confirms no missed-wave replay / last-seen persistence. |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies (RESEARCH audit = N/A). |
</threat_model>

<verification>
- Owner runs the six-step two-device script over the HTTPS tunnel; result recorded in `20-05-SUMMARY.md` / `20-HUMAN-UAT.md`.
- Full automated suite (`npm test -- --run`) is green before the device gate (regression backstop).
</verification>

<success_criteria>
- Two devices see each other's live presence dots + coarse activity (incl. `At a show 🎸`), send/receive targeted + broadcast waves correctly (third-party target excluded), burst as distinct pops, and honor the offline dot-hide / self-offline rules — with nothing persisted (PRES-01/02/03/05/07 confirmed live).
</success_criteria>

<output>
Create `.planning/phases/20-presence-interactions/20-05-SUMMARY.md` when done (record the device UAT result; if any check fails, note it for a `/gsd-plan-phase 20 --gaps` closure pass).
</output>
