---
phase: 04-show-mode
plan: 04
type: execute
wave: 3
depends_on: [04-03]
files_modified:
  - packages/app/src/show/useShowSession.ts
  - packages/app/src/show/ShowView.tsx
  - packages/app/src/show/PreShowLauncher.tsx
  - packages/app/src/App.tsx
  - packages/app/src/components/AppShell.tsx
  - packages/app/test/showSession.test.ts
autonomous: true
requirements: [SHOW-03, SHOW-11, DEX-01]
must_haves:
  truths:
    - "Tapping Start Show opens the orbit immediately with today's date auto-stamped and writes provisional attendance (DEX-01/D-01/D-02)"
    - "Tapping a predicted orb logs it as a hit, appends the old current to the trail, recenters, and recomputes predictions (SHOW-03)"
    - "Every confirmed song is write-through to IndexedDB; force-quit + relaunch auto-resumes the exact active show (SHOW-11/D-03)"
    - "ShowView branches pre-show launcher / active orbit / finalized read-only off the single active show"
    - "The orbit stage does not scroll or rubber-band inside AppShell (SHOW-13 layout seam resolved)"
  artifacts:
    - path: "packages/app/src/show/useShowSession.ts"
      provides: "reactive useLiveQuery hooks: active show, ordered entries, tally, current predictions"
      exports: ["useShowSession"]
    - path: "packages/app/src/show/ShowView.tsx"
      provides: "root #/show view with lifecycle branching + recenter orchestration"
      min_lines: 40
    - path: "packages/app/src/show/PreShowLauncher.tsx"
      provides: "Start Show CTA"
      contains: "startShow"
  key_links:
    - from: "packages/app/src/show/useShowSession.ts"
      to: "packages/app/src/db/db.ts"
      via: "useLiveQuery over trackedShows/trackedEntries"
      pattern: "useLiveQuery"
    - from: "packages/app/src/show/ShowView.tsx"
      to: "packages/app/src/show/OrbitStage.tsx"
      via: "onTapOrb → logSong(hit) → recenter"
      pattern: "logSong|onTapOrb"
    - from: "packages/app/src/App.tsx"
      to: "packages/app/src/show/ShowView.tsx"
      via: "route === 'show' renders ShowView"
      pattern: "ShowView"
---

<objective>
Close the slice-1 core loop end-to-end: the reactive `useShowSession` hooks (the app's first `useLiveQuery` usage), the `ShowView` lifecycle root, the `PreShowLauncher` (Start Show → provisional attendance), and the tap→log-hit→recenter→re-predict orchestration — wired into `App.tsx` at `#/show` with the AppShell no-scroll seam resolved. After this plan a user can Start Show, watch predictions, tap an orb to log a hit, and force-quit/relaunch straight back into the exact session.

Purpose: This is the MVP vertical slice the whole phase is organized around — the first point at which a real user can do something end-to-end (tap→log→persist→restore) using live predictions.
Output: the reactive session layer + the branching view + the closed recenter loop + App/AppShell wiring, with the restore integration test extended in showSession.test.ts.
</objective>

## Phase Goal (MVP user story)

**As a** KGLW showgoer, **I want to** start a show with one tap and log hits by tapping orbs, **so that** the app tracks the setlist live and restores exactly if my phone force-quits it.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-show-mode/04-RESEARCH.md
@.planning/phases/04-show-mode/04-PATTERNS.md
@.planning/phases/04-show-mode/04-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: useShowSession reactive hooks (first useLiveQuery in the app)</name>
  <files>packages/app/src/show/useShowSession.ts</files>
  <read_first>
    - .planning/phases/04-show-mode/04-RESEARCH.md §"Reactive restore + tally" code example + §Pattern 5 (useLiveQuery over active show + entries; DB is the single source of truth — do NOT hand-sync trail/tally React state)
    - .planning/phases/04-show-mode/04-PATTERNS.md "useShowSession.ts — NO ANALOG" (follow RESEARCH verbatim; useLiveQuery is unused in packages/app/src today)
    - packages/app/src/db/db.ts (04-01 helpers: getActiveShow query shape, logSong, TrackedEntry ordering by position)
    - packages/app/src/show/scoring.ts (deriveTally from 04-01)
    - packages/app/src/show/matrix.ts, showContext.ts (04-03: getMatrixIndex/loadMatrix, buildShowContext, predictFan)
    - packages/app/src/show/confidence.ts, orbitLayout.ts (04-03: selectFan/isWeakFan for the fan the hook exposes)
  </read_first>
  <action>
    Create `useShowSession()` using `useLiveQuery` from dexie-react-hooks: (a) `active` = `db.trackedShows.where("status").equals("active").first()`; (b) `entries` = ordered `db.trackedEntries.where("sessionId").equals(active.sessionId).sortBy("position")` keyed on `[active?.sessionId]`, defaulting to `[]`; (c) derived `tally` via `deriveTally(entries)`; (d) the current song id (last non-placeholder entry, or null pre-first-tap) and the current `PredictionCandidate[]` fan computed from `predictFan(matrix, buildShowContext(currentSongId, entries, []))` then `selectFan(...)` — recomputed only when `currentSongId`/entries change (not per render, RESEARCH Pitfall 6). Expose the raw candidates, the selected fan, `isWeakFan`, the shown-fan song-id list (for classifyOutcome/shownFanSongIds at log time), the active show, entries, and tally. Return a stable object. Do NOT hand-sync any trail/tally state — the DB is the single source of truth (write-through invariant, SHOW-11).
  </action>
  <verify>
    <automated>npx tsc -p packages/app --noEmit && npx vitest run packages/app</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `useShowSession.ts` uses `useLiveQuery` and holds NO `useState` mirror of entries/tally (single-source-of-truth).
    - Source assertion: predictions recompute on entry/current-song change, not unconditionally every render.
    - `npx tsc -p packages/app --noEmit` passes against the 04-01/04-03 exports.
  </acceptance_criteria>
  <done>A reactive hook exposes active show, ordered entries, derived tally, and the current predicted fan straight from Dexie + core, with no hand-synced state.</done>
</task>

<task type="auto">
  <name>Task 2: ShowView lifecycle + PreShowLauncher + closed tap→log→recenter loop + App/AppShell wiring</name>
  <files>packages/app/src/show/ShowView.tsx, packages/app/src/show/PreShowLauncher.tsx, packages/app/src/App.tsx, packages/app/src/components/AppShell.tsx, packages/app/test/showSession.test.ts</files>
  <read_first>
    - .planning/phases/04-show-mode/04-UI-SPEC.md §Layout & Interaction (region stack; "Tap orb → log hit + append old current to trail + recenter + recompute", no confirm) + §Component Inventory (ShowView/PreShowLauncher contracts) + §Copywriting (pre-show copy, accent Start Show CTA)
    - .planning/phases/04-show-mode/04-PATTERNS.md "ShowView.tsx" (App.tsx composition-root + PlaceholderView centered-state/config.copy analog; the AppShell scroll pitfall) and "PreShowLauncher.tsx" analog
    - .planning/phases/04-show-mode/04-RESEARCH.md §Pitfall 5 (AppShell `<main class="flex-1 overflow-y-auto pb-16">` vs the non-scrolling orbit stage — decide: parametrize AppShell or let ShowView own a full-height non-scroll layout)
    - packages/app/src/App.tsx (current render tree — PlaceholderView at every route; wire ShowView when route === "show")
    - packages/app/src/components/AppShell.tsx (the `<main>` scroll to reconcile)
    - packages/app/src/components/PlaceholderView.tsx (heading/body classes + config.copy pattern to reuse for pre-show + model-load-failure states)
    - packages/app/src/db/db.ts (startShow, logSong from 04-01) + packages/app/src/show/scoring.ts (classifyOutcome)
  </read_first>
  <action>
    Create `PreShowLauncher.tsx`: the "Ready when you are" heading + body (copy.show) + an accent-gold **Start Show** CTA that calls `startShow()` (auto-stamps today's date + writes the provisional-attendance trackedShows row per D-01/D-02/DEX-01). Reuse PlaceholderView's Heading/Body token classes. Create `ShowView.tsx`: consume `useShowSession`; branch — no active show → PreShowLauncher; active show → the orbit region stack (header date + tally slot, an OrbitStage mount, an ActionBar slot placeholder for 04-05/06, a CometTrail slot placeholder for 04-06); matrix load failure → the model-load-failure state (copy.show, ASVS V7, blocks only the orbit not nav); finalized → read-only orbit. Wire `OrbitStage.onTapOrb(candidate)`: compute outcome via `classifyOutcome(candidate.songId, shownFanSongIds)` (a tapped orb is by definition in the fan → "hit", D-06), `logSong(sessionId, { songId, songName, setNumber: active.currentSetNumber, outcome, shownFanSongIds, isPlaceholder:false, loggedAt })`, which — being a write-through — makes useLiveQuery recompute the new center + fan automatically (recenter + re-predict, SHOW-03). Wire `OrbitStage.onWhy` → open WhyDetail. Resolve the AppShell seam (RESEARCH Pitfall 5): give ShowView a full-height non-scrolling flex layout (stage is a flex child that does not overflow) — either add an AppShell prop to disable `<main>` scroll for `#/show` or have ShowView own its own full-height container; document the choice. Update `App.tsx` to render `ShowView` when `route === "show"` (keep PlaceholderView for explore/dex). Extend showSession.test.ts with the end-to-end restore assertion: seed an active show + entries, mount/re-query via the same DB path, confirm exact resume (SHOW-11) and that a tapped-orb entry is recorded as a hit.
  </action>
  <verify>
    <automated>npx vitest run packages/app -t "restore" && npx tsc -p packages/app --noEmit && npx vitest run packages/app</automated>
    <human-check>On device: Start Show → tap an orb → the tapped song becomes center and a fresh fan appears; force-quit the installed PWA and relaunch → the app resumes straight into the active show with the same trail (SHOW-11 real-device confirmation per VALIDATION).</human-check>
  </verify>
  <acceptance_criteria>
    - `npx vitest run packages/app -t "restore"` passes (exact resume; tapped-orb entry classified hit).
    - Source assertion: `App.tsx` renders `ShowView` for `route === "show"` (Placeholdershow branch removed).
    - Source assertion: the orbit stage is NOT inside a scrolling container — AppShell `<main>` scroll is disabled for `#/show` OR ShowView owns a full-height non-scroll layout (Pitfall 5 resolved, comment present).
    - Behavior assertion: `onTapOrb` calls `logSong` with `outcome:"hit"` and does not open WhyDetail (D-11 separation preserved).
    - `npx tsc -p packages/app --noEmit` passes.
  </acceptance_criteria>
  <done>The full tap→log-hit→recenter→re-predict→persist→restore loop works end-to-end at #/show; Start Show writes provisional attendance; AppShell scroll seam resolved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → IndexedDB | Local write-through of user-authored setlist; no network in Phase 4 |
| location.hash → view | Route selection (already allow-listed by useHashRoute) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Tampering | location.hash routing | mitigate | ShowView mounts only via the existing allow-listed `useHashRoute` (never assigned to innerHTML/eval) — inherited Phase 3 control (ASVS V5). |
| T-04-09 | Denial of Service (crash) | matrix load-failure branch | mitigate | ShowView renders a calm model-load-failure state instead of crashing the orbit; nav remains usable (ASVS V7). |
| T-04-10 | Repudiation / data loss | write-through timing | mitigate | `logSong` awaits the Dexie write before recenter; restore test proves exact resume (SHOW-11). |
</threat_model>

<verification>
- `npx vitest run packages/app` green incl. the extended restore test.
- `npx tsc -p packages/app --noEmit` passes.
- On-device SHOW-11 force-quit/restore confirmation (manual per VALIDATION).
</verification>

<success_criteria>
A user can Start Show (provisional attendance written), see live predictions, tap an orb to log a hit that recenters and re-predicts, and force-quit/relaunch into the exact session. The orbit stage does not scroll. The slice is genuinely end-to-end.
</success_criteria>

## Artifacts this phase produces (this plan)

- **Hook:** `useShowSession` (`show/useShowSession.ts`) — active show, entries, tally, current fan, shown-fan ids, isWeakFan
- **Components:** `ShowView` (root #/show, lifecycle branching + recenter orchestration), `PreShowLauncher` (Start Show)
- **Wiring:** `App.tsx` renders `ShowView` at `#/show`; `AppShell` no-scroll seam for the orbit stage resolved
- **Test:** extended `showSession.test.ts` restore/hit assertions

<output>
Create `.planning/phases/04-show-mode/04-04-SUMMARY.md` when done.
</output>
