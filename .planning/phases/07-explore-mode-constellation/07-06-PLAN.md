---
phase: 07-explore-mode-constellation
plan: 06
type: execute
wave: 5
depends_on: [07-05]
files_modified:
  - packages/app/src/explore/ConstellationCanvas.tsx
  - packages/app/src/explore/ExploreView.tsx
  - packages/app/src/explore/ExploreFilterPanel.tsx
  - packages/app/src/explore/RankedBar.tsx
autonomous: true
requirements: [DEX-05]
must_haves:
  truths:
    - "The constellation opens as YOUR sky (overlay ON by default): caught songs at full tuning color with a green sighting ring, unseen songs as dimmed 35% silhouettes (DEX-05/D-10)"
    - "Sighting-count numbers draw only past the count zoom threshold — no 264-badge soup at rest (D-11)"
    - "The overlay switch in the filter panel toggles to the neutral tuning-family view (dim = data, not dex) (D-10)"
    - "Ranked-bar rows show a green caught tick when the overlay is ON and the target song is caught (B2)"
    - "Marking a show in the Dex tab recolors the constellation live via useDexStats/useLiveQuery — no second derivation path"
  artifacts:
    - path: "packages/app/src/explore/ConstellationCanvas.tsx"
      provides: "dex-dim + sighting ring + zoom-gated count draw layers"
      contains: "DEX_DIM_OPACITY"
  key_links:
    - from: "packages/app/src/explore/ExploreView.tsx"
      to: "useDexStats"
      via: "reactive dex overlay source (sightings / neverSeen)"
      pattern: "useDexStats"
    - from: "packages/app/src/explore/ConstellationCanvas.tsx"
      to: "sighting ring / count draw"
      via: "nodeCanvasObject overlay layer"
      pattern: "#22C55E|sightings"
---

<objective>
Slice 4 — your sky. Overlay the personal dex on the constellation: caught songs stay at full tuning color and wear a green sighting ring (with the count number drawn only past the zoom threshold), unseen songs render as dimmed 35% silhouettes. The overlay is ON by default (the constellation opens as the Pokédex made spatial), and the filter panel's switch restores the neutral tuning-family view. Bar rows gain a caught tick. Everything reads live from `useDexStats` — marking a show in the Dex tab recolors the sky with zero new derivation.

Purpose: This closes DEX-05, the last requirement, and delivers the phase's emotional payoff. The two dimming systems (dex-dim 0.35 vs focus-dim 0.12) are deliberately far apart and combine by MINIMUM (not multiply) so state always reads unambiguously.
Output: The dex overlay (silhouettes + rings + zoom-gated counts), its toggle, and the bar caught-tick.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-explore-mode-constellation/07-RESEARCH.md
@.planning/phases/07-explore-mode-constellation/07-PATTERNS.md
@.planning/phases/07-explore-mode-constellation/07-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dex overlay draw layers on the canvas (silhouettes, rings, zoom-gated counts)</name>
  <files>packages/app/src/explore/ConstellationCanvas.tsx, packages/app/src/explore/ExploreView.tsx</files>
  <read_first>
    - packages/app/src/dex/useDexStats.ts lines 54-106 (reactive `{ready,error,dex,rarity,archive,albums}`; `dex.perSong.get(songId)?.sightings`, `neverSeen`; useLiveQuery reactivity — guard on `stats.ready`/`stats.error`)
    - packages/app/src/explore/ConstellationCanvas.tsx (from 07-05: nodeCanvasObject with fill + focus-dim + edge-threshold)
    - 07-UI-SPEC.md §B2 (sighting ring #22C55E 1.5px screen-space, count 11px past COUNT_ZOOM_THRESHOLD 2.5), §B4 (dex-dim DEX_DIM_OPACITY 0.35 grayscale silhouette; focus-dim stacks by MINIMUM not multiply; overlay OFF → full tuning color)
    - 07-PATTERNS.md lines 278-280 (reactive dex overlay: single derivation path via useDexStats)
    - 07-RESEARCH.md §"Dex Overlay Wiring" / DEX-05 row
  </read_first>
  <action>
    In `ExploreView.tsx`: add `overlay: boolean` state (default TRUE, D-10). Read `useDexStats()`; guard on `stats.ready`/`stats.error` (never block the constellation on a dex error — degrade to neutral view). Pass `overlay` + a `sightingsFor(songId): number` accessor (from `dex.perSong`) down to ConstellationCanvas and the panel/bars.
    In `ConstellationCanvas.tsx`: extend the `nodeCanvasObject` draw with the overlay layers, ordered fill → dex-dim → focus-dim → ring → count → label:
      • When `overlay` ON and the song is UNSEEN (sightings 0 / in neverSeen): draw the fill desaturated to its grayscale-luminance equivalent at `DEX_DIM_OPACITY` (0.35); no ring.
      • When `overlay` ON and CAUGHT: full tuning color + a `#22C55E` sighting ring (1.5px screen-space, xs offset outside the fill). Draw the sighting-count number (11px, tabular-metrics) in a small green-tinted pill above the node ONLY when `globalScale >= COUNT_ZOOM_THRESHOLD` (2.5) — no badge soup at rest (D-11).
      • Focus-dim (0.12) stacks on dex-dim by taking the MINIMUM opacity (an unseen non-neighbor → 0.12, not 0.35×0.12).
      • When `overlay` OFF: every node at full tuning color, no rings, no dex-dim (dim = data, not dex).
    Overlay toggling is a pure render-pass change — NO graphData rebuild, NO reheat. `useLiveQuery` inside useDexStats already re-renders on a Dex mark.
    Update the canvas `aria-label` to reflect the shown-song count.
  </action>
  <verify>
    <automated>pnpm --filter @guezzer/app exec tsc --noEmit 2>&1 | tail -8; pnpm -w test --run 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - Overlay defaults ON; caught nodes get a `#22C55E` ring, unseen nodes render at `DEX_DIM_OPACITY` (0.35) grayscale silhouette
    - Sighting-count numbers draw only when `globalScale >= COUNT_ZOOM_THRESHOLD`
    - Focus-dim + dex-dim combine by MINIMUM, not multiplication
    - Overlay OFF → all nodes full tuning color, no rings
    - Overlay data comes from `useDexStats` (single path); toggling overlay does not rebuild graphData or reheat
    - app typecheck + full suite green
  </acceptance_criteria>
  <done>The constellation opens as the user's dex sky; counts are zoom-gated; the two dims never confuse.</done>
</task>

<task type="auto">
  <name>Task 2: Overlay switch in the filter panel + caught tick on bar rows</name>
  <files>packages/app/src/explore/ExploreFilterPanel.tsx, packages/app/src/explore/RankedBar.tsx</files>
  <read_first>
    - packages/app/src/explore/ExploreFilterPanel.tsx (from 07-05: the reserved overlay-switch slot)
    - packages/app/src/explore/RankedBar.tsx (from 07-04: the caught-tick prop seam)
    - packages/app/src/dex/SongRow.tsx (caught green Check idiom + hollow unseen circle)
    - 07-UI-SPEC.md §B2 (caught tick on bar rows: green Check when overlay ON + caught; hollow #2A2A34 circle when unseen; overlay OFF hides both), §"Copywriting Contract" (overlay switch "My dex overlay")
    - 07-PATTERNS.md line 223 (RankedBar caught tick)
  </read_first>
  <action>
    In `ExploreFilterPanel.tsx`: fill the reserved slot with the dex-overlay switch — label `config.copy.explore` "My dex overlay", ON by default (D-10), ≥44px row, keyboard/AT operable, gold focus-visible ring. Wire it to ExploreView's `overlay` state. Toggling it OFF restores the neutral tuning view.
    In `RankedBar.tsx`: when `overlay` ON, lead the target song name with a small green `Check` (lucide, `#22C55E`) if the target is caught, or a hollow `#2A2A34` circle if unseen — same language as the dex SongRow. When overlay OFF, render neither (the panel mirrors the sky's current semantics). Use the caught state passed from ExploreView (via the `sightingsFor` accessor) — no new derivation.
  </action>
  <verify>
    <automated>pnpm --filter @guezzer/app exec tsc --noEmit 2>&1 | tail -8; pnpm -w test --run 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - The filter panel's third row is the "My dex overlay" switch, ON by default, wired to overlay state
    - Toggling it recolors the sky to neutral (OFF) / dex (ON)
    - Bar rows show a green `Check` when overlay ON + target caught, a hollow circle when unseen, nothing when overlay OFF
    - Caught state reuses `useDexStats` (no second derivation)
    - app typecheck + full suite green
  </acceptance_criteria>
  <done>The overlay switch and bar caught-tick complete the dex overlay; the panel mirrors the sky's semantics.</done>
</task>

</tasks>

<artifacts_produced>
- dex-overlay draw layers (silhouette/ring/count) + `overlay` state + `sightingsFor` accessor added to `ConstellationCanvas`/`ExploreView`
- "My dex overlay" switch in `ExploreFilterPanel`
- caught-tick on `RankedBar`
(All extend symbols created in 07-02/07-04/07-05; no brand-new files.)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dexie attendance (local) → useDexStats → canvas | user's own local dex data drives the overlay; read-only in this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | Information disclosure | dex overlay | accept | The overlay reads the user's own local dex (no accounts, no sharing surface here); nothing new is exposed and nothing is written. No high-severity threat. |
| T-07-09 | Tampering/DoS | useDexStats error path | mitigate | Guard on `stats.error`/`stats.ready` → degrade to the neutral tuning view; a dex-derivation error never blocks the constellation (inherited guard discipline) |

No writes (this phase writes nothing to Dexie), no network, no auth. ASVS L1 satisfied by inherited controls.
</threat_model>

<verification>
- `pnpm -w test --run` + `pnpm --filter @guezzer/app exec tsc --noEmit` green
- Manual (executor, dev server): Explore opens with caught nodes ringed + unseen dimmed; zoom in past threshold → counts appear; toggle overlay OFF → neutral tuning view; bars show caught ticks with overlay ON
</verification>

<success_criteria>
- DEX-05 delivered: dimmed silhouettes (unseen) + full color & sighting ring (caught) + zoom-gated counts, overlay ON by default
- Live recolor via useDexStats; two-dim contract holds; bar caught-tick mirrors the sky
</success_criteria>

<output>
Create `.planning/phases/07-explore-mode-constellation/07-06-SUMMARY.md` when done.
</output>
