---
quick_id: 260717-0s3
title: GizzDex Albums — always show the full grayed shelf; Mark-attended Shows-only
status: complete
date: 2026-07-17
commit: 48c45e1
---

# Summary — Quick Task 260717-0s3

The GizzDex Albums toggle now always renders the full album shelf (zero-catch
covers grayed via §B4) instead of an empty state, and the Mark-attended CTA is
Shows-only. Implements + resolves todo
`2026-07-17-gizzdex-albums-always-show-full-grayed-grid-mark-attended-on`.

## Changed

- **`packages/app/src/dex/DexView.tsx`**
  - Albums branch now always renders `<AlbumGrid dex albums onOpen />` (dropped
    the `emptyDex ? <EmptyState><MarkCta/></EmptyState> : <AlbumGrid/>` ternary).
  - Removed the orphaned `const emptyDex`, the local `EmptyState` component +
    `EmptyStateProps` interface, and the now-unused `type ReactNode` import.
  - `MarkCta` kept (Shows segment still uses it) → CTA is Shows-only.
- **`packages/app/test/dexView.test.tsx`** — replaced the "renders the empty
  state when nothing is caught" test with one asserting the new behavior: full
  shelf renders, all covers dimmed (`opacity-40`), no empty heading, no
  Mark-attended CTA on Albums.

## Verification

- `npx tsc -p packages/app/tsconfig.json --noEmit` → exit 0.
- `npx vitest run` → **481 passed / 66 files** (updated dex test included).
- Ran the app (dev server + Playwright, empty dex): GizzDex → Albums shows **31
  album cards, all 31 covers dimmed**, `HAS_MARK_CTA=false`, zero console errors.
  Header reads `0/264 · 0%`. Screenshot confirms the full grayed shelf.

## Notes

- `config.copy.dex.emptyHeading`/`emptyBody` are now unreferenced (harmless config
  keys) — left in place.
- The full-width Share CTA on the dex header is a SEPARATE open todo
  (`2026-07-17-gizzdex-header-share-icon-button-...`) — untouched here.

## Out of scope

Shows segment, DexHeader, tier colors. Routes/storage untouched.
