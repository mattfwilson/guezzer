---
phase: 19-shared-dex-progress
plan: 03
type: execute
wave: 3
depends_on: ["19-01", "19-02"]
files_modified:
  - packages/app/src/dex/FriendRow.tsx
  - packages/app/src/dex/SelfRow.tsx
  - packages/app/src/dex/RarestShowcase.tsx
  - packages/app/src/dex/FriendDetail.tsx
  - packages/app/src/dex/FriendsList.tsx
  - packages/app/src/dex/DexView.tsx
autonomous: true
requirements: [PROG-04, PROG-06, PROG-07, PROG-08]

must_haves:
  truths:
    - "The Friends surface is a THIRD segment inside DexView's existing Albums|Shows toggle → Albums|Shows|Friends — view-state within #/dex, never a new hash route, never a 6th bottom tab (D-01, hard constraint)"
    - "FriendsList renders a pinned 'You' SelfRow (sourced from live useDexStats, always current + offline-safe) above friend rows sorted completion% desc → caught → name, 0-catch last; empty state below the SelfRow (D-02/03/04/05, PROG-04)"
    - "Tapping a friend opens FriendDetail (full-screen Sheet overlay, view-state not a route) that reconstructs theirs via reconstructDexStats and feeds the UNCHANGED compareDexes — head-to-head columns first, then expandable per-album/per-rarity breakdown, then their rarest showcase (PROG-06/07, D-07/08/09)"
    - "packages/app/src/dex/CompareView.tsx is UNCHANGED — FriendDetail echoes its layout but never imports or edits it (D-09, hard constraint)"
    - "RarestShowcase shows top-N (config.friends.showcaseCount=5) rarest catches via core selectRarestCaught, reusing TierBadge + config.dex.tierColors; shared by FriendDetail (friend) and the 'You' trophy case (D-06/10, PROG-08)"
    - "Friend-supplied display_name and song names render as escaped React text, truncate/min-w-0 clamped, never dangerouslySetInnerHTML; all set arithmetic by songId only (D-19, hard constraint)"
    - "FriendRow leaves an empty leading slot (online dot) and trailing slot (coarse activity) so Phase 20 PRES-07 fuses presence in without a rebuild; no presence is implemented here (hard constraint)"
    - "Offline: friend rows render dimmed with an 'Offline · as of {time}' muted chip reusing SyncDot vocabulary; the SelfRow stays live; never a spinner, never blank (D-18)"
    - "Accent (#F2C14E) is used ONLY on the active Friends segment tab fill; rarity + identity palettes carry data meaning, never chrome (UI-SPEC §Color)"
  artifacts:
    - path: "packages/app/src/dex/FriendRow.tsx"
      provides: "Friend list row: identity glyph + name + {pct}% + {n} caught + single rarest TierBadge + Phase-20 slots"
      min_lines: 25
    - path: "packages/app/src/dex/SelfRow.tsx"
      provides: "Pinned 'You' row from live useDexStats; taps to own trophy case (no compare)"
      min_lines: 15
    - path: "packages/app/src/dex/RarestShowcase.tsx"
      provides: "Top-N rarest catches via selectRarestCaught + TierBadge; shared by FriendDetail and You trophy case"
      min_lines: 20
    - path: "packages/app/src/dex/FriendDetail.tsx"
      provides: "Full-screen overlay: reconstructDexStats → unchanged compareDexes → columns/breakdown/showcase"
      exports: ["FriendDetail"]
    - path: "packages/app/src/dex/FriendsList.tsx"
      provides: "Pinned SelfRow + sorted FriendRow[] + offline marker + empty state"
      exports: ["FriendsList"]
    - path: "packages/app/src/dex/DexView.tsx"
      provides: "Friends segment option + FriendDetail overlay view-state wiring"
      contains: "friends"
  key_links:
    - from: "packages/app/src/dex/FriendDetail.tsx"
      to: "@guezzer/core"
      via: "reconstructDexStats(summary, rarity) then compareDexes(mine, theirs) — unchanged core"
      pattern: "reconstructDexStats|compareDexes"
    - from: "packages/app/src/dex/DexView.tsx"
      to: "packages/app/src/dex/FriendsList.tsx"
      via: "segment === 'friends' branch renders FriendsList; row tap opens FriendDetail as useState overlay"
      pattern: "FriendsList|FriendDetail"
    - from: "packages/app/src/dex/FriendsList.tsx"
      to: "packages/app/src/sync/useFriendsProgress.ts"
      via: "reads {friends, offline, asOf} for the list; SelfRow reads useDexStats directly"
      pattern: "useFriendsProgress"
---

<objective>
Build the Friends UI inside GizzDex: a third `Friends` segment in `DexView`'s existing toggle (D-01), a `FriendsList` (pinned live "You" SelfRow + sorted FriendRow[] + offline marker + empty state), a `FriendDetail` full-screen overlay that reconstructs `theirs` and feeds the UNCHANGED `compareDexes` for the live head-to-head (PROG-06/07), and a shared `RarestShowcase` (top-5, PROG-08). Every component echoes a shipped analog without importing/altering it; `CompareView.tsx` stays untouched.

Purpose: This is the visible payoff of v2.0 — friends' real dex progress, visible and comparable live, replacing the manual JSON handoff. It composes the pure-core reconstruction (19-01) and the sync read path (19-02) into the shipped GizzDex design language.

Output: `FriendRow.tsx`, `SelfRow.tsx`, `RarestShowcase.tsx`, `FriendDetail.tsx`, `FriendsList.tsx`, and the `DexView.tsx` segment edit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-shared-dex-progress/19-CONTEXT.md
@.planning/phases/19-shared-dex-progress/19-PATTERNS.md
@.planning/phases/19-shared-dex-progress/19-UI-SPEC.md
@.planning/phases/19-shared-dex-progress/19-RESEARCH.md
</context>

<artifacts>
## Artifacts this plan produces

- `packages/app/src/dex/FriendRow.tsx` — identity glyph + name + `{pct}%` + `{n} caught` + single rarest `TierBadge`; empty leading (online dot) + trailing (activity) slots for Phase 20.
- `packages/app/src/dex/SelfRow.tsx` — pinned "You" row from live `useDexStats`; tap → own trophy case (no compare, D-06).
- `packages/app/src/dex/RarestShowcase.tsx` — top-N rarest via `selectRarestCaught` + `TierBadge`; shared by `FriendDetail` and the "You" trophy case.
- `packages/app/src/dex/FriendDetail.tsx` — full-screen `Sheet` overlay; `reconstructDexStats` → unchanged `compareDexes`; StatColumn + DiffSection + per-album/per-rarity breakdown + `RarestShowcase`.
- `packages/app/src/dex/FriendsList.tsx` — pinned SelfRow + sorted FriendRow[] + offline marker + empty state.
- `packages/app/src/dex/DexView.tsx` (EDIT) — `Friends` segment + `FriendDetail`/trophy-case overlay view-state.
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: FriendRow + SelfRow + RarestShowcase (leaf presentational components)</name>
  <files>packages/app/src/dex/FriendRow.tsx, packages/app/src/dex/SelfRow.tsx, packages/app/src/dex/RarestShowcase.tsx</files>
  <read_first>
    - `packages/app/src/dex/ShowsList.tsx:221–255` — the row `<button>` skeleton (`min-h-11 … px-4 py-3 text-left touch-manipulation`, `tabular-nums`, trailing `ChevronRight`) to echo for `FriendRow`.
    - `packages/app/src/auth/IdentityAvatar.tsx:31–74` — the deterministic identity glyph (`identityColorIndex(userId, config.auth.IDENTITY_COLORS.length)` → fill, initials in `#0C0C10`, `h-8 w-8`). Reuse this glyph pattern for FriendRow + SelfRow.
    - `packages/app/src/dex/TierBadge.tsx` + `packages/app/src/dex/rarityStyle.ts` — the six-tier badge + `rarityColor`/`RARITY_ORB_TEXT_COLOR` (`#0C0C10`) to reuse VERBATIM (do not recolor); tier WORD always renders (WCAG 1.4.1).
    - `packages/app/src/dex/DexHeader.tsx:30–68` — the rarest-subline pattern (resolve name from `archive.songs[String(id)]`, render name as React text + `<TierBadge>`) to extend into `RarestShowcase`.
    - `packages/app/src/dex/useDexStats.ts` + `packages/app/src/auth/useAuthIdentity.ts` — the live local dex + identity source for `SelfRow` (D-02).
    - `@guezzer/core` `selectRarestCaught` (19-01) + `config.friends.showcaseCount` / `config.copy.friends` (19-02).
    - `.planning/phases/19-shared-dex-progress/19-UI-SPEC.md` §Component Inventory + §Typography + §Color — the slot mandate, `tabular-nums`, escaped/clamped names, accent budget.
  </read_first>
  <action>
    Create `packages/app/src/dex/FriendRow.tsx`: a ≥44px (`min-h-11`) row `<button>` echoing the `ShowsList` row. Layout left→right: a LEADING SLOT (empty placeholder element reserved for the Phase-20 online dot — render nothing but keep the structural slot), the identity glyph (copy the `IdentityAvatar` fill/initials pattern via `identityColorIndex(userId, ...)`), a `min-w-0 flex-1` column with the display name (`truncate` escaped React text, D-19) and a caption line `{pct}% · {n} caught` (`tabular-nums`), a single rarest `<TierBadge tier={rarest.tier} />` (the D-04 row badge; omit when `rarest == null` for a 0-catch friend, D-05), a TRAILING SLOT (empty placeholder reserved for the Phase-20 coarse activity label), and a trailing `ChevronRight`. Props: `{ userId, displayName, pct, caught, rarest, dimmed?, onClick }`. When `dimmed` (offline), apply the muted/dimmed treatment (opacity) — never remove the row. Implement NO presence logic; the two slots stay empty.

    Create `packages/app/src/dex/SelfRow.tsx`: the pinned "You" row echoing `FriendRow`'s shape but sourced from LIVE `useDexStats()` + `useAuthIdentity()` (NOT the Supabase read — always current, offline-safe, D-02). Label `config.copy.friends.you` (`You`). Never dimmed by offline state. `onClick` opens the own trophy case (the caller renders `RarestShowcase` only, no compare columns — D-06). Derive `pct`/`caught`/`rarest` from the live `dex`.

    Create `packages/app/src/dex/RarestShowcase.tsx`: given `caughtSongIds`, the `rarity` index, and `archive`, call core `selectRarestCaught(caughtSongIds, rarity, config.friends.showcaseCount)` and render up to 5 rarest catches — each as the song name (escaped React text resolved via `archive.songs[String(songId)]`, songId-only identity, D-19) + `<TierBadge>`, reusing `rarityStyle`. Heading via `config.copy.friends` (`{name}'s rarest catches` / `Your rarest catches`), empty state `No catches yet`. Shared by `FriendDetail` (friend) and `SelfRow`'s trophy case (own); accept a `heading` + the data as props so it is context-agnostic.
  </action>
  <verify>
    <automated>npx tsc -p packages/app --noEmit && npx vitest run packages/app</automated>
  </verify>
  <acceptance_criteria>
    - `FriendRow.tsx`, `SelfRow.tsx`, `RarestShowcase.tsx` exist and typecheck.
    - `FriendRow` has a reserved empty leading slot and trailing slot (grep/comment marking them for Phase-20 presence); it implements NO presence/online logic.
    - Names render as plain `{displayName}` / `{songName}` React text with `truncate`/`min-w-0`; no `dangerouslySetInnerHTML` anywhere in the three files.
    - `RarestShowcase` calls core `selectRarestCaught` with `config.friends.showcaseCount` and reuses `TierBadge` (no bespoke tier colors).
    - `SelfRow` sources its numbers from `useDexStats()`/`useAuthIdentity()`, not from the sync read path.
    - `npx tsc -p packages/app --noEmit` clean; `npx vitest run packages/app` green.
  </acceptance_criteria>
  <done>The row and showcase primitives exist, reuse shipped identity/rarity idioms, carry the Phase-20 slots, and escape untrusted names; app typecheck + suite green.</done>
</task>

<task type="auto">
  <name>Task 2: FriendDetail full-screen overlay (reconstruct → unchanged compareDexes → columns/breakdown/showcase)</name>
  <files>packages/app/src/dex/FriendDetail.tsx</files>
  <read_first>
    - `packages/app/src/dex/CompareView.tsx` (READ IN FULL — the layout to ECHO, never import or edit, D-09): the `useMemo → compareDexes` pattern (46–52), `Sheet variant="fullscreen"` shell + safe-area header (99–107, 54–72), `StatColumn` (146–178), `DiffSection` collapse (180–224), `nameOf(songId)` name resolution from `archive.songs` (97). It reuses `config.copy.compare` column strings.
    - `@guezzer/core` `reconstructDexStats` + `compareDexes` + `type CompareResult`/`CompareColumn` (19-01 exports the first; the latter already exist).
    - `packages/app/src/dex/useDexStats.ts` — the signed-in user's live `dex` + `rarity` + `archive` (the `mine` side of the compare and the album/name resolution).
    - `packages/app/src/dex/RarestShowcase.tsx` (Task 1) — rendered last in the detail (D-08).
    - `packages/app/src/dex/DexView.tsx:43–61` — the `resolveOpenAlbum` idiom for per-album title resolution if album labels are needed in the breakdown.
    - `.planning/phases/19-shared-dex-progress/19-UI-SPEC.md` §Component Inventory + §Layering + §Copywriting — overlay ordering (head-to-head → breakdown → showcase), the `sheet` z-tier, `You vs {name}` heading, `By album`/`By rarity` headings, reuse of `config.copy.compare` columns.
  </read_first>
  <action>
    Create `packages/app/src/dex/FriendDetail.tsx` exporting `FriendDetail({ friend, onClose })` where `friend` is a `FriendRowData` (from 19-02). Echo `CompareView`'s layout in a NEW file — do NOT import or modify `CompareView.tsx`.

    Compute the compare with a `useMemo`: read `useDexStats()` for `stats.dex`/`stats.rarity`/`stats.archive`; when non-null, `const theirs = reconstructDexStats(friend.summary, stats.rarity)` then `compareDexes(stats.dex, theirs)` — the UNCHANGED core diff. Guard null (hold a calm frame) exactly like CompareView.

    Render inside `Sheet variant="fullscreen"` (z-tier `config.ui.z.sheet`) with the shipped safe-area header (`calc(env(safe-area-inset-top) + 12px)`) and a ≥44px back/close control (aria `config.copy.friends` `Back`; `X` or `ChevronLeft` — echo CompareView). Body order per D-08:
    1. Head-to-head heading `You vs {name}` (name escaped), then the two StatColumns (echo CompareView's `StatColumn` as a private local component) reading `compare.columns.mine`/`.theirs` and REUSING `config.copy.compare` strings (`columnYou`/`{name}`, `statCompletion`, `statCaught`, `statShows`) so live + file-import never disagree; `tabular-nums` on all figures.
    2. Expandable per-rarity breakdown (`By rarity`) and per-album breakdown (`By album`) reusing the shipped `CompareColumn`/`DiffSection` semantics (D-11), echoing CompareView's `DiffSection` collapse (`ChevronDown`/`ChevronRight`, `min-h-11` header). Diff lists render `nameOf(songId)` (escaped) + `<TierBadge>`, songId-only set arithmetic (D-19). The per-album breakdown reads the reconstructed `theirs.perAlbum` Map, resolving titles via the `resolveOpenAlbum` idiom.
    3. `<RarestShowcase>` for the friend's top-5 (heading `{name}'s rarest catches`).

    View-state only — `FriendDetail` is opened by `DexView` as a `useState` overlay (Task 3), NEVER a new hash route (D-07).
  </action>
  <verify>
    <automated>npx tsc -p packages/app --noEmit && git diff --exit-code packages/app/src/dex/CompareView.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `packages/app/src/dex/FriendDetail.tsx` exists, exports `FriendDetail`, and computes `compareDexes(stats.dex, reconstructDexStats(friend.summary, stats.rarity))`.
    - `FriendDetail` does NOT import `CompareView` (`grep -n "CompareView" packages/app/src/dex/FriendDetail.tsx` returns nothing); `packages/app/src/dex/CompareView.tsx` is byte-for-byte unchanged (`git diff --exit-code`).
    - The overlay leads with the head-to-head columns (reusing `config.copy.compare` strings), then per-album/per-rarity breakdown, then `RarestShowcase` (D-08 order).
    - Uses `Sheet variant="fullscreen"` at the shipped `config.ui.z.sheet` tier — no new z-index tier, no new hash route.
    - Friend name + song names are escaped React text; set arithmetic is songId-only.
    - `npx tsc -p packages/app --noEmit` clean.
  </acceptance_criteria>
  <done>FriendDetail renders the live head-to-head via the unchanged compareDexes over a reconstructed theirs, plus breakdown and showcase; CompareView stays untouched; app typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 3: FriendsList + DexView Friends segment wiring</name>
  <files>packages/app/src/dex/FriendsList.tsx, packages/app/src/dex/DexView.tsx</files>
  <read_first>
    - `packages/app/src/dex/DexView.tsx` (READ IN FULL): the `Segment` type (line 27), the `Albums|Shows` segment toggle (96–114), the `openAlbum`/`openShow` `useState` overlay pattern (68–70, 142–166) to mirror for the `FriendDetail`/trophy-case overlay — NEVER a new hash route (D-01/D-07).
    - `packages/app/src/dex/ShowsList.tsx:78–140,204–217` — the pure row-builder + sorted list + loading/empty-state shape to echo for `FriendsList`.
    - `packages/app/src/sync/useFriendsProgress.ts` + `buildFriendRows` (19-02) — the `{friends, offline, asOf, error}` source and the pure sort helper.
    - `packages/app/src/dex/FriendRow.tsx`, `SelfRow.tsx`, `RarestShowcase.tsx` (Task 1), `FriendDetail.tsx` (Task 2).
    - `packages/app/src/live/SyncDot.tsx` — the muted `#A1A1AA` hollow-ring connection vocabulary for the offline `Offline · as of {time}` chip (D-18) — reuse, do not invent a second indicator.
    - `.planning/phases/19-shared-dex-progress/19-UI-SPEC.md` §Component Inventory + §Interaction & States + §Copywriting — sort order, offline behavior, empty state, tap targets, accent budget.
  </read_first>
  <action>
    Create `packages/app/src/dex/FriendsList.tsx`: read `useFriendsProgress()`. Always render the pinned `<SelfRow>` (from live `useDexStats`, D-02) at the top. Below it, render `buildFriendRows(friends, myUserId)` as `<FriendRow>`s (sort completion desc → caught → name, 0-catch last, own excluded — D-03/05). When `offline`, render an `Offline · as of {time}` muted chip (echo `SyncDot` vocabulary; format `asOf`/`fetchedAt` via the app's existing time idiom) and pass `dimmed` to the friend rows; the SelfRow stays live (never dimmed). When `error` (online fetch failed), show the degraded-read copy `Can't reach friends right now — showing the last sync.`. When there are no friend rows at all, render the empty state (`No friends yet` + body) BELOW the SelfRow (echo `ShowsList` empty-state shape). Hold a calm frame (no spinner/NaN) during first pull. `FriendsList` accepts an `onOpenFriend(friend)` and `onOpenSelf()` callback so `DexView` owns the overlay state.

    Edit `packages/app/src/dex/DexView.tsx`: extend the `Segment` type to `"albums" | "shows" | "friends"` and add `"friends"` to the mapped segment array (label `config.copy.friends` segment label; active fill stays `bg-accent text-surface` — the single accent use this phase adds). Add a `segment === "friends"` branch rendering `<FriendsList onOpenFriend={...} onOpenSelf={...} />`. Add `useState` overlay state (mirroring `openAlbum`/`openShow`) for an open friend (`FriendRowData | null`) and an open-self trophy-case flag; render `<FriendDetail friend={...} onClose={...} />` when a friend is open, and a trophy-case overlay (a `Sheet` rendering `<RarestShowcase>` for the live own dex, NO compare columns — D-06) when self is open. These are component overlays within `#/dex` — do NOT add any route string.
  </action>
  <verify>
    <automated>npx tsc -p packages/app --noEmit && npx vitest run packages/app</automated>
  </verify>
  <acceptance_criteria>
    - `packages/app/src/dex/FriendsList.tsx` exists, renders a pinned `SelfRow` above `buildFriendRows`-ordered `FriendRow`s, an offline `Offline · as of {time}` chip with dimmed friend rows, a degraded-read message, and a `No friends yet` empty state below the SelfRow.
    - `DexView.tsx` `Segment` type includes `"friends"`; the segment array renders three tabs; a `segment === "friends"` branch renders `FriendsList`.
    - `FriendDetail` and the own trophy case open as `useState` overlays — `grep -nE "ROUTES|hash|route" packages/app/src/dex/DexView.tsx` shows no new route string added for Friends/FriendDetail.
    - The active Friends tab uses `bg-accent text-surface`; no other accent surface is introduced.
    - `npx tsc -p packages/app --noEmit` clean; `npx vitest run packages/app` green.
  </acceptance_criteria>
  <done>The Friends segment lists live friend progress with a pinned live You row, offline handling, and empty state; tapping a friend opens the live head-to-head and tapping You opens the trophy case — all view-state within #/dex; app typecheck + suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| synced friend data → React render | `display_name` and resolved song names (untrusted) are rendered in FriendRow/FriendDetail/RarestShowcase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-xss | Tampering (XSS, V5) | FriendRow / FriendDetail / RarestShowcase name rendering | mitigate | All friend-supplied strings render as escaped React text with `truncate`/`min-w-0`; never `dangerouslySetInnerHTML` (D-19 / T-06-26). Verified by the no-`dangerouslySetInnerHTML` grep gate. |
| T-19-dupname | Tampering (data integrity) | diff/set arithmetic | mitigate | All set math is songId-only (the matrix has duplicate names); names are resolved for display only via `archive.songs[String(id)]` (D-19). |
| T-19-privacy | Information Disclosure | full `caughtSongIds` shown in head-to-head diff lists | accept | Accepted by owner (D-14) — same exposure as the old manual JSON handoff; private ~5-friend tool. |
| T-19-SC | Supply chain (Tampering) | npm installs | accept | No new packages installed this plan; all UI primitives are shipped Guezzer components. slopcheck N/A. |
</threat_model>

<verification>
- `npx tsc -p packages/app --noEmit` — clean.
- `npx vitest run packages/app` + `npx vitest run packages/core` — full suites green.
- `git diff --exit-code packages/app/src/dex/CompareView.tsx packages/core/src/dex/compare.ts` — the shipped compare surfaces are untouched (hard constraint).
- Manual device/two-device verification of live propagation, reconnect, and the venue-offline view is deferred to 19-04.
</verification>

<success_criteria>
- The Friends segment lists each friend's headline progress with a pinned live "You" row (PROG-04).
- Tapping a friend shows the live head-to-head via reconstructed `DexStats` → unchanged `compareDexes`, with per-album/per-tier breakdown (PROG-06/07).
- Each friend's top-5 rarest catches are showcased with the shipped six-tier language (PROG-08).
- Friends is view-state within `#/dex`; `CompareView.tsx` and `compareDexes` are unchanged; FriendRow carries the Phase-20 presence slots.
</success_criteria>

<output>
Create `.planning/phases/19-shared-dex-progress/19-03-SUMMARY.md` when done.
</output>
