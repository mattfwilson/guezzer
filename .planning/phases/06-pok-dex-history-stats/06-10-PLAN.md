---
phase: 06-pok-dex-history-stats
plan: 10
type: execute
wave: 8
depends_on: ["06-06", "06-07", "06-09"]
files_modified:
  - packages/core/src/dex/compare.ts
  - packages/core/src/index.ts
  - packages/app/src/settings/importPicker.ts
  - packages/app/src/settings/SettingsView.tsx
  - packages/app/src/dex/CompareView.tsx
  - packages/app/src/dex/DexView.tsx
  - packages/app/src/config.ts
  - packages/core/test/dex/compare.test.ts
  - packages/app/test/importFork.test.ts
autonomous: true
requirements: [SHAR-01]
must_haves:
  truths:
    - "Importing a friend's backup opens a read-only compare view and writes NOTHING to the database — ever; importing my own file keeps the Phase-5 merge path verbatim (D-17)"
    - "A file with no owner name prompts 'Whose dex is this?' before anything else happens — never guess, never merge"
    - "The compare view shows You vs {name} stat columns and the two diff lists, with the friend's name escaped and length-clamped; no adopt/merge/copy affordance exists"
  artifacts:
    - path: "packages/core/src/dex/compare.ts"
      provides: "compareDexes(mine, theirs) pure diff — never merges"
      contains: "compareDexes"
    - path: "packages/app/src/dex/CompareView.tsx"
      provides: "Read-only You vs {name} columns + diff lists"
      contains: "deriveDex"
  key_links:
    - from: "packages/app/src/settings/importPicker.ts"
      to: "compare vs merge fork"
      via: "envelope.owner vs meta ownerName BEFORE parseAndMergeImport (Pattern 5)"
      pattern: "ownerName"
    - from: "packages/app/src/dex/CompareView.tsx"
      to: "@guezzer/core deriveDex"
      via: "run twice — local snapshot + friend envelope — zero DB writes"
      pattern: "deriveDex"
---

<objective>
The friend-compare half of the phase's social payoff (SHAR-01, D-17): the pure core `compareDexes` diff, the import fork that is structurally incapable of merging a friend's file (RESEARCH Pattern 5 — fork BEFORE `parseAndMergeImport`, so the compare path never reaches the two functions that write), and the read-only CompareView.

User story slice: *As a friend group hitting multiple nights, we want to swap dex files and see who caught what, so that the collection game is shared — without anyone's file ever inflating anyone else's counts.*

Purpose: D-17's guarantee is structural, not behavioral — the fork happens on `envelope.owner` vs the local meta `ownerName` (06-07's fork key) after zod validation and before any merge code is reachable. The share-card half of SHAR-02 follows in 06-11.
Output: one core module, the import fork, CompareView, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-pok-dex-history-stats/06-CONTEXT.md
@.planning/phases/06-pok-dex-history-stats/06-RESEARCH.md
@.planning/phases/06-pok-dex-history-stats/06-PATTERNS.md
@.planning/phases/06-pok-dex-history-stats/06-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Core compareDexes</name>
  <files>packages/core/src/dex/compare.ts, packages/core/src/index.ts, packages/core/test/dex/compare.test.ts</files>
  <read_first>
    - packages/core/src/dex/derive-dex.ts (DexStats/DexSnapshotInput — compare runs deriveDex twice per RESEARCH Pattern 1; envelope v2 is structurally assignable to DexSnapshotInput)
    - packages/core/src/data-safety/merge.ts (identity-key discipline: songId only, NEVER names — "Bit"×3/"Jam"×2/"Ghost"×2 duplicates verified)
    - packages/core/src/dex/rarity.ts (tier data for diff lists + tier breakdown)
    - 06-UI-SPEC.md §Layout 4 (compare columns: completion %, songs caught, shows, per-tier counts)
  </read_first>
  <behavior>
    - compareDexes(mine, theirs) — both DexStats — returns { columns: { mine, theirs } (completion, caught, shows, per-tier counts), onlyMine: songId[], onlyTheirs: songId[], shared: songId[] } with diffs keyed strictly by songId.
    - The function is pure and read-only: inputs are not mutated (deep-equal check in test).
  </behavior>
  <action>
    Create `packages/core/src/dex/compare.ts` exporting `CompareResult` and `compareDexes(mine: DexStats, theirs: DexStats): CompareResult` — set arithmetic over the perSong maps (caught = sightings > 0), tier counts via the stats' tier fields; sort diff lists by tier rarity (legendary first — trophy browsing) then songId (keep core name-free: return songIds; the view resolves names from archive.songs). Barrel-export. Tests: two-fixture compare (hand-computed diff lists + columns; input immutability via deep-equal before/after), empty-vs-populated compare.
  </action>
  <verify>
    <automated>npx vitest run --project @guezzer/core test/dex/compare.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - compare.test.ts pins onlyMine/onlyTheirs/shared for a hand-built pair and asserts inputs unmutated.
    - grep confirms compare.ts contains no DB/table/import from data-safety merge paths (pure diff only).
    - Full core suite green.
  </acceptance_criteria>
  <done>Comparing is pure core — the friend path gets full diff stats with zero writes by construction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Import fork + CompareView (D-17, Pattern 5)</name>
  <files>packages/app/src/settings/importPicker.ts, packages/app/src/settings/SettingsView.tsx, packages/app/src/dex/CompareView.tsx, packages/app/src/dex/DexView.tsx, packages/app/src/config.ts, packages/app/test/importFork.test.ts</files>
  <read_first>
    - packages/app/src/settings/importPicker.ts (pickAndImport lines ~36-58 — the commit seam; openBackupFilePicker reused as-is; the fork goes AFTER zod validation, BEFORE parseAndMergeImport/importSnapshot)
    - packages/app/src/settings/SettingsView.tsx (import-result rendering lines ~99-123 — where the fork's outcomes surface)
    - packages/core/src/data-safety/export-schema.ts (exportEnvelope.safeParse — the validation gate; owner field from 06-07)
    - packages/app/src/db/db.ts (getMeta ownerName)
    - 06-UI-SPEC.md §Layout 4 + §Copywriting (exact strings: "Viewing {name}'s dex — nothing was added to yours.", "You" / "{name}", "Only {name} has caught ({n})" / "Only you have caught ({n})", "Whose dex is this?", "This is {name}'s dex — opening compare view.")
  </read_first>
  <behavior>
    - Import fork: (a) file fails envelope validation → existing rejection path unchanged; (b) envelope.owner matches local meta ownerName (case-insensitive trim) or local ownerName unset AND user confirms it's theirs → existing Phase-5 merge path VERBATIM; (c) owner names someone else → the PARSED envelope routes to CompareView with zero DB calls; (d) owner is null → "Whose dex is this?" prompt; the answer routes to (b) or (c).
    - Fake-indexeddb test proves the friend path writes nothing: snapshot all tables before/after → deep-equal.
    - CompareView: persistent banner, You vs {name} columns (tabular-nums), two collapsible diff lists with TierBadges + names resolved from archive.songs, Close — and NO adopt/merge/copy control exists.
    - The friend's name renders escaped and visually clamped everywhere it appears.
  </behavior>
  <action>
    Refactor importPicker: extract a `classifyImport(rawJson, localOwnerName): { kind: "invalid"; error } | { kind: "mine"; rawJson } | { kind: "friend"; envelope: ExportEnvelope } | { kind: "unowned"; envelope }` step running `exportEnvelope.safeParse` first (validation before ANY routing); `pickAndImport` handles "mine" exactly as today (parseAndMergeImport + importSnapshot untouched), returns the friend/unowned outcomes to the caller — the compare path structurally never reaches the two writing functions (Pattern 5; envelope v1 files arriving here have no owner → "unowned"). In SettingsView: on "friend"/"unowned" outcomes show the fork copy ("This is {name}'s dex — opening compare view." / the "Whose dex is this?" prompt with a text input) and hand the envelope up to open CompareView (mount it from DexView or App-level state — component state, no new routes; pick the simplest host consistent with DexView's drill-in pattern). Create `CompareView.tsx`: derive `theirs = deriveDex(envelope, archive, albums, rarity)` (envelope v2 is a DexSnapshotInput superset) and `mine` from useDexStats; render banner (muted, always visible), stat columns, diff sections (collapsible, tier-sorted, names from archive.songs as React text), Close. Clamp the displayed name (CSS truncate + the schema's 40-char cap already bounds it). Add `config.copy.compare` block. Tests in importFork.test.ts: classifyImport all four kinds; the zero-writes deep-equal proof for the friend path; owner match case-insensitivity; v1-file → unowned prompt path.
  </action>
  <verify>
    <automated>npx vitest run --project @guezzer/app test/importFork.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - importFork.test.ts: all four classify kinds pinned; friend-path before/after table snapshots deep-equal (the D-17 proof); merge path still green for "mine".
    - grep confirms CompareView.tsx imports neither parseAndMergeImport nor importSnapshot nor db write helpers.
    - CompareView renders the banner string from config and has no button whose handler writes (source assertion).
    - Full app suite green.
  </acceptance_criteria>
  <done>A friend's file is a read-only trophy case: full stats, zero writes, structurally guaranteed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Friend-supplied backup file → app | Hostile JSON crosses into parse + display (never into the DB on the friend path) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-24 | Tampering (attendance inflation) | import fork | mitigate | Structural D-17 fork: classifyImport routes friend files before parseAndMergeImport/importSnapshot are reachable; zero-writes deep-equal test pins it |
| T-06-25 | Tampering (prototype pollution / oversized fields) | friend envelope | mitigate | exportEnvelope strictObject whole-file gate BEFORE routing; owner .max(40) clamp (06-07) |
| T-06-26 | Injection (XSS) | friend owner name + song names in CompareView | mitigate | React text only + CSS clamp; never dangerouslySetInnerHTML (inherited rule, grep-verifiable) |
| T-06-SC | Tampering (supply chain) | package installs | mitigate | No new dependencies in this plan |
</threat_model>

<verification>
- `npx vitest run` (both projects) green; `npx tsc --noEmit` green.
- Dev-server spot check: import a friend fixture file → compare view, local counts unchanged.
</verification>

<success_criteria>
- SHAR-01: friend exchange complete — v2 files fork to compare; own files merge; v1 files prompt.
- D-17: zero-DB-writes on the friend path proven by test, guaranteed by structure.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 10)

New symbols (exclude from source-drift verification — created here):
- `CompareResult`, `compareDexes` — `packages/core/src/dex/compare.ts`
- `classifyImport` — `packages/app/src/settings/importPicker.ts`
- Component `CompareView` — `packages/app/src/dex/CompareView.tsx`
- Config: `config.copy.compare.*` — `packages/app/src/config.ts`
- Barrel re-exports — `packages/core/src/index.ts`
- New tests: `compare.test.ts`, `importFork.test.ts`
</artifacts_produced>

<output>
Create `.planning/phases/06-pok-dex-history-stats/06-10-SUMMARY.md` when done
</output>
