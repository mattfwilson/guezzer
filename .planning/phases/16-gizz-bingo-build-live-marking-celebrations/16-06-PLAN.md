---
phase: 16-gizz-bingo-build-live-marking-celebrations
plan: 06
type: execute
wave: 3
depends_on: [16-01, 16-02, 16-03]
files_modified:
  - packages/core/src/dex/share-stats.ts
  - packages/app/src/dex/shareCard.ts
  - packages/app/src/dex/ShareCardSheet.tsx
  - packages/app/src/dex/RecapView.tsx
  - packages/app/src/games/GamesView.tsx
  - packages/app/test/dex/bingoShareCard.test.ts
autonomous: true
requirements: [BINGO-08]

must_haves:
  truths:
    - "buildBingoShareCard assembles a pure 'bingo'-scoped ShareCardData (4×4 board stamps + free center + win badges + show date + venue)"
    - "drawShareCard paints the bingo board + badges + date/venue on the same galaxy aesthetic + wordmark (trophy, not the per-square 'lit by' detail)"
    - "The bingo share is auto-offered in the RecapView bingo section at the win, and re-shareable from any past card in GizzGames replay history"
    - "The share File is pre-built on sheet-open so the share tap has no async before navigator.share"
  artifacts:
    - path: "packages/core/src/dex/share-stats.ts"
      provides: "buildBingoShareCard pure assembler → BingoShareCard"
      exports: ["buildBingoShareCard"]
    - path: "packages/app/src/dex/shareCard.ts"
      provides: "bingo scope branch in drawShareCard"
      contains: "scope === \"bingo\""
    - path: "packages/app/test/dex/bingoShareCard.test.ts"
      provides: "unit test for the pure bingo share-data assembly"
      contains: "buildBingoShareCard"
  key_links:
    - from: "packages/app/src/dex/RecapView.tsx"
      to: "packages/app/src/dex/ShareCardSheet.tsx"
      via: "bingo share trigger opens the sheet with bingo-scoped data"
      pattern: "ShareCardSheet"
    - from: "packages/app/src/dex/shareCard.ts"
      to: "packages/core/src/dex/share-stats.ts BingoShareCard"
      via: "drawShareCard reads the bingo scope"
      pattern: "scope === \"bingo\""
---

<objective>
Ship the shareable trophy (BINGO-08, D-22/D-23): add `buildBingoShareCard` (pure core assembler) that produces the `"bingo"`-scoped `ShareCardData` added in Plan 01, a bingo branch in `drawShareCard` that paints the final 4×4 board (stamped squares + free center) + win badges + show date + venue on the shipped galaxy canvas, and share triggers auto-offered in the RecapView bingo section (at the win) and re-shareable from GizzGames replay history. Reuses the existing share-card canvas, wordmark, and `ShareCardSheet` — a second render target, not a new pipeline.

Purpose: The reveal is a pillar — a clean visual brag image ("my card from KGLW at X"), NOT the dense per-square "which song lit it" (that stays the in-app replay payoff).
Output: `buildBingoShareCard`, the bingo `drawShareCard` branch, RecapView + GamesView share entry points, and a pure assembly test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-gizz-bingo-build-live-marking-celebrations/16-CONTEXT.md
@.planning/phases/16-gizz-bingo-build-live-marking-celebrations/16-RESEARCH.md
@.planning/phases/16-gizz-bingo-build-live-marking-celebrations/16-PATTERNS.md
@.planning/phases/16-gizz-bingo-build-live-marking-celebrations/16-UI-SPEC.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 06)
- **New core function:** `buildBingoShareCard(marked, wins, show)` → `BingoShareCard` (`packages/core/src/dex/share-stats.ts`) — pure assembler of the `"bingo"` scope shape defined in Plan 01.
- **Changed canvas:** `drawShareCard` gains a `scope === "bingo"` branch (4×4 board + win badges + date + venue on the galaxy aesthetic + wordmark).
- **Changed entry points:** `RecapView.tsx` (bingo share trigger, auto-offered at the win), `GamesView.tsx` (re-share from a past replay card), `ShareCardSheet.tsx` (accepts bingo-scoped data).
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: buildBingoShareCard pure assembler + unit test</name>
  <read_first>
    - packages/core/src/dex/share-stats.ts:50-90 — the `ShareCardData` union + the `BingoShareCard` member added in Plan 01 (`scope: "bingo"`, `squares[{label,marked,isFree}]`, `wins: BingoWinKind[]`, `show:{date,venue}`); mirror the `buildRecapShareStats`/`buildShareStats` pure-projection idiom.
    - packages/core/src/bingo/types.ts — `MarkedCard`/`MarkedSquare` (`markedByPosition !== null` = marked; `kind === "free"`), `Win`/`BingoWinKind`.
    - packages/app/src/games/bingoReplay.ts — the `ReplayResult` ({ marked, wins, songNameByPosition }) the app hands in.
  </read_first>
  <behavior>
    - `buildBingoShareCard(marked, wins, show)` → `BingoShareCard`: 16 `squares` row-major with `{ label: def.label or free label, marked: markedByPosition !== null, isFree: kind === "free" }`; `wins` = the deduped set of `BingoWinKind`s present (line/corners/x/blackout); `show` = `{ date, venue }`. NO "lit by" song detail (trophy only, D-22). A zero-win board yields a valid card (empty `wins`).
  </behavior>
  <action>
    Add `buildBingoShareCard(marked: MarkedCard, wins: Win[], show: { date: string; venue: string | null }): BingoShareCard` to share-stats.ts as a pure projection (no I/O). Derive the 16 canvas-ready squares from `marked.squares`, the badge set from `wins` (dedupe win kinds), and pass `show` through. Add `packages/app/test/dex/bingoShareCard.test.ts` (or a core test under packages/core/test/dex) asserting: square count 16, free center flagged, a marked square carries `marked:true` with no song detail, win-kind dedup, and a zero-win board is valid.
  </action>
  <verify>
    <automated>npm test -- --run packages/app/test/dex/bingoShareCard.test.ts</automated>
  </verify>
  <done>`buildBingoShareCard` exists, is pure, and its test is green (16 squares, free flagged, dedup wins, trophy-only).</done>
  <acceptance_criteria>
    - `grep -n "export function buildBingoShareCard" packages/core/src/dex/share-stats.ts` matches.
    - `npm test -- --run packages/app/test/dex/bingoShareCard.test.ts` exits 0.
    - The assembled shape carries NO per-square song/"lit by" field (trophy, not spreadsheet).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: drawShareCard bingo branch — paint board + badges + date + venue</name>
  <read_first>
    - packages/app/src/dex/shareCard.ts:24-32 (`COLOR`/`FONT_STACK`), :45-91 (`centerText`/`leftText`/`rightText`), :99-119 (the `data.scope` branch + wordmark paint), :218 (`buildShareCardFile`), :259 (`shareOrDownload`).
    - packages/core/src/dex/share-stats.ts — `BingoShareCard` fields to read.
    - .planning/phases/16-.../16-UI-SPEC.md §Color — data-semantic marked `#22C55E`/`#0C0C10`, unmarked `#17171F`/`#2A2A34`, wordmark `config.share.wordmarkGold`; win-badge/glyph always renders (not color alone).
    - packages/app/src/dex/covers.ts — cover art is NOT used on the share card (canvas trophy is board + badges + text only).
  </read_first>
  <action>
    In shareCard.ts add a `data.scope === "bingo"` branch to `drawShareCard` (or a sibling `drawBingoShareCard` invoked from it). Paint the same galaxy full-bleed background + wordmark, then a 4×4 grid of the `squares` using the data-semantic hexes (marked `#22C55E` fill + `#0C0C10` label; unmarked `#17171F` + `#2A2A34` border), the free center distinctly marked, a row of win badges (line/corners/X/blackout — glyph + word, never color alone), and the show date + venue via the existing text helpers. Names via canvas `fillText` (inert re XSS). Ensure `buildShareCardFile` handles the bingo scope so the File is pre-built (no async before `navigator.share`).
  </action>
  <verify>
    <automated>cd packages/app && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>drawShareCard paints the bingo trophy (board + badges + date + venue) on the galaxy aesthetic; app typechecks.</done>
  <acceptance_criteria>
    - `grep -n "scope === \"bingo\"\|drawBingoShareCard" packages/app/src/dex/shareCard.ts` matches.
    - `grep -n "#22C55E" packages/app/src/dex/shareCard.ts` shows the marked-square fill on the bingo branch.
    - `cd packages/app && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Share entry points — RecapView auto-offer + GamesView replay re-share</name>
  <read_first>
    - packages/app/src/dex/RecapView.tsx:29 (`ShareCardSheet` import), :56 (`shareOpen` state), :107-114 (`shareData` memo idiom), :120-142 (the `bingo` memo → marked/wins), and the bingo section (now rendering `<BingoBoard>` from Plan 02) where the share trigger attaches.
    - packages/app/src/dex/ShareCardSheet.tsx:39 — `ShareCardSheet({open, onClose, data})` (pre-builds the File on open); confirm it accepts the bingo-scoped `ShareCardData`.
    - packages/app/src/games/GamesView.tsx (Plan 03) — the replay list rows; add a per-row share affordance that opens ShareCardSheet with that card's bingo share data (re-derive via `replayCard` for the row).
    - config.copy.games.bingo (Plan 01) — the share-entry heading/label copy.
  </read_first>
  <action>
    In RecapView add a bingo `shareData` memo = `buildBingoShareCard(bingo.marked, bingo.wins, { date, venue })` and a share trigger inside the existing bingo section (auto-offered at the win — e.g. a share button that opens the already-present `ShareCardSheet` with the bingo-scoped data; reuse the `shareOpen` sheet or a second instance). Ensure `ShareCardSheet` accepts and forwards the bingo scope to `buildShareCardFile`. In GamesView add a re-share affordance on each replay-list row: on tap, `replayCard(row, entries, …)` → `buildBingoShareCard` → open ShareCardSheet with that card's data. Keep the File pre-built on sheet-open (Pitfall 7 — no async before `navigator.share`).
  </action>
  <verify>
    <automated>npm test -- --run packages/app/test/dex 2>/dev/null; cd packages/app && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>Bingo share is auto-offered in RecapView at the win and re-shareable from every GizzGames replay row; app typechecks and existing dex tests stay green.</done>
  <acceptance_criteria>
    - `grep -n "buildBingoShareCard\|ShareCardSheet" packages/app/src/dex/RecapView.tsx` matches.
    - `grep -n "buildBingoShareCard\|ShareCardSheet\|replayCard" packages/app/src/games/GamesView.tsx` matches (replay re-share).
    - `cd packages/app && npx tsc --noEmit` exits 0; `npm test -- --run` full suite green.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| kglw-derived song/venue names → canvas + share File | untrusted corpus strings painted onto the share image |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-12 | Tampering (XSS) | share-card labels/venue | mitigate | Canvas `fillText` is inert; React entry-point labels escaped; no `dangerouslySetInnerHTML` (T-06-21) |
| T-16-13 | Denial of service | same-tick object-URL revoke aborting the download | mitigate | Reuse the shipped `buildShareCardFile`/`triggerDownload` (deferred revoke, SAFE-02) — no new download path |
| T-16-SC | Tampering | package installs | accept | No installs this phase — not applicable |
</threat_model>

<verification>
- `cd packages/app && npx tsc --noEmit` clean.
- `npm test -- --run packages/app/test/dex/bingoShareCard.test.ts` green.
- `npm test -- --run` full suite green.
- Manual (device UAT, tracked separately): share from recap + GizzGames replay; confirm board + badges + venue/date render.
</verification>

<success_criteria>
- User can share a bingo result-card image from the recap and from replay history — BINGO-08.
- The trophy is the board + badges + date + venue on the shipped galaxy canvas (no "lit by" detail).
</success_criteria>

<output>
Create `.planning/phases/16-gizz-bingo-build-live-marking-celebrations/16-06-SUMMARY.md` when done.
</output>
