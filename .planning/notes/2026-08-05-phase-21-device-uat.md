---
date: "2026-08-05 00:30"
promoted: false
---

Phase 21 device UAT session #3 (2026-08-05, owner, installed standalone PWA on iPhone 16 Pro via cloudflared tunnel build e847183) — RESULTS FOR PLAN 21-13 TO RECORD. Context correction first: an earlier run this session was done in a SAFARI TAB (probe read sab:0, standalone nav=false mq=false, browser toolbar visible in layoutprobe.PNG) and ALL of its observations were DISCARDED, including an apparent "gap between BackupToast and the tab bar" which was an iOS Safari dynamic-viewport artifact (on scroll the toolbar collapses, layout viewport grows, fixed-bottom elements anchor below the visible glass). Owner then re-installed correctly and RE-RAN everything from the home-screen icon. PASSES (standalone, re-run): Test 2 overlay overlap FOUND-02 — ALL FIVE overlays confirmed clearing the tab-bar buttons with no internal dead space: InstallBanner, BingoCelebration, WaveToast (second device), BackupToast (End Show), UpdateToast (verified against a deliberately bumped dist/sw.js, three bumps were needed because each re-install re-baselined the cached worker). Test 3 NAV-01 dynamic type — PASS at MAXIMUM on the SIX-tab strip (Live/GizzVerse/Map/Sched/Me/Games); note the Sched tab landed in commit 2028a95 on 2026-07-30 AFTER session #2 passed this at five tabs, so this supersedes that reading. Test 4 SearchSheet with soft keyboard up FOUND-03/D-17 — PASS, no visualViewport fix needed, change nothing. Test 8 gesture suppression after portaling FOUND-03/D-23 — PASS on BOTH SearchSheet and FabMenu, upgrading the previous PARTIAL: double-tap does not zoom, long-press raises no iOS callout, no overscroll chaining, focus/Escape/restore intact. STILL OPEN: Test 6 NAV-03 mixed-build presence — NOT RUN, remains PENDING, needs the two-worktree/two-tunnel harness and a second device on the pre-rename build (pre-rename base is e92d4a8, i.e. 1cc5787^, NOT the SHA the harness section names). Test 7 layer paint order — still resolved by static analysis only, desktop ?layerRepro=1 repro never run. Test 1 independently corroborated this session: probe showed mainBottom 650 / tabTop 651 / GAP 1, the same 1px nav border calibration, so the stale "Gaps" block claiming FOUND-01 failed is superseded and must be reconciled. Evidence screenshots sitting untracked in repo root awaiting the move into the phase evidence/ dir: bottom_gap_recap.PNG, gap2.PNG (both Safari-context, keep as the discarded-context record), layoutprobe.PNG (the probe readout that settled it).
