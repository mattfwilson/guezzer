---
phase: 11-live-sync-prediction-correctness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/live/suggest.ts
  - packages/core/src/ingest/latest-types.ts
  - packages/core/src/live/poll-latest.ts
  - packages/core/src/index.ts
  - packages/core/test/suggest.test.ts
  - packages/core/test/latest-types.test.ts
  - packages/core/test/poll-latest.test.ts
autonomous: true
requirements: [LIVE-01, LIVE-02, LIVE-03]
must_haves:
  truths:
    - "A pure guardLatestRows filters latest rows to the tracked show (show_id when bound, else the show's own date) — a cached previous-night row is dropped"
    - "A row carrying an unknown additive API key still parses and stays usable; a wrong-typed CONSUMED field still skips that row"
    - "pollLatest returns a PollResult carrying a one-per-poll schemaDrift flag + novel key names, and preserves its never-throw soft-fail-to-empty contract"
    - "pollLatest surfaces only artist_id === 1 rows — a mixed-artist payload never leaks a foreign-band row downstream"
  artifacts:
    - path: "packages/core/src/live/suggest.ts"
      provides: "guardLatestRows + TonightGuardInput"
      contains: "export function guardLatestRows"
    - path: "packages/core/src/ingest/latest-types.ts"
      provides: "lenient latestSetlistRow + KNOWN_LATEST_KEYS + detectNovelKeys"
      contains: "detectNovelKeys"
    - path: "packages/core/src/live/poll-latest.ts"
      provides: "PollResult return + per-poll drift aggregation"
      contains: "PollResult"
    - path: "packages/core/src/index.ts"
      provides: "barrel exports for guardLatestRows, TonightGuardInput, PollResult, detectNovelKeys, KNOWN_LATEST_KEYS"
      contains: "guardLatestRows"
  key_links:
    - from: "packages/core/src/live/poll-latest.ts"
      to: "packages/core/src/ingest/latest-types.ts detectNovelKeys"
      via: "per-row detectNovelKeys collected into a Set, logged once after the loop"
      pattern: "detectNovelKeys\\("
    - from: "packages/core/src/live/poll-latest.ts"
      to: "artist_id !== 1 filter"
      via: "single ingress filter preserved as the sole artist-scope point"
      pattern: "artist_id"
---

<objective>
Fix the three live-path correctness bugs in pure core (CLAUDE.md strict separation — all decision logic stays DOM-free in `packages/core`; no app wiring here, that is plan 11-04):
- **LIVE-01:** add `guardLatestRows`, a pure filter that keeps only the tracked show's rows (identity `show_id` match when bound; the show's OWN date when unbound — never wall-clock, so a past-midnight set is not self-rejected).
- **LIVE-03:** loosen `latestSetlistRow` from `z.strictObject` to `z.object(...).catchall(z.unknown())` so an additive API key leaves the row usable, add a `KNOWN_LATEST_KEYS` set + a pure `detectNovelKeys` detector, and widen `pollLatest` to return a `PollResult { rows, schemaDrift, novelKeys }` that carries the drift signal without throwing.
- **LIVE-02:** VERIFIED already satisfied — `pollLatest`'s single-ingress `artist_id !== 1` filter is the sole artist-scope point. Add a mixed-artist regression test that locks the invariant; do NOT add a second downstream filter.

Purpose: On night 2+ of a residency a cached previous-night `latest` leaks yesterday's songs into suggestions/fill-hints (LIVE-01), one new kglw.net field silently empties suggestions (LIVE-03), and a foreign-band show (Stu Mackenzie solo set, SCHEMA §9) must never enter suggestions/auto-bind (LIVE-02).
Output: Three pure-core fixes + their regression tests + barrel exports, ready for app wiring in 11-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-live-sync-prediction-correctness/11-RESEARCH.md
@.planning/phases/11-live-sync-prediction-correctness/11-PATTERNS.md
@.planning/phases/11-live-sync-prediction-correctness/11-VALIDATION.md
@docs/SCHEMA.md
</context>

<artifacts>
This plan produces:
- `guardLatestRows(rows, guard)` + `TonightGuardInput` interface — `packages/core/src/live/suggest.ts`
- lenient `latestSetlistRow` (`z.object().catchall(z.unknown())`), `KNOWN_LATEST_KEYS: ReadonlySet<string>`, `detectNovelKeys(raw): string[]` — `packages/core/src/ingest/latest-types.ts`
- `PollResult { rows, schemaDrift, novelKeys? }` type + widened `pollLatest` — `packages/core/src/live/poll-latest.ts`
- barrel registrations for all of the above — `packages/core/src/index.ts`
</artifacts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: guardLatestRows tonight/show guard (LIVE-01)</name>
  <files>packages/core/src/live/suggest.ts, packages/core/test/suggest.test.ts, packages/core/src/index.ts</files>
  <read_first>
    - `packages/core/src/live/suggest.ts:1-28` (header + pure-decision idiom) and `:67`/`:105` (`diffLatestAgainstTrail`, `resolvePlaceholders` — the two consumers that currently apply NO guard).
    - `packages/core/src/live/bind-show.ts:21-49` — the EXACT analog: `TrackedShowInput` minimal-projection idiom + the existing `head.showdate !== todayIso` date-guard that `guardLatestRows` mirrors and strengthens (11-PATTERNS.md §"suggest.ts add guardLatestRows").
    - `.planning/phases/11-live-sync-prediction-correctness/11-RESEARCH.md` §"Pattern 1: Pure guard function applied once" + §"Pitfall 3" (never wall-clock date, D-10).
    - `packages/core/test/suggest.test.ts` (existing fixture idiom to extend) and `packages/core/test/bind-show.test.ts` (date-guard test shape).
  </read_first>
  <behavior>
    - Bound show (showId != null): rows with `show_id !== guard.showId` are dropped; matching rows kept. Per D-09.
    - Unbound show (showId == null): rows with `showdate !== guard.date` are dropped; rows with `showdate === guard.date` kept (the show's OWN stored date).
    - Past-midnight: a row whose `showdate` equals the show's date is retained even though wall-clock "today" has rolled over (D-10) — the guard never reads `todayIso()`.
    - Empty input → empty output; no throw.
  </behavior>
  <action>
    In `suggest.ts`, add an exported `TonightGuardInput { showId: number | null; date: string }` (re-declare locally; never import app types — mirrors `bind-show.ts` `TrackedShowInput`). Add `export function guardLatestRows(rows: LatestSetlistRow[], guard: TonightGuardInput): LatestSetlistRow[]` that returns `rows.filter(r => r.show_id === guard.showId)` when `guard.showId !== null`, else `rows.filter(r => r.showdate === guard.date)`. Do NOT sort. Do NOT read wall-clock/`todayIso`. Do NOT embed the guard inside `diffLatestAgainstTrail`/`resolvePlaceholders` — it is applied ONCE by the app in 11-04 (RESEARCH anti-pattern). Register `guardLatestRows` + `type TonightGuardInput` in `index.ts` beside the existing suggest exports (`:123-129`). Add `suggest.test.ts` cases: bound identity match, unbound date match, past-midnight retention, empty input.
  </action>
  <verify>
    <automated>npx vitest run packages/core/test/suggest.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/core/src/live/suggest.ts` contains `export function guardLatestRows` and `export interface TonightGuardInput`.
    - `packages/core/src/index.ts` exports `guardLatestRows` and `type TonightGuardInput`.
    - `npx vitest run packages/core/test/suggest.test.ts` exits 0 with new bound / unbound / past-midnight / empty cases green.
    - No wall-clock/`todayIso` reference exists in `guardLatestRows`.
  </acceptance_criteria>
  <done>guardLatestRows filters latest rows to the tracked show by show_id (bound) or the show's own date (unbound), covered by tests, exported from the barrel.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lenient-but-detecting latest schema + detectNovelKeys (LIVE-03 parse)</name>
  <files>packages/core/src/ingest/latest-types.ts, packages/core/test/latest-types.test.ts, packages/core/src/index.ts</files>
  <read_first>
    - `packages/core/src/ingest/latest-types.ts:14-23` (header explaining why keys are enumerated) + `:36-77` (`z.strictObject`, the 11 consumed typed validators, the present-key enumeration) — 11-PATTERNS.md §"latest-types.ts lenient-but-detecting schema".
    - `.planning/phases/11-live-sync-prediction-correctness/11-RESEARCH.md` §"Pattern 2: Lenient-but-detecting schema" + §"Security Domain V5" (consumed validators stay strict; D-07) + §"Anti-Patterns → .catchall without a key diff".
    - `docs/SCHEMA.md` §11 (the 5 keys `latest` omits) + §12 (`songname`/`venuename` untrusted editor content — detector returns key NAMES only, never values).
    - `packages/core/test/latest-types.test.ts` (existing schema test idiom).
  </read_first>
  <behavior>
    - A raw row with all consumed fields valid PLUS one unknown extra key parses successfully and retains the consumed fields (D-05/D-07).
    - `detectNovelKeys(raw)` returns the unknown key name(s) for that row; returns `[]` for a row with only known keys.
    - A row with a wrong-typed CONSUMED field (e.g. `song_id: "x"`) still fails `latestSetlistRow.safeParse` (D-07 unchanged).
    - `detectNovelKeys` returns key names only — never any editor-supplied value string.
  </behavior>
  <action>
    Change `latestSetlistRow` from `z.strictObject({...})` to `z.object({...}).catchall(z.unknown())`, keeping every one of the 11 consumed field validators (`z.number().int()`, `z.string().regex(...)`, etc.) EXACTLY as-is (D-07). Add `export const KNOWN_LATEST_KEYS: ReadonlySet<string> = new Set([...])` enumerating every present key the schema knows (the ~36 keys already listed in the file). Add `export function detectNovelKeys(raw: Record<string, unknown>): string[]` returning `Object.keys(raw).filter(k => !KNOWN_LATEST_KEYS.has(k))`. Update the file header comment (`:14-23`) to state the enumeration now feeds `KNOWN_LATEST_KEYS` for drift DETECTION rather than `strictObject` rejection. Register `KNOWN_LATEST_KEYS` + `detectNovelKeys` in `index.ts` beside `latestSetlistRow` (`:122`). Add `latest-types.test.ts` cases: additive-key parses+usable, wrong-typed consumed field still fails, `detectNovelKeys` returns the novel name and never a value.
  </action>
  <verify>
    <automated>npx vitest run packages/core/test/latest-types.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/core/src/ingest/latest-types.ts` uses `.catchall(z.unknown())` (no `z.strictObject`) and exports `KNOWN_LATEST_KEYS` + `detectNovelKeys`.
    - `packages/core/src/index.ts` exports `detectNovelKeys` and `KNOWN_LATEST_KEYS`.
    - `npx vitest run packages/core/test/latest-types.test.ts` exits 0: additive-key-usable, consumed-field-still-fatal, and names-only-detection cases green.
  </acceptance_criteria>
  <done>latestSetlistRow tolerates additive keys while consumed-field validation stays strict; detectNovelKeys surfaces novel key names only, exported from the barrel.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Widen pollLatest to PollResult + drift aggregation, lock LIVE-02 invariant</name>
  <files>packages/core/src/live/poll-latest.ts, packages/core/test/poll-latest.test.ts, packages/core/src/index.ts</files>
  <read_first>
    - `packages/core/src/live/poll-latest.ts:52` (return type), `:71-92` (per-row loop: `safeParse` skip, `artist_id !== 1` filter `:83`, outer never-throw `catch {}` `:88`) — 11-PATTERNS.md §"poll-latest.ts widen to PollResult".
    - `.planning/phases/11-live-sync-prediction-correctness/11-RESEARCH.md` §"Pattern 3: Result-object return" + §"Pitfall 2" (aggregate drift, log ONCE after the loop) + §"Pitfall 5" (breaking-signature call-site checklist) + §"Security Domain V7" (preserve never-throw soft-fail-to-empty).
    - `packages/core/test/poll-latest.test.ts` (existing array-return assertions to update; `makeRow` fixture idiom) — every assertion on the old `LatestSetlistRow[]` return must move to `result.rows`.
    - `docs/SCHEMA.md` §9 (multi-artist / Stu Mackenzie solo set → the mixed-artist regression fixture).
  </read_first>
  <behavior>
    - `pollLatest` resolves to `{ rows, schemaDrift, novelKeys? }`; `rows` is the artist-filtered validated array (unchanged content vs before).
    - A payload where one row carries a novel additive key → `schemaDrift === true` and `novelKeys` lists that key; drift is logged exactly once for the poll, not once per row.
    - A clean payload → `schemaDrift === false`, `rows` unchanged.
    - A payload mixing `artist_id: 1` and `artist_id: 4` (Stu Mackenzie) → `rows` contains only the `artist_id: 1` rows (LIVE-02 invariant).
    - Fetch failure / malformed body → returns `{ rows: [], schemaDrift: false }` (never throws — V7 soft-fail).
  </behavior>
  <action>
    Add `export interface PollResult { rows: LatestSetlistRow[]; schemaDrift: boolean; novelKeys?: string[] }`. Change `pollLatest`'s signature to `Promise<PollResult>`. Inside the existing loop, before/after `safeParse`, call `detectNovelKeys(raw)` and collect results into a `Set<string>`; KEEP the `artist_id !== 1 → continue` filter exactly where it is as the sole artist-scope point (do NOT add a second filter, RESEARCH anti-pattern). After the loop, emit a SINGLE `console.debug` with the collected key names only (never editor values) if the set is non-empty, and return `{ rows: validated, schemaDrift: novel.size > 0, novelKeys: novel.size > 0 ? [...novel] : undefined }`. Ensure the outer `catch {}` returns `{ rows: [], schemaDrift: false }` (never a throw/stack trace — V7). Register `type PollResult` in `index.ts` beside `pollLatest` (`:121`). Update `poll-latest.test.ts`: migrate existing assertions from the array to `result.rows`; ADD (a) an additive-key drift case asserting `schemaDrift`/`novelKeys` and one-log discipline, (b) a wrong-typed consumed-field row still skipped, (c) a mixed-artist regression case asserting only `artist_id: 1` rows survive (LIVE-02), (d) a fetch-failure case asserting the empty soft-fail result.
  </action>
  <verify>
    <automated>npx vitest run packages/core/test/poll-latest.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/core/src/live/poll-latest.ts` exports `PollResult` and `pollLatest` returns `Promise<PollResult>`; the `artist_id !== 1` filter remains the only artist-scope point.
    - Drift is logged at most once per poll (assert via a spied `console.debug` call count of 1 for a multi-novel-key payload).
    - `packages/core/src/index.ts` exports `type PollResult`.
    - `npx vitest run packages/core/test/poll-latest.test.ts` exits 0 with the drift, consumed-field-skip, mixed-artist (LIVE-02), and never-throw soft-fail cases green.
  </acceptance_criteria>
  <done>pollLatest returns PollResult carrying a once-per-poll drift flag, preserves the single-ingress artist filter and never-throw contract, with the mixed-artist invariant locked by a regression test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| kglw.net `/latest.json` → core (`pollLatest`) | Untrusted volunteer-run API; additive/malformed fields + foreign-band rows cross here. |
| Editor content (`songname`/`venuename`) → core | Untrusted editor-supplied strings, carried verbatim, never rendered/evaluated in core (SCHEMA §12). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02-01 | Spoofing/Tampering | Foreign-band show injection via `latest` (Stu Mackenzie solo, SCHEMA §9) | mitigate | Keep `artist_id !== 1` as the sole single-ingress filter; lock with the mixed-artist regression test (LIVE-02). |
| T-11-02-02 | Denial of Service | Malformed/additive field empties suggestions or throws mid-set | mitigate | `.catchall(z.unknown())` keeps rows usable; `pollLatest` never-throw soft-fails to `{ rows: [], schemaDrift: false }` (V7). |
| T-11-02-03 | Tampering (XSS) | Untrusted editor string surfaced via drift detail | mitigate | `detectNovelKeys` + drift log return key NAMES only, never editor values; core stays DOM-free (SCHEMA §12). |
| T-11-02-04 | Information disclosure | Stack trace leaked from poller | mitigate | Preserve outer `catch {}`; single `console.debug` with key names only, no stack (V7). |
| T-11-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs — edits to existing source + tests only (RESEARCH: no new dependencies). |
</threat_model>

<verification>
- `npx vitest run packages/core/test/suggest.test.ts packages/core/test/latest-types.test.ts packages/core/test/poll-latest.test.ts` — all green.
- Barrel: `packages/core/src/index.ts` exports `guardLatestRows`, `TonightGuardInput`, `detectNovelKeys`, `KNOWN_LATEST_KEYS`, `PollResult`.
- Note (wave merge): the `pollLatest` signature change ripples into app callers/tests (`useLatestPoll.ts`, `mockLatest.ts`, app tests) — those are updated in plan 11-04. A full `npm test` is NOT green until 11-04 lands; this plan's gate is the three core test files above.
</verification>

<success_criteria>
- LIVE-01: guardLatestRows drops previous-show rows by show_id (bound) or the show's own date (unbound), past-midnight-safe.
- LIVE-02: pollLatest's single-ingress artist filter is locked by a mixed-artist regression test; no second filter added.
- LIVE-03 (core half): additive keys tolerated, consumed-field breakage still fatal-per-row, drift surfaced once via PollResult without breaking the never-throw contract.
</success_criteria>

<output>
Create `.planning/phases/11-live-sync-prediction-correctness/11-02-SUMMARY.md` when done. Record the `PollResult` shape and the list of app-tier call sites (11-04) that must consume it.
</output>
