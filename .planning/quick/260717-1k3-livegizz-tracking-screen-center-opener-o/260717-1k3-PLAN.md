---
quick_id: 260717-1k3
title: LiveGizz tracking screen — 4 tweaks (center orb, search-to-seed, kill strip bar, End Show in FAB)
created: 2026-07-17
mode: quick
---

# Quick Task 260717-1k3

Implements + resolves todo
`2026-07-17-livegizz-tracking-screen-opener-orb-search-center-strip-ends`.

Shared idea: `openerSeeded = session.currentSongId !== null`. The SuggestionStrip
slot and the FAB offset reserve the strip's 56px ONLY once the opener is seeded
(a fan exists to protect, SHOW-02). Pre-opener there's no fan, so the strip
collapses to 0 → the opener orb centers and there's no blank bar.

## Task 1 — Rename + tappable opener; center follows from Task 3

- `config.ts:333`: `centerPrompt: "Tap the opener"` → `"Search for the opener"`.
- `CenterNode.tsx`: in the `songName == null` branch, render a `<button>` (keep
  the dashed pill look, `min-h-11`, add `aria-label`) that calls a new
  `onOpenSearch` prop. Seeded branch unchanged (presentational).
- `OrbitStage.tsx`: accept `onOpenSearch` and pass it to `<CenterNode>`.
- `ShowView.tsx`: pass `onOpenSearch={() => setSearchOpen(true)}` to `<OrbitStage>`
  (same SearchSheet path the FAB uses; selecting seeds the opener, SHOW-04).

## Task 2 — Kill the blank strip bar (state-aware) + center the orb

- `SuggestionStrip.tsx`: new `reserveSpace: boolean` prop. Height =
  `(reserveSpace || hasContent) ? SUGGESTION_STRIP_HEIGHT : 0`. Visible chrome
  (`border-t border-hairline bg-elevated`) only when `hasContent` — so an empty
  slot is never a visible gray bar, and pre-opener it takes zero height.
- `ShowView.tsx`: pass `reserveSpace={openerSeeded}`.
- Result: pre-opener → strip 0 → orbit stage fills → CenterNode (already
  `absolute inset-0 flex items-center justify-center` in OrbitStage) centers
  vertically + horizontally (Task-1 goal). Mid-show → 56px reserved (transparent
  when empty) so the fan never re-lays-out when a suggestion toggles (SHOW-02).

## Task 3 — FAB offset follows the strip reservation

- `FabMenu.tsx`: new `stripReserved: boolean` prop; bottom offset adds
  `SUGGESTION_STRIP_HEIGHT` only when `stripReserved` (else just tab bar + gap),
  so pre-opener the FAB drops to just above the tab bar (no floating gap).
- `ShowView.tsx`: pass `stripReserved={openerSeeded}`.

## Task 4 — End Show as the LAST FAB item (functionality identical)

- `FabMenu.tsx`: new `onEndShow` prop; append `{ key:"endShow", label:copy.endCta,
  Icon:CircleStop, onClick:onEndShow }` as the FINAL entry in `actions` (renders
  bottom-most). Import `CircleStop`.
- `ShowView.tsx`: remove the header End Show button (`:335-342`); header right =
  `SyncDot` + `TallyReadout`. Remove now-unused `CircleStop` import from ShowView.
  Wire `onEndShow={() => setEndOpen(true)}` into `<FabMenu>`. `endOpen` +
  `EndShowDialog` + `endShow(sessionId)` unchanged.

## Verify

- `tsc` clean; `vitest run` green (update suggestionStrip/fabMenu/showView tests
  that assert the header End Show button, old prompt copy, or always-56px strip).
- Run the app: pre-opener orb centered + "Search for the opener" opens Search; no
  blank bar; End Show is the last FAB row and still confirms; mid-show a suggestion
  appearing does NOT shift the orbit. Screenshot.

## Out of scope

Tier colors, dates, GizzVerse. Routes/storage untouched.
