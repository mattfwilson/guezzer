---
phase: 14-gizz-bingo-core-marking-generation
plan: 06
type: execute
wave: 5
depends_on: [14-05]
files_modified:
  - packages/core/src/config.ts
  - packages/core/test/config.test.ts
  - data/bingo-roster-candidates.md
  - data/bingo-calibration-report.md
  - data/bingo-calibration.json
autonomous: false
requirements: [BINGO-03]
must_haves:
  truths:
    - "Jam-vehicle + album rosters are surfaced for human sign-off BEFORE any constant is written to config.ts (D-20)"
    - "After the user approves the rosters and the Monte-Carlo lands within the D-02/D-03/D-05 bands, the locked constants (rosters, freeIndex, bustOutGapShows, per-vibe mix weights) are written to config.bingo"
    - "The calibration gate exits 0 with the locked constants — GATE 2 cleared"
  artifacts:
    - path: "data/bingo-roster-candidates.md"
      provides: "corpus-measured jam-vehicle + album candidate rosters for user review (D-16/D-20)"
    - path: "packages/core/src/config.ts"
      provides: "locked config.bingo constants (rosters no longer empty, mix weights filled)"
    - path: "data/bingo-calibration-report.md"
      provides: "the per-vibe human trust-gate report (D-19a)"
  key_links:
    - from: "human checkpoint"
      to: "config.ts constant lock"
      via: "constants are written ONLY after roster sign-off (D-20 — never auto-write in one pass)"
      pattern: "jamVehicleSongIds|albumSquarePool"
    - from: "node cli/bingo-calibrate.ts"
      to: "exit 0"
      via: "gate green with locked constants (GATE 2)"
      pattern: "process.exit"
---

<objective>
Execute the D-20 calibration process and clear GATE 2. Sequence (MANDATORY, D-20 — never auto-write config
in one pass): (1) generate corpus-measured candidate rosters and emit them to a review file; (2) a HUMAN
REVIEW CHECKPOINT where the user reviews/edits the jam-vehicle (~20 songs, ~95% fire) + album pool (D-17)
BEFORE any constant is written; (3) with the approved rosters, iterate the Monte-Carlo mix weights until the
per-vibe P(line)/P(blackout)/dark-square-floor invariants land in-band, then lock the final constants to
`config.bingo` and confirm the gate exits 0.

Purpose: This is the Bingo equivalent of the Phase-2 backtest trust gate — the user signs off the rosters
(corpus proposes, user disposes) and the numbers before cards are relied on live. It is the phase's final gate.
Output: the roster-candidates review file, the calibration report/json, and the locked config.bingo constants.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/14-gizz-bingo-core-marking-generation/14-CONTEXT.md
@.planning/phases/14-gizz-bingo-core-marking-generation/14-RESEARCH.md
@.planning/phases/14-gizz-bingo-core-marking-generation/14-VALIDATION.md
@packages/core/src/config.ts
@packages/core/src/cli/bingo-calibrate.ts
</context>

## Artifacts this plan produces

- **Roster candidates** (`data/bingo-roster-candidates.md`): corpus-measured jam-vehicle songId list + album fire-rate pool for user review.
- **Calibration outputs** (`data/bingo-calibration-report.md`, `data/bingo-calibration.json`): the per-vibe trust-gate report (P(line)/P(blackout)/dark-share/expected-marks) + machine-readable results.
- **Locked constants** (`packages/core/src/config.ts` `bingo` section): `jamVehicleSongIds`, `albumSquarePool`, final `freeIndex`, re-confirmed `bustOutGapShows`, and per-vibe `vibes.*.mix` weights — no longer [ASSUMED].
- **Updated `config.test.ts`**: assertions upgraded to require non-empty rosters + present per-vibe mix weights (the lock is pinned by test).

<tasks>

<task type="auto">
  <name>Task 1: Generate corpus-measured roster candidates (--candidates)</name>
  <read_first>
    - packages/core/src/cli/bingo-calibrate.ts (the `--candidates` mode + `buildRosterCandidates`)
    - 14-CONTEXT.md D-16 (jam-vehicle ~20 songs, name-substring hand-tag), D-17 (album pool: ≥53% set + lower-fire albums clearing ≥20% floor)
    - 14-RESEARCH.md §"D-20 process" (lines 388-391); Discretion note on bustOutGapShows re-confirmation (lines 60, 496-497)
    - .planning/notes/gizz-bingo-design-vetting.md (empirical fire-rate source of truth)
  </read_first>
  <action>
    Run `node packages/core/src/cli/bingo-calibrate.ts --candidates`. Confirm it writes
    `data/bingo-roster-candidates.md` containing: the jam-vehicle candidate songId list (seeded from a
    name-substring roster, resolved to songIds, ranked by measured recent-era corpus fire-rate) and the
    album-square candidate pool (each album with its measured fire-rate, flagging the ≥53% set and the
    lower-fire albums that still clear the ≥20% floor). Also surface the corpus-measured `bustOutGapShows`
    re-confirmation (does the vetting's ≥50-show gap still land near ~21% fire?). Do NOT write anything to
    config.ts in this task. Verify the review file is non-empty and lists candidate songIds + album fire-rates.
  </action>
  <verify>
    <automated>node packages/core/src/cli/bingo-calibrate.ts --candidates && test -s data/bingo-roster-candidates.md && echo CANDIDATES_OK</automated>
  </verify>
  <acceptance_criteria>
    - `data/bingo-roster-candidates.md` exists and is non-empty.
    - It lists jam-vehicle candidate songIds ranked by measured fire-rate and an album pool with per-album fire-rates.
    - `config.bingo.jamVehicleSongIds` / `albumSquarePool` are STILL empty at the end of this task (no config write yet — D-20).
  </acceptance_criteria>
  <done>Corpus-measured roster candidates are emitted to a review file; config.ts is untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: HUMAN REVIEW CHECKPOINT — roster sign-off before any constant is locked (D-20)</name>
  <read_first>
    - data/bingo-roster-candidates.md (the candidate rosters emitted by Task 1)
    - 14-CONTEXT.md D-16/D-20 (corpus proposes, user disposes; review checkpoint BEFORE config write)
  </read_first>
  <action>
    PAUSE for the user. Present `data/bingo-roster-candidates.md` and request sign-off on the jam-vehicle roster,
    the album-square pool, `bustOutGapShows`, and `freeIndex` BEFORE any constant is written to config.ts (D-20 —
    never auto-write in one pass). Do NOT proceed to Task 3 until the user replies with approval or edits. Record
    the approved values verbatim to hand to Task 3. This task writes NOTHING to config.ts.
  </action>
  <what-built>
    A corpus-measured candidate roster file (`data/bingo-roster-candidates.md`) proposing the jam-vehicle
    songId list (~20 songs, ~95% fire) and the album-square pool (D-17), plus a bustOutGapShows re-confirmation.
    NO constant has been written to config.ts yet — this checkpoint gates the write (D-16/D-20: corpus proposes, user disposes).
  </what-built>
  <how-to-verify>
    1. Open `data/bingo-roster-candidates.md`.
    2. Review the jam-vehicle candidate list — add/remove songs so it reflects the true long-jam vehicles (~20 songs). The corpus can only measure name-substrings and fire-rates; your ear is the authority.
    3. Review the album-square pool — confirm the ≥53% set and decide which lower-fire albums (still ≥20% floor) to include as variety.
    4. Confirm or adjust `bustOutGapShows` (starting ~50-show gap ≈ 21% fire) and pick `freeIndex` ∈ {5,6,9,10}.
    5. Reply with the approved rosters (or "candidates as-is") and any edits. These exact values will be written to config.bingo in Task 3.
  </how-to-verify>
  <acceptance_criteria>
    - The user has replied with approval ("candidates as-is") or explicit edits to the jam-vehicle list, album pool, bustOutGapShows, and freeIndex.
    - No value has been written to config.bingo before this sign-off (D-20 enforced).
  </acceptance_criteria>
  <resume-signal>Reply "approved" (candidates as-is) or provide the edited jam-vehicle list, album pool, bustOutGapShows, and freeIndex.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Lock constants to config.bingo + clear the gate (GATE 2)</name>
  <read_first>
    - packages/core/src/config.ts (the `bingo` section from Plan 01 — the [ASSUMED] slots to fill)
    - packages/core/src/cli/bingo-calibrate.ts (the gate: report + assertCalibrationInvariants + exit code)
    - packages/core/test/config.test.ts (upgrade the roster/mix assertions)
    - 14-RESEARCH.md §"Config surface" (per-vibe mix shape) + gate wiring (lines 381-386); §"Tuning: manual iterate-and-rerun" (line 379 — no automated optimizer)
    - 14-CONTEXT.md D-02/D-03/D-05/D-18 (target bands, floor, calibration owns album-square count)
  </read_first>
  <action>
    Write the user-approved rosters into `config.bingo`: `jamVehicleSongIds` (the approved songId list),
    `albumSquarePool` (the approved album_urls), the confirmed `bustOutGapShows`, and the chosen `freeIndex`.
    Then iterate calibration MANUALLY (RESEARCH line 379 — no automated optimizer): run
    `node packages/core/src/cli/bingo-calibrate.ts`, read the per-vibe report, adjust the per-vibe
    `vibes.{chill,balanced,glory}.mix` weights (event-type counts + album-square count + song-square count,
    D-18) and rerun until the gate is green — P(line) within chill 0.82 / balanced 0.70 / glory 0.50 ± tol
    (D-02), P(blackout) within balanced 0.02–0.05 / glory 0.05–0.10 and chill under its cap (D-03), every
    RELIABLE square ≥ 0.20 dark-floor (D-05; bustOut+neverCaught EXEMPT, D-15). Gate on the mid-collection
    dex assumption; the empty-dex run is reported (not gated). Once green, the CLI writes
    `data/bingo-calibration-report.md` + `data/bingo-calibration.json`. Update every filled value's JSDoc
    comment from `[ASSUMED]` to `[VERIFIED: bingo-calibrate gate <date>]`. Upgrade
    `packages/core/test/config.test.ts` to assert `jamVehicleSongIds.length > 0`, `albumSquarePool.length > 0`,
    and that each per-vibe `mix` has weights present. Commit the locked config + calibration outputs.
  </action>
  <verify>
    <automated>node packages/core/src/cli/bingo-calibrate.ts && npx vitest run --project @guezzer/core config bingo</automated>
  </verify>
  <acceptance_criteria>
    - `node packages/core/src/cli/bingo-calibrate.ts` exits 0 (GATE 2 green with the locked constants).
    - `config.bingo.jamVehicleSongIds` and `config.bingo.albumSquarePool` are non-empty; per-vibe `mix` weights are present.
    - `data/bingo-calibration-report.md` shows per-vibe P(line)/P(blackout)/dark-share/expected-marks.
    - `config.test.ts` asserts non-empty rosters + present mix weights and passes.
    - Full suite: `npx vitest run` exits 0.
  </acceptance_criteria>
  <done>The user-approved rosters + calibrated mix weights are locked in config.bingo, the gate exits 0, and the constant lock is pinned by config.test.ts — GATE 2 cleared.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human review → config lock | The roster sign-off is the trust gate; constants must not be written before it (D-20) |
| calibration gate → shippable config | A green gate is the precondition for relying on cards live |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-15 | Tampering | Constants auto-written without human roster sign-off | mitigate | D-20 sequence enforced by a blocking human checkpoint (Task 2) BEFORE the Task-3 config write — never auto-write in one pass |
| T-14-16 | Tampering | Un-calibrated config silently shipped | mitigate | The Task-3 verify requires `bingo-calibrate.ts` to exit 0 (gate green) before the lock is accepted (D-19b) |
| T-14-17 | Repudiation | Which corpus produced the lock is untraceable | mitigate | `data/bingo-calibration.json` records `generatedAt` from `corpus.generatedAt`; JSDoc stamped `[VERIFIED: gate <date>]` |
</threat_model>

<verification>
- `node packages/core/src/cli/bingo-calibrate.ts` exits 0.
- `npx vitest run` (full both-project suite) exits 0.
- `data/bingo-roster-candidates.md`, `data/bingo-calibration-report.md`, `data/bingo-calibration.json` all exist and are non-empty.
- The blocking human checkpoint (Task 2) precedes the config write (Task 3) — no constant in config.bingo is non-empty before sign-off.
</verification>

<success_criteria>
GATE 2 is cleared: corpus-measured rosters were surfaced, the user signed off BEFORE any constant was
written (D-20), the Monte-Carlo landed within the D-02/D-03/D-05 bands, and the locked constants (rosters +
per-vibe mix + freeIndex + bustOutGapShows) are written to config.bingo with the gate exiting 0 —
pinned by config.test.ts and the full suite green. Success Criterion 4 met.
</success_criteria>

<output>
Create `.planning/phases/14-gizz-bingo-core-marking-generation/14-06-SUMMARY.md` when done.
</output>
