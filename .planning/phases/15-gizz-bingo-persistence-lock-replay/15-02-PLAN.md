---
phase: 15-gizz-bingo-persistence-lock-replay
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - packages/app/src/db/db.ts
  - packages/app/src/config.ts
  - packages/app/test/migrationV5.test.ts
  - packages/app/test/bingoLock.test.ts
  - packages/app/test/exportImportRoundtrip.test.ts
autonomous: true
requirements: [BINGO-07]
must_haves:
  truths:
    - "A populated v4 database upgrades in place to Dexie version(5) preserving all v1-v4 tables; a new empty bingoCards table exists"
    - "A dealt-but-unlocked card persists with lockedAt null and caughtSnapshot []; lockCard stamps lockedAt + freezes caughtSnapshot; a second lockCard is a no-op"
    - "saveDraftCard is rejected (throws) when the session is finalized or the existing card is already locked (reshuffle rejection, SC-1)"
    - "bingoCards round-trips through export/import; a v2 backup with no bingoCards still imports"
  artifacts:
    - path: "packages/app/src/db/db.ts"
      provides: "BingoCardRow interface, version(5) + bingoCards table, saveDraftCard/lockCard write helpers, DbSnapshot/snapshot/importSnapshot threading"
      contains: "this.version(5).stores"
    - path: "packages/app/src/config.ts"
      provides: "SCHEMA_VERSION 3 + copy.games / copy.recap bingo / copy.catchUp strings"
      contains: "SCHEMA_VERSION: 3"
  key_links:
    - from: "db.ts importSnapshot"
      to: "db.bingoCards"
      via: "stable-cardId bulkPut upsert (NOT clear+rewrite)"
      pattern: "db\\.bingoCards\\.bulkPut"
    - from: "db.ts BingoCardRow.cardId"
      to: "TrackedShow.sessionId"
      via: "cardId = sessionId (D-12 stable merge-safe key)"
      pattern: "cardId"
---

<objective>
Land the app-side persistence for Gizz Bingo: Dexie `version(5)` with a new
additive `bingoCards` table, the `BingoCardRow` shape (embedding the frozen
`BingoCard` + the D-08/D-12 `caughtSnapshot` freeze), the lock/draft write helpers
with the reshuffle-rejection guard (SC-1/D-10), and the export/import threading so
cards ride the backup. Also bump the app envelope to `SCHEMA_VERSION = 3` and add
every Phase-15 copy string in the single config file.

Purpose: freeze the card artifact so a corpus/config change can never re-deal a
locked historical card (D-08), and keep the backup an honest full-state snapshot
(D-13/D-14). The Start-Show / deal UI that TRIGGERS a lock is Phase 16 — this plan
BUILDS and fixture-tests the machinery so it fires for real once a card is dealt
(CONTEXT sequencing note).
Output: Dexie v5 + write helpers + round-trip, all covered by fake-indexeddb tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-gizz-bingo-persistence-lock-replay/15-CONTEXT.md
@.planning/phases/15-gizz-bingo-persistence-lock-replay/15-RESEARCH.md
@.planning/phases/15-gizz-bingo-persistence-lock-replay/15-PATTERNS.md
@.planning/phases/15-gizz-bingo-persistence-lock-replay/15-UI-SPEC.md
</context>

<artifacts_this_plan_produces>
Symbols this plan creates (excluded from downstream drift verification):
- `BingoCardRow` interface (db.ts) — `{ cardId, sessionId, card: BingoCard, caughtSnapshot: number[], lockedAt: number | null, showDate, venueName, city }`.
- `bingoCards!: Table<BingoCardRow, string>` field + `this.version(5).stores({ bingoCards: "&cardId, sessionId" })`.
- `saveDraftCard(...)` write helper (writes an unlocked draft; guard throws on finalized session / already-locked card).
- `lockCard(sessionId, caughtSongIds)` write helper (stamps lockedAt, freezes caughtSnapshot; idempotent; no-op when no card row).
- `bingoCards` on `DbSnapshot` + `snapshot()` read + `importSnapshot` `bulkPut`.
- `config.dataSafety.SCHEMA_VERSION === 3`; `config.copy.games.*`, `config.copy.recap` bingo keys, `config.copy.catchUp.*` copy blocks.
</artifacts_this_plan_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BingoCardRow + version(5) table + saveDraftCard/lockCard/guard</name>
  <files>packages/app/src/db/db.ts, packages/app/test/bingoLock.test.ts</files>
  <read_first>
    - packages/app/src/db/db.ts (the v4 additive block at 228-236; the table fields at 173-178; the `TrackedShow` doc-comment discipline at 69-88; `startShow`'s single-active assertion + transaction at 275-303; the `markShowAttended` transaction+put idiom at 466-480)
    - packages/core/src/bingo/types.ts (the `BingoCard` interface at 60-67 — imported as a type from @guezzer/core)
    - packages/app/test/migrationV3.test.ts (the resetDb + startShow + write-helper + db.*.get() assertion idiom to copy for bingoLock.test.ts)
  </read_first>
  <behavior>
    - saveDraftCard writes a row with `lockedAt === null` and `caughtSnapshot === []`; `db.bingoCards.get(cardId)` returns it after.
    - cardId === sessionId (D-12): a second saveDraftCard for the same session with a new seed OVERWRITES the same row in place (no orphan).
    - lockCard(sessionId, [1,2,3]) sets `lockedAt` to a number and `caughtSnapshot` to [1,2,3]; a second lockCard call leaves the first lockedAt/snapshot unchanged (idempotent).
    - lockCard on a session with NO card row is a no-op (does not throw) — so wiring it into a card-less Start Show is safe.
    - saveDraftCard throws when the target session is `finalized`.
    - saveDraftCard throws when the existing card for that session has `lockedAt != null` (reshuffle rejected on a locked card, SC-1/D-10).
  </behavior>
  <action>
    In db.ts add an exported `BingoCardRow` interface with the doc-comment discipline of `TrackedShow` (69-88): `cardId: string` (D-12 stable inbound PK, NOT the volatile `++id`), `sessionId: string` (FK -> TrackedShow.sessionId, indexed for replay lookup), `card: BingoCard` (import the type from `@guezzer/core`; embeds the frozen `resolvedDefs` as `card.squares`, D-08), `caughtSnapshot: number[]` (the D-08/D-12 freeze; REQUIRED per RESEARCH Pitfall 1), `lockedAt: number | null` (null = unlocked draft, D-08), `showDate: string`, `venueName: string | null`, `city: string | null` (denormalized identity, D-11). Add `bingoCards!: Table<BingoCardRow, string>;` beside the other table fields (173-178), string-keyed like `trackedShows`. Append an ADDITIVE `this.version(5).stores({ bingoCards: "&cardId, sessionId" })` block AFTER v4 (228-236) — do NOT touch v1-v4; a new table needs no `.upgrade` (D-14, the v4 precedent). Set `cardId = sessionId` (RESEARCH Pattern 6 / A3: one card per show D-07, and a pre-lock reshuffle overwrites the same row because the seed lives IN the row). Add two write helpers mirroring the `startShow` transaction+assertion idiom (275-303): `saveDraftCard(input)` — inside a `db.transaction("rw", db.trackedShows, db.bingoCards, …)` read the session, THROW if `show.status === "finalized"`, read any existing card and THROW if `existing?.lockedAt != null` (the D-10 reshuffle-rejection invariant, app-side per RESEARCH Pitfall 5 — never in packages/core), then `put` the row with `cardId: sessionId, lockedAt: null, caughtSnapshot: []`; and `lockCard(sessionId, caughtSongIds: number[])` — read the card by sessionId, `return` (no-op) if absent, `return` if `lockedAt != null` (idempotent), else `db.bingoCards.update(card.cardId, { lockedAt: Date.now(), caughtSnapshot: caughtSongIds })` (D-08/D-12 freeze). Note in a comment that the caught-set the caller passes is derivable from `deriveDex(...).perSong.keys()` at lock time (RESEARCH A1) — Phase 16 wires the real Start-Show/deal trigger. Create bingoLock.test.ts (copy migrationV3.test.ts's resetDb/startShow shape) covering all six behaviors.
  </action>
  <verify>
    <automated>npx vitest run packages/app/test/bingoLock.test.ts</automated>
  </verify>
  <done>BingoCardRow + version(5) `bingoCards` table + saveDraftCard/lockCard with the finalized/locked guard; six bingoLock.test.ts behaviors green; no React/DOM leaked into packages/core.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DbSnapshot/snapshot/importSnapshot threading + v4->v5 migration test</name>
  <files>packages/app/src/db/db.ts, packages/app/test/migrationV5.test.ts</files>
  <read_first>
    - packages/app/src/db/db.ts (the `DbSnapshot` interface at 162-171; `snapshot()` Promise.all at 501-512; `importSnapshot`'s per-table commit at 534-556 — note `archiveShows.bulkPut` at 549 is the stable-key upsert to copy, NOT the clear+rewrite used for trackedShows/trackedEntries)
    - packages/core/src/data-safety/serialize.ts (ExportSnapshot.bingoCards from plan 15-01 — the cross-boundary counterpart)
    - packages/app/test/migrationV3.test.ts (the seed-old-version + db.open() + `expect(db.verno)` + table-preserved assertion shape to copy)
  </read_first>
  <behavior>
    - `snapshot()` includes `bingoCards` (an array) in its result.
    - `importSnapshot(snap)` upserts `snap.bingoCards` via bulkPut — a locally-present card absent from the snapshot is NOT deleted (union-only, like archiveShows), and a same-cardId row is overwritten by the merged snapshot.
    - A DB seeded at version(4) with rows in every prior table, reopened, reports `db.verno === 5`; all v1-v4 tables retain their rows; `db.bingoCards` exists and is empty.
  </behavior>
  <action>
    In db.ts add `bingoCards: BingoCardRow[]` to the `DbSnapshot` interface (162-171). In `snapshot()` (501-512) add `db.bingoCards.toArray()` to the `Promise.all` and include `bingoCards` in the returned object. In `importSnapshot` (534-556) add `db.bingoCards` to the transaction table list and commit with `await db.bingoCards.bulkPut(snapshot.bingoCards)` — the stable-`cardId` upsert path (copy the `archiveShows.bulkPut` at 549), NEVER the clear-and-rewrite path used for trackedShows/trackedEntries (cardId is stable and the merge is union-only, D-13). This keeps `DbSnapshot` mutually assignable with core `ExportSnapshot` so `serializeExport(snapshot(), …)` and `importSnapshot(mergedExportSnapshot)` both compile (the tsc cross-boundary contract) — the app export (exportDownload.ts) and import (importPicker.ts) bridges then thread bingoCards with NO edits, since they forward the whole snapshot. Create migrationV5.test.ts (copy migrationV3.test.ts): seed a v4 DB with meta/attendedShows/archiveShows/trackedShows/trackedEntries rows, reopen, assert `db.verno === 5`, all prior tables preserved, `db.bingoCards` present and empty (D-14 / SC-4).
  </action>
  <verify>
    <automated>npx vitest run packages/app/test/migrationV5.test.ts</automated>
  </verify>
  <done>bingoCards threaded through DbSnapshot/snapshot/importSnapshot (bulkPut upsert); migrationV5.test.ts proves v4->v5 additive upgrade preserves all tables; app `tsc --noEmit` clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SCHEMA_VERSION 3 + Phase-15 copy + export/import round-trip test</name>
  <files>packages/app/src/config.ts, packages/app/test/exportImportRoundtrip.test.ts</files>
  <read_first>
    - packages/app/src/config.ts (`dataSafety.SCHEMA_VERSION` at ~294; the `copy.recap` / `copy.dex` / `copy.settings` blocks that RecapView/views read — mirror their structure)
    - packages/app/test/exportImportRoundtrip.test.ts (the existing seed -> export -> import -> assert idiom to extend)
    - .planning/phases/15-gizz-bingo-persistence-lock-replay/15-UI-SPEC.md (the Copywriting Contract table — the EXACT strings to add)
  </read_first>
  <behavior>
    - `config.dataSafety.SCHEMA_VERSION === 3`.
    - Seeding a bingoCards row, exporting, then importing the produced envelope preserves that card row (round-trip).
    - A v2 envelope (SCHEMA_VERSION 2, no bingoCards key) still imports without error (`.default([])` + MIGRATIONS[2]).
  </behavior>
  <action>
    In config.ts bump `dataSafety.SCHEMA_VERSION` from 2 to 3 (the v3 envelope adds bingoCards; pre-v3 backups migrate via core MIGRATIONS[2]). Add all Phase-15 copy strings from the 15-UI-SPEC Copywriting Contract into the single config file (CLAUDE.md no-scattered-strings rule): a `copy.games` block (`sectionHeading` "GizzGames", `teaserTitle` "Gizz Bingo", `teaserBody` "Deal a bingo card before your next show.", `teaserAffordance` "Coming soon", `emptyHeading` "No cards yet", `emptyBody` "Cards you play at a show will show up here to relive."); bingo keys under `copy.recap` (`bingoHeading` "Bingo", win-badge labels "Line"/"Four corners"/"X"/"Blackout", `noWin` "No lines this show", `litBy` a `(songName) => \`Lit by ${songName}\`` function, `freeLabel` "Free", `lockExplainer` "Locked at Start Show"); and a `copy.catchUp` block (`cta` "Catch me up", `addN` an `(n) => …` function ("Add 1 song" when n===1 else "Add {n}"), `heading` "Songs the tracker missed", `body` "Untick anything wrong, then add the rest to your setlist.", `searchAffordance` "Search to add a song", `allCaughtUp` "You're all caught up — nothing to add.", `feedError` "Couldn't reach the setlist feed. Search to add songs manually."). These strings are consumed read-only by plans 15-03/15-04 (config.ts is owned here to avoid a same-wave collision). Extend exportImportRoundtrip.test.ts: seed a `bingoCards` row, assert an export->import round-trip preserves it, and assert a hand-built v2 envelope (no bingoCards key) still imports.
  </action>
  <verify>
    <automated>npx vitest run packages/app/test/exportImportRoundtrip.test.ts</automated>
  </verify>
  <done>SCHEMA_VERSION === 3; all Phase-15 copy present in config; round-trip + v2-import tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| import file -> importSnapshot | Merged (already core-validated) snapshot commits to Dexie in one rw transaction |
| Dexie v4 -> v5 upgrade | Existing populated databases upgrade in place on next load |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | DoS (bricked view) | Dexie `version(5)` upgrade of a populated v4 DB | mitigate | ADDITIVE new table only, no `.upgrade`, v1-v4 stores untouched (D-14) — proven by migrationV5.test.ts; loaders keep their handled-sentinel idiom |
| T-15-05 | Tampering | `importSnapshot` bingoCards commit | mitigate | Commits only the already core-validated merged snapshot; stable-cardId `bulkPut` union-only (no destructive clear); whole commit inside the existing atomic rw transaction |
| T-15-06 | Elevation (invariant bypass) | reshuffle/draft-write on a locked/finalized session | mitigate | App-side write-helper invariant in `saveDraftCard` throws on finalized session or `lockedAt != null` (SC-1/D-10); guard is app-side, packages/core stays DB-free (RESEARCH Pitfall 5) |
</threat_model>

<verification>
- `npx vitest run packages/app/test/bingoLock.test.ts` green.
- `npx vitest run packages/app/test/migrationV5.test.ts` green.
- `npx vitest run packages/app/test/exportImportRoundtrip.test.ts` green.
- `npx vitest run` (both projects) green after the wave; app + core `tsc --noEmit` clean.
</verification>

<success_criteria>
- v4 DB upgrades to version(5) losslessly; bingoCards table present.
- Draft persists unlocked; lockCard freezes caughtSnapshot + lockedAt; reshuffle rejected on locked/finalized.
- Cards round-trip through backup; v2 backups still import.
</success_criteria>

<output>
Create `.planning/phases/15-gizz-bingo-persistence-lock-replay/15-02-SUMMARY.md` when done.
</output>
