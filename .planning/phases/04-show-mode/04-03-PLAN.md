---
phase: 04-show-mode
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - packages/app/src/show/orbitLayout.ts
  - packages/app/src/show/confidence.ts
  - packages/app/src/show/matrix.ts
  - packages/app/src/show/showContext.ts
  - packages/app/src/show/tuningColor.ts
  - packages/app/src/show/OrbitStage.tsx
  - packages/app/src/show/CenterNode.tsx
  - packages/app/src/show/PredictionOrb.tsx
  - packages/app/src/show/WhyDetail.tsx
  - packages/app/vite.config.ts
  - packages/app/test/orbitLayout.test.ts
  - packages/app/test/confidence.test.ts
autonomous: true
requirements: [SHOW-01, SHOW-02, SHOW-10, EVAL-04]
must_haves:
  truths:
    - "The current song sits at center; the top 5–8 predictions render as tappable orbs sized/placed by probability via a deterministic radial layout (SHOW-01/02)"
    - "The same (rank, score, count, stage) input always produces the same orb positions — no drift between repredicts (SHOW-02)"
    - "Every orb is ≥56px visual / ≥44px hit area regardless of probability (SHOW-02)"
    - "Each orb shows its absolute model % (round(score*100), <1% floor, never renormalized) and a tuning-family color (SHOW-01/D-09)"
    - "An Info dot (not a plain tap) opens the verbatim reason string as the 'why' (SHOW-10/D-11)"
    - "A weak fan (top score < 0.15) softens visually rather than faking precision (EVAL-04/D-10)"
  artifacts:
    - path: "packages/app/src/show/orbitLayout.ts"
      provides: "layoutOrbs pure radial-layout helper"
      exports: ["layoutOrbs"]
    - path: "packages/app/src/show/matrix.ts"
      provides: "bundled matrix load + buildMatrixIndex once + schemaVersion guard"
      contains: "schemaVersion"
    - path: "packages/app/src/show/OrbitStage.tsx"
      provides: "center + adaptive 5-8 orb fan renderer"
      min_lines: 30
  key_links:
    - from: "packages/app/src/show/OrbitStage.tsx"
      to: "packages/core predict()"
      via: "PredictionCandidate[] → fan"
      pattern: "predict\\("
    - from: "packages/app/src/show/PredictionOrb.tsx"
      to: "packages/app/src/show/tuningColor.ts"
      via: "tuningFamily union string → fill color"
      pattern: "tuningFamily"
    - from: "packages/app/src/show/matrix.ts"
      to: "data/normalized/transition-matrix.json"
      via: "Vite resolve.alias bundle-import"
      pattern: "@matrix|transition-matrix"
---

<objective>
Render the focal region: the pure `layoutOrbs` radial-layout math (deterministic, ≥56px, adaptive 5–8), the `confidence` display helpers (absolute %, <1% floor, weak-fan softening flag), the bundled matrix loader (Vite alias + `buildMatrixIndex` once + `schemaVersion` guard), the `showContext` assembly + predict wrapper, and the presentational orbit components (OrbitStage / CenterNode / PredictionOrb / WhyDetail). Tap is wired to a callback prop only (the live logging loop is closed in plan 04-04) so this plan is a self-contained, testable render layer.

Purpose: Deliver SHOW-01/02/10 and the EVAL-04 softening expression with the hardest-to-get-right pieces (layout determinism, honest %, tuning color keyed off the exact core union) locked behind unit tests before they are wired into the live loop.
Output: two pure helpers with RED→GREEN tests, the matrix loader, and four render components consuming the frozen `predict()` API with zero new scoring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-show-mode/04-RESEARCH.md
@.planning/phases/04-show-mode/04-PATTERNS.md
@.planning/phases/04-show-mode/04-UI-SPEC.md
@packages/core/src/domain/types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: layoutOrbs radial helper + confidence display helper (pure, tested)</name>
  <files>packages/app/src/show/orbitLayout.ts, packages/app/src/show/confidence.ts, packages/app/test/orbitLayout.test.ts, packages/app/test/confidence.test.ts</files>
  <read_first>
    - .planning/phases/04-show-mode/04-RESEARCH.md §Pattern 3 (the concrete layoutOrbs math: rank→angle, score→radius, clamp diameter) and §Pattern 8 + §Pitfall 2 (score ∈ [0,0.97] absolute; round(score*100)%, <1% floor; weak-fan check candidates[0].score < 0.15)
    - .planning/phases/04-show-mode/04-UI-SPEC.md §Spacing (ORB_MIN_DIAMETER 56 / 44px hit floor) and §Typography (tabular-nums, <1% rule)
    - packages/app/src/config.ts (config.show tunables from plan 04-01 — ORB_MIN_DIAMETER, ORB_COUNT_MIN/MAX, ORB_DROP_SCORE, WEAK_FAN_THRESHOLD; import, never inline literals)
    - packages/app/test/platform.test.ts (pure-fn unit-test idiom for the app/jsdom project)
    - packages/core/src/model/decay.ts (pure exported math-fn shape analog)
  </read_first>
  <action>
    Create `orbitLayout.ts` exporting `layoutOrbs(candidates, stage, cfg): OrbLayout[]` per RESEARCH Pattern 3 — angle even by RANK (rank 0 at -90°), radius from score (higher score → nearer center, guard equal-score divide-by-zero), diameter clamped to `cfg.orbMinDiameter` (never below the 44px hit floor). Also export the adaptive-fan selector `selectFan(candidates, cfg)`: drop candidates with `score < ORB_DROP_SCORE`, then clamp to [ORB_COUNT_MIN, ORB_COUNT_MAX] (always ≥5 up to 8 — D-12). All constants come from `config.show` (passed in as `cfg` so the fn stays pure/testable). No React, no DOM reads — viewport px passed in as `stage`. Create `confidence.ts` exporting `formatOrbPercent(score): string` (round(score*100); if it rounds to 0 → "<1%", else "{n}%") and `isWeakFan(candidates, cfg): boolean` (candidates[0].score < WEAK_FAN_THRESHOLD). NEVER renormalize (no divide by fan sum — D-09).
    Write orbitLayout.test.ts: determinism (same input → deep-equal output), all diameters ≥ ORB_MIN_DIAMETER, positions within stage bounds, adaptive fan drops sub-drop-score and clamps 5–8 (D-12). Write confidence.test.ts: absolute % (0.24 → "24%"), <1% floor (0.003 → "<1%"), no renormalization (two 0.2 scores both render "20%", not "50%"), weak-fan trips when top < 0.15 and not at 0.20 (D-10).
  </action>
  <verify>
    <automated>npx vitest run packages/app -t "orbitLayout" && npx vitest run packages/app -t "orb percent" && npx vitest run packages/app -t "softening"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run packages/app -t "orbitLayout"` passes (determinism + ≥56px + in-bounds + adaptive fan 5–8).
    - `npx vitest run packages/app -t "orb percent"` and `-t "softening"` pass (absolute %, <1% floor, no renormalization, weak-fan threshold).
    - Source assertion: neither helper divides any score by a fan sum (no renormalization).
    - Source assertion: no numeric literal for diameter/threshold/count is inlined — all read from the passed `cfg`/`config.show`.
  </acceptance_criteria>
  <done>layoutOrbs + selectFan + confidence helpers pure, config-driven, and green across the three suites.</done>
</task>

<task type="auto">
  <name>Task 2: bundled matrix loader + ShowContext assembly + predict wrapper</name>
  <files>packages/app/src/show/matrix.ts, packages/app/src/show/showContext.ts, packages/app/vite.config.ts</files>
  <read_first>
    - .planning/phases/04-show-mode/04-RESEARCH.md §Pitfall 4 (Vite resolve.alias for the matrix, schemaVersion===1 guard, fail to "Couldn't load the prediction model" state) and §Pattern 2 (assemble ShowContext from entries; recentShowSongSets = [] for night 1; predict() rebuilds index internally — trivial at 264 nodes)
    - packages/app/vite.config.ts (whole file — add `resolve.alias` without disturbing the PWA/define config; note Workbox globPatterns already precache **/*.js, so a bundle-import needs NO json glob edit)
    - packages/core/src/index.ts (barrel: import `predict`, `buildMatrixIndex`, types `TransitionMatrix`, `PredictionCandidate`, `ShowContext`, `MatrixNode`)
    - packages/core/src/model/predict.ts:482-504 (predict signature — returns sorted PredictionCandidate[])
    - packages/app/src/db/db.ts (TrackedEntry shape from 04-01 — filter songId!=null for the trail)
  </read_first>
  <action>
    Add a `resolve.alias` entry to vite.config.ts mapping e.g. `@matrix` to the absolute path of `data/normalized/transition-matrix.json` (repo root, outside packages/app — see Pitfall 4). Create `matrix.ts`: bundle-import the JSON typed as core's `TransitionMatrix`, guard `schemaVersion === 1` (throw/return an error sentinel the ShowView renders as the model-load-failure state, NOT a crash — ASVS V7), and expose `loadMatrix()` + a memoized `getMatrixIndex()` calling `buildMatrixIndex(matrix)` exactly once. Because it is bundle-imported it is precached by the existing `**/*.js` Workbox glob — do NOT add a `json` glob. Create `showContext.ts` exporting `buildShowContext(currentSongId, entries, recentFinalizedShowSongSets): ShowContext` per RESEARCH Pattern 2 (trail = entries with non-null songId mapped to songId; recentShowSongSets defaults to [] for night 1) and a thin `predictFan(matrix, ctx)` wrapper returning `predict(matrix, ctx)`. Note the emergent multi-night rotation-suppression bonus (RESEARCH Pattern 2 / Open Q2) is additive and NOT required for this slice — leave `recentFinalizedShowSongSets` an accepted param defaulting to [].
  </action>
  <verify>
    <automated>npx vitest run packages/app && npx tsc -p packages/app --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: vite.config.ts contains a `resolve.alias` for the matrix and the Workbox `globPatterns` is unchanged (no `json` added — bundle-import path).
    - Source assertion: matrix.ts guards `schemaVersion === 1` and returns/throws a handled error rather than letting an unguarded read crash.
    - Behavior assertion: `getMatrixIndex()` builds the index once (memoized) — a second call returns the same reference.
    - `npx tsc -p packages/app --noEmit` passes (matrix typed as core `TransitionMatrix`).
  </acceptance_criteria>
  <done>Matrix loads offline via bundle-import with a schemaVersion guard; ShowContext assembly + predict wrapper compile against the frozen core API.</done>
</task>

<task type="auto">
  <name>Task 3: OrbitStage + CenterNode + PredictionOrb + WhyDetail (render fan, tap→callback)</name>
  <files>packages/app/src/show/OrbitStage.tsx, packages/app/src/show/CenterNode.tsx, packages/app/src/show/PredictionOrb.tsx, packages/app/src/show/WhyDetail.tsx, packages/app/src/show/tuningColor.ts</files>
  <read_first>
    - .planning/phases/04-show-mode/04-UI-SPEC.md §Color B1 (tuning palette) + §Layout region 3 (orbit stage contract) + §Component Inventory (OrbitStage/CenterNode/PredictionOrb/WhyDetail contracts)
    - .planning/phases/04-show-mode/04-RESEARCH.md §Pitfall 3 (tuning color MUST key off the exact core union "standard"|"cs-standard"|"microtonal"|"other" — NOT the UI-SPEC display label "C# standard"; map all four literals, unmapped → text-muted)
    - .planning/phases/04-show-mode/04-PATTERNS.md "OrbitStage"/"PredictionOrb" analogs (BottomTabBar tap-button idiom; theme tokens; 44px floor; lucide Info dot) + §"Theme tokens (inherited — never re-derive)"
    - packages/app/src/components/BottomTabBar.tsx (tap-button + min-h-11 min-w-11 + lucide idiom)
    - packages/app/src/components/PlaceholderView.tsx (centered dark-state + config.copy idiom)
    - packages/core/src/domain/types.ts:104-189 (MatrixNode.tuningFamily, PredictionCandidate.score/reason)
  </read_first>
  <action>
    Create `tuningColor.ts` mapping ALL FOUR core union literals to the UI-SPEC B1 hexes: `standard`→`#5EC8E5`, `cs-standard`→`#B98CF2`, `microtonal`→`#FF8A5B`, `other`→`#A1A1AA`; unmapped/missing → `#A1A1AA` (text-muted). Key off the exact union string, never the display label (Pitfall 3). Create `PredictionOrb.tsx`: absolutely-positioned button at layout x/y, diameter from layoutOrbs, family fill from tuningColor, on-orb truncated song name + `formatOrbPercent(score)` with `tabular-nums`, a small lucide `Info` dot that calls an `onWhy` prop (NOT the log path — D-11), and the whole orb face calls `onTap(candidate)` (log path wired later). `min-h-11 min-w-11` floor. Apply weak-fan softening (reduced opacity ~55% + desaturate) when an `isWeak` prop is set (D-10). Create `CenterNode.tsx`: current song at center, tuning-family colored, Heading-size name, no % (or the "Tap the opener" prompt state from copy.show when no current song). Create `OrbitStage.tsx`: measure the stage px (ref/resize), call `selectFan` + `layoutOrbs`, render CenterNode + PredictionOrb×N; compute `isWeakFan` once and pass to orbs + a muted weak-fan hint (copy.show); accept `onTapOrb`/`onWhy` callbacks and the candidate list as props (data fetching/predict happens in ShowView, plan 04-04). Create `WhyDetail.tsx`: an overlay/sheet (AppMenu analog) rendering `PredictionCandidate.reason` verbatim as text — read-only, never dangerouslySetInnerHTML (XSS). Use inherited theme tokens only; no new spacing/typography scale.
  </action>
  <verify>
    <automated>npx tsc -p packages/app --noEmit && npx vitest run packages/app</automated>
    <human-check>On the smallest target phone width, render a mock 8-orb fan and a 5-orb fan: orbs do not overlap, all ≥44px, tuning colors distinct, weak-fan softening visibly mutes the fan. (Perceptual/on-device per VALIDATION manual-only, SHOW-01.)</human-check>
  </verify>
  <acceptance_criteria>
    - `npx tsc -p packages/app --noEmit` passes; `npx vitest run packages/app` stays green.
    - Source assertion: `tuningColor.ts` switches on `"cs-standard"` (exact union), NOT `"C# standard"` (display label) — Pitfall 3.
    - Source assertion: WhyDetail renders `reason` as text (no `dangerouslySetInnerHTML` anywhere in the show/ components).
    - Source assertion: PredictionOrb's Info dot handler is distinct from the orb-face tap handler (D-11 — Info never logs).
    - Behavior assertion: every PredictionOrb carries `min-h-11 min-w-11` and a diameter ≥ config.show.ORB_MIN_DIAMETER.
  </acceptance_criteria>
  <done>OrbitStage renders a deterministic, correctly-colored, honestly-labeled adaptive fan with a separate "why" affordance; tap is a callback prop ready for wiring; typecheck + suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| matrix JSON → app | Build-frozen, trusted artifact; still schemaVersion-guarded on load |
| kglw-derived song names → DOM | Untrusted-origin text rendered in orbs/center/why |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering (XSS) | PredictionOrb / CenterNode / WhyDetail | mitigate | Song names + reason render as React text only; never `dangerouslySetInnerHTML` on any kglw-derived string (ASVS V5, SCHEMA §12). |
| T-04-06 | Denial of Service (crash) | matrix.ts load | mitigate | `schemaVersion === 1` guard → handled model-load-failure state, never an unguarded read that bricks the orbit (ASVS V7). |
| T-04-07 | Tampering | renormalized/false-confidence % | mitigate | Display absolute `score` only; unit test forbids renormalization (D-09). |
</threat_model>

<verification>
- `npx vitest run packages/app` green (orbitLayout + confidence suites + existing).
- `npx tsc -p packages/app --noEmit` passes.
- Manual/on-device fan check per VALIDATION manual-only (SHOW-01 perceptual).
</verification>

<success_criteria>
Deterministic ≥56px adaptive 5–8 fan renders from the frozen `predict()` output with honest absolute %, correct tuning color (exact-union keyed), a separate Info "why", and weak-fan softening; matrix loads offline with a guard; all pure helpers unit-tested. Tap remains a callback (loop closed in 04-04).
</success_criteria>

## Artifacts this phase produces (this plan)

- **Pure helpers:** `layoutOrbs`, `selectFan` (`show/orbitLayout.ts`); `formatOrbPercent`, `isWeakFan` (`show/confidence.ts`); `tuningColor` map (`show/tuningColor.ts`)
- **Loader/assembly:** `loadMatrix`, `getMatrixIndex` (`show/matrix.ts`); `buildShowContext`, `predictFan` (`show/showContext.ts`)
- **Components:** `OrbitStage`, `CenterNode`, `PredictionOrb`, `WhyDetail`
- **Build:** Vite `resolve.alias` for the bundled matrix artifact
- **Files/tests:** `packages/app/test/orbitLayout.test.ts`, `packages/app/test/confidence.test.ts`

<output>
Create `.planning/phases/04-show-mode/04-03-SUMMARY.md` when done.
</output>
