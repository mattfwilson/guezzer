---
phase: 02-transition-matrix-model-backtest
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - packages/core/src/model/predict.ts
  - packages/core/test/model/predict.test.ts
autonomous: true
requirements: [MODL-04, MODL-05, MODL-06, MODL-07, MODL-10, EVAL-05]
must_haves:
  truths:
    - "A song already played in the current show drops to near-zero score, but never hard-zero (sandwich/reprise-aware)"
    - "A song played in the last 1-3 shows of the current tour is suppressed, approaching hard exclusion in a multi-night run"
    - "Era prior boosts songs hot in the current era and dampens retired songs, as a relative multiplier centered near 1"
    - "A consistently-notated hard segue overrides the base model to near-1.0 confidence; only notated segues reach ~100%"
    - "An inconsistent/one-off segue gets a strong multiplicative boost, not a 100% override"
    - "Each signal is an independently-toggleable multiplier so ablation is a single flag-flip"
  artifacts:
    - path: "packages/core/src/model/predict.ts"
      provides: "rotationSuppression, alreadyPlayedFactor, eraPrior, hardSegueOverride, applyOverride — the real signal bodies"
      exports: ["rotationSuppression", "alreadyPlayedFactor", "eraPrior", "hardSegueOverride"]
  key_links:
    - from: "packages/core/src/model/predict.ts"
      to: "packages/core/src/model/matrix.ts"
      via: "hardSegueOverride reads edge segueCount + totalExits"
      pattern: "segueCount"
    - from: "packages/core/src/model/predict.ts"
      to: "ShowContext"
      via: "rotation + already-played read the in-progress trail and recent shows"
      pattern: "recentShowSongSets|trail"
---

<objective>
Fill the real bodies of the five toggle-gated multipliers stubbed in Plan 02, completing the D-01 multiplicative pipeline: decay toggle (MODL-04 in scoring), rotation suppression (MODL-06), already-played conditioning (MODL-10), era prior (MODL-07), and the consistency-gated hard-segue override (MODL-05/D-04). After this plan the predictor combines every signal the phase requires, each independently ablatable.

This is MVP slice 2: layer the remaining signals onto the working predictor. Each signal is verifiable in isolation against a dedicated fixture and flips a single toggle for ablation.

Purpose: Complete the honest scoring model — near-zero-not-zero repeats (D-05), rotation that approaches exclusion in multi-night runs (MODL-06), and hard segues that reach ~100% only when consistently notated (D-04).
Output: Real multiplier bodies + `applyOverride` + the D-06 segue reason string in `model/predict.ts`, plus per-signal Wave-0 tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-transition-matrix-model-backtest/02-CONTEXT.md
@.planning/phases/02-transition-matrix-model-backtest/02-RESEARCH.md
@.planning/phases/02-transition-matrix-model-backtest/02-PATTERNS.md
@.planning/phases/02-transition-matrix-model-backtest/02-02-SUMMARY.md
</context>

<artifacts_produced>
Symbols this plan creates (exclude from drift verification — they are new; they replace Plan 02 stubs):
- **Functions** (`model/predict.ts`): `rotationSuppression(B, ctx, cfg)`, `alreadyPlayedFactor(B, ctx, cfg)`, `eraPrior(B, index, cfg)`, `hardSegueOverride(A, B, index, cfg)`, `applyOverride(score, override, cfg)`, plus a decay-toggle path in `transitionProb`.
- New assertions in `packages/core/test/model/predict.test.ts`.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Decay toggle + rotation suppression + already-played + era prior multipliers</name>
  <files>packages/core/src/model/predict.ts, packages/core/test/model/predict.test.ts</files>
  <read_first>
    - packages/core/src/model/predict.ts (the Plan 02 scoreCandidate skeleton with neutral 1.0 stubs to replace; the tiers + baseFactor)
    - packages/core/src/model/matrix.ts (MatrixNode.eraPlayCount / playCount baked at build time — the leak-safe inputs for eraPrior)
    - packages/core/src/domain/types.ts (ShowContext.trail + ShowContext.recentShowSongSets; SignalToggles)
    - packages/core/src/config.ts (rotationWindowShows, rotationPenaltyPerShow, alreadyPlayedFactor, eraWindowShows, eraPriorSmoothingK, eraPriorFloor, eraPriorCeil, decayHalfLifeDays)
    - packages/core/test/fixtures/2025-sandwich.json (already-played / reprise fixture — reuse for MODL-10)
    - .planning/phases/02-transition-matrix-model-backtest/02-RESEARCH.md (M3 rotation vs already-played lines 201-204; M8 era-prior definition lines 227-230; Pitfall 5 era double-counting lines 260-263; anti-pattern hard-zeros lines 169)
    - .planning/phases/02-transition-matrix-model-backtest/02-CONTEXT.md (D-05 already-played near-zero; D-10 decay relative to cutoff)
  </read_first>
  <behavior>
    - Test "already played" (MODL-10, D-05): with `2025-sandwich`, a candidate whose songId is already in `ctx.trail` is multiplied by `cfg.alreadyPlayedFactor` (≈0.02) — its score drops to near-zero but is strictly `> 0` (a hard-zero would mispredict the sandwich/reprise). Applied once per candidate regardless of how many times it appears in the trail.
    - Test "rotation suppression" (MODL-06): a candidate played in all 3 of `ctx.recentShowSongSets` is multiplied by `rotationPenaltyPerShow ^ 3` (≈0.125); a candidate in none is multiplied by 1.0; the score never reaches 0.
    - Test "era prior" (MODL-07): `eraPrior(B)` is a relative multiplier centered near 1 — a song with `eraPlayCount/eraWindowShows` above its all-time rate scores `> 1`, a retired song (`eraPlayCount === 0`) scores `< 1` but `≥ cfg.eraPriorFloor` (never 0); the result is clamped to `[eraPriorFloor, eraPriorCeil]` and is distinct from tier-3 `albumEraAffinity` (Pitfall 5, no double-count).
    - Test "decay toggle" (MODL-04): with `toggles.decay = false`, `transitionProb` uses raw `count` instead of `weightedCount`; with it true, recent instances dominate.
  </behavior>
  <action>
    Replace the Plan 02 neutral stubs with real bodies (each still toggle-gated so ablation is a flag-flip, M6): `alreadyPlayedFactor(B, ctx, cfg)` returns `cfg.alreadyPlayedFactor` if `ctx.trail` contains `B` else `1.0` — conditions on the IN-PROGRESS current show, near-zero not hard-zero (D-05/MODL-10), applied once per candidate (a boolean membership check, not per-occurrence). `rotationSuppression(B, ctx, cfg)` returns `cfg.rotationPenaltyPerShow ^ (count of the last cfg.rotationWindowShows shows in ctx.recentShowSongSets that contain B)` — conditions on RECENT PRIOR TOUR shows (distinct from already-played per M3), approaching hard exclusion in a multi-night run without hitting 0 (MODL-06). `eraPrior(B, index, cfg)` = `clamp( smooth(node.eraPlayCount / cfg.eraWindowShows, cfg.eraPriorSmoothingK) / smooth(node.playCount / totalShows, cfg.eraPriorSmoothingK), cfg.eraPriorFloor, cfg.eraPriorCeil )` — a RELATIVE "is B hot right now vs its career" boost centered at 1 (M8), orthogonal to t3 and t4 (Pitfall 5). Wire the `decay` toggle into `transitionProb`: use `weightedCount` when `toggles.decay` is true, raw `count` when false (MODL-04 / ablation-off semantics, D-10/M6). Confirm each multiplier returns exactly `1.0` when its toggle is off.

    Extend `test/model/predict.test.ts` (D-17 known-output fixtures) with the four `behavior` tests, each building a matrix from the relevant fixture (or a small hand-authored context) and asserting concrete multiplier values.
  </action>
  <verify>
    <automated>npx vitest run packages/core test/model/predict.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run packages/core -t "already played"` passes — trail member ×≈0.02, strictly `> 0`, once-per-candidate.
    - `npx vitest run packages/core -t "rotation suppression"` passes — `0.5^n` over the recent-shows window, never 0.
    - `npx vitest run packages/core -t "era prior"` passes — relative multiplier centered ~1, clamped to `[floor, ceil]`, retired song `< 1` but `≥ floor`.
    - `npx vitest run packages/core -t "decay half-life"` (or a `decay toggle` test) passes — toggle switches weightedCount vs raw count.
  </acceptance_criteria>
  <done>Rotation, already-played, era-prior, and decay-toggle are real toggle-gated multipliers; no hard-zeros; all four tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Consistency-gated hard-segue override + applyOverride + D-06 segue reason string</name>
  <files>packages/core/src/model/predict.ts, packages/core/test/model/predict.test.ts</files>
  <read_first>
    - packages/core/src/model/predict.ts (scoreCandidate — the hard-segue hook returning null in Plan 02, to be filled)
    - packages/core/src/model/matrix.ts (MatrixEdge.segueCount / firstDate / lastDate; how totalExits(A) is derived from the index)
    - packages/core/src/config.ts (hardSegueConsistencyThreshold 0.70, hardSegueMinSupport 3, hardSegueOverrideCeiling 0.97, hardSegueBoost 3.0)
    - packages/core/test/fixtures/2017-segues.json + 2025-segue-chain.json (notated-segue fixtures — reuse for MODL-05/D-04)
    - .planning/phases/02-transition-matrix-model-backtest/02-RESEARCH.md (M4 hard-segue gating lines 206-210; Pitfall 1 segue direction lines 165/238-242; reason string derivation)
    - .planning/phases/02-transition-matrix-model-backtest/02-CONTEXT.md (D-04 consistency-gated override; D-06 reason string "notated segue 14/15 times since 2024")
    - .planning/phases/02-transition-matrix-model-backtest/02-PATTERNS.md (§ Shared Pattern "Segue direction" lines 351-354)
  </read_first>
  <behavior>
    - Test "hard segue override" (MODL-05, D-04): for an edge A→B where `segueRate = segueCount(A→B) / totalExits(A) ≥ hardSegueConsistencyThreshold` AND `totalExits(A) ≥ hardSegueMinSupport`, the final score is pinned to `hardSegueOverrideCeiling` (0.97 — near-1.0 but never literally 1.0, honoring "only notated hard segues reach 100%") and `factors.hardSegueFlag === true`.
    - Test "inconsistent segue boost" (D-04): a one-off/low-consistency segue (fails the gate) is NOT pinned — instead its base score is multiplied by `hardSegueBoost` (3.0), a strong boost not an override; `hardSegueFlag === false`.
    - Test "segue direction" (Pitfall 1): the override fires iff `A.transitionKind === "segue"` on the EARLIER performance (A's row), never B's — reuse the `2017-segues` assertion that segue-kind lives on the earlier position.
    - Test "no false 100%": a free-choice (non-segue) top candidate never reaches a score of 1.0; only gated segues reach the 0.97 ceiling.
    - Test "segue reason" (D-06): a gated candidate's `reason` reads like "notated segue N/M times since YYYY" using stored `segueCount`/`totalExits`/`firstDate`.
  </behavior>
  <action>
    Implement `hardSegueOverride(A, B, index, cfg)`: compute `totalExits(A)` = sum of `count` over all edges out of A (from the index), `segueCount(A→B)` from the stored edge, and `segueRate = segueCount / totalExits` (denominator = all of A's within-set exits per A8/D-04 wording; keep the denominator a swappable helper). Return an override descriptor: if `segueRate ≥ cfg.hardSegueConsistencyThreshold && totalExits ≥ cfg.hardSegueMinSupport` → `{ kind: "pin", value: cfg.hardSegueOverrideCeiling }`; else if the edge has any `segueCount > 0` → `{ kind: "boost", value: cfg.hardSegueBoost }`; else `null`. Segue direction is CORRECTNESS-CRITICAL — the signal is `A.transitionKind === "segue"` already accumulated into `segueCount` during matrix build (Pitfall 1); do not re-read B's kind. Implement `applyOverride(score, override, cfg)`: `pin` → return the ceiling (clamped so nothing else can push a non-segue to 1.0), `boost` → `score * override.value`, `null` → `score`. Wire it as the final step of `scoreCandidate` (D-01: override applied on top), gated by `toggles.hardSegue`. Build the D-06 reason string for gated candidates from `segueCount`/`totalExits`/`firstDate` (concrete counts, e.g. "notated segue 14/15 times since 2024").

    Extend `test/model/predict.test.ts` with the five `behavior` tests.
  </action>
  <verify>
    <automated>npx vitest run packages/core test/model/predict.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run packages/core -t "hard segue override"` passes — consistent+supported segue pins to 0.97, hardSegueFlag true.
    - `npx vitest run packages/core -t "inconsistent segue boost"` passes — one-off segue boosted ×3.0, not pinned.
    - `npx vitest run packages/core -t "segue direction"` passes — override keys on A's transitionKind, not B's.
    - A non-segue candidate never scores 1.0; a gated candidate's reason names concrete counts + "since YYYY".
    - `npx tsc -p packages/core --noEmit` passes; each multiplier returns 1.0 (or override null) when its toggle is off.
  </acceptance_criteria>
  <done>The consistency-gated hard-segue override + boost + D-06 reason string complete the D-01 pipeline; only notated segues reach ~100%; all five tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | All five multipliers are pure fns over the matrix + `ShowContext`; no I/O, no argv, no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering | Division by zero in `segueRate` when `totalExits(A) === 0` | mitigate | Gate requires `totalExits ≥ hardSegueMinSupport` (≥3) before dividing; a song with no exits yields `null` (no override), never NaN |
| T-02-07 | Tampering | eraPrior divide-by-zero / NaN when `totalShows === 0` or `playCount === 0` | mitigate | Additive smoothing (`eraPriorSmoothingK`) in numerator and denominator guarantees a finite ratio; result clamped to `[floor, ceil]` |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase (RESEARCH § Package Legitimacy Audit: zero new deps) |
</threat_model>

<verification>
- `npx vitest run packages/core` — full core suite green (matrix + all predict signals).
- No hard-zero and no false-100% assertions hold across the predict suite.
- `npx tsc -p packages/core --noEmit` clean.
</verification>

<success_criteria>
- Predictor combines transition frequency, recency decay, hard-segue overrides, rotation suppression, era prior, and the tuning-family backoff chain; already-played drops to near-zero; nothing plausible is hard-zero; only notated hard segues reach ~100% (Phase success criterion 2 — complete).
- Fixture unit tests with known expected outputs pass for every signal (success criterion 5).
</success_criteria>

<output>
Create `.planning/phases/02-transition-matrix-model-backtest/02-03-SUMMARY.md` when done.
</output>
