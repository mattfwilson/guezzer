---
phase: 11-live-sync-prediction-correctness
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/core/test/model/predict.test.ts
autonomous: true
requirements: [PRED-02]
must_haves:
  truths:
    - "An era-prior test at production scale FAILS on the current buggy eraPrior (proves the masking bug is removed from the test)"
    - "A zero-eraPlayCount retired song is asserted to reach eraPriorFloor (~0.3), not ~1.0"
  artifacts:
    - path: "packages/core/test/model/predict.test.ts"
      provides: "Production-scale era-prior regression test (rewritten Test 10)"
      contains: "eraPriorFloor"
  key_links:
    - from: "packages/core/test/model/predict.test.ts"
      to: "packages/core/src/model/predict.ts eraPrior"
      via: "buildMatrixIndex(fixtureMatrix) → eraPrior(retiredSong, index, cfg)"
      pattern: "eraPrior\\("
---

<objective>
Wave 0 RED gate for PRED-02. Rewrite the era-prior unit test (`predict.test.ts` Test 10) so it exercises `eraPrior` at PRODUCTION scale instead of the 3-node fixture that accidentally aligns units and masks the bug. On the current (buggy) `eraPrior` arithmetic this rewritten test MUST fail — that failure is the deliverable, proving the floor is genuinely unreachable today. Plan 11-03 (PRED-02 fix) flips it green.

Purpose: The existing Test 10 (`predict.test.ts:392`) passes on buggy code because its tiny fixture (Σ playCount = 120) makes `basePlayRate` accidentally commensurate with the per-show `eraRate`. At real catalog scale (Σ playCount ~14,000) the `k=1` smoothing pins every ratio near 1.0 and `eraPriorFloor` (0.3) never fires (RESEARCH Pitfall 1). A green suite is currently hiding a dead prediction signal. This gate makes the bug testable BEFORE it is fixed.
Output: A rewritten, production-scale era-prior test that is RED against current `predict.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-live-sync-prediction-correctness/11-RESEARCH.md
@.planning/phases/11-live-sync-prediction-correctness/11-VALIDATION.md
</context>

<artifacts>
This plan produces (test-only, no production symbols):
- Rewritten production-scale era-prior regression test in `packages/core/test/model/predict.test.ts` (replaces the masking Test 10 at `:392`).
</artifacts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite the masking era-prior test at production scale (RED)</name>
  <files>packages/core/test/model/predict.test.ts</files>
  <read_first>
    - `packages/core/test/model/predict.test.ts:392-430` — the current Test 10 (the masking fixture: hand-built `MatrixIndex` literal at `:400`, Σ playCount = 120).
    - `.planning/phases/11-live-sync-prediction-correctness/11-RESEARCH.md` §"Pitfall 1" + §"Code Examples → PRED-02 the exact unit mismatch (predict.ts:280-288)" — why the tiny fixture masks the bug and what production scale looks like.
    - `.planning/phases/11-live-sync-prediction-correctness/11-PATTERNS.md` §"Fixture-based core test idiom" (`predict.test.ts:392-430`) and §"index-build.ts add showCount" (note: Test 10's index literal will gain `showCount` in 11-03 — build the index via `buildMatrixIndex(fixtureMatrix)` here so it stays forward-compatible).
    - `packages/core/src/model/predict.ts` `eraPrior` (:280), `basePlayRate` (:145) — the function under test and its current denominator.
    - `packages/core/src/config.ts:154-163` — `eraWindowShows: 40`, `eraPriorSmoothingK: 1`, `eraPriorFloor: 0.3`, `eraPriorCeil: 2.0` (the constants the assertion reasons about).
  </read_first>
  <behavior>
    - Retired song (eraPlayCount = 0) at production scale: eraPrior returns ≈ eraPriorFloor (0.3), NOT ~1.0. (RED on current code.)
    - Currently-hot song (high eraPlayCount) at production scale: eraPrior returns > 1.0 (toward eraPriorCeil).
    - Both assertions read the config bounds by name, never hardcode 0.3/2.0 as bare literals divorced from `scoringConfig`.
  </behavior>
  <action>
    Replace the 3-node hand-built `MatrixIndex` literal in Test 10 with a production-scale fixture. Build a `TransitionMatrix` whose nodes sum to a realistic Σ playCount (~14,000 across ~260 nodes — a representative spread is sufficient; you do NOT need all 264), including (a) one RETIRED song node with `eraPlayCount: 0` and a modest career `playCount` (e.g. ~50), and (b) one HOT song node with a high `eraPlayCount` near `eraWindowShows`. Set the matrix header `showCount` to a realistic value (e.g. ~241, the corpus show count per `matrix.ts:186`). Construct the index via `buildMatrixIndex(matrix)` (do NOT hand-build the `MatrixIndex` literal — this keeps the test forward-compatible with the `showCount` field 11-03 adds). Assert `eraPrior(retiredSongId, index, scoringConfig)` is approximately `scoringConfig.eraPriorFloor` (use `toBeCloseTo` / `toBeLessThanOrEqual(scoringConfig.eraPriorFloor + epsilon)`), and `eraPrior(hotSongId, index, scoringConfig)` is `> 1`. Keep the surrounding describe/it structure and imports (`buildMatrixIndex`, `eraPrior`, `scoringConfig`/`defaultScoringConfig`) intact. Do NOT touch `predict.ts` or `config.ts` — this task only rewrites the test. The test is EXPECTED to be RED until 11-03 lands the fix; document that in an inline comment referencing PRED-02 / plan 11-03.
  </action>
  <verify>
    <automated>npx vitest run packages/core/test/model/predict.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/core/test/model/predict.test.ts` no longer contains the 3-node `Σ playCount = 120` era-prior fixture; the era-prior test builds its index via `buildMatrixIndex(...)` over a production-scale matrix.
    - Running `npx vitest run packages/core/test/model/predict.test.ts` reports the era-prior assertion FAILING on current code (retired song returns ~1.0, not ~floor) — this RED result is the accepted deliverable of this Wave 0 gate.
    - No other previously-green test in `predict.test.ts` regresses (only the era-prior case is red).
    - No production source file (`predict.ts`, `config.ts`, `index-build.ts`) is modified by this plan.
  </acceptance_criteria>
  <done>The masking fixture is gone; the era-prior test is production-scale and RED against current code, ready for the 11-03 fix to turn it green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none new) | Test-only change over pure model math; no untrusted input, no I/O, no rendering. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-T | Tampering | Test fixture correctness | mitigate | Fixture built from named config bounds + realistic scale so the test cannot silently re-mask the bug; RED-until-fixed is asserted. |
| T-11-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — test edit only. |
</threat_model>

<verification>
- `npx vitest run packages/core/test/model/predict.test.ts` — era-prior case RED (expected), all other cases green.
</verification>

<success_criteria>
- The era-prior test is rewritten at production scale and fails on the current buggy `eraPrior`, proving PRED-02's floor is unreachable today. Plan 11-03 will make it pass.
</success_criteria>

<output>
Create `.planning/phases/11-live-sync-prediction-correctness/11-01-SUMMARY.md` when done. Record the observed RED assertion output verbatim so 11-03 can confirm it flips to green.
</output>
