---
phase: 11-live-sync-prediction-correctness
plan: 04
type: execute
wave: 2
depends_on: [11-02]
files_modified:
  - packages/app/src/live/SyncDot.tsx
  - packages/app/src/live/useLatestPoll.ts
  - packages/app/src/live/mockLatest.ts
  - packages/app/src/show/ShowView.tsx
  - packages/app/test/useLatestPoll.test.tsx
  - packages/app/test/mockLatest.test.ts
  - packages/app/test/adopt.test.tsx
autonomous: true
requirements: [LIVE-01, LIVE-03]
must_haves:
  truths:
    - "ShowView filters latest rows through guardLatestRows ONCE and passes the guarded rows to suggestions, fill-hints, and auto-bind"
    - "pollLatest's PollResult.schemaDrift is threaded through useLatestPoll to ShowView and sets a distinct amber, non-blocking SyncDot state"
    - "The amber drift state never blocks logging and its detail shows key names only, never editor content"
  artifacts:
    - path: "packages/app/src/live/SyncDot.tsx"
      provides: "third amber schemaDrift visual state"
      contains: "schemaDrift"
    - path: "packages/app/src/show/ShowView.tsx"
      provides: "guardedRows wiring + drift → SyncDot threading"
      contains: "guardLatestRows"
  key_links:
    - from: "packages/app/src/show/ShowView.tsx"
      to: "packages/core guardLatestRows"
      via: "one guardedRows value feeding diffLatestAgainstTrail / resolvePlaceholders / bindShowFromLatest"
      pattern: "guardLatestRows\\("
    - from: "packages/app/src/live/useLatestPoll.ts"
      to: "packages/app/src/live/SyncDot.tsx"
      via: "PollResult.schemaDrift → ShowView state → SyncDot schemaDrift prop"
      pattern: "schemaDrift"
---

<objective>
Wire the LIVE-01 guard and LIVE-03 drift signal into the app tier (the thin lifecycle/render half; all decision logic already lives in core from plan 11-02):
- **LIVE-01:** in `ShowView`, compute `guardedRows = guardLatestRows(latestRows, { showId: session.active.showId, date: session.active.date })` ONCE and pass it to `diffLatestAgainstTrail`, `resolvePlaceholders`, and `bindShowFromLatest` — so a cached previous-night `latest` can no longer leak yesterday's songs into suggestions/fill-hints/auto-bind.
- **LIVE-03:** consume the widened `PollResult` in `useLatestPoll`, surface `schemaDrift`/`novelKeys` to `ShowView`, and add a third amber, non-blocking "syncing — API shape changed" state to `SyncDot` (tap for key-name detail). Update `mockLatest` + app tests for the new `PollResult` shape.

Purpose: Plan 11-02 produced the pure guard + the drift-carrying `PollResult`; without this wiring the guard is never called and the drift flag is dropped. This is the app-side completion of LIVE-01 and LIVE-03.
Output: Guarded suggestion/bind path in ShowView, an amber SyncDot drift state, and updated poll hook + mocks + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-live-sync-prediction-correctness/11-RESEARCH.md
@.planning/phases/11-live-sync-prediction-correctness/11-PATTERNS.md
@.planning/phases/11-live-sync-prediction-correctness/11-VALIDATION.md
@.planning/phases/11-live-sync-prediction-correctness/11-02-SUMMARY.md
</context>

<artifacts>
This plan produces:
- third amber `schemaDrift` visual state + `schemaDrift?`/`novelKeys?` props on `SyncDot` — `packages/app/src/live/SyncDot.tsx`
- widened `onRows`/drift channel consuming `PollResult` — `packages/app/src/live/useLatestPoll.ts`
- `PollResult`-shaped mock — `packages/app/src/live/mockLatest.ts`
- `guardedRows` computation + drift-state threading — `packages/app/src/show/ShowView.tsx`
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: PollResult through useLatestPoll + amber SyncDot state (LIVE-03 app)</name>
  <files>packages/app/src/live/useLatestPoll.ts, packages/app/src/live/SyncDot.tsx, packages/app/src/live/mockLatest.ts, packages/app/test/useLatestPoll.test.tsx, packages/app/test/mockLatest.test.ts</files>
  <read_first>
    - `.planning/phases/11-live-sync-prediction-correctness/11-02-SUMMARY.md` — the `PollResult` shape + the app call-site checklist recorded by 11-02.
    - `packages/app/src/live/useLatestPoll.ts:57-59` (`onRows` contract) + `:105-107` (`const rows = await pollLatest(...)` → `onRowsRef.current(rows)`) — 11-PATTERNS.md §"useLatestPoll.ts expose schemaDrift"; keep the `onRowsRef` stable-identity idiom (:65-69) and tolerant `catch {}` (:110-112).
    - `packages/app/src/live/SyncDot.tsx:9-18` (color-token WCAG documentation discipline) + `:22-46` (current two-state render: `ONLINE_GREEN` #22C55E, `MUTED` #A1A1AA) — 11-PATTERNS.md §"SyncDot.tsx amber drift state" (D-08: distinct amber token, distinct aria-label, non-blocking, key-names-only detail).
    - `packages/app/src/live/mockLatest.ts` — the `?mockLatest=1` harness stub that must now return/emit a `PollResult`.
    - `.planning/phases/11-live-sync-prediction-correctness/11-RESEARCH.md` §"Pitfall 5" (four call sites) + §"Security V5/V7" (drift detail = key names only).
  </read_first>
  <action>
    Update `useLatestPoll` to `await pollLatest(...)` as a `PollResult`, pass `result.rows` to the existing `onRows` channel, and surface `result.schemaDrift`/`result.novelKeys` — widen `onRows` to `(result: PollResult) => void` OR add a second `onDrift(novelKeys)` callback (choose the smaller diff for the existing call site; record the choice). Preserve the stable `onRowsRef` and the tolerant `catch {}`. In `SyncDot.tsx`, add a `schemaDrift?: boolean` (and optional `novelKeys?: string[]`) prop and a THIRD amber state — define a new `DRIFT_AMBER` constant (default `#F59E0B`) with a WCAG ≥3:1-on-#0C0C10 justification comment matching the file's existing token discipline, a distinct `aria-label` (e.g. "Sync: API shape changed"), and a tap-for-detail affordance that shows novel key NAMES only (never editor values). The amber state is non-blocking — no modal, no banner, logging continues. Update `mockLatest.ts` to emit/return the `PollResult` shape (add a mock path that injects an extra unknown key so drift can be exercised). Update `useLatestPoll.test.tsx` and `mockLatest.test.ts` for the new shape + a drift-propagation assertion.
  </action>
  <verify>
    <automated>npx vitest run packages/app/test/useLatestPoll.test.tsx packages/app/test/mockLatest.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/app/src/live/useLatestPoll.ts` consumes `PollResult` and surfaces `schemaDrift`/`novelKeys` (no reference to the old bare-array return remains).
    - `packages/app/src/live/SyncDot.tsx` accepts `schemaDrift` and renders a distinct amber, non-modal third state with a documented `DRIFT_AMBER` token and a distinct aria-label; detail shows key names only.
    - `packages/app/src/live/mockLatest.ts` returns/emits a `PollResult` and can inject a novel key.
    - `npx vitest run packages/app/test/useLatestPoll.test.tsx packages/app/test/mockLatest.test.ts` exits 0.
  </acceptance_criteria>
  <done>The drift flag flows pollLatest → useLatestPoll → SyncDot as a quiet amber state; the poll hook and mock speak PollResult.</done>
</task>

<task type="auto">
  <name>Task 2: guardedRows in ShowView + drift wiring (LIVE-01 app)</name>
  <files>packages/app/src/show/ShowView.tsx, packages/app/test/adopt.test.tsx</files>
  <read_first>
    - `packages/app/src/show/ShowView.tsx:160-162` (poll wire / `setLatestRows`), `:180` (suggestions via `diffLatestAgainstTrail`), `:190` (fill-hints via `resolvePlaceholders`), `:198-204` (auto-bind via `bindShowFromLatest`, already passing `activeShow.date`) — 11-PATTERNS.md §"suggest.ts add guardLatestRows" anti-pattern (filter ONCE, not per consumer).
    - `.planning/phases/11-live-sync-prediction-correctness/11-RESEARCH.md` §"Pattern 1" (compute `guardedRows` once from `session.active`) + §"Per-Requirement Readiness LIVE-01" (data available at `ShowView.tsx:198`).
    - `packages/app/src/show/useShowSession.ts` — `session.active` is the `TrackedShow` carrying `showId` + `date` consumed here.
    - `packages/app/test/adopt.test.tsx` — the existing suggestion/adopt-path app test to extend with a stale-previous-night-row exclusion case.
  </read_first>
  <action>
    In `ShowView`, after `latestRows` is available and `session.active` exists, compute `const guardedRows = guardLatestRows(latestRows, { showId: session.active.showId, date: session.active.date })` ONCE (import `guardLatestRows` from `@guezzer/core`). Replace `latestRows` with `guardedRows` at all three consumer call sites: `diffLatestAgainstTrail` (:180), `resolvePlaceholders` (:190), and `bindShowFromLatest` (:198-204). Do NOT guard inside the consumers (single-filter discipline). Thread the `schemaDrift`/`novelKeys` from the Task-1 poll channel into `ShowView` state and pass to the `SyncDot` `schemaDrift` prop. Add an `adopt.test.tsx` case: a `latestRows` set containing a previous-show row (mismatched `show_id`/`showdate`) produces NO suggestion/fill-hint for that stale row after guarding.
  </action>
  <verify>
    <automated>npx vitest run packages/app/test/adopt.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `packages/app/src/show/ShowView.tsx` calls `guardLatestRows` exactly once and passes the guarded result to all three of `diffLatestAgainstTrail`, `resolvePlaceholders`, `bindShowFromLatest`; no consumer receives raw `latestRows`.
    - `ShowView` passes `schemaDrift` to `SyncDot`.
    - `npx vitest run packages/app/test/adopt.test.tsx` exits 0 including the stale previous-show-row exclusion case.
  </acceptance_criteria>
  <done>ShowView guards latest rows once before every consumer and surfaces schema drift on the SyncDot; a previous night's cached row can no longer leak into tonight's suggestions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Core `PollResult` → app render (SyncDot / suggestions) | Guarded rows + drift key-names cross into the render tier; must render as text only, never HTML/eval. |
| Editor content (`songname`/`venuename`) → SyncDot detail | Untrusted editor strings must never appear in the drift-detail affordance (key names only). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04-01 | Tampering (XSS) | Amber SyncDot drift detail | mitigate | Detail renders novel key NAMES only via React JSX escaping; never `dangerouslySetInnerHTML`, never editor values (SCHEMA §12). |
| T-11-04-02 | Denial of Service (UX) | Drift state blocks mid-set logging | mitigate | Amber state is non-modal/non-blocking (D-08); logging path is independent of SyncDot render. |
| T-11-04-03 | Spoofing | Previous-show/foreign row leaks into suggestions | mitigate | `guardLatestRows` applied once (LIVE-01) atop the core artist filter (LIVE-02) — both hold together. |
| T-11-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs — edits to existing app source + tests only. |
</threat_model>

<verification>
- `npx vitest run packages/app/test/useLatestPoll.test.tsx packages/app/test/mockLatest.test.ts packages/app/test/adopt.test.tsx` — green.
- `npm test` (full suite) green — this plan completes the `pollLatest` signature ripple started in 11-02.
- <human-check> Device UAT (VALIDATION.md Manual-Only): over an HTTPS tunnel with `?mockLatest=1` injecting an extra `latest` key, confirm SyncDot goes amber, tap shows key-name detail, and suggestions keep working; confirm a previous-night cached payload yields no stale suggestions on night 2. </human-check>
</verification>

<success_criteria>
- LIVE-01: previous-show rows are guarded out of suggestions/fill-hints/auto-bind in ShowView (single-filter).
- LIVE-03: an additive API field surfaces as a quiet amber SyncDot state instead of silently emptying suggestions.
</success_criteria>

<output>
Create `.planning/phases/11-live-sync-prediction-correctness/11-04-SUMMARY.md` when done. Record the chosen drift channel shape (widened `onRows` vs `onDrift`) and the `DRIFT_AMBER` token value for device UAT.
</output>
