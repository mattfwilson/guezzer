---
phase: 04-show-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/app/src/db/db.ts
  - packages/app/src/config.ts
  - packages/app/src/show/scoring.ts
  - packages/app/test/showSession.test.ts
  - packages/app/test/tally.test.ts
autonomous: true
requirements: [SHOW-11, SHOW-06, SHOW-07, SHOW-09, SHOW-03, DEX-01]
must_haves:
  truths:
    - "Starting a show writes a date-keyed tracked-show row that survives relaunch (provisional attendance, DEX-01)"
    - "Every confirmed song is written through to IndexedDB immediately and re-read exactly on relaunch (SHOW-11)"
    - "Exactly one tracked show is active at a time; End Show is required before starting the next (D-03/D-04)"
    - "Set break marks setNumber '2', encore marks 'e', entries snapshot the set number (SHOW-06)"
    - "Undo removes the most recent entry in one call (SHOW-07/D-15)"
    - "The running tally is derived from entries as hits/total and stays correct after any edit (SHOW-09/D-07)"
    - "The tracked-show row persists date + local sessionId only (showId null, no venue label); canonical show_id/venue binding is deferred to Phase 5/6 (D-05)"
  artifacts:
    - path: "packages/app/src/db/db.ts"
      provides: "Dexie version(2) trackedShows + trackedEntries tables, row types, and write helpers"
      contains: "this.version(2).stores"
    - path: "packages/app/src/config.ts"
      provides: "config.show tunables + config.copy.show strings"
      contains: "show:"
    - path: "packages/app/src/show/scoring.ts"
      provides: "classifyOutcome (hit-if-in-fan) + deriveTally pure helpers"
      exports: ["classifyOutcome", "deriveTally"]
  key_links:
    - from: "packages/app/src/db/db.ts"
      to: "packages/core/src/domain/types.ts (SetNumber)"
      via: "SetNumber union mirrored '1'|'2'|'e'"
      pattern: "\"1\" \\| \"2\" \\| \"e\""
    - from: "packages/app/test/showSession.test.ts"
      to: "packages/app/src/db/db.ts"
      via: "real Dexie writes under fake-indexeddb"
      pattern: "db\\.trackedEntries\\.(add|where)"
---

<objective>
Build the persistence + scoring substrate the whole Show Mode loop writes through to: the additive Dexie `version(2)` schema (tracked setlist + provisional attendance), the write helpers (start / log / undo / set-break / encore / rename / end), the app-side `config.show` tunables + `copy.show` strings, and the pure hit/miss + tally derivation. No UI in this plan — this is the source-of-truth layer every later slice reads.

Purpose: Deliver crash-proof write-through (SHOW-11) and provisional dex credit (DEX-01) — the two invariants that make show #1 safe — plus the set-structure (SHOW-06), undo (SHOW-07), and tally (SHOW-09) primitives, all Node-testable via `fake-indexeddb` before any component exists.
Output: `db.ts` grown to `version(2)`, `config.ts` extended, new `show/scoring.ts`, and two RED→GREEN test files.
</objective>

## Phase Goal (MVP user story)

**As a** KGLW showgoer, **I want to** log the setlist one-thumbed with every tap written through instantly, **so that** a force-quit never loses my night's state or my dex credit for show #1.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-show-mode/04-CONTEXT.md
@.planning/phases/04-show-mode/04-RESEARCH.md
@.planning/phases/04-show-mode/04-PATTERNS.md
@.planning/phases/04-show-mode/04-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dexie version(2) additive migration + tracked-show/entry types + write helpers</name>
  <files>packages/app/src/db/db.ts, packages/app/test/showSession.test.ts</files>
  <read_first>
    - packages/app/src/db/db.ts (the whole file — v1 schema at :39-42 and the documented version(2) hook at :44-49; NEVER rewrite version(1))
    - packages/app/test/db.test.ts (exact analog: real Dexie under fake-indexeddb, beforeEach clears each touched table, async it blocks)
    - packages/app/test/setup.ts (confirms `fake-indexeddb/auto` already imported — no new wiring)
    - packages/core/src/domain/types.ts:48 (the canonical `SetNumber = "1" | "2" | "e"` union to mirror)
    - .planning/phases/04-show-mode/04-RESEARCH.md §Pattern 1 (TrackedShow/TrackedEntry field shapes + single-active invariant + restore query) and §Pattern 5 (logSong/undoLast helpers)
    - .planning/phases/04-show-mode/04-PATTERNS.md "packages/app/src/db/db.ts — MODIFY" section (copy-from excerpt + JSDoc-per-interface style)
  </read_first>
  <action>
    Append (never rewrite v1) `this.version(2).stores({ trackedShows: "&sessionId, status, date", trackedEntries: "++id, sessionId, [sessionId+position]" })` after the version(1) block. Add `Table` field declarations `trackedShows!` and `trackedEntries!` next to `meta!`/`attendedShows!`. Declare and export interfaces `TrackedShow` (fields: sessionId, date, status, currentSetNumber, startedAt, showId:number|null) and `TrackedEntry` (fields: id?, sessionId, position, songId:number|null, songName, setNumber, outcome, shownFanSongIds:number[], isPlaceholder, loggedAt) plus the supporting unions `ShowStatus = "active"|"finalized"`, `SetNumber = "1"|"2"|"e"` (mirror core's union — re-declare with a comment pointing at packages/core/src/domain/types.ts:48, or import from @guezzer/core), `EntryOutcome = "hit"|"miss"`. Give each new interface a JSDoc block citing the D-0x rationale, matching the existing AttendedShow doc style. Add module-level async write helpers following the existing setMeta/getMeta idiom, each wrapping db.transaction("rw", ...): `startShow()` (assert no active row first per D-03, generate crypto.randomUUID sessionId, ISO YYYY-MM-DD date via D-01, status "active", currentSetNumber "1", showId null — this row IS the provisional attendance record per D-02/DEX-01); `logSong(sessionId, entry)` (compute next contiguous position = count+1, add — SHOW-11 write-through); `undoLast(sessionId)` (delete the max-position entry — SHOW-07/D-15); `markSetBreak(sessionId)` / `markEncore(sessionId)` (patch currentSetNumber to "2"/"e" per SHOW-06/D-04 — does NOT change status); `renameEntry(id, songId, songName)` (patch a "???" placeholder, isPlaceholder→false — D-14/D-15); `endShow(sessionId)` (set status "finalized", read-only — D-04); `getActiveShow()` (where status equals "active" first). Do NOT re-declare meta/attendedShows in version(2). Position is sort-only; deletion may leave a gap (acceptable per RESEARCH A5 — export re-numbers later).
    Write showSession.test.ts RED-first covering: write-through (logSong → row present immediately), restore + single-active (seed active show + entries, getActiveShow returns exact resume, startShow throws/refuses when one is already active), set structure (markSetBreak→"2", markEncore→"e", entries snapshot currentSetNumber, grouping by setNumber round-trips), undo (undoLast removes max-position entry), provisional attendance (startShow writes a date-keyed trackedShows row). beforeEach clears trackedShows + trackedEntries (add to the existing clear pattern). NAME the describe/it blocks so BOTH the broad plan filter (-t "showSession") AND the granular VALIDATION bindings resolve: the it names MUST contain the literal substrings write-through, restore, set-structure, undo, and attendance (e.g. it("write-through: logSong persists immediately"), it("restore: single active show resumes exactly"), it("set-structure: encore snapshots e and round-trips"), it("undo: removes max-position entry"), it("attendance: startShow writes a date-keyed row")). This test file is the single home of the set-structure/undo HELPER assertions; 04-06 only adds the ActionBar-wiring flow, not duplicate helper tests.
  </action>
  <verify>
    <automated>npx vitest run packages/app -t "showSession"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run packages/app -t "showSession"` passes (write-through, restore, single-active, set-structure, undo, attendance cases green).
    - Source assertion: `db.ts` contains `this.version(2).stores(` and does NOT modify the `this.version(1).stores(` call (v1 intact).
    - Source assertion: `SetNumber` in db.ts is exactly the closed union `"1" | "2" | "e"` (no "3").
    - Behavior assertion: after `logSong`, a `db.trackedEntries.where("sessionId").equals(id).count()` reflects the write with no explicit flush.
    - Behavior assertion: calling `startShow()` while an active row exists rejects (single-active invariant, D-03).
  </acceptance_criteria>
  <done>version(2) tables + typed write helpers exist and pass the RED→GREEN showSession suite; v1 untouched; SetNumber mirrors core.</done>
</task>

<task type="auto">
  <name>Task 2: config.show + copy.show block; pure hit/miss + tally derivation</name>
  <files>packages/app/src/config.ts, packages/app/src/show/scoring.ts, packages/app/test/tally.test.ts</files>
  <read_first>
    - packages/app/src/config.ts (whole file — the `export const config = {...} as const` idiom with JSDoc-per-key and nested `copy.*`; add to it, do not restructure)
    - .planning/phases/04-show-mode/04-UI-SPEC.md §"Config surface" (the exact keys + defaults: ORB_COUNT_MIN 5, ORB_COUNT_MAX 8, ORB_DROP_SCORE 0.02, WEAK_FAN_THRESHOLD 0.15, ORB_MIN_DIAMETER 56, TRAIL_VISIBLE_RECENT 4, TRAIL_COMPRESS_AT 30) and §"Copywriting Contract" (every Show-Mode copy string)
    - .planning/phases/04-show-mode/04-CONTEXT.md D-06/D-07/D-08 (hit = was in the shown fan; miss = search/???; single combined tally)
    - packages/app/src/db/db.ts (TrackedEntry shape from Task 1 — outcome/shownFanSongIds)
  </read_first>
  <action>
    Extend `config` with a `show` block holding the seven UI-SPEC tunables (keys above, JSDoc-per-key with the D-10/D-12/SHOW-08 citations) and a `copy.show` block mirroring the existing `copy.placeholders` shape, holding every 04-UI-SPEC Show-Mode string verbatim (Start Show, "Ready when you are", pre-show body, End Show, "Tap the opener", search placeholder, no-match copy, ??? / Unknown, Set break, Encore, Undo, weak-fan hint, tally zero-state, rename heading, wake-lock fallback, model-load-failure). Create `show/scoring.ts` exporting two pure functions: `classifyOutcome(confirmedSongId, shownFanSongIds): EntryOutcome` — returns "hit" iff confirmedSongId is in shownFanSongIds, else "miss" (D-06); a "???"/search log passes an id not in the fan → "miss" (D-08). `deriveTally(entries): { hits, total, pct }` — hits = count of outcome==="hit", total = entries.length, pct = total ? round(100*hits/total) : null (render "—" for null per zero-state). Pure, zero DOM, import types from db.ts.
    Write tally.test.ts covering: hit classification (song in fan → hit), miss classification (song not in fan / placeholder → miss), tally math over a mixed fixture, zero-state (0/0 → pct null).
  </action>
  <verify>
    <automated>npx vitest run packages/app -t "tally"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run packages/app -t "tally"` passes (classification + tally math + zero-state).
    - Source assertion: `config.show.WEAK_FAN_THRESHOLD === 0.15` and `config.show.TRAIL_COMPRESS_AT === 30`.
    - Source assertion: `config.copy.show` contains the Start Show and weak-fan hint strings (no hardcoded copy leaks into components later).
    - Behavior assertion: `classifyOutcome(id, fan)` returns "hit" only when `fan.includes(id)`.
  </acceptance_criteria>
  <done>config.show + copy.show exist with UI-SPEC defaults/strings; scoring.ts pure helpers pass the tally suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → IndexedDB | Only local, user-authored data crosses here; no network, no server in Phase 4 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | db.ts version(2) migration | mitigate | Additive-only migration; never rewrite version(1) so existing `meta`/`attendedShows` cannot be corrupted (verified by v1-intact acceptance criterion). |
| T-04-02 | Denial of Service | IndexedDB quota | accept | One show's setlist is tiny; `navigator.storage.persist()` already requested in Phase 3; JSON export backstop is Phase 5. |
| T-04-03 | Repudiation | crypto.randomUUID sessionId | accept | Non-security local id only (ASVS V3 n/a — no auth session). |
</threat_model>

<verification>
- `npx vitest run packages/app` green (new showSession + tally suites plus existing suites unaffected).
- Manual grep: `this.version(1).stores(` unchanged; `this.version(2).stores(` added.
</verification>

<success_criteria>
Dexie v2 write-through + restore + single-active + set-structure + undo + provisional-attendance all pass under fake-indexeddb; config.show/copy.show populated from UI-SPEC; scoring helpers pure and tested. No component/UI code introduced.
</success_criteria>

## Artifacts this phase produces (this plan)

- **Dexie tables:** `trackedShows` (`"&sessionId, status, date"`), `trackedEntries` (`"++id, sessionId, [sessionId+position]"`)
- **Row types:** `TrackedShow`, `TrackedEntry`, `ShowStatus`, `SetNumber`, `EntryOutcome` (exported from `db.ts`)
- **Write helpers:** `startShow`, `logSong`, `undoLast`, `markSetBreak`, `markEncore`, `renameEntry`, `endShow`, `getActiveShow`
- **Config:** `config.show.{ORB_COUNT_MIN, ORB_COUNT_MAX, ORB_DROP_SCORE, WEAK_FAN_THRESHOLD, ORB_MIN_DIAMETER, TRAIL_VISIBLE_RECENT, TRAIL_COMPRESS_AT}`; `config.copy.show.*`
- **Pure helpers:** `classifyOutcome`, `deriveTally` (`packages/app/src/show/scoring.ts`)
- **Files:** `packages/app/src/show/scoring.ts`, `packages/app/test/showSession.test.ts`, `packages/app/test/tally.test.ts`

<output>
Create `.planning/phases/04-show-mode/04-01-SUMMARY.md` when done.
</output>
