---
phase: 07-explore-mode-constellation
plan: 07
type: execute
wave: 6
depends_on: [07-06]
files_modified: []
autonomous: false
requirements: [EXPL-01, EXPL-02, EXPL-03, EXPL-04, EXPL-05, EXPL-06, DEX-05]
must_haves:
  truths:
    - "On a real phone, the full Explore loop works end to end: the sky renders + freezes, tap→bars→chain-hop walks the paths, filters shape the map, and the dex overlay is the opening view"
    - "Marking a show in the Dex tab recolors the constellation live (useLiveQuery reactivity proven on device) (DEX-05)"
    - "The whole surface is usable one-thumb in a dark venue — 44px targets, gesture suppression, no page-scroll fights"
  artifacts: []
  key_links: []
---

<objective>
End-of-phase on-device verification of the complete Explore Mode loop before `/gsd-verify-work`. This is the holistic human gate that the automated fixture tests and per-slice executor smokes cannot cover — the canvas interaction feel, live dex reactivity across tabs, and one-thumb dark-venue usability on real hardware.

Purpose: Explore is a browse surface with real interaction/perf risk (canvas + force sim on mobile). The early Plan 07-03 spike validated label readability + Full-catalog perf; this gate validates the assembled loop (bars, chain-hop, filters, dex overlay, live recolor) that only exists after Slice 4.
Output: A signed-off device verdict (or a gap list feeding `/gsd-plan-phase --gaps`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-explore-mode-constellation/07-UI-SPEC.md
@$HOME/.claude/projects/C--Users-mattf-git-guezzer/memory/device-uat-hosting.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Full Explore-loop device verification</name>
  <files>none (verification-only)</files>
  <read_first>
    - 07-UI-SPEC.md §"Layout & Interaction Contract" (the full interaction rule set to verify against)
    - MEMORY device-uat-hosting.md (HTTPS cloudflared tunnel with `--http-host-header localhost`)
    - .planning/phases/07-explore-mode-constellation/07-03-SUMMARY.md (the label/perf verdict already recorded — do not re-litigate; verify the assembled loop)
  </read_first>
  <what-built>
    Plans 07-01..07-06 delivered: the tested core engine, the settle-and-freeze constellation (Slice 1), the tap→ranked-bars sheet + focus-dim + chain-hop (Slice 2), the filter FAB with rotation/full toggle + edge slider (Slice 3), and the dex overlay with sighting rings + zoom-gated counts (Slice 4).
  </what-built>
  <action>
    Executor: build the app (`pnpm --filter @guezzer/app exec vite build`), serve it over the HTTPS cloudflared tunnel per MEMORY device-uat-hosting (include `--http-host-header localhost`), and give the owner the tunnel URL. Present the how-to-verify checklist, pause for the human verdict, and record the result (PASS or the concrete gap list) in the SUMMARY. Do NOT make code changes here — if gaps are reported, they feed `/gsd-plan-phase --gaps`, not an in-place fix.
  </action>
  <how-to-verify>
    Serve the built app over the HTTPS cloudflared tunnel (per MEMORY) and open it on the owner's phone. Verify, one thumb, in order:
    1. Open Explore → the sky renders as YOUR dex by default (caught nodes ringed green, unseen dimmed), settles, and FREEZES; Rotation is the default (~56 nodes).
    2. Tap a node → neighborhood lights, rest dims, gold ring, camera pans it up, the 40% sheet opens with ranked bars + honest count/date "why" lines. Tap a bar → it chain-hops (refocus + reload bars). Tap empty → clears + dismisses.
    3. Open the filter FAB → toggle Full catalog (more nodes, positions stable) → drag the edge slider (edges thin, nodes stay as free-floating stars, no sim restart) → toggle the dex overlay OFF (neutral tuning view) and back ON.
    4. Cross-tab live reactivity: go to the Dex tab, mark (or unmark) a show, return to Explore → the affected nodes' rings/silhouettes have updated with no manual refresh.
    5. Dark-venue feel: everything is one-thumb reachable, targets never feel sub-44px, no page-scroll/rubber-band fights, no jitter at rest.
    Report PASS, or a list of concrete gaps (which step, what went wrong).
  </how-to-verify>
  <verify>Human confirms the full loop on device; verdict recorded in the SUMMARY.</verify>
  <done>Human has verified all 5 checklist steps on real hardware and returned PASS or a gap list.</done>
  <resume-signal>Type "approved" if the full loop passes, or list the gaps (step number + issue) to feed `/gsd-plan-phase --gaps`.</resume-signal>
</task>

</tasks>

<artifacts_produced>
- none (verification-only plan; no new symbols or files)
</artifacts_produced>

<threat_model>
## Trust Boundaries

No new trust boundary — this plan runs and observes the already-built app on device.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-10 | Information disclosure | cloudflared tunnel | accept | Transient owner-only dev-server exposure for the test session (inherited Phase-4 device-UAT practice); torn down after. No new persistent surface. |

No code changes, no writes. No high-severity threat.
</threat_model>

<verification>
- Full suite green before the gate (`pnpm -w test --run`); production build succeeds
- Human sign-off recorded; any gaps captured for `--gaps` follow-up
</verification>

<success_criteria>
- The complete Explore loop (EXPL-01..06 + DEX-05) verified on real hardware
- Live cross-tab dex recolor proven; one-thumb dark-venue usability confirmed
</success_criteria>

<output>
Create `.planning/phases/07-explore-mode-constellation/07-07-SUMMARY.md` when done, recording the device verdict.
</output>
