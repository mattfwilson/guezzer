---
phase: 21-layout-layering-foundations
plan: 09
type: execute
wave: 4
depends_on: ["21-05", "21-07"]
files_modified:
  - packages/app/src/components/Sheet.tsx
  - packages/app/src/dex/ArchiveBrowser.tsx
  - packages/app/src/show/CometTrail.tsx
  - packages/app/src/dex/RecapView.tsx
  - packages/app/src/explore/NodeSheet.tsx
  - packages/app/src/map/MapView.tsx
  - packages/app/src/show/BingoPeekStrip.tsx
  - packages/app/test/cometTrail.test.tsx
  - packages/app/test/sheet.a11y.test.tsx
autonomous: true
requirements: [FOUND-02]

must_haves:
  truths:
    - "Every modal sheet's scroll container reads var(--gz-sheet-pad-bottom) — the three copy-pasted calc strings are gone"
    - "The RecapView page footer and the NodeSheet gutter read the owner's safe-area var and keep their exact shipped values"
    - "Every remaining bottom-anchored surface is either converted or carries a comment saying why it was not — including the peek strip, whose D-10 out-of-scope status is recorded in its own source"
  artifacts:
    - path: "packages/app/src/components/Sheet.tsx"
      provides: "the shared sheet primitive reading the owned padding value"
      contains: "var(--gz-sheet-pad-bottom)"
    - path: "packages/app/src/map/MapView.tsx"
      provides: "an audited, commented bottom-anchored surface (D-08 escape hatch)"
    - path: "packages/app/src/show/BingoPeekStrip.tsx"
      provides: "the recorded D-10 exemption — in-flow, never fixed, no bottom offset to unify (comment-only)"
  key_links:
    - from: "packages/app/src/components/Sheet.tsx"
      to: "packages/app/src/layout/bottomSpace.ts"
      via: "var(--gz-sheet-pad-bottom), inherited through document.documentElement by the portaled node"
      pattern: "var\\(--gz-sheet-pad-bottom\\)"
---

<objective>
Convert the sheet-padding family and the remaining non-tab-relative bottom surfaces onto the
owner's vars, and complete the D-08 audit so nothing bottom-anchored is left unexplained.

Purpose: FOUND-02. `calc(env(safe-area-inset-bottom) + 32px)` is copy-pasted into four files
(`Sheet.tsx:104`, `ArchiveBrowser.tsx:379`, `CometTrail.tsx:231`, and `RecapView.tsx:442` with a
16px variant). D-07 gives sheets their own owned value because a sheet **covers** the tab bar
rather than sitting above it — it must not be merged with the reserve. D-08 requires every
bottom-anchored surface to be audited, converted if tab-bar-relative, and commented if not. D-10
settles the one surface ROADMAP's FOUND-02 criterion names that is not bottom-anchored at all —
the peek strip — as out of scope; that exemption is recorded in its own source here so the
milestone audit does not read FOUND-02 as unmet. All
of these are `fixed`/`absolute` and therefore arithmetically correct today (RESEARCH Group B):
every conversion here is value-preserving.

Output: five converted surfaces, two audited-and-commented surfaces (`MapView` under D-08,
`BingoPeekStrip` under D-10), and render assertions on the two shipped sheet tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-layout-layering-foundations/21-CONTEXT.md
@.planning/phases/21-layout-layering-foundations/21-RESEARCH.md
@.planning/phases/21-layout-layering-foundations/21-UI-SPEC.md
@.planning/phases/21-layout-layering-foundations/21-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collapse the three copy-pasted sheet paddings onto --gz-sheet-pad-bottom (D-07)</name>
  <files>packages/app/src/components/Sheet.tsx, packages/app/src/dex/ArchiveBrowser.tsx, packages/app/src/show/CometTrail.tsx</files>
  <read_first>
    - packages/app/src/components/Sheet.tsx lines 95-115 — the bottom-sheet card's `style={{ zIndex: config.ui.z.sheet, paddingBottom: "calc(env(safe-area-inset-bottom) + 32px)" }}`; also read lines 60-90 to see that this component portals to `document.body`, which is why the ladder lives on `document.documentElement`
    - packages/app/src/dex/ArchiveBrowser.tsx around line 379 — the scroll container's `style={{ paddingBottom: "calc(env(safe-area-inset-bottom) + 32px)" }}`. **Plan 21-05 also edits this file** (`formatFullDate` in the rows and in the unmark-confirm aria-label) and is in this plan's `depends_on` for exactly that reason — it has already landed, so read from disk, leave its date changes intact, and expect the line number to have shifted
    - packages/app/src/show/CometTrail.tsx around line 231 — the same string
    - packages/app/src/layout/bottomSpace.ts — `--gz-sheet-pad-bottom` = `calc(var(--gz-safe-bottom) + SHEET_PAD_BOTTOM_PX px)`
  </read_first>
  <action>
    Replace the string `"calc(env(safe-area-inset-bottom) + 32px)"` with
    `"var(--gz-sheet-pad-bottom)"` at all three sites. The value is identical; this is a
    single-ownership change only (RESEARCH Group B — these are `fixed`/`absolute` surfaces that
    never saw the body double-count).

    Add a one-line comment at the `Sheet.tsx` site citing D-07: sheet bottom padding is
    deliberately **not** tab-bar-relative — a sheet covers the tab bar, it does not sit above it —
    so it composes from `--gz-safe-bottom`, never `--gz-chrome-reserve`. Add a second line noting
    that this value is inherited from `document.documentElement`, which is why it still resolves
    inside a `createPortal(…, document.body)` subtree.

    Change nothing else in these files: `Sheet.tsx`'s portal, focus-trap hooks, `dialogProps`,
    early-return guards, scrim classes and both `zIndex` expressions stay byte-identical — plans
    21-11 and 21-12 own the layering work, and D-22 keeps the five hand-rolled surfaces
    hand-rolled.
  </action>
  <verify>
    <automated>cd packages/app &amp;&amp; npx vitest run --project @guezzer/app test/sheet.a11y.test.tsx test/cometTrail.test.tsx test/archiveBrowser.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'calc(env(safe-area-inset-bottom) + 32px)' packages/app/src` returns 0
    - `grep -rc 'var(--gz-sheet-pad-bottom)' packages/app/src/components/Sheet.tsx packages/app/src/dex/ArchiveBrowser.tsx packages/app/src/show/CometTrail.tsx` returns ≥ 1 for each
    - `git diff packages/app/src/components/Sheet.tsx` touches only the `paddingBottom` value and adjacent comments — `createPortal`, `useFocusTrap`, `useDialogDismiss`, both `zIndex` expressions and the two early-return guards are unchanged
    - `npm run build --workspace packages/app` succeeds
  </acceptance_criteria>
  <done>One owned value backs every modal sheet's bottom padding; the copy-paste is gone and the sheet primitive is otherwise untouched.</done>
</task>

<task type="auto">
  <name>Task 2: RecapView page footer, NodeSheet gutter, and the two audit entries — MapView (D-08) and BingoPeekStrip (D-10)</name>
  <files>packages/app/src/dex/RecapView.tsx, packages/app/src/explore/NodeSheet.tsx, packages/app/src/map/MapView.tsx, packages/app/src/show/BingoPeekStrip.tsx</files>
  <read_first>
    - packages/app/src/dex/RecapView.tsx around line 442 — `style={{ paddingBottom: "calc(env(safe-area-inset-bottom) + 16px)" }}`; this is a full-screen opaque PAGE footer at the `z.page` tier, not a sheet. **Plan 21-05 also edits this file** (the `formatFullDate` subline conversion) and is in this plan's `depends_on` for exactly that reason — it has already landed, so read from disk, leave its date changes intact, and expect the line number to have shifted
    - packages/app/src/explore/NodeSheet.tsx lines 143-160 — the `fixed inset-x-0 bottom-0` dialog with `paddingBottom: "env(safe-area-inset-bottom)"`, `aria-modal={false}`, and `touchAction: "none"`
    - packages/app/src/map/MapView.tsx lines 460-475 — the `absolute bottom-0 left-0 right-0 … px-3 pb-3 pt-6` status-chip strip with `zIndex: config.ui.z.content` and its gradient background; and line ~213, the pan/zoom stage's `transform: translate(...) scale(...)`
    - packages/app/src/show/BingoPeekStrip.tsx lines 1-25 — the header doc block whose opening sentence already records that the strip lives in the show column and is NEVER fixed; this is the file's only edit site in the whole phase, and it is comment-only
    - .planning/phases/21-layout-layering-foundations/21-RESEARCH.md §Bottom-Space Call-Site Inventory Groups B8, B9, C1 and **C2** — C2 is the peek strip, "confirmed out of scope (D-10) — grep shows no bottom offset of any kind"
  </read_first>
  <action>
    **`RecapView.tsx`** — replace with `"calc(var(--gz-safe-bottom) + 16px)"`. Byte-identical.
    Add a comment: this is a full-screen opaque **page** footer at `config.ui.z.page`, not a
    modal sheet, so it deliberately keeps its own 16px rather than adopting
    `--gz-sheet-pad-bottom` (UI-SPEC §Surfaces converted). It still eliminates the raw `env()`.

    **`NodeSheet.tsx`** — replace `"env(safe-area-inset-bottom)"` with `"var(--gz-safe-bottom)"`.
    Byte-identical. Add a comment: the NodeSheet is `fixed bottom-0` and **covers** the tab bar,
    so it is **not** tab-bar-relative — it composes from `--gz-safe-bottom`, never
    `--gz-chrome-reserve`. Leave `aria-modal={false}`, `touchAction: "none"`, the drag/snap
    geometry and the `zIndex` untouched; D-26 carries the non-modal exemption by name and plan
    21-12 owns the portal question for this surface.

    **`MapView.tsx`** — this is a D-08 **audit**, not a conversion. Confirm from the source that
    the status-chip strip is `absolute` inside the MapView stage container (not `fixed`, not
    viewport-anchored) and that the stage itself sits inside `<main>`'s already-reserved area, so
    it is not tab-bar-relative and has no inset to own. Also confirm the strip is **outside** the
    `transform`ed pan/zoom stage at ~line 213 (a transform ancestor would make any `fixed`
    descendant position relative to it — strictly worse than mispainting, D-28). Then **leave the
    code unchanged** and add a comment above the strip recording: the audit outcome, that it is
    `absolute` within the map stage rather than viewport-anchored, that it therefore intentionally
    composes no `--gz-*` value, and the decision ID (D-08's escape hatch). If the audit finds the
    strip IS viewport-anchored or IS inside the transformed stage, convert it to
    `bottom: var(--gz-chrome-reserve)` instead and say so in the SUMMARY.

    **`BingoPeekStrip.tsx`** — the second audit entry, and **comment-only**: change no code.
    ROADMAP's FOUND-02 success criterion 2 names the "peek strip" among the surfaces to unify, but
    **D-10** records that wording as an overreach — the strip is in-flow in the show column and
    NEVER `fixed` (its own header doc says so in its first sentence), and its expanded panel is
    absolutely positioned inside its own `relative` container on the `z.peek` tier, so it has no
    bottom offset to unify and nothing to compose from the owner. Add one comment to the existing
    header doc block recording exactly that and citing **D-10** and FOUND-02, so the exemption
    lives in an executed artifact rather than only in the planning record — D-10's own stated
    purpose is that the milestone audit not read this as unmet. Add no `--gz-*` read; do not touch
    the strip's layout, its `role="link"` single-tap-target shape, its `min-h-11` tap zone or its
    `z.peek` panel.
  </action>
  <verify>
    <automated>cd packages/app &amp;&amp; npx vitest run --project @guezzer/app test/recapView.test.tsx test/map</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc 'env(safe-area-inset-bottom)' packages/app/src/dex/RecapView.tsx packages/app/src/explore/NodeSheet.tsx` returns 0 for both
    - `RecapView.tsx` contains `calc(var(--gz-safe-bottom) + 16px)`; `NodeSheet.tsx` contains `var(--gz-safe-bottom)`
    - `MapView.tsx` contains a comment naming D-08 and stating the audit outcome for the status-chip strip
    - `BingoPeekStrip.tsx` contains a comment naming D-10 (and FOUND-02) stating the strip is in-flow, never `fixed`, and has no bottom offset to unify; `git diff packages/app/src/show/BingoPeekStrip.tsx` shows comment lines only — no JSX, class or style change
    - `git diff packages/app/src/explore/NodeSheet.tsx` touches only the `paddingBottom` value and adjacent comments — `aria-modal`, `touchAction`, `height`, `transition` and `zIndex` are unchanged
    - `npm run build --workspace packages/app` succeeds
  </acceptance_criteria>
  <done>The two remaining non-tab-relative surfaces read owned values; both deliberately unconverted surfaces — MapView's status-chip strip (D-08) and the peek strip (D-10) — carry their reasons in the source.</done>
</task>

<task type="auto">
  <name>Task 3: Render assertions for the converted sheet surfaces</name>
  <files>packages/app/test/cometTrail.test.tsx, packages/app/test/sheet.a11y.test.tsx</files>
  <read_first>
    - packages/app/test/sheet.a11y.test.tsx (whole file) — the 8 shipped a11y cases, the `mountAppContent` fixture, the `afterEach(cleanup)` + manual DOM teardown, and the raw-DOM query idiom (`document.querySelector(".bg-black\\/50")`, `dialog.className`)
    - packages/app/test/cometTrail.test.tsx — the existing CometTrail render harness
    - packages/app/src/components/Sheet.tsx and packages/app/src/show/CometTrail.tsx — the converted style strings
  </read_first>
  <action>
    Extend both test files with a FOUND-02 / D-07 assertion block whose doc comment states that
    these conversions are value-preserving (RESEARCH Group B) and that the test exists to stop a
    future edit from re-introducing a hand-written `calc(env(...))`.

    In `sheet.a11y.test.tsx`: render an open bottom-sheet `<Sheet>` and assert the dialog
    element's **`style` attribute string** contains `var(--gz-sheet-pad-bottom)` and does not
    contain `env(`. Assert against `getAttribute("style")` rather than
    `el.style.paddingBottom` — jsdom's CSS parser may not round-trip a `var()` value through a
    typed longhand property. Leave all 8 shipped a11y cases untouched.

    In `cometTrail.test.tsx`: the same assertion against the CometTrail scroll container.

    Do not add a source-wide scan here — the D-12 guard that fails on any remaining `4rem`,
    `64px`, `bottom-16` or bare bottom `env()` in `packages/app/src` lands in plan 21-10, once the
    toast family is converted and the codebase can actually satisfy it.
  </action>
  <verify>
    <automated>cd packages/app &amp;&amp; npx vitest run --project @guezzer/app test/sheet.a11y.test.tsx test/cometTrail.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Both test files contain an assertion on `getAttribute("style")` for `var(--gz-sheet-pad-bottom)`
    - Both contain a negative assertion that the style attribute does not contain `env(`
    - `test/sheet.a11y.test.tsx`'s 8 shipped a11y cases are unmodified (`git diff` shows additions only)
    - `npm test` passes with no new failures against the 954-test baseline
  </acceptance_criteria>
  <done>The sheet-padding conversion is locked by render assertions on the shared primitive and one hand-rolled consumer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `document.documentElement` custom properties → portaled sheet subtree | The sheet padding value must survive the portal boundary or every sheet silently loses its bottom gutter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-23 | Denial of Service (usability) | `Sheet.tsx` inside `createPortal(…, document.body)` | mitigate | `--gz-sheet-pad-bottom` is set on `document.documentElement` (plan 21-07), from which both `#root` and `document.body` portals inherit. RESEARCH Pitfall 1 warns that a `#root` target would silently collapse this to `0` on every sheet; plan 21-07's Pitfall-1 test asserts the target element, and the `?layerRepro=1` desktop pass would show it immediately. |
| T-21-24 | Denial of Service (usability) | `MapView` status-chip strip left unconverted | accept | Audited as `absolute` inside the map stage and outside the transformed pan/zoom container, therefore not tab-bar-relative and holding no inset. D-08's escape hatch requires a comment, which Task 2 adds. If the audit contradicts this, the task converts it instead. |
| T-21-25 | Tampering | `Sheet.tsx` a11y contract | mitigate | The edit is scoped to one style value; the `git diff` acceptance criterion pins `createPortal`, the focus-trap/dismiss hooks, `dialogProps` and both early-return guards as unchanged, and the 8 shipped a11y cases must pass unmodified. |
| T-21-26 | Repudiation | `BingoPeekStrip` recorded as out of scope | mitigate | ROADMAP's FOUND-02 criterion names the "peek strip", so an exemption living only in planning docs reads as an unmet requirement at milestone audit. D-10's finding (in-flow, never `fixed`, no bottom offset to unify) is written into the component's own header doc as a comment-only change, alongside the D-08 `MapView` entry. |
| T-21-SC | Tampering | npm installs | mitigate | Zero packages added, removed or upgraded (`21-RESEARCH.md` §Package Legitimacy Audit). No install task; `package.json` is not in `files_modified`. |
</threat_model>

<verification>
- `npm test` green (baseline 125 files / 954 tests)
- `npm run build --workspace packages/app` succeeds
- `grep -rn 'env(safe-area-inset-bottom)' packages/app/src` now returns only `styles.css` and the five toast files plan 21-10 owns
</verification>

<success_criteria>
- The three copy-pasted `calc(env(...) + 32px)` strings are collapsed to one owned value
- The page footer and the NodeSheet gutter compose from `--gz-safe-bottom` with unchanged values
- Both deliberately unconverted surfaces carry their reasons in the source — `MapView` under D-08, `BingoPeekStrip` under D-10
- No shipped a11y or drag behavior changed
</success_criteria>

<artifacts_this_phase_produces>
Symbols, files and flags newly created by Phase 21 (all plans). Nothing below exists on `master`
before this phase.

**CSS custom properties (new):** `--gz-safe-bottom`, `--gz-tab-bar-h`, `--gz-overlay-inset`,
`--gz-chrome-reserve`, `--gz-content-reserve`, `--gz-sheet-pad-bottom`, `--gz-fab-offset`

**Config keys (new):** `config.ui.bottomSpace`, `config.ui.bottomSpace.TAB_BAR_HEIGHT_REM`,
`config.ui.bottomSpace.FAB_CLEARANCE_PX`, `config.ui.bottomSpace.SHEET_PAD_BOTTOM_PX`,
`config.copy.tabs` (`show`/`explore`/`map`/`dex`/`games`), `config.copy.presence.activity`,
`config.copy.presence.activityUnknown`

**New modules and exports:** `packages/app/src/layout/bottomSpace.ts`
(`BOTTOM_SPACE_VAR_NAMES`, `bottomSpaceVarEntries`, `applyBottomSpaceVars`,
`useBottomSpaceVars`); `packages/app/src/dex/formatDate.ts` (`formatFullDate`, plus the re-homed
`formatMonYear`); `packages/app/src/dev/layerRepro.tsx` (`isLayerReproEnabled`,
`LayerReproToast`); `packages/app/src/dev/LayoutProbe.tsx` (`isLayoutProbeEnabled`,
`LayoutProbe`, `readSafeAreaInsets`); `packages/app/src/dex/FriendRow.tsx`
(`presenceActivityLabel`); `packages/app/src/dex/shareCard.ts` (module-private
`composeFooterLine`)

**New test-local helpers:** `packages/app/test/layerOrder.test.tsx` (`createsStackingContext`,
`ancestorsUpToBody`)

**Changed signatures:** `showBottomFabOffset(stripSlotReserved: boolean)` (was
`stripHasContent`); `FabMenu` / `OrbitStage` prop `stripSlotReserved` (was `stripHasContent`);
`data-strip-slot-reserved` DOM attribute (was `data-strip-has-content`); `PresenceActivitySlot`
gains an `online?: boolean` prop

**URL flags (new):** `?layerRepro=1`, `?layoutProbe=1`

**New test files:** `packages/app/test/layerRepro.test.tsx`,
`packages/app/test/bottomSpace.test.ts`, `packages/app/test/layerOrder.test.tsx`,
`packages/app/test/formatDate.test.ts`, `packages/app/test/presenceLabels.test.ts`,
`packages/app/test/setlistView.test.tsx`

**New docs:** `.planning/phases/21-layout-layering-foundations/21-HUMAN-UAT.md`,
`.planning/todos/pending/2026-07-24-simultaneous-bottom-overlay-stacking.md`

**Removed by this phase:** `packages/app/src/dex/formatMonYear.ts` (renamed to `formatDate.ts`);
the `padding-bottom: env(safe-area-inset-bottom)` declaration on `body` in `styles.css`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/21-layout-layering-foundations/21-09-SUMMARY.md` when done.
</output>
