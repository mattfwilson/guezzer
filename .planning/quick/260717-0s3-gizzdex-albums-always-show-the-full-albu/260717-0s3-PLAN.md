---
quick_id: 260717-0s3
title: GizzDex Albums — always show the full grayed shelf; Mark-attended Shows-only
created: 2026-07-17
mode: quick
---

# Quick Task 260717-0s3

Implements todo `2026-07-17-gizzdex-albums-always-show-full-grayed-grid-mark-attended-on`.
Make the GizzDex Albums toggle always render the full album shelf (zero-catch
covers grayed via §B4) instead of an EmptyState, which also removes the
Mark-attended CTA from Albums (it lived only in the empty branch) — leaving that
CTA on the Shows toggle only.

## Task 1: Always render AlbumGrid on the Albums toggle

- **files:** `packages/app/src/dex/DexView.tsx`
- **action:**
  - Replace the `segment === "albums"` branch's `emptyDex ? <EmptyState…> :
    <AlbumGrid…>` ternary with **always** `<AlbumGrid dex={dex} albums={albums}
    onOpen={setOpenAlbumKey} />`.
  - Remove the now-orphaned `const emptyDex = dex.completion.caught === 0;`.
  - Remove the local `EmptyState` component + `EmptyStateProps` interface (only
    used by the removed branch).
  - Remove `type ReactNode` from the react import (only used by `EmptyStateProps`).
  - Keep `MarkCta` (still used by the Shows segment) and `Plus` import.
  - `config.copy.dex.emptyHeading`/`emptyBody` become unreferenced — harmless
    (config object keys, not lint-visible); leave them.
- **verify:** `AlbumGrid` already dims zero-catch cards (40% + grayscale, §B4,
  `AlbumGrid.tsx:77-81`) and has no empty guard, so a fully-empty dex renders the
  whole shelf grayed. Typecheck + tests clean; run the app with an empty dex.
- **done:** Albums toggle shows the full grayed shelf at zero catches; no
  empty-state text; no Mark-attended button on Albums; Shows keeps its CTA.

## Out of scope

Shows segment (unchanged), DexHeader, tier colors, the separate GizzDex header
share-icon/toggle-color todo. Routes/storage untouched.
