---
phase: 02-transition-matrix-model-backtest
plan: 04
type: execute
wave: 4
depends_on: [02-03]
files_modified:
  - packages/core/src/eval/holdout.ts
  - packages/core/src/eval/backtest.ts
  - packages/core/src/index.ts
  - packages/core/test/eval/backtest.test.ts
  - packages/core/test/fixtures/synthetic-multitour.json
  - packages/core/test/fixtures/synthetic-multitour.meta.json
autonomous: true
requirements: [EVAL-01, EVAL-03, EVAL-05]
must_haves:
  truths:
    - "The backtest holds out the most recent COMPLETE tour, identified by the tour containing the latest-dated show (never max tourId)"
    - "Evaluation is walk-forward: each held-out show is scored against a matrix rebuilt as-of an exclusive (date, showOrder) bound just before it"
    - "The backtest reports top-1 / top-5 / top-10 next-song hit rates, overall and split by hard-segue vs free-choice"
    - "Placeholder targets, set-openers, and single-song sets are excluded from eval transitions"
    - "The whole backtest runs as pure core functions with zero browser dependencies"
  artifacts:
    - path: "packages/core/src/eval/holdout.ts"
      provides: "findHoldoutShows — most-recent-complete-tour identification"
      exports: ["findHoldoutShows"]
    - path: "packages/core/src/eval/backtest.ts"
      provides: "runBacktest — walk-forward + top-k metrics + split"
      exports: ["runBacktest"]
    - path: "packages/core/test/eval/backtest.test.ts"
      provides: "Wave-0 backtest scaffold (holdout, metrics) with a synthetic multi-tour fixture"
  key_links:
    - from: "packages/core/src/eval/backtest.ts"
      to: "packages/core/src/model/matrix.ts"
      via: "rebuild matrix as-of each held-out show"
      pattern: "buildMatrix\\("
    - from: "packages/core/src/eval/backtest.ts"
      to: "packages/core/src/model/predict.ts"
      via: "score each within-set transition"
      pattern: "predict\\(|scoreCandidate\\("
    - from: "packages/core/src/eval/backtest.ts"
      to: "packages/core/src/eval/holdout.ts"
      via: "identify the holdout tour"
      pattern: "findHoldoutShows\\("
---

<objective>
Deliver the walk-forward backtest core (EVAL-01, D-12/D-13): `findHoldoutShows` identifies the most-recent-complete tour by the latest-dated show's tour (never max tourId), and `runBacktest` rebuilds a leak-free matrix as-of each held-out show, scores every within-set transition, and reports top-1/5/10 hit rates overall and split by hard-segue vs free-choice. Pure core, zero browser deps (EVAL-03).

This is MVP slice 3 (part A): the backtest leg produces the first trust numbers. Ablation + the paired report CLI follow in Plan 05.

Purpose: The non-negotiable trust gate — honestly measure whether the model can be relied on live, exercising rotation suppression and already-played on consecutive tour nights exactly as real use does.
Output: `eval/holdout.ts`, `eval/backtest.ts` (metrics-only for now), barrel exports, Wave-0 `backtest.test.ts`, and a synthetic multi-tour fixture with a hand-computed holdout + top-k.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-transition-matrix-model-backtest/02-CONTEXT.md
@.planning/phases/02-transition-matrix-model-backtest/02-RESEARCH.md
@.planning/phases/02-transition-matrix-model-backtest/02-PATTERNS.md
@.planning/phases/02-transition-matrix-model-backtest/02-03-SUMMARY.md
</context>

<artifacts_produced>
Symbols this plan creates (exclude from drift verification — they are new):
- **Functions** (`eval/holdout.ts`): `findHoldoutShows(corpus, tourIdSentinel) → NormalizedShow[]`.
- **Functions** (`eval/backtest.ts`): `runBacktest(corpus, cfg) → BacktestResult` (metrics + split; the `ablation` array is populated in Plan 05 — leave it empty `[]` here or behind a flag), plus internal helpers `evalTransitions`, `hitAtK`, `splitBy`.
- Barrel re-exports of `runBacktest`, `findHoldoutShows`.
- **Files**: `test/fixtures/synthetic-multitour.json` (+ `.meta.json`), `test/eval/backtest.test.ts`.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: findHoldoutShows + synthetic multi-tour fixture</name>
  <files>packages/core/src/eval/holdout.ts, packages/core/src/index.ts, packages/core/test/eval/backtest.test.ts, packages/core/test/fixtures/synthetic-multitour.json, packages/core/test/fixtures/synthetic-multitour.meta.json</files>
  <read_first>
    - packages/core/src/ingest/census.ts (pure-reducer analog + module doc-comment + `.ts` imports)
    - packages/core/src/config.ts (`tourIdSentinel` — use the config value, never a literal 1)
    - packages/core/src/domain/types.ts (NormalizedShow.tourId / date / showOrder)
    - packages/core/test/fixtures/2017-segues.meta.json (the `.meta.json` sidecar template)
    - packages/core/test/normalize.test.ts (fixture-normalize + numbered-assertion pattern)
    - .planning/phases/02-transition-matrix-model-backtest/02-RESEARCH.md (M5 holdout lines 212-216; holdout body lines 280-289; Pitfall 3 tourId non-monotonic lines 166/250-253)
    - .planning/phases/02-transition-matrix-model-backtest/02-PATTERNS.md (§ holdout.ts lines 228-240)
  </read_first>
  <behavior>
    - Test "holdout identification" (D-12): `findHoldoutShows` returns exactly the shows whose `tourId` equals the tourId of the latest-dated show; on the synthetic fixture (built so max `tourId` is NOT the chronologically-latest tour — mirroring the real corpus's non-monotonic tourId) it returns the chronologically-latest tour's shows, NOT the max-id tour's.
    - Test "holdout sentinel guard": if the latest show's tourId equals `config.tourIdSentinel`, `findHoldoutShows` throws a loud error (no complete-tour holdout exists).
  </behavior>
  <action>
    Create `eval/holdout.ts` exporting `findHoldoutShows(corpus, tourIdSentinel = config.tourIdSentinel)`: reduce `corpus.shows` to the show with max `(date, showOrder)`, read its `tourId`, throw `new Error(...)` if it equals `tourIdSentinel` (a one-off, no complete-tour holdout — fail loudly per the Phase 1 error convention), else return all shows with that `tourId`. Identify by the LATEST-DATED show's tour, NEVER `max(tourId)` — tourId is non-monotonic (Pitfall 3, verified: tour 57 starts 2025-05-18 but tour 58 starts 2024-11-01). Pure, no I/O.

    Create the `synthetic-multitour.json` raw-row fixture: several shows across at least two tours where the max `tourId` deliberately is NOT the chronologically-latest tour (so a `max(tourId)` bug fails the test), with a known within-set transition structure. Include a `.meta.json` sidecar (sourceFile/showId/showdate/why + the hand-computed expected holdout tour and top-k for the Task 2 metrics test). This fixture is reused by Task 2 and by Plan 05's ablation test (D-17 / EVAL-05 — hand-computed known outputs).

    Create `test/eval/backtest.test.ts` with the two `behavior` tests (normalize the fixture, call `findHoldoutShows`). Add `findHoldoutShows` to the barrel `index.ts`.
  </action>
  <verify>
    <automated>npx vitest run packages/core test/eval/backtest.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run packages/core -t "holdout identification"` passes — returns the latest-dated tour's shows, not the max-tourId tour's.
    - `npx vitest run packages/core -t "holdout sentinel"` passes — throws when the latest show is a one-off (`tourIdSentinel`).
    - `findHoldoutShows` is exported from `index.ts`; the synthetic multi-tour fixture + sidecar exist; `npx tsc -p packages/core --noEmit` passes.
  </acceptance_criteria>
  <done>findHoldoutShows correctly identifies the most-recent-complete tour via latest date (not max id); synthetic multi-tour fixture exists; two tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: runBacktest — walk-forward loop, top-k metrics, hard-segue/free-choice split</name>
  <files>packages/core/src/eval/backtest.ts, packages/core/src/index.ts, packages/core/test/eval/backtest.test.ts</files>
  <read_first>
    - packages/core/src/ingest/census.ts (the "one exported *Result interface + interior sub-interfaces + one top-level pure fn" batch-derivation shape lines 57-81/149)
    - packages/core/src/eval/holdout.ts (findHoldoutShows from Task 1)
    - packages/core/src/model/matrix.ts (buildMatrix + the AsOfBound exclusive-tuple signature)
    - packages/core/src/model/predict.ts (predict / scoreCandidate — reuse the single scoring code path)
    - packages/core/src/domain/types.ts (BacktestResult / BacktestSplit shapes from Plan 01)
    - packages/core/test/fixtures/synthetic-multitour.json (+ .meta.json expected top-k from Task 1)
    - .planning/phases/02-transition-matrix-model-backtest/02-RESEARCH.md (M5 walk-forward lines 212-216; M7 metrics + eval exclusions lines 221-225; D-13 split; Pitfall 3 exclusive tuple bound)
    - .planning/phases/02-transition-matrix-model-backtest/02-PATTERNS.md (§ backtest.ts lines 212-224)
  </read_first>
  <behavior>
    - Test "backtest metrics" (EVAL-01, D-13): on the synthetic multi-tour fixture with hand-computed expected values, `runBacktest` returns `overall.top1/top5/top10` matching the hand-computed hit rates, plus `evalTransitionCount` matching the number of eligible within-set transitions in the holdout.
    - Test "hard-segue / free-choice split" (D-13): eval transitions are partitioned by whether the ACTUAL transition was notated (`A.transitionKind === "segue"`); `hardSegue` and `freeChoice` splits each carry their own top-k and `n`, and `hardSegue.n + freeChoice.n === overall.n`.
    - Test "eval-target exclusions" (M7): transitions where `B.isPlaceholder`, set-openers (no within-set predecessor), and single-song sets contribute 0 eval transitions.
    - Test "no leakage" (M5): for the first held-out show, the as-of matrix contains only strictly-prior shows — a transition unique to the holdout show is not "learned" from itself.
  </behavior>
  <action>
    Create `eval/backtest.ts` exporting `runBacktest(corpus, cfg = config)` returning `BacktestResult` (follow the `census.ts` batch-derivation shape). Algorithm (M5/D-12): (1) `findHoldoutShows(corpus)`; (2) process held-out shows in `(date, showOrder)` order; (3) for each held-out show S, `buildMatrix(corpus, asOf = { date: S.date, showOrder: S.showOrder, inclusive: false })` — the EXCLUSIVE tuple bound so S's own transitions are never in training but tour-S earlier nights ARE (this is what exercises rotation suppression, MODL-06); (4) within S, walk each within-set adjacent transition A→B where B is not a placeholder and A has a within-set predecessor context; the current context = A plus the in-progress trail (songs before A in S) plus `recentShowSongSets` from the last `rotationWindowShows` prior tour nights; (5) `predict` ranked candidates, record whether true B ∈ top-k for k ∈ {1,5,10} (M7 top-k hit rate); (6) split each eval transition into `hardSegue` (actual A→B was notated, `A.transitionKind === "segue"`) vs `freeChoice`, accumulate `BacktestSplit` counts for `overall`/`hardSegue`/`freeChoice`. Skip set-openers and single-song sets (M7). Populate the `BacktestResult` header; leave `ablation: []` (Plan 05 fills it). Deterministic accumulation order (sorted iteration). Export `runBacktest` from the barrel.

    Extend `test/eval/backtest.test.ts` with the four `behavior` tests using the synthetic fixture's hand-computed expected values.
  </action>
  <verify>
    <automated>npx vitest run packages/core test/eval/backtest.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run packages/core -t "backtest metrics"` passes — top-1/5/10 match hand-computed values on the synthetic fixture.
    - `npx vitest run packages/core -t "hard-segue"` (split test) passes — `hardSegue.n + freeChoice.n === overall.n`, each split has its own top-k.
    - `npx vitest run packages/core -t "eval-target exclusions"` passes — placeholder targets / set-openers / single-song sets excluded.
    - `npx vitest run packages/core -t "no leakage"` passes — as-of matrix strictly precedes the held-out show.
    - `runBacktest` exported from `index.ts`; `npx tsc -p packages/core --noEmit` passes.
  </acceptance_criteria>
  <done>runBacktest performs leak-free walk-forward evaluation, reports top-1/5/10 overall + hard-segue/free-choice split with correct exclusions; four tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corpus.json → runBacktest | Committed, Phase-1 zod-validated artifact read as `NormalizedCorpus`; no external input to the pure backtest fn |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Tampering | Off-by-one leakage in the as-of tuple bound | mitigate | Exclusive `(date, showOrder)` predicate with a unit "no leakage" test asserting the held-out show's own transitions are absent from its training matrix (Pitfall 3) |
| T-02-09 | Denial of Service | Corpus with no complete tour (all one-offs) | mitigate | `findHoldoutShows` throws loudly (T-02 error convention) rather than emitting a meaningless empty backtest |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase (RESEARCH § Package Legitimacy Audit: zero new deps) |
</threat_model>

<verification>
- `npx vitest run packages/core` — full suite green (matrix + predict + backtest).
- Walk-forward split proven leak-free by the "no leakage" test.
- `npx tsc -p packages/core --noEmit` clean.
</verification>

<success_criteria>
- Backtest runs from pure core (zero browser deps), holds out the most recent complete tour, and reports top-1/5/10 hit rates overall and split by hard-segue vs free-choice (Phase success criterion 3 — metrics leg; CLI + ablation in Plan 05).
- Fixture unit tests with known expected outputs pass (success criterion 5).
</success_criteria>

<output>
Create `.planning/phases/02-transition-matrix-model-backtest/02-04-SUMMARY.md` when done.
</output>
