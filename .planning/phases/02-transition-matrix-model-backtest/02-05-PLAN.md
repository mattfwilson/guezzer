---
phase: 02-transition-matrix-model-backtest
plan: 05
type: execute
wave: 5
depends_on: [02-04]
files_modified:
  - packages/core/src/eval/backtest.ts
  - packages/core/src/cli/run-backtest.ts
  - packages/core/src/index.ts
  - packages/core/test/eval/backtest.test.ts
  - data/backtest-report.md
  - data/backtest.json
autonomous: true
requirements: [EVAL-02, EVAL-03]
must_haves:
  truths:
    - "The backtest runs the full model once, then re-runs with each signal individually disabled and reports each variant's hit rates plus the delta vs the full model"
    - "Ablation is report-only — no automated go/no-go gate in Phase 2 (the owner reads the numbers)"
    - "run-backtest is a Node CLI with zero browser dependencies that emits a paired human-readable .md and machine-readable .json"
    - "Every song name / reason string written into the .md report is markdown-escaped"
    - "backtest.json is byte-stable across reruns so accuracy history is diffable across model changes"
  artifacts:
    - path: "packages/core/src/eval/backtest.ts"
      provides: "ablation loop populating BacktestResult.ablation"
      exports: ["runBacktest"]
    - path: "packages/core/src/cli/run-backtest.ts"
      provides: "corpus → backtest-report.md + backtest.json paired CLI"
      exports: ["runBacktestCli", "formatBacktestReport"]
    - path: "data/backtest-report.md"
      provides: "The committed human-readable trust report"
    - path: "data/backtest.json"
      provides: "The committed diffable machine-readable metrics"
  key_links:
    - from: "packages/core/src/cli/run-backtest.ts"
      to: "packages/core/src/eval/backtest.ts"
      via: "runBacktestCli calls runBacktest then writes paired outputs"
      pattern: "runBacktest\\("
    - from: "packages/core/src/cli/run-backtest.ts"
      to: "escapeMarkdownExcerpt"
      via: "escape song names / reason strings into report tables"
      pattern: "escapeMarkdownExcerpt"
---

<objective>
Complete the trust gate (EVAL-02, EVAL-03, D-14/D-15): add the leave-one-signal-out ablation loop to `runBacktest` (report-only, no go/no-go gate) and ship the `run-backtest` Node CLI that emits a paired human-readable `.md` + machine-readable `.json`, mirroring the Phase 1 census report exactly. Song names and reason strings are markdown-escaped on write; `backtest.json` is byte-stable so accuracy history diffs cleanly across model tweaks.

This is MVP slice 3 (part B): the report + ablation leg — the owner reads the numbers and judges whether each signal earns its place.

Purpose: Make the model's trustworthiness inspectable and its signal contributions auditable, so any signal that does not earn its place gets deleted (D-14).
Output: Ablation in `eval/backtest.ts`, `cli/run-backtest.ts` (paired report), barrel export, ablation test, and the committed `data/backtest-report.md` + `data/backtest.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-transition-matrix-model-backtest/02-CONTEXT.md
@.planning/phases/02-transition-matrix-model-backtest/02-RESEARCH.md
@.planning/phases/02-transition-matrix-model-backtest/02-PATTERNS.md
@.planning/phases/02-transition-matrix-model-backtest/02-04-SUMMARY.md
</context>

<artifacts_produced>
Symbols this plan creates (exclude from drift verification — they are new):
- **Functions** (`eval/backtest.ts`): the ablation loop populating `BacktestResult.ablation` (per-signal `AblationEntry` with `deltaVsFull`).
- **CLI** (`cli/run-backtest.ts`): `runBacktestCli(options)`, `formatBacktestReport(result)` (mirrors `run-census.ts`); default outputs `config.backtestReportPath` + `config.backtestJsonPath`; optional `--out`/`--json-out` flags.
- Barrel re-export of `runBacktestCli`? No — CLI internals stay behind the boundary; only pure `runBacktest` (from Plan 04) is exported.
- **Files**: `data/backtest-report.md`, `data/backtest.json`.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Leave-one-signal-out ablation loop</name>
  <files>packages/core/src/eval/backtest.ts, packages/core/test/eval/backtest.test.ts</files>
  <read_first>
    - packages/core/src/eval/backtest.ts (the Plan 04 runBacktest metrics loop + the empty `ablation: []` to populate)
    - packages/core/src/model/predict.ts (the SignalToggles-driven single scoring code path — ablation flips one flag; backoff tiers re-normalize weights)
    - packages/core/src/domain/types.ts (AblationEntry / SignalToggles / BacktestSplit)
    - packages/core/test/fixtures/synthetic-multitour.json (+ .meta.json — reuse for a hand-computed ablation delta)
    - .planning/phases/02-transition-matrix-model-backtest/02-RESEARCH.md (M6 ablation mechanics lines 218-219; D-14 report-only, no gate)
    - .planning/phases/02-transition-matrix-model-backtest/02-CONTEXT.md (D-14 leave-one-signal-out, report-only; NO automated go/no-go gate in Phase 2)
  </read_first>
  <behavior>
    - Test "ablation" (EVAL-02, D-14): `runBacktest` returns an `ablation` array with one `AblationEntry` per toggleable signal (decay, rotation, alreadyPlayed, eraPrior, hardSegue, tuning, albumEra); each entry has its own top-k splits and a `deltaVsFull` computed as the signal-off hit rate minus the full-model hit rate.
    - Test "ablation is a flag-flip" (M6): the signal-off variant runs through the SAME `predict`/`scoreCandidate` code path with exactly one `SignalToggles` flag flipped (no forked scoring code); a backoff-tier ablation re-normalizes the remaining weights to sum to 1.
    - Test "no gate": `runBacktest` never throws or returns a pass/fail flag based on the numbers — it is pure reporting (D-14).
  </behavior>
  <action>
    Extend `runBacktest` to populate `BacktestResult.ablation` (D-14/M6): after computing the full-model result, for each signal in `{ decay, rotation, alreadyPlayed, eraPrior, hardSegue, tuning, albumEra }`, re-run the identical walk-forward loop with that one `SignalToggles` flag flipped false (every other signal on), producing an `AblationEntry` `{ signal, overall, hardSegue, freeChoice, deltaVsFull }` where `deltaVsFull` = the variant's top-k minus the full model's top-k. "Off" semantics per signal (M6): `decay` → `transitionProb` uses raw `count`; `rotation`/`alreadyPlayed`/`eraPrior` → multiplier 1.0; `hardSegue` → no override/boost; backoff tiers (`tuning`/`albumEra`) → drop the tier and re-normalize remaining `backoffWeights` to sum to 1. Single scoring code path — one flag flipped, never a code fork. Report-only: NO automated go/no-go gate, no threshold pass/fail (D-14 — the `< ~25%` wider-framing behavior is EVAL-04/Phase 4, not here). Keep accumulation deterministic.

    Extend `test/eval/backtest.test.ts` with the three `behavior` tests (assert the ablation array length = number of signals, `deltaVsFull` sign/shape on the synthetic fixture, and that no pass/fail flag is emitted).
  </action>
  <verify>
    <automated>npx vitest run packages/core test/eval/backtest.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run packages/core -t "ablation"` passes — one `AblationEntry` per toggleable signal, each with `deltaVsFull`.
    - The signal-off variant is produced by flipping exactly one `SignalToggles` flag through the shared `predict` path (no forked scoring code).
    - `runBacktest` emits no pass/fail flag and never throws on the numbers (report-only, D-14).
    - `npx tsc -p packages/core --noEmit` passes.
  </acceptance_criteria>
  <done>Leave-one-signal-out ablation populates BacktestResult.ablation with per-signal deltas via a single flag-flipped code path; report-only; three tests green.</done>
</task>

<task type="auto">
  <name>Task 2: run-backtest paired .md + .json CLI, markdown-escaped, and emit committed reports</name>
  <files>packages/core/src/cli/run-backtest.ts, packages/core/src/index.ts, data/backtest-report.md, data/backtest.json</files>
  <read_first>
    - packages/core/src/cli/run-census.ts (the canonical paired-report emitter D-15 says to mirror: `runCensusCli` paired-write lines 186-203; `formatReport` sections/tables lines 78-183; `escapeMarkdownExcerpt` lines 65-67 + usage line 175; `parseArgs`/`isMain`/try-catch/`process.exit(1)` lines 205-229)
    - packages/core/src/eval/backtest.ts (runBacktest from Plan 04/Task 1 — the BacktestResult the CLI renders)
    - packages/core/src/config.ts (`backtestReportPath`, `backtestJsonPath`, `corpusArtifactPath`, `cliYearMin/Max`)
    - packages/core/src/cli/generate-tuning-tags.ts (corpus-load-from-disk pattern ~line 69)
    - .planning/phases/02-transition-matrix-model-backtest/02-PATTERNS.md (§ run-backtest.ts lines 282-309; § Shared Pattern markdown escaping lines 372-374; Pitfall 2 determinism)
    - .planning/phases/02-transition-matrix-model-backtest/02-CONTEXT.md (D-15 paired .md + .json mirroring census; specifics lines 104-106 — diffable ablation table reads like census)
  </read_first>
  <action>
    Create `cli/run-backtest.ts` mirroring `run-census.ts` verbatim: export `runBacktestCli(options)` that loads the corpus from `options.corpusPath ?? config.corpusArtifactPath`, calls `runBacktest`, then writes the machine-readable JSON to `options.jsonOutPath ?? config.backtestJsonPath` with `JSON.stringify(result, null, 2) + "\n"` (diffable, Pitfall 2) and the human-readable report to `options.reportOutPath ?? config.backtestReportPath` via `formatBacktestReport(result)` — each after `mkdir(dirname, { recursive: true })`. Also print the `.md` to stdout (D-15). Implement `formatBacktestReport(result)` building a `sections: string[]` joined with `\n\n` + trailing `\n`: a header (holdout tour name/id, show count, eval transition count), a top-k table (overall / hard-segue / free-choice × top-1/5/10, mirror the census markdown-table helper `["| Col |", "|---|", ...rows].join("\n")`), and an ablation table (one row per signal with its top-k + `deltaVsFull`, reading like the census tables per D-15 specifics). MANDATORY security control: any song name or reason string interpolated into a table cell MUST pass through `escapeMarkdownExcerpt` (reuse from `run-census.ts` — threat T-01-03/F1, RESEARCH Security Domain; markdown viewers render embedded HTML). Include `parseArgs` accepting optional `--out`/`--json-out` (reject unknown flags with `throw new Error(\`Unknown flag\`)`), the `isMain` guard via `pathToFileURL`, and try/catch/`console.error((err as Error).message)`/`process.exit(1)`. Do NOT export CLI internals from the barrel (boundary discipline).

    Run the CLI to emit `data/backtest-report.md` + `data/backtest.json` against the real corpus and commit both.
  </action>
  <verify>
    <automated>node packages/core/src/cli/run-backtest.ts && node -e "const r=require('./data/backtest.json'); if(typeof r.overall.top5!=='number'||!Array.isArray(r.ablation)||r.ablation.length<1) process.exit(1); console.log('holdout',r.holdoutTourName,'top5',r.overall.top5,'ablation',r.ablation.length)" && test -f data/backtest-report.md</automated>
  </verify>
  <acceptance_criteria>
    - `node packages/core/src/cli/run-backtest.ts` exits 0 and writes both `data/backtest-report.md` and `data/backtest.json`.
    - `data/backtest.json` parses with numeric `overall.top1/top5/top10`, populated `hardSegue`/`freeChoice` splits, and a non-empty `ablation` array.
    - Running the CLI twice produces a byte-identical `data/backtest.json` (`git diff --quiet data/backtest.json` after a second run) — diffable across model changes (D-15).
    - `grep -c escapeMarkdownExcerpt packages/core/src/cli/run-backtest.ts` returns ≥ 1 — song names / reason strings are escaped on write.
    - The `.md` report contains a top-k table split overall / hard-segue / free-choice and an ablation table.
  </acceptance_criteria>
  <done>The run-backtest CLI emits a byte-stable paired .md + .json trust report with markdown-escaped prose and a diffable ablation table; both artifacts committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv → run-backtest | `--out` / `--json-out` flags are the only external input; corpus is a committed, validated artifact |
| BacktestResult prose → .md report | Song names + reason strings (catalog-sourced) are written into a rendered markdown file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Tampering (stored) | Untrusted prose (song names / reason strings) injected into rendered `.md` | mitigate | REUSE `escapeMarkdownExcerpt` from `run-census.ts` on every name/reason written to a table cell (threat T-01-03/F1; markdown viewers render embedded HTML) — Security V5/V12 |
| T-02-11 | Tampering | `--out` / `--json-out` path from CLI argv | mitigate | Defaults from `config.backtestReportPath`/`backtestJsonPath`; reject unknown flags (Phase 1 `parseArgs` convention); write only intended data-dir paths |
| T-02-12 | Repudiation | Non-diffable/churning report defeating accuracy-history tracking | mitigate | Deterministic accumulation + `JSON.stringify(x, null, 2) + "\n"` + byte-stability test so `git diff` is the review surface (Pitfall 2) |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase (RESEARCH § Package Legitimacy Audit: zero new deps) |
</threat_model>

<verification>
- `npx vitest run packages/core` — full suite green (matrix + predict + backtest + ablation).
- `node packages/core/src/cli/run-backtest.ts` emits a byte-stable paired `.md` + `.json`.
- Manual (D-14): owner reads `data/backtest-report.md` per-split hit rates + ablation deltas and judges trust — no automated gate in Phase 2.
- `npx tsc -p packages/core --noEmit` clean.
</verification>

<success_criteria>
- A per-feature ablation report shows accuracy with each signal toggled off (Phase success criterion 4).
- The backtest runs from Node CLI with zero browser dependencies and reports top-1/5/10 overall + split (Phase success criterion 3 — complete).
- Paired diffable `.md` + `.json` committed, mirroring the census report ergonomics (D-15).
</success_criteria>

<output>
Create `.planning/phases/02-transition-matrix-model-backtest/02-05-SUMMARY.md` when done.
</output>
