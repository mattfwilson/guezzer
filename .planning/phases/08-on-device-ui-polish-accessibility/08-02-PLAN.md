---
phase: 08-on-device-ui-polish-accessibility
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - packages/app/src/show/EndShowDialog.tsx
  - packages/app/src/show/TrailNodeSheet.tsx
  - packages/app/src/show/WhyDetail.tsx
  - packages/app/test/endShowDialog.test.tsx
  - packages/app/test/trailNodeSheet.test.tsx
autonomous: true
requirements: [A11Y-01]
must_haves:
  truths:
    - "EndShowDialog, TrailNodeSheet, and WhyDetail each dismiss with Escape, trap focus while open, and restore focus to their trigger on close"
    - "The EndShowDialog destructive-confirm flow still gates End Show behind explicit confirmation (no behavior regression)"
    - "TrailNodeSheet's one-tap Undo and confirm-gated deleteEntry still work after migration"
  artifacts:
    - path: "packages/app/src/show/EndShowDialog.tsx"
      provides: "End-show confirm dialog rendered through <Sheet modal>"
      contains: "Sheet"
    - path: "packages/app/src/show/TrailNodeSheet.tsx"
      provides: "Trail node editor sheet rendered through <Sheet modal>"
      contains: "Sheet"
    - path: "packages/app/src/show/WhyDetail.tsx"
      provides: "Orb 'why' detail sheet rendered through <Sheet modal>"
      contains: "Sheet"
  key_links:
    - from: "packages/app/src/show/EndShowDialog.tsx"
      to: "packages/app/src/components/Sheet.tsx"
      via: "wraps content in <Sheet modal ariaLabel=…>"
      pattern: "<Sheet"
---

<objective>
Migrate the three Show-area modal surfaces — `EndShowDialog`, `TrailNodeSheet`, and `WhyDetail` — off their hand-rolled `role="dialog"` + `bg-black/50` shells onto the shared `<Sheet modal variant="bottom-sheet">` primitive from plan 01 (D-01), giving each Escape-dismiss, focus-trap, and focus-restore — the full-modal treatment D-02 mandates for the audited true modals (A11Y-01). Include `WhyDetail` per the RESEARCH recommendation on Open Question 1 (same idiom, cheapest win).

Purpose: Three of the seven audited A11Y-01 surfaces. Keyboard/AT users can dismiss and are never stranded.
Output: Three migrated components + refreshed/added focus-Escape assertions in their existing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-on-device-ui-polish-accessibility/08-RESEARCH.md
@.planning/phases/08-on-device-ui-polish-accessibility/08-PATTERNS.md
@.planning/phases/08-on-device-ui-polish-accessibility/08-UI-SPEC.md
@.planning/phases/08-on-device-ui-polish-accessibility/08-01-PLAN.md
</context>

<artifacts_produced>
## Artifacts this phase produces (created/changed by THIS plan)

- `EndShowDialog`, `TrailNodeSheet`, `WhyDetail` re-expressed through `<Sheet modal variant="bottom-sheet">` (their raw `z-*` classes removed; `ariaLabel` sourced from existing `config.copy.*` headings, no new strings).
- Focus/Escape assertions added to `endShowDialog.test.tsx` and `trailNodeSheet.test.tsx`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Migrate EndShowDialog to `<Sheet modal>`</name>
  <read_first>
    - packages/app/src/show/EndShowDialog.tsx (current shell at ~:90-95, `z-30`, backdrop + cancel; the destructive-confirm gate)
    - packages/app/src/components/Sheet.tsx (the primitive props from plan 01)
    - .planning/phases/08-on-device-ui-polish-accessibility/08-PATTERNS.md §"The 6 true modals + WhyDetail" (migration map row for EndShowDialog)
    - packages/app/test/endShowDialog.test.tsx (existing coverage to preserve)
    - packages/app/src/config.ts (`config.copy.show.endHeading` for the ariaLabel — do NOT author a new string)
  </read_first>
  <action>
    Replace the inline `role="dialog"`/`aria-modal`/`bg-black/50 z-30` shell with `<Sheet open={open} onClose={onClose} modal variant="bottom-sheet" ariaLabel={config.copy.show.endHeading}>`, moving the existing heading/body/Cancel/End-Show buttons inside as children. Delete the raw `z-30` class (the primitive owns z via `config.ui.z`). Keep the `if (!open) return null` guard semantics (the primitive already returns null when closed — remove any now-duplicate guard cleanly). Preserve the confirm-gated `onConfirm`/End-Show wiring and the destructive styling EXACTLY (04-07 EndShowDialog behavior, D-04 destructive). The Cancel button maps to `onClose`.
  </action>
  <verify>
    <automated>pnpm vitest run packages/app/test/endShowDialog.test.tsx</automated>
    <automated>pnpm --filter @guezzer/app exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `EndShowDialog.tsx` renders `<Sheet ... modal ...>`; no `z-30` (or any raw `z-[0-9]`) class remains in the file.
    - `endShowDialog.test.tsx` still asserts confirm-gated End Show + Cancel; add an assertion that `fireEvent.keyDown(document,{key:"Escape"})` calls `onClose`.
    - App `tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>End-show confirmation is a focus-managed, Escape-dismissible modal with its destructive gate intact.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate TrailNodeSheet to `<Sheet modal>` (stacked-with-SearchSheet aware)</name>
  <read_first>
    - packages/app/src/show/TrailNodeSheet.tsx (shell at ~:82-94, `z-30`; one-tap Undo vs confirm-gated deleteEntry from 04-06 D-15)
    - packages/app/src/components/Sheet.tsx
    - .planning/phases/08-on-device-ui-polish-accessibility/08-RESEARCH.md §"Pitfall 2: Escape closes the whole dialog stack" (TrailNodeSheet swaps to SearchSheet — the LIFO reason)
    - packages/app/test/trailNodeSheet.test.tsx
  </read_first>
  <action>
    Wrap TrailNodeSheet content in `<Sheet open={open} onClose={onClose} modal variant="bottom-sheet" ariaLabel={<the trail entry's song name>}>`; remove the raw `z-30`. Preserve the one-tap Undo (no dialog) and the confirm-gated `deleteEntry` split EXACTLY (04-06 D-15). Because this sheet can swap to SearchSheet, rely on the plan-01 `dialogStack` LIFO for Escape (do NOT add a bespoke `document` keydown listener here). Keep the recompute-after-edit contract (CometTrail/TallyReadout consume live entries — no change).
  </action>
  <verify>
    <automated>pnpm vitest run packages/app/test/trailNodeSheet.test.tsx</automated>
    <automated>pnpm --filter @guezzer/app exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `TrailNodeSheet.tsx` renders `<Sheet ... modal ...>`; no raw `z-[0-9]` class remains.
    - No `document.addEventListener("keydown", …)` for Escape is added in this file (Escape is centralized in `dialogStack.ts`).
    - `trailNodeSheet.test.tsx` still asserts Undo + confirm-gated delete; add an Escape→onClose assertion.
    - App `tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The trail node editor is focus-managed and Escape-dismissible via the shared LIFO, with its edit/undo/delete behavior unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate WhyDetail to `<Sheet modal>` (Open Q1 — include)</name>
  <read_first>
    - packages/app/src/show/WhyDetail.tsx (shell at ~:37, `z-20`, backdrop + X)
    - packages/app/src/components/Sheet.tsx
    - .planning/phases/08-on-device-ui-polish-accessibility/08-RESEARCH.md §"Open Questions" Q1 (recommendation: include WhyDetail)
    - packages/app/src/show/PredictionOrb.tsx (how WhyDetail is opened — long-press; the trigger that focus restores to)
  </read_first>
  <action>
    Wrap WhyDetail content in `<Sheet open={open} onClose={onClose} modal variant="bottom-sheet" ariaLabel={<song name> + " — why"}>`; remove the raw `z-20`. Map the existing X/backdrop-close to `onClose`. No copy or scoring change — the "why" breakdown content is unchanged; only the shell + a11y wiring change. Confirm the long-press trigger element is the focus-restore target (the primitive captures `document.activeElement` automatically — no extra code).
  </action>
  <verify>
    <automated>pnpm vitest run packages/app/test/predictionOrb.test.tsx</automated>
    <automated>pnpm --filter @guezzer/app exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `WhyDetail.tsx` renders `<Sheet ... modal ...>`; no raw `z-[0-9]` class remains.
    - The "why" content and long-press open path are unchanged (predictionOrb test stays green).
    - App `tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The orb "why" sheet is Escape-dismissible and focus-restoring, consistent with the other modals.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kglw-derived data → sheet chrome | Song names appear in ariaLabel/headings; render as React text only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Tampering/Elevation | song name in `ariaLabel` / headings | mitigate | Names pass as React text props; no `dangerouslySetInnerHTML`. The migration reuses existing `config.copy.*`/name strings — no new interpolation surface. |
| T-08-06 | Error Handling (V7) | closed/empty sheet state | mitigate | Migration preserves `if (!open) return null` via the primitive; the destructive-confirm and undo/delete guards are unchanged — no guarded state becomes a throw. |
| T-08-SC | Tampering (supply chain) | npm installs | accept | No new packages; refactor only. |
</threat_model>

<verification>
- `pnpm vitest run packages/app` green.
- Grep: no `z-[0-9]` literal remains in `EndShowDialog.tsx`, `TrailNodeSheet.tsx`, `WhyDetail.tsx`.
- No bespoke Escape `document` listener added in any of the three files.
</verification>

<success_criteria>
- All three Show-area modals dismiss with Escape, trap focus, and restore to trigger (A11Y-01).
- Destructive/undo/delete behaviors and the "why" content are unchanged.
</success_criteria>

<output>
Create `.planning/phases/08-on-device-ui-polish-accessibility/08-02-SUMMARY.md` when done
</output>
