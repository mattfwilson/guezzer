---
phase: 07-explore-mode-constellation
plan: 03
type: execute
wave: 2
depends_on: [07-02]
files_modified:
  - packages/app/src/config.ts
autonomous: false
requirements: [EXPL-06]
must_haves:
  truths:
    - "On a real mid-range phone, at-rest labels (top-K=8) are readable and do not jitter-crowd a 375px-wide screen — or LABEL_AT_REST_TOP_K is tuned down (possibly to 0) from the observed result (D-15)"
    - "Full catalog + edge threshold=1 (the ~264-node / 2,987-edge stress case) settles without unacceptable jank on device — or a mitigation is recorded (EXPL-06, RESEARCH Pitfall 4 / Assumption A3)"
    - "LABEL_ZOOM_THRESHOLD / COUNT_ZOOM_THRESHOLD are confirmed or tuned so labels and counts fade in at a comfortable zoom"
  artifacts:
    - path: "packages/app/src/config.ts"
      provides: "label/zoom constants tuned from the device spike result (if changes needed)"
      contains: "LABEL_AT_REST_TOP_K"
  key_links: []
---

<objective>
Resolve the STATE-flagged blocker — "Canvas label rendering quality at ~250 nodes on small screens needs a spike" — by validating D-15's zoom-gated + focus-forced label strategy on a real device, and tune `LABEL_AT_REST_TOP_K` / the zoom thresholds from the observed result BEFORE the polish slices (bars focus-labels, dex sighting counts) depend on them.

Purpose: RESEARCH mandates this spike run EARLY (Assumptions A2/A3 are the only MEDIUM-confidence items in the phase). Slice 4 reuses the exact zoom-gating pattern for sighting counts, so tuning the label constants now avoids reworking the canvas draw later.
Output: A signed-off label/zoom config (unchanged or tuned) and a recorded device-perf verdict on the Full-catalog stress case.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-explore-mode-constellation/07-RESEARCH.md
@.planning/phases/07-explore-mode-constellation/07-UI-SPEC.md
@$HOME/.claude/projects/C--Users-mattf-git-guezzer/memory/device-uat-hosting.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: On-device canvas-label readability + Full-catalog perf spike</name>
  <files>packages/app/src/config.ts</files>
  <read_first>
    - 07-RESEARCH.md §"Pitfall 4" (dense-graph render cost on mobile Safari at Full+threshold=1), §"Open Questions" Q1 (does LABEL_AT_REST_TOP_K=8 crowd small screens?), §"Assumptions Log" A2/A3
    - 07-UI-SPEC.md §"Layout" zoom-gated labels rule, §"Config surface" (LABEL_AT_REST_TOP_K 8, LABEL_ZOOM_THRESHOLD 1.5, COUNT_ZOOM_THRESHOLD 2.5)
    - MEMORY device-uat-hosting.md (serve over an HTTPS cloudflared tunnel with `--http-host-header localhost` for iOS testing)
  </read_first>
  <what-built>
    Plan 07-02 renders the constellation at `#/explore` with settle-and-freeze and zoom-gated + top-K-at-rest labels. Note: rotation-default and the edge slider land in later slices, so the Full-catalog + threshold=1 stress case is exercised here by temporarily rendering the full node/edge set (Rotation/Full toggle not yet built — verify against the current full-catalog default view).
  </what-built>
  <how-to-verify>
    1. Start the dev server and expose it over the HTTPS cloudflared tunnel (per MEMORY device-uat-hosting: include `--http-host-header localhost`). Open the tunnel URL on a real mid-range phone (owner's iPhone 16 Pro is the baseline device from prior phases).
    2. Navigate to the Explore tab. Watch the initial settle: does it complete and FREEZE (motion stops)? Note any jank/fan noise/dropped frames DURING settle on the full catalog (~264 nodes / 2,987 edges is the stress case).
    3. At rest (frozen), read the top-8 labels: are they legible at 375px width, or do they overlap/crowd? Pinch-zoom in: do all labels fade in cleanly past the threshold? Pinch-zoom out: do labels disappear without leaving artifacts?
    4. Report: (a) is settle-then-freeze smooth enough on Full catalog? (b) is LABEL_AT_REST_TOP_K=8 comfortable, or should it drop (name the number, possibly 0)? (c) do LABEL_ZOOM_THRESHOLD=1.5 / COUNT_ZOOM_THRESHOLD=2.5 feel right or need adjusting?
  </how-to-verify>
  <action>
    After the human reports: if any label/zoom constant needs changing, edit ONLY `packages/app/src/config.ts` `explore` block to the human-confirmed values and note the rationale in the SUMMARY (replace the `[ASSUMED]` marker with `[VERIFIED: device spike YYYY-MM-DD]` on the tuned keys). If the Full-catalog stress case janks badly, record the chosen mitigation in the SUMMARY (e.g. gate arrows below a zoom, or an edge cap) as a note for Slice 3's slider work — do NOT implement the mitigation here. If no changes are needed, record the sign-off and leave config untouched.
  </action>
  <acceptance_criteria>
    - Human has verified on a real phone and reported readability + perf
    - `LABEL_AT_REST_TOP_K` / `LABEL_ZOOM_THRESHOLD` / `COUNT_ZOOM_THRESHOLD` are either confirmed as-is or updated in `packages/app/src/config.ts` to the reported values with a `[VERIFIED]` marker
    - Any Full-catalog perf mitigation is recorded in the SUMMARY as a note for Plan 07-05 (not implemented here)
    - The STATE blocker "[Phase 7] Canvas label rendering quality…" can be marked resolved
  </acceptance_criteria>
  <verify>If constants changed: `pnpm --filter @guezzer/app exec tsc --noEmit` green. Human device sign-off recorded in the SUMMARY.</verify>
  <done>Label/zoom constants confirmed or tuned from the device result; Full-catalog perf verdict + any mitigation note recorded; STATE blocker resolvable.</done>
  <resume-signal>Type "approved" (keep current constants) or state the tuned values (e.g. "K=4, label zoom 1.3") and any perf note.</resume-signal>
</task>

</tasks>

<artifacts_produced>
- (possibly) tuned `config.explore` label/zoom constants — no new symbols; this plan validates and may adjust values created in 07-01
</artifacts_produced>

<threat_model>
## Trust Boundaries

No new trust boundary. This is an on-device verification of an already-rendered view plus a possible config-constant edit.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Tampering | config constant edit | accept | Edit is a bounded numeric config change to app render constants only; no logic, no data, no new surface. No high-severity threat. |

The cloudflared tunnel exposes the dev server transiently for owner-only testing (inherited Phase-4 device-UAT practice); it is torn down after the session.
</threat_model>

<verification>
- Human sign-off recorded; STATE blocker resolvable
- If config changed: `pnpm --filter @guezzer/app exec tsc --noEmit` still green
</verification>

<success_criteria>
- D-15 label strategy validated on device; `LABEL_AT_REST_TOP_K` set from real evidence
- Full-catalog perf verdict recorded (with mitigation note if needed)
</success_criteria>

<output>
Create `.planning/phases/07-explore-mode-constellation/07-03-SUMMARY.md` when done, recording the device verdict and final label/zoom constants.
</output>
