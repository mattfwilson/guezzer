---
phase: 16-gizz-bingo-build-live-marking-celebrations
plan: 04
type: execute
wave: 2
depends_on: [16-01, 16-02]
files_modified:
  - packages/app/src/show/ShowView.tsx
  - packages/app/src/show/BingoPeekStrip.tsx
  - packages/app/src/show/PreShowLauncher.tsx
  - packages/app/src/show/StartShowNudge.tsx
autonomous: true
requirements: [BINGO-04]

must_haves:
  truths:
    - "When a locked card exists for the active session, a collapsible bingo peek strip appears in-flow on LiveGizz (never over the FAB/orbit)"
    - "The peek strip shows a board thumbnail + the single closest one-away banner; tapping it routes to the full board in GizzGames"
    - "Tapping Start Show with a draft card locks it (freezes caughtSnapshot from the live dex); with no draft card it fires a dismissible 'Deal a bingo card for tonight?' nudge"
    - "The nudge's [Deal] routes to GizzGames; [Not now] dismisses for this show only (no permanent suppression)"
    - "The peek strip and one-away banner re-derive from deriveLiveBoard on every logSong — marks are never stored"
  artifacts:
    - path: "packages/app/src/show/BingoPeekStrip.tsx"
      provides: "in-flow board thumbnail + one-away banner on LiveGizz (D-21)"
      exports: ["BingoPeekStrip"]
    - path: "packages/app/src/show/StartShowNudge.tsx"
      provides: "dismissible Deal-a-card nudge at Start Show (D-08/D-09)"
      exports: ["StartShowNudge"]
  key_links:
    - from: "packages/app/src/show/PreShowLauncher.tsx"
      to: "packages/app/src/db/db.ts lockCard"
      via: "Start Show → lockCard(sessionId, caughtSongIds) or nudge if no card"
      pattern: "lockCard"
    - from: "packages/app/src/show/BingoPeekStrip.tsx"
      to: "packages/app/src/games/bingoReplay.ts deriveLiveBoard + @guezzer/core nearMiss"
      via: "live re-derivation on logSong"
      pattern: "deriveLiveBoard|nearMiss"
---

<objective>
Bring the light-up magic onto the trust-critical LiveGizz screen without displacing the orbit predictions: an in-flow collapsible bingo peek strip (board thumbnail + the single closest one-away banner, D-21), and wire the Start-Show trigger to lock the draft card (freezing the caught-snapshot from the live dex) or, when no card exists, fire the dismissible "Deal a bingo card for tonight?" nudge (D-08/D-09). Songs are logged here; the strip and banner re-derive from `deriveLiveBoard` + `nearMiss` on every log. Delivers the live-access + near-miss half of BINGO-04 and the Start-Show lock connection (D-07).

Purpose: The setlist log is sacred — the strip sits in the show column, never over the FAB/orbit, and the nudge gives discoverability without a silent path.
Output: BingoPeekStrip, StartShowNudge, and the ShowView/PreShowLauncher lock-and-nudge wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-gizz-bingo-build-live-marking-celebrations/16-CONTEXT.md
@.planning/phases/16-gizz-bingo-build-live-marking-celebrations/16-RESEARCH.md
@.planning/phases/16-gizz-bingo-build-live-marking-celebrations/16-PATTERNS.md
@.planning/phases/16-gizz-bingo-build-live-marking-celebrations/16-UI-SPEC.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 04)
- **New components:** `BingoPeekStrip` (in-flow thumbnail + one-away banner on LiveGizz, D-21), `StartShowNudge` (dismissible Deal-a-card prompt at Start Show, D-08/D-09).
- **Changed:** `ShowView.tsx` (peek-strip slot near CometTrail; live re-derivation via `deriveLiveBoard`/`nearMiss`); `PreShowLauncher.tsx` (Start Show → `lockCard` with frozen caught-set, or nudge when no card).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: BingoPeekStrip — in-flow thumbnail + closest one-away banner (D-21/D-13/D-14/D-15)</name>
  <read_first>
    - packages/app/src/show/ShowView.tsx:29/87 (`useReducedMotion() ?? false`), :324/339/355 (the three `logSong` funnels), :472 (`<CometTrail entries={session.entries} …>` — the in-flow slot to add the strip adjacent to).
    - packages/app/src/games/bingoReplay.ts (Plan 02) — `deriveLiveBoard(card, liveEntries, ctx, liveCaughtSnapshot)`.
    - packages/app/src/games/bingoContext.ts (Plan 02) — `getBingoContext()` memoized ctx + dexSnapshot helper.
    - packages/core/src/bingo/estimate.ts (Plan 01) — `nearMiss(marked, card, ctx, caughtSnapshot, cfg)` returning the single closest `{ neededSquareIndex, neededLabel, bucket, kind: "line"|"blackout" }`.
    - packages/app/src/components/BingoBoard.tsx (Plan 02) — `<BingoBoard thumbnail oneAwayIndex=… />`.
    - packages/app/src/routing/useHashRoute.ts — route to `#/games` on strip tap (RESEARCH A6 default).
    - config.copy.games.bingo (Plan 01) — one-away line banner formatter + "👑 ONE SQUARE FROM BLACKOUT" callout.
    - .planning/phases/16-.../16-UI-SPEC.md §Color/§Layering — `bg-elevated` strip, in-flow (NOT fixed), min-h-11 tap zone, accent glow reserved for the single one-away square.
  </read_first>
  <action>
    Create `packages/app/src/show/BingoPeekStrip.tsx`: a `bg-elevated` in-flow card that renders a compact `<BingoBoard thumbnail marked=… oneAwayIndex=… />` plus the one-away banner. It takes the session's locked card + live entries, builds the live board via `deriveLiveBoard(card, entries, getBingoContext(), liveDexSnapshot)`, and computes `nearMiss` for the banner: for `kind:"line"` render "🔥 One away — need a {bucket} song!"; for `kind:"blackout"` render the "👑 ONE SQUARE FROM BLACKOUT" crown callout; when null, no banner. Pass `nearMiss.neededSquareIndex` as `oneAwayIndex` to glow the exact needed square (single closest only, D-14). Tapping the strip routes to `#/games` via `useHashRoute` (min-h-11 tap zone). Reduced-motion static ring via `useReducedMotion`. All names escaped React text.
  </action>
  <verify>
    <automated>cd packages/app && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>BingoPeekStrip renders a thumbnail + closest one-away banner, glows the single needed square, and routes to #/games on tap; app typechecks.</done>
  <acceptance_criteria>
    - `grep -n "deriveLiveBoard\|nearMiss\|BingoBoard" packages/app/src/show/BingoPeekStrip.tsx` matches.
    - `grep -n "useHashRoute\|#/games\|games" packages/app/src/show/BingoPeekStrip.tsx` shows the route-on-tap.
    - `cd packages/app && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Mount the peek strip in-flow on ShowView (only when a card is locked/active)</name>
  <read_first>
    - packages/app/src/show/ShowView.tsx:472 — the `<CometTrail>` in-flow slot; add `<BingoPeekStrip>` adjacent (in the show column, never fixed, never over the FAB/orbit — RESEARCH §3 seam).
    - packages/app/src/show/ShowView.tsx — the existing `useLiveQuery` usage to add a `db.bingoCards` read guarded to the active session; the `session.entries` live trail already in scope.
    - packages/app/src/db/db.ts:157 — `BingoCardRow` (`lockedAt`, `sessionId`) to select the active locked card.
  </read_first>
  <action>
    In ShowView add a `useLiveQuery` selecting the active session's `bingoCards` row; render `<BingoPeekStrip>` in the in-flow slot adjacent to `<CometTrail>` ONLY when a row exists for the active session AND `lockedAt != null` (strip appears only for a locked/active card, D-21). Feed it the card + `session.entries`. Do not add any fixed positioning; it flows in the show column above the tab bar. Leave the orbit/FAB/CometTrail untouched.
  </action>
  <verify>
    <automated>npm test -- --run packages/app/test/show 2>/dev/null; cd packages/app && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>The peek strip appears in-flow on LiveGizz only for a locked active card; existing show tests + typecheck stay green.</done>
  <acceptance_criteria>
    - `grep -n "BingoPeekStrip\|bingoCards\|lockedAt" packages/app/src/show/ShowView.tsx` matches.
    - `cd packages/app && npx tsc --noEmit` exits 0.
    - Existing `packages/app/test/show` tests still pass.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Start-Show lock + dismissible nudge (D-07/D-08/D-09)</name>
  <read_first>
    - packages/app/src/show/PreShowLauncher.tsx:14/32 — `startShow()` + the Start Show CTA onClick (the trigger to wrap).
    - packages/app/src/db/db.ts:525-538 — `lockCard(sessionId, caughtSongIds)` (idempotent, no-op if no card, first-freeze-wins); the doc note that `caughtSongIds` MUST be the frozen `deriveDex(...).perSong.keys()` as of lock.
    - packages/app/src/db/db.ts:329 — `startShow()` (mints the sessionId); how the active session id becomes available post-start.
    - the app dex derivation (useDexStats / deriveDex) — source of the frozen caught set at lock time (RESEARCH Pitfall 1 — frozen, never live-after).
    - config.copy.games.bingo (Plan 01) — nudge heading + `[Deal]`/`[Not now]`.
    - packages/app/src/routing/useHashRoute.ts — `[Deal]` routes to `#/games`.
  </read_first>
  <action>
    Wrap the Start Show trigger: after `startShow()` resolves to the active sessionId, check for a draft `bingoCards` row for that session. If one exists → call `lockCard(sessionId, caughtSongIds)` passing the frozen caught set derived from `deriveDex(...).perSong.keys()` AS OF lock (never the live dex thereafter). If none exists → render `<StartShowNudge>` (new `packages/app/src/show/StartShowNudge.tsx`): a dismissible prompt (dialog/toast idiom) with the heading "Deal a bingo card for tonight?", `[Deal]` → route `#/games`, `[Not now]` → dismiss for THIS show only (per-session state, no persisted "don't ask again" — D-09; the nudge re-fires next card-less Start Show). Locking must never block or delay the show start (fire-and-forget after start).
  </action>
  <verify>
    <automated>cd packages/app && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>Start Show with a draft card locks it (frozen caught-set); with no card it shows the dismissible nudge; [Deal] routes to GizzGames, [Not now] dismisses for the show only.</done>
  <acceptance_criteria>
    - `grep -n "lockCard" packages/app/src/show/PreShowLauncher.tsx` matches.
    - `grep -n "export function StartShowNudge" packages/app/src/show/StartShowNudge.tsx` matches; `[Not now]` uses per-session dismissal (no `setMeta`/persisted suppression key).
    - `cd packages/app && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| kglw-derived names → peek strip / banner | untrusted corpus strings rendered as labels |
| live dex → lock freeze | the caught-set frozen at lock must be a snapshot, not a live reference |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-06 | Tampering (XSS) | peek-strip labels / one-away banner | mitigate | Escaped React text; no `dangerouslySetInnerHTML` |
| T-16-07 | Tampering | wrong caught-set frozen at lock (replay drift) | mitigate | Freeze `deriveDex(...).perSong.keys()` as an array at lock time (Pitfall 1); `lockCard` stores the snapshot, first-freeze-wins |
| T-16-08 | Denial of service | lock/nudge blocking the log loop | mitigate | Lock is fire-and-forget after `startShow`; nudge is non-blocking; strip is in-flow, never over the FAB/orbit |
| T-16-SC | Tampering | package installs | accept | No installs this phase — not applicable |
</threat_model>

<verification>
- `cd packages/app && npx tsc --noEmit` clean.
- `npm test -- --run` full suite green.
- Manual (device UAT, tracked separately): log songs → strip lights + closest one-away only; Start Show locks / nudges correctly.
</verification>

<success_criteria>
- Live board access on LiveGizz without displacing predictions; single closest one-away banner — BINGO-04.
- Start Show locks the draft (frozen snapshot) or nudges when card-less — D-07/D-08/D-09.
</success_criteria>

<output>
Create `.planning/phases/16-gizz-bingo-build-live-marking-celebrations/16-04-SUMMARY.md` when done.
</output>
