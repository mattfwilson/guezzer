---
phase: 04-show-mode
plan: 06
type: execute
wave: 5
depends_on: [04-05]
files_modified:
  - packages/app/src/show/ActionBar.tsx
  - packages/app/src/show/CometTrail.tsx
  - packages/app/src/show/TallyReadout.tsx
  - packages/app/src/show/TrailNodeSheet.tsx
  - packages/app/src/show/ShowView.tsx
  - packages/app/test/showSession.test.ts
autonomous: true
requirements: [SHOW-06, SHOW-07, SHOW-08, SHOW-09]
must_haves:
  truths:
    - "User can mark a set break (setNumber → '2') and the encore (→ 'e'); entries snapshot the set number and round-trip kglw.net encoding (SHOW-06)"
    - "Undo removes the most recent song in one tap; tapping an older trail node opens edit/delete (and rename for ???) (SHOW-07/D-15)"
    - "The comet trail shows the last ~4 songs as diminishing nodes with hit (green) / miss (red) rings and compresses older history into a tappable +N at 30+ songs (SHOW-08)"
    - "The running hit/miss tally is persistently visible and recomputes automatically after any edit (SHOW-09)"
  artifacts:
    - path: "packages/app/src/show/CometTrail.tsx"
      provides: "diminishing recent-songs strip + hit/miss rings + +N compression"
      contains: "TRAIL_COMPRESS_AT|TRAIL_VISIBLE_RECENT"
    - path: "packages/app/src/show/TallyReadout.tsx"
      provides: "persistent {hits}/{total} · {pct}% readout"
      contains: "tabular-nums"
    - path: "packages/app/src/show/TrailNodeSheet.tsx"
      provides: "edit / delete (confirm) / rename-??? sheet"
      contains: "renameEntry|undoLast|delete"
  key_links:
    - from: "packages/app/src/show/CometTrail.tsx"
      to: "packages/app/src/show/useShowSession.ts"
      via: "useLiveQuery entries → trail nodes"
      pattern: "entries"
    - from: "packages/app/src/show/ActionBar.tsx"
      to: "packages/app/src/db/db.ts"
      via: "Set break/Encore/Undo → markSetBreak/markEncore/undoLast"
      pattern: "markSetBreak|markEncore|undoLast"
---

<objective>
Layer the setlist structure and history views onto the working loop: the ActionBar secondary row (Set break / Encore / Undo), the CometTrail (last ~4 diminishing nodes with hit/miss rings + "+N" compression at 30), the persistent TallyReadout, and the TrailNodeSheet (edit / delete-with-confirm / rename-???). All read reactively from Dexie via useLiveQuery so the trail and tally recompute automatically after any edit.

Purpose: SHOW-06 (round-trippable set structure), SHOW-07/D-15 (one-tap undo + older-entry edit), SHOW-08 (comet trail + compression), SHOW-09 (persistent tally).
Output: the secondary action row wired, three new trail/tally components mounted in ShowView, and set-structure/undo test coverage extended.
</objective>

## Phase Goal (MVP user story)

**As a** KGLW showgoer, **I want to** mark set breaks and the encore, see my recent songs and running hit-rate, and fix a wrong entry in one tap, **so that** the tracked setlist is complete, honest, and correctable in the dark.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-show-mode/04-UI-SPEC.md
@.planning/phases/04-show-mode/04-PATTERNS.md
@.planning/phases/04-show-mode/04-RESEARCH.md
@docs/SCHEMA.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ActionBar secondary row — Set break / Encore / Undo</name>
  <files>packages/app/src/show/ActionBar.tsx, packages/app/test/showSession.test.ts</files>
  <read_first>
    - .planning/phases/04-show-mode/04-RESEARCH.md §Pattern 6 (set break → currentSetNumber "1"→"2"; encore → "e"; NEITHER ends the show, D-04; never infer structure from transition_id) + §Pattern 5 (undoLast)
    - .planning/phases/04-show-mode/04-UI-SPEC.md §Copywriting (Set break / Encore / Undo copy; Undo is no-dialog per D-15) + §Design System (SkipForward/Minus, Star, Undo2 icons)
    - docs/SCHEMA.md (setnumber / "e" encore encoding for round-trip fidelity)
    - packages/app/src/db/db.ts (04-01 helpers markSetBreak/markEncore/undoLast)
    - packages/app/src/show/ActionBar.tsx (04-05 — the secondary-row placeholder buttons to activate)
    - packages/app/test/showSession.test.ts (04-01/04-04 — extend with set-structure + undo cases)
  </read_first>
  <action>
    Activate the ActionBar secondary row (built as placeholders in 04-05): Set break → `markSetBreak(sessionId)` (currentSetNumber "1"→"2"), Encore → `markEncore(sessionId)` (→"e") — neither ends the show (D-04); Undo → `undoLast(sessionId)` with NO dialog (D-15, the common "oops"). Icons per UI-SPEC (SkipForward/Minus, Star, Undo2), all ≥44px, theme tokens (not accent). Subsequent logged entries snapshot the current setNumber (already handled by logSong callers passing currentSetNumber). NOTE: the set-structure + undo HELPER assertions already exist in showSession.test.ts from 04-01 (markSetBreak/markEncore/undoLast at the helper level). This task does NOT duplicate them — it adds the ActionBar-WIRING assertions: that the secondary-row buttons invoke markSetBreak/markEncore/undoLast, and that an entry logged AFTER an action snapshots the expected setNumber through the UI flow. Reuse the existing set-structure/undo-named it blocks (extend, don't clone) so the -t "set-structure" and -t "undo" filters still resolve.
  </action>
  <verify>
    <automated>npx vitest run packages/app -t "set-structure" && npx vitest run packages/app -t "undo" && npx tsc -p packages/app --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run packages/app -t "set-structure"` and `-t "undo"` pass.
    - Source assertion: Set break/Encore call `markSetBreak`/`markEncore` and do NOT change show status (D-04 — no auto-end).
    - Source assertion: Undo calls `undoLast` with no confirmation dialog (D-15).
    - Behavior assertion: an entry logged after markEncore has `setNumber === "e"`; grouping by setNumber round-trips the structure.
  </acceptance_criteria>
  <done>Set break, Encore, and one-tap Undo are wired; entries snapshot the correct setNumber and round-trip kglw.net encoding; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: CometTrail (diminishing + hit/miss rings + +N compression) + TallyReadout</name>
  <files>packages/app/src/show/CometTrail.tsx, packages/app/src/show/TallyReadout.tsx, packages/app/src/show/ShowView.tsx</files>
  <read_first>
    - .planning/phases/04-show-mode/04-UI-SPEC.md §Layout region 1-2 (header tally slot; comet-trail strip: last ~4 diminishing, nearest orbit = most recent, hit/miss rings, +N at TRAIL_COMPRESS_AT, horizontally scrollable, never wraps into the fan) + §Color B2 (hit #22C55E / miss #EF4444) + §Typography (tally tabular-nums, zero-state "0/0 · —")
    - .planning/phases/04-show-mode/04-PATTERNS.md "CometTrail.tsx"/"TallyReadout.tsx" analogs (BottomTabBar / VersionStamp) + §Shared "44px tap floor"
    - packages/app/src/show/useShowSession.ts (04-04 — reactive entries + derived tally)
    - packages/app/src/config.ts (config.show.TRAIL_VISIBLE_RECENT 4, TRAIL_COMPRESS_AT 30)
    - packages/app/src/show/scoring.ts (deriveTally)
  </read_first>
  <action>
    Create `CometTrail.tsx`: reactive over `entries` (useShowSession), render the last `TRAIL_VISIBLE_RECENT` (4) as diminishing nodes (nearest the orbit = most recent), each wearing a hit (green) or miss (red) ring from `entry.outcome` (D-06/D-08). When `entries.length >= TRAIL_COMPRESS_AT` (30), collapse the overflow into a tappable "+N" chip (≥44px) that opens a scrollable full-setlist sheet. The strip is fixed-height, horizontally scrollable, and MUST NOT wrap into the orbit fan. Each node tap calls `onNodeTap(entry)` (opens TrailNodeSheet in Task 3). Create `TallyReadout.tsx`: render `{hits}/{total} · {pct}%` with `tabular-nums` (zero-state `0/0 · —`), text-primary (never accent), persistently visible in the header slot (SHOW-09). Mount both in ShowView (CometTrail strip below header, TallyReadout in the header top-right). All constants from config.show — no inline literals.
  </action>
  <verify>
    <automated>npx tsc -p packages/app --noEmit && npx vitest run packages/app -t "tally"</automated>
    <human-check>On device: log several hits and misses → trail nodes show correct green/miss rings and diminish; tally updates live; simulate a 30+ song set → older history collapses to a tappable +N. (SHOW-08 perceptual.)</human-check>
  </verify>
  <acceptance_criteria>
    - Source assertion: CometTrail reads `config.show.TRAIL_VISIBLE_RECENT` and `TRAIL_COMPRESS_AT` (no inline 4/30).
    - Source assertion: ring color derives from `entry.outcome` using the B2 hit/miss hexes.
    - Source assertion: TallyReadout uses `tabular-nums` and renders the `0/0 · —` zero-state; not accent-colored.
    - `npx vitest run packages/app -t "tally"` green; typecheck passes.
  </acceptance_criteria>
  <done>The comet trail shows diminishing hit/miss-ringed recent nodes with +N compression, and the persistent tally updates reactively.</done>
</task>

<task type="auto">
  <name>Task 3: TrailNodeSheet — edit / delete (confirm) / rename ???</name>
  <files>packages/app/src/show/TrailNodeSheet.tsx, packages/app/src/show/ShowView.tsx</files>
  <read_first>
    - .planning/phases/04-show-mode/04-UI-SPEC.md §Copywriting "Destructive actions" (delete confirm heading/body/buttons; rename-??? sheet "Name this song" reusing the Search field, "Skip" leaves ???) + §Component Inventory (TrailNodeSheet contract)
    - .planning/phases/04-show-mode/04-PATTERNS.md "TrailNodeSheet" analog (AppMenu/IosInstallInstructions overlay/dialog)
    - packages/app/src/show/SearchSheet.tsx (04-05 — reuse for the rename flow)
    - packages/app/src/db/db.ts (04-01 — renameEntry, and a delete-by-id helper; add one if absent following the logSong idiom)
    - packages/app/src/show/CometTrail.tsx (04-06 Task 2 — the onNodeTap that opens this sheet)
  </read_first>
  <action>
    Create `TrailNodeSheet.tsx` (AppMenu overlay idiom): opened by a CometTrail node tap. For a normal entry, offer Edit (re-pick song via SearchSheet) and Delete; Delete requires a confirm dialog (heading "Delete this song?", destructive red Delete / Cancel per UI-SPEC) then removes the entry by id (the derived tally recomputes automatically). For a "???" placeholder, offer Rename (reuse SearchSheet → `renameEntry(id, songId, songName)`, isPlaceholder→false, D-14/D-15) with a "Skip" that leaves it as ???. If db.ts lacks a delete-by-id helper, add `deleteEntry(id)` there following the existing transaction idiom. Wire ShowView to render TrailNodeSheet from CometTrail's onNodeTap. Names render as text only (no dangerouslySetInnerHTML).
  </action>
  <verify>
    <automated>npx tsc -p packages/app --noEmit && npx vitest run packages/app</automated>
    <human-check>On device: tap an older trail node → edit/delete sheet; delete asks to confirm then removes it and the tally recomputes; tap a ??? node → rename via search or Skip. (SHOW-07/D-15 perceptual.)</human-check>
  </verify>
  <acceptance_criteria>
    - Source assertion: Delete goes through a confirm dialog (destructive), while Undo (Task 1) does not — the D-15 split is preserved.
    - Source assertion: rename reuses SearchSheet and calls `renameEntry` (isPlaceholder cleared).
    - Source assertion: no `dangerouslySetInnerHTML` in TrailNodeSheet.
    - `npx tsc -p packages/app --noEmit` passes; `npx vitest run packages/app` green.
  </acceptance_criteria>
  <done>Older entries are editable/deletable (with confirm) and ??? placeholders renamable from the trail; the tally stays correct after any change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → IndexedDB | Local set-structure + edit writes; no network |
| kglw song names → DOM | Trail node captions render untrusted-origin text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-13 | Tampering | set-structure serialization | mitigate | Author `setNumber` directly from the closed `"1"|"2"|"e"` union; never infer from `transition_id` (proven unreliable, SCHEMA §4/§13b) — guarantees kglw round-trip. |
| T-04-14 | Tampering (XSS) | CometTrail / TrailNodeSheet captions | mitigate | Song names render as React text; never `dangerouslySetInnerHTML` (ASVS V5). |
| T-04-15 | Repudiation / data loss | delete vs undo | mitigate | Destructive delete requires an explicit confirm; one-tap Undo is bounded to the most-recent entry only (D-15). |
</threat_model>

<verification>
- `npx vitest run packages/app` green incl. extended set-structure + undo cases.
- `npx tsc -p packages/app --noEmit` passes.
- On-device SHOW-08 trail/compression + SHOW-07 edit/delete checks (manual per VALIDATION).
</verification>

<success_criteria>
Set breaks and encore mark round-trippable structure; the comet trail shows diminishing hit/miss-ringed nodes with +N compression; the persistent tally updates reactively; undo is one tap and older entries edit/delete (with confirm) or rename. The tracked show is complete and correctable.
</success_criteria>

## Artifacts this phase produces (this plan)

- **Components:** `CometTrail` (diminishing + hit/miss rings + +N compression), `TallyReadout` (persistent tally), `TrailNodeSheet` (edit/delete/rename)
- **Wiring:** ActionBar secondary row (Set break/Encore/Undo) live; trail + tally mounted in ShowView; possible `deleteEntry(id)` helper added to db.ts
- **Tests:** extended set-structure + undo coverage in `showSession.test.ts`

<output>
Create `.planning/phases/04-show-mode/04-06-SUMMARY.md` when done.
</output>
