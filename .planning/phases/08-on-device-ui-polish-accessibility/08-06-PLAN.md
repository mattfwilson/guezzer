---
phase: 08-on-device-ui-polish-accessibility
plan: 06
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - packages/app/src/config.ts
  - packages/app/src/show/orbLabelFit.ts
  - packages/app/test/orbLabelFit.catalog.test.ts
  - packages/app/src/dev/OrbFitHarness.tsx
  - packages/app/src/App.tsx
autonomous: true
requirements: [POLISH-01]
must_haves:
  truths:
    - "Every real catalog song name (all 264 @matrix nodes) renders fully — no ellipsis — at the realistic minimum rendered orb diameter and at center-node size"
    - "The ellipsis last-line fallback remains in the heuristic but does not fire for any real name at its real rendered diameter (unreachable safety net)"
    - "A documented minimum-legibility font floor of 10px @ weight 600 is recorded in config"
    - "A dev-only orb-fit harness can render all 264 real names on-device and programmatically flag overflow"
  artifacts:
    - path: "packages/app/test/orbLabelFit.catalog.test.ts"
      provides: "Zero-ellipsization assertion over all 264 real @matrix names (orb + center)"
      contains: "@matrix"
    - path: "packages/app/src/dev/OrbFitHarness.tsx"
      provides: "Dev-only route rendering all 264 real names + scrollWidth/scrollHeight overflow flagging"
      exports: ["OrbFitHarness"]
    - path: "packages/app/src/config.ts"
      provides: "Retuned ORB_LABEL constants + documented 10px floor"
      contains: "ORB_LABEL_MAX_LINES"
  key_links:
    - from: "packages/app/src/show/PredictionOrb.tsx"
      to: "config.show.ORB_LABEL_MAX_LINES"
      via: "fitOrbLabel opts"
      pattern: "ORB_LABEL_MAX_LINES"
---

<objective>
Close POLISH-01: retune `orbLabelFit` / `ORB_LABEL_*` so every real catalog song name renders fully legible on a small phone — no truncation, overflow, or oversizing — keeping the ellipsis fallback only as an unreachable safety net (D-05). Lock the retune with an automated catalog test over the real 264-node `@matrix` artifact, and build a dev-only on-device harness that renders every real name and flags overflow by real measurement (the heuristic self-report cannot be trusted — it drifts optimistic).

Purpose: The bar is the actual 264-song catalog rendered on real hardware, not the no-DOM heuristic's estimate.
Output: Retuned constants + `orbLabelFit.catalog.test.ts` + the throwaway `OrbFitHarness` dev route.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-on-device-ui-polish-accessibility/08-RESEARCH.md
@.planning/phases/08-on-device-ui-polish-accessibility/08-PATTERNS.md
@.planning/phases/08-on-device-ui-polish-accessibility/08-UI-SPEC.md
@.planning/phases/08-on-device-ui-polish-accessibility/08-01-PLAN.md
</context>

<artifacts_produced>
## Artifacts this phase produces (created/changed by THIS plan)

- `config.show.ORB_LABEL_MAX_LINES` 3→4, `config.show.ORB_LABEL_MIN_FONT_PX` 11→10 (documented 10px @600 floor); `_CENTER` analogues verified/kept unless the harness shows overflow.
- `orbLabelFit.ts` internal `CHAR_WIDTH_FACTOR` (raised to ~0.55–0.58, conservative) and/or `USABLE_WIDTH_FACTOR` (<1) retuned to remove optimistic drift — paired with the extra line / lower floor.
- `packages/app/test/orbLabelFit.catalog.test.ts` — zero-ellipsization over all 264 real names (orb + center).
- `packages/app/src/dev/OrbFitHarness.tsx` — dev-only route (`#/dev/orb-fit`) rendering all 264 real names + `scrollWidth/scrollHeight` overflow flagging; mounted only for the dev hash, never on a production route.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Retune orb-fit constants + lock with a real-catalog test</name>
  <read_first>
    - packages/app/src/show/orbLabelFit.ts (the two-pass wrap+scale algorithm; `CHAR_WIDTH_FACTOR = 0.52` at :37; `USABLE_WIDTH_FACTOR = 1` at :39; the ellipsis fallback)
    - packages/app/src/config.ts (orb block :87-99 — `ORB_LABEL_BASE_FONT_PX` 13, `_MAX_LINES` 3, `_MIN_FONT_PX` 11; `_CENTER` variants 18/3/12)
    - packages/app/src/show/PredictionOrb.tsx (:63-67 — how the orb variant consumes the constants) and packages/app/src/show/CenterNode.tsx (:61-63 — center variant)
    - .planning/phases/08-on-device-ui-polish-accessibility/08-RESEARCH.md §"Orb-Label Legibility" (the 264-name simulation table + the three optimistic drift sources) and §"Assumptions Log" A1/A2/A3
    - packages/app/test/orbLabelFit.test.ts (existing fixture test to keep green)
    - vitest.config.ts (the `@matrix` alias to the real artifact the new test imports)
  </read_first>
  <behavior>
    - For all 264 real `@matrix` song names, `fitOrbLabel(name, REALISTIC_MIN_ORB_PX, orbOpts).ellipsized === false` (orb variant) and `fitOrbLabel(name, CENTER_INNER_PX, centerOpts).ellipsized === false` (center variant), where `REALISTIC_MIN_ORB_PX = 64` and `CENTER_INNER_PX = 92` (ORB_CENTER_DIAMETER 116 − 24 padding) per RESEARCH assumption A1.
    - At the absolute `ORB_MIN_DIAMETER` (56px) floor, at most the single 44-char outlier `(You Gotta) Fight for Your Right (To Party!)` may report ellipsized — documented as the safety-net case verified on-device (Task 2), not a real failure.
    - The retuned `CHAR_WIDTH_FACTOR` is ≥ 0.55 (conservative vs. the old optimistic 0.52) and `ORB_LABEL_MIN_FONT_PX` ≥ 10 (the documented legibility floor).
  </behavior>
  <action>
    Write `packages/app/test/orbLabelFit.catalog.test.ts` first: import the real matrix via `@matrix`, extract all `songName`s, and assert the behavior above for both orb and center opts (built from `config.show.ORB_LABEL_*`). Then retune to make it pass: in `config.ts` set `ORB_LABEL_MAX_LINES` 3→4 and `ORB_LABEL_MIN_FONT_PX` 11→10 (add a doc comment declaring 10px @ weight 600 as the minimum-legibility floor for orb data-text in the dark); verify the `_CENTER` analogues and only bump them if the assertion needs it. In `orbLabelFit.ts`, raise `CHAR_WIDTH_FACTOR` to a conservative value (~0.55–0.58) and/or lower `USABLE_WIDTH_FACTOR` below 1 to remove the optimistic drift (RESEARCH drift sources), pairing the conservatism with the extra line / lower floor so the catalog assertion still passes. Keep the ellipsis fallback in place (unreachable safety net). Do NOT change the matrix artifact or any scoring/layout code — constants only.
  </action>
  <verify>
    <automated>pnpm vitest run packages/app/test/orbLabelFit.test.ts packages/app/test/orbLabelFit.catalog.test.ts</automated>
    <automated>pnpm --filter @guezzer/app exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `config.ts` shows `ORB_LABEL_MAX_LINES: 4` and `ORB_LABEL_MIN_FONT_PX: 10` with a comment documenting the 10px @600 floor.
    - `orbLabelFit.ts` `CHAR_WIDTH_FACTOR >= 0.55`.
    - `orbLabelFit.catalog.test.ts` exists and passes: zero ellipsization over all 264 names at 64px (orb) and 92px (center); the pre-existing `orbLabelFit.test.ts` fixtures stay green.
    - App `tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The heuristic is conservative and every real catalog name fits without ellipsis at realistic rendered sizes, locked by an automated test.</done>
</task>

<task type="auto">
  <name>Task 2: Build the dev-only on-device orb-fit harness</name>
  <read_first>
    - packages/app/src/show/PredictionOrb.tsx and packages/app/src/show/CenterNode.tsx (render these at the smallest real diameter + center size)
    - packages/app/src/App.tsx (routing entry; #app-content wrapper from plan 01 — mount the harness only on the dev hash, outside normal routes)
    - packages/app/src/routing/useHashRoute.ts (hash routing; mirror the dev-gating idiom used by the existing `?mockLatest=1` harness)
    - .planning/phases/08-on-device-ui-polish-accessibility/08-RESEARCH.md §"Verification harness (A)" (renders all 264 real names; flag overflow via `el.scrollWidth > el.clientWidth || el.scrollHeight > el.clientHeight`)
    - $HOME/.claude/projects/... device-uat-hosting memory (cloudflared HTTPS tunnel with --http-host-header localhost for iOS)
  </read_first>
  <action>
    Create `packages/app/src/dev/OrbFitHarness.tsx` (`OrbFitHarness`) that imports the real `@matrix` names and renders each as a `PredictionOrb` at `ORB_MIN_DIAMETER` (and a representative solved diameter) plus a `CenterNode`, then after layout flags any element whose `scrollWidth > clientWidth || scrollHeight > clientHeight`, listing offenders on-screen. In `App.tsx`, mount `OrbFitHarness` ONLY when `location.hash === "#/dev/orb-fit"` (dev-gated; must not appear on any production tab/route and must not alter existing routing). Add a code comment marking it throwaway (POLISH-01 on-device verification) with a note that it is removed post-phase.
  </action>
  <verify>
    <automated>pnpm --filter @guezzer/app exec tsc --noEmit</automated>
    <automated>pnpm vitest run packages/app/test/route.test.ts</automated>
    <human-check>End-of-phase device check (POLISH-01): serve over the cloudflared HTTPS tunnel, open `#/dev/orb-fit` on the owner's iPhone, confirm all 264 names render fully with the harness reporting ZERO overflow offenders (esp. the 44-char outlier + `Deserted Dunes Welcome Weary Feet`); if any overflow, lower the floor toward 10px / raise `CHAR_WIDTH_FACTOR` per Task 1 and re-run.</human-check>
  </verify>
  <acceptance_criteria>
    - `OrbFitHarness.tsx` exists, imports real `@matrix` names, renders orb + center, and flags overflow via `scrollWidth/scrollHeight`.
    - The harness mounts only on `#/dev/orb-fit`; existing routes are unaffected (route.test.ts green).
    - App `tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The owner can verify real rendered legibility of all 264 names on-device; the harness is dev-gated and marked for post-phase removal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kglw-derived song names → canvas/SVG orb text | Names render via canvas `fillText` / React text only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-15 | Tampering/Elevation | orb label text | mitigate | Song names remain canvas `fillText` / React text (T-07-02) — no `dangerouslySetInnerHTML`; the retune changes only sizing constants, not the render path. |
| T-08-16 | Information Disclosure | dev harness route | accept | `#/dev/orb-fit` renders only the already-bundled public catalog names; no secrets. It is dev-gated and marked for removal; leaving it briefly exposes nothing beyond the shipped matrix. |
| T-08-SC | Tampering (supply chain) | npm installs | accept | No new packages; constants + one dev component. |
</threat_model>

<verification>
- `pnpm vitest run packages/app` green including `orbLabelFit.catalog.test.ts`.
- On-device: `#/dev/orb-fit` harness reports zero overflow offenders across all 264 names.
- Grep: `ORB_LABEL_MAX_LINES: 4`, `ORB_LABEL_MIN_FONT_PX: 10`, `CHAR_WIDTH_FACTOR >= 0.55`.
</verification>

<success_criteria>
- Every prediction-orb and center-node real catalog name renders fully — no truncation/overflow/oversizing — at realistic rendered sizes (POLISH-01), locked by an automated catalog test and confirmed on-device.
- The ellipsis fallback stays as an unreachable safety net; the 10px @600 floor is documented.
</success_criteria>

<output>
Create `.planning/phases/08-on-device-ui-polish-accessibility/08-06-SUMMARY.md` when done
</output>
