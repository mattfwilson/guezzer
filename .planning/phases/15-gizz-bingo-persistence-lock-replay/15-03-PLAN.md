---
phase: 15-gizz-bingo-persistence-lock-replay
plan: 03
type: execute
wave: 3
depends_on: [15-02]
files_modified:
  - packages/app/src/games/bingoReplay.ts
  - packages/app/src/games/GamesView.tsx
  - packages/app/src/dex/RecapView.tsx
  - packages/app/src/components/BottomTabBar.tsx
  - packages/app/src/routing/useHashRoute.ts
  - packages/app/src/App.tsx
  - packages/app/test/bingoReplay.test.ts
  - packages/app/test/recapView.test.tsx
autonomous: true
requirements: [BINGO-07]
must_haves:
  truths:
    - "A 4th bottom tab GizzGames routes to a replay-only view with a 'Deal a card — coming soon' teaser; empty state reads as intentional, not broken"
    - "A past show's frozen card renders inside RecapView as a read-only 4x4 board with marked squares, win badges (line / four-corners / X / blackout), and per-square 'Lit by {song}' full detail — a 'relive it' payoff, not just an outcome summary; a pure re-derivation, marks never stored (D-06)"
    - "The opener square fires on the first real entry (0-based reindex); a neverCaught square uses the FROZEN caughtSnapshot so a later show's catch never retroactively un-marks it"
    - "RecapView shows NO Bingo section for a show with no card"
  artifacts:
    - path: "packages/app/src/games/bingoReplay.ts"
      provides: "replayCard adapter: BingoCardRow + trail + artifacts -> { marked, wins }"
      exports: ["replayCard"]
    - path: "packages/app/src/games/GamesView.tsx"
      provides: "GizzGames replay list + coming-soon teaser"
    - path: "packages/app/src/routing/useHashRoute.ts"
      provides: "games route added to ROUTES allow-list"
      contains: "games"
  key_links:
    - from: "bingoReplay.ts replayCard"
      to: "@guezzer/core deriveMarks/detectWins"
      via: "0-based reindexed trail + frozen caughtSnapshot"
      pattern: "deriveMarks\\("
    - from: "dex/RecapView.tsx"
      to: "db.bingoCards"
      via: "useLiveQuery by sessionId; section absent when no card"
      pattern: "bingoCards"
---

<objective>
Deliver BINGO-07 replay and the D-01/D-02 GizzGames tab: a pure app->core adapter
(`replayCard`) that re-derives a frozen card's final board over the persisted trail,
a read-only Bingo section inside `RecapView` (full board + win badges + which-song-lit
each), and a 4th "GizzGames" bottom tab whose view lists replayable cards with an
honest "Deal — coming soon" teaser (the deal UX is Phase 16).

Purpose: a "relive it" payoff derived on every read — marks are never stored (D-23) —
that is byte-identical to what the user saw live, drift-proofed by the frozen
`caughtSnapshot` and the 0-based opener reindex.
Output: replay adapter + RecapView section + GizzGames tab, all read-only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-gizz-bingo-persistence-lock-replay/15-CONTEXT.md
@.planning/phases/15-gizz-bingo-persistence-lock-replay/15-RESEARCH.md
@.planning/phases/15-gizz-bingo-persistence-lock-replay/15-PATTERNS.md
@.planning/phases/15-gizz-bingo-persistence-lock-replay/15-UI-SPEC.md
</context>

<artifacts_this_plan_produces>
Symbols this plan creates (excluded from downstream drift verification):
- `replayCard(row, entries, matrix, archive, rarity, albums)` (games/bingoReplay.ts) -> `{ marked: MarkedCard, wins: Win[] }`.
- `GamesView` React component (games/GamesView.tsx).
- 4th `TABS` entry `{ route: "games", label: "GizzGames", Icon: Gamepad2 }` (BottomTabBar.tsx).
- `"games"` added to `ROUTES` (useHashRoute.ts) + the `route === "games"` branch in App.tsx.
- The RecapView "Bingo" section (board grid + win badges + "Lit by {song}" captions).
</artifacts_this_plan_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: replayCard app->core adapter (0-based reindex + frozen caughtSnapshot)</name>
  <files>packages/app/src/games/bingoReplay.ts, packages/app/test/bingoReplay.test.ts</files>
  <read_first>
    - packages/core/src/bingo/mark.ts (the `MarkTrailEntry` contract 33-37; opener = `position === 0` at 84; neverCaught = `!caughtSnapshot.has(songId)` at 91-94; `deriveMarks` signature 111-117)
    - packages/core/src/bingo/context.ts (`buildBingoContext(matrix, archive, rarity, albums, cfg)` 92-98)
    - packages/core/src/bingo/wins.ts (`detectWins` 49; `MarkedSquare.markedByPosition` / FREE_SENTINEL usage)
    - packages/core/src/bingo/types.ts (BingoCard 60-67, MarkedCard/Win/MarkedSquare 81-97, FREE_SENTINEL 74)
    - packages/app/src/db/db.ts (BingoCardRow from plan 15-02; TrackedEntry 96-118 — `position` is 1-based and gapped)
    - packages/core/test/bingo/mark.test.ts (the fixture idiom: a card + trail with a pre-computed expected board)
  </read_first>
  <behavior>
    - Given a locked BingoCardRow + a TrackedEntry[] trail whose first-by-position real entry is the card's opener song, the opener event square is MARKED (proves the 0-based reindex — passing raw 1-based positions would leave it dark).
    - Given a neverCaught square and a frozen `row.caughtSnapshot` that does NOT contain a song played in the trail, that square marks; a caughtSnapshot that DOES contain it leaves the predicate to reject — proving replay reads the FROZEN set, never the live dex.
    - Placeholder / null-songId trail entries are skipped; `detectWins` over the marked board returns the expected Win[] for a fixture that completes a line.
    - The free-center square is pre-marked (markedByPosition === FREE_SENTINEL).
  </behavior>
  <action>
    Create games/bingoReplay.ts exporting `replayCard(row: BingoCardRow, entries: TrackedEntry[], matrix, archive, rarity, albums)` returning `{ marked, wins }`. Reconstruct the pure `BingoCard` from `row.card` (nested per plan 15-02). Build the context with `buildBingoContext(matrix, archive, rarity, albums)` from `@guezzer/core`. Adapt the trail with the TWO load-bearing correctness points (RESEARCH Pitfalls 1+2): (1) sort a COPY of entries by stored `position` ascending, then map to `MarkTrailEntry` assigning fresh contiguous indices `0..N-1` as `position` (mark.ts:84 hard-codes opener = index 0; the stored TrackedEntry.position is 1-based and gapped by deleteEntry, db.ts:334); (2) pass `new Set(row.caughtSnapshot)` — the FROZEN set — as the caughtSnapshot arg, never the live dex (mark.ts:91-94). Then `const marked = deriveMarks(card, trail, ctx, new Set(row.caughtSnapshot))` and `const wins = detectWins(marked)`. Pass ONLY the `{songId, position, isPlaceholder}` subset to the fold (D-22 — never the raw app row). This adapter is the single shared derivation for replay (and Phase-16 live marking) so `live == replay == catch-up` holds. Create bingoReplay.test.ts covering all four behaviors with an explicit fixture (reuse the mark.test.ts fixture idiom).
  </action>
  <verify>
    <automated>npx vitest run packages/app/test/bingoReplay.test.ts</automated>
  </verify>
  <done>replayCard produces the expected board + wins; opener fires via 0-based reindex; neverCaught uses the frozen caughtSnapshot; tests green; app `tsc --noEmit` clean.</done>
</task>

<task type="auto">
  <name>Task 2: GizzGames 4th tab + games route + GamesView replay list + teaser</name>
  <files>packages/app/src/components/BottomTabBar.tsx, packages/app/src/routing/useHashRoute.ts, packages/app/src/App.tsx, packages/app/src/games/GamesView.tsx</files>
  <read_first>
    - packages/app/src/components/BottomTabBar.tsx (the whole file — the `TABS` array 4-8 and the `flex-1 min-h-11 min-w-11` render 24-42)
    - packages/app/src/routing/useHashRoute.ts (the `ROUTES` allow-list at 9 — the T-03-02 security control)
    - packages/app/src/App.tsx (the route switch at 69-79 + the `scroll` prop logic at 67)
    - packages/app/src/dex/RecapView.tsx (the `useLiveQuery(() => db.bingoCards.toArray())` list idiom + the config.copy read pattern)
    - packages/app/src/config.ts (config.copy.games.* added in plan 15-02 — read only)
  </read_first>
  <acceptance_criteria>
    - `ROUTES` in useHashRoute.ts includes `"games"` (the allow-list still validates the hash and only SELECTs a view — T-03-02).
    - BottomTabBar renders exactly four tabs (`show`, `explore`, `dex`, `games`); the games tab label is "GizzGames" (14px semibold) with the lucide-react `Gamepad2` icon; each tab stays `flex-1 min-h-11 min-w-11`.
    - App.tsx renders `<GamesView />` when `route === "games"` (Dex/Games scroll; not in the show/explore non-scroll set).
    - GamesView renders the "Deal a card — Coming soon" teaser card (config.copy.games) with a visibly DISABLED affordance, plus a `useLiveQuery(() => db.bingoCards.toArray())` list; with zero cards it shows the "No cards yet" empty-list copy — never a blank screen or an error (D-02).
    - GamesView owns its own `calc(env(safe-area-inset-top) + 12px)` header inset (UX-01 per-surface idiom), applied once.
  </acceptance_criteria>
  <action>
    In useHashRoute.ts change `ROUTES` to `["show", "explore", "dex", "games", "settings"] as const`. In BottomTabBar.tsx import `Gamepad2` from `lucide-react` (beside `BookOpen, Compass, Music`) and add a 4th `TABS` entry `{ route: "games", label: "GizzGames", Icon: Gamepad2 }` (D-01 — generic games-hub icon since the tab is the forward-compatible home for future games; accept the 3->4 tap-target tightening, `flex-1` keeps each >= 44px). In App.tsx add a `route === "games" ? <GamesView />` branch to the switch (69-79) and import GamesView; leave the `scroll` prop so games scrolls (route not in show/explore). Create games/GamesView.tsx composing the RecapView `useLiveQuery` idiom: read `db.bingoCards.toArray()`, render a replay list (each card row keyed by cardId, showing denormalized `showDate`/`venueName`/`city` as ESCAPED React text — never innerHTML) plus the coming-soon teaser card from `config.copy.games`; the teaser affordance is a disabled control labeled "Coming soon" (must read as forthcoming, not broken). Row taps that open a card's RecapView are acceptable but optional this phase (RecapView is already reachable from Dex history); keep the empty state honest. All strings from `config.copy.games` (read-only; added in 15-02).
  </action>
  <verify>
    <automated>npx vitest run packages/app/test/bingoReplay.test.ts && npx tsc -p packages/app --noEmit</automated>
  </verify>
  <done>Four tabs incl. GizzGames; `games` route in ROUTES + App switch; GamesView shows teaser + live card list + honest empty state; app tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: RecapView Bingo section — read-only board + win badges + Lit-by + present/absent render test</name>
  <files>packages/app/src/dex/RecapView.tsx, packages/app/test/recapView.test.tsx</files>
  <read_first>
    - packages/app/src/dex/RecapView.tsx (the whole file — the `useLiveQuery` reads 57-60, the loader guards 63-68, the `useMemo` derivation 67-94, the section layout, the T-06-21 escaped-text discipline at line 16)
    - packages/app/test/recapView.test.tsx (the existing RecapView render-test idiom — resetDb + seed + render + query assertions to extend)
    - packages/app/src/games/bingoReplay.ts (replayCard, from Task 1)
    - packages/app/src/show/matrix.ts (`loadMatrix()` guarded/memoized `{ ok, matrix }` sentinel to add beside loadArchive/loadDexAlbums/getRarityIndex)
    - packages/app/src/config.ts (config.copy.recap bingo keys added in 15-02 — read only)
    - .planning/phases/15-gizz-bingo-persistence-lock-replay/15-UI-SPEC.md (Color contract: marked=caught-green #22C55E w/ #0C0C10 text; unmarked=#17171F w/ hairline; free=marked "Free"; win badge=accent #F2C14E)
  </read_first>
  <acceptance_criteria>
    - RecapView reads `db.bingoCards` via `useLiveQuery` and finds the row where `sessionId === this session`; when none matches, NO Bingo section renders (D-05 — early-return null for that block, not an empty shell).
    - When a card exists, the section renders a 4x4 grid: marked squares use caught-green `#22C55E` fill with near-black `#0C0C10` label; unmarked squares use `#17171F` with `#F5F5F7` label + `#2A2A34` hairline; the free center shows "Free" in the marked treatment.
    - Win badges (config.copy.recap labels "Line"/"Four corners"/"X"/"Blackout") render as small accent `#F2C14E` chips from `detectWins`; when there are no wins the "No lines this show" copy shows and the full board still renders.
    - Each marked non-free square shows a "Lit by {songName}" caption (config.copy.recap.litBy) resolved from the trail entry at `markedByPosition`; the free center shows "Free" and no caption.
    - All kglw-derived strings (square `label`, song names, venue/city) render as escaped React text only — no `dangerouslySetInnerHTML`.
    - recapView.test.tsx (jsdom) proves the D-05 present/absent contract: seeding a session WITH a locked bingoCards row renders the Bingo section (the config.copy.recap.bingoHeading is present); seeding a session with NO card renders NO Bingo section (heading absent). This is the mandated render assertion for the new UI surface.
  </acceptance_criteria>
  <action>
    In RecapView.tsx add `loadMatrix()` beside the existing loaders (63-65) and a `useLiveQuery(() => db.bingoCards.toArray())` read (copy the 57-60 idiom). In a new `useMemo` (mirroring the deriveRecap memo 67-94, guarding `loadMatrix().ok` exactly like `archiveResult.ok`), find the card row for this `sessionId`; if absent return null. Otherwise call `replayCard(row, trackedEntries-for-this-session, matrix, archive, rarity, albums)` to get `{ marked, wins }`. Render a "Bingo" section (heading from config.copy.recap.bingoHeading) BELOW the existing setlist block: a 4x4 grid over `marked.squares` (row-major, ~80px cells, 8px gap) applying the 15-UI-SPEC color contract (marked -> caught-green #22C55E / #0C0C10 text; unmarked -> #17171F / #F5F5F7 / #2A2A34 hairline; the FREE_SENTINEL cell -> marked treatment with the "Free" label). For each marked non-free cell render a muted 14px "Lit by {songName}" caption, resolving songName from the trail entry whose reindexed position equals `markedByPosition` (build a position->songName map from the same 0-based reindex the adapter used, or map back via the sorted trail). Render win badges from `wins` as small accent `#F2C14E` chips (labels from config.copy.recap); if `wins` is empty show config.copy.recap.noWin. Read-only — NO tap-to-mark (D-04), NO celebration/supernova (Phase 16). Section is ABSENT when no card (D-05). Escaped React text only (T-06-21). Extend recapView.test.tsx (copy the existing seed+render idiom): add a case that seeds a session with a locked BingoCardRow + trail and asserts the config.copy.recap.bingoHeading text renders (present-with-card), and a case that seeds a session with no bingoCards row and asserts the heading is NOT in the document (absent-without-card, D-05).
  </action>
  <verify>
    <automated>npx vitest run packages/app/test/recapView.test.tsx && npx tsc -p packages/app --noEmit</automated>
  </verify>
  <done>RecapView shows a read-only frozen board + win badges + Lit-by captions when a card exists, and nothing when it doesn't; recapView.test.tsx asserts the present/absent D-05 contract and existing cases still green; app tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored card/trail -> replay render | Frozen `resolvedDefs[].label` + denormalized venue/city are kglw-derived untrusted strings rendered on the replay board |
| location.hash -> route | The hash selects a view via the fixed `ROUTES` allow-list |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-07 | Tampering / Injection (XSS) | RecapView + GamesView rendering square labels / venue / city | mitigate | Render as escaped React text nodes only; no `dangerouslySetInnerHTML` (T-06-21) |
| T-15-08 | Tampering | `location.hash` route selection | mitigate | `games` added to the fixed `ROUTES` allow-list; hash only ever SELECTs a view, never assigned to innerHTML/location/eval (T-03-02) |
| T-15-09 | Information consistency (silent divergence) | replay board differing from what was seen live | mitigate | 0-based reindex fires opener; FROZEN caughtSnapshot drives neverCaught — both pinned by bingoReplay.test.ts |
</threat_model>

<verification>
- `npx vitest run packages/app/test/bingoReplay.test.ts` green.
- `npx vitest run packages/app/test/recapView.test.tsx` green (present/absent Bingo section + no regression).
- `npx vitest run` (both projects) green; app `tsc --noEmit` clean.
</verification>

<success_criteria>
- GizzGames tab present with an honest replay-only view + coming-soon teaser.
- Any past show with a card replays a read-only full board + win badges + Lit-by; shows without a card show no Bingo section.
- Replay is byte-identical to live over the same trail.
</success_criteria>

<output>
Create `.planning/phases/15-gizz-bingo-persistence-lock-replay/15-03-SUMMARY.md` when done.
</output>
