---
phase: 10-pre-show-validation-device-dry-run
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/cli/review-tuning-tags.ts
  - packages/core/src/ingest/tuning-tags.ts
  - packages/core/test/review-tuning-tags.test.ts
  - data/tuning-review.md
  - data/tuning-tags.json
  - data/normalized/transition-matrix.json
  - data/backtest.json
  - data/backtest-report.md
  - .planning/phases/10-pre-show-validation-device-dry-run/10-HUMAN-UAT.md
autonomous: false
requirements: [VALID-01]

must_haves:
  truths:
    - "Running the review CLI prints an expected-vs-actual family table for >=10 named canonical songs (VALID-01)"
    - "The report surfaces cs-standard/other candidates (a family the album-default logic can never assign) AND, via a satisfiable predicate, hand-tagged overrides worth re-confirming — both those diverging from their album default and those with no album default to compare against; an empty leg is a valid 'nothing to re-confirm' pass, not a promise of non-empty output (D-02)"
    - "The owner has confirmed the ~10 canonical songs are musically sensible, OR any genuine error was hand-fixed and build-model + run-backtest re-run with no top-k regression vs the committed baseline (D-03)"
  artifacts:
    - path: "packages/core/src/cli/review-tuning-tags.ts"
      provides: "Read-only VALID-01 tuning-review CLI (runReviewTuningTags, formatReviewSummary, formatReviewReport)"
      exports: ["runReviewTuningTags", "formatReviewSummary", "formatReviewReport"]
    - path: "packages/core/test/review-tuning-tags.test.ts"
      provides: "Unit coverage of spot-check + anomaly derivation over fixtures"
    - path: ".planning/phases/10-pre-show-validation-device-dry-run/10-HUMAN-UAT.md"
      provides: "VALID-01 spot-check + sweep evidence section"
      contains: "### 1."
  key_links:
    - from: "packages/core/src/cli/review-tuning-tags.ts"
      to: "packages/core/src/ingest/tuning-tags.ts"
      via: "reuse deriveCatalogFromCorpus + tuningTagsFileSchema + newly-exported findMatchedAlbumTitles/defaultFamilyForAlbum (read-only, never mergeTuningTags)"
      pattern: "deriveCatalogFromCorpus|findMatchedAlbumTitles|defaultFamilyForAlbum"
    - from: "data/tuning-tags.json"
      to: "data/backtest-report.md"
      via: "conditional D-03 rebuild: build-model -> run-backtest"
      pattern: "build-model|run-backtest"
---

<objective>
Deliver VALID-01: prove the tuning-family tags are musically sane before show #1 without blindly regenerating them. Build a read-only review CLI that (a) cross-checks ~10 canonical well-known songs (expected vs actual `family`) and (b) surfaces `cs-standard`/`other` candidates the album-default logic can never assign, plus hand-tagged overrides worth re-confirming (those that diverge from their album default AND those with no studio-album default to compare against) — the owner-knowledge edits the auto-logic cannot check. The owner eyeballs the report and either confirms (the likely outcome — `needsReview` is already 0, 52 songs hand-tagged) or hand-fixes genuine errors and re-runs `build-model` + `run-backtest` to prove no prediction regression (D-03).

Purpose: Close DATA-04's human spot-check so predictions aren't skewed by silent tuning misclassification.
Output: A read-only CLI + unit test, an optional `data/tuning-review.md` report, and the VALID-01 evidence section of `10-HUMAN-UAT.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-pre-show-validation-device-dry-run/10-CONTEXT.md
@.planning/phases/10-pre-show-validation-device-dry-run/10-PATTERNS.md
@packages/core/src/cli/generate-tuning-tags.ts
@packages/core/src/ingest/tuning-tags.ts
@packages/core/src/cli/run-backtest.ts
@packages/core/src/cli/build-model.ts
@data/tuning-tags.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the read-only review-tuning-tags CLI + unit test (with an export-only helper change)</name>
  <files>packages/core/src/cli/review-tuning-tags.ts, packages/core/src/ingest/tuning-tags.ts, packages/core/test/review-tuning-tags.test.ts</files>
  <read_first>
    - packages/core/src/cli/generate-tuning-tags.ts (copy the load half: imports, extractRows lines 38-54, defaultOptions lines 56-62, the corpus/albums/tags load block lines 69-95, and the isMain guard lines 120-131 — OMIT generateTuningTags/mergeTuningTags/writeFile, this CLI is read-only per D-01)
    - packages/core/src/ingest/tuning-tags.ts (tuningFamilyValues line 25, tuningTagsFileSchema, albumRowSchema, deriveCatalogFromCorpus; findMatchedAlbumTitles lines 150-167 and defaultFamilyForAlbum lines 176-180 — these two are currently module-PRIVATE and must be made `export`-only in this task so the CLI reuses the exact slug/`islive` album-join and album-default rule instead of duplicating it; note defaultFamilyForAlbum ONLY ever emits standard/microtonal, which is why cs-standard/other candidates must be nominated by heuristic and hand-tagged overrides are the outliers worth re-confirming)
    - packages/core/src/cli/run-backtest.ts (lines 41-43 escapeMarkdownExcerpt, lines 54-112 sections/table builder, parseArgs lines ~140-153 for the optional --out flag)
    - packages/core/test/tuning-tags.test.ts (fixture-catalog + albumRow() helper idiom for the new test)
    - data/tuning-tags.json (the committed 264-entry file: 238 standard / 26 microtonal, 0 cs-standard/other, 52 source:hand-tagged — of which 16 match their album default and 36 have no studio-album match)
  </read_first>
  <action>
    Make an EXPORT-ONLY edit to `packages/core/src/ingest/tuning-tags.ts`: add the `export` keyword to the two existing pure, read-only helpers `findMatchedAlbumTitles` (line 150) and `defaultFamilyForAlbum` (line 176). Change NOTHING else in that file — no body edits, no signature changes, no new logic. This makes the exact song->album slug/`islive` join and the album-default rule reusable by the review CLI so it does NOT silently re-implement the private join (WARNING 2 fix). Both helpers are pure and never write — the read-only guarantee is unaffected.

    Create `packages/core/src/cli/review-tuning-tags.ts` as a Node-native TS CLI (`.ts` import extensions, erasable-syntax-only, `config` from `../config.ts`) opening with a docblock that states DATA-04/D-01/D-02, that it is READ-ONLY (never writes tuning-tags.json), and "Never invoked from CI (CLAUDE.md manual-run only)". Reuse (do NOT re-implement) `deriveCatalogFromCorpus`, `tuningTagsFileSchema`, `albumRowSchema`, and the now-exported `findMatchedAlbumTitles` + `defaultFamilyForAlbum` from `../ingest/tuning-tags.ts`; copy `extractRows`, `defaultOptions` (using `config.corpusArtifactPath`, `join(config.dataRawDir, "albums.json")`, `config.tuningTagsPath`), and the isMain guard verbatim from generate-tuning-tags.ts. Do NOT import `mergeTuningTags` or `generateTuningTags` (the write path).

    Export `interface ReviewTuningTagsCliResult` with two arrays: `spotChecks: { songId, name, expected, actual, ok }[]` and `anomalies: { songId, name, actual, reason }[]`, plus a `summary: { total, standard, microtonal, csStandard, other, handTagged }` count block. Export `async function runReviewTuningTags(options)` that loads corpus + KGLW-filtered albums (artist_id === 1) + zod-parsed tuning-tags.json into a `familyBySongId` Map and a `sourceBySongId` Map, then computes:

    (1) SPOT CHECKS — declare a module-level `CANONICAL_SPOT_CHECKS: { name: string; expected: TuningFamily }[]` constant seeded with the Phase 01 anchors and album-anchored well-known songs: "12 Bar Bruise"->standard, "Doom City"->microtonal, "Rattlesnake"->microtonal, "Robot Stop"->standard, "Gamma Knife"->standard, "Crumbling Castle"->standard, "The Dripping Tap"->standard, "Honey"->standard, "Hot Water"->microtonal, "Straws in the Wind"->microtonal, "Planet B"->standard, "Mars for the Rich"->standard (>=10 entries; microtonal = Flying Microtonal Banana / K.G. / L.W. members, everything else standard). Match each by case-insensitive name against the catalog+tags; emit `{ songId, name, expected, actual, ok: expected === actual }`. A name absent from the catalog is emitted with `actual: "MISSING"` and `ok: false` so the owner sees the gap rather than a silent skip.

    (2) ANOMALY SWEEP (D-02) — surface the two families the album-default logic can never assign, plus owner-overrides worth re-confirming. For each catalog song, compute its matched studio-album titles via the exported `findMatchedAlbumTitles(song, kglwAlbumRows)`, then:
      (a) CS-STANDARD CANDIDATES — flag any `source: "album-default"` entry whose matched titles intersect a local `DOWN_TUNED_ALBUM_HINT` list (declare it, seeded with "Infest the Rats' Nest") as a `cs-standard` candidate with `reason: "down-tuned era album — album-default cannot assign cs-standard"`.
      (b) OTHER CANDIDATES — flag any `isCover` catalog song still carrying an `album-default` family as an `other` candidate with `reason: "cover — original-artist tuning is owner knowledge"`.
      (c) HAND-TAGGED OVERRIDES WORTH RE-CONFIRMING (replaces the vacuous album-default-vs-its-own-default check — WARNING 1 fix; VALID-01's whole point is that the owner re-eyeballs the judgment-heavy hand-edits the auto-logic can NEVER check). For each `source: "hand-tagged"` entry, compute the set of its matched album-default families via `findMatchedAlbumTitles` + `defaultFamilyForAlbum`, then split into TWO sub-groups surfaced under distinct sub-headings:
        (c-i) DIVERGENCE — the entry HAS at least one matched studio album but its `family` equals NONE of those album-default values. Surface with `reason: "hand-tagged override diverges from album default — already reviewed, confirm still intended"`. (A hand-tagged `cs-standard`/`other` always diverges since defaultFamilyForAlbum only emits standard/microtonal; a hand-tagged standard<->microtonal flip diverges from its non-matching album default.)
        (c-ii) NO ALBUM DEFAULT TO COMPARE — the entry has NO matched studio album via `findMatchedAlbumTitles` (empty result), OR is flagged `needsReview`. Surface with `reason: "hand-tagged, no album default to compare against — confirm still intended"` under a sub-heading like "hand-tagged, no album default to compare against — confirm still intended". These are exactly the owner-knowledge edits the auto-logic cannot check, so the owner must re-eyeball them directly — this is VALID-01's core coverage.
      Leg (c) is a SATISFIABLE predicate, not a guarantee of output. On the current committed data the divergence sub-group (c-i) MAY legitimately return zero rows (of the 52 hand-tags, 16 match their album default), while sub-group (c-ii) surfaces the ~36 hand-tags with no studio-album match. An empty leg-(c) section (or an empty sub-group) is a valid "nothing new to re-confirm" PASS, never a bug. A hand-tagged entry whose `family` matches its (present) album default is NOT surfaced. NEVER treat an `album-default` entry as an outlier against its own default — that intersection is empty by construction and surfaces nothing.

    Export `formatReviewSummary(result): string` (a one-line stdout summary: total, family counts, spot-check pass/fail count, anomaly count) and `formatReviewReport(result): string` that builds a `sections: string[]` markdown report (join with blank line, trailing newline) with a "# Tuning-Family Review (VALID-01)" heading, a "## Canonical spot-check" expected-vs-actual table, and a "## Anomaly sweep" section (cs-standard/other candidates and BOTH hand-tagged sub-groups under clear sub-headings) — escaping every catalog-sourced song name via a copied `escapeMarkdownExcerpt` (markdown injection surface, T-10-01). Add an optional `--out <path>` flag (copy run-backtest.ts parseArgs) that writes the report; with no flag the isMain block `console.log`s `formatReviewSummary` then `formatReviewReport` to stdout. Never write `data/tuning-tags.json`.

    Create `packages/core/test/review-tuning-tags.test.ts` (node env, mirrors tuning-tags.test.ts): build a small fixture catalog + album rows + tags map and assert (i) a correct spot-check yields `ok:true`; (ii) a deliberately-wrong family yields `ok:false`; (iii) a `MISSING` canonical name is flagged `ok:false`; (iv) a down-tuned-album `album-default` entry (its fixture album row titled "Infest the Rats' Nest", islive 0) appears in `anomalies` as a cs-standard candidate; (v) an `isCover` song still on `album-default` appears as an `other` candidate; (vi) a `hand-tagged` entry whose `family` DIVERGES from its matched album default (e.g. hand-tagged `cs-standard`, or a standard->microtonal flip vs a non-microtonal-seed album) IS surfaced as a leg-(c-i) "override diverges — confirm still intended" anomaly; (vii) a `hand-tagged` entry whose `family` MATCHES its (present) album default is NOT surfaced as an anomaly; (viii) a `hand-tagged` entry with NO matched studio album (findMatchedAlbumTitles returns empty) IS surfaced as a leg-(c-ii) "no album default to compare against — confirm still intended" anomaly. Test the pure `runReviewTuningTags` against injected fixture paths OR factor the derivation into a pure helper the test calls directly.
  </action>
  <verify>
    <automated>npx vitest run packages/core/test/review-tuning-tags.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run packages/core/test/review-tuning-tags.test.ts` passes with the eight assertions above (including (viii): a hand-tagged entry with no matched studio album is surfaced under the leg-(c-ii) no-album-default sub-group).
    - `node packages/core/src/cli/review-tuning-tags.ts` exits 0 and prints a canonical spot-check table with expected-vs-actual family for >=10 named songs.
    - The report includes an anomaly section flagging cs-standard/other candidates AND hand-tagged overrides worth re-confirming under two clear sub-headings — both those that diverge from their album default (c-i) and those with no album default to compare against (c-ii). On current committed data, sub-group (c-ii) MUST list the ~36 hand-tags with no studio-album match; either sub-group may legitimately be empty (an empty section is a valid "nothing to re-confirm" pass, not a failure).
    - `grep -vE '^\s*(//|\*|/\*)' packages/core/src/cli/review-tuning-tags.ts | grep -c "mergeTuningTags\|writeFile.*tuningTagsPath"` returns 0 (read-only: no merge/write path; the pure read-only helpers deriveCatalogFromCorpus/findMatchedAlbumTitles/defaultFamilyForAlbum MAY be imported).
    - `git diff packages/core/src/ingest/tuning-tags.ts` shows ONLY the two `export` keyword additions (no body/logic change).
    - App+core typecheck clean (the tuning-tags.ts export-only edit adds no new types or behavior).
  </acceptance_criteria>
  <done>The read-only review CLI exists, is unit-tested, reuses the load-half helpers plus the newly-exported album-join/album-default helpers, surfaces a hand-tagged-override re-confirmation leg with both a divergence sub-group and a no-album-default sub-group (each of which may legitimately be empty), and never touches the write path.</done>
</task>

<task type="auto">
  <name>Task 2: Run the sweep and scaffold the VALID-01 evidence in 10-HUMAN-UAT.md</name>
  <files>data/tuning-review.md, .planning/phases/10-pre-show-validation-device-dry-run/10-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/05-live-sync-data-safety/05-HUMAN-UAT.md (front-matter + Tests/Summary/Gaps skeleton to copy verbatim, adjusting phase)
    - .planning/phases/01-corpus-ingestion-schema-foundation/01-HUMAN-UAT.md (the prior tuning spot-check this re-confirms — reference test #1)
    - packages/core/src/cli/review-tuning-tags.ts (the CLI built in Task 1, to run it)
  </read_first>
  <action>
    Run `node packages/core/src/cli/review-tuning-tags.ts --out data/tuning-review.md` and capture the stdout summary + report. Create `.planning/phases/10-pre-show-validation-device-dry-run/10-HUMAN-UAT.md` using the `05-HUMAN-UAT.md` front-matter + section skeleton (status: pending, phase: 10-pre-show-validation-device-dry-run, source: [10-VERIFICATION.md], started/updated ISO timestamps). Add the VALID-01 evidence as test "### 1. Tuning-family spot-check + anomaly sweep (VALID-01)": in the `expected:` line, paste the canonical spot-check expected-vs-actual table and the anomaly candidate list (cs-standard/other candidates plus both hand-tagged sub-groups) from the CLI output, and instruct the owner to confirm each canonical song is musically sensible and to eyeball the cs-standard/other candidates and the hand-tagged overrides. Leave `result:` as `[pending owner review]`. Note the Phase 01 spot-check that this re-confirms. Do NOT yet fill the VALID-02 steps (that is plan 10-02). Set Summary counts to reflect one pending VALID-01 test.
  </action>
  <verify>
    <automated>test -f .planning/phases/10-pre-show-validation-device-dry-run/10-HUMAN-UAT.md && node packages/core/src/cli/review-tuning-tags.ts >/dev/null 2>&1 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `data/tuning-review.md` exists and contains the canonical spot-check table + anomaly sweep sections.
    - `10-HUMAN-UAT.md` exists with valid front-matter and a "### 1." VALID-01 test whose `expected:` line embeds the pre-run spot-check table and anomaly list, `result:` pending.
    - The CLI ran successfully to produce the embedded content (no hand-authored numbers).
  </acceptance_criteria>
  <done>The VALID-01 review has been run and its output is captured for owner review in both data/tuning-review.md and the UAT doc.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Owner eyeballs the review — confirm, or fix + rebuild + backtest (D-03)</name>
  <files>data/tuning-tags.json, data/backtest-report.md, .planning/phases/10-pre-show-validation-device-dry-run/10-HUMAN-UAT.md</files>
  <read_first>
    - data/tuning-review.md (the generated review)
    - data/tuning-tags.json (the file to hand-edit ONLY if a genuine error is found)
    - data/backtest-report.md (the committed no-regression baseline)
    - packages/core/src/cli/build-model.ts, packages/core/src/cli/run-backtest.ts (the rebuild path)
  </read_first>
  <action>Owner-executed review checkpoint (no autonomous code produced): the owner eyeballs `data/tuning-review.md`, confirms the ~10 canonical songs are musically sane and the cs-standard/other candidates + hand-tagged overrides are correct, and — ONLY if a genuine error is found — hand-edits `data/tuning-tags.json` (flip `family` to the correct closed-vocabulary value, set `source: "hand-tagged"`) then re-runs `build-model` + `run-backtest` and diffs `data/backtest-report.md` to confirm no top-k regression (D-03). The executor then records the final verdict (confirmation-only OR the fix + backtest delta) into the `result:` line of test #1 in `10-HUMAN-UAT.md` and flips its status.</action>
  <verify>
    <human-check>Owner confirms the review; executor records the verdict in 10-HUMAN-UAT.md test #1. If D-03 fired, `git diff data/backtest-report.md` shows no top-k regression.</human-check>
  </verify>
  <what-built>
    A read-only review CLI + `data/tuning-review.md` listing ~10 canonical songs (expected vs actual tuning family) and a candidate list of cs-standard/other candidates plus hand-tagged overrides worth re-confirming (those that diverge from their album default AND those with no album default to compare against), for your musical judgment.
  </what-built>
  <how-to-verify>
    1. Open `data/tuning-review.md` (or re-run `node packages/core/src/cli/review-tuning-tags.ts`).
    2. Confirm every canonical spot-check row is musically sensible (e.g. "Doom City" microtonal, "12 Bar Bruise" standard). Any `MISSING` or `ok:false` row needs your eye.
    3. Eyeball the anomaly candidates: are any flagged songs genuinely `cs-standard` (down-tuned) or `other`, does each divergent hand-tagged override still reflect what you intended, and — under the "no album default to compare against" sub-heading — do the hand-tags the auto-logic can't check still look right?
    4. CONFIRMATION BRANCH (likely): if nothing is genuinely wrong, no file changes needed — reply "confirmed".
    5. FIX BRANCH (D-03): if you find a genuine error, hand-edit `data/tuning-tags.json` — flip `family` to the correct closed-vocabulary value (standard | cs-standard | microtonal | other) and set `source: "hand-tagged"`, keeping 2-space formatting + trailing newline. Then run `node packages/core/src/cli/build-model.ts`, then `node packages/core/src/cli/run-backtest.ts`, and `git diff data/backtest-report.md` to confirm the top-k table shows NO regression before declaring done.
  </how-to-verify>
  <resume-signal>Reply "confirmed" (no errors found), or "fixed: <song(s)>" after editing + rebuilding + confirming no backtest regression. Either way, the executor records the final VALID-01 verdict (and any fixes + backtest delta) into the `result:` line of test #1 in 10-HUMAN-UAT.md and flips its status.</resume-signal>
  <acceptance_criteria>
    - Owner has reviewed every canonical spot-check row and the anomaly candidates.
    - If any edit was made: `data/tuning-tags.json` still parses under `tuningTagsFileSchema` (build-model ran clean), and `data/backtest-report.md` shows no top-k regression vs the committed baseline.
    - Test #1 `result:` in 10-HUMAN-UAT.md records the verdict (confirmation-only OR the specific fix + backtest delta).
  </acceptance_criteria>
  <done>VALID-01 is closed: tuning tags confirmed musically sane (or corrected with a proven no-regression backtest), recorded in the UAT doc.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed JSON -> review CLI | Local already-committed corpus/albums/tags read into a Node process; no network, no user input |
| owner hand-edit -> build-model reload (D-03 branch only) | A hand-authored tuning-tags.json edit re-enters the model pipeline |
| markdown report -> viewer (GitHub/VS Code preview) | Catalog-sourced song names embedded in generated markdown |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | review-tuning-tags.ts markdown report (data/tuning-review.md) | mitigate | Escape every catalog-sourced song name with copied `escapeMarkdownExcerpt` (&/</>) before embedding — markdown-viewers render embedded HTML |
| T-10-02 | Tampering | data/tuning-tags.json hand-edit (D-03 branch) | mitigate | `tuningTagsFileSchema.parse` on next build-model load hard-fails a corrupt/duplicate/out-of-vocabulary edit; the D-03 backtest compare catches a semantic regression |
| T-10-03 | Information disclosure | read-only review CLI | accept | Reads only already-committed local JSON; no network, no secrets, writes only the local review report |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | This plan installs NO new packages (all deps present); legitimacy gate N/A |
</threat_model>

<verification>
- `npx vitest run packages/core/test/review-tuning-tags.test.ts` green.
- `node packages/core/src/cli/review-tuning-tags.ts` exits 0 with a >=10-song spot-check table + anomaly section (cs-standard/other candidates + both hand-tagged sub-groups).
- Read-only proven: no merge/write of tuning-tags.json in the CLI source; tuning-tags.ts change is export-only.
- 10-HUMAN-UAT.md test #1 has a recorded VALID-01 verdict.
- If D-03 fired: `git diff data/backtest-report.md` shows no top-k regression.
</verification>

<success_criteria>
VALID-01 complete: the owner has hands-on proof the tuning model is musically sane — ~10 canonical songs verified and the cs-standard/other candidates + hand-tagged-override sweep (divergent AND no-album-default hand-tags) eyeballed, with any genuine error fixed and backtested with no regression, all recorded in 10-HUMAN-UAT.md.
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 10-01)

- `packages/core/src/cli/review-tuning-tags.ts` (NEW) — read-only VALID-01 review CLI. Exports: `runReviewTuningTags()`, `formatReviewSummary()`, `formatReviewReport()`, `ReviewTuningTagsCliResult`, `ReviewTuningTagsCliOptions`. New flag: `--out <path>`.
- `packages/core/src/ingest/tuning-tags.ts` (MODIFIED, export-only) — adds the `export` keyword to the two existing pure, read-only helpers `findMatchedAlbumTitles` and `defaultFamilyForAlbum` so the review CLI reuses the exact slug/`islive` album-join and album-default rule instead of duplicating the private logic. No body/signature/logic change; read-only guarantee intact.
- `packages/core/test/review-tuning-tags.test.ts` (NEW) — unit coverage.
- `data/tuning-review.md` (NEW, generated) — the read-only review report.
- `.planning/phases/10-pre-show-validation-device-dry-run/10-HUMAN-UAT.md` (NEW, VALID-01 section) — evidence doc; owner-executed review is a human task (Task 3), not autonomous code.
- Conditional (D-03 branch only, re-run of existing CLIs — no new code): edits to `data/tuning-tags.json` and regenerated `data/normalized/transition-matrix.json`, `data/backtest.json`, `data/backtest-report.md`.
</artifacts>

<output>
Create `.planning/phases/10-pre-show-validation-device-dry-run/10-01-SUMMARY.md` when done.
</output>
</output>
