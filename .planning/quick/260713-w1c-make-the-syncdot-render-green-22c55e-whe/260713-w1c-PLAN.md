---
quick_id: 260713-w1c
type: quick
autonomous: true
files_modified:
  - packages/app/src/live/SyncDot.tsx
---

<objective>
Owner-requested visual tweak (2026-07-14): the SyncDot should render as a GREEN
dot when online instead of muted gray, so connectivity reads at a glance during
a show. Offline stays the current hollow muted ring, unchanged.

This is an explicit owner override of 05-UI-SPEC §Color B3 (which reserved the
dot to text-muted only, "connectivity is unremarkable"). Green now means both
"hit" (trail rings) and "online" (dot) — accepted by the owner. Reuse the
existing hit-green #22C55E (CometTrail.tsx:33); do NOT mint a new color.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Green online state for SyncDot</name>
  <files>packages/app/src/live/SyncDot.tsx</files>
  <action>
    Change the online fill from MUTED (#A1A1AA) to the existing hit-green
    #22C55E. Offline remains the hollow 1px #A1A1AA ring exactly as-is.
    Keep both aria-labels unchanged. Rewrite the doc comment: it currently
    claims muted-only per 05-UI-SPEC §Color B3 — note the owner explicitly
    overrode B3 on 2026-07-14 (green online state), and that #22C55E is the
    same hit-green used by CometTrail rings, not a new token.
  </action>
  <verify>npm test passes; tsc --noEmit clean in packages/app; vite build succeeds</verify>
  <done>SyncDot fills #22C55E when online, hollow #A1A1AA ring when offline; doc comment reflects the B3 override.</done>
</task>

</tasks>
