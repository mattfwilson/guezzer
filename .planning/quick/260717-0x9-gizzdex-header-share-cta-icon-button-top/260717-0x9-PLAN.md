---
quick_id: 260717-0x9
title: GizzDex header — Share icon-button top-right + brighter segment toggle
created: 2026-07-17
mode: quick
---

# Quick Task 260717-0x9

Implements todo `2026-07-17-gizzdex-header-share-icon-button-and-brighter-segment-toggle`.

## Task 1: Share CTA → icon button, top-right by the %

- **files:** `packages/app/src/dex/DexHeader.tsx`
- **action:** Wrap the completion headline block + an icon-only Share button in a
  top `flex items-start justify-between` row. Drop the full-width bottom CTA
  (`DexHeader.tsx:59-67`). The icon button: `Share2` in accent gold (reserved
  accent use #1), `aria-label={shareCopy.cta}` (keeps the accessible name so
  `getByRole('button',{name})` still resolves), ≥44px hit target
  (`min-h-11 min-w-11`), subtle `bg-accent/15 rounded-full` affordance.
- **verify:** button top-right aligned with the `{caught}/{total} · {pct}%`
  headline; opens the share sheet; a11y name preserved.
- **done:** vertical space freed; single accent icon control on the dex face.

## Task 2: Brighter Albums/Shows toggle selected state

- **files:** `packages/app/src/dex/DexView.tsx`
- **action:** Active segment class `bg-accent/20 text-text-primary` →
  `bg-accent text-surface` (full-strength accent `#f2c14e`, dark text). Inactive
  stays `text-text-muted`. Update the adjacent comment.
- **note:** "brighter" is interpreted as the FULL existing accent gold (not a new
  hotter-orange token). If the owner wants a distinct orange, that's a follow-up
  color-token change.
- **verify:** selected half is solid gold with legible dark text; unselected
  unchanged.
- **done:** the selected toggle reads as the brighter accent.

## Verify (both)

- `tsc` clean; `vitest run` green; run the app — screenshot the GizzDex header
  showing the icon Share button + solid-gold active toggle; no console errors.

## Out of scope

Tier colors, the always-grayed shelf (done), routes/storage.
