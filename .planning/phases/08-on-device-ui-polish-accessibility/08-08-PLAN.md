---
phase: 08-on-device-ui-polish-accessibility
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/app/src/show/orbLabelFit.ts
  - packages/app/src/show/PredictionOrb.tsx
  - packages/app/src/show/CenterNode.tsx
  - packages/app/src/config.ts
  - packages/app/test/orbLabelFit.catalog.test.ts
  - packages/app/test/orbLabelFit.test.ts
  - packages/app/src/dev/OrbFitHarness.tsx
autonomous: true
gap_closure: true
requirements: [POLISH-01]

must_haves:
  truths:
    - "Every real @matrix song name fits fully INSIDE its circular prediction orb across the swept diameter range [56..112] — no line's rendered width exceeds the circle chord at that line's vertical offset, and the stacked name lines + the always-present percent line fit the circle's vertical extent (no overflow, no ellipsis)"
    - "Every real @matrix song name fits fully inside the center-node circle at the center inner width (reserved percent height = 0)"
    - "The catalog test asserts a GEOMETRIC fit predicate (chord-per-line + total height), FAILS against the current rectangular heuristic, and PASSES after the circle-aware rewrite"
    - "fitOrbLabel derives each wrapped line's usable width from the circle chord at that line's vertical offset (chord = 2·√(r² − y²)) and rejects any wrap whose total height exceeds the circle"
    - "PredictionOrb fits against the padding-subtracted content diameter and reserves vertical room for the 14px percent line (mirroring CenterNode's padding subtraction)"
    - "The OrbFitHarness flags a rendered line as an overflow offender by measuring its real width against the circle chord at its y (no overflow:hidden clip, no rectangular scrollWidth blind spot)"
  artifacts:
    - path: "packages/app/src/show/orbLabelFit.ts"
      provides: "Circle-aware fit heuristic + exported geometric fit predicate + exported CHAR_WIDTH_FACTOR"
      contains: "labelFitsCircle"
    - path: "packages/app/test/orbLabelFit.catalog.test.ts"
      provides: "Geometric zero-overflow lock swept over [56..112]"
      contains: "labelFitsCircle"
    - path: "packages/app/src/dev/OrbFitHarness.tsx"
      provides: "On-device circular-boundary spill measurement"
    - path: "packages/app/src/config.ts"
      provides: "ORB_LABEL_LINE_HEIGHT_FACTOR, ORB_LABEL_PERCENT_LINE_PX, ORB_LABEL_FACE_PADDING_PX"
  key_links:
    - from: "packages/app/src/show/PredictionOrb.tsx"
      to: "fitOrbLabel"
      via: "content diameter (diameterPx − 2·ORB_LABEL_FACE_PADDING_PX) + reservedHeightPx = ORB_LABEL_PERCENT_LINE_PX"
      pattern: "fitOrbLabel\\("
    - from: "packages/app/src/show/CenterNode.tsx"
      to: "fitOrbLabel"
      via: "content diameter (diameter − 2·padding) + reservedHeightPx = 0"
      pattern: "fitOrbLabel\\("
    - from: "packages/app/test/orbLabelFit.catalog.test.ts"
      to: "labelFitsCircle"
      via: "geometric predicate over every @matrix name at each swept diameter"
      pattern: "labelFitsCircle"
---

<objective>
Close the POLISH-01 regression found on real iPhone hardware: many smaller prediction orbs overflow their song-name labels outside the circular fill.

Root cause (08-HUMAN-UAT.md Gaps + .planning/debug/orb-label-overflow-small-orbs.md): `fitOrbLabel` models the orb as a RECTANGLE. It grants the full diameter as usable text chord for every wrapped line (USABLE_WIDTH_FACTOR = 1) and never checks total text height against the circle's vertical extent. On small orbs (real min ORB_MIN_DIAMETER = 56px, passed raw) long names wrap to 3–4 near-full-diameter lines that satisfy the rectangular char budget (`ellipsized: false`) but spill past the circular chord and the vertical fill. PredictionOrb also passes the raw diameter (never subtracting its `px-1` face padding, unlike CenterNode) and renders a fixed 14px percent line below the name that the fitter reserves no room for. The catalog test only asserted `.ellipsized === false` at 3 fixed sizes, so it never validated circular fit — the coverage gap that let this ship.

Purpose: Make the fit heuristic geometrically honest (circle, not rectangle), guard it with a geometric regression test that fails-before / passes-after, and fix the on-device harness's rectangular blind spot so POLISH-01 can be re-verified truthfully on the owner's phone.

Output: Circle-aware `fitOrbLabel` + exported geometric predicate, retuned/added config constants, padding-subtracting callers, a geometric catalog test swept over [56..112], and a circular-boundary OrbFitHarness. Core/UI separation preserved (all changes live in packages/app; no core, schema, or artifact edits). No scattered magic numbers — every geometric constant lives in config.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# The diagnosed gap + full debug session (root cause, artifacts, missing items)
@.planning/phases/08-on-device-ui-polish-accessibility/08-HUMAN-UAT.md
@.planning/debug/orb-label-overflow-small-orbs.md

# Repo conventions for this UI-only phase
@.planning/phases/08-on-device-ui-polish-accessibility/08-PATTERNS.md

# The files under repair (read before editing)
@packages/app/src/show/orbLabelFit.ts
@packages/app/src/show/PredictionOrb.tsx
@packages/app/src/show/CenterNode.tsx
@packages/app/test/orbLabelFit.catalog.test.ts
@packages/app/test/orbLabelFit.test.ts
@packages/app/src/dev/OrbFitHarness.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Geometric fit predicate + swept geometric catalog test</name>
  <files>packages/app/src/show/orbLabelFit.ts, packages/app/src/config.ts, packages/app/test/orbLabelFit.catalog.test.ts</files>
  <behavior>
    New exported geometry (pure, no DOM/React):
    - `export const CHAR_WIDTH_FACTOR` — promote the existing module-private `CHAR_WIDTH_FACTOR` (0.55) to an export so the test shares the SAME char-advance proxy as the heuristic (single source; no duplicated magic number).
    - `export function labelFitsCircle(result, contentDiameterPx, { lineHeightFactor, reservedHeightPx }): boolean` — the geometric predicate:
        - r = contentDiameterPx / 2
        - lineHeight = result.fontPx × lineHeightFactor
        - blockH = result.lines.length × lineHeight + reservedHeightPx
        - HEIGHT check: blockH ≤ contentDiameterPx (+ a tiny epsilon, e.g. 0.01) — the stacked name lines plus the reserved percent-line height must fit the circle's vertical extent.
        - topY = −blockH / 2 (the combined content block is vertically centered)
        - For each name line i (0-based): the line box spans [topY + i×lineHeight, topY + (i+1)×lineHeight]; worstY = max(|topY + i×lineHeight|, |topY + (i+1)×lineHeight|); chord = 2×√(max(0, r² − worstY²)); estWidth = line.length × CHAR_WIDTH_FACTOR × result.fontPx; require estWidth ≤ chord.
        - Return true only when the height check AND every line's chord check pass.
    Geometric catalog test (rewrite of orbLabelFit.catalog.test.ts):
    - Sweep the real dynamic orb diameter range at every integer (or ≤4px step) diameter d in [ORB_MIN_DIAMETER (56) .. ORB_MAX_DIAMETER (112)]. For each d, contentDiameter = d − 2×ORB_LABEL_FACE_PADDING_PX; for every @matrix songName assert `labelFitsCircle(fitOrbLabel(name, contentDiameter, orbOpts), contentDiameter, { lineHeightFactor, reservedHeightPx: ORB_LABEL_PERCENT_LINE_PX })` is true AND `.ellipsized === false` (POLISH-01 bar: fully rendered, no truncation and no overflow). Document that at the absolute 56px floor at most the single 44-char outlier `(You Gotta) Fight for Your Right (To Party!)` may ellipsize (the documented safety-net case) — allow that one name to be exempt only at d = 56.
    - Sweep the center variant at the center inner width (config.show.ORB_CENTER_DIAMETER − 12×2 = 92) with reservedHeightPx = 0 and centerOpts; assert `labelFitsCircle` + `.ellipsized === false` for every name.
    - Keep the "bundles the full 264-song catalog" sanity assertion.
    This test MUST FAIL against the current rectangular heuristic (RED) — on small orbs the current code returns near-full-diameter multi-line wraps whose top line's chord and whose total height violate the circle.
  </behavior>
  <action>
    Add three constants to config.show (single-config ethos, CLAUDE.md — placed in the ORB_LABEL_* block at config.ts ~89-113): `ORB_LABEL_LINE_HEIGHT_FACTOR` (line box height as a multiple of font px, i.e. `leading-tight`; start ~1.15 — a TUNABLE the executor confirms via Task 3's harness), `ORB_LABEL_PERCENT_LINE_PX` (reserved vertical px for the always-present prediction-orb percent line; the current line is `text-[14px]` so start at 14 × the line-height factor, rounded), and `ORB_LABEL_FACE_PADDING_PX` (the `px-1` face-button padding = 4, subtracted per side before fitting — mirrors CenterNode's inline 12px subtraction). Add doc comments explaining each drives the circular fit geometry, not chrome.

    In orbLabelFit.ts, export `CHAR_WIDTH_FACTOR` and add the pure `labelFitsCircle` predicate exactly per the behavior block. Do NOT yet change `fitOrbLabel`'s wrapping logic — this task only lands the geometry helper, the constants, and the failing test (RED). Keep the helper NO-DOM / NO-React (mirrors the orbitLayout pure-helper idiom, CLAUDE.md core/UI separation for the show layer).

    Rewrite orbLabelFit.catalog.test.ts per the behavior block: import the new `labelFitsCircle` + `CHAR_WIDTH_FACTOR` from orbLabelFit.ts and the new constants from config; build `orbOpts`/`centerOpts` including the new required `lineHeightFactor` + `reservedHeightPx` fields. Delete the old fixed-3-diameter `.ellipsized`-only assertions — they are the coverage gap being closed.

    Do NOT place fenced code inside this action — the formulas above are the spec; implement them directly.

    Commit RED: `test(08-08): add geometric circular-fit lock swept over [56..112] (fails against rectangular heuristic)`.
  </action>
  <verify>
    <automated>cd C:/Users/mattf/git/guezzer && npx vitest run packages/app/test/orbLabelFit.catalog.test.ts 2>&1 | tail -30</automated>
    Expected: the geometric catalog test FAILS (RED) — at least the small-diameter sweep reports names whose top-line chord or total height is violated by the current rectangular heuristic. Confirm the failure is geometric (chord/height), not an import/type error.
  </verify>
  <done>orbLabelFit.ts exports `labelFitsCircle` + `CHAR_WIDTH_FACTOR`; config.show has the three new constants; orbLabelFit.catalog.test.ts sweeps [56..112] + center width asserting the geometric predicate and DEMONSTRABLY FAILS against the unchanged heuristic (RED commit recorded).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Circle-aware fitOrbLabel + padding-subtracting callers</name>
  <files>packages/app/src/show/orbLabelFit.ts, packages/app/src/show/PredictionOrb.tsx, packages/app/src/show/CenterNode.tsx, packages/app/test/orbLabelFit.test.ts, packages/app/src/config.ts</files>
  <behavior>
    fitOrbLabel becomes circle-aware. Extend `FitOrbLabelOptions` with two REQUIRED fields (required, not optional-with-defaults — optimistic defaults are exactly what caused this bug): `lineHeightFactor: number` and `reservedHeightPx: number`. `diameterPx` is now the CONTENT circle diameter (callers subtract padding).
    Wrapping algorithm (replaces the rectangular passes; keep the two-pass whole-word→hard-break→ellipsis structure and the pure/deterministic contract):
    - For fontPx from baseFontPx down to minFontPx (integer steps):
        - lineHeight = fontPx × lineHeightFactor.
        - For candidate line count k = 1..maxLines (fewest first):
            - blockH = k × lineHeight + reservedHeightPx; if blockH > diameterPx, skip this k (too tall for the circle).
            - topY = −blockH/2. For each line index i in 0..k−1 compute worstY = max(|topY + i×lineHeight|, |topY + (i+1)×lineHeight|), chord_i = 2×√(max(0, r² − worstY²)), budget_i = max(1, floor(chord_i / (CHAR_WIDTH_FACTOR × fontPx))). This yields a per-line char budget array (outer lines get fewer chars than center lines — the circular narrowing the old code ignored).
            - Greedy-wrap words honoring each line's own budget (fill line i to budget_i, advance to line i+1 on overflow). Pass 1: keep words whole. If the wrap places every word within ≤ k lines, return { fontPx, lines, ellipsized: false }.
        - (Pass 2, per-line budgets identical) allow hard-breaking a single over-long word across lines; return the largest font whose broken wrap fits ≤ maxLines within the height + chord budget.
    - Fallback (floor + still over budget): wrap at the floor using the tightest line budgets, keep ≤ maxLines, ellipsize the last line (unchanged safety-net semantics). For real names at realistic sizes this stays UNREACHABLE.
    Retune to keep the POLISH-01 bar (every real name fits, no ellipsis) satisfied under the stricter circular model: the executor tunes `ORB_LABEL_LINE_HEIGHT_FACTOR` (a tighter line box lets more lines fit vertically), `ORB_LABEL_PERCENT_LINE_PX`, and if needed `CHAR_WIDTH_FACTOR` / floor / max-lines, until BOTH the geometric catalog test (Task 1) and the boundary fixtures (orbLabelFit.test.ts) pass. Record the final tuned values + rationale in the summary.
    Callers:
    - PredictionOrb.tsx: pass `layout.diameterPx − 2 × config.show.ORB_LABEL_FACE_PADDING_PX` as the content diameter and `reservedHeightPx: config.show.ORB_LABEL_PERCENT_LINE_PX`, `lineHeightFactor: config.show.ORB_LABEL_LINE_HEIGHT_FACTOR`.
    - CenterNode.tsx: keep the existing `diameter − CENTER_LABEL_PADDING_PX × 2` content width; pass `reservedHeightPx: 0` (no percent line) and `lineHeightFactor: config.show.ORB_LABEL_LINE_HEIGHT_FACTOR`.
    - orbLabelFit.test.ts: the synthetic boundary fixtures now pass the two new required fields (`reservedHeightPx: 0`, `lineHeightFactor` from config) so they remain pure wrap/shrink/break/ellipsis pins; realign any pin whose expected wrap shifts under the circular model, preserving each test's INTENT (mirrors the plan 08-06 fixture realignment precedent — document each realigned pin in the summary).
  </behavior>
  <action>
    Rewrite fitOrbLabel's two wrap passes to the circular per-line-budget algorithm in the behavior block. Replace the module-private `USABLE_WIDTH_FACTOR = 1` rectangle assumption entirely (delete it or repurpose as a ≤1 safety shrink applied to every chord — the executor's choice, but the primary width source MUST be the per-line chord geometry, not a flat diameter). Keep `charsPerLine` only if it becomes chord-driven; otherwise inline the per-line budget computation. Preserve the pure/deterministic, no-DOM contract and the greedy-wrap / hard-break / ellipsis fallback shape.

    Wire both callers to subtract padding and pass the new required options. Update orbLabelFit.test.ts opts objects to include `reservedHeightPx` + `lineHeightFactor`; realign only the specific boundary pins whose outcome legitimately changes under circular fit, keeping the branch each pin exercises (short pass-through, multi-line wrap, shrink-to-floor, hard-break-whole-word, ellipsis-safety-net, center budget, determinism).

    Iterate config constants until GREEN across the geometric catalog test AND the fixtures. Do NOT weaken the geometric predicate or the catalog test to force a pass — the test is the regression guard; the heuristic and constants move to satisfy it. If a genuinely un-fittable name emerges at the 56px floor, it may only be the documented 44-char outlier (already the sole exemption at d = 56).

    Commit GREEN: `fix(08-08): make orb-label fit circle-aware (per-line chord + height budget); subtract face padding + reserve percent line`.
  </action>
  <verify>
    <automated>cd C:/Users/mattf/git/guezzer && npx vitest run packages/app/test/orbLabelFit.catalog.test.ts packages/app/test/orbLabelFit.test.ts 2>&1 | tail -30 && npx tsc -p packages/app/tsconfig.json --noEmit && echo TSC_OK</automated>
    Expected: both label-fit test files PASS (geometric sweep green — GREEN) and `tsc --noEmit` exits 0 (prints TSC_OK). Then run the full app suite to prove no regression: `npx vitest run` (all previously-green phase-08 tests still pass).
  </automated>
  <done>fitOrbLabel derives per-line width from the circle chord and rejects over-tall wraps; PredictionOrb and CenterNode fit against padding-subtracted diameters with correct reserved percent height; the geometric catalog test and the realigned boundary fixtures both pass; `tsc --noEmit` is clean; the full app suite is green.</done>
</task>

<task type="auto">
  <name>Task 3: Fix OrbFitHarness to measure circular spill (on-device gate)</name>
  <files>packages/app/src/dev/OrbFitHarness.tsx</files>
  <action>
    Remove the rectangular blind spot (08-HUMAN-UAT.md artifact OrbFitHarness.tsx:83 + debug Evidence): today each cell is an `overflow:hidden` `rounded-full` square measured by `scrollWidth/scrollHeight` vs `clientWidth/clientHeight` — a rectangular test that both clips the circular spill (so it's invisible) and never checks the circle chord.

    Change the measurement to a real-glyph CIRCULAR check:
    - Drop `overflow: "hidden"` from each `[data-fit-cell]` so any real spill is visually apparent on the phone AND measurable by getBoundingClientRect (the round cell keeps `rounded-full` for the visual boundary).
    - Replace the offender detection: for each cell, compute its center (cx, cy) and radius r from its bounding rect. Select every rendered text LINE element inside the orb/center content (the name-line spans and, for a PredictionOrb, the percent-line span). For each line element read its getBoundingClientRect; compute the line's vertical offset y = (line vertical-center − cy) using the edge farther from center (max(|top−cy|, |bottom−cy|)) and its half-width = lineRect.width / 2; the line SPILLS when half-width > √(max(0, r² − y²)) (its real width exceeds the circle chord at its y) OR when |farther edge| > r (it pokes out the top/bottom). Any spilling line → push an offender with the name + variant + which line.
    - Keep the two-rAF settle-then-measure timing, the pass/fail banner, and the on-screen offender list. Update the header copy to state it now measures each line against the circular boundary (not rectangular scroll).

    Selecting line elements: prefer robust DOM traversal (e.g. the leaf text spans within the cell) so no production component needs a harness-only marker; if a marker is genuinely necessary, add a minimal `data-fit-line` attribute to the line spans in PredictionOrb.tsx / CenterNode.tsx (executor discretion — but do not otherwise change rendering). This remains a THROWAWAY dev-only route (`#/dev/orb-fit`), removed post-phase.
  </action>
  <verify>
    <automated>cd C:/Users/mattf/git/guezzer && npx tsc -p packages/app/tsconfig.json --noEmit && echo TSC_OK</automated>
    Automated: `tsc --noEmit` clean (prints TSC_OK); the harness compiles and is still gated on `#/dev/orb-fit` only.
    <human-check>On-device (memory: device-uat-hosting): serve over the cloudflared HTTPS tunnel (`--http-host-header localhost`), open `#/dev/orb-fit` on the owner's iPhone, and confirm the banner reads ZERO overflow offenders across all 264 names at the swept orb sizes and the center node — watching the 44-char outlier `(You Gotta) Fight for Your Right (To Party!)` and `Deserted Dunes Welcome Weary Feet`. This is the POLISH-01 re-verification gate; record the result in 08-HUMAN-UAT.md.</human-check>
  </verify>
  <done>OrbFitHarness measures each rendered line's real width against the circle chord at its y (no overflow:hidden clip, no rectangular scroll), lists true circular-spill offenders, and compiles clean; on-device re-verification of POLISH-01 is enabled.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kglw-derived song name → rendered label | Song names originate from the volunteer-run kglw corpus; they are the only external data touched here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08-01 | Injection (Tampering) | song name in PredictionOrb/CenterNode labels + OrbFitHarness | accept | Names render as escaped React text (JSX children), never `dangerouslySetInnerHTML` — unchanged by this plan; no new injection surface. |
| T-08-08-SC | Tampering | package installs | n/a | No package installs; pure TypeScript UI + test edits only. No npm/pip/cargo changes → package legitimacy gate not applicable. |

No new trust boundaries, no external services, no schema/artifact/core changes. The work is a pure UI-layer geometry fix plus a dev-only test/harness.
</threat_model>

<verification>
- `npx vitest run packages/app/test/orbLabelFit.catalog.test.ts` — FAILS at Task 1 RED (geometric/height violation), PASSES after Task 2 GREEN. This is the regression guard for POLISH-01.
- `npx vitest run packages/app/test/orbLabelFit.test.ts` — boundary fixtures pass (with documented realignments) after Task 2.
- `npx vitest run` (full app suite) — no regression; all previously-green phase-08 tests still pass.
- `npx tsc -p packages/app/tsconfig.json --noEmit` — exits 0 after every task.
- Grep confirms the new constants exist and no raw diameter is passed unfitted: `ORB_LABEL_LINE_HEIGHT_FACTOR`, `ORB_LABEL_PERCENT_LINE_PX`, `ORB_LABEL_FACE_PADDING_PX` in config.ts; `labelFitsCircle` exported from orbLabelFit.ts and imported by the catalog test.
- On-device `<human-check>` (Task 3): `#/dev/orb-fit` banner reads zero circular-spill offenders across all 264 names + center node on the owner's iPhone → POLISH-01 re-verified; result recorded in 08-HUMAN-UAT.md.
</verification>

<success_criteria>
- POLISH-01 truth holds: every prediction-orb and center-node song name renders fully inside its circular fill across the real orb diameter range [56..112] — no overflow, no truncation, no oversizing.
- The fit heuristic is geometrically honest: per-line usable width comes from the circle chord at the line's vertical offset, and total text height (lines + reserved percent line) never exceeds the circle.
- PredictionOrb subtracts its face padding and reserves the percent line's vertical room; CenterNode continues to subtract its padding (reserved = 0).
- A geometric catalog test swept over [56..112] guards the fix: it fails against the pre-fix rectangular heuristic and passes after — the coverage gap that let this ship is closed.
- The OrbFitHarness measures circular spill (real glyph width vs chord), not rectangular scroll on a clipped square.
- Core/UI separation and the single-config-file rule are preserved: all edits are in packages/app; every new geometric value is a named config constant.
</success_criteria>

<output>
Create `.planning/phases/08-on-device-ui-polish-accessibility/08-08-SUMMARY.md` when done. Record: the final tuned values for ORB_LABEL_LINE_HEIGHT_FACTOR / ORB_LABEL_PERCENT_LINE_PX / CHAR_WIDTH_FACTOR (and any floor/max-lines change) with rationale; each realigned orbLabelFit.test.ts boundary pin and why; the RED and GREEN commit SHAs; and the on-device POLISH-01 re-verification result.
</output>
