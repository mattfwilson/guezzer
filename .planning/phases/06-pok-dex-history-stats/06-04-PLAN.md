---
phase: 06-pok-dex-history-stats
plan: 04
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - packages/app/scripts/fetch-covers.ts
  - packages/app/src/assets/covers/covers-manifest.json
  - packages/app/src/assets/covers/*.webp
  - packages/app/package.json
  - packages/app/test/coversManifest.test.ts
autonomous: true
requirements: [DEX-04]
must_haves:
  truths:
    - "Album cover thumbnails are fetched once at build time by a manual script and committed as bundled WebP assets — no runtime cover fetching exists anywhere (D-03)"
    - "Every committed cover is 160×160 WebP at or under 25 KB, with total committed cover weight ≤ ~300 KB (UI-SPEC budget)"
    - "A committed covers-manifest.json records the provenance (source URL) of every cover; card albums with no cover are absent from the manifest and fall back to the initials placeholder"
    - "The fetch script paces requests ≥1/s with a descriptive User-Agent (MusicBrainz etiquette) and is never wired into CI"
  artifacts:
    - path: "packages/app/scripts/fetch-covers.ts"
      provides: "MusicBrainz→CAA→sharp one-time cover pipeline"
      contains: "coverartarchive.org"
    - path: "packages/app/src/assets/covers/covers-manifest.json"
      provides: "Cover provenance manifest (slug → source URL)"
      contains: "{"
  key_links:
    - from: "packages/app/scripts/fetch-covers.ts"
      to: "data/normalized/dex-albums.json"
      via: "card-album list drives which covers are fetched"
      pattern: "dex-albums"
    - from: "packages/app/test/coversManifest.test.ts"
      to: "packages/app/src/assets/covers/"
      via: "size-budget and manifest-coverage assertions over committed assets"
      pattern: "25 \\* 1024|25600"
---

<objective>
Build and run the one-time album-cover pipeline (D-03, Claude's-discretion source): MusicBrainz release-group search → Cover Art Archive `front-250` thumbnails → sharp resize to 160×160 WebP, committed under `packages/app/src/assets/covers/` with a provenance manifest — the "album shelf's face" for the D-01 card grid.

Slice contribution: *As a collector, I want the dex to read as a discography shelf of real album covers, so that browsing my collection feels like browsing records.* Assets land here; plan 06-05 renders them.

Source decision (documented deviation from UI-SPEC §Assets "kglw.net first"): RESEARCH verified albums.json has NO image-URL field, so kglw.net covers would require HTML scraping — forbidden by project etiquette. CAA is a real JSON/binary API with ready-made 250px thumbnails (RESEARCH §Don't Hand-Roll, cited musicbrainz.org docs). D-03 delegates the source to Claude; CAA is the pick, kglw.net is not scraped.
Output: fetch script, committed WebP thumbnails + manifest, sharp devDependency, budget-guard test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-pok-dex-history-stats/06-CONTEXT.md
@.planning/phases/06-pok-dex-history-stats/06-RESEARCH.md
@.planning/phases/06-pok-dex-history-stats/06-PATTERNS.md
@.planning/phases/06-pok-dex-history-stats/06-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: sharp devDependency + fetch-covers script</name>
  <files>packages/app/scripts/fetch-covers.ts, packages/app/package.json</files>
  <read_first>
    - packages/core/src/cli/fetch-corpus.ts (the polite-fetch idiom to copy: fetchJson with User-Agent from config.userAgent + AbortSignal.timeout, strictly-sequential paceNextRequest lines ~138-150, fail-loud-with-endpoint-named, fetch-meta.json provenance idiom lines ~99-119)
    - packages/core/src/cli/build-model.ts (isMain guard + parseArgs + summary printout shape)
    - packages/core/src/config.ts (userAgent — includes owner contact per MusicBrainz rules; fetchDelayMs 2000; fetchTimeoutMs)
    - data/normalized/dex-albums.json (the committed 06-01 artifact — card album titles/urls drive the fetch list)
    - 06-RESEARCH.md §Code Examples "Cover pipeline" (MB WS/2 search + CAA front-250 endpoints) and Package Legitimacy Audit (sharp 0.35.3 [OK], devDependency only)
  </read_first>
  <action>
    Install the audited package exactly: `npm install --save-dev --workspace packages/app sharp@0.35.3` (RESEARCH slopcheck verdict [OK]; note the earlier slopcheck passthrough crash made zero repo changes, so this is the real install). Create `packages/app/scripts/fetch-covers.ts` (Node-run via `node`, erasable-syntax TS like the core CLIs): (1) read `data/normalized/dex-albums.json` and take the card albums (buckets get no covers — they use the initials placeholder by design); (2) for each album, query MusicBrainz WS/2 release-group search `https://musicbrainz.org/ws/2/release-group/?query=releasegroup:"{title}" AND artist:"King Gizzard"&fmt=json` with headers `User-Agent: config.userAgent` (import from @guezzer/core — Node context, the header is real here) and `AbortSignal.timeout`, strictly sequentially with ≥ `config.fetchDelayMs` (2000ms) between ALL requests (MB wants ~1 req/s; 2s is comfortably polite — reuse the paceNextRequest closure idiom); (3) take the top-scored release-group mbid and GET `https://coverartarchive.org/release-group/{mbid}/front-250` (follows redirects, no auth); (4) pipe the bytes through `sharp(buf).resize(160, 160).webp({ quality: 70 })` and write `packages/app/src/assets/covers/{album-slug}.webp` where album-slug = the album_url basename (e.g. `/albums/nonagon-infinity` → `nonagon-infinity`); (5) FAIL LOUDLY (process.exit(1), file named) if any output exceeds 25 KB — the accidental-full-res guard; (6) write `packages/app/src/assets/covers/covers-manifest.json` mapping slug → { title, sourceUrl (the CAA URL), mbid, fetchedAt } for provenance (fetch-meta idiom); (7) albums with no MB match or no CAA art are SKIPPED with a console warning and omitted from the manifest (A2 fallback — the UI initials placeholder covers them); (8) idempotent: skip albums whose .webp already exists unless a `--force` flag is passed. Add app package.json script `"fetch:covers": "node scripts/fetch-covers.ts"`. NEVER add it to CI, refresh, or any build hook (manual re-run only — CLAUDE.md etiquette).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "fetch:covers" packages/app/package.json</automated>
  </verify>
  <acceptance_criteria>
    - `packages/app/package.json` devDependencies contains `"sharp": "0.35.3"` (exact pin) and the `fetch:covers` script.
    - fetch-covers.ts uses `config.userAgent`, sequential pacing with `config.fetchDelayMs`, and contains the 25 KB fail-loud guard (source assertions via grep: `userAgent`, `fetchDelayMs`, `25`).
    - The script is referenced by NO CI/build/refresh config (grep `fetch-covers` outside packages/app/scripts returns only the package.json script line).
    - `npx tsc --noEmit` green.
  </acceptance_criteria>
  <done>A polite, budget-guarded, provenance-writing cover pipeline exists as a manual one-command script.</done>
</task>

<task type="auto">
  <name>Task 2: Run the pipeline, commit assets, budget-guard test</name>
  <files>packages/app/src/assets/covers/covers-manifest.json, packages/app/src/assets/covers/*.webp, packages/app/test/coversManifest.test.ts</files>
  <read_first>
    - packages/app/scripts/fetch-covers.ts (Task 1 output — run what was built)
    - packages/app/test/db.test.ts (app-project test conventions; fs access is available under jsdom via node imports)
    - data/normalized/dex-albums.json (card list the coverage assertions check against)
  </read_first>
  <action>
    Run `npm run fetch:covers --workspace packages/app` (network: musicbrainz.org + coverartarchive.org — available per RESEARCH environment table). Review the console output: expect most of the ~25-30 card albums to resolve (KGLW is heavily documented on MusicBrainz, A2); missing covers are acceptable and fall back to placeholders. Commit the resulting `.webp` files + `covers-manifest.json`. Create `packages/app/test/coversManifest.test.ts` using `node:fs`/`node:path` to assert over the COMMITTED assets: (1) every manifest entry has a corresponding `.webp` file and vice versa; (2) every `.webp` is ≤ 25 * 1024 bytes; (3) the summed size of all covers is ≤ 350 KB (headroom over the ~300 KB target); (4) every manifest slug corresponds to a card album in `data/normalized/dex-albums.json` (no stray assets); (5) each manifest entry carries a non-empty `sourceUrl` (provenance). If the network fetch fails entirely (offline execution), commit an EMPTY manifest (`{}`) plus the test (assertions 1-5 pass vacuously), log the miss in the SUMMARY, and note that `npm run fetch:covers` re-runs it — the UI ships placeholder-complete either way.
  </action>
  <verify>
    <automated>npx vitest run --project @guezzer/app test/coversManifest.test.ts</automated>
    <human-check>End-of-phase device gate: covers render crisply at 80px on the device grid; shelf reads as a discography.</human-check>
  </verify>
  <acceptance_criteria>
    - `git status` shows committed covers-manifest.json (and .webp files when network succeeded).
    - coversManifest.test.ts passes all five assertions against the committed assets.
    - No individual .webp exceeds 25 KB; total ≤ 350 KB (asserted by the test).
    - Full app suite green.
  </acceptance_criteria>
  <done>The album shelf's face is committed, budget-verified, and provenance-tracked; placeholders cover any gaps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MusicBrainz/CAA responses → committed assets | External image bytes and JSON enter the repo at build time |
| npm install → dev toolchain | sharp is a native-binary devDependency |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | Tampering (asset poisoning) | fetched cover bytes | mitigate | Bytes are re-encoded through sharp (resize+webp) — never committed verbatim; 25 KB hard cap; provenance manifest makes every asset inspectable |
| T-06-10 | Denial of service (API abuse) | MB/CAA endpoints | mitigate | Strictly sequential ≥2s pacing, descriptive UA with contact, manual-run-only (never CI), idempotent skip of existing files |
| T-06-SC | Tampering (supply chain) | sharp@0.35.3 install | mitigate | RESEARCH Package Legitimacy Audit: registry-verified, ~12yr history, github.com/lovell/sharp, slopcheck [OK]; exact-version pin; devDependency only — never shipped in the bundle |
</threat_model>

<verification>
- `npx vitest run --project @guezzer/app` green.
- `du`-style check via the test: per-file ≤25 KB, total ≤350 KB.
- grep confirms no runtime code fetches covers (no coverartarchive/musicbrainz reference outside scripts/).
</verification>

<success_criteria>
- D-03: covers fetched once at build time, bundled, offline-first; source = MB/CAA (documented discretion call).
- DEX-04: the shelf face exists as committed, fingerprintable Vite assets.
- Etiquette: paced, UA'd, manual-only; budget-guarded against full-res commits.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 04)

New symbols (exclude from source-drift verification — created here):
- `packages/app/scripts/fetch-covers.ts` (script; `--force` CLI flag)
- `packages/app/src/assets/covers/{slug}.webp` committed assets + `covers-manifest.json`
- npm script `fetch:covers`; devDependency `sharp@0.35.3` — `packages/app/package.json`
- New test: `packages/app/test/coversManifest.test.ts`
</artifacts_produced>

<output>
Create `.planning/phases/06-pok-dex-history-stats/06-04-SUMMARY.md` when done
</output>
