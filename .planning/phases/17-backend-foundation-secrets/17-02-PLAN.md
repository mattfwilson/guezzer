---
phase: 17-backend-foundation-secrets
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - .env.example
  - packages/core/test/purity.test.ts
autonomous: true
requirements: [SETUP-03, SETUP-04]
user_setup: []

must_haves:
  truths:
    - "A committed .env.example documents every required var; real .env files are git-ignored (SETUP-03 / D-11)"
    - "The VITE_ prefix is the mechanical secret boundary: public anon key + URL are VITE_; service_role + passwords are not (D-09/D-10)"
    - "An automated test fails if packages/core ever imports Supabase or a browser DOM/transport global (SETUP-04 / D-12)"
  artifacts:
    - path: ".gitignore"
      provides: "Ignores .env / .env.* except .env.example (closes the current gap)"
      contains: "!.env.example"
    - path: ".env.example"
      provides: "Valueless template of every env var name, VITE_ vs non-VITE separated"
      contains: "VITE_SUPABASE_URL"
    - path: "packages/core/test/purity.test.ts"
      provides: "Static-scan core-purity guard (with the fetch + bare-window exceptions)"
      min_lines: 20
      contains: "@supabase"
  key_links:
    - from: "packages/core/test/purity.test.ts"
      to: "packages/core/src/**/*.ts"
      via: "fs read + regex scan"
      pattern: "createClient|@supabase"
---

<objective>
Close the secrets-hygiene gap and harden core purity — both fully autonomous, no live project required (SETUP-03, SETUP-04). Add `.env` patterns to `.gitignore` (D-11), commit a valueless `.env.example` documenting every var behind the `VITE_` boundary (D-09/D-10), and add an automated Vitest guard that fails if `packages/core` ever imports Supabase or a browser DOM/transport global (D-12).

Purpose: These are the phase's two guardrails — they must exist before real secrets are created (17-04) and must catch a future regression automatically.
Output: A corrected `.gitignore`, a committed `.env.example`, and `packages/core/test/purity.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-backend-foundation-secrets/17-RESEARCH.md
@.planning/phases/17-backend-foundation-secrets/17-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Close the .gitignore gap + commit the valueless .env.example (SETUP-03 / D-11)</name>
  <files>.gitignore, .env.example</files>
  <read_first>
    - .gitignore (current full file — 4 lines: node_modules/, dist/, .claude/, *.tsbuildinfo; NO .env handling — the gap)
    - .planning/phases/17-backend-foundation-secrets/17-RESEARCH.md §"Code Examples" (.gitignore addition + .env.example template)
    - .planning/phases/17-backend-foundation-secrets/17-PATTERNS.md §"Shared Patterns → Secret boundary / .gitignore / .env.example"
  </read_first>
  <action>
    Append to `.gitignore` exactly three lines below the existing four: `.env`, `.env.*`, then `!.env.example` — the negation MUST be last so the committed template survives. No leading slash, so the pattern also covers `packages/app/.env.local` at any depth. Create a valueless `.env.example` at repo root documenting EVERY var name grouped by the boundary: under a "Client (public — VITE_-prefixed, safe in bundle — D-09)" heading list `VITE_SUPABASE_URL=` and `VITE_SUPABASE_ANON_KEY=`; under an "Ops / server secrets (NEVER VITE_, NEVER committed — D-10)" heading list `SUPABASE_SERVICE_ROLE_KEY=`, `SUPABASE_DB_PASSWORD=` (db push), `SUPABASE_ACCESS_TOKEN=` (PAT for link), `SUPABASE_PROJECT_REF=` (for `supabase link --project-ref`), and `SUPABASE_URL=` (the non-VITE copy the Node seed reads); under a "Per-friend account secrets, keyed by roster slug (D-08)" heading list example pairs `SEED_EMAIL_MATT=` / `SEED_PASSWORD_MATT=` and `SEED_EMAIL_PLACEHOLDER1=` / `SEED_PASSWORD_PLACEHOLDER1=`. All values EMPTY. No `VITE_`-prefixed name may contain "service", "password", or "secret".
  </action>
  <verify>
    <automated>cd "C:/Users/mattf/git/guezzer" && grep -qx '.env' .gitignore && grep -qx '.env.\*' .gitignore && grep -qx '!.env.example' .gitignore && test -f .env.example && grep -q '^VITE_SUPABASE_URL=$' .env.example && grep -q '^SUPABASE_SERVICE_ROLE_KEY=$' .env.example && ! grep -iE '^VITE_[A-Z_]*(SERVICE|PASSWORD|SECRET)' .env.example && printf 'x' > .env.__probe && git check-ignore .env.__probe >/dev/null && rm -f .env.__probe && git check-ignore .env >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `.gitignore` ends with `.env`, `.env.*`, `!.env.example` (negation last); `git check-ignore .env` exits 0.
    - `.env.example` exists, is committed-safe (all values empty), and lists the full var set: the two `VITE_*`, `SUPABASE_SERVICE_ROLE_KEY`, `SUPABASE_DB_PASSWORD`, `SUPABASE_ACCESS_TOKEN`, `SUPABASE_PROJECT_REF`, `SUPABASE_URL`, and example `SEED_EMAIL_*` / `SEED_PASSWORD_*` pairs.
    - No `VITE_`-prefixed name in `.env.example` contains service/password/secret (the mechanical D-10 boundary holds).
  </acceptance_criteria>
  <done>Real `.env` files can never be committed; every var name is documented behind the VITE_ boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Core-purity static-scan test with the fetch + bare-window exceptions (SETUP-04 / D-12)</name>
  <files>packages/core/test/purity.test.ts</files>
  <read_first>
    - packages/core/test/config.test.ts (Vitest describe/it idiom; fs-free but same import style)
    - vitest.config.ts (the `@guezzer/core` project runs `environment: node`, `include: ["test/**/*.test.ts"]` — a file at packages/core/test/purity.test.ts auto-collects, zero config change)
    - packages/core/src/model/predict.ts (line ~257: `window.filter(...)` — a LOCAL variable named `window`, NOT the DOM global; a bare `window.` ban would falsely red this file)
    - packages/core/src/live/poll-latest.ts + src/cli/fetch-corpus.ts + src/map/relay-client.ts + src/dex/recent-shows.ts (all legitimately use global `fetch` in Node — `fetch` MUST NOT be banned)
    - packages/core/package.json (deps: only fuse.js + zod)
    - .planning/phases/17-backend-foundation-secrets/17-RESEARCH.md §"Pattern 4: Core-purity static scan" + §"Pitfall 2"
  </read_first>
  <action>
    Create `packages/core/test/purity.test.ts` — a dependency-free Vitest test (node env) that reads every `packages/core/src/**/*.ts` file (via `fs.promises.glob` on Node 24, or a manual recursive walk fallback) and asserts none matches the FORBIDDEN set. FORBIDDEN = Supabase specifiers `/@supabase\//`, `/supabase-js/`, `/\bcreateClient\b/`; and unambiguous browser DOM/transport globals `/\bdocument\./`, `/\blocalStorage\b/`, `/\bnavigator\./`, `/\bXMLHttpRequest\b/`, `/\bWebSocket\b/`, `/\bEventSource\b/`. TWO load-bearing exceptions the test MUST encode with an explanatory comment: (1) DO NOT ban `fetch` — core uses global `fetch` in four Node CLIs (poll-latest.ts, fetch-corpus.ts, relay-client.ts, recent-shows.ts); banning it reds the existing suite. (2) DO NOT add a bare `/\bwindow\./` rule — `predict.ts` has a local array variable named `window` calling `window.filter(...)`; a bare window ban false-positives it. The real DOM-window risk is already covered by the Supabase specifiers + the other DOM globals; if window-DOM detection is wanted, use only specific forms like `/window\.location/`, `/window\.document/`, `/window\.addEventListener/` that cannot match `.filter`. Also assert `packages/core/package.json` has no `@supabase/*` dependency (structural hardening — read + JSON.parse the deps). Add a comment block citing D-12 and both exceptions.
  </action>
  <verify>
    <automated>cd "C:/Users/mattf/git/guezzer" && npx vitest run packages/core/test/purity.test.ts && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run packages/core/test/purity.test.ts` passes.
    - The FULL suite (`npm test`) stays green — specifically the `@guezzer/core` project does NOT red on `predict.ts` (bare-window exception) or on the four `fetch`-using CLIs (fetch exception).
    - The test source contains no `/\bfetch\b/` ban and no bare `/\bwindow\./` ban, and DOES include `@supabase`, `createClient`, and `supabase-js` patterns.
    - Manually verified: adding a line `import { createClient } from "@supabase/supabase-js"` to any `packages/core/src` file makes the test FAIL; then revert.
    - The test asserts `packages/core/package.json` has no `@supabase/*` dependency.
  </acceptance_criteria>
  <done>Core purity is guarded automatically and the guard is proven to catch a Supabase import without false-positiving existing core code.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 17-02)
- `.gitignore` — adds `.env`, `.env.*`, `!.env.example` (closes the secret-commit gap).
- `.env.example` — valueless template of every var name (VITE_ public vars vs non-VITE ops secrets).
- `packages/core/test/purity.test.ts` — SETUP-04 static-scan guard; test name "core purity (SETUP-04)"; encodes the `fetch` + bare-`window` exceptions.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git repo → secrets | Committed files must never carry real secrets |
| Vite bundle → env | Only `VITE_`-prefixed vars ship to the client |
| packages/core → app | Core must never reach across into the Supabase client / DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03 | Information Disclosure | `.env` committed to git | mitigate | `.gitignore` adds `.env`/`.env.*` with `!.env.example` last (D-11); `git check-ignore .env` verified |
| T-17-04 | Information Disclosure | secret placed behind a `VITE_` prefix | mitigate | `.env.example` separates VITE_ public vars from non-VITE ops secrets; grep asserts no VITE_ name contains service/password/secret (D-10) |
| T-17-05 | Elevation of Privilege / Information Disclosure | `packages/core` importing the Supabase client or DOM/network | mitigate | purity static-scan test fails on `@supabase`/`createClient`/DOM globals (D-12); `fetch` + bare-`window` exceptions prevent false negatives that would force disabling the guard |
| T-17-SC | Tampering | npm installs | accept | No package installs in this plan (all installs gated in 17-01); the purity test additionally asserts core has no `@supabase/*` dep |
</threat_model>

<verification>
- `git check-ignore .env` returns 0; `.env.example` present and valueless.
- `npx vitest run packages/core/test/purity.test.ts` passes; `npm test` (full suite) green.
</verification>

<success_criteria>
- No secret can be committed; every var is documented; core purity is guarded and proven to catch a Supabase import without false positives.
</success_criteria>

<output>
Create `.planning/phases/17-backend-foundation-secrets/17-02-SUMMARY.md` when done.
</output>
