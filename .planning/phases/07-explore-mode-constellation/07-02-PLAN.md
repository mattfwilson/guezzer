---
phase: 07-explore-mode-constellation
plan: 02
type: execute
wave: 1
depends_on: [07-01]
files_modified:
  - packages/app/package.json
  - packages/app/src/explore/ConstellationCanvas.tsx
  - packages/app/src/explore/ExploreView.tsx
  - packages/app/src/App.tsx
autonomous: true
requirements: [EXPL-01, EXPL-06]
must_haves:
  truths:
    - "Opening the Explore tab renders a force-directed constellation from the same matrix artifact the predictor uses — nodes sized by play count, colored by tuning family, directed edges thickened by transition count (EXPL-01)"
    - "The simulation settles and freezes (fx/fy fixed on onEngineStop); pan/zoom never reheats and labels never jitter permanently (EXPL-06)"
    - "Every node is tappable at ≥22px screen-space radius regardless of visual size (44px equivalence)"
    - "At rest only the top-K biggest nodes carry labels; labels fade in past the zoom threshold"
    - "A matrix load failure shows a calm error state, never a crash; the canvas never overflows under the tab bar"
  artifacts:
    - path: "packages/app/src/explore/ConstellationCanvas.tsx"
      provides: "single ForceGraph2D component with nodeCanvasObject fill/label/edge draw + settle-freeze"
      min_lines: 60
    - path: "packages/app/src/explore/ExploreView.tsx"
      provides: "#/explore route root: guarded matrix load + derive + render"
      min_lines: 30
  key_links:
    - from: "packages/app/src/explore/ExploreView.tsx"
      to: "deriveConstellation + loadMatrix"
      via: "guarded load → derive → pass graphData"
      pattern: "deriveConstellation|loadMatrix"
    - from: "packages/app/src/App.tsx"
      to: "ExploreView"
      via: "route branch + scroll=false seam"
      pattern: "ExploreView"
---

<objective>
Slice 1 — the constellation renders. Install `react-force-graph-2d@1.29.1` (app package only), build the single `ConstellationCanvas` component wrapping `<ForceGraph2D>` fed by `deriveConstellation`, and mount `ExploreView` at `#/explore` replacing `PlaceholderView`. After this plan a user opens the Explore tab and sees the band's transition graph as a settling-then-frozen night sky they can pan and zoom.

Purpose: Get something real on screen early (MVP vertical-slice directive). This is the app's FIRST canvas / force-simulation view; it establishes the render surface every later slice (bars, filters, dex overlay) layers onto. Full catalog is the initial population; rotation-as-default arrives in Slice 3.
Output: A pannable, zoomable, settle-and-freeze constellation at `#/explore`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-explore-mode-constellation/07-RESEARCH.md
@.planning/phases/07-explore-mode-constellation/07-PATTERNS.md
@.planning/phases/07-explore-mode-constellation/07-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install react-force-graph-2d + ConstellationCanvas render stage</name>
  <files>packages/app/package.json, packages/app/src/explore/ConstellationCanvas.tsx</files>
  <read_first>
    - packages/app/src/show/OrbitStage.tsx lines 41-54 (ResizeObserver container-sizing pattern to copy verbatim), 64-65 (gesture-suppression classes `touch-none select-none overflow-hidden` + `overscrollBehavior: none`)
    - packages/app/src/show/tuningColor.ts lines 31-34 (`tuningColor(tuningFamily)` — reuse VERBATIM, keyed on the core union, null→#A1A1AA)
    - 07-RESEARCH.md §"Architecture Patterns" Patterns 1-4 (verbatim ForceGraph2D prop API: nodeCanvasObject(node,ctx,globalScale), nodeCanvasObjectMode 'replace', nodePointerAreaPaint, linkDirectionalArrowLength, cooldownTicks, onEngineStop, d3AlphaDecay/VelocityDecay), §"Pitfall 1" (mutation), §"Pitfall 2" (must measure container), §"Pitfall 3" (camera via ref)
    - 07-UI-SPEC.md §B3 (edge color/width/arrow), §"Layout" region 2 (node radius sqrt(playCount) NODE_RADIUS_MIN 4..MAX 14), §"Typography" canvas label rules (12px/globalScale, ellipsis at LABEL_MAX_CHARS)
    - packages/core/src/config.ts + packages/app/src/config.ts (the `explore` blocks from 07-01)
  </read_first>
  <action>
    Add `react-force-graph-2d@1.29.1` to `packages/app` only via `pnpm --filter @guezzer/app add react-force-graph-2d@1.29.1` — the `-2d` package ONLY (the umbrella `react-force-graph` is banned per CLAUDE.md; do not add a separate `d3-force` dep). Do NOT add it to core (core stays React/DOM-free).
    Create `packages/app/src/explore/ConstellationCanvas.tsx`: copy OrbitStage's `stageRef` + `useState({width,height})` + `ResizeObserver` sizing and pass measured `width`/`height` props to `<ForceGraph2D>` (Pitfall 2 — it defaults to window dims and overflows `<main>`). Keep the gesture-suppression container classes. Props: the derived `{nodes, links}` graphData + config. Wire a single `nodeCanvasObject={(node, ctx, globalScale) => ...}` with `nodeCanvasObjectMode={() => 'replace'}` drawing: radius = NODE_RADIUS_MIN + sqrt(playCount) scaled, clamped to NODE_RADIUS_MAX; fill = `tuningColor(node.tuningFamily)`; zoom-gated label (draw if `globalScale >= LABEL_ZOOM_THRESHOLD` OR node is in the top-`LABEL_AT_REST_TOP_K` by playCount), font `fontSizePx/globalScale` world units, ellipsize at LABEL_MAX_CHARS, `ctx.fillText` only (never innerHTML). Add `nodePointerAreaPaint` with a `Math.max(radiusFor(playCount), NODE_HIT_MIN_RADIUS_PX / globalScale)` hit floor. Edges: `linkColor` neutral `#A1A1AA` @20%, `linkWidth` = clamp(0.5 + sqrt(count)*0.5, ,4), `linkDirectionalArrowLength ≈ 3.5`, `linkDirectionalArrowRelPos 0.9`. Settle-freeze: `cooldownTicks={COOLDOWN_TICKS}` `d3AlphaDecay={ALPHA_DECAY}` `d3VelocityDecay={VELOCITY_DECAY}` and `onEngineStop={() => graphData.nodes.forEach(n => { n.fx = n.x; n.fy = n.y; })}`. Hold a `fgRef` (useRef) for later camera control. Add `role="img"` + `aria-label` "Song transition constellation — {n} songs shown" on the container.
    Compute the top-K-by-playCount set ONCE (memoized off graphData), not per draw call.
  </action>
  <verify>
    <automated>pnpm --filter @guezzer/app exec tsc --noEmit 2>&1 | tail -8; pnpm --filter @guezzer/app exec vite build 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `packages/app/package.json` lists `react-force-graph-2d` at `1.29.1`; the umbrella `react-force-graph` is absent; no new `d3-force` dep; core `package.json` unchanged
    - ConstellationCanvas measures its container via ResizeObserver and passes width/height to ForceGraph2D (no `window.innerWidth` default)
    - `nodeCanvasObjectMode` returns `'replace'`; node fill uses `tuningColor`; labels are zoom-gated + top-K at rest; `ctx.fillText` used (no dangerouslySetInnerHTML)
    - `onEngineStop` sets `fx=x, fy=y` on every node; `nodePointerAreaPaint` enforces the 22px screen-space floor
    - app typecheck + vite build succeed
  </acceptance_criteria>
  <done>ConstellationCanvas renders nodes/edges with settle-and-freeze and zoom-gated labels; library installed to app only.</done>
</task>

<task type="auto">
  <name>Task 2: ExploreView route root + App.tsx wiring</name>
  <files>packages/app/src/explore/ExploreView.tsx, packages/app/src/App.tsx</files>
  <read_first>
    - packages/app/src/App.tsx lines 44-57 (current route branch + `scroll={route !== "show"}` seam to widen)
    - packages/app/src/show/matrix.ts lines 35-49 (`loadMatrix()` guarded sentinel `{ok:true,matrix}|{ok:false,error}` — reuse VERBATIM, no new loader)
    - packages/app/src/dex/useDexStats.ts lines 62-75 (guarded-load + error-sentinel branch idiom; "split, don't ||" comment discipline)
    - packages/app/src/components/PlaceholderView.tsx (what #/explore currently renders — being replaced)
    - packages/app/src/components/AppShell.tsx lines 14-19 (scroll=false → non-scrolling full-height flex column)
    - 07-UI-SPEC.md §"Copywriting Contract" (error + rotation-empty states), §"Layout" region 1 (scroll=false mount)
    - packages/app/src/routing/useHashRoute.ts line 9 (ROUTES already includes "explore" — no routing change needed)
  </read_first>
  <action>
    Create `packages/app/src/explore/ExploreView.tsx`: call `loadMatrix()`, branch on `.ok` (return the calm error state using `config.copy.explore` error heading/body with a `Telescope`/`Orbit` lucide glyph — mirrors the Phase-4 model-load-failure pattern, blocks only this view). On success, `useMemo(() => deriveConstellation(result.matrix), [result.matrix])` and render `<ConstellationCanvas graphData={...} />`. Own local `useState` for focus/filter/overlay now, even if only the render is wired this slice (fields consumed by later slices). The initial node population is the full catalog (rotation default lands in Slice 3). No Dexie writes.
    Modify `packages/app/src/App.tsx`: add `route === "explore" ? <ExploreView />` to the branch (replacing the `PlaceholderView` fall-through for explore) and widen the scroll seam to `scroll={route !== "show" && route !== "explore"}` so the canvas owns all gestures. Do not touch `ROUTES` or the BottomTabBar (Compass tab already exists).
  </action>
  <verify>
    <automated>pnpm --filter @guezzer/app exec tsc --noEmit 2>&1 | tail -8; pnpm -w test --run 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `#/explore` renders `<ExploreView>` (not `PlaceholderView`); `scroll={false}` applies for explore
    - `loadMatrix()` reused verbatim; `.ok===false` → calm error state via `config.copy.explore`, never a throw
    - graphData derived via `deriveConstellation` inside a `useMemo` keyed on the matrix
    - full test suite + app typecheck green
  </acceptance_criteria>
  <done>Opening the Explore tab shows the settling constellation; matrix-load failure shows the calm error state.</done>
</task>

</tasks>

<artifacts_produced>
- `ConstellationCanvas` component — `packages/app/src/explore/ConstellationCanvas.tsx`
- `ExploreView` component — `packages/app/src/explore/ExploreView.tsx`
- new app dependency `react-force-graph-2d@1.29.1`
- `radiusFor`/top-K-label helpers (local to ConstellationCanvas)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bundled artifact → app loader | `transition-matrix.json` loaded via the schemaVersion-guarded `loadMatrix()` sentinel |
| kglw-derived song name → canvas | untrusted-origin song-name strings drawn to canvas `ctx.fillText` |
| npm registry → app dependency | new `react-force-graph-2d` install |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02 | Tampering | ConstellationCanvas label draw | mitigate | Song names rendered via canvas `ctx.fillText` only — inherently non-executing; never `dangerouslySetInnerHTML` (inherited T-04-14, ASVS V5) |
| T-07-03 | Tampering/DoS | ExploreView matrix load | mitigate | `loadMatrix()` schemaVersion===1 guard → calm error state, never an unguarded crash |
| T-07-SC | Tampering | `react-force-graph-2d` install | accept | RESEARCH §Package Legitimacy Audit rates it `[OK]` (slopcheck), `Approved`, ~7-yr established lib by vasturiano, no postinstall, `-2d` tree contains no three.js. CLAUDE.md-vetted lock. Install is autonomous — legitimacy gate satisfied. |

No new secrets, no new network, no new storage. No high-severity threat.
</threat_model>

<verification>
- `pnpm -w test --run` green; `pnpm --filter @guezzer/app exec tsc --noEmit` green; `vite build` succeeds (bundle includes react-force-graph-2d)
- Manual smoke (executor, dev server): `#/explore` shows a graph that settles and stops moving; pan/zoom works and does not restart the sim; canvas does not overflow the tab bar
- On-device readability/perf is deferred to the Plan 07-03 checkpoint
</verification>

<success_criteria>
- Constellation renders from the matrix artifact at `#/explore`, nodes tuning-colored + playCount-sized, edges directed/weighted
- Physics settles and freezes; pan/zoom never reheats
- 22px tap floor + zoom-gated top-K labels in place
- Matrix-load failure → calm error state
</success_criteria>

<output>
Create `.planning/phases/07-explore-mode-constellation/07-02-SUMMARY.md` when done.
</output>
