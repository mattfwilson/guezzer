---
phase: 06-pok-dex-history-stats
plan: 08
type: execute
wave: 6
depends_on: ["06-03", "06-07"]
files_modified:
  - packages/app/src/dex/ShowsList.tsx
  - packages/app/src/dex/SetlistView.tsx
  - packages/app/src/dex/RecapView.tsx
  - packages/app/src/dex/DexView.tsx
  - packages/app/src/show/ShowView.tsx
  - packages/app/src/show/EndShowDialog.tsx
  - packages/app/src/config.ts
  - packages/app/test/showsList.test.tsx
  - packages/app/test/recapView.test.tsx
autonomous: true
requirements: [SHOW-14, STAT-02, HIST-01]
must_haves:
  truths:
    - "Ending a show auto-opens the recap — the payoff moment: hero tally, Your calls vs Editor assists, rarity block, +N new catches, and the final setlist with set structure and hit/miss rings (SHOW-14, D-13/D-14)"
    - "The Dex Shows segment lists every attended show newest-first — tracked and retro-marked unified and deduped; tap opens the recap (tracked) or the full setlist with set structure (retro-marked) (D-16, HIST-01)"
    - "The recap shows a show rarity score derived from the night's average corpus gap, plus the rarest catch of the night with its tier (STAT-02, D-15)"
    - "A recap remains reachable forever from show history — ending a show never orphans its scorecard (D-13)"
  artifacts:
    - path: "packages/app/src/dex/RecapView.tsx"
      provides: "Post-show scorecard rendering core RecapStats"
      contains: "RecapStats"
    - path: "packages/app/src/dex/ShowsList.tsx"
      provides: "Attended shows newest-first with recap/setlist routing"
      contains: "ShowsList"
    - path: "packages/app/src/dex/SetlistView.tsx"
      provides: "Retro-marked show full setlist with set structure (HIST-01)"
      contains: "sets"
  key_links:
    - from: "packages/app/src/show/EndShowDialog.tsx"
      to: "packages/app/src/show/ShowView.tsx recap state"
      via: "onEnded(sessionId) callback after endShow + auto-backup"
      pattern: "onEnded"
    - from: "packages/app/src/show/ShowView.tsx"
      to: "packages/app/src/dex/RecapView.tsx"
      via: "recapSessionId state rendered BEFORE the !session.active early return (RESEARCH Pattern 6)"
      pattern: "recapSessionId"
    - from: "packages/app/src/dex/RecapView.tsx"
      to: "@guezzer/core deriveRecap"
      via: "pure core stats in, render out"
      pattern: "deriveRecap"
---

<objective>
The night's scorecard and the collection's memory: RecapView auto-shown at End Show (SHOW-14, STAT-02, D-13/D-14/D-15 — consuming the Phase-5 `source` tags built for exactly this), the Shows segment listing all attended shows newest-first (D-16), and complete set-structured setlist views for retro-marked shows (HIST-01).

User story slice: *As a show-goer walking out of the venue, I want an instant scorecard — my calls vs the editors, what was rare, what was new — and I want every past show browsable forever.*

Critical seam (RESEARCH Pattern 6, verified): ShowView's `!session.active → PreShowLauncher` early return fires the instant endShow finalizes — the recap state MUST be checked before that return or the payoff screen is swallowed. Recap is view state, never a hash route (ROUTES allow-list is a security control). Retro-marked shows get plain setlist views — no trackedEntries exist for them (RESEARCH Open Question 3, locked).
Output: RecapView, ShowsList + ShowRow, SetlistView, the EndShow→recap seam, config copy, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-pok-dex-history-stats/06-CONTEXT.md
@.planning/phases/06-pok-dex-history-stats/06-RESEARCH.md
@.planning/phases/06-pok-dex-history-stats/06-PATTERNS.md
@.planning/phases/06-pok-dex-history-stats/06-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ShowsList + SetlistView in the Dex Shows segment (D-16, HIST-01)</name>
  <files>packages/app/src/dex/ShowsList.tsx, packages/app/src/dex/SetlistView.tsx, packages/app/src/dex/DexView.tsx, packages/app/src/config.ts, packages/app/test/showsList.test.tsx</files>
  <read_first>
    - packages/app/src/dex/DexView.tsx (06-05/06-07 state shape — the Shows segment slot + Mark CTA placement; extend the view state with openShow)
    - packages/app/src/show/SearchSheet.tsx (result-row idiom lines ~116-127 — the ShowRow analog)
    - packages/app/src/db/db.ts (trackedShows finalized rows + attendedShows; the dedupe key rule from deriveDex: bound showId, else date)
    - packages/app/src/dex/archive-loader.ts (archive setlist source for retro-marked rows; archiveShows cache for fallback-marked)
    - 06-UI-SPEC.md §Layout 1.5 (Shows segment) + §Copywriting ("{date} · {venue}", "{songCount} songs", tally chip, "No shows yet" body)
  </read_first>
  <behavior>
    - The Shows segment lists attended shows newest-first: tracked (finalized) and retro-marked in ONE list, deduped by the show_id/date group rule (a tracked+marked night renders once, as tracked).
    - Tracked rows show {date} · {venue} + {songCount} songs + a tally chip; retro rows show {date} · {venue} + {songCount} songs (from archive or archiveShows cache).
    - Tap a tracked row → RecapView (Task 2); tap a retro row → SetlistView with sets labeled Set 1 / Set 2 / Encore in order (HIST-01).
    - SetlistView renders every song of every set in position order as plain rows (no rings — no outcome data exists for retro shows).
    - Empty state: "No shows yet" + body copy (already present from 06-05 — now replaced by the list when rows exist).
  </behavior>
  <action>
    Create `ShowsList.tsx` (dumb over props from DexView): merge finalized trackedShows + attendedShows into display rows using the SAME group-key rule as deriveDex (bound showId → id-key, else date; tracked wins the render when deduped), sorted date-descending; venue for tracked rows from the Phase-5 binding fields (venueName/city, nullable → date-only fallback), for retro rows from the archive show (by show_id) or archiveShows cache row. Create `SetlistView.tsx`: full-screen drill-in (same overlay idiom as AlbumDetail) with header {date} · {venue} and set-grouped song rows (set labels from config copy: Set 1 / Set 2 / Encore mapped from "1"/"2"/"e"), each row song name (React text) + optional TierBadge from the rarity index. Extend DexView state with `openShow: { kind: "tracked"; sessionId: string } | { kind: "retro"; showId: number } | null`; tracked opens RecapView (Task 2 — stub the import this task if needed by task ordering, or build Task 2 first within the plan), retro opens SetlistView. Add `config.copy.dex` additions (set labels, songs-count template). Tests in showsList.test.tsx (fake-indexeddb + mocked artifacts): newest-first order, tracked+retro dedupe renders once, tally chip only on tracked, retro tap opens set-structured setlist (Set 1/Encore headings + row order asserted), empty-state replacement.
  </action>
  <verify>
    <automated>npx vitest run --project @guezzer/app test/showsList.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - showsList.test.tsx passes all five scenarios; set-structure assertion checks heading order Set 1 → Set 2 → Encore and per-set song order.
    - Dedupe asserted: one fixture night tracked AND marked renders exactly one row.
    - grep confirms ShowsList/SetlistView render names/venues as React text only.
    - Full app suite green.
  </acceptance_criteria>
  <done>Every show the user ever attended is one list — history is browsable as complete set-structured setlists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RecapView — the payoff screen (SHOW-14, STAT-02, D-14/D-15)</name>
  <files>packages/app/src/dex/RecapView.tsx, packages/app/src/config.ts, packages/app/test/recapView.test.tsx</files>
  <read_first>
    - packages/core/src/dex/recap.ts (06-03 RecapStats shape — this view ONLY renders it; no math in the component)
    - packages/app/src/dex/useDexStats.ts (table reads the recap derivation needs: trackedEntries for the session + the full snapshot for new-catch detection)
    - packages/app/src/show/CometTrail.tsx or TrailNodeSheet.tsx (the Phase-4 hit/miss ring rendering to reuse verbatim on recap setlist rows)
    - 06-UI-SPEC.md §Layout 3 (exact section order) + §Copywriting (exact strings: "Show recap", "{hits}/{total} · {pct}%", "calls hit", "Your calls: {a}/{b} · Editor assists: {c}", "Show rarity: {score}", tier chips "× {count}", "Rarest catch of the night: {song}", "+{n} new catches", omit-at-zero rule, footer "Done")
  </read_first>
  <behavior>
    - RecapView(sessionId) computes RecapStats via core deriveRecap over liveQuery table reads and renders, top to bottom: heading + {date} · {venue} → hero tally (Display, tabular-nums) + "calls hit" → source split line → rarity block (score + tier chips ×count + rarest of the night with TierBadge) → "+{n} new catches" with Sparkles (row OMITTED entirely when n is 0) → final setlist grouped by set with each row wearing its hit/miss ring + TierBadge → footer Done CTA (neutral; Share card joins in 06-09).
    - Scrollable full-screen view on the dominant surface; footer respects safe-area inset; placeholder (???) rows render flagged, ring-less.
    - Done invokes the onClose callback (ShowView clears recap state; Dex history closes the drill-in).
  </behavior>
  <action>
    Create `RecapView.tsx` taking `{ sessionId, onClose }`: liveQuery the session's trackedShow + entries and the full snapshot tables, memoized `deriveRecap(sessionId, snapshot, archive, albums, rarityIndex)` (all pure core — the component contains zero stat arithmetic; assert this in review), render per the UI-SPEC section order with Display 28px hero, Label chips, and the inherited hit `#22C55E` / miss `#EF4444` rings on setlist rows (reuse the Phase-4 ring treatment — read the trail components first). Tier chips render each tier word × count with TierBadge colors; bonus muted stat lines are allowed only if corpus-derived and color-free (D-15 discretion boundary). All strings via a new `config.copy.recap` block. Tests in recapView.test.tsx with a fixture session (mocked artifacts + fake-indexeddb): hero tally text, source-split line with exact numbers, rarity score + rarest-of-night rendering, +N row present at n>0 and ABSENT at n=0, setlist set-grouping with ring classes per outcome, Done fires onClose.
  </action>
  <verify>
    <automated>npx vitest run --project @guezzer/app test/recapView.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - recapView.test.tsx passes all six assertions; the +0-omission case queries for the Sparkles row's absence.
    - grep confirms RecapView imports deriveRecap and contains no local tally/percentage arithmetic beyond formatting.
    - All recap strings sourced from config.copy.recap (spot-check "calls hit" appears only in config.ts).
    - Full app suite green.
  </acceptance_criteria>
  <done>The recap renders the night's honest scorecard from one pure core derivation — game feel on top, corpus truth underneath.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: End Show → recap seam (D-13, RESEARCH Pattern 6)</name>
  <files>packages/app/src/show/ShowView.tsx, packages/app/src/show/EndShowDialog.tsx, packages/app/test/recapView.test.tsx</files>
  <read_first>
    - packages/app/src/show/ShowView.tsx (the `!session.active` early return ~line 163 the recap MUST render before; the endOpen state pattern ~lines 70-75; FabMenu wiring from 06-02)
    - packages/app/src/show/EndShowDialog.tsx (handleConfirm ~lines 77-81: endShow + exportBackup + onClose — the onEnded hook point AFTER the auto-backup)
    - packages/app/test/endShowDialog.test.tsx (existing seam tests to keep green)
  </read_first>
  <behavior>
    - Confirming End Show finalizes the session, fires the Phase-5 auto-backup, closes the dialog, AND opens the recap for that sessionId — automatically, no extra tap (D-13).
    - The recap renders even though the session is no longer active (the PreShowLauncher early return does not swallow it).
    - Done on the recap returns to the normal pre-show state; re-opening the same show later from Dex history shows the same recap.
  </behavior>
  <action>
    Add an `onEnded(sessionId: string)` callback prop to EndShowDialog, invoked in handleConfirm after `endShow` and `exportBackup` (keep their order and fire-and-forget semantics untouched). In ShowView: add `recapSessionId: string | null` state set by onEnded; render `<RecapView sessionId={recapSessionId} onClose={() => setRecapSessionId(null)} />` BEFORE the `!session.active` early return (the load-bearing line — cite RESEARCH Pattern 6; a comment in code should mark why the ordering matters). Extend recapView.test.tsx with the seam test: render ShowView with an active fixture session → confirm End Show → recap heading appears without navigation; Done → PreShowLauncher appears.
  </action>
  <verify>
    <automated>npx vitest run --project @guezzer/app test/recapView.test.tsx test/endShowDialog.test.tsx</automated>
    <human-check>End-of-phase device gate: end a real tracked show; the recap appears immediately after the backup download, still in the venue flow.</human-check>
  </verify>
  <acceptance_criteria>
    - The seam test passes: End Show confirm → recap visible → Done → PreShowLauncher.
    - ShowView renders the recap branch before the session-active early return (source assertion: the recapSessionId check precedes the `!session` return in file order).
    - Existing endShowDialog tests remain green (endShow/exportBackup call order unchanged).
    - Full app suite green.
  </acceptance_criteria>
  <done>The payoff moment fires itself: End Show → backup → scorecard, and the scorecard stays reachable from history forever.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Archive/tracked strings → recap/history DOM | Venue and song names from external data render in new surfaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-21 | Injection (XSS) | recap/setlist/history strings | mitigate | React text rendering only (inherited rule); no HTML from data ever |
| T-06-22 | Repudiation (lost scorecard) | End Show → recap seam | mitigate | Recap derives from persisted trackedEntries — even if the auto-open is missed (crash), history re-derives the identical recap (pure derivation, nothing transient) |
| T-06-23 | Tampering (state confusion) | recap-before-early-return ordering | mitigate | Source-order acceptance criterion + seam test pin the Pattern-6 fix |
| T-06-SC | Tampering (supply chain) | package installs | mitigate | No new dependencies in this plan |
</threat_model>

<verification>
- `npx vitest run` (both projects) green; `npx tsc --noEmit` green.
- Dev-server spot check: end a mock show (?mockLatest harness ok) → recap auto-appears; Shows segment lists it.
</verification>

<success_criteria>
- SHOW-14/D-14: recap = tally + source split + rarity + new catches + set-structured ringed setlist, auto-shown (D-13).
- STAT-02/D-15: show rarity score + tier chips + rarest of the night, all corpus-derived.
- HIST-01/D-16: complete setlist views for every attended show, newest-first in the Dex tab.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 08)

New symbols (exclude from source-drift verification — created here):
- Components `ShowsList`, `SetlistView`, `RecapView` — `packages/app/src/dex/`
- `onEnded` prop — `packages/app/src/show/EndShowDialog.tsx`; `recapSessionId` state — `packages/app/src/show/ShowView.tsx`
- `config.copy.recap.*` block + set-label copy — `packages/app/src/config.ts`
- New tests: `showsList.test.tsx`, `recapView.test.tsx`
</artifacts_produced>

<output>
Create `.planning/phases/06-pok-dex-history-stats/06-08-SUMMARY.md` when done
</output>
