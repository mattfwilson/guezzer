{
  "//": "Node-context files in the app package: the one-time build scripts and the fs-based committed-asset guard tests. Kept separate from tsconfig.json so browser src stays DOM-typed and node globals never leak into it.",
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2023"],
    "erasableSyntaxOnly": true,
    "allowImportingTsExtensions": true,
    "esModuleInterop": true,
    "noEmit": true,
    "module": "nodenext",
    "moduleResolution": "nodenext",
    "types": ["node"]
  },
  "include": ["scripts/**/*.ts", "test/coversManifest.test.ts"]
}
