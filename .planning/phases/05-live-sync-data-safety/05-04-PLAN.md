---
phase: 05-live-sync-data-safety
plan: 04
type: execute
wave: 2
depends_on: [05-01, 05-03]
files_modified:
  - packages/app/src/live/useOnlineStatus.ts
  - packages/app/src/live/useLatestPoll.ts
  - packages/app/src/live/SuggestionStrip.tsx
  - packages/app/src/live/SyncDot.tsx
  - packages/app/src/show/ShowView.tsx
  - packages/app/test/useLatestPoll.test.tsx
  - packages/app/test/adopt.test.tsx
autonomous: true
requirements: [SYNC-01, SYNC-02, SYNC-03]
must_haves:
  truths:
    - "During an active show the app polls latest at most once per 60s via a single self-scheduling timer, and stops on End Show / unmount (SYNC-01)"
    - "Polling pauses when navigator.onLine is false or the document is hidden, and resumes silently when online + visible (SYNC-03)"
    - "The next 1-2 un-logged editor songs appear in a fixed-height strip below the orbit; adopting one logs it with source:'editor' and the correct hit/miss (SYNC-02/D-01/D-03)"
    - "A quiet SyncDot (filled=online, hollow=offline) shows in the Show header with a one-time offline reassurance line, never a banner (D-08)"
    - "The tracked show auto-binds to the real show_id/venue when latest's date is today and the show is unbound (D-07)"
    - "The app stays fully functional offline; the orbit never re-lays-out when a suggestion appears/dismisses (SYNC-03/SHOW-02 preservation)"
  artifacts:
    - path: "packages/app/src/live/useLatestPoll.ts"
      provides: "the single-timer, gated, tolerant poll loop hook"
      contains: "useLatestPoll"
    - path: "packages/app/src/live/useOnlineStatus.ts"
      provides: "useSyncExternalStore over online/offline events"
      contains: "useOnlineStatus"
    - path: "packages/app/src/live/SuggestionStrip.tsx"
      provides: "the fixed-height advisory strip (adopt/dismiss/fill-???)"
      contains: "SuggestionStrip"
    - path: "packages/app/src/live/SyncDot.tsx"
      provides: "the quiet online/offline indicator"
      contains: "SyncDot"
  key_links:
    - from: "packages/app/src/live/useLatestPoll.ts"
      to: "@guezzer/core pollLatest"
      via: "injected fetch inside the gated tick"
      pattern: "pollLatest"
    - from: "packages/app/src/show/ShowView.tsx"
      to: "adoptSuggestion / bindShow"
      via: "write-through on adopt and after a successful poll"
      pattern: "adoptSuggestion|bindShow"
    - from: "packages/app/src/live/SuggestionStrip.tsx"
      to: "diffLatestAgainstTrail / resolvePlaceholders"
      via: "deriving the shown suggestions from latest rows + trail"
      pattern: "diffLatestAgainstTrail"
---

<objective>
Deliver the live-sync vertical slice end to end: *As a show-tracker, I want editor-logged songs to appear as deduped suggestions during a show, so that I get a second set of eyes without ever clobbering my manual tracking (SYNC-01, SYNC-02, SYNC-03).* The app tier owns the one poll timer (gated on active-show + online + visible), renders the advisory SuggestionStrip and the quiet SyncDot, adopts suggestions as `source:'editor'` write-throughs, resolves "???" placeholders via the Phase-4 rename path, and auto-binds the show — all fully functional offline.

Purpose: SYNC-01 (polite ≤1/60s active-show-only polling), SYNC-02 (suggestions-only, deduped, never clobbering), SYNC-03 (offline-resilient with silent resume), and the D-07 binding.
Output: `useOnlineStatus.ts`, `useLatestPoll.ts`, `SuggestionStrip.tsx`, `SyncDot.tsx`, ShowView wiring, and two app tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-live-sync-data-safety/05-CONTEXT.md
@.planning/phases/05-live-sync-data-safety/05-RESEARCH.md
@.planning/phases/05-live-sync-data-safety/05-PATTERNS.md
@.planning/phases/05-live-sync-data-safety/05-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useOnlineStatus + the single-timer tolerant useLatestPoll loop</name>
  <files>packages/app/src/live/useOnlineStatus.ts, packages/app/src/live/useLatestPoll.ts, packages/app/test/useLatestPoll.test.tsx</files>
  <read_first>
    - packages/app/src/routing/useHashRoute.ts (useSyncExternalStore over a browser event, lines 21-28 — EXACT structural match for useOnlineStatus; swap hashchange for online/offline)
    - packages/app/src/wakeLock.ts (visibilitychange lifecycle idiom lines 91-105) and packages/app/src/show/ShowView.tsx (the wake-lock effect mount/cleanup ref discipline lines 83-91)
    - packages/app/src/config.ts (config.live.POLL_INTERVAL_MS / POLL_MAX_INTERVAL_MS — from Plan 03)
    - 05-RESEARCH.md Pattern 1 (self-scheduling single-timer sketch) + Pitfalls 2, 3, 4 (onLine one-directional; timer leaks; iOS background freeze)
    - packages/app/test/wakeLock.test.ts (jsdom lifecycle test style)
  </read_first>
  <behavior>
    - useOnlineStatus returns navigator.onLine and updates on online/offline events (subscribe/unsubscribe correctly).
    - useLatestPoll(active, onRows): when active is undefined, no timer is ever scheduled (no poll without an active show, SYNC-01).
    - With an active show and fake timers, exactly one poll fires per POLL_INTERVAL_MS interval — never more (single-timer guarantee; advance timers and assert onRows call count).
    - A tick is skipped (no fetch/onRows) while navigator.onLine===false OR document.visibilityState==="hidden", and resumes on the next eligible tick (SYNC-03).
    - Unmounting clears the pending timer (no post-unmount poll; assert no onRows after cleanup).
    - A thrown pollLatest is swallowed (tolerant) and the loop still schedules the next tick.
    - Adaptive backoff: after N consecutive empty polls the next delay grows but never below POLL_INTERVAL_MS and never above POLL_MAX_INTERVAL_MS.
  </behavior>
  <action>
    Create `packages/app/src/live/useOnlineStatus.ts` exporting `useOnlineStatus(): boolean` via `useSyncExternalStore` subscribing to window `online`/`offline` events with `() => navigator.onLine` as the snapshot and `() => true` as the server snapshot (copy useHashRoute's shape). Create `packages/app/src/live/useLatestPoll.ts` exporting `function useLatestPoll(active: { sessionId: string } | undefined, onRows: (rows: LatestSetlistRow[]) => void): void`. Implement the single self-scheduling setTimeout loop from RESEARCH Pattern 1: one `useRef<ReturnType<typeof setTimeout> | null>` for the pending timer and one `useRef<number>` for the idle streak; a `useEffect` keyed on `[active?.sessionId, online]` (from useOnlineStatus) that returns early when `!active`, defines an async `tick` that (a) gates on `navigator.onLine && document.visibilityState === "visible"`, (b) on eligibility calls `await pollLatest({ fetch })` from `@guezzer/core` inside try/catch (swallow errors, D-06), passes rows to `onRows`, updates the idle streak (reset on rows, increment on empty), (c) computes the next delay `Math.min(config.live.POLL_INTERVAL_MS * (1 + idleStreak), config.live.POLL_MAX_INTERVAL_MS)`, and (d) schedules the next tick; the effect schedules the FIRST tick at POLL_INTERVAL_MS and its cleanup sets a cancelled flag and clearTimeouts the ref (Pitfall 3 — always clear before scheduling and on cleanup). Never use setInterval. Create `packages/app/test/useLatestPoll.test.tsx` using Vitest fake timers + a mocked `pollLatest` covering the behavior block.
  </action>
  <verify>
    <automated>cd packages/app && npx vitest run test/useLatestPoll.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - A test with fake timers asserts at most one onRows call per POLL_INTERVAL_MS across several intervals (≤1/60s honored).
    - A test asserts NO timer/poll is scheduled when `active` is undefined.
    - A test asserts a tick is skipped while offline/hidden and resumes when eligible.
    - A test asserts unmount clears the timer (no onRows after unmount).
    - A test asserts a thrown pollLatest does not stop the loop.
    - `npx vitest run test/useLatestPoll.test.tsx` passes.
  </acceptance_criteria>
  <done>One gated, tolerant, self-scheduling timer polls latest ≤1/60s only while a show is active, online, and visible — leak-free and offline-resilient.</done>
</task>

<task type="auto">
  <name>Task 2: SuggestionStrip + SyncDot components</name>
  <files>packages/app/src/live/SuggestionStrip.tsx, packages/app/src/live/SyncDot.tsx</files>
  <read_first>
    - packages/app/src/show/TrailNodeSheet.tsx (adopt/rename write-through idiom, untrusted-string rendering rule, min-h-11/border-hairline/bg-elevated/touch-manipulation tokens)
    - packages/app/src/show/WakeLockNotice.tsx (small quiet status component — analog for SyncDot's calm styling + aria)
    - packages/app/src/config.ts (config.copy.live.*, config.ui.SUGGESTION_STRIP_HEIGHT, config.ui.SYNC_DOT_DIAMETER — from Plan 03)
    - 05-UI-SPEC.md §Color B3 (SyncDot filled/hollow, 8px, #A1A1AA, aria-label), §Layout (SuggestionStrip fixed 56px slot, below-orbit, blank-when-empty), §Component Inventory
    - @guezzer/core exports Suggestion, FillHint types (Plan 01)
  </read_first>
  <action>
    Create `packages/app/src/live/SuggestionStrip.tsx` exporting `SuggestionStrip({ suggestions, fillHints, onAdopt, onDismiss, onFill })` — a component occupying a FIXED height of `config.ui.SUGGESTION_STRIP_HEIGHT` px (inline style height so the slot is always allocated and the orbit never re-lays-out, SHOW-02 preservation) on the `bg-elevated` surface with a `border-hairline` top edge. Each row: a muted Label eyebrow (`config.copy.live` "kglw editor" for a Suggestion, "Fill in ???" for a FillHint), the song name in `text-text-primary` Body, a `Plus`-icon **Add** control (min-h-11, ≥44px) calling `onAdopt(suggestion)` / for a fill hint a `Pencil` control calling `onFill(hint)`, and and an `X`-icon dismiss control (min-h-11) calling `onDismiss`. Implement BOTH dismiss affordances per the approved 05-UI-SPEC (§Interaction rules + §Component Inventory "X/swipe dismisses"): the `X` control AND a horizontal swipe on the row both call `onDismiss` for that suggestion. Wire swipe via pointer events (pointerdown → track horizontal delta → on pointerup past a threshold, e.g. ~40px, invoke `onDismiss(suggestion)`); keep it non-destructive (nothing is logged, D-01/SYNC-02). Both the tap-X and swipe paths dismiss the same row. Render song names/venue as PLAIN escaped React text only — NEVER dangerouslySetInnerHTML (untrusted editor content, SCHEMA §12). When both arrays are empty, render an empty fixed-height slot (blank calm space, no empty-state card). Create `packages/app/src/live/SyncDot.tsx` exporting `SyncDot({ online })` — an 8px (`config.ui.SYNC_DOT_DIAMETER`) round span: filled `#A1A1AA` (text-muted) when online, a 1px `#A1A1AA` ring with transparent fill when offline; passive (not a button); with an `aria-label` reflecting "Sync: online"/"Sync: offline". Never use hit-green/miss-red/accent-gold. Do not render a banner; the one-time offline reassurance LINE is rendered by ShowView (Task 3) using `config.copy.live` wording.
  </action>
  <verify>
    <automated>cd packages/app && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `SuggestionStrip` sets an explicit height equal to `config.ui.SUGGESTION_STRIP_HEIGHT` regardless of content (source assertion: the height style is present unconditionally).
    - Every interactive control in the strip carries a `min-h-11` (or equivalent ≥44px) class.
    - Neither component references `dangerouslySetInnerHTML`, hit/miss color tokens, or the accent token (source assertion).
    - `SyncDot` renders an `aria-label` distinguishing online vs offline.
    - The strip dismisses a suggestion via BOTH the `X` control and a horizontal swipe on the row (per the approved UI-SPEC "X/swipe dismisses"); both invoke `onDismiss` for that row and are non-destructive.
    - Typecheck passes.
  </acceptance_criteria>
  <done>The advisory strip is a fixed-height, calm, escaped-text-only surface with ≥44px controls, and the SyncDot is a passive token-only indicator.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire poll + strip + dot + adopt/bind into ShowView</name>
  <files>packages/app/src/show/ShowView.tsx, packages/app/test/adopt.test.tsx</files>
  <read_first>
    - packages/app/src/show/ShowView.tsx (the WHOLE file: the sub-header row lines 179-197 where SyncDot mounts near TallyReadout; the CometTrail/OrbitStage/ActionBar region lines 209-233 where the strip mounts above ActionBar; handleTapOrb lines 120-130 and handleSearchSelect lines 152-162 as the write-through template; the wake-lock effect lines 83-91 as the hook-mount pattern)
    - packages/app/src/show/useShowSession.ts (session.active, session.entries, session.shownFanSongIds — the reactive inputs; adopt/bind are write-throughs that re-render via useLiveQuery, no useState mirror)
    - packages/app/src/db/db.ts (adoptSuggestion, bindShow helpers — from Plan 03) and renameEntry (the fill-??? path)
    - packages/app/src/show/scoring.ts (classifyOutcome — adopt reclassification)
    - packages/app/src/show/TrailNodeSheet.tsx (handlePick rename path lines 54-62 — reuse for fill-???)
    - @guezzer/core diffLatestAgainstTrail, resolvePlaceholders, bindShowFromLatest (Plan 01)
    - 05-UI-SPEC.md §Layout & Interaction (region stack; adopt = no-confirm fast path; offline reassurance one-time line)
  </read_first>
  <behavior>
    - Adopting a suggestion calls adoptSuggestion(sessionId, {songId, songName, shownFanSongIds}) → the entry is written with source:"editor" and a hit iff the song was in session.shownFanSongIds; the trail/tally re-render and the adopted song drops out of the strip (deduped, D-02).
    - A fill-??? hint routes through the existing rename path (renameEntry) pre-filled with the editor's song name — never auto-applied (D-04).
    - After a successful poll while the show is unbound and latest's showdate === today, bindShow is called once with the returned binding; an already-bound show or a date mismatch calls nothing (D-07).
    - The SyncDot reflects useOnlineStatus; a one-time offline reassurance line appears on the first online→offline transition and auto-clears when back online (D-08) — no banner.
    - The suggestions shown are diffLatestAgainstTrail(latestRows, entries, config.live.SUGGESTION_COUNT) plus resolvePlaceholders(latestRows, entries); dismiss removes a row from local component state without logging anything (SYNC-02 non-destructive).
  </behavior>
  <action>
    In `packages/app/src/show/ShowView.tsx`: add local state `const [latestRows, setLatestRows] = useState<LatestSetlistRow[]>([])` and mount `useLatestPoll(session.active, setLatestRows)` (unconditional hook, above the early returns — same discipline as the wake-lock effect). Compute `online = useOnlineStatus()`. Derive `suggestions = diffLatestAgainstTrail(latestRows, session.entries, config.live.SUGGESTION_COUNT)` and `fillHints = resolvePlaceholders(latestRows, session.entries)` (memoized on latestRows + entries), and maintain a dismissed-ids set in component state to filter them. After each poll (in an effect keyed on latestRows), if `session.active && session.active.showId === null`, call `bindShowFromLatest(latestRows, session.active, session.active.date)` and, when it returns a binding, `void bindShow(session.active.sessionId, binding)` (D-07). Render `<SyncDot online={online} />` in the sub-header row next to `<TallyReadout>` and, on the first online→offline flip, a one-time muted reassurance line using `config.copy.live` (track shown-once with a ref, clear on return-to-online). Render `<SuggestionStrip suggestions={visibleSuggestions} fillHints={visibleFillHints} onAdopt={handleAdopt} onDismiss={handleDismiss} onFill={handleFill} />` directly above the `<ActionBar>` (its fixed height keeps the orbit stable). `handleAdopt(s)` = `void adoptSuggestion(sessionId, { songId: s.songId, songName: s.songName, shownFanSongIds: session.shownFanSongIds })` (no confirm — advisory-to-logged fast path); `handleFill(h)` = open the existing rename path pre-filled with the editor song name and confirm via renameEntry (reuse TrailNodeSheet's handlePick classification); `handleDismiss` = add the id to the dismissed set. All adopt/bind writes are write-throughs — do NOT mirror trail/tally in useState (useLiveQuery re-derives). Create `packages/app/test/adopt.test.tsx` (jsdom + fake-indexeddb) asserting an adopted suggestion is written with source:"editor" and the correct outcome and disappears from the derived suggestions.
  </action>
  <verify>
    <automated>cd packages/app && npx vitest run test/adopt.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `adopt.test.tsx` asserts an adopted editor song is persisted with `source === "editor"` and `outcome` matching `classifyOutcome` against the shown fan.
    - A test asserts an adopted song no longer appears in `diffLatestAgainstTrail` output for the updated trail (deduped, D-02).
    - ShowView calls `bindShow` only when the show is unbound AND latest's date matches (source assertion of the guarded branch using `bindShowFromLatest`).
    - ShowView renders `SuggestionStrip` above `ActionBar` and `SyncDot` in the sub-header (source assertion).
    - No `useState` mirror of trail/tally is introduced (write-through discipline).
    - `npx vitest run test/adopt.test.tsx` passes.
  </acceptance_criteria>
  <done>The live-sync loop is wired end to end: suggestions appear, adopt logs an editor-sourced classified entry, "???" fills route through rename, the show auto-binds, and the SyncDot reflects connectivity — fully functional offline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| latest rows → ShowView render + write | Editor-sourced strings and rows reach the DOM and the DB via adopt |
| poll loop → volunteer-run kglw.net | Client-initiated network requests to a guest host |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11 | Denial of Service (etiquette) | useLatestPoll | mitigate | Single self-scheduling timer, ≥60s floor (config.live.POLL_INTERVAL_MS), active-show + online + visible gating, unmount cleanup — at most one request per 60s by construction (SYNC-01, Pitfall 3) |
| T-05-12 | Tampering / XSS | SuggestionStrip song/venue rendering | mitigate | Editor strings rendered as escaped React text only; no dangerouslySetInnerHTML (SCHEMA §12) |
| T-05-13 | Tampering | auto-bind (bindShowFromLatest guard) | mitigate | Wrong-show/date guard; never overwrites a bound showId; binding is reversible via export/import (D-07) |
| T-05-14 | Information Disclosure | offline handling | mitigate | Offline is a first-class calm state (dot + one-time line), no stack traces, no loud banner (D-08, ASVS V7) |
| T-05-SC | Tampering (supply chain) | package installs | mitigate | No new dependencies this phase (RESEARCH §Package Legitimacy Audit — vacuously satisfied) |
</threat_model>

<verification>
- `cd packages/app && npx vitest run test/useLatestPoll.test.tsx test/adopt.test.tsx` green.
- `cd packages/app && npx tsc --noEmit -p tsconfig.json` clean.
- Full app suite green: `cd packages/app && npx vitest run`.
- Manual (device gate — deferred to phase gate, see 05-VALIDATION §Manual-Only): real latest poll ≤1/60s on installed iOS PWA; airplane-mode flap shows calm offline dot + silent resume.
</verification>

<success_criteria>
- SYNC-01: ≤1/60s active-show-only polling via a single leak-free timer.
- SYNC-02: deduped advisory suggestions; adopt logs source:'editor' with honest hit/miss; never clobbers manual tracking.
- SYNC-03: fully functional offline; silent resume; quiet dot.
- D-07: guarded auto-bind. SHOW-02 preserved (fixed-height strip).
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 04)

New symbols (exclude from source-drift verification — created here):
- `useOnlineStatus` — `packages/app/src/live/useOnlineStatus.ts`
- `useLatestPoll` — `packages/app/src/live/useLatestPoll.ts`
- `SuggestionStrip` — `packages/app/src/live/SuggestionStrip.tsx`
- `SyncDot` — `packages/app/src/live/SyncDot.tsx`
- ShowView additions: latestRows state, poll mount, suggestion derivation, adopt/dismiss/fill/bind handlers, SyncDot + SuggestionStrip render — `packages/app/src/show/ShowView.tsx`
- New tests: `useLatestPoll.test.tsx`, `adopt.test.tsx`
</artifacts_produced>

<output>
Create `.planning/phases/05-live-sync-data-safety/05-04-SUMMARY.md` when done
</output>
