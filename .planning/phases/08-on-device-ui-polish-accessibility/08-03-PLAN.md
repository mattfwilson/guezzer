---
phase: 08-on-device-ui-polish-accessibility
plan: 03
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - packages/app/src/components/AppMenu.tsx
  - packages/app/src/dex/ShareCardSheet.tsx
  - packages/app/src/dex/CompareView.tsx
  - packages/app/src/settings/SettingsView.tsx
  - packages/app/test/compareView.test.tsx
  - packages/app/test/shareCard.test.tsx
  - packages/app/test/settingsOwner.test.tsx
autonomous: true
requirements: [A11Y-01]
must_haves:
  truths:
    - "AppMenu, ShareCardSheet, CompareView, and the 'Whose dex is this?' prompt each dismiss with Escape, trap focus, and restore focus to their trigger on close"
    - "CompareView renders through the fullscreen <Sheet> variant (header X, no backdrop) with trap + restore"
    - "The 'Whose dex is this?' text input receives focus on open via initialFocusRef, and its maxLength clamp + escaped-text rendering are unchanged"
    - "The ShareCardSheet build-failure state and CompareView error branch still render (no throw through the migration)"
  artifacts:
    - path: "packages/app/src/components/AppMenu.tsx"
      provides: "App menu rendered through <Sheet modal>"
      contains: "Sheet"
    - path: "packages/app/src/dex/CompareView.tsx"
      provides: "Compare overlay rendered through <Sheet modal variant=fullscreen>"
      contains: "Sheet"
    - path: "packages/app/src/settings/SettingsView.tsx"
      provides: "Whose-dex prompt via <Sheet modal> + initialFocusRef on the text input"
      contains: "initialFocusRef"
  key_links:
    - from: "packages/app/src/settings/SettingsView.tsx"
      to: "packages/app/src/components/Sheet.tsx"
      via: "<Sheet modal initialFocusRef={inputRef}>"
      pattern: "initialFocusRef"
---

<objective>
Migrate the remaining four audited modal surfaces — `AppMenu`, `ShareCardSheet`, `CompareView` (fullscreen variant), and the inline "Whose dex is this?" prompt in `SettingsView` — onto the shared `<Sheet>` primitive (D-01), giving each Escape-dismiss, focus-trap, and focus-restore — the full-modal treatment per D-02 (A11Y-01). The name prompt additionally gets `initialFocusRef` so the text field is focused on open.

Purpose: The other four of the seven audited A11Y-01 surfaces, including the fullscreen and text-input special cases.
Output: Four migrated components + focus/Escape assertions in their tests, with all guarded error/empty states preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-on-device-ui-polish-accessibility/08-RESEARCH.md
@.planning/phases/08-on-device-ui-polish-accessibility/08-PATTERNS.md
@.planning/phases/08-on-device-ui-polish-accessibility/08-UI-SPEC.md
@.planning/phases/08-on-device-ui-polish-accessibility/08-01-PLAN.md
</context>

<artifacts_produced>
## Artifacts this phase produces (created/changed by THIS plan)

- `AppMenu`, `ShareCardSheet` re-expressed through `<Sheet modal variant="bottom-sheet">`.
- `CompareView` re-expressed through `<Sheet modal variant="fullscreen">` (header X, no backdrop).
- `SettingsView` "Whose dex is this?" prompt re-expressed through `<Sheet modal>` with `initialFocusRef` on the `#whose-dex` input.
- Focus/Escape assertions added to `compareView.test.tsx`, `shareCard.test.tsx`, `settingsOwner.test.tsx`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Migrate AppMenu to `<Sheet modal>`</name>
  <read_first>
    - packages/app/src/components/AppMenu.tsx (full file — shell at ~:40-52, `z-20`, backdrop + X, the `if (!open) return null` guard at ~:21)
    - packages/app/src/components/Sheet.tsx
    - packages/app/src/App.tsx (AppMenu is rendered here as a sibling of AppShell inside #app-content; it portals out via the primitive)
    - .planning/phases/08-on-device-ui-polish-accessibility/08-PATTERNS.md §"The 6 true modals + WhyDetail"
  </read_first>
  <action>
    Wrap the menu rows in `<Sheet open={open} onClose={onClose} modal variant="bottom-sheet" ariaLabel="Menu">` (reuse the existing "Menu" accessible label — no new string). Remove the raw `z-20`; the X button and backdrop-tap both map to `onClose`. Preserve the existing menu row callbacks verbatim. The `if (!open) return null` guard is now owned by the primitive — remove the duplicate cleanly.
  </action>
  <verify>
    <automated>pnpm vitest run packages/app/test/version.test.tsx</automated>
    <automated>pnpm --filter @guezzer/app exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `AppMenu.tsx` renders `<Sheet ... modal ...>`; no `z-20` (or any raw `z-[0-9]`) class remains.
    - Menu row callbacks unchanged; app suite green.
    - App `tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The app menu is a focus-managed, Escape-dismissible modal.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate ShareCardSheet (bottom-sheet) and CompareView (fullscreen) to `<Sheet>`</name>
  <read_first>
    - packages/app/src/dex/ShareCardSheet.tsx (shell at ~:83-88, `z-40`, backdrop + Close; the build-failure state to preserve)
    - packages/app/src/dex/CompareView.tsx (shell at ~:80/100, `z-40`, fullscreen header-X, NO backdrop; the error branch at ~:75-89)
    - packages/app/src/components/Sheet.tsx (fullscreen variant contract)
    - .planning/phases/08-on-device-ui-polish-accessibility/08-PATTERNS.md §"Closed-sheet guard / error preservation (V7)"
    - packages/app/test/shareCard.test.tsx, packages/app/test/compareView.test.tsx
    - packages/app/src/config.ts (`config.copy.share.sheetLabel`, `config.copy.compare.banner` for ariaLabels)
  </read_first>
  <action>
    ShareCardSheet: wrap in `<Sheet open={open} onClose={onClose} modal variant="bottom-sheet" ariaLabel={config.copy.share.sheetLabel}>`; Close/backdrop → `onClose`; remove `z-40`. Preserve the pre-built-File-on-open behavior (06-11 Pitfall 7) and the build-failure render branch untouched.
    CompareView: wrap in `<Sheet open onClose={onClose} modal variant="fullscreen" ariaLabel={config.copy.compare.banner}>`; the header X → `onClose`; NO backdrop (fullscreen). Remove both `z-40` literals. Preserve the error branch and the read-only, no-DB-writes contract (06-10) exactly.
  </action>
  <verify>
    <automated>pnpm vitest run packages/app/test/shareCard.test.tsx packages/app/test/compareView.test.tsx</automated>
    <automated>pnpm --filter @guezzer/app exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `ShareCardSheet.tsx` renders `<Sheet ... variant="bottom-sheet">`; `CompareView.tsx` renders `<Sheet ... variant="fullscreen">`; no raw `z-[0-9]` remains in either.
    - The ShareCardSheet build-failure branch and CompareView error branch still render (no throw) — assert both in their tests.
    - Add an Escape→onClose assertion to each test.
    - App `tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Both dex overlays are focus-managed and Escape-dismissible; the fullscreen variant is exercised; error/build-failure states preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate the "Whose dex is this?" prompt to `<Sheet modal>` + initialFocusRef</name>
  <read_first>
    - packages/app/src/settings/SettingsView.tsx (inline prompt at ~:269-299, `z-40`, backdrop; the `#whose-dex` text input at ~:291-299 with NO autoFocus; the maxLength clamp)
    - packages/app/src/components/Sheet.tsx (initialFocusRef prop)
    - .planning/phases/08-on-device-ui-polish-accessibility/08-RESEARCH.md §"Security Domain" (V5 — adding autoFocus/initialFocusRef changes focus, NOT validation)
    - packages/app/test/settingsOwner.test.tsx
    - packages/app/src/config.ts (`config.copy.compare.namePrompt` for the ariaLabel)
  </read_first>
  <action>
    Replace the inline `role="dialog"`/`bg-black/50 z-40` prompt with `<Sheet open={promptOpen} onClose={…} modal variant="bottom-sheet" ariaLabel={config.copy.compare.namePrompt} initialFocusRef={inputRef}>`. Create a `useRef` for the text input and pass it as `initialFocusRef` so the field is focused on open. Keep the existing buttons ("It's mine, restore it" + name-submit path), the `maxLength` clamp (40), and escaped-React-text rendering EXACTLY — this changes focus only, not validation (V5). Preserve the WARNING-1 name→restore resolution path (quick 260716-vw2) unchanged.
  </action>
  <verify>
    <automated>pnpm vitest run packages/app/test/settingsOwner.test.tsx</automated>
    <automated>pnpm --filter @guezzer/app exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `SettingsView.tsx` renders the prompt via `<Sheet ... initialFocusRef={inputRef}>`; no `z-40` (or raw `z-[0-9]`) remains in the prompt.
    - The text input keeps `maxLength={40}` and is rendered as escaped React text (no `dangerouslySetInnerHTML`).
    - `settingsOwner.test.tsx` still asserts the name-submit + "It's mine" restore paths; add an Escape→close assertion.
    - App `tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The owner-name prompt is a focus-trapping modal whose text field is auto-focused on open; validation unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner text input → app state | The "Whose dex is this?" field accepts free text (owner identity) |
| kglw-derived data → sheet chrome | Compare/share names render as React text only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | Input Validation (V5) | "Whose dex" text input | mitigate | `initialFocusRef` changes focus, not validation; `maxLength={40}` clamp + escaped-text rendering preserved verbatim. No new input surface. |
| T-08-08 | Error Handling (V7) | ShareCardSheet build failure / CompareView error branch | mitigate | Both guarded states kept; `<Sheet>` renders children as-is, `if (!open) return null` preserved — no throw. |
| T-08-09 | Tampering/Elevation | compare/share song+venue names | mitigate | React-text/`fillText`-only invariant preserved; no `dangerouslySetInnerHTML`. |
| T-08-SC | Tampering (supply chain) | npm installs | accept | No new packages; refactor only. |
</threat_model>

<verification>
- `pnpm vitest run packages/app` green.
- Grep: no `z-[0-9]` literal remains in `AppMenu.tsx`, `ShareCardSheet.tsx`, `CompareView.tsx`, or the SettingsView prompt.
- `maxLength={40}` still present on the `#whose-dex` input.
</verification>

<success_criteria>
- AppMenu, ShareCardSheet, CompareView, and the name prompt dismiss with Escape, trap focus, and restore to trigger (A11Y-01).
- Fullscreen variant + initialFocusRef exercised; all guarded error/empty states preserved; validation unchanged.
</success_criteria>

<output>
Create `.planning/phases/08-on-device-ui-polish-accessibility/08-03-SUMMARY.md` when done
</output>
