---
phase: 17-backend-foundation-secrets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - packages/app/package.json
  - packages/app/src/db/supabase.ts
  - supabase/config.toml
  - supabase/.gitignore
autonomous: false
requirements: [SETUP-04]
user_setup: []

must_haves:
  truths:
    - "The app has a single module that constructs the Supabase client from the anon key + URL only (SETUP-04 / D-14)"
    - "The Supabase CLI is available via npx and a supabase/ project scaffold exists for migrations"
    - "packages/core has zero @supabase dependency (client is app-layer only)"
  artifacts:
    - path: "packages/app/src/db/supabase.ts"
      provides: "The ONLY createClient call; exported `supabase` client (D-14)"
      min_lines: 4
      contains: "createClient"
    - path: "packages/app/package.json"
      provides: "@supabase/supabase-js@2.110.8 in dependencies"
      contains: "@supabase/supabase-js"
    - path: "package.json"
      provides: "supabase CLI root devDependency + db:push / seed:accounts scripts"
      contains: "supabase"
    - path: "supabase/config.toml"
      provides: "CLI project scaffold (from `supabase init`)"
  key_links:
    - from: "packages/app/src/db/supabase.ts"
      to: "import.meta.env.VITE_SUPABASE_URL / VITE_SUPABASE_ANON_KEY"
      via: "createClient args"
      pattern: "import\\.meta\\.env\\.VITE_SUPABASE_(URL|ANON_KEY)"
---

<objective>
Add the Supabase client dependency to the app layer (D-14), scaffold the root `supabase/` CLI project (D-13), and author the single app-layer client module that every downstream v2.0 phase (18 auth, 19 progress, 20 presence) imports (SETUP-04). Installs are gated behind a package-legitimacy checkpoint (T-17-SC).

Purpose: Stand up the app-side foundation and tooling. The client is isolated in `packages/app` by construction — `packages/core` never imports it (the guard test is 17-02).
Output: `@supabase/supabase-js` in the app package, `supabase` CLI as a root devDep with `db:push`/`seed:accounts` scripts, a scaffolded `supabase/` directory, and `packages/app/src/db/supabase.ts` exporting the client.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-backend-foundation-secrets/17-RESEARCH.md
@.planning/phases/17-backend-foundation-secrets/17-PATTERNS.md
@.claude/skills/spike-findings-guezzer/references/multi-user-supabase.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package-legitimacy gate for @supabase/supabase-js + supabase CLI (T-17-SC)</name>
  <read_first>
    - .planning/phases/17-backend-foundation-secrets/17-RESEARCH.md §"Package Legitimacy Audit" (both packages Approved — first-party; slopcheck was unavailable at research time, so per protocol they are treated as [ASSUMED] and verified here before install)
  </read_first>
  <what-built>Nothing yet — this gate precedes the first `npm install` of external packages.</what-built>
  <action>Pause for the owner to verify both packages on npmjs.com before any install runs (T-17-SC). No files change here; this gate precedes Task 2's installs.</action>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/@supabase/supabase-js — confirm it is the official Supabase org package, current version 2.110.8, repository github.com/supabase/supabase-js.
    2. Open https://www.npmjs.com/package/supabase — confirm it is the official Supabase CLI, current version ~2.109.1, repository github.com/supabase/cli.
    3. Confirm neither is a typosquat and both resolve to the `supabase` GitHub org.
  </how-to-verify>
  <acceptance_criteria>
    - Owner has visually confirmed both packages are first-party official Supabase packages on npmjs.com.
    - Note: the `supabase` CLI package downloads a platform binary on install (expected for a CLI wrapper); `@supabase/supabase-js` has no unexpected postinstall.
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed with installs, or describe concerns.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install deps, scaffold supabase/ CLI project, add root npm scripts</name>
  <files>package.json, packages/app/package.json, supabase/config.toml, supabase/.gitignore</files>
  <read_first>
    - package.json (root — existing scripts `refresh`/`build:archive`/`build:albums` use the `node <path>.ts` idiom; devDependencies block)
    - packages/app/package.json (existing dependencies block; note NO @supabase yet)
    - packages/relay/package.json (precedent for a CLI run via npx — `npx wrangler`)
    - .planning/phases/17-backend-foundation-secrets/17-RESEARCH.md §"Installation" + §"Provisioning + deploy runbook"
  </read_first>
  <action>
    Install the app client dependency pinned exactly: `npm install -w @guezzer/app @supabase/supabase-js@2.110.8`. Install the CLI as a ROOT devDependency (NOT `-g`, unsupported on Windows; NOT pnpm/corepack — this repo is npm workspaces): `npm install -D supabase@2.109.1`. Then scaffold the CLI project: `npx supabase init` (declining optional editor-settings prompts) — this creates `supabase/config.toml` and `supabase/.gitignore` (the latter ignores `.branches/.temp/`; do NOT hand-edit it). Add two root scripts to package.json "scripts" (names per discretion): `"db:push": "supabase db push --linked"` and `"seed:accounts": "node --env-file=.env supabase/seed/seed-users.ts"`. Do NOT add `@supabase/*` to packages/core (D-14 — enforced by 17-02). Do NOT run `supabase link`/`db push` here (needs the live project — that is 17-04).
  </action>
  <verify>
    <automated>cd "C:/Users/mattf/git/guezzer" && grep -q '"@supabase/supabase-js": "2.110.8"' packages/app/package.json && grep -q '"supabase": "2.109.1"' package.json && grep -q '"db:push"' package.json && grep -q '"seed:accounts"' package.json && test -f supabase/config.toml && npx supabase --version</automated>
  </verify>
  <acceptance_criteria>
    - `packages/app/package.json` dependencies contains `@supabase/supabase-js` at `2.110.8`.
    - root `package.json` devDependencies contains `supabase` at `2.109.1`; scripts contain `db:push` and `seed:accounts`.
    - `supabase/config.toml` and `supabase/.gitignore` exist (from `npx supabase init`).
    - `packages/core/package.json` still lists only `fuse.js` + `zod` in dependencies (no `@supabase/*`).
    - `npx supabase --version` prints a version (CLI binary resolved).
  </acceptance_criteria>
  <done>App has the pinned client dep, the CLI is runnable via npx, supabase/ is scaffolded, and root scripts exist.</done>
</task>

<task type="auto">
  <name>Task 3: Author the app-layer Supabase client module (D-14)</name>
  <files>packages/app/src/db/supabase.ts</files>
  <read_first>
    - packages/app/src/db/db.ts (module-singleton + config-import idiom; feature-folder placement `src/db/`)
    - packages/app/src/vite-env.d.ts (already references `vite/client`, so `import.meta.env.VITE_*` is typed)
    - .planning/phases/17-backend-foundation-secrets/17-RESEARCH.md §"Pattern 3: The app-layer client" (exact body)
    - .claude/skills/spike-findings-guezzer/references/multi-user-supabase.md §"1. Client"
  </read_first>
  <action>
    Create `packages/app/src/db/supabase.ts` as the ONE place `createClient` is called (D-14). Import `createClient` from `@supabase/supabase-js` and export a module-level singleton `supabase = createClient(import.meta.env.VITE_SUPABASE_URL, import.meta.env.VITE_SUPABASE_ANON_KEY)`. Read ONLY `VITE_`-prefixed vars via `import.meta.env` (D-09) — never `process.env`, never any non-VITE secret (D-10). Keep the client minimal (no custom auth options this phase — offline-session tuning is Phase 18). The `VITE_` pair is loaded from `packages/app/.env.local` (Vite loads env from the app package root; RESEARCH Pitfall 5) — do not change `envDir`. Add a one-line comment noting this file is the sole `createClient` site (the 17-02 purity test asserts it never appears under `packages/core`).
  </action>
  <verify>
    <automated>cd "C:/Users/mattf/git/guezzer" && grep -q "createClient" packages/app/src/db/supabase.ts && grep -q "import.meta.env.VITE_SUPABASE_URL" packages/app/src/db/supabase.ts && grep -q "import.meta.env.VITE_SUPABASE_ANON_KEY" packages/app/src/db/supabase.ts && ! grep -q "process.env" packages/app/src/db/supabase.ts && cd packages/app && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `packages/app/src/db/supabase.ts` exports `supabase` built from `createClient` using `import.meta.env.VITE_SUPABASE_URL` and `import.meta.env.VITE_SUPABASE_ANON_KEY`.
    - The module references NO `process.env` and NO non-`VITE_` variable name.
    - App typecheck (`npx tsc --noEmit` in packages/app) passes — the `@supabase/supabase-js` import resolves.
  </acceptance_criteria>
  <done>The single app-layer client module exists, typechecks, and reads only public VITE_ vars.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 17-01)
- `packages/app/src/db/supabase.ts` — exported `supabase` client (the ONLY `createClient` call).
- `packages/app/package.json` — new dependency `@supabase/supabase-js@2.110.8` (app-layer only).
- `package.json` (root) — new devDependency `supabase@2.109.1`; new scripts `db:push`, `seed:accounts`.
- `supabase/config.toml` + `supabase/.gitignore` — CLI project scaffold from `supabase init`.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → repo build | Third-party package code enters the tree at install |
| app bundle → Supabase | Anon-keyed client crosses to the hosted backend (public JWT) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | `@supabase/supabase-js`, `supabase` CLI installs | mitigate | Blocking-human legitimacy checkpoint (Task 1) verifying both on npmjs.com; versions pinned exactly in the lockfile |
| T-17-02 | Information Disclosure | `packages/app/src/db/supabase.ts` | mitigate | Client reads ONLY `VITE_` anon key + URL (public by design, D-09); grep asserts no `process.env` / non-VITE secret referenced (D-10) |
| T-17-SC | Tampering | npm installs | mitigate | slopcheck unavailable at research → treated as [ASSUMED]; blocking-human checkpoint (Task 1) verifies first-party provenance before install |
</threat_model>

<verification>
- `npx supabase --version` resolves the CLI binary.
- `packages/app` typecheck passes with the new client module.
- `packages/core/package.json` has no `@supabase/*` dependency.
</verification>

<success_criteria>
- App layer has the pinned Supabase client dep and the single client module; CLI + root scripts exist; core is untouched.
</success_criteria>

<output>
Create `.planning/phases/17-backend-foundation-secrets/17-01-SUMMARY.md` when done.
</output>
