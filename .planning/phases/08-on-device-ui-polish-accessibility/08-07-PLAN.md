---
phase: 08-on-device-ui-polish-accessibility
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/STATE.md
autonomous: true
requirements: [POLISH-02]
must_haves:
  truths:
    - "The D-20 FabMenu speed-dial behaves per its originating Phase-6 todo (five-callback contract, ≥44px targets), proven by the existing fabMenu test"
    - "The D-22 once-per-version InstallBanner behaves per its originating todo (shows once per build via the persisted version stamp, session dismissal layered on top), proven by the existing installBannerVersion test"
    - "Both POLISH-02 todos are formally marked resolved in STATE.md Deferred Items"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Deferred Items rows for the two POLISH-02 todos flipped to resolved"
      contains: "POLISH-02"
  key_links:
    - from: "packages/app/test/fabMenu.test.tsx"
      to: "packages/app/src/show/FabMenu.tsx"
      via: "existing automated coverage of the speed-dial contract"
      pattern: "FabMenu"
---

<objective>
Close POLISH-02: verify the D-20 FabMenu speed-dial and the D-22 once-per-version InstallBanner behave as their originating Phase-6 todos intended — using the automated coverage that already exists — then formally move those two todos to resolved. This is confirm-and-file, not a design change.

Purpose: The two Phase-6 behaviors are already shipped; POLISH-02 is the formal verification + bookkeeping gate.
Output: A verification record + STATE.md Deferred Items updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-on-device-ui-polish-accessibility/08-CONTEXT.md
@.planning/phases/08-on-device-ui-polish-accessibility/08-UI-SPEC.md
@.planning/phases/06-pok-dex-history-stats/06-02-PLAN.md
</context>

<artifacts_produced>
## Artifacts this phase produces (created/changed by THIS plan)

- A POLISH-02 verification record (in the plan SUMMARY) confirming the D-20 FabMenu speed-dial + D-22 once-per-version InstallBanner behaviors against their originating todos.
- STATE.md Deferred Items: the two POLISH-02 rows (`collapse Show-Mode actions into FAB menu`, `InstallBanner should show once per version`) marked resolved.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Verify D-20 FabMenu speed-dial + D-22 InstallBanner behaviors against their todos</name>
  <read_first>
    - .planning/phases/06-pok-dex-history-stats/06-02-PLAN.md (D-20 FabMenu speed-dial + D-22 InstallBanner-once-per-version originating plan)
    - .planning/todos/completed/2026-07-14-collapse-show-actions-into-fab-menu.md (D-20 originating todo — already in completed/)
    - .planning/todos/completed/2026-07-14-install-banner-reappears-every-reload.md (D-22 originating todo — already in completed/)
    - packages/app/test/fabMenu.test.tsx and packages/app/test/installBannerVersion.test.tsx (the existing automated coverage)
    - packages/app/src/show/FabMenu.tsx, packages/app/src/components/InstallBanner.tsx (current behavior — the same-five-callback contract + `installBannerSeenVersion` meta stamp)
  </read_first>
  <action>
    Run the two existing test files and read the originating todo + 06-02 plan to confirm the shipped behavior matches intent: FabMenu collapses the Show-Mode actions into a speed-dial preserving the five-callback contract (D-20/D-13..D-15) with ≥44px targets; InstallBanner shows once per build via the persisted `installBannerSeenVersion` stamp with session dismissal layered on top (D-22, supersedes D-05). Record the pass/behavior mapping in the SUMMARY. Do NOT modify FabMenu or InstallBanner source (verification only — any z-literal on FabMenu is handled by plan 05).
  </action>
  <verify>
    <automated>pnpm vitest run packages/app/test/fabMenu.test.tsx packages/app/test/installBannerVersion.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Both test files pass, exercising the speed-dial five-callback contract and the once-per-version banner stamp.
    - The SUMMARY records the behavior→todo mapping (which assertion proves which todo intent).
    - No source edit to FabMenu.tsx or InstallBanner.tsx in this plan.
  </acceptance_criteria>
  <done>Both Phase-6 behaviors are confirmed to match their originating todos via existing automated coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Formally move the two POLISH-02 todos to resolved</name>
  <read_first>
    - .planning/STATE.md (Deferred Items table — the two POLISH-02 rows currently `pending`: `collapse Show-Mode actions into FAB menu` and `InstallBanner should show once per version`)
    - .planning/todos/completed/ (confirm both todo files already live here)
  </read_first>
  <action>
    In STATE.md Deferred Items, change the Status of the two POLISH-02 rows from `pending` to `resolved` (noting: verified in Phase 8 / POLISH-02, both originating todo files already in `todos/completed/`). Do not alter unrelated rows. If either todo file is somehow still in `todos/pending/`, move it to `todos/completed/`.
  </action>
  <verify>
    <automated>grep -c "resolved" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - The two POLISH-02 Deferred Items rows read `resolved` (not `pending`).
    - Both originating todo files are in `.planning/todos/completed/`.
    - No unrelated Deferred Items row changed.
  </acceptance_criteria>
  <done>POLISH-02 is formally closed: behaviors verified and todos filed resolved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Verification + planning-doc bookkeeping only; no code or runtime surface changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-17 | Tampering | STATE.md bookkeeping | accept | Planning-doc edit only; no runtime/security surface. Scoped to the two POLISH-02 rows. |
| T-08-SC | Tampering (supply chain) | npm installs | accept | No new packages; verification + docs only. |
</threat_model>

<verification>
- `pnpm vitest run packages/app/test/fabMenu.test.tsx packages/app/test/installBannerVersion.test.tsx` green.
- STATE.md Deferred Items: both POLISH-02 rows `resolved`.
</verification>

<success_criteria>
- D-20 FabMenu speed-dial + D-22 once-per-version InstallBanner verified against their originating todos (POLISH-02).
- Both todos formally moved to resolved.
</success_criteria>

<output>
Create `.planning/phases/08-on-device-ui-polish-accessibility/08-07-SUMMARY.md` when done
</output>
