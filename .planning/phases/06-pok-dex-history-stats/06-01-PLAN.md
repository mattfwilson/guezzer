---
phase: 06-pok-dex-history-stats
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/config.ts
  - packages/core/src/dex/archive-types.ts
  - packages/core/src/dex/archive.ts
  - packages/core/src/dex/albums.ts
  - packages/core/src/cli/build-archive.ts
  - packages/core/src/cli/build-albums.ts
  - packages/core/src/index.ts
  - packages/core/test/dex/archive-artifact.test.ts
  - packages/core/test/dex/albums.test.ts
  - data/normalized/archive.json
  - data/normalized/dex-albums.json
  - package.json
autonomous: true
requirements: [DEX-02, DEX-04, STAT-04]
must_haves:
  truths:
    - "A committed data/normalized/archive.json exists with schemaVersion 1, latestShowDate, a songId→name map, and 738 shows each carrying stable id, date, venue, city, and set-structured songId lists"
    - "A committed data/normalized/dex-albums.json exists whose card albums are the studio discography shelf (~25-30 cards from a config allowlist keyed by album_url) plus Covers and Miscellaneous buckets — never 147 raw albums"
    - "Every one of the 264 matrix catalog songs appears in exactly one album card or bucket, and album tracklists include never-played songs (the debut candidates, STAT-04)"
    - "Running npm run build:archive and npm run build:albums regenerates byte-identical artifacts from the committed inputs"
    - "A drift-guard test fails if any allowlisted album_url disappears from data/raw/albums.json"
  artifacts:
    - path: "data/normalized/archive.json"
      provides: "Compact offline archive (DEX-02 retro-mark substrate, ~141 KB)"
      contains: "latestShowDate"
    - path: "data/normalized/dex-albums.json"
      provides: "Album-shelf mapping artifact (D-04)"
      contains: "schemaVersion"
    - path: "packages/core/src/dex/albums.ts"
      provides: "deriveDexAlbums pure derivation"
      contains: "deriveDexAlbums"
    - path: "packages/core/src/dex/archive.ts"
      provides: "deriveArchive pure derivation"
      contains: "deriveArchive"
    - path: "packages/core/src/config.ts"
      provides: "config.dex block: RARITY_QUANTILES, RARITY_MIN_PLAYS, cardAlbumUrls allowlist, artifact paths"
      contains: "RARITY_QUANTILES"
  key_links:
    - from: "packages/core/src/cli/build-archive.ts"
      to: "packages/core/src/dex/archive.ts"
      via: "CLI wraps the pure fn (build-model idiom)"
      pattern: "deriveArchive"
    - from: "packages/core/src/dex/albums.ts"
      to: "data/raw/songs.json slug join"
      via: "song_url.replace('/song/','') slug matching (SCHEMA.md §11)"
      pattern: "replace\\([\"']/song/"
---

<objective>
Create the two build-time artifacts everything else in Phase 6 consumes: a compact show archive (`archive.json`) derived from the 4.86 MB corpus, and the album-shelf mapping (`dex-albums.json`) derived from the dirty 2,750-row albums.json — plus the core `config.dex` block (rarity thresholds per D-15, the card-album allowlist per D-04) that pins all Phase-6 tunables in the single config file.

This is the enabling substrate for the phase's user story: *As a show-goer, I want my collection browsable by album with honest per-song stats, so that my dex reflects what I actually saw.* Implements D-03 (build-time bundling pattern), D-04 (canonical studio album mapping + Covers/Miscellaneous buckets), and the data foundation for D-15 and STAT-04 (album tracklists include never-played songs — the debut candidates exist by construction).

Purpose: bundling raw corpus.json is out of the question (34× too large); the archive artifact is the offline retro-marking substrate (DEX-02) and the gap/last-played source (STAT-01 — the matrix has playCount but no dates). The album mapping cannot come from D-04's raw heuristic alone (RESEARCH Pitfalls 1-3: `islive=0` does NOT exclude recent live albums, earliest-date maps songs to singles, titles collide) — a config-resident allowlist keyed by `album_url` is required.
Output: two committed artifacts, two pure derivation modules, two CLIs, config.dex, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-pok-dex-history-stats/06-CONTEXT.md
@.planning/phases/06-pok-dex-history-stats/06-RESEARCH.md
@.planning/phases/06-pok-dex-history-stats/06-PATTERNS.md
@.planning/phases/06-pok-dex-history-stats/06-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: config.dex block + archive/albums artifact zod schemas</name>
  <files>packages/core/src/config.ts, packages/core/src/dex/archive-types.ts, packages/core/src/index.ts</files>
  <read_first>
    - packages/core/src/config.ts (the `search:` sub-object idiom at lines ~172-187 and the `[ASSUMED]`-annotated tunable doc-comment style, e.g. decayHalfLifeDays ~107-115; also existing `sentinelSongIds`, `corpusArtifactPath`, `matrixArtifactPath` keys)
    - packages/core/src/data-safety/export-schema.ts (z.strictObject row + `z.infer` type-export idiom; enum-pinned string unions)
    - data/raw/albums.json (spot-check 20-30 rows: album_url, album_title trailing-space duplicates, releasedate "(N)" suffixes, islive semantics)
    - docs/SCHEMA.md §11 (slug join, duplicate-title and multi-show-day documentation)
    - 06-RESEARCH.md §Code Examples "Compact archive artifact (measured shape)" and Pitfalls 1-4
  </read_first>
  <behavior>
    - `archiveArtifact.parse` accepts a fixture matching the RESEARCH measured shape: { schemaVersion: 1, latestShowDate: string, songs: Record<songId,string>, shows: [{ id, date, venue, city, state (nullable), country, sets: [{ n: "1"|"2"|"e", songs: number[] }] }] } and rejects an object with an extra top-level key (strictObject).
    - `dexAlbumsArtifact.parse` accepts { schemaVersion: 1, albums: [...], buckets: { covers: [...], miscellaneous: [...] } } and rejects a track row with a non-integer songId.
    - `config.dex.RARITY_QUANTILES` equals { legendary: 0.05, rare: 0.2, uncommon: 0.5 } and `config.dex.RARITY_MIN_PLAYS` equals 3 (UI-SPEC §Config defaults, D-15).
  </behavior>
  <action>
    Add a `dex` sub-object to `packages/core/src/config.ts` mirroring the `search:` idiom, with doc comments citing D-15/A5 and marking tuning values `[ASSUMED]`: `RARITY_QUANTILES: { legendary: 0.05, rare: 0.2, uncommon: 0.5 }` (corpus play-rate quantile cut-points; ≥0.50 = Common), `RARITY_MIN_PLAYS: 3` (below this a played song caps at Rare — guards tiny-sample fake Legendary, RESEARCH Pitfall 12), `archiveArtifactPath: "data/normalized/archive.json"`, `dexAlbumsArtifactPath: "data/normalized/dex-albums.json"`, `archiveSearch: { threshold, distance }` (copy the values from `config.search` as starting points for date/venue search), and `cardAlbumUrls: string[]` — the D-04 card-album allowlist keyed by `album_url` (populated in Task 2; declare it here as the config-resident home so no allowlist ever lives in derivation code). Create `packages/core/src/dex/archive-types.ts` with zod strictObject schemas + inferred types: `archiveSetSchema` ({ n: z.enum(["1","2","e"]), songs: z.array(z.number().int()) }), `archiveShowSchema` ({ id: z.number().int(), date: z.string(), venue: z.string(), city: z.string(), state: z.string().nullable(), country: z.string(), sets: z.array(archiveSetSchema) }), `archiveArtifact` ({ schemaVersion: z.literal(1), latestShowDate: z.string(), songs: z.record(z.string(), z.string()), shows: z.array(archiveShowSchema) }), plus `albumTrackSchema` ({ songId: z.number().int().nullable(), slug: z.string(), title: z.string(), position: z.number().int(), inMatrix: z.boolean() }), `dexAlbumSchema` ({ albumUrl: z.string(), title: z.string(), releaseDate: z.string(), tracks: z.array(albumTrackSchema) }), and `dexAlbumsArtifact` ({ schemaVersion: z.literal(1), albums: z.array(dexAlbumSchema), buckets: z.strictObject({ covers: z.array(albumTrackSchema), miscellaneous: z.array(albumTrackSchema) }) }). Export types `ArchiveArtifact`, `ArchiveShow`, `ArchiveSet`, `DexAlbumsArtifact`, `DexAlbum`, `AlbumTrack` via z.infer. Barrel-export the schemas and types from `packages/core/src/index.ts` in a new Phase-6 dex block. NOTE per D-04: `inMatrix: false` tracks are the STAT-04 debut candidates by construction (album track with no matrix node — RESEARCH Pitfall 4); `songId` stays nullable for album tracks absent from songs.json.
  </action>
  <verify>
    <automated>npx vitest run --project @guezzer/core test/dex/archive-artifact.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `config.dex.RARITY_QUANTILES.legendary === 0.05`, `.rare === 0.2`, `.uncommon === 0.5`, and `config.dex.RARITY_MIN_PLAYS === 3` asserted in a test.
    - `archiveArtifact.parse` round-trips a minimal fixture and throws on an extra top-level key (strictObject asserted in test).
    - `archiveSetSchema` rejects `n: "3"` (enum-pinned set vocabulary, matching db.ts SetNumber "1"|"2"|"e").
    - `packages/core/src/index.ts` re-exports `archiveArtifact`, `dexAlbumsArtifact`, and the inferred types.
    - `npx vitest run --project @guezzer/core test/dex/archive-artifact.test.ts` passes.
  </acceptance_criteria>
  <done>All Phase-6 core tunables live in config.dex; the two artifact shapes are strict zod schemas with inferred types shared by the CLIs and (later) the app loaders.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Album mapping derivation + build-albums CLI + committed dex-albums.json</name>
  <files>packages/core/src/dex/albums.ts, packages/core/src/cli/build-albums.ts, packages/core/src/config.ts, packages/core/test/dex/albums.test.ts, data/normalized/dex-albums.json, package.json</files>
  <read_first>
    - packages/core/src/model/matrix.ts (the pure-module shape: one top-level fn, Map-keyed accumulation, explicit sort comparators, config injected with default — signature at lines ~75-80)
    - packages/core/src/cli/build-model.ts (CLI idiom: exported run fn, isMain guard, deterministic provenance, stable 2-space JSON + trailing newline, formatted summary — lines ~70-155)
    - data/raw/albums.json and data/raw/songs.json (the real inputs — verify the join before writing code)
    - data/normalized/transition-matrix.json (nodes list — the 264-song catalog the mapping must fully cover)
    - 06-RESEARCH.md Pitfalls 1-3 (islive=0 insufficiency, singles-before-LPs, dirty joins) and Open Question 4 (Fishing For Fishies duplicate URL)
  </read_first>
  <behavior>
    - deriveDexAlbums maps a synthetic fixture (3 card albums, 1 single predating an LP, 1 "Live in ..." islive=0 album, 1 cover song, 1 unmatched original, 1 album track with no matrix node) to: the song landing on the LP not the single; the live album absent from cards; the cover in buckets.covers; the unmatched original in buckets.miscellaneous; the no-matrix-node track present with inMatrix false.
    - Album keying is by album_url (never album_title); releasedate values like "2020-09-29 (1)" parse via a /^\d{4}-\d{2}-\d{2}/ prefix match; album titles are trimmed.
    - Every matrix node songId appears exactly once across cards + buckets (full-coverage invariant).
    - A drift-guard test parses the real data/raw/albums.json and asserts every config.dex.cardAlbumUrls entry exists as an album_url.
  </behavior>
  <action>
    Create `packages/core/src/dex/albums.ts` exporting `deriveDexAlbums(albumRows, songRows, matrixNodes, cfg = config): DexAlbumsArtifact` — pure, zero I/O, matrix.ts shape. Algorithm (RESEARCH-verified): (1) filter albumRows to `artist_id === 1`; (2) key albums by `album_url`, trim `album_title`, parse `releasedate` with the `/^\d{4}-\d{2}-\d{2}/` prefix (rows with unparseable dates are skipped from card candidacy); (3) card albums = rows whose `album_url` is in `cfg.dex.cardAlbumUrls` (D-04's `islive=0` + earliest-date operates WITHIN this allowlist, honoring the decision's spirit while defeating Pitfalls 1-2); (4) join album tracks to songs via slug: `albumRow.song_url.replace("/song/", "")` ↔ songs.json `.slug`, giving songId; mark `inMatrix` by presence in matrixNodes (by songId — NEVER by name, duplicate names exist); (5) each matrix song maps to the EARLIEST-dated card album containing its slug; (6) matrix songs on no card album route to buckets: `isoriginal === 0` in songs.json → covers, else → miscellaneous; (7) card tracklists keep album `position` order and include tracks with no matrix node (`inMatrix: false` — the debut candidates, STAT-04); exclude `cfg.sentinelSongIds` everywhere; (8) sort albums alphabetically by title (D-02 default) and validate the result through `dexAlbumsArtifact.parse` before returning. Populate `config.dex.cardAlbumUrls` with the canonical studio discography allowlist derived from the real albums.json: start from `islive === 0` albums with ≥4 joined tracks, then curate to the known studio LPs/EPs (12 Bar Bruise, Eyes Like the Sky, Float Along – Fill Your Lungs, Oddments, I'm in Your Mind Fuzz, Quarters!, Paper Mâché Dream Balloon, Nonagon Infinity, Flying Microtonal Banana, Murder of the Universe, Sketches of Brunswick East, Polygondwanaland, Gumboot Soup, Fishing For Fishies, Infest the Rats' Nest, K.G., L.W., Butterfly 3000, Made In Timeland, Omnium Gatherum, Ice Death Planets Lungs Mushrooms and Lava, Laminated Denim, Changes, PetroDragonic Apocalypse, The Silver Cord, Flight b741, Phantom Island, plus Willoughby's Beach EP and Teenage Gizzard per RESEARCH A4), verifying each album_url against the actual file; pin `/albums/fishing-for-fishies` (NOT the `-video` duplicate); exclude demos, singles, promos, and every "Live in/at ..." title. The final list is ~25-30 entries (assumption A4 — surfaced at the end-of-phase human gate; cosmetic and config-editable). Create `packages/core/src/cli/build-albums.ts` mirroring build-model.ts: exported `runBuildAlbums(options)` reading `data/raw/albums.json` + `data/raw/songs.json` + the matrix artifact, calling the pure fn, writing `cfg.dex.dexAlbumsArtifactPath` with stable 2-space JSON + trailing newline; summary printout MUST report: card count, per-bucket sizes, matrix-coverage count (joined/264), and unmatched count (expect ~31 = covers + misc, RESEARCH-verified). Add root package.json script `"build:albums": "node packages/core/src/cli/build-albums.ts"`. Run it and commit `data/normalized/dex-albums.json`.
  </action>
  <verify>
    <automated>npx vitest run --project @guezzer/core test/dex/albums.test.ts && npm run build:albums</automated>
  </verify>
  <acceptance_criteria>
    - Synthetic-fixture tests pass for: single-vs-LP earliest-card mapping, live-album exclusion, covers bucket via isoriginal 0, miscellaneous bucket, inMatrix false debut candidate, sentinel exclusion.
    - Drift-guard test against the real albums.json passes: every `config.dex.cardAlbumUrls` entry exists as an `album_url`; the test also asserts the card count is between 20 and 35 (never 147 — UI-SPEC grid-membership rule).
    - Full-coverage test against real data: every matrix node songId appears exactly once across cards + buckets.
    - `npm run build:albums` writes data/normalized/dex-albums.json; a second run produces zero git diff (deterministic).
    - `data/normalized/dex-albums.json` is committed and passes `dexAlbumsArtifact.parse` in a test.
  </acceptance_criteria>
  <done>The album shelf artifact is committed: studio-discography cards + Covers/Miscellaneous buckets, every catchable song mapped exactly once, debut candidates present by construction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Archive derivation + build-archive CLI + committed archive.json</name>
  <files>packages/core/src/dex/archive.ts, packages/core/src/cli/build-archive.ts, packages/core/test/dex/archive-artifact.test.ts, data/normalized/archive.json, package.json</files>
  <read_first>
    - data/normalized/corpus.json (input shape: read the first show object fully — show id/date/venue fields and setlist/sets structure, song ids, set numbers)
    - packages/core/src/cli/build-model.ts (CLI idiom to mirror — see Task 2)
    - packages/core/src/cli/refresh.ts (the one-command refresh pipeline — where the new builds chain in)
    - 06-RESEARCH.md §Code Examples "Compact archive artifact (measured shape)" (~141 KB target; latestShowDate 2025-12-13 boundary)
  </read_first>
  <behavior>
    - deriveArchive transforms a 2-show synthetic corpus fixture into { schemaVersion: 1, latestShowDate: max show date, songs: songId→name map, shows: [...] } with shows sorted newest-first and each show's sets preserving set structure (n: "1"|"2"|"e") and song order.
    - Placeholder/sentinel song ids (config.sentinelSongIds) are excluded from set song lists and the songs map.
    - The output validates through archiveArtifact.parse.
  </behavior>
  <action>
    Create `packages/core/src/dex/archive.ts` exporting `deriveArchive(corpus, cfg = config): ArchiveArtifact` — pure transform of the normalized corpus into the RESEARCH measured shape: `latestShowDate` = the maximum show date (the online-fallback boundary consumed by the ArchiveBrowser in plan 06-08), `songs` = songId→songName record built from all performances (sentinels excluded), `shows` sorted date-descending each with { id: stable show_id, date, venue, city, state, country, sets: [{ n, songs: songId[] }] } preserving corpus set structure and in-set ordering (HIST-01 needs set structure for retro setlist views). Derive field names by reading the actual corpus.json show shape — do not guess; the normalizer's domain types in packages/core/src/domain/types.ts are the authority. Create `packages/core/src/cli/build-archive.ts` mirroring build-model.ts (exported `runBuildArchive`, isMain guard, deterministic output reusing the corpus's own generatedAt for provenance, stable 2-space JSON + newline, summary printing show count / song count / latestShowDate / output size in KB with a fail-loud guard if the file exceeds 250 KB — the A6 bundle-budget tripwire). Add root package.json script `"build:archive": "node packages/core/src/cli/build-archive.ts"` and chain both new builds into the existing refresh pipeline (read refresh.ts first; append build-archive and build-albums steps after the corpus/matrix builds so `npm run refresh` regenerates everything). Run it and commit `data/normalized/archive.json`. Extend `packages/core/test/dex/archive-artifact.test.ts` with the deriveArchive fixture tests plus a real-artifact test: parse the committed archive.json through archiveArtifact, assert shows.length === 738 and latestShowDate matches the corpus.
  </action>
  <verify>
    <automated>npx vitest run --project @guezzer/core test/dex/archive-artifact.test.ts && npm run build:archive</automated>
  </verify>
  <acceptance_criteria>
    - Fixture test: newest-first ordering, set structure preserved, sentinel exclusion, latestShowDate correctness all asserted.
    - `data/normalized/archive.json` is committed, parses through `archiveArtifact`, contains 738 shows, and is under 250 KB.
    - `npm run build:archive` twice produces zero git diff (deterministic).
    - `npm run refresh` includes the two new build steps (verified by reading the updated refresh.ts).
    - Full core suite green: `npx vitest run --project @guezzer/core`.
  </acceptance_criteria>
  <done>The compact offline archive is committed and regenerable by one command — the DEX-02 retro-marking and STAT-01 gap/last-played substrate exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kglw.net-sourced data files → committed artifacts | Untrusted strings (venue names, song names, album_notes HTML) flow from data/raw into bundled artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Injection (stored XSS) | albums.json `album_notes` HTML | mitigate | deriveDexAlbums NEVER copies album_notes (or any HTML-bearing field) into the artifact — only album_url, trimmed title, date, track slug/title/position (RESEARCH §Security); artifact schemas are strictObject so a leaked field fails parse |
| T-06-02 | Tampering | artifact shape drift | mitigate | Both CLIs validate output through the zod artifact schemas before writing; app loaders (plan 06-05) re-guard schemaVersion at runtime |
| T-06-03 | Information disclosure | committed artifacts | accept | Artifacts contain only public setlist data from kglw.net; no personal data at build time |
| T-06-SC | Tampering (supply chain) | package installs | mitigate | No new dependencies in this plan (sharp lands in 06-04, slopcheck [OK]) |
</threat_model>

<verification>
- `npx vitest run --project @guezzer/core` green.
- `npm run build:archive && npm run build:albums` → zero git diff on second run.
- `git diff --stat` shows both artifacts committed under data/normalized/.
</verification>

<success_criteria>
- DEX-02 substrate: 738 shows browsable offline from a ~141 KB artifact keyed by stable show_id.
- DEX-04/D-04: album shelf artifact = studio discography cards + Covers/Miscellaneous, every matrix song mapped exactly once.
- STAT-04: debut candidates exist by construction (inMatrix false album tracks).
- D-15: rarity thresholds are config-resident constants, not scattered.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

New symbols (exclude from source-drift verification — created here):
- `config.dex` block: `RARITY_QUANTILES`, `RARITY_MIN_PLAYS`, `archiveArtifactPath`, `dexAlbumsArtifactPath`, `archiveSearch`, `cardAlbumUrls` — `packages/core/src/config.ts`
- `archiveArtifact`, `archiveShowSchema`, `archiveSetSchema`, `dexAlbumsArtifact`, `dexAlbumSchema`, `albumTrackSchema` (zod) + types `ArchiveArtifact`, `ArchiveShow`, `ArchiveSet`, `DexAlbumsArtifact`, `DexAlbum`, `AlbumTrack` — `packages/core/src/dex/archive-types.ts`
- `deriveDexAlbums` — `packages/core/src/dex/albums.ts`
- `deriveArchive` — `packages/core/src/dex/archive.ts`
- `runBuildAlbums` — `packages/core/src/cli/build-albums.ts`; `runBuildArchive` — `packages/core/src/cli/build-archive.ts`
- npm scripts `build:albums`, `build:archive` — root `package.json`
- Committed artifacts: `data/normalized/archive.json`, `data/normalized/dex-albums.json`
- New tests: `packages/core/test/dex/archive-artifact.test.ts`, `packages/core/test/dex/albums.test.ts`
</artifacts_produced>

<output>
Create `.planning/phases/06-pok-dex-history-stats/06-01-SUMMARY.md` when done
</output>
