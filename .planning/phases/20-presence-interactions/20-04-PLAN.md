---
phase: 20-presence-interactions
plan: 04
type: execute
wave: 4
depends_on: [20-01, 20-02, 20-03]
files_modified:
  - packages/app/src/dex/FriendRow.tsx
  - packages/app/src/dex/SelfRow.tsx
  - packages/app/src/dex/FriendsList.tsx
  - packages/app/src/dex/FriendDetail.tsx
  - packages/app/test/dex/friendPresence.test.tsx
autonomous: true
requirements: [PRES-01, PRES-04, PRES-05, PRES-07]

must_haves:
  truths:
    - "An online friend's row shows an 8px green dot in the reserved presence-online slot and their coarse activity label (or `At a show 🎸`) in the reserved presence-activity slot"
    - "The pinned You row shows its own live dot + activity, and reads `offline` with the dot hidden when the viewer is offline (D-15/17)"
    - "When the viewer is offline, ALL friend online dots + activity labels are hidden while the dimmed cached PROG rows still render (D-16)"
    - "Presence decorates existing PROG rows only — no online-only placeholder rows are added (D-13)"
    - "FriendsList has a React/wave entry that opens the ReactionPalette (target Everyone default); FriendDetail has a Wave-at-{name} button pre-targeted at that friend — both share the one sendWave path (D-04/07)"
    - "Decision provenance: a wave can also be sent from inside FriendDetail, pre-targeted at that friend, sharing the single sendWave path (D-07)"
  artifacts:
    - path: "packages/app/src/dex/FriendRow.tsx"
      provides: "Reserved presence slots filled from props (online dot + activity label)"
      contains: "presence-online"
    - path: "packages/app/src/dex/SelfRow.tsx"
      provides: "Own live dot + activity/offline via useSelfPresence"
      contains: "useSelfPresence"
    - path: "packages/app/src/dex/FriendsList.tsx"
      provides: "Per-row presence via usePresenceFor + React/wave palette entry"
      contains: "ReactionPalette"
    - path: "packages/app/src/dex/FriendDetail.tsx"
      provides: "Pre-targeted Wave-at-{name} button opening the palette"
      contains: "waveAtFriend"
  key_links:
    - from: "packages/app/src/dex/FriendsList.tsx"
      to: "packages/app/src/sync/usePresenceReaders.ts"
      via: "usePresenceFor(friend.userId) per row"
      pattern: "usePresenceFor"
    - from: "packages/app/src/dex/FriendRow.tsx"
      to: "config.copy.presence.atShow"
      via: "activity label rendering (At a show emphasis)"
      pattern: "presence"
    - from: "packages/app/src/dex/FriendDetail.tsx"
      to: "packages/app/src/dex/ReactionPalette.tsx"
      via: "pre-targeted wave button opens the shared palette"
      pattern: "ReactionPalette"
---

<objective>
Fuse presence into the Friends screen: fill the already-reserved `FriendRow`/`SelfRow` presence slots (online dot + coarse activity label) from the pure presence readers, honor the offline dot-hide + self-offline rules, add the `ReactionPalette` entry control on the Friends header, and add the pre-targeted `Wave at {name}` button in `FriendDetail`. This is PRES-07 — the hardest fusion constraint, already pre-scaffolded by Phase 19's empty slots.

Purpose: deliver the visible payoff — live online dots + "what they're doing" (incl. the residency-defining `At a show 🎸`) on real friend rows, and the two send entry points — with zero new backend and zero row rebuild (D-13).
Output: edits to `FriendRow.tsx`, `SelfRow.tsx`, `FriendsList.tsx`, `FriendDetail.tsx`, and `friendPresence.test.tsx` (PRES-07 slot-fill + offline-hide).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-presence-interactions/20-CONTEXT.md
@.planning/phases/20-presence-interactions/20-RESEARCH.md
@.planning/phases/20-presence-interactions/20-PATTERNS.md
@.planning/phases/20-presence-interactions/20-UI-SPEC.md
@.planning/phases/20-presence-interactions/20-03-PLAN.md
</context>

## Artifacts this plan produces

- **Files (edited):** `FriendRow.tsx`, `SelfRow.tsx`, `FriendsList.tsx`, `FriendDetail.tsx`; **new test:** `packages/app/test/dex/friendPresence.test.tsx`.
- **FriendRow.tsx:** accepts `online: boolean` + `activity: Activity | null` props, fills the reserved `presence-online` (8px `#22C55E` dot) + `presence-activity` (activity label) slots.
- **SelfRow.tsx:** consumes `useSelfPresence()` to fill its own slots (dot + activity, or hidden dot + `offline`).
- **FriendsList.tsx:** wraps each row with `usePresenceFor(friend.userId)`; adds a `React`/wave header affordance opening `ReactionPalette` (target Everyone default, `friends` = current rows).
- **FriendDetail.tsx:** adds a `Wave at {name}` button opening `ReactionPalette` pre-targeted at `friend.userId`.
- **Consumes:** `usePresenceFor`/`useSelfPresence` (20-03), `ReactionPalette` (20-02), `config.copy.presence` + `config.presence` (20-01), shipped `SyncDot` green `#22C55E`, `IdentityGlyph`, `useOnlineStatus`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fill FriendRow + SelfRow presence slots; wire per-row presence in FriendsList (offline-aware)</name>
  <files>packages/app/src/dex/FriendRow.tsx, packages/app/src/dex/SelfRow.tsx, packages/app/src/dex/FriendsList.tsx, packages/app/test/dex/friendPresence.test.tsx</files>
  <read_first>
    - packages/app/src/dex/FriendRow.tsx (the ENTIRE file — the reserved `presence-online` slot `:96-99` and `presence-activity` slot `:115-117` to fill; keep the row a pure props consumer, NO channel logic — D-13/D-19)
    - packages/app/src/dex/SelfRow.tsx (the ENTIRE file — the reserved slots `:47-48` and `:63`; it is LOCAL-sourced, never the Supabase read path — fill from useSelfPresence)
    - packages/app/src/dex/FriendsList.tsx (the ENTIRE file — the row map `:76-88` to wrap with per-row presence; the offline marker + dimmed rows `:54-59`,`:84` stay unchanged — D-16/18)
    - packages/app/src/live/SyncDot.tsx:50-60 (the shipped online-green `#22C55E` — reuse this exact hue for the 8px dot; do NOT route through --color-accent)
    - packages/app/src/sync/usePresenceReaders.ts (from 20-03 — usePresenceFor/useSelfPresence)
    - packages/app/src/sync/presenceActivity.ts (from 20-01 — the `Activity`/`Tab` types; Tab tokens ARE the display labels)
    - packages/app/src/config.ts (`config.copy.presence.atShow`/`offline`)
    - packages/app/test/sync/progressSync.test.ts:13-59 (the Supabase-mock idiom if the test needs the store) and any existing packages/app/test/dex/*.test.tsx for the RTL/jsdom setup
    - .planning/phases/20-presence-interactions/20-PATTERNS.md:192-212 (FriendRow/SelfRow/FriendsList edits) and 20-UI-SPEC.md:101-110 (online-green data-semantic palette, dot never color-alone)
  </read_first>
  <behavior>
    - FriendRow given online=true renders an 8px `#22C55E` `rounded-full` dot in the presence-online slot; online=false renders nothing there
    - FriendRow given activity={tab:"GizzVerse"} renders `GizzVerse` (muted) in the presence-activity slot; activity={tab:"LiveGizz",atShow:true} renders `At a show 🎸` with text-primary emphasis; activity=null renders nothing
    - The dot never conveys state by color alone — a present friend's row shows BOTH the dot and the text activity label (WCAG 1.4.1)
    - SelfRow with useSelfPresence()={online:true, activity} shows its own dot + label; ={online:false, activity:null} hides the dot and shows `offline`
    - In FriendsList, when the viewer is offline, EVERY friend row shows no dot + no activity label (usePresenceFor returns offline shape) while the dimmed cached PROG rows + `Offline · as of {time}` marker still render (D-16)
    - No online-only placeholder rows are ever added — list membership stays PROG-owned (D-13)
  </behavior>
  <action>
    `FriendRow.tsx`: add `online: boolean` and `activity: Activity | null` to `FriendRowProps`. Fill the leading `presence-online` slot with an 8px dot (`h-2 w-2 rounded-full`, `backgroundColor: "#22C55E"` — the shipped SyncDot online-green) rendered only when `online`; drop the `aria-hidden` from the slot when it carries the meaningful dot but keep the row's accessible name intact. Fill the trailing `presence-activity` slot with the activity label: `activity == null` → nothing; `activity.atShow` → `config.copy.presence.atShow` in `text-text-primary`; else the `activity.tab` token (brand label) in `text-[13px] leading-tight text-text-muted`. The row stays a PURE props consumer — no presence channel/store logic here.

    `SelfRow.tsx`: call `useSelfPresence()`; fill its reserved slots the same way — dot when `online`, and the activity label when online, else `config.copy.presence.offline` (muted) with the dot hidden (D-15/17). Keep SelfRow otherwise local-sourced and never-dimmed.

    `FriendsList.tsx`: for each mapped friend, read `usePresenceFor(friend.userId)` and pass `online`/`activity` into `<FriendRow>`. Because `usePresenceFor` already returns the offline shape when the viewer is offline, no extra offline branch is needed here (D-16) — the existing dimmed-rows + offline marker path is untouched. Extract the per-row presence read into a tiny child component (e.g. `PresenceFriendRow`) so the hook is called per row legally (rules of hooks). Do NOT add placeholder rows for online-only friends (D-13).

    Write `friendPresence.test.tsx` (jsdom + RTL, seed the presence store via `setPresenceState` from presenceSync): cover the full `<behavior>` table — dot render on online, activity-label variants incl. `At a show 🎸` emphasis, SelfRow offline reads `offline`, and the viewer-offline case hides all friend dots while dimmed rows persist (PRES-07/PRES-01/PRES-04, D-16).
  </action>
  <acceptance_criteria>
    - Source assertion: `FriendRow.tsx` renders `#22C55E` in the `presence-online` slot gated on `online`, and `config.copy.presence.atShow` for the atShow activity.
    - Source assertion: `SelfRow.tsx` calls `useSelfPresence()` and renders `config.copy.presence.offline` when offline.
    - Source assertion: `FriendsList.tsx` reads `usePresenceFor` per row and adds NO online-only placeholder rows (row source is still `buildFriendRows`).
    - Behavior assertion: every row of the `<behavior>` table passes in `friendPresence.test.tsx`, including the viewer-offline hide-all-dots-but-keep-dimmed-rows case.
    - Test command: `npm test -- --run packages/app/test/dex/friendPresence.test.tsx` green.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run packages/app/test/dex/friendPresence.test.tsx</automated>
  </verify>
  <done>FriendRow/SelfRow reserved slots are filled (dot + activity, At-a-show emphasis, self offline), FriendsList wires per-row presence with no placeholder rows, offline hides all friend dots while dimmed cached rows persist, and friendPresence.test.tsx is green (PRES-07/01/04, D-13/15/16/17).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wave send entry points — FriendsList header palette + FriendDetail pre-targeted button</name>
  <files>packages/app/src/dex/FriendsList.tsx, packages/app/src/dex/FriendDetail.tsx</files>
  <read_first>
    - packages/app/src/dex/FriendsList.tsx (from Task 1 state — add the header React/wave affordance + palette open state; already imports the friend rows)
    - packages/app/src/dex/FriendDetail.tsx:68-85 (the header/button idiom to echo for the pre-targeted wave button; friend name already escaped/clamped `:57-58`)
    - packages/app/src/dex/ReactionPalette.tsx (from 20-02 — props `open`/`onClose`/`initialTarget`/`friends`; opens as a Sheet)
    - packages/app/src/config.ts (`config.copy.presence.waveEntry` / `waveAtFriend`)
    - packages/app/src/sync/friendCache.ts:28-33 (`FriendRowData` — the `friends` array the palette target-picker consumes)
    - .planning/phases/20-presence-interactions/20-PATTERNS.md:216-218 (FriendDetail edit) and 20-UI-SPEC.md:120-124,:145-147 (entry-point copy + ≥44px)
  </read_first>
  <behavior>
    - FriendsList shows a compact `React`/wave header affordance (≥44px, aria-label) that opens ReactionPalette with the current friend rows and target defaulting to Everyone (to:null)
    - FriendDetail shows a `Wave at {name}` button (≥44px) that opens ReactionPalette pre-targeted at friend.userId (initialTarget)
    - Both entry points route through the SAME ReactionPalette → the one shared sendWave(emoji, to) path (D-04/07)
    - Closing the palette leaves the Friends screen untouched (view-state only, no new hash route)
  </behavior>
  <action>
    `FriendsList.tsx`: add a compact header control labeled `config.copy.presence.waveEntry` (`React`), an icon-button (lucide) with `aria-label`, `min-h-11 min-w-11 touch-manipulation`, that toggles a `paletteOpen` state. Render `<ReactionPalette open={paletteOpen} onClose={...} initialTarget={null} friends={rows} />` (Everyone default). Keep the control on the Friends header row — a lightweight in-flow affordance, NOT a full-width accent CTA (UI-SPEC).

    `FriendDetail.tsx`: add a `Wave at {name}` button (echo the header button idiom `:68-85`, `min-h-11 min-w-11 touch-manipulation`, escaped `friendName`) using `config.copy.presence.waveAtFriend(friendName)`, opening `<ReactionPalette open onClose initialTarget={friend.userId} friends={[]} />` pre-targeted at that friend (the target is fixed to this friend; `friends` can be empty since the target is pre-set, or pass the single friend). Shares the one `sendWave` path. No new hash route — the palette is a Sheet over the existing overlay.
  </action>
  <acceptance_criteria>
    - Source assertion: `FriendsList.tsx` imports and renders `ReactionPalette` with `initialTarget={null}`; the entry control has an `aria-label` and `min-h-11 min-w-11`.
    - Source assertion: `FriendDetail.tsx` renders a `Wave at {name}` button using `config.copy.presence.waveAtFriend` and opens `ReactionPalette` with `initialTarget={friend.userId}`.
    - Source assertion: neither edit introduces a new hash route or a new z-tier (palette rides the shipped `Sheet`).
    - Typecheck: `npx tsc -p packages/app --noEmit` clean; `npm test -- --run packages/app/test/dex` green.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc -p packages/app --noEmit && npm test -- --run packages/app/test/dex</automated>
  </verify>
  <done>FriendsList has a React/wave header entry (Everyone default) and FriendDetail has a pre-targeted Wave-at-{name} button, both opening the shared ReactionPalette → one sendWave path; typecheck + dex tests green (PRES-02/05, D-04/07).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| presence store → friend rows | Per-user activity read from the store is rendered as a label; the `Tab` set is a closed vocabulary, so a malformed activity was already reduced/dropped upstream. |
| friend displayName → palette/detail | Names in the target picker + FriendDetail are Supabase-synced (untrusted) — escaped React text, clamped. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-06 | Tampering (XSS) | FriendRow activity label / palette target rows / FriendDetail button | mitigate | Activity labels come from the closed `Tab` set + `config.copy.presence` (no user text); friend names render as escaped React text, `truncate min-w-0` clamped — never `dangerouslySetInnerHTML`. |
| T-20-03 | Information Disclosure | activity label rendering | mitigate | Label is the coarse tab token or `At a show 🎸` (a boolean) — never a song/setlist position (D-03 scope line). |
| T-20-11 | Spoofing / stale-green lie | offline dot-hide (D-16/17) | mitigate | When the viewer is offline, `usePresenceFor`/`useSelfPresence` return the offline shape so NO green dot is shown — honest absence, never a stale "online now" lie; unit-tested. |
| T-20-12 | Access Control (V4) | FriendDetail pre-targeted send | mitigate | Targeted wave carries `to:friend.userId`; the receiver still enforces `to===null || to===me` (validateWave, 20-01) so a mis-target is ignored on receipt. |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies (RESEARCH audit = N/A). |
</threat_model>

<verification>
- `npm test -- --run packages/app/test/dex` green (friendPresence + existing dex tests)
- `npm test -- --run` full suite green (no regression across sync/components/core)
- `npx tsc -p packages/app --noEmit` clean
- `npm test -- --run packages/core/test/purity.test.ts` green (SETUP-04)
</verification>

<success_criteria>
- Friend rows show live online dots + coarse activity (incl. `At a show 🎸`); the You row shows its own dot/activity and reads `offline` when offline; offline hides all friend dots while dimmed cached rows persist (PRES-07/01/04, D-13/15/16/17).
- Two send entry points (Friends header palette + FriendDetail pre-targeted button) share the one `sendWave` path (PRES-02/05, D-04/07).
- No new backend, no new hash route, no new z-tier or color.
</success_criteria>

<output>
Create `.planning/phases/20-presence-interactions/20-04-SUMMARY.md` when done.
</output>
