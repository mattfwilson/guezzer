---
phase: 15-gizz-bingo-persistence-lock-replay
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/data-safety/export-schema.ts
  - packages/core/src/data-safety/serialize.ts
  - packages/core/src/data-safety/merge.ts
  - packages/core/test/merge.test.ts
autonomous: true
requirements: [BINGO-07]
must_haves:
  truths:
    - "A pre-v3 backup (envelope with no `bingoCards` key) still parses and imports — the field defaults to []"
    - "A v3 backup carrying bingoCards round-trips through serialize -> parse -> merge unchanged"
    - "On a same-cardId import collision, the LOCKED row survives (a locked card is never reverted to a draft)"
  artifacts:
    - path: "packages/core/src/data-safety/export-schema.ts"
      provides: "bingoCardRow z.strictObject + bingoCards envelope field (default [])"
      contains: "bingoCardRow"
    - path: "packages/core/src/data-safety/merge.ts"
      provides: "MIGRATIONS[2] + bingoCards union merge (locked-wins collision rule)"
      contains: "MIGRATIONS[2]"
    - path: "packages/core/src/data-safety/serialize.ts"
      provides: "bingoCards on ExportSnapshot + serializeExport passthrough"
      contains: "bingoCards"
  key_links:
    - from: "export-schema.ts bingoCardRow.card"
      to: "bingo/types.ts bingoCardSchema"
      via: "reuse the shipped discriminated-union card schema verbatim"
      pattern: "card:\\s*bingoCardSchema"
---

<objective>
Extend the versioned export envelope to carry Gizz-Bingo cards (envelope v3) so a
locked card round-trips through the JSON backup — the persistence half of BINGO-07
replay and roadmap SC-4. Pure core only: a new `bingoCardRow` strict-zod schema,
the `bingoCards` envelope field with a back-compat default, a `MIGRATIONS[2]`
normalizer, and a `bingoCards` union merge whose collision direction resolves the
D-13 contradiction explicitly.

Purpose: the backup is the iOS-eviction backstop (D-13/D-14); bingo cards must ride
inside it and survive import without corrupting or reverting a locked historical card.
Output: envelope-v3 core schema + merge, Node-testable, zero DOM, zero new dependency.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-gizz-bingo-persistence-lock-replay/15-CONTEXT.md
@.planning/phases/15-gizz-bingo-persistence-lock-replay/15-RESEARCH.md
@.planning/phases/15-gizz-bingo-persistence-lock-replay/15-PATTERNS.md
</context>

<artifacts_this_plan_produces>
Symbols this plan creates (excluded from downstream drift verification):
- `bingoCardRow` (exported zod `z.strictObject`, export-schema.ts) — nested `card: bingoCardSchema`, plus `cardId`, `sessionId`, `caughtSnapshot`, `lockedAt`, `showDate`, `venueName`, `city`.
- `bingoCards` field on `exportEnvelope` (`z.array(bingoCardRow).default([])`).
- `bingoCards` field on the `ExportSnapshot` interface (serialize.ts) + `serializeExport` passthrough.
- `MIGRATIONS[2]` entry (merge.ts).
- `mergedBingoCards` union-merge block + `bingoCards` on the returned `merged` object (merge.ts).
</artifacts_this_plan_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: bingoCardRow schema + envelope v3 field + serialize passthrough</name>
  <files>packages/core/src/data-safety/export-schema.ts, packages/core/src/data-safety/serialize.ts</files>
  <read_first>
    - packages/core/src/data-safety/export-schema.ts (the `archiveShowRow` strictObject at 46-62 and the `.default([])` envelope idiom at 122-131 — copy both)
    - packages/core/src/data-safety/serialize.ts (the `ExportSnapshot` interface 33-40 and the verbatim passthrough in `serializeExport` 51-65)
    - packages/core/src/bingo/types.ts (the shipped `bingoCardSchema` at 129 — a `z.discriminatedUnion("kind", …)`; reuse it verbatim as the nested `card`)
  </read_first>
  <behavior>
    - A valid v3 envelope object with a `bingoCards` array of well-formed rows PARSES.
    - A pre-v3 envelope object with NO `bingoCards` key PARSES and yields `bingoCards === []` (the `.default([])`).
    - A bingoCards row with an unknown `card.squares[].kind` or an extra top-level key HARD-FAILS (strictObject + reused discriminated union).
    - `serializeExport(snapshot, 3)` returns an envelope whose `bingoCards` equals `snapshot.bingoCards` verbatim (no mutation, no id-strip — `cardId` is stable, unlike trackedEntries' volatile `++id`).
  </behavior>
  <action>
    In export-schema.ts, add an exported `bingoCardRow = z.strictObject({ ... })` mirroring the `archiveShowRow` discipline (line 46). Import `bingoCardSchema` from `../bingo/types.ts` and nest it as `card: bingoCardSchema` (RESEARCH Pattern 2 — nesting lets the shipped discriminated-union validate the card verbatim, avoiding a re-declaration). Fields: `cardId: z.string()`, `sessionId: z.string()`, `card: bingoCardSchema`, `caughtSnapshot: z.array(z.number().int())`, `lockedAt: z.number().nullable()`, `showDate: z.string()`, `venueName: z.string().nullable()`, `city: z.string().nullable()`. `caughtSnapshot` is REQUIRED (RESEARCH Pitfall 1 — CONTEXT D-11's field list omits it, but D-08/D-12 freeze it; without it `neverCaught` drifts on replay). Add to the `exportEnvelope` strictObject the field `bingoCards: z.array(bingoCardRow).default([])` (copy the `archiveShows: z.array(archiveShowRow).default([])` back-compat pattern at line 128) so a genuine v2 backup lacking the key still parses. In serialize.ts, add `bingoCards: z.infer<typeof bingoCardRow>[]` to the `ExportSnapshot` interface (import the row type alongside the others at 13-20) and pass it through verbatim in `serializeExport` (like `archiveShows` at line 62 — NO map/strip). Do NOT widen any enum to `z.string()` — the inferred row type must stay assignable to the app `Table<BingoCardRow>` at `db.importSnapshot` (the tsc cross-boundary contract, export-schema.ts:16-24). Serves D-11/D-13/D-14.
  </action>
  <verify>
    <automated>npx vitest run packages/core/test/merge.test.ts</automated>
  </verify>
  <done>bingoCardRow exported; `bingoCards` on envelope (default []) and on ExportSnapshot + serializeExport passthrough; a v2 (no-key) object parses to []; core `tsc --noEmit` clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MIGRATIONS[2] + bingoCards union merge (locked-wins collision) + tests</name>
  <files>packages/core/src/data-safety/merge.ts, packages/core/test/merge.test.ts</files>
  <read_first>
    - packages/core/src/data-safety/merge.ts (MIGRATIONS[1] at 52-56; the `archiveShows` union block at 137-140 — LOCAL-wins; the migration loop at 107-116 that ERRORS if MIGRATIONS[v] is missing; the `merged` object at 273-284)
    - packages/core/test/merge.test.ts (the existing round-trip + collision test idiom to extend)
    - packages/core/src/data-safety/serialize.ts (ExportSnapshot.bingoCards — the merge's local/incoming source shape)
  </read_first>
  <behavior>
    - A v2 envelope (schemaVersion 2, no bingoCards) migrated to v3 gains `bingoCards: []` via MIGRATIONS[2] and imports without error.
    - Non-colliding cards from local and incoming both survive the union.
    - Collision, LOCKED-vs-DRAFT: local card `lockedAt != null`, incoming same cardId `lockedAt == null` → LOCAL (locked) survives.
    - Collision, both same lock-state (both draft or both locked): the INCOMING (imported) row wins (D-13 literal).
  </behavior>
  <action>
    Register `MIGRATIONS[2] = (e) => ({ ...e, bingoCards: e.bingoCards ?? [] })` (mirrors MIGRATIONS[1] at 52). This is MANDATORY even though `.default([])` fills the field at parse — the migration loop (107-116) errors "too old" if `MIGRATIONS[v]` is missing for a step it must take. Add a `bingoCards` union-merge block modeled on the `archiveShows` block (137-140) but with an EXPLICIT collision resolution that resolves the D-13 contradiction (RESEARCH Open Q1 / Pitfall 3): D-13 says "imported wins" yet cites the local-wins `archiveShows` precedent — a genuine contradiction. DECISION (make this the locked, tested rule): key a `Map<string, row>` by `cardId`; on a same-`cardId` collision the LOCKED row wins (`lockedAt != null` beats `null`) so a locked historical card is never reverted to a draft; when both rows share lock-state (both locked or both draft) the INCOMING row wins per D-13's literal first clause. Add `bingoCards: mergedBingoCards` to the returned `merged` object (273-284). Do NOT blind-copy the local-wins loop. Extend merge.test.ts with the four behavior cases above (v2→v3 default-fill, non-colliding union, locked-beats-draft, both-draft-incoming-wins), each with an explicit comment citing D-13 + the Open-Q1 resolution.
  </action>
  <verify>
    <automated>npx vitest run packages/core/test/merge.test.ts</automated>
  </verify>
  <done>MIGRATIONS[2] registered; bingoCards union merge with locked-wins-then-imported-wins resolution on `merged`; four new merge.test.ts cases green; core `tsc --noEmit` clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| import file -> core merge | An untrusted user/friend backup JSON crosses into the local dex via `parseAndMergeImport` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | `bingoCardRow` at the import boundary | mitigate | `z.strictObject` + reused `bingoCardSchema` `z.discriminatedUnion` reject extra keys / `__proto__` / unknown square `kind`; whole-file reject before any merge (merge.ts step 2) |
| T-15-02 | Tampering | version-confusion import (envelope newer/older than current) | mitigate | `schemaVersion > current` rejects; missing `MIGRATIONS[v]` rejects "too old" (merge.ts:98-116) — MIGRATIONS[2] added so v2 files migrate, not fail |
| T-15-03 | DoS | unbounded bingoCards array in a crafted file | accept | Local personal tool (<10 users), no network; array size is bounded by IndexedDB/device; no per-row unbounded growth introduced |
</threat_model>

<verification>
- `npx vitest run packages/core/test/merge.test.ts` green (existing + 4 new cases).
- `npx vitest run` (both projects) green after the wave.
- Core `tsc --noEmit` clean — inferred `bingoCardRow` stays assignable downstream.
</verification>

<success_criteria>
- A pre-v3 backup imports (bingoCards defaults to []); a v3 backup round-trips.
- A locked card survives a same-cardId collision (never reverts to draft).
- No enum widened; core stays DOM/DB-free.
</success_criteria>

<output>
Create `.planning/phases/15-gizz-bingo-persistence-lock-replay/15-01-SUMMARY.md` when done.
</output>
