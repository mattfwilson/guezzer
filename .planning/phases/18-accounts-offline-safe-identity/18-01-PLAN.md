---
phase: 18-accounts-offline-safe-identity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/identity/color.ts
  - packages/core/src/index.ts
  - packages/core/test/identity/color.test.ts
  - packages/app/src/config.ts
  - packages/app/index.html
  - packages/app/vite.config.ts
  - packages/app/src/dex/shareCard.ts
  - packages/app/test/rebrand.test.ts
autonomous: true
requirements: [AUTH-06, AUTH-07]
must_haves:
  truths:
    - "Every identity gets a deterministic auto color from its user id, stable across devices (D-13, AUTH-07)"
    - "The app chrome reads 'Gizz With Friends' in title, manifest name/short_name, and the share-card wordmark (D-15/D-16, AUTH-06)"
    - "The rebrand touches display/chrome strings only — DB_NAME, storage keys, routes, and file paths are unchanged (D-15)"
  artifacts:
    - path: "packages/core/src/identity/color.ts"
      provides: "Pure identityColorIndex(userId, paletteLength) hash helper"
      exports: ["identityColorIndex"]
    - path: "packages/app/src/config.ts"
      provides: "config.auth.IDENTITY_COLORS palette + config.copy.auth strings + rebranded copy"
      contains: "IDENTITY_COLORS"
    - path: "packages/app/index.html"
      provides: "Rebranded document title"
      contains: "Gizz With Friends"
  key_links:
    - from: "packages/app/src/config.ts (config.auth.IDENTITY_COLORS)"
      to: "packages/core/src/identity/color.ts (identityColorIndex)"
      via: "app injects palette length into the pure core helper"
      pattern: "identityColorIndex"
    - from: "packages/app/src/dex/shareCard.ts"
      to: "config.copy.share.card.wordmark"
      via: "canvas wordmark text draw"
      pattern: "cardCopy.wordmark"
---

<objective>
Build the two static foundations the rest of Phase 18 stands on, with zero auth logic: (1) the pure-core deterministic identity-color helper (AUTH-07 / D-13) that Phases 19–20 also reuse, plus the `config.auth` palette + `config.copy.auth` strings it and the sign-in/chrome plans consume; and (2) the "Gizz With Friends" rebrand of every remaining chrome surface (AUTH-06 / D-15/D-16). This plan is the sole owner of `packages/app/src/config.ts` for the phase, so all config additions and rebrand string swaps happen here to avoid cross-plan file contention.

Purpose: The identity color is the enabling primitive for the header avatar (Plan 05), Phase 19 friend rows, and Phase 20 presence dots — it must exist, be deterministic from the Supabase user id, and be node-testable in pure core. The rebrand is a chrome-only string swap that must never touch persisted state.
Output: `packages/core/src/identity/color.ts` (+ barrel export + core test), a `config.auth` block and `config.copy.auth` block, rebranded `index.html`/`vite.config.ts`/`config.copy`/share-card wordmark, and `rebrand.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-accounts-offline-safe-identity/18-CONTEXT.md
@.planning/phases/18-accounts-offline-safe-identity/18-RESEARCH.md
@.planning/phases/18-accounts-offline-safe-identity/18-PATTERNS.md
@.planning/phases/18-accounts-offline-safe-identity/18-UI-SPEC.md
</context>

## Artifacts this phase produces (Plan 01)

- `identityColorIndex(userId: string, paletteLength: number): number` — NEW pure function, `packages/core/src/identity/color.ts`, exported from the `@guezzer/core` barrel.
- `config.auth.IDENTITY_COLORS` — NEW app config key: a 6-hue light/pastel palette (`["#7DD3FC","#FDA4AF","#86EFAC","#FCD34D","#C4B5FD","#5EEAD4"]`).
- `config.copy.auth` — NEW copy block: `signIn`, `whosHere`, `passwordPlaceholder`, `invalidCredentials`, `forgotOwner`, `connectOnceHeading`, `connectOnceBody`, `reconnecting`, `signOut`, `signOutSubline` strings (exact values from 18-UI-SPEC Copywriting Contract; owner reset name is "Matt").
- Rebranded string values (not new keys): `index.html` `<title>`, `vite.config.ts` manifest `name`/`short_name`/`description`, `config.copy.installBanner`/`installCta`/`installUnavailable`, `config.copy.share.card.wordmark`.
- `ORB_TEXT_COLOR` reuse convention: identity initials render at `#0C0C10` (existing token, not new).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure-core identityColorIndex helper + barrel export + core test (AUTH-07 / D-13)</name>
  <files>packages/core/src/identity/color.ts, packages/core/src/index.ts, packages/core/test/identity/color.test.ts</files>
  <read_first>
    - packages/app/src/map/MapView.tsx (lines 52-58, the `memberColor` hash idiom to mirror)
    - packages/core/src/index.ts (lines 383-394, the `map/presence.ts` export block to follow for the new barrel block)
    - packages/core/test/purity.test.ts (the static DOM/@supabase scan the new file must not trip)
    - packages/app/src/dex/rarityStyle.ts + packages/app/src/show/tuningColor.ts (the `#0C0C10` dark-on-light pairing, for context only — NOT imported into core)
  </read_first>
  <behavior>
    - identityColorIndex("abc", 6) returns the same integer on every call (deterministic).
    - For any userId, 0 <= identityColorIndex(userId, len) < len (range-safe for len >= 1).
    - Two different plausible Supabase UUIDs map into the palette range without throwing.
    - The hash matches the MapView idiom exactly: hash = (hash * 31 + charCodeAt(i)) | 0, index = Math.abs(hash) % paletteLength.
  </behavior>
  <action>
    Create `packages/core/src/identity/color.ts` as a NEW pure module (no DOM, no `@supabase`, no config import — palette length is a parameter). Export `identityColorIndex(userId: string, paletteLength: number): number` using the exact `hash = (hash * 31 + userId.charCodeAt(i)) | 0` loop from `MapView.memberColor` (MapView.tsx:53-58), returning `Math.abs(hash) % paletteLength`. Do NOT return a color string — return the index only, so the app injects `config.auth.IDENTITY_COLORS` and core stays palette-agnostic (D-13 discretion: helper lives in core for node-testability + Phase 19/20 reuse). Add an `export { identityColorIndex } from "./identity/color.ts";` block in `packages/core/src/index.ts` mirroring the `map/presence.ts` block at lines 383-394. Write `packages/core/test/identity/color.test.ts` (node env, no jsdom) asserting determinism and range per the behavior block above.
  </action>
  <verify>
    <automated>npx vitest run packages/core/test/identity/color.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run packages/core/test/identity/color.test.ts` exits 0
    - `npx vitest run packages/core/test/purity.test.ts` exits 0 (core stays DOM/Supabase-free)
    - `packages/core/src/index.ts` contains `identityColorIndex`
    - Source assertion: `packages/core/src/identity/color.ts` contains `hash * 31` and `% paletteLength`
  </acceptance_criteria>
  <done>The pure identity-color index is deterministic, range-safe, exported from the core barrel, and passes the core purity scan.</done>
</task>

<task type="auto">
  <name>Task 2: config.auth palette + config.copy.auth strings (D-13 / D-18 / D-03 / D-07)</name>
  <files>packages/app/src/config.ts</files>
  <read_first>
    - packages/app/src/config.ts (lines 715-758, the `config.map` block incl. `MEMBER_COLORS` to sit beside; lines 760-782, the `config.copy` block to extend)
    - .planning/phases/18-accounts-offline-safe-identity/18-UI-SPEC.md (§Color palette table + §Copywriting Contract — every exact string)
  </read_first>
  <action>
    Add a new `config.auth` block (beside `config.map`, NOT inside it) with `IDENTITY_COLORS: ["#7DD3FC","#FDA4AF","#86EFAC","#FCD34D","#C4B5FD","#5EEAD4"] as const` — a fresh, decoupled 6-hue light/pastel family (D-13; explicitly NOT reusing `config.map.MEMBER_COLORS`), each hue clearing >=4.5:1 against `#0C0C10` initials. Add a `config.copy.auth` block with the exact strings from the 18-UI-SPEC Copywriting Contract: `signIn: "Sign in"`, `whosHere: "Who's here?"`, `passwordPlaceholder: "Your password"`, `invalidCredentials: "Invalid login credentials"`, `forgotOwner: "Forgot? Ask Matt to reset it."`, `connectOnceHeading: "Connect once to sign in"`, `connectOnceBody` (the offline "connect to Wi-Fi or data once…" copy), `reconnecting: "Reconnecting…"`, `signOut: "Sign out"`, `signOutSubline: "Hand this phone to another Gizz friend."`. No scattered string literals anywhere else — single-config rule (CLAUDE.md).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p packages/app/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `packages/app/src/config.ts` contains `IDENTITY_COLORS` and `#7DD3FC`
    - Source assertion: `packages/app/src/config.ts` contains `whosHere` and `invalidCredentials` and `reconnecting`
    - `npx tsc --noEmit -p packages/app/tsconfig.json` exits 0
  </acceptance_criteria>
  <done>The auth palette and auth copy block exist in the single config file; app typecheck is clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: "Gizz With Friends" rebrand of remaining chrome surfaces (AUTH-06 / D-15/D-16)</name>
  <files>packages/app/index.html, packages/app/vite.config.ts, packages/app/src/config.ts, packages/app/src/dex/shareCard.ts, packages/app/test/rebrand.test.ts</files>
  <read_first>
    - packages/app/index.html (line 12, `<title>Guezzer</title>`)
    - packages/app/vite.config.ts (lines 74-92, the manifest `name`/`short_name`/`description`)
    - packages/app/src/config.ts (lines 762-769, `config.copy.installBanner`/`installCta`/`installUnavailable`; line 1335, `config.copy.share.card.wordmark`)
    - packages/app/src/dex/shareCard.ts (line 131, the `centerText(ctx, cardCopy.wordmark, cx, height * 0.10, 68, ...)` wordmark draw)
    - .planning/phases/18-accounts-offline-safe-identity/18-UI-SPEC.md (§Copywriting Contract Rebrand table)
    - packages/app/src/db/db.ts (line 16 area / config.DB_NAME — the value that must NOT change)
  </read_first>
  <behavior>
    - rebrand.test.ts asserts the manifest name, short_name, document title, install copy, and share wordmark all equal "Gizz With Friends" (or contain it).
    - rebrand.test.ts asserts config.DB_NAME still equals "guezzer" (chrome-only discipline guard).
  </behavior>
  <action>
    Swap display strings to "Gizz With Friends" on: `index.html` `<title>` (→ "Gizz With Friends", resolving Research OQ1 — the requirement is authoritative over D-15's parenthetical); `vite.config.ts` manifest `name` and `short_name` (→ "Gizz With Friends", per D-16 short_name may truncate under the home-screen icon — accepted) and `description` (→ the "Gizz With Friends" framing); `config.copy.installBanner.headline`/`installCta`/`installUnavailable` (swap "Guezzer" brand references); and `config.copy.share.card.wordmark` (→ "Gizz With Friends"). In `shareCard.ts` line 131, reduce the wordmark font size from 68 to a value that fits the longer "Gizz With Friends" string on the fixed card width (choose ~40-44px; size the mark to fit, D-15). Do NOT touch `config.DB_NAME`, any persisted Dexie/localStorage key, tab route strings (`show`/`explore`/`dex`), or file paths — chrome-only (D-15). Write `packages/app/test/rebrand.test.ts` importing the manifest config from vite.config (or asserting the string constants) + `config` + reading `index.html`, asserting the rebrand strings and the `DB_NAME === "guezzer"` guard per the behavior block.
  </action>
  <verify>
    <automated>npx vitest run packages/app/test/rebrand.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run packages/app/test/rebrand.test.ts` exits 0
    - Source assertion: `packages/app/index.html` contains `Gizz With Friends`
    - Source assertion: `packages/app/vite.config.ts` contains `name: "Gizz With Friends"` and `short_name: "Gizz With Friends"`
    - Source assertion: `packages/app/src/config.ts` DB_NAME line still reads `DB_NAME: "guezzer"`
    - Behavior: rebrand.test asserts DB_NAME unchanged
  </acceptance_criteria>
  <done>Every remaining chrome surface says "Gizz With Friends", the share wordmark fits the longer string, and a test guards that no persisted key or DB name changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public bundle → any scraper | The deployed bundle (config, manifest, roster in later plans) is world-readable at the public URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01-I | Information Disclosure | `config.auth`/rebrand strings in the public bundle | accept | Palette hues + brand copy are non-secret display data; no PII, no credentials. The roster (PII risk) is Plan 04's surface, not here. |
| T-18-01-T | Tampering | identity color hash | accept | Non-security display hash (RESEARCH Security Domain V6) — collisions only cause two friends to share a hue, never a privilege effect. |
| T-18-01-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed this plan (RESEARCH Package Legitimacy Audit: none) — nothing to vet. |
</threat_model>

<verification>
- `npx vitest run packages/core/test/identity/color.test.ts` green
- `npx vitest run packages/core/test/purity.test.ts` green (core purity preserved)
- `npx vitest run packages/app/test/rebrand.test.ts` green
- `npm test` full suite green (no regression to existing 40+ tests)
- Both `tsc --noEmit` (core + app) clean
</verification>

<success_criteria>
- AUTH-07: `identityColorIndex` is pure, deterministic, range-safe, barrel-exported, palette lives in `config.auth.IDENTITY_COLORS` (SC-5 color half).
- AUTH-06: title, manifest name/short_name/description, install copy, and share wordmark all read "Gizz With Friends"; DB_NAME and all persisted keys unchanged (SC-5 rebrand half).
</success_criteria>

<output>
Create `.planning/phases/18-accounts-offline-safe-identity/18-01-SUMMARY.md` when done.
</output>
