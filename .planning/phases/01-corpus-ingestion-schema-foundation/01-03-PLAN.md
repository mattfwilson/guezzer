---
phase: 01-corpus-ingestion-schema-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - packages/core/src/cli/fetch-corpus.ts
  - packages/core/src/cli/refresh.ts
  - packages/core/src/ingest/census.ts
  - packages/core/src/cli/run-census.ts
  - packages/core/test/fetch.test.ts
  - packages/core/test/census.test.ts
  - data/raw/
  - data/census-report.md
  - data/census.json
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03]
tags: [ingestion, fetch, census, api-etiquette]

must_haves:
  truths:
    - "The fetcher is strictly sequential with a ~2s courtesy delay between every request, sends the descriptive User-Agent, and never retries (D-07)"
    - "Every per-year fetch is followed by assertFilterApplied on showyear; a mismatch hard-fails naming endpoint/field/values (D-12)"
    - "The full historical raw corpus (per-year setlists 2010–2026 + shows/songs/albums/jamcharts) is committed under data/raw/ with fetch timestamps"
    - "A census report exists enumerating every distinct value of each enum-ish field with counts and example show IDs, plus the derived checks (terminal distribution by year, per-year segue frequency, footnote parse failures, side-project row counts)"
    - "An empty year (data: []) is tolerated as valid, not an error"
  artifacts:
    - path: "packages/core/src/cli/fetch-corpus.ts"
      provides: "Paced sequential fetcher (Pattern 3) + per-year orchestration + raw file writes"
      exports: ["fetchJson", "fetchCorpus"]
    - path: "packages/core/src/ingest/census.ts"
      provides: "Pure distinct-value census over raw rows (D-10)"
      exports: ["runCensus"]
    - path: "data/raw/fetch-meta.json"
      provides: "Per-file fetchedAt timestamps + row counts"
    - path: "data/census-report.md"
      provides: "First-class owner-readable census deliverable (D-10)"
    - path: "data/census.json"
      provides: "Machine-readable census for tests and plan 01-04 enum locking"
  key_links:
    - from: "packages/core/src/cli/fetch-corpus.ts"
      to: "packages/core/src/ingest/validate.ts"
      via: "assertFilterApplied after every filtered fetch"
      pattern: "assertFilterApplied"
    - from: "packages/core/src/cli/fetch-corpus.ts"
      to: "packages/core/src/config.ts"
      via: "apiBase, fetchDelayMs, userAgent, corpusYearStart from config"
      pattern: "config\\."
    - from: "data/census.json"
      to: "data/raw/"
      via: "census derived from committed raw corpus only (zero API traffic)"
      pattern: "setlists-"
---

<objective>
Build the polite fetcher, pull the full historical corpus from kglw.net exactly once, commit it as the offline source of truth, and run the census that resolves SCHEMA.md's open unknowns with full-corpus evidence.

Purpose: Implements D-05 (per-year incremental), D-06 (raw committed), D-07 (pacing/UA/no-retries), D-10 (census), D-12 (post-fetch assertions). Completes the fetch half of DATA-02, exercises DATA-03 on every live request, and produces the evidence DATA-01's unknowns need. The one-time live fetch happens HERE, in this plan, manually invoked by the executor — never in CI, never in tests (P11).
Output: `fetch-corpus.ts`, extended `refresh.ts` (--all/--year/--fetch-only/--census-only), committed `data/raw/*`, `data/census-report.md` + `data/census.json`, mocked-fetch and census test suites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-corpus-ingestion-schema-foundation/01-02-SUMMARY.md
@.planning/phases/01-corpus-ingestion-schema-foundation/01-RESEARCH.md
@.planning/research/ARCHITECTURE.md
@docs/SCHEMA.md
@packages/core/src/config.ts
@packages/core/src/ingest/validate.ts
</context>

## Artifacts this phase produces (plan 03 subset)

- **Files:** `packages/core/src/cli/fetch-corpus.ts`, `packages/core/src/ingest/census.ts`, `packages/core/src/cli/run-census.ts`, `packages/core/test/fetch.test.ts`, `packages/core/test/census.test.ts`, `data/raw/setlists-2010.json` … `data/raw/setlists-2026.json`, `data/raw/shows.json`, `data/raw/songs.json`, `data/raw/albums.json`, `data/raw/jamcharts.json`, `data/raw/fetch-meta.json`, `data/census-report.md`, `data/census.json`
- **Exported symbols:** `fetchJson(path, deps?)`, `fetchCorpus(options, deps?)`, `runCensus(rowsByYear)`, `CensusResult` type
- **CLI flags (refresh.ts, added this plan):** `--all` (full 2010→current pull), `--year <N>` (repeatable, validated integer 2010–2100), `--fetch-only`, `--census-only`; default invocation (no flags) = refetch current year only (D-05), then census + normalize
- **Data contract:** `census.json` shape — per-field array of `{ value, rowCount, showCount, exampleShows: [{showId, showdate}] }` plus `derived` block (terminal distribution, segue frequency, footnote failures, side-project counts, contiguity violations, per-year show counts, distinct KGLW songs, covers count)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Paced sequential fetcher + refresh orchestration, tested entirely with mocked fetch</name>
  <files>packages/core/src/cli/fetch-corpus.ts, packages/core/src/cli/refresh.ts, packages/core/test/fetch.test.ts</files>
  <read_first>
    - .planning/phases/01-corpus-ingestion-schema-foundation/01-RESEARCH.md (Architecture Pattern 3 "Paced sequential fetcher" — copy nearly verbatim; Pitfall 3 row-count sanity; Open Question 4 — jamcharts fetched now, normalized in Phase 2)
    - .planning/research/ARCHITECTURE.md (Part 1 — exact endpoint paths and API base URL; envelope semantics; which sibling tables exist)
    - packages/core/src/ingest/validate.ts (assertFilterApplied signature — every filtered fetch calls it)
    - packages/core/src/cli/refresh.ts (current skeleton — extend, don't rewrite; keep --normalize-only working)
    - packages/core/src/config.ts (apiBase, fetchDelayMs, userAgent, corpusYearStart, cliYearMin/Max, maxRowsPerYearSanity)
  </read_first>
  <behavior>
    - Test 1: `fetchCorpus({ years: [2012, 2013] }, { fetch: mockFetch, sleep: mockSleep })` issues requests strictly sequentially (second request not started before first resolves) and calls the sleep hook between EVERY pair of consecutive requests with `config.fetchDelayMs`
    - Test 2: every request carries the `User-Agent` header `"Guezzer setlist tool (matt.f.wilson@gmail.com)"`
    - Test 3: envelope with `error: true` → throws naming the endpoint and error_message; HTTP 500 → throws naming status + path and mockFetch was called exactly once for that path (NO retry, D-07)
    - Test 4: `data: []` for a year → written as an empty rows file, no throw (valid empty result)
    - Test 5: a year response where one row has the wrong showyear → throws via assertFilterApplied (message contains the endpoint and both year values)
    - Test 6: a year response with more than `config.maxRowsPerYearSanity` rows → throws (silent-filter-ignore tripwire, Pitfall 3)
    - Test 7: refresh arg validation — `--year 1999` and `--year banana` exit non-zero with a message naming the valid range 2010–2100 (security V5); `--year 2013` is accepted
  </behavior>
  <action>
    Write failing tests first with injected `{ fetch, sleep }` dependencies (default to real `globalThis.fetch` and a `setTimeout` sleep in production), then implement.

    `packages/core/src/cli/fetch-corpus.ts`:
    - `fetchJson(path, deps)` per RESEARCH Pattern 3: GET `config.apiBase + path` with User-Agent header and `AbortSignal.timeout(config.fetchTimeoutMs)`; non-OK → throw `HTTP {status} from {path} — NOT retrying (D-07)`; parse envelope; `error: true` → throw with endpoint + error_message; return `data` array (empty array is valid).
    - `fetchCorpus({ years, includeTables })`: strictly sequential `for` loop over year paths `/setlists/showyear/{year}.json` then sibling tables `shows.json`, `songs.json`, `albums.json`, `jamcharts.json` (jamcharts fetched now per RESEARCH Open Question 4; normalized in Phase 2). `await deps.sleep(config.fetchDelayMs)` between every request. After each per-year fetch: validate rows structurally with `rawSetlistRowCensus` (census-mode — the point is to fetch, not to enum-gate), `assertFilterApplied(rows, "setlists/showyear/{year}", { field: "showyear", expected: year })`, and row-count sanity check against `config.maxRowsPerYearSanity`. After `albums.json`: rows have `artist_id` — do NOT filter the raw file (raw = full source of truth, D-06) but log the KGLW/non-KGLW split. Write each result to `data/raw/setlists-{year}.json` (bare rows array, 2-space indent, trailing newline) and sibling files; maintain `data/raw/fetch-meta.json` `{ [filename]: { fetchedAt: ISO, rowCount } }` merged per file so partial refreshes update only touched entries (D-05).
    - IMPORTANT: raw per-year setlist files keep ALL artists' rows (raw is untouched source of truth; artist filtering is client-side at census/normalize, per RESEARCH "artist filtering is CLIENT-SIDE").

    `packages/core/src/cli/refresh.ts` — extend the skeleton: `--all` = years `config.corpusYearStart..currentYear` + all sibling tables; `--year N` (repeatable, integer-validated within cliYearMin..cliYearMax BEFORE any URL/path interpolation) = only those years, no sibling tables unless `--tables` passed; no flags = current year only (D-05 routine refresh), then census + normalize once those stages exist (wire census in Task 3); `--fetch-only` skips census/normalize; keep `--normalize-only` working. Update usage text.
  </action>
  <verify>
    <automated>pnpm vitest run packages/core/test/fetch.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 7 behavior tests pass without ANY real network call (assert mockFetch call counts)
    - `grep -cE "retry|p-retry|backoff" packages/core/src/cli/fetch-corpus.ts` returns 0 (no retry logic exists)
    - `grep -c "assertFilterApplied" packages/core/src/cli/fetch-corpus.ts` >= 1
    - fetch.test.ts contains no real endpoint invocation: tests inject mock fetch (P11 — tests never touch the volunteer API)
    - Full `pnpm vitest run` green
  </acceptance_criteria>
  <done>A fully-tested polite fetcher exists whose pacing, filter-assertions, error UX, and no-retry behavior are proven by mocks — before it ever touches the live API.</done>
</task>

<task type="auto">
  <name>Task 2: One-time real corpus fetch — run the documented command, commit data/raw</name>
  <files>data/raw/</files>
  <read_first>
    - packages/core/src/cli/fetch-corpus.ts (the code you are about to run against the live volunteer API — re-verify pacing/UA before running)
    - .planning/phases/01-corpus-ingestion-schema-foundation/01-CONTEXT.md (D-05/D-06/D-07 + Specifics: "failure UX matters")
  </read_first>
  <action>
    Run the one-time full historical pull: `pnpm refresh --all --fetch-only`. This is THE single sanctioned live-API interaction of the phase (P11): ~17 per-year requests (2010–2026) + 4 sibling tables ≈ 21 requests at 2s spacing ≈ under a minute. Do NOT wrap it in loops, do NOT re-run on transient failure without inspecting the error first (D-07: no automatic retries — a hard failure names the endpoint/field/value and is diagnosable in minutes by design; fix the cause, then re-run only the failed year via `--year N`).

    After the run: sanity-inspect with Node one-liners (not by re-fetching): every `data/raw/setlists-{year}.json` exists for 2010–2026 (early years may legitimately be small or empty); every row of a spot-checked year matches its showyear; `fetch-meta.json` has an entry per written file; total setlist row count is plausible (order 10^4, not 10^5). Commit all of `data/raw/` — this is the offline source of truth enabling zero-API-traffic re-normalization forever after (D-06).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const meta=JSON.parse(fs.readFileSync('data/raw/fetch-meta.json','utf8')); const years=Array.from({length:17},(_,i)=>2010+i); let total=0; for(const y of years){ const f='data/raw/setlists-'+y+'.json'; if(!fs.existsSync(f)){console.error('MISSING '+f);process.exit(1);} const rows=JSON.parse(fs.readFileSync(f,'utf8')); rows.forEach(r=>{ if(r.showyear!==y){console.error('WRONG YEAR in '+f);process.exit(1);} }); total+=rows.length; } for(const t of ['shows.json','songs.json','albums.json','jamcharts.json']){ if(!fs.existsSync('data/raw/'+t)){console.error('MISSING '+t);process.exit(1);} } if(total<3000||total>60000){console.error('IMPLAUSIBLE total rows: '+total);process.exit(1);} console.log('raw corpus OK: '+total+' setlist rows, '+Object.keys(meta).length+' files in fetch-meta')"</automated>
  </verify>
  <acceptance_criteria>
    - `data/raw/setlists-2010.json` through `setlists-2026.json` all exist; every row in every year file has `showyear` equal to its filename year
    - `data/raw/{shows,songs,albums,jamcharts}.json` and `data/raw/fetch-meta.json` exist
    - Total setlist rows across all years is between 3,000 and 60,000 (plausibility band; a single year >5,000 would have hard-failed during fetch)
    - All raw files committed with LF endings (per .gitattributes)
    - The fetch was executed exactly once (or once per failed-year retry after diagnosis) — no loops, no CI wiring anywhere (`grep -rc "refresh" .github/ 2>/dev/null` finds nothing / directory absent)
  </acceptance_criteria>
  <done>The full historical corpus is committed as the permanent offline source of truth; the volunteer API never needs to be re-hammered for schema or normalizer work.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Census over the full raw corpus — the evidence that resolves SCHEMA.md unknowns</name>
  <files>packages/core/src/ingest/census.ts, packages/core/src/cli/run-census.ts, packages/core/src/cli/refresh.ts, packages/core/test/census.test.ts, data/census-report.md, data/census.json</files>
  <read_first>
    - .planning/phases/01-corpus-ingestion-schema-foundation/01-RESEARCH.md ("Code Examples" census report shape — the authoritative output spec; "Verified Schema Facts" for expected sample values)
    - docs/SCHEMA.md (§13 Open Unknowns — the census exists to resolve exactly these)
    - packages/core/src/ingest/api-types.ts (census-mode row schema — rows validated before counting)
    - data/samples/showyear2013.json (test input with known expected census values)
  </read_first>
  <behavior>
    - Test 1: census over the two sample files yields `settype` distinct values exactly {"Set", "One Set", "Live Session"} with Live Session showCount 2 and PBS-show example IDs
    - Test 2: `transition_id` distinct values include 4 (rowCount 1 from rr1010) and every value carries rowCount + up to 3 exampleShows with showId + showdate
    - Test 3: derived `lastRowTransitionByYear` for 2013 reports 14 shows ending with transition_id 1 (terminal unreliability evidence)
    - Test 4: derived `segueFrequencyByYear` for 2013 ≈ 4/149 rows and for 2022 (rr1010 only) 9/27
    - Test 5: derived `sideProjectRows` counts rows with artist_id !== 1 (0 in samples) and `contiguityViolations` is an empty array for the samples
  </behavior>
  <action>
    Write failing tests over the sample data first, then implement.

    `packages/core/src/ingest/census.ts` — pure `runCensus(rowsByFile: Map<string, unknown[]>): CensusResult`. Validate rows with `rawSetlistRowCensus`. Census EVERYTHING enum-ish per the RESEARCH spec: `settype`, `setnumber`, `transition_id`, `isoriginal`, `isreprise`, `isjam`, `isjamchart`, `isverified`, `soundcheck`, `opener` (distinct presence), `css_class`, `tour_id` sentinel usage — each as `{ value, rowCount, showCount, exampleShows: [{showId, showdate}] (max 3) }`. Derived checks block: (a) last-row transition_id distribution per year (terminal reliability by era); (b) per-year segue frequency (ids 2/3 as % of rows — Phase 2 era-restriction evidence, RESEARCH fact 8); (c) rows where `footnotes` fails JSON.parse (guarded — collect, never throw, Pitfall 5); (d) non-contiguous position sequences per show (expect zero; list violators); (e) side-project (artist_id !== 1) row counts per year — visible but excluded from KGLW stats; (f) distinct KGLW song count, shows per year, covers count; (g) footnote/shownotes rows matching /tease/i — the tease-notation hunt (SCHEMA.md unknown c). Census counts KGLW rows only except where the point is the side-project split.

    `packages/core/src/cli/run-census.ts` — read all `data/raw/setlists-*.json` (default) or `--input data/samples`; call `runCensus`; write `data/census.json` (machine-readable, stable key order) and `data/census-report.md` (owner-readable: one section per field, markdown tables value|rows|shows|examples, then the derived-checks sections). Wire `--census-only` in refresh.ts to this.

    Then RUN it over the full committed corpus: `pnpm refresh --census-only` → commit both outputs. The report is a first-class deliverable the owner reads (CONTEXT Specifics) — lead it with a "What this resolves" section mapping each SCHEMA.md unknown (a)–(e) to the table that answers it.
  </action>
  <verify>
    <automated>pnpm vitest run packages/core/test/census.test.ts && node -e "const c=JSON.parse(require('fs').readFileSync('data/census.json','utf8')); const assert=require('assert'); assert.ok(c.fields.settype.length>=2); assert.ok(c.fields.transition_id.length>=4); assert.ok(c.derived.segueFrequencyByYear); assert.ok(c.derived.lastRowTransitionByYear); assert.ok(Array.isArray(c.derived.contiguityViolations)); console.log('census.json OK: settype values='+c.fields.settype.map(v=>v.value).join(','))"</automated>
  </verify>
  <acceptance_criteria>
    - All 5 behavior tests pass on sample data
    - `data/census.json` and `data/census-report.md` exist, generated from the FULL committed raw corpus, and are committed
    - census-report.md contains a section for every enum-ish field listed in the action, each with counts and example show IDs, plus all 7 derived-check sections
    - The report's "What this resolves" section references each SCHEMA.md §13 unknown (a)–(e)
    - Census generation performed zero network requests (it reads only data/raw)
  </acceptance_criteria>
  <done>Full-corpus evidence exists for every open schema question: enum vocabularies, terminal unreliability by era, segue-notation drift, settype variant list, footnote quirks — ready for the plan 01-04 enum lock.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live kglw.net API → data/raw | The one-time bulk ingestion of untrusted external data into the committed repo |
| CLI `--year` arg → URL/file paths | User-supplied value interpolated into request paths and filenames |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Spoofing/Tampering | per-year fetch responses | mitigate | assertFilterApplied on showyear after EVERY fetch + maxRowsPerYearSanity tripwire — a typo'd path returning the whole multi-artist table cannot be committed as a "year" file (Pitfall 3) |
| T-01-08 | Tampering | --year CLI arg | mitigate | Validated as integer within 2010–2100 BEFORE URL/path construction (ASVS V5); non-integers rejected with usage |
| T-01-09 | DoS (against volunteer site) | fetch-corpus.ts | mitigate | Strictly sequential, 2s delay between all requests, AbortSignal timeout, NO retries, no CI/test invocation (tests use injected mock fetch), descriptive User-Agent with owner contact (D-07/P11) |
| T-01-10 | Tampering | envelope/rows parsing | mitigate | zod census-mode validation on every fetched row before write; malformed payload fails loudly naming endpoint; JSON.parse only (never eval) |
| T-01-06 | Integrity | committed data/raw | mitigate | Committed raw + fetch-meta timestamps = reviewable provenance; normalizer re-runs need zero API traffic (D-06) |
</threat_model>

<verification>
- `pnpm vitest run` green (fetch mocked suite + census suite + all prior)
- `data/raw/` complete and committed; verify script for raw corpus passes
- `data/census-report.md` + `data/census.json` committed, derived from committed raw only
</verification>

<success_criteria>
- Phase success criterion 2's fetch half done: one documented command pulled the full historical corpus
- Phase success criterion 3 enforced on every live request path (showyear assertion + client-side artist accounting)
- D-10 satisfied: every SCHEMA.md unknown now has full-corpus evidence waiting in the census report
</success_criteria>

<output>
Create `.planning/phases/01-corpus-ingestion-schema-foundation/01-03-SUMMARY.md` when done.
</output>
