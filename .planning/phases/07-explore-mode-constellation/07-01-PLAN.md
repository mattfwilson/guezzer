---
phase: 07-explore-mode-constellation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/core/src/config.ts
  - packages/app/src/config.ts
  - packages/core/src/explore/derive-constellation.ts
  - packages/core/src/explore/rotation.ts
  - packages/core/src/explore/rank-outgoing.ts
  - packages/core/test/explore/derive-constellation.test.ts
  - packages/core/test/explore/rotation.test.ts
  - packages/core/test/explore/rank-outgoing.test.ts
  - packages/core/src/index.ts
autonomous: true
requirements: [EXPL-01, EXPL-02, EXPL-03, EXPL-04]
must_haves:
  truths:
    - "deriveConstellation turns the matrix into {nodes,links} using react-force-graph-2d default keys (node.id, link.source/target) while carrying fromId/toId + count/segueCount as separate mutation-safe fields (EXPL-01)"
    - "rotationSongIds returns exactly the distinct songIds of the last N=5 shows (newest-first), and the real corpus yields 56 songs (EXPL-03)"
    - "rankOutgoing returns a node's COMPLETE outgoing history sorted by count with pct=count/total off raw edges, never predict() (EXPL-02)"
    - "An edge-count threshold predicate hides links below X while leaving the node population untouched (EXPL-04/D-08)"
    - "All Explore tunables live in the single config file: core (N, threshold, slider bounds, top-N) and app (radii, zoom thresholds, opacities, physics)"
  artifacts:
    - path: "packages/core/src/explore/derive-constellation.ts"
      provides: "deriveConstellation(matrix, cfg?) → {nodes, links}"
      exports: ["deriveConstellation"]
    - path: "packages/core/src/explore/rotation.ts"
      provides: "rotationSongIds(archive, windowShows) → Set<number>"
      exports: ["rotationSongIds"]
    - path: "packages/core/src/explore/rank-outgoing.ts"
      provides: "rankOutgoing(matrix, songId) → { total, bars }"
      exports: ["rankOutgoing"]
    - path: "packages/core/test/explore/rotation.test.ts"
      provides: "fixture + real-corpus N=5→56 guard"
      contains: "expect"
  key_links:
    - from: "packages/core/src/index.ts"
      to: "packages/core/src/explore/*.ts"
      via: "barrel export"
      pattern: "export \\{[^}]*deriveConstellation"
    - from: "packages/core/src/explore/rotation.ts"
      to: "cfg.explore.ROTATION_WINDOW_SHOWS"
      via: "config constant"
      pattern: "ROTATION_WINDOW_SHOWS"
---

<objective>
Build the fixture-tested pure-core engine and the single-config-file surface that every Explore UI slice consumes. Three pure derivation functions (`deriveConstellation`, `rotationSongIds`, `rankOutgoing`) transform the SAME `transition-matrix.json` the predictor uses (plus the Phase-6 `@archive` artifact) into the constellation graph data, the rotation node set, and the ranked-bar payload — with zero React/DOM dependencies, per the CLAUDE.md core/UI separation constraint.

Purpose: This wave is the tested substrate. deriveConstellation feeds Slice 1 (render), rankOutgoing feeds Slice 2 (bars), rotationSongIds feeds Slice 3 (filters). Centralizing every tunable here (core derivation constants + app render constants + copy) means no downstream slice ever edits a config file — eliminating cross-wave file conflicts.
Output: 3 pure core functions, 3 fixture test files, core+app `explore` config blocks, `copy.explore`, and barrel exports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-explore-mode-constellation/07-CONTEXT.md
@.planning/phases/07-explore-mode-constellation/07-RESEARCH.md
@.planning/phases/07-explore-mode-constellation/07-PATTERNS.md
@.planning/phases/07-explore-mode-constellation/07-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Explore config surface (core + app) + copy</name>
  <files>packages/core/src/config.ts, packages/app/src/config.ts</files>
  <read_first>
    - packages/core/src/config.ts (existing `dex:` block ~lines 195-282 — copy the `[VERIFIED]`/`[ASSUMED]` comment discipline and `as const` shape)
    - packages/app/src/config.ts (existing `show:`/`dex:` render blocks + `copy.show`/`copy.dex` function-template idiom, e.g. `copy.dex.*` interpolators — PATTERNS.md lines 242-248)
    - 07-UI-SPEC.md §"Config surface" (exact keys + defaults table) and §"Copywriting Contract" (verbatim copy strings)
    - 07-RESEARCH.md §"Data-Driven Defaults" (N=5→56, threshold=2 justification to cite in comments)
  </read_first>
  <action>
    Add a `explore` block to `packages/core/src/config.ts` with `[VERIFIED]`-commented data-driven constants: `ROTATION_WINDOW_SHOWS: 5`, `EDGE_COUNT_THRESHOLD_DEFAULT: 2`, `EDGE_SLIDER_MIN: 1`, `EDGE_SLIDER_MAX: 10`, `BARS_TOP_N: 10` — each with a one-line comment citing the corpus analysis (N=5→56 nodes; threshold≥2 removes all 1,946 one-play edges → 56-node/174-edge default sky).
    Add a `explore` block to `packages/app/src/config.ts` with `[ASSUMED]`-commented (spike-tunable) render constants: `NODE_RADIUS_MIN: 4`, `NODE_RADIUS_MAX: 14`, `NODE_HIT_MIN_RADIUS_PX: 22`, `LABEL_ZOOM_THRESHOLD: 1.5`, `COUNT_ZOOM_THRESHOLD: 2.5`, `LABEL_AT_REST_TOP_K: 8`, `LABEL_MAX_CHARS: 18`, `DEX_DIM_OPACITY: 0.35`, `FOCUS_DIM_OPACITY: 0.12`, `SHEET_PEEK_FRACTION: 0.4`, `ALPHA_DECAY: 0.035`, `VELOCITY_DECAY: 0.45`, `COOLDOWN_TICKS: 200`.
    Add a `copy.explore` block (function-template idiom mirroring `copy.dex`) with the verbatim UI-SPEC strings: filter FAB aria-label, `rotationHelper(n)` → "Songs from the last {n} shows.", `edgeSliderLabel(x)` → "Played together ≥ {x}×", overlay switch label "My dex overlay", `sheetSubline(playCount)` → "Played {playCount}× all-time", `barWhy(count, total, lastDate, segueCount)` → "Played {count} of {total} times after this song · last {lastDate}" appending " · {segueCount} hard segues" only when segueCount>0, `barsExpander(n)` / "Show top 10", zero-edges heading/body, rotation-empty heading/body, matrix-load error heading/body. Never hardcode N — interpolate from `config.explore.ROTATION_WINDOW_SHOWS`.
    Do NOT scatter any of these values into components later — this is the single source per CLAUDE.md. (The `packages/core/src/index.ts` barrel export block for the three explore functions is created in Task 2 when the first function file lands — not here — so this task's typecheck never forward-references a nonexistent file.)
  </action>
  <verify>
    <automated>pnpm -w test --run 2>&1 | tail -20; pnpm --filter @guezzer/core exec tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `config.explore.ROTATION_WINDOW_SHOWS === 5`, `EDGE_COUNT_THRESHOLD_DEFAULT === 2`, `BARS_TOP_N === 10` present in `packages/core/src/config.ts`
    - `config.explore.SHEET_PEEK_FRACTION === 0.4`, `FOCUS_DIM_OPACITY === 0.12`, `DEX_DIM_OPACITY === 0.35`, `COOLDOWN_TICKS === 200` present in `packages/app/src/config.ts`
    - `config.copy.explore.barWhy` is a function; calling it with segueCount>0 appends " · {n} hard segues", with segueCount===0 omits it
    - `config.copy.explore.rotationHelper` interpolates N from config (no literal "5" in the string)
    - core typecheck passes cleanly — this task adds only config/copy (no explore-function imports), so `tsc --noEmit` is green at THIS task's gate, not deferred to Tasks 2-3
  </acceptance_criteria>
  <done>Both config files carry a complete `explore` block; `copy.explore` holds every UI-SPEC string as interpolating templates. The barrel export lands in Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: deriveConstellation + rankOutgoing (pure, fixture-tested)</name>
  <files>packages/core/src/explore/derive-constellation.ts, packages/core/src/explore/rank-outgoing.ts, packages/core/test/explore/derive-constellation.test.ts, packages/core/test/explore/rank-outgoing.test.ts, packages/core/src/index.ts</files>
  <read_first>
    - packages/core/src/model/matrix.ts lines 1-18 (module doc "Zero I/O" header), 75-80 (`cfg: typeof config = config` param idiom), 156-166 (explicit sort discipline) — the analog to copy verbatim
    - packages/core/src/domain/types.ts lines 104-148 (MatrixNode {songId, songName, playCount, eraPlayCount, tuningFamily}, MatrixEdge {from, to, count, weightedCount, segueCount, firstDate, lastDate}, TransitionMatrix {nodes, edges})
    - packages/core/test/model/matrix.test.ts (describe/it structure, requirement IDs in test names)
    - packages/core/test/dex/albums.test.ts lines 14-22 (`node()`/`repoFile()` fixture-factory idiom to mirror)
    - 07-RESEARCH.md §"Code Examples" (rankOutgoing body given verbatim), §"Pattern 1" (library default-key mapping), §"Pitfall 1" (in-place mutation → carry fromId/toId separately)
    - 07-PATTERNS.md lines 51-80 (derive-constellation assignment), 101-105 (rank-outgoing assignment)
  </read_first>
  <behavior>
    - deriveConstellation: a fixture matrix (3 nodes, 2 edges) → `nodes` carry `{id: songId, name: songName, playCount, tuningFamily}`; `links` carry `{source: from, target: to, count, segueCount, fromId: from, toId: to}`. Node order is deterministic (sorted by songId). fromId/toId equal the original songIds and are SEPARATE fields from source/target (survive the library's post-tick source/target mutation).
    - deriveConstellation edge-threshold helper (exported predicate or `edgesAtThreshold(links, x)`): x=2 drops all count===1 links; the node array is returned unchanged (D-08).
    - rankOutgoing: fixture matrix, songId with 3 outgoing edges (counts 12/8/1) → `total===21`, `bars.length===3`, sorted desc by count, `bars[0].pct === 12/21`, each bar carries `{songId, count, pct, lastDate, segueCount}`.
    - rankOutgoing zero-outgoing node → `{ total: 0, bars: [] }` (honest zero, no throw — D-08 free-floating star).
    - rankOutgoing reads the FULL matrix edge list for the songId (never a pre-filtered link set) — D-03.
  </behavior>
  <action>
    Create `packages/core/src/explore/derive-constellation.ts` mirroring `model/matrix.ts` shape: leading "Zero I/O" doc-comment, `cfg: typeof config = config` param, Map-keyed accumulation not needed (direct `.map()`), explicit `.sort((a,b)=>a.songId-b.songId)` on nodes. Emit react-force-graph-2d default accessor keys on nodes (`id`) and links (`source`/`target`) AND carry `fromId`/`toId`/`count`/`segueCount` as separate fields so focus-dim adjacency never reads the mutated `source`/`target` (RESEARCH Pitfall 1). Export a named type for the node/link shapes. Include the threshold predicate here (link-level, node-population-preserving).
    Create `packages/core/src/explore/rank-outgoing.ts` from the RESEARCH §Code Examples body verbatim: filter matrix.edges by `from === songId`, `total = sum of count`, map to `{songId: e.to, count, pct: total ? count/total : 0, lastDate, segueCount}`, sort `b.count - a.count`. Return the FULL list (`{ total, bars }`) — the app applies BARS_TOP_N. Do NOT assemble the "why" string here (app-side).
    Write the failing tests FIRST in the two `test/explore/*.test.ts` files using the `node()`/`repoFile()` factory idiom, then implement to green. Create the documented explore barrel export block in `packages/core/src/index.ts` (mirroring the existing dex export block ~lines 181-193) and add both functions + their exported types to it — this is where the barrel first appears, so it never forward-references a missing file.
  </action>
  <verify>
    <automated>pnpm vitest run packages/core/test/explore/derive-constellation.test.ts packages/core/test/explore/rank-outgoing.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run packages/core/test/explore/derive-constellation.test.ts packages/core/test/explore/rank-outgoing.test.ts` green
    - Test asserts a derived link exposes `fromId`/`toId` equal to original songIds (separate from source/target)
    - Test asserts threshold x=2 removes count===1 links while `nodes.length` is unchanged
    - Test asserts `rankOutgoing` pct = count/total and zero-outgoing → `{total:0, bars:[]}`
    - `deriveConstellation` and `rankOutgoing` importable from `@guezzer/core` barrel
  </acceptance_criteria>
  <done>Both functions exist, are barrel-exported, and their fixture tests are green with the mutation-safe fromId/toId contract asserted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: rotationSongIds (pure, fixture + real-corpus N=5→56 guard)</name>
  <files>packages/core/src/explore/rotation.ts, packages/core/test/explore/rotation.test.ts, packages/core/src/index.ts</files>
  <read_first>
    - packages/core/src/dex/derive-dex.ts lines 99-112 (archive-shows iteration idiom to copy)
    - packages/core/src/dex/archive-types.ts lines 22-48 (ArchiveArtifact/ArchiveShow: shows[{id,date,venue,city,state,country,sets:[{n,songs:number[]}]}]; `set.songs` is already number[])
    - packages/core/test/dex/albums.test.ts lines 9-22 (real-artifact `import ... with { type: "json" }` + `repoFile()` idiom for the corpus guard)
    - 07-RESEARCH.md §"Code Examples" rotationSongIds (body given), §"Pitfall 5" (sort newest-first before slicing — do NOT trust array order)
    - 07-PATTERNS.md lines 84-97 (rotation assignment; sort-descending-before-slice mandate)
  </read_first>
  <behavior>
    - rotationSongIds: fixture archive of 6 shows (dated, out of order) with known set songs → `rotationSongIds(archive, 3)` returns exactly the distinct songIds of the 3 NEWEST shows by date (proving the sort, not array order).
    - Real-corpus guard: importing `data/normalized/archive.json` and calling `rotationSongIds(archive, 5)` returns a Set of size 56 (guards the D-06 data-driven default `[VERIFIED]`).
    - Empty/zero-window edge cases return an empty Set without throwing.
  </behavior>
  <action>
    Create `packages/core/src/explore/rotation.ts` from RESEARCH §Code Examples: copy `[...archive.shows]` (don't mutate the artifact), sort by `date` DESCENDING, `.slice(0, windowShows)`, iterate `show.sets → set.songs → add(songId)` into a `Set<number>`. Signature `rotationSongIds(archive: ArchiveArtifact, windowShows: number): Set<number>`. "Zero I/O" doc-comment; import types with `.ts` extension. Default caller passes `config.explore.ROTATION_WINDOW_SHOWS`.
    Write the fixture + real-corpus tests FIRST (red), then implement to green. Add the function + type to the barrel.
  </action>
  <verify>
    <automated>pnpm vitest run packages/core/test/explore/rotation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run packages/core/test/explore/rotation.test.ts` green
    - Fixture test uses out-of-order dates and asserts the NEWEST-N are selected (proves sort, not slice-0)
    - Real-corpus case asserts `rotationSongIds(archive, 5).size === 56`
    - `rotationSongIds` importable from `@guezzer/core`
  </acceptance_criteria>
  <done>rotation.ts exists, barrel-exported, both fixture and real-corpus (56) guards green.</done>
</task>

</tasks>

<artifacts_produced>
New symbols/paths this plan creates (exclude from source-drift verification — they do not exist upstream):
- `deriveConstellation` (+ node/link output types) — `packages/core/src/explore/derive-constellation.ts`
- edge-threshold predicate helper — same file
- `rotationSongIds` — `packages/core/src/explore/rotation.ts`
- `rankOutgoing` (+ bars payload type) — `packages/core/src/explore/rank-outgoing.ts`
- `config.explore.*` (core: ROTATION_WINDOW_SHOWS, EDGE_COUNT_THRESHOLD_DEFAULT, EDGE_SLIDER_MIN/MAX, BARS_TOP_N)
- `config.explore.*` (app: NODE_RADIUS_MIN/MAX, NODE_HIT_MIN_RADIUS_PX, LABEL_ZOOM_THRESHOLD, COUNT_ZOOM_THRESHOLD, LABEL_AT_REST_TOP_K, LABEL_MAX_CHARS, DEX_DIM_OPACITY, FOCUS_DIM_OPACITY, SHEET_PEEK_FRACTION, ALPHA_DECAY, VELOCITY_DECAY, COOLDOWN_TICKS)
- `config.copy.explore.*` (barWhy, rotationHelper, edgeSliderLabel, sheetSubline, barsExpander, zero/empty/error strings)
- New test files under `packages/core/test/explore/`
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bundled artifact → core function | `transition-matrix.json` / `archive.json` are trusted build-time inputs, but carry kglw-derived (untrusted-origin) song names as data strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | core derivation functions | accept | Pure functions consume already-schema-guarded artifacts (loaders live in the app layer, Plan 07-02); core does no I/O and only reshapes trusted data. Song-name strings are carried as inert data, never evaluated. No high-severity threat at this tier. |

No network, no persistence, no auth, no user free-text input reaches these functions (slider is a bounded int, toggle is binary — both app-side). ASVS L1 satisfied by inherited schema guards + the read-only nature of this tier.
</threat_model>

<verification>
- `pnpm -w test --run` fully green (core `node` project includes the 3 new explore test files)
- `pnpm --filter @guezzer/core exec tsc --noEmit` passes (core stays React/DOM-free — no `window`/DOM/React import anywhere in `explore/`)
- Real-corpus guard proves the data-driven defaults: N=5 → 56 songs
</verification>

<success_criteria>
- Three pure explore functions exist, barrel-exported, fixture-tested green
- All Explore tunables centralized in the two config files with `[VERIFIED]`/`[ASSUMED]` discipline
- `copy.explore` holds every UI-SPEC string as interpolating templates
- Core purity preserved (no DOM/React), corpus guard (56) green
</success_criteria>

<output>
Create `.planning/phases/07-explore-mode-constellation/07-01-SUMMARY.md` when done.
</output>
