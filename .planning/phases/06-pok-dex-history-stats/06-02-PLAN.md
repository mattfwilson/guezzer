---
phase: 06-pok-dex-history-stats
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/app/src/show/FabMenu.tsx
  - packages/app/src/show/ActionBar.tsx
  - packages/app/src/show/ShowView.tsx
  - packages/app/src/show/PredictionOrb.tsx
  - packages/app/src/show/CenterNode.tsx
  - packages/app/src/show/orbLabelFit.ts
  - packages/app/src/components/InstallBanner.tsx
  - packages/app/src/pwa/install/useInstallState.ts
  - packages/app/src/config.ts
  - packages/app/src/styles.css
  - packages/app/test/fabMenu.test.tsx
  - packages/app/test/actionBar.test.tsx
  - packages/app/test/orbLabelFit.test.ts
  - packages/app/test/installBannerVersion.test.tsx
autonomous: true
requirements: [SHOW-02, SHOW-04, SHOW-05]
must_haves:
  truths:
    - "All five Show-Mode actions (Search, ???, Set break, Encore, Undo) live in a single collapsed-by-default FAB anchored bottom-right; the in-flow ActionBar rows are gone and the orbit absorbs the reclaimed height (D-20)"
    - "An expanded FAB menu sits over a scrim so a menu tap can never hit an orb; any action tap auto-collapses then runs the inherited Phase-4 behavior"
    - "Song names inside orbs are readable in full via wrap + scale-to-fit down to a config floor before ellipsis (D-21), with 44px hit targets untouched"
    - "The InstallBanner appears at most once per app version, keyed on the build version stamp (D-22)"
  artifacts:
    - path: "packages/app/src/show/FabMenu.tsx"
      provides: "Collapsed speed-dial replacing ActionBar"
      contains: "onUndo"
    - path: "packages/app/src/show/orbLabelFit.ts"
      provides: "Pure fitOrbLabel(name, diameterPx, opts) wrap/scale/ellipsis helper"
      contains: "fitOrbLabel"
    - path: "packages/app/src/config.ts"
      provides: "ui.FAB_DIAMETER, ui.FAB_ACTION_HEIGHT, show.ORB_LABEL_MAX_LINES, show.ORB_LABEL_MIN_FONT_PX constants"
      contains: "FAB_DIAMETER"
  key_links:
    - from: "packages/app/src/show/ShowView.tsx"
      to: "packages/app/src/show/FabMenu.tsx"
      via: "same onSearch/onUnknown/onSetBreak/onEncore/onUndo callback contract ActionBar had"
      pattern: "FabMenu"
    - from: "packages/app/src/components/InstallBanner.tsx"
      to: "db meta table"
      via: "installBannerSeenVersion flag keyed on __APP_VERSION__+__GIT_SHA__"
      pattern: "installBannerSeenVersion"
    - from: "packages/app/src/styles.css"
      to: "FabMenu gesture suppression"
      via: "touch-action/overscroll/user-select scope extended to .fab-menu"
      pattern: "fab-menu"
---

<objective>
The folded Show-Mode polish pass, all three owner-decided items: collapse the five Show-Mode actions into a bottom-right FAB speed-dial (D-20 — owner chose max orbit space, extra tap on ???/Undo deliberately accepted 2026-07-14), make orb song names readable in full via wrap + scale-to-fit (D-21), and gate the InstallBanner to once per app version (D-22).

This slice's user outcome: *As a show-goer in the dark, I get a taller orbit with readable song names, reachable actions under one thumb, and no nagging banner on every reload.* The plan re-delivers the SHOW-02 tap-target floor and the SHOW-04/SHOW-05 search/??? surfaces in their new FAB home — action semantics, icons, and confirmation rules unchanged from 04-UI-SPEC (this supersedes Phase 4 decisions D-13..D-15 on ActionBar layout; note the supersession so the old layout isn't "restored").

Purpose: three owner-reported todos folded into Phase 6 by CONTEXT.md (full problem statements in `.planning/todos/pending/2026-07-11-orb-song-name-text-truncated-and-oversized.md`, `2026-07-14-install-banner-reappears-every-reload.md`, `2026-07-14-collapse-show-actions-into-fab-menu.md`).
Output: FabMenu replacing ActionBar, pure fitOrbLabel helper wired into PredictionOrb/CenterNode, version-gated InstallBanner, config constants, replacement tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-pok-dex-history-stats/06-CONTEXT.md
@.planning/phases/06-pok-dex-history-stats/06-RESEARCH.md
@.planning/phases/06-pok-dex-history-stats/06-PATTERNS.md
@.planning/phases/06-pok-dex-history-stats/06-UI-SPEC.md
@.planning/todos/pending/2026-07-11-orb-song-name-text-truncated-and-oversized.md
@.planning/todos/pending/2026-07-14-install-banner-reappears-every-reload.md
@.planning/todos/pending/2026-07-14-collapse-show-actions-into-fab-menu.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FabMenu replaces ActionBar (D-20)</name>
  <files>packages/app/src/show/FabMenu.tsx, packages/app/src/show/ActionBar.tsx, packages/app/src/show/ShowView.tsx, packages/app/src/config.ts, packages/app/src/styles.css, packages/app/test/fabMenu.test.tsx, packages/app/test/actionBar.test.tsx</files>
  <read_first>
    - packages/app/src/show/ActionBar.tsx (the exact callback-prop contract to preserve: onSearch/onUnknown/onSetBreak/onEncore/onUndo, lines ~24-35; min-h-11/min-w-11 + safe-area padding lines ~57-61; never-accent buttons)
    - packages/app/src/show/ShowView.tsx (ActionBar wiring ~355-361; the flex-column layout and the non-scrolling AppShell scroll=false seam; SuggestionStrip slot placement)
    - packages/app/src/styles.css (gesture-suppression scope lines ~27-34: `.orbit-stage, .action-bar` bound to touch-action/overscroll-behavior/user-select/-webkit-touch-callout)
    - packages/app/src/pwa/bottomOverlayInset.ts and packages/app/src/components/InstallBanner.tsx line ~24 (useBottomOverlayHeightRegistration idiom)
    - packages/app/src/config.ts (config.ui SUGGESTION_STRIP_HEIGHT ~line 79; config.show block structure; config.copy.show strings)
    - packages/app/test/actionBar.test.tsx (handler-spy render idiom — fabMenu.test.tsx replaces this file wholesale)
    - .planning/todos/pending/2026-07-14-collapse-show-actions-into-fab-menu.md (full problem statement + flagged seams)
  </read_first>
  <behavior>
    - Collapsed by default: only the 56px Plus FAB renders; no action buttons are in the accessibility tree.
    - Tap FAB → five action rows render (bottom→top: Undo, ???, Search, Set break, Encore — hottest nearest thumb per UI-SPEC) plus a scrim; the Plus glyph rotates to read as close.
    - Tapping the scrim or the FAB again collapses without firing any action.
    - Tapping any action row collapses the menu AND fires exactly its callback once (auto-collapse-then-act).
    - All five callbacks (onSearch/onUnknown/onSetBreak/onEncore/onUndo) are exercised by tests via vi.fn() spies.
  </behavior>
  <action>
    Create `packages/app/src/show/FabMenu.tsx` with the same five-callback prop contract as ActionBar (D-20 cites: full collapse of all five actions). Anatomy: a `position: fixed` bottom-right container; main button 56px diameter (`config.ui.FAB_DIAMETER`, new key, default 56), elevated surface `#17171F` (bg-elevated) + hairline border, `Plus` lucide glyph rotating 45° when open (CSS transition; instant under `prefers-reduced-motion`), `aria-label` from new `config.copy.show.fabLabel` = "Show actions". Bottom offset = `env(safe-area-inset-bottom)` + BottomTabBar height + `config.ui.SUGGESTION_STRIP_HEIGHT` (56) + sm gap (8px); right offset = md (16px) + `env(safe-area-inset-right)` — the FAB must never overlap the SuggestionStrip dismiss X (RESEARCH Pitfall 10). Expanded state: vertical speed-dial opening upward, five rows each ≥48px tall (`config.ui.FAB_ACTION_HEIGHT`, new key, default 48) with icon + Label-size text (icons inherited from Phase 4: Search, CircleHelp, Minus, Star, Undo2), plus a full-viewport scrim (`#0C0C10` at ~60% opacity) rendered under the menu that blocks and collapses on tap; menu container gets `role="dialog"`-appropriate semantics or an aria-expanded button pattern. NOT accent-gold anywhere (UI-SPEC reserved list). Add a `.fab-menu` selector to the existing styles.css gesture-suppression rule (same scope as `.orbit-stage, .action-bar`). In ShowView: remove the ActionBar rows from the flex column, render FabMenu instead (fixed, so the orbit stage absorbs the freed height automatically), keep the exact handler wiring, and re-verify the AppShell scroll=false seam and SuggestionStrip position still hold (the strip previously sat "directly above the ActionBar" — re-anchor it above the BottomTabBar/inset). Delete `ActionBar.tsx` and `actionBar.test.tsx`; create `fabMenu.test.tsx` replacing it (collapsed default, scrim blocks+collapses, auto-collapse-then-act, all five callbacks fire, aria-label from config copy). Add config keys `ui.FAB_DIAMETER: 56`, `ui.FAB_ACTION_HEIGHT: 48`, and `copy.show.fabLabel` (single-config rule).
  </action>
  <verify>
    <automated>npx vitest run --project @guezzer/app test/fabMenu.test.tsx</automated>
    <human-check>End-of-phase device gate: FAB thumb-reach in the dark on iPhone; no overlap with SuggestionStrip X or home indicator; orbit visibly taller.</human-check>
  </verify>
  <acceptance_criteria>
    - `packages/app/src/show/ActionBar.tsx` and `packages/app/test/actionBar.test.tsx` no longer exist; `FabMenu.tsx` and `fabMenu.test.tsx` do.
    - fabMenu.test.tsx asserts: collapsed default (action buttons absent), expand on FAB tap, scrim tap collapses without firing callbacks, each of the five actions fires its spy exactly once and collapses.
    - styles.css gesture-suppression rule includes `.fab-menu` (grep `fab-menu` in styles.css).
    - `config.ui.FAB_DIAMETER === 56` and `config.ui.FAB_ACTION_HEIGHT === 48`; no dimension literal is hardcoded in FabMenu (component reads config).
    - Full app suite green: `npx vitest run --project @guezzer/app`.
  </acceptance_criteria>
  <done>One collapsed FAB carries all five Show-Mode actions with Phase-4 semantics intact; the orbit owns the reclaimed vertical space.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Orb label fit — pure helper + orb/center wiring (D-21)</name>
  <files>packages/app/src/show/orbLabelFit.ts, packages/app/src/show/PredictionOrb.tsx, packages/app/src/show/CenterNode.tsx, packages/app/src/config.ts, packages/app/test/orbLabelFit.test.ts</files>
  <read_first>
    - packages/app/src/show/PredictionOrb.tsx (current truncate span lines ~75-77; orb diameter prop; dumb-component contract)
    - packages/app/src/show/CenterNode.tsx (center-node label rendering — larger type role, 3-line budget)
    - packages/app/src/show/orbitLayout.ts and packages/app/test/orbitLayout.test.ts (the pure-testable-helper idiom this mirrors)
    - packages/app/src/config.ts (config.show block lines ~33-58 where the new constants join)
    - .planning/todos/pending/2026-07-11-orb-song-name-text-truncated-and-oversized.md (owner problem statement + candidate approaches)
  </read_first>
  <behavior>
    - fitOrbLabel("Rattlesnake", 88, orbOpts) returns the base font size, 1 line, no ellipsis (short name fits).
    - A long name ("The Dripping Tap") wraps to 2 lines before any font shrink; a very long name shrinks toward the floor; a pathological name at the floor with over-budget lines ellipsizes.
    - Font size never returns below ORB_LABEL_MIN_FONT_PX (11 orb / 14 center); line count never exceeds ORB_LABEL_MAX_LINES (2 orb / 3 center).
    - Output is deterministic for identical inputs (pure fn).
  </behavior>
  <action>
    Create `packages/app/src/show/orbLabelFit.ts` exporting a pure `fitOrbLabel(name: string, diameterPx: number, opts: { baseFontPx: number; minFontPx: number; maxLines: number }): { fontPx: number; lines: string[]; ellipsized: boolean }`. Strategy per UI-SPEC Typography exception: estimate usable chord width per line inside the circle (a simple width heuristic — average character width factor of the 600-weight system font, e.g. ~0.58×fontPx — is acceptable; the fn is a layout heuristic, not a text measurer), greedily wrap on word boundaries up to maxLines, and if the name still overflows shrink fontPx uniformly (integer steps) down to minFontPx before ellipsizing the final line. Add config constants to `config.show`: `ORB_LABEL_MAX_LINES: 2`, `ORB_LABEL_MIN_FONT_PX: 11`, `ORB_LABEL_MAX_LINES_CENTER: 3`, `ORB_LABEL_MIN_FONT_PX_CENTER: 14` (UI-SPEC §Config; base sizes stay the existing role sizes: 14px orb Label, 20px center Heading). In PredictionOrb replace the single truncate span (lines ~75-77) with a multi-line render of `lines` at `fontPx` (weight 600 unchanged, leading-tight); in CenterNode do the same with the center constants. Do NOT touch orb diameters, tap targets, or layout — SHOW-02 floors (44px hit / 56px visual) are untouched; the full name remains available via the Info why-detail. Tests in orbLabelFit.test.ts pin the wrap/scale/floor/ellipsis boundaries with exact inputs and expected outputs.
  </action>
  <verify>
    <automated>npx vitest run --project @guezzer/app test/orbLabelFit.test.ts</automated>
    <human-check>End-of-phase device gate: real long titles (e.g. "The Dripping Tap", "Am I in Heaven?") readable inside orbs on-device.</human-check>
  </verify>
  <acceptance_criteria>
    - orbLabelFit.test.ts asserts: short-name passthrough, 2-line wrap before shrink, shrink stops at exactly opts.minFontPx, ellipsis only at floor+over-budget, determinism (two calls deep-equal).
    - PredictionOrb.tsx no longer contains the `truncate` single-span pattern for the song name; it maps `fitOrbLabel(...).lines`.
    - `config.show.ORB_LABEL_MAX_LINES === 2`, `ORB_LABEL_MIN_FONT_PX === 11`, `ORB_LABEL_MAX_LINES_CENTER === 3`, `ORB_LABEL_MIN_FONT_PX_CENTER === 14`.
    - Full app suite green (orbit layout/confidence tests unaffected).
  </acceptance_criteria>
  <done>Song names render in full via wrap + scale-to-fit with config-resident floors; SHOW-02 targets untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: InstallBanner once per app version (D-22)</name>
  <files>packages/app/src/components/InstallBanner.tsx, packages/app/src/pwa/install/useInstallState.ts, packages/app/test/installBannerVersion.test.tsx</files>
  <read_first>
    - packages/app/src/components/InstallBanner.tsx (current render conditions + useBottomOverlayHeightRegistration usage)
    - packages/app/src/pwa/install/useInstallState.ts (the deliberately session-only `dismissed` state lines ~38-40 — the Phase-3 D-05 decision this SUPERSEDES; keep its comment trail updated)
    - packages/app/src/show/EndShowDialog.tsx (the PERSIST_WARNING_SHOWN meta-flag gate idiom, lines ~38-69 — copy this shape)
    - packages/app/src/db/db.ts (getMeta/setMeta helpers, ~lines 210-212)
    - packages/app/vite.config.ts (__APP_VERSION__/__GIT_SHA__ globals lines ~23-27) and packages/app/src/vite-env.d.ts (their declarations)
    - .planning/todos/pending/2026-07-14-install-banner-reappears-every-reload.md
  </read_first>
  <behavior>
    - First render on a build stamp never seen: banner shows, and the meta flag installBannerSeenVersion is written with the current stamp.
    - Re-render with the same stamp already in meta: banner never appears.
    - Meta contains an OLD stamp (simulated prior version): banner shows again once.
    - Dismissing still hides it immediately within the session (existing behavior preserved on top of the version gate).
  </behavior>
  <action>
    Add a persisted once-per-version gate to InstallBanner using the EndShowDialog meta-flag idiom: define `const INSTALL_BANNER_SEEN_VERSION = "installBannerSeenVersion"` and `const buildStamp = ${__APP_VERSION__}+${__GIT_SHA__}` (template literal); in a guarded effect, read `getMeta<string>(INSTALL_BANNER_SEEN_VERSION)` — if it equals buildStamp, never show; otherwise allow the existing eligibility logic (platform/installed checks in useInstallState) to decide, and on first actual render write `setMeta(INSTALL_BANNER_SEEN_VERSION, buildStamp)`. Visuals and copy unchanged (D-22 is behavior-only). Update the useInstallState comment that documents session-only dismissal to note the Phase-6 supersession (D-22 deliberately replaces Phase-3 D-05's session-only choice — RESEARCH State-of-the-Art table; the permanent AppMenu Install entry remains the always-on fallback). Tests in installBannerVersion.test.tsx with fake-indexeddb meta: unseen stamp shows once + writes flag; same stamp suppressed; changed stamp re-shows. Mock the __APP_VERSION__/__GIT_SHA__ globals per the existing version.test.tsx approach.
  </action>
  <verify>
    <automated>npx vitest run --project @guezzer/app test/installBannerVersion.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - installBannerVersion.test.tsx passes its three scenarios (show-once+persist, same-version suppressed, new-version re-shows).
    - InstallBanner reads/writes the meta key `installBannerSeenVersion` (grep confirms) and no copy string changed.
    - useInstallState.ts contains an updated comment noting the D-22 supersession of the session-only dismissal.
    - Full app suite green.
  </acceptance_criteria>
  <done>The banner appears at most once per app version, persisted in the meta table; install remains reachable from the AppMenu.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Touch input → Show-Mode actions | Accidental/pass-through taps during a live show are the failure mode (data corruption via mis-logged songs) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering (accidental input) | FabMenu expanded state | mitigate | Full-viewport scrim under the open menu blocks orbit taps; auto-collapse-then-act ensures one tap = one action; gesture-suppression CSS scope extended to .fab-menu |
| T-06-05 | Denial of service (UI) | FAB anchor vs SuggestionStrip/home indicator | mitigate | Bottom offset computed from env(safe-area-inset-bottom) + tab bar + strip slot + gap per UI-SPEC; end-of-phase device check |
| T-06-06 | Repudiation (lost state) | InstallBanner meta write | accept | Meta flag write failure only re-shows a banner — harmless; never-throw idiom applies |
| T-06-SC | Tampering (supply chain) | package installs | mitigate | No new dependencies in this plan |
</threat_model>

<verification>
- `npx vitest run --project @guezzer/app` green (actionBar.test.tsx replaced by fabMenu.test.tsx, no orphaned imports).
- `npx tsc --noEmit` (workspace) green after ActionBar deletion.
</verification>

<success_criteria>
- D-20: five actions in one collapsed FAB; scrim; auto-collapse; 44px/48px floors; orbit taller.
- D-21: orb names wrap/scale to config floors before ellipsis; SHOW-02 untouched.
- D-22: banner once per version via meta flag keyed on the build stamp.
- SHOW-02/SHOW-04/SHOW-05 surfaces re-delivered in the FAB with unchanged semantics (supersedes Phase-4 D-13..D-15 layout decisions — recorded, not restored).
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

New symbols (exclude from source-drift verification — created here):
- `FabMenu` component — `packages/app/src/show/FabMenu.tsx` (replaces deleted `ActionBar.tsx`)
- `fitOrbLabel` pure helper — `packages/app/src/show/orbLabelFit.ts`
- Config keys: `ui.FAB_DIAMETER`, `ui.FAB_ACTION_HEIGHT`, `show.ORB_LABEL_MAX_LINES`, `show.ORB_LABEL_MIN_FONT_PX`, `show.ORB_LABEL_MAX_LINES_CENTER`, `show.ORB_LABEL_MIN_FONT_PX_CENTER`, `copy.show.fabLabel` — `packages/app/src/config.ts`
- Meta key constant `installBannerSeenVersion` — `packages/app/src/components/InstallBanner.tsx`
- CSS scope `.fab-menu` in the gesture-suppression rule — `packages/app/src/styles.css`
- New tests: `fabMenu.test.tsx`, `orbLabelFit.test.ts`, `installBannerVersion.test.tsx` (deleted: `actionBar.test.tsx`)
</artifacts_produced>

<output>
Create `.planning/phases/06-pok-dex-history-stats/06-02-SUMMARY.md` when done
</output>
