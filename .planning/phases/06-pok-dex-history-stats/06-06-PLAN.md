---
phase: 06-pok-dex-history-stats
plan: 06
type: execute
wave: 4
depends_on: ["06-03", "06-05"]
files_modified:
  - packages/app/src/dex/DexView.tsx
  - packages/app/src/dex/DexHeader.tsx
  - packages/app/src/dex/AlbumGrid.tsx
  - packages/app/src/dex/AlbumDetail.tsx
  - packages/app/src/dex/SongRow.tsx
  - packages/app/src/dex/TierBadge.tsx
  - packages/app/src/show/WhyDetail.tsx
  - packages/app/src/App.tsx
  - packages/app/src/config.ts
  - packages/app/test/dexView.test.tsx
  - packages/app/test/songRow.test.tsx
autonomous: true
requirements: [DEX-03, DEX-04, STAT-01, STAT-03, STAT-04]
must_haves:
  truths:
    - "Opening #/dex shows the collection: completion headline, rarest catch, show count, and a 2-column album-cover card grid sorted alphabetically with Miscellaneous and Covers pinned last (D-01/D-02/D-07)"
    - "Tapping an album opens its track-ordered song list: green check for caught, hollow circle + dimmed for unseen, stat sublines (Seen n× · last · gap), rarity or Debut candidate badges — and NO toggle affordance exists anywhere (D-05/D-06/D-08)"
    - "The Show-Mode why-detail shows corpus gap, play count, and last-played date (STAT-01), with the debut-candidate framing replacing percentages for zero-history songs (D-08)"
    - "Albums with zero catches render dimmed covers (40% opacity + grayscale); never-seen songs are readable, never hidden (B4)"
  artifacts:
    - path: "packages/app/src/dex/DexView.tsx"
      provides: "#/dex root: header + Albums|Shows segments + drill-in view state"
      contains: "DexHeader"
    - path: "packages/app/src/dex/TierBadge.tsx"
      provides: "Rarity/debut pill — word always rendered, color reinforcement only"
      contains: "Debut"
  key_links:
    - from: "packages/app/src/App.tsx"
      to: "packages/app/src/dex/DexView.tsx"
      via: "route === 'dex' branch replaces PlaceholderView"
      pattern: "DexView"
    - from: "packages/app/src/dex/DexView.tsx"
      to: "packages/app/src/dex/useDexStats.ts"
      via: "the 06-05 hook feeds every rendered number"
      pattern: "useDexStats"
---

<objective>
The phase's first big user-visible payoff: the Dex tab becomes the album-shelf Pokédex. DexView with header + Albums|Shows segments, the album card grid with covers, album drill-in with per-song derived state, tier/debut badges, and the STAT-01 why-detail extension in Show Mode — all rendered over the 06-05 data foundation (loaders + useDexStats + covers).

User story slice: *As a collector, I want to browse my collection album by album with honest per-song stats, so that I can see exactly what I've caught and what's left.* Implements D-01 (album card grid), D-02 (alpha sort), D-05 (display-only derived state), D-06 (dimmed silhouettes), D-07 (stats placement), D-08 (debut badges everywhere incl. Show-Mode why-detail); delivers DEX-03/DEX-04 rendering, STAT-01, STAT-03, STAT-04.

Scope notes: the Shows segment ships as its empty state only ("No shows yet" copy — list arrives in 06-09, Mark CTA in 06-08); the DexHeader Share CTA arrives in 06-11 (no dead buttons); useDexStats reads the three existing tables — the archiveShows cache table joins in 06-08 after the 06-07 migration.
Output: the dex/ component family, App mount, config copy, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-pok-dex-history-stats/06-CONTEXT.md
@.planning/phases/06-pok-dex-history-stats/06-RESEARCH.md
@.planning/phases/06-pok-dex-history-stats/06-PATTERNS.md
@.planning/phases/06-pok-dex-history-stats/06-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DexView + DexHeader + segment control + AlbumGrid (D-01/D-02/D-07)</name>
  <files>packages/app/src/dex/DexView.tsx, packages/app/src/dex/DexHeader.tsx, packages/app/src/dex/AlbumGrid.tsx, packages/app/src/App.tsx, packages/app/src/config.ts, packages/app/test/dexView.test.tsx</files>
  <read_first>
    - packages/app/src/settings/SettingsView.tsx (scrolling max-w-md view idiom, config-copy-only strings, min-h-11 controls, lucide icons)
    - packages/app/src/App.tsx (route switch lines ~47-53 — the "dex" PlaceholderView branch to replace; scroll prop line ~45 stays correct since DexView scrolls)
    - packages/app/src/config.ts (copy.show block structure lines ~133-182 — mirror for copy.dex; ui block for ALBUM_ART_DISPLAY_PX)
    - 06-UI-SPEC.md §Layout 1 (Dex tab stack) and §Copywriting Contract (exact strings: "caught", "Rarest catch: {song}", "{n} shows attended", "Albums"/"Shows", "No catches yet", "No shows yet", empty-state bodies)
    - packages/app/src/dex/useDexStats.ts + covers.ts (06-05)
  </read_first>
  <behavior>
    - Route #/dex renders DexView (PlaceholderView gone for dex): header with Display-size completion headline "{caught}/{total} · {pct}%" + "caught" caption, "Rarest catch: {song}" subline with tier badge, "{n} shows attended".
    - Segment control Albums | Shows: two ≥44px halves, active half accent-tinted, component state only (no hash-route change).
    - Albums segment: 2-column card grid alphabetical by title with Miscellaneous and Covers pinned last; each card shows the 80px cover (or initials placeholder) + name (2-line clamp) + {caught}/{total} tabular-nums tally; zero-catch albums dim covers 40% + grayscale.
    - Shows segment: the "No shows yet" empty state (list lands in 06-09).
    - Empty dex: "No catches yet" heading + body copy (the Mark CTA joins in 06-08).
  </behavior>
  <action>
    Create `DexView.tsx` as the #/dex root: internal view state `{ segment: "albums" | "shows"; openAlbumUrl: string | null }` (RESEARCH Pattern 6 — ROUTES allow-list untouched, no routing library), rendering DexHeader → segment control → AlbumGrid | shows-empty-state → AlbumDetail overlay when openAlbumUrl set (Task 2). Create `DexHeader.tsx` (dumb component over DexStats): Display 28px/600 `tabular-nums` headline, sublines per copy contract, generous xl rhythm; no Share CTA yet (06-11). Create `AlbumGrid.tsx` + inline AlbumCard: grid-cols-2 with sm gutters and md screen gutters; cards on `#17171F` elevated surface with hairline border; cover `<img>` at 80px (`config.dex.ALBUM_ART_DISPLAY_PX`, new app config key, default 80) via `coverUrlFor(slug)` with the initials-placeholder fallback (album initials in Heading/text-muted centered on the elevated surface, hairline border, same 80px footprint — no layout shift); album name Heading 2-line clamp; `{caught}/{total}` Label tabular-nums with a green `Check` when caught === total && total > 0; §B4 dimming (opacity-40 grayscale on the cover) when caught === 0; whole card is the ≥44px tap target opening the detail. Sort: albums alphabetically by title (D-02), then append Miscellaneous and Covers bucket pseudo-cards last (they render with initials placeholders). Replace the App.tsx dex PlaceholderView branch with `<DexView />`. Add `config.copy.dex` block with ALL strings from the UI-SPEC copy contract used this plan (header captions, segment labels, empty states) and `config.dex.ALBUM_ART_DISPLAY_PX: 80` — no hardcoded strings in components. Extend dexView.test.tsx: renders header numbers from a fixture snapshot; segment switch toggles content; grid sorts alphabetically with buckets last; fully-caught album shows the check; zero-catch album has the dim classes; empty-dex state renders "No catches yet".
  </action>
  <verify>
    <automated>npx vitest run --project @guezzer/app test/dexView.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - App.tsx renders DexView for route "dex" (PlaceholderView no longer receives "dex").
    - dexView.test.tsx asserts: headline text matches "{caught}/{total}" from fixture data; alphabetical order + buckets-last (queried card order); check on complete album; dimming classes on zero-catch album; segment toggle.
    - grep of packages/app/src/dex/*.tsx finds no user-facing string literal that isn't sourced from config.copy.dex (spot-check: "No catches yet" exists only in config.ts).
    - Full app suite green.
  </acceptance_criteria>
  <done>Opening the Dex tab shows the collection's face: completion headline, rarest catch, and the discography shelf.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AlbumDetail + SongRow + TierBadge + WhyDetail corpus stats (D-05/D-06/D-08, STAT-01/03/04)</name>
  <files>packages/app/src/dex/AlbumDetail.tsx, packages/app/src/dex/SongRow.tsx, packages/app/src/dex/TierBadge.tsx, packages/app/src/show/WhyDetail.tsx, packages/app/src/config.ts, packages/app/test/songRow.test.tsx</files>
  <read_first>
    - packages/app/src/show/SearchSheet.tsx (full-screen overlay idiom: role="dialog" aria-modal, min-h-11 rows, untrusted strings as React text)
    - packages/app/src/show/WhyDetail.tsx (current why-detail content — the STAT-01 extension point; how it receives its candidate/current song data)
    - packages/app/src/show/PredictionOrb.tsx (dumb-component prop contract)
    - packages/core/src/dex/rarity.ts (SongRarity: playCount, lastPlayedDate, corpusGap, tier) and derive-dex.ts (SongDexStats: sightings, lastSeenDate, personalGap)
    - 06-UI-SPEC.md §B3 (tier pill rendering rules), §B4 (unseen treatment), §Copywriting Contract (exact sublines: "Seen {n}× · last {Mon YYYY} · {g} of your shows ago", "Seen {n}× · last show" at gap 0, "Played {playCount}× all-time", "Debut candidate", "Never played live — no odds to fake.")
  </read_first>
  <behavior>
    - AlbumDetail: header (cover + name + tally + back control ≥44px) then track-ordered rows; slides over within #/dex (component state, prefers-reduced-motion → instant).
    - SongRow (caught): green Check + name + "Seen {n}× · last {Mon YYYY} · {g} of your shows ago" (gap 0 → "· last show") + TierBadge.
    - SongRow (unseen, has history): hollow hairline circle + dimmed name + "Played {playCount}× all-time" + TierBadge.
    - SongRow (debut candidate, inMatrix false): dimmed + "Debut candidate" badge, NO tier, NO percentage, detail line "Never played live — no odds to fake."
    - No element in any row toggles seen state (D-05 — informational rows; no onClick mutation anywhere).
    - TierBadge: Label-size pill, tier word always rendered, tier color text + 40%-opacity tier-color border, transparent fill; colors Common #A1A1AA / Uncommon #60A5FA / Rare #E879F9 / Legendary #F2C14E.
    - WhyDetail: gains a corpus-stats line (play count, last played, gap — STAT-01) for the focused song; when the song has zero live history it renders the Debut candidate badge + copy INSTEAD of any percentage (D-08).
  </behavior>
  <action>
    Create `TierBadge.tsx` taking `tier: RarityTier | "debut"` and rendering per §B3 (word + color reinforcement, never color-only; the two new hues #60A5FA/#E879F9 appear ONLY here — data semantics, never chrome). Create `SongRow.tsx` (dumb: props { track, songStats: SongDexStats | undefined, rarity: SongRarity | undefined }) implementing the three states above with `tabular-nums` sublines; format last-seen as "Mon YYYY" via Intl.DateTimeFormat; all strings from `config.copy.dex`. Create `AlbumDetail.tsx` rendering the drill-in over DexView (fixed overlay per SearchSheet idiom or an in-place slide — keep within #/dex state; back control returns to grid). Extend `WhyDetail.tsx`: add a muted stat line sourcing SongRarity from the module-memoized rarity index (import loadArchive + buildRarityIndex via a tiny shared helper in dex/ — do NOT rebuild the index per render) showing "Played {playCount}× · last {Mon YYYY} · gap {corpusGap}" for the detailed song; if the songId is absent from the index (zero live history — defensive: predictions come from the matrix so this is rare, but D-08 mandates the branch), render the Debut candidate badge + "Never played live — no odds to fake." and suppress the percentage line. Add the remaining copy strings to `config.copy.dex`. Tests in songRow.test.tsx: all three row states with exact subline strings from fixture stats; gap-0 variant; debut row has no tier badge and no "%" character anywhere; assert NO interactive toggle (row has no button with a mutation handler — the only button is the drill-in navigation where applicable); TierBadge renders the word for each tier.
  </action>
  <verify>
    <automated>npx vitest run --project @guezzer/app test/songRow.test.tsx</automated>
    <human-check>End-of-phase device gate: album drill-in readable in the dark; dimmed rows legible; badges readable without color.</human-check>
  </verify>
  <acceptance_criteria>
    - songRow.test.tsx asserts exact rendered sublines for caught/unseen/debut states from fixture data, the gap-0 phrasing, and absence of "%" on debut rows.
    - TierBadge renders "Legendary" (etc.) as text — a test queries by the word, not the color.
    - WhyDetail shows the corpus-stat line for a known song and the debut branch for an unknown songId (component test with mocked rarity index).
    - grep confirms SongRow has no seen-toggle handler (D-05): no db import, no write call in dex/SongRow.tsx.
    - Full app suite + `npx tsc --noEmit` green.
  </acceptance_criteria>
  <done>Per-song truth is visible everywhere: derived checkmarks, honest sublines, rarity words, and debut framing replacing fake precision in both the dex and Show Mode.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kglw.net-sourced strings (song/album/venue names in artifacts) → DOM | Untrusted strings render in dex surfaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-11 | Injection (XSS) | song/album names in DexView family | mitigate | React text rendering only — no dangerouslySetInnerHTML anywhere in dex/ (inherited T-04-05 rule; grep-verifiable) |
| T-06-13 | Tampering (state integrity) | seen/unseen state | mitigate | D-05 structural: no mutation affordance exists in any dex row; state is pure derivation (T-06-07 chain) |
| T-06-SC | Tampering (supply chain) | package installs | mitigate | No new dependencies in this plan |
</threat_model>

<verification>
- `npx vitest run --project @guezzer/app` green; `npx tsc --noEmit` green.
- Manual dev-server spot check: #/dex renders the shelf with committed covers.
</verification>

<success_criteria>
- DEX-04: completion %, rarest catch, show count, never-seen (dimmed rows) all visible and derived.
- DEX-03/D-05: zero write paths from dex rendering; counts recompute live on attendance changes.
- STAT-01/03/04: corpus stats in why-detail + personal gap sublines + debut framing shipping everywhere percentages would lie.
- D-01/02/06/07: album shelf per the UI contract.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 06)

New symbols (exclude from source-drift verification — created here):
- Components `DexView`, `DexHeader`, `AlbumGrid`, `AlbumDetail`, `SongRow`, `TierBadge` — `packages/app/src/dex/`
- Config: `config.dex.ALBUM_ART_DISPLAY_PX`, `config.copy.dex.*` block — `packages/app/src/config.ts`
- WhyDetail corpus-stats line + debut branch — `packages/app/src/show/WhyDetail.tsx`
- New test: `songRow.test.tsx`; extended: `dexView.test.tsx`
</artifacts_produced>

<output>
Create `.planning/phases/06-pok-dex-history-stats/06-06-SUMMARY.md` when done
</output>
