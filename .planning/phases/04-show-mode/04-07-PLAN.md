---
phase: 04-show-mode
plan: 07
type: execute
wave: 6
depends_on: [04-06]
files_modified:
  - packages/app/src/wakeLock.ts
  - packages/app/src/show/WakeLockNotice.tsx
  - packages/app/src/show/EndShowDialog.tsx
  - packages/app/src/show/ShowView.tsx
  - packages/app/src/styles.css
autonomous: false
requirements: [SHOW-12, SHOW-13, EVAL-04]
must_haves:
  truths:
    - "During an active show a screen wake lock is held and reacquired on visibilitychange; when unsupported (iOS <18.4 installed PWA) a calm fallback message shows instead of a silent failure (SHOW-12)"
    - "The orbit stage and action bar suppress pull-to-refresh, overscroll bounce, double-tap zoom, and text selection — usable one-handed in the dark (SHOW-13)"
    - "Weak-fan softening is applied live on the orbit when the top score < 0.15 (EVAL-04/D-10)"
    - "End Show finalizes the setlist to read-only via a confirm dialog and is required before starting the next night (D-04)"
  artifacts:
    - path: "packages/app/src/wakeLock.ts"
      provides: "acquire/verify-held/reacquire/release wake-lock helper"
      exports: ["acquireWakeLock"]
    - path: "packages/app/src/show/WakeLockNotice.tsx"
      provides: "once-per-show unsupported-fallback message"
    - path: "packages/app/src/show/EndShowDialog.tsx"
      provides: "End Show finalize confirm"
      contains: "endShow"
  key_links:
    - from: "packages/app/src/show/ShowView.tsx"
      to: "packages/app/src/wakeLock.ts"
      via: "acquire on active show + reacquire on visibilitychange"
      pattern: "acquireWakeLock|visibilitychange"
    - from: "packages/app/src/show/EndShowDialog.tsx"
      to: "packages/app/src/db/db.ts endShow"
      via: "confirm → finalize to read-only"
      pattern: "endShow"
---

<objective>
Complete the dark-venue survivability layer: the Wake Lock helper (acquire + verify the sentinel actually held + reacquire on visibilitychange + calm fallback when unsupported), gesture suppression CSS on the stage/action bar, the live application of weak-fan softening on the orbit, and the End Show finalize flow. Closes SHOW-12 (the highest-risk item — Wake Lock silently fails on iOS <18.4 installed PWAs), SHOW-13, the EVAL-04 softening expression, and D-04.

Purpose: Make the whole loop usable one-handed in a dark crowded venue without the screen sleeping or a fat thumb triggering a scroll/zoom, and give the user an explicit, confirmed end to the night.
Output: wakeLock.ts + WakeLockNotice + gesture CSS + softening wire-up + EndShowDialog, plus the mandatory on-device spike checkpoint (STATE.md blocker).
</objective>

## Phase Goal (MVP user story)

**As a** KGLW showgoer in a dark venue, **I want to** keep the screen awake, tap without triggering scroll/zoom, and end the show explicitly, **so that** the app never stalls, dims, or mis-fires under my thumb all night.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-show-mode/04-RESEARCH.md
@.planning/phases/04-show-mode/04-PATTERNS.md
@.planning/phases/04-show-mode/04-UI-SPEC.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wake Lock helper + reacquire + WakeLockNotice fallback</name>
  <files>packages/app/src/wakeLock.ts, packages/app/src/show/WakeLockNotice.tsx, packages/app/src/show/ShowView.tsx</files>
  <read_first>
    - .planning/phases/04-show-mode/04-RESEARCH.md §Pitfall 1 (HIGHEST RISK — feature-detect AND verify the sentinel held; treat immediate release/rejection as unsupported; reacquire on visibilitychange while show active; the acquire/reacquire code sketch) + §Environment (Wake Lock runtime device-dependent)
    - .planning/phases/04-show-mode/04-PATTERNS.md "wakeLock.ts" analog (persist.ts — silent-on-failure browser-API wrapper, feature-detect + try/catch + never throw + idempotent)
    - packages/app/src/pwa/persist.ts (the requestPersistenceOnce structural analog — read directly)
    - .planning/phases/04-show-mode/04-UI-SPEC.md §Copywriting (wake-lock fallback copy; muted, dismissible, once per show) — string lives in config.copy.show from 04-01
    - packages/app/src/show/ShowView.tsx (04-04/06 — active-show branch where the lock is acquired/released)
  </read_first>
  <action>
    Create `wakeLock.ts` mirroring persist.ts: `acquireWakeLock(onUnsupported)` — feature-detect `"wakeLock" in navigator`; `request("screen")` in try/catch; VERIFY the returned sentinel did not immediately fire `release` / is live (iOS <18.4 installed-PWA false-positive, Pitfall 1) — treat an immediate release or rejection as unsupported and call `onUnsupported`. Add a `release()` and a module-level `visibilitychange` reacquire (on `visibilityState === "visible"` while the show is active and no sentinel held). Never throw. Create `WakeLockNotice.tsx`: the muted, dismissible, once-per-show fallback message (copy.show wake-lock string) shown only when `onUnsupported` fired. Wire ShowView: acquire on entering the active-show branch, release on End Show / leaving, and register the reacquire; render WakeLockNotice when unsupported. Reacquire is silent (no copy) per UI-SPEC.
  </action>
  <verify>
    <automated>npx tsc -p packages/app --noEmit && npx vitest run packages/app</automated>
    <human-check>SHOW-12 is manual/device (VALIDATION manual-only — no reliable jsdom test). Covered by the Task 3 device checkpoint.</human-check>
  </verify>
  <acceptance_criteria>
    - Source assertion: wakeLock.ts feature-detects `"wakeLock" in navigator` AND verifies the sentinel held (does not trust presence alone — Pitfall 1).
    - Source assertion: a `visibilitychange` reacquire path exists and only fires while a show is active.
    - Source assertion: the helper never throws (try/catch, silent-on-failure like persist.ts); fallback copy comes from config.copy.show.
    - `npx tsc -p packages/app --noEmit` passes; `npx vitest run packages/app` green.
  </acceptance_criteria>
  <done>Wake lock is acquired-and-verified, reacquired on visibility change, and degrades to a calm once-per-show notice when unsupported.</done>
</task>

<task type="auto">
  <name>Task 2: Gesture suppression CSS + live weak-fan softening + End Show dialog</name>
  <files>packages/app/src/styles.css, packages/app/src/show/EndShowDialog.tsx, packages/app/src/show/ShowView.tsx</files>
  <read_first>
    - .planning/phases/04-show-mode/04-RESEARCH.md §Pattern 7 (gesture CSS: touch-action manipulation, overscroll-behavior none, user-select/-webkit-user-select none, -webkit-touch-callout none on stage+action bar; html/body overscroll-behavior-y none) + §Pitfall 5 (stage must be non-scrolling — confirm the 04-04 AppShell seam holds) + §Pattern 8 (weak-fan softening: top score < WEAK_FAN_THRESHOLD → reduce fill opacity ~55% + desaturate + muted hint)
    - .planning/phases/04-show-mode/04-UI-SPEC.md §Layout (gesture suppression rules) + §Copywriting "Destructive actions" (End Show confirm heading/body/buttons) + §Color (weak-fan softening)
    - packages/app/src/styles.css (existing @theme + safe-area handling — extend, don't duplicate tokens)
    - packages/app/src/show/OrbitStage.tsx + confidence.ts (04-03 — isWeakFan already computed; ensure it is applied to orb rendering here if not already live)
    - packages/app/src/db/db.ts (endShow from 04-01)
  </read_first>
  <action>
    Add gesture-suppression CSS (RESEARCH Pattern 7) scoped to the orbit stage + action bar classes: `touch-action: manipulation`, `overscroll-behavior: none`, `user-select: none` (+ `-webkit-user-select`, `-webkit-touch-callout: none`); set `html, body { overscroll-behavior-y: none }`. Confirm the orbit stage is a non-scrolling fixed region (the 04-04 AppShell seam) — the comet-trail strip may scroll horizontally but the stage never scrolls/rubber-bands. Ensure weak-fan softening is applied live: OrbitStage passes `isWeakFan` to orbs (reduced opacity + desaturate) and renders the muted "Low confidence · Wide-open moment" hint (copy.show) when the top score < config.show.WEAK_FAN_THRESHOLD (D-10/EVAL-04) — the honest small % still renders, no faked numbers. Create `EndShowDialog.tsx`: confirm dialog (heading "End show?", body + destructive-styled "End show" / "Keep tracking" per UI-SPEC) that on confirm calls `endShow(sessionId)` (status → finalized, read-only) — required before starting the next night (D-03/D-04). Wire the End Show control (header, per UI-SPEC region 1) to open it.
  </action>
  <verify>
    <automated>npx tsc -p packages/app --noEmit && npx vitest run packages/app</automated>
    <human-check>Gesture suppression (SHOW-13) is manual/device per VALIDATION — covered by the Task 3 checkpoint.</human-check>
  </verify>
  <acceptance_criteria>
    - Source assertion: styles.css sets `touch-action: manipulation`, `overscroll-behavior: none`, and `user-select: none` on the stage/action-bar scope, plus `overscroll-behavior-y: none` on html/body.
    - Source assertion: the weak-fan hint + orb softening are gated on `config.show.WEAK_FAN_THRESHOLD` and still render the honest % (no renormalization/faked number).
    - Source assertion: EndShowDialog calls `endShow` only after an explicit confirm (D-04) and finalizes to read-only.
    - `npx tsc -p packages/app --noEmit` passes; `npx vitest run packages/app` green.
  </acceptance_criteria>
  <done>Gestures are suppressed on the non-scrolling stage, weak fans soften honestly, and End Show finalizes the setlist to read-only behind a confirm.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: On-device Wake Lock + gesture spike (STATE.md blocker)</name>
  <files>packages/app/src/wakeLock.ts, packages/app/src/styles.css, packages/app/src/show/ShowView.tsx</files>
  <what-built>Wake Lock acquire/verify/reacquire + fallback notice (Task 1), gesture suppression + weak-fan softening + End Show (Task 2). SHOW-12 and SHOW-13 are device-only per 04-VALIDATION (no reliable jsdom assertion).</what-built>
  <how-to-verify>
    1. Build + preview the PWA (`npm run build` then serve/preview), and INSTALL it to the home screen on the oldest iOS device in the friend group (the STATE.md-flagged spike — test the installed PWA, not a Safari tab).
    2. Start a show. Confirm the screen stays awake for several minutes without manual interaction. If the device is pre-iOS-18.4, confirm the calm "Keep your screen on manually…" fallback appears ONCE (not a silent dim).
    3. Background the app (home/app-switch) and return. Confirm the wake lock reacquires silently (screen stays awake again).
    4. Over the orbit stage: attempt pull-to-refresh, double-tap-zoom, and long-press text-select — NONE should fire. Confirm the stage does not scroll/rubber-band.
    5. Log a sparse moment (or force a weak fan) and confirm the orbs soften + the "Low confidence" hint shows while the honest % still renders.
    6. Tap End Show → confirm dialog → the setlist finalizes read-only.
    Also run the full suite green: `npm test`.
  </how-to-verify>
  <action>
    Present the built/installed PWA for on-device validation of SHOW-12 (installed-PWA wake lock hold + silent reacquire + pre-18.4 fallback) and SHOW-13 (gesture suppression on the non-scrolling stage), plus weak-fan softening and End Show finalize. Record the tested iOS/Android versions and the oldest friend-group device in the SUMMARY (resolves the STATE.md iOS-lifecycle blocker). This is a blocking human checkpoint — no auto-advance.
  </action>
  <verify>
    <human-check>All six how-to-verify steps pass on the oldest available installed-PWA device; `npm test` is green. Tester records device/iOS versions.</human-check>
  </verify>
  <done>On-device wake lock (or fallback), silent reacquire, gesture suppression, weak-fan softening, and End Show all confirmed on a real installed PWA; device/iOS versions recorded.</done>
  <resume-signal>Type "approved" once the installed-PWA wake lock, reacquire, gesture suppression, and End Show all behave (note the tested iOS/device versions), or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wake Lock / Page Visibility API → app | Device-API results that can fail silently (iOS <18.4 installed PWA) |
| touch input → orbit stage | Fat-thumb gestures that must not scroll/zoom/select |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-16 | Denial of Service (mid-show failure) | wakeLock.ts | mitigate | Feature-detect AND verify the sentinel held; try/catch never throws; fallback notice guarantees the loop never bricks when the API fails (ASVS V7 — dominant concern). |
| T-04-17 | Denial of Service (accidental gesture) | gesture CSS | mitigate | Declarative `touch-action`/`overscroll-behavior`/`user-select` on the non-scrolling stage — no passive-listener footguns (SHOW-13). |
| T-04-18 | Repudiation / data loss | End Show finalize | mitigate | Explicit confirm before `endShow`; finalized shows are read-only, preventing accidental mid-show ending (D-04). |
</threat_model>

<verification>
- `npm test` full suite green.
- `npx tsc -p packages/app --noEmit` passes.
- On-device SHOW-12 (installed-PWA wake lock + reacquire + fallback) and SHOW-13 (gesture suppression) confirmed via the Task 3 checkpoint (manual-only per VALIDATION, justified).
</verification>

<success_criteria>
The screen stays awake during an active show (or a calm fallback shows on unsupported devices), reacquires on return, gestures are suppressed on the non-scrolling stage, weak fans soften honestly, and End Show finalizes to read-only. The full loop is usable one-handed in a dark venue.
</success_criteria>

## Artifacts this phase produces (this plan)

- **Helper:** `acquireWakeLock` / `release` / visibilitychange-reacquire (`packages/app/src/wakeLock.ts`)
- **Components:** `WakeLockNotice` (once-per-show fallback), `EndShowDialog` (finalize confirm)
- **CSS:** gesture-suppression rules on the orbit stage + action bar (`styles.css`)
- **Wiring:** wake-lock lifecycle + live weak-fan softening + End Show control in `ShowView`

<output>
Create `.planning/phases/04-show-mode/04-07-SUMMARY.md` when done.
</output>
