{
  "name": "@guezzer/relay",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "GizzMap group relay — a single Cloudflare Worker + one SQLite Durable Object per friend group. Stores ONLY client-side-encrypted envelopes it cannot read; TTL-purges beacons (12h) and pins (48h) so no location history ever exists.",
  "scripts": {
    "dev": "npx wrangler@latest dev",
    "deploy": "npx wrangler@latest deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260620.0"
  }
}
