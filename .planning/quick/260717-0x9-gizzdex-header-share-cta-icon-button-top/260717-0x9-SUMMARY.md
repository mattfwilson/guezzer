---
quick_id: 260717-0x9
title: GizzDex header — Share icon-button top-right + brighter segment toggle
status: complete
date: 2026-07-17
commit: ed96dd8
---

# Summary — Quick Task 260717-0x9

Implements + resolves todo
`2026-07-17-gizzdex-header-share-icon-button-and-brighter-segment-toggle`.

## Changed

- **`packages/app/src/dex/DexHeader.tsx`** — the completion headline
  (`{caught}/{total} · {pct}%`) and an **icon-only Share button** now share a top
  `flex items-start justify-between` row; the old full-width bottom Share CTA is
  gone. Icon button: `Share2` in accent gold, `bg-accent/15 rounded-full`,
  ≥44px hit target (`min-h-11 min-w-11`), `aria-label={shareCopy.cta}` so the
  accessible name ("Share card") is preserved.
- **`packages/app/src/dex/DexView.tsx`** — Albums/Shows toggle selected state
  brightened from `bg-accent/20 text-text-primary` (dim tint) to
  `bg-accent text-surface` (solid accent `#f2c14e`, dark text). Comment updated.

## Verification

- `npx tsc -p packages/app/tsconfig.json --noEmit` → exit 0.
- `npx vitest run` → **481 passed / 66 files**.
- Ran the app (dev server + Playwright): Share button is icon-only with
  `aria-label="Share card"`; active Albums toggle computed bg = `rgb(242,193,78)`
  (= `#f2c14e` full accent); zero console errors. Screenshot confirms the icon
  top-right by the `%` and the solid-gold active toggle.

## Notes / decisions

- "Brighter" interpreted as the FULL existing accent gold (not a new hotter-orange
  token). If the owner wants a distinct orange, that's a follow-up color-token
  change.
- Making the active segment a solid accent fill is a stronger accent use than the
  06-UI-SPEC §Color-A tint — an owner override, kept consistent with the single
  accent icon control now on the dex face.

## Out of scope

Tier colors, routes/storage.
