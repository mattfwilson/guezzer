---
phase: 06-pok-dex-history-stats
plan: 07
type: execute
wave: 5
depends_on: ["06-01", "06-06"]
files_modified:
  - packages/core/src/data-safety/export-schema.ts
  - packages/core/src/data-safety/serialize.ts
  - packages/core/src/data-safety/merge.ts
  - packages/core/src/config.ts
  - packages/core/src/index.ts
  - packages/app/src/db/db.ts
  - packages/app/src/settings/exportDownload.ts
  - packages/app/src/settings/SettingsView.tsx
  - packages/app/src/config.ts
  - packages/core/test/serialize.test.ts
  - packages/core/test/merge.test.ts
  - packages/app/test/retroMark.test.ts
  - packages/app/test/exportImportRoundtrip.test.ts
autonomous: true
requirements: [SHAR-01, DEX-02]
must_haves:
  truths:
    - "The export envelope is v2: it carries an owner name and archiveShows cache rows; a v1 file imports cleanly via the MIGRATIONS chain (owner null, archiveShows empty)"
    - "Dexie version(4) adds ONLY the archiveShows cache table — additive, no rewrite of prior tables"
    - "markShowAttended writes attendedShows (+ the archiveShows setlist cache row in the SAME transaction when a fallback setlist is supplied); unmarkShowAttended is a plain delete — derivation makes it free (D-11/D-12)"
    - "An online-fallback-marked show survives export → import → reload with its sightings intact (Pitfall 5 round-trip)"
    - "The user can set an owner name in Settings (meta row); exports carry it (D-17's fork key, consumed in 06-10)"
  artifacts:
    - path: "packages/core/src/data-safety/export-schema.ts"
      provides: "exportEnvelope v2: owner (length-clamped, nullable) + archiveShows rows"
      contains: "archiveShowRow"
    - path: "packages/core/src/data-safety/merge.ts"
      provides: "MIGRATIONS v1→v2 slot + archiveShows union-merge by show_id"
      contains: "archiveShows"
    - path: "packages/app/src/db/db.ts"
      provides: "version(4) archiveShows table + markShowAttended/unmarkShowAttended helpers + snapshot round-trip"
      contains: "markShowAttended"
  key_links:
    - from: "packages/app/src/db/db.ts markShowAttended"
      to: "attendedShows + archiveShows tables"
      via: "single rw transaction (Pitfall 5)"
      pattern: "db.transaction"
    - from: "packages/app/src/settings/exportDownload.ts"
      to: "meta ownerName row"
      via: "owner read into the v2 envelope"
      pattern: "ownerName"
    - from: "packages/core/src/data-safety/merge.ts"
      to: "packages/core/src/data-safety/export-schema.ts"
      via: "v1 files migrated forward before merge"
      pattern: "MIGRATIONS"
---

<objective>
The envelope-v2 atomic cluster (RESEARCH Pattern 4: schema + serialize + merge + db.ts + exportDownload move in ONE plan — the row types are pinned across the core/app boundary and `tsc --noEmit` fails on any partial change): owner name on the export envelope (D-17's fork key), the `archiveShows` setlist cache (Pitfall 5 — online-fallback marks must survive reload and backup round-trips), Dexie `version(4)`, the retro mark/unmark write helpers, and the Settings owner-name field (RESEARCH Open Question 2 resolution: meta row `ownerName`).

Slice contribution: *As a dex owner, I want my backup to carry everything — including shows marked from the live archive — and my name, so that restores are lossless and friends' files are distinguishable from mine.* Delivers the SHAR-01 envelope half (the compare fork consumes it in 06-10) and the DEX-02 persistence substrate (the ArchiveBrowser consumes the helpers in 06-08).

Purpose: without the cache table + envelope field shipping together, an online-fallback mark credits zero sightings after reload and the backup silently loses catches — the exact data-loss class Phase 5 existed to kill.
Output: envelope v2, MIGRATIONS[v1→v2], Dexie v4, mark/unmark helpers, owner-name Settings field, round-trip tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-pok-dex-history-stats/06-CONTEXT.md
@.planning/phases/06-pok-dex-history-stats/06-RESEARCH.md
@.planning/phases/06-pok-dex-history-stats/06-PATTERNS.md
@.planning/phases/05-live-sync-data-safety/05-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Envelope v2 — owner + archiveShows + MIGRATIONS chain (core)</name>
  <files>packages/core/src/data-safety/export-schema.ts, packages/core/src/data-safety/serialize.ts, packages/core/src/data-safety/merge.ts, packages/core/src/config.ts, packages/core/src/index.ts, packages/core/test/serialize.test.ts, packages/core/test/merge.test.ts</files>
  <read_first>
    - packages/core/src/data-safety/export-schema.ts (existing strictObject rows + the header's cross-boundary type-pinning contract lines ~17-25 — enum-pinned unions MUST stay assignable to db.ts Table types)
    - packages/core/src/data-safety/merge.ts (the wired-but-empty MIGRATIONS array line ~43 + version loop lines ~98-108 + newer-version rejection ~90-96 — indexed by SOURCE version: MIGRATIONS[n] upgrades n → n+1)
    - packages/core/src/data-safety/serialize.ts (verbatim-passthrough idiom lines ~43-55)
    - packages/core/test/merge.test.ts + serialize.test.ts (existing fixtures to EXTEND, not replace)
    - packages/core/src/dex/archive-types.ts (06-01 set-shape vocabulary the archiveShowRow mirrors)
  </read_first>
  <behavior>
    - exportEnvelope v2 parses { schemaVersion: 2, ..., owner: string|null, archiveShows: [...] } and rejects an owner longer than the clamp or an archiveShows row with an extra key.
    - A v1 envelope (no owner/archiveShows keys) run through parseAndMergeImport with currentSchemaVersion 2 migrates to { owner: null, archiveShows: [] } and merges cleanly.
    - archiveShows union-merge by show_id: local + incoming rows union, local wins on collision, no local row dropped.
    - A file with schemaVersion 3 is still rejected (newer-version protection intact).
  </behavior>
  <action>
    In `export-schema.ts`: add `export const archiveShowRow = z.strictObject({ show_id: z.number().int(), date: z.string(), venueName: z.string(), city: z.string(), sets: z.array(z.strictObject({ n: z.enum(["1","2","e"]), songs: z.array(z.strictObject({ songId: z.number().int(), songName: z.string() })) })) })` — this row shape is the db.ts `ArchiveShowRow` mirror (Task 2 declares the interface; enum-pin `n` exactly like setNumber). Add to `exportEnvelope`: `owner: z.string().max(config.dex.OWNER_NAME_MAX_LENGTH).nullable()` and `archiveShows: z.array(archiveShowRow)`. Add `OWNER_NAME_MAX_LENGTH: 40` to `config.dex` in core config (ASVS V5 length clamp, doc-commented). In `serialize.ts`: extend `ExportSnapshot` with `owner: string | null` and `archiveShows` (typed from the schema), passthrough both verbatim. In `merge.ts`: implement the v1→v2 migration in the MIGRATIONS chain following ITS OWN indexing convention (read the loop first — RESEARCH: `MIGRATIONS[1] = (e) => ({ ...e, owner: null, archiveShows: [] })` with index 0 unused): a v1 file gains owner null + archiveShows []. Add archiveShows to the union-merge with key `show_id` (local wins on collision, mirroring attendedShows). Owner on merge of MY OWN file: keep LOCAL owner (meta is device-local; the envelope owner is a fork key, not merged state). Barrel-export `archiveShowRow`. Tests: EXTEND serialize.test.ts (v2 round-trip: serializeExport output parses; owner/archiveShows carried verbatim; over-length owner rejected) and merge.test.ts (v1 fixture migrates to owner null + archiveShows [] and merges; archiveShows union by show_id with no local drop; schemaVersion 3 still rejected; extra-key archiveShows row rejected whole-file).
  </action>
  <verify>
    <automated>npx vitest run --project @guezzer/core test/serialize.test.ts test/merge.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - serialize.test.ts: v2 envelope round-trip passes; `owner` of length OWNER_NAME_MAX_LENGTH+1 rejected by parse.
    - merge.test.ts: v1→v2 migration test passes (owner null, archiveShows []); archiveShows union-merge keeps every local row; newer-version rejection test still green.
    - `archiveShowRow.sets[].n` rejects "3" (enum pin asserted).
    - Full core suite green.
  </acceptance_criteria>
  <done>The envelope is v2 with the migration chain's first real entry; old backups import losslessly, new backups carry owner + fallback setlists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dexie version(4) + archiveShows cache + mark/unmark helpers (app)</name>
  <files>packages/app/src/db/db.ts, packages/app/src/config.ts, packages/app/test/retroMark.test.ts, packages/app/test/exportImportRoundtrip.test.ts</files>
  <read_first>
    - packages/app/src/db/db.ts (version(1)-(3) history — version(3) additive idiom lines ~180-200; startShow rw-transaction idiom lines ~235-263; deleteEntry ~333-335; DbSnapshot ~141-146; snapshot()/importSnapshot ~434-450 incl. the meta/attendedShows upsert branch vs clear-and-rewrite branch)
    - packages/core/src/data-safety/export-schema.ts (Task 1's archiveShowRow — the interface this mirrors; enum vocabulary)
    - packages/app/test/db.test.ts and migrationV3.test.ts (fake-indexeddb test idioms)
    - packages/app/test/exportImportRoundtrip.test.ts (existing round-trip test to EXTEND)
    - 06-RESEARCH.md Pattern 3 (cache table written ONLY by fallback marks) and Pitfall 5
  </read_first>
  <behavior>
    - version(4) adds archiveShows keyed "&show_id"; existing tables and data untouched (migration test: v3-shaped data survives opening at v4).
    - markShowAttended({ show_id, showDate }) writes one attendedShows row; markShowAttended with a cachedSetlist also writes the archiveShows row — both in ONE transaction (a forced failure writes neither).
    - unmarkShowAttended(show_id) deletes the attendedShows row and any archiveShows cache row; no other table is touched.
    - snapshot() includes archiveShows + owner (from meta ownerName, null when unset); importSnapshot bulkPuts archiveShows (upsert branch — stable &show_id key).
    - Round-trip: mark a fallback show with a setlist → snapshot → clear DB → importSnapshot → the archiveShows row is back (Pitfall 5 killed).
  </behavior>
  <action>
    In db.ts: declare `export interface ArchiveShowRow { show_id: number; date: string; venueName: string; city: string; sets: Array<{ n: SetNumber; songs: Array<{ songId: number; songName: string }> }> }` (reusing the existing SetNumber union — the core schema enum-pins to it, Task 1). Add `this.version(4).stores({ archiveShows: "&show_id" })` — additive only, NO .upgrade (new table). Add the table property + typing. Add module-level helpers mirroring startShow's transaction idiom: `markShowAttended(input: { show_id: number; showDate: string; cachedSetlist?: ArchiveShowRow }): Promise<void>` — `db.transaction("rw", db.attendedShows, db.archiveShows, ...)` putting the attendance stub and, when cachedSetlist is provided (online-fallback marks ONLY — corpus-era marks pass nothing, the bundled archive has their setlists), putting the cache row; `unmarkShowAttended(show_id: number): Promise<void>` deleting from both tables (plain deletes — D-12, derivation recomputes everything). Extend `DbSnapshot` + `snapshot()` + `importSnapshot()` with `archiveShows` (bulkPut upsert branch, NOT clear-and-rewrite) and `owner` (snapshot reads `getMeta<string>("ownerName")` → null when unset; importSnapshot does NOT write owner into meta — owner is a fork key). Bump `config.dataSafety.SCHEMA_VERSION` to 2 in app config. Tests: retroMark.test.ts (fake-indexeddb): mark corpus-era (attendedShows only, archiveShows empty), mark fallback (both rows, atomically), unmark removes both, double-mark same show_id is idempotent (&show_id upsert). Extend exportImportRoundtrip.test.ts with the Pitfall-5 case above plus owner round-trip (set meta ownerName → snapshot().owner equals it).
  </action>
  <verify>
    <automated>npx vitest run --project @guezzer/app test/retroMark.test.ts test/exportImportRoundtrip.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - retroMark.test.ts passes all four scenarios (corpus mark, fallback mark atomic pair, unmark both-table delete, idempotent re-mark).
    - exportImportRoundtrip.test.ts asserts a fallback-marked show's archiveShows row survives snapshot→clear→import, and owner round-trips.
    - `config.dataSafety.SCHEMA_VERSION === 2` in app config.
    - `npx tsc --noEmit` green (the enum-pinned cross-boundary contract holds: core archiveShowRow rows assign to Table<ArchiveShowRow>).
    - Full app suite green (migrationV3 tests unaffected).
  </acceptance_criteria>
  <done>Retro marks persist losslessly — corpus-era via the v1-era attendedShows stub, fallback-era via the atomic cache pair — and the backup carries all of it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Owner identity in Settings + v2 export assembly</name>
  <files>packages/app/src/settings/exportDownload.ts, packages/app/src/settings/SettingsView.tsx, packages/app/src/config.ts, packages/app/test/exportImportRoundtrip.test.ts</files>
  <read_first>
    - packages/app/src/settings/exportDownload.ts (snapshot assembly lines ~37-53 — where owner + archiveShows join the serializeExport call)
    - packages/app/src/settings/SettingsView.tsx (useLiveQuery(getMeta) idiom lines ~36-39; section/control styling; min-h-11)
    - packages/app/src/db/db.ts (getMeta/setMeta; Task 2's snapshot() owner read)
    - 06-UI-SPEC.md §Copywriting (voice) — the owner-name field label copy goes in config.copy
  </read_first>
  <behavior>
    - Settings shows a "Your name" text field persisted to meta key ownerName (useLiveQuery-reactive, saved on blur/change); input is ≥16px (iOS form-zoom rule).
    - Export with ownerName set produces an envelope whose owner equals it; unset → owner null.
    - The exported envelope has schemaVersion 2 and parses through the core exportEnvelope.
    - The field clamps input length to config.dex.OWNER_NAME_MAX_LENGTH (maxLength attribute + trim).
  </behavior>
  <action>
    In exportDownload.ts: extend the snapshot assembly to include `archiveShows: await db.archiveShows.toArray()` and `owner` from `getMeta<string>("ownerName")` (trimmed, null when empty) — or delegate to Task 2's snapshot() if the existing assembly already routes through it (read first; keep one assembly path, no duplication). In SettingsView.tsx: add an identity section with a labeled text input bound to the meta row via the existing getMeta/setMeta + useLiveQuery idiom, `maxLength` from `config.dex.OWNER_NAME_MAX_LENGTH` (add the mirror constant to app config or import the core value — single source: import from @guezzer/core config), label + helper copy in a new `config.copy.settings` addition (e.g. label "Your name", helper "Stamped on your exports so friends' files stay separate."). Auto-download at End Show (exportBackup) inherits the same assembly — verify it compiles with the new fields. Extend exportImportRoundtrip.test.ts: a full export via the exportDownload assembly path parses through exportEnvelope with schemaVersion 2 and the meta-set owner.
  </action>
  <verify>
    <automated>npx vitest run --project @guezzer/app test/exportImportRoundtrip.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The Settings field writes meta ownerName (test: change event → getMeta returns the value).
    - Export envelope test asserts schemaVersion 2, owner from meta, archiveShows array present.
    - Owner input enforces the length clamp (maxLength attribute asserted).
    - Full app suite + tsc green.
  </acceptance_criteria>
  <done>Exports are v2 and owner-stamped; the D-17 fork key exists end-to-end for plan 06-10.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Imported JSON (own backup or friend file) → core/DB | v2 envelope crosses the trust boundary; new fields widen the attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-14 | Tampering (oversized/crafted input) | owner + archiveShows fields | mitigate | strictObject rows, enum-pinned set vocabulary, `.max(OWNER_NAME_MAX_LENGTH)` clamp on owner; whole-file rejection before any merge (existing D-12 gate) |
| T-06-15 | Denial of service (data loss) | fallback-mark persistence | mitigate | Atomic two-table transaction; archiveShows in the envelope + upsert import branch; round-trip test pins it |
| T-06-16 | Spoofing | owner field as identity | accept | Owner is a display/fork label for ≤10 trusted friends, not authentication; rendered as escaped text + length-clamped (06-10 renders it) |
| T-06-SC | Tampering (supply chain) | package installs | mitigate | No new dependencies in this plan |
</threat_model>

<verification>
- `npx vitest run` (both projects) green.
- `npx tsc --noEmit` green — the atomic-cluster contract (core schema ↔ db.ts types) holds.
</verification>

<success_criteria>
- SHAR-01 (envelope half): v2 export/import with owner + archiveShows; v1 files migrate forward losslessly.
- DEX-02 (persistence half): mark/unmark helpers exist, atomic, idempotent; fallback marks survive reload and backup round-trips.
- Additive-only rule honored: version(4) adds one table, touches nothing prior.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 07)

New symbols (exclude from source-drift verification — created here):
- `archiveShowRow` (zod) + envelope fields `owner`, `archiveShows` — `packages/core/src/data-safety/export-schema.ts`
- `MIGRATIONS` v1→v2 entry — `packages/core/src/data-safety/merge.ts`
- `ExportSnapshot.owner` / `.archiveShows` fields — `packages/core/src/data-safety/serialize.ts`
- `config.dex.OWNER_NAME_MAX_LENGTH` — `packages/core/src/config.ts`
- `ArchiveShowRow` interface, Dexie `version(4)` `archiveShows` table, `markShowAttended`, `unmarkShowAttended`, extended `DbSnapshot`/`snapshot`/`importSnapshot` — `packages/app/src/db/db.ts`
- `config.dataSafety.SCHEMA_VERSION = 2`, `config.copy.settings.*` owner-field strings — `packages/app/src/config.ts`
- Meta key `ownerName`; Settings identity field — `packages/app/src/settings/SettingsView.tsx`
- New test: `retroMark.test.ts`; extended: `serialize.test.ts`, `merge.test.ts`, `exportImportRoundtrip.test.ts`
</artifacts_produced>

<output>
Create `.planning/phases/06-pok-dex-history-stats/06-07-SUMMARY.md` when done
</output>
