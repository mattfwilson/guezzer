---
phase: 21-layout-layering-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/app/src/dev/layerRepro.tsx
  - packages/app/src/dev/LayoutProbe.tsx
  - packages/app/src/App.tsx
  - packages/app/test/layerRepro.test.tsx
  - .planning/phases/21-layout-layering-foundations/21-HUMAN-UAT.md
autonomous: true
requirements: [FOUND-01, FOUND-03]

must_haves:
  truths:
    - "Loading the app with ?layerRepro=1 in a desktop browser shows a persistent band at the toast tier that any sheet or the FabMenu can be opened over"
    - "Loading the app with ?layoutProbe=1 shows a selectable text readout of every number the FOUND-01 diagnosis needs, capturable in one screenshot"
    - "A normal load (no query flag) renders neither harness and behaves exactly as it does today"
    - "21-HUMAN-UAT.md exists with all 8 manual-only verifications scaffolded and un-run"
  artifacts:
    - path: "packages/app/src/dev/layerRepro.tsx"
      provides: "?layerRepro=1 gate + the repro toast band"
      exports: ["isLayerReproEnabled", "LayerReproToast"]
    - path: "packages/app/src/dev/LayoutProbe.tsx"
      provides: "?layoutProbe=1 gate + the FOUND-01 measurement readout"
      exports: ["isLayoutProbeEnabled", "LayoutProbe", "readSafeAreaInsets"]
    - path: "packages/app/test/layerRepro.test.tsx"
      provides: "inert-unless-explicit assertions for both flags"
    - path: ".planning/phases/21-layout-layering-foundations/21-HUMAN-UAT.md"
      provides: "the 8 manual-only verifications in the 10-HUMAN-UAT.md format"
      contains: "## Current Test"
  key_links:
    - from: "packages/app/src/App.tsx"
      to: "packages/app/src/dev/layerRepro.tsx"
      via: "conditional render guarded by isLayerReproEnabled()"
      pattern: "isLayerReproEnabled\\(\\)"
    - from: "packages/app/src/App.tsx"
      to: "packages/app/src/dev/LayoutProbe.tsx"
      via: "conditional render guarded by isLayoutProbeEnabled()"
      pattern: "isLayoutProbeEnabled\\(\\)"
---

<objective>
Build the two diagnostic harnesses D-30 puts first, and scaffold the phase's device-test
document. `?layerRepro=1` reproduces the FOUND-03 stacking-context defect in a desktop browser;
`?layoutProbe=1` produces the FOUND-01 before/after measurement. Neither changes shipped
behavior on a normal load.

Purpose: D-30 fixes the order — "the repro is browser-only and costs nothing, and its result
sizes the layering work"; D-14 requires the FOUND-01 diagnosis before any fix. Both harnesses
gate work in later waves, so they ship first. D-14 also notes the probe "pays for itself twice":
it is both the diagnosis and the before/after evidence.

Output: two dev-only modules behind URL flags, a test proving they are inert on a normal load,
and `21-HUMAN-UAT.md` carrying all 8 manual-only verifications from `21-VALIDATION.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-layout-layering-foundations/21-CONTEXT.md
@.planning/phases/21-layout-layering-foundations/21-RESEARCH.md
@.planning/phases/21-layout-layering-foundations/21-PATTERNS.md
@.planning/phases/21-layout-layering-foundations/21-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ?layerRepro=1 — the FOUND-03 repro harness (D-29)</name>
  <files>packages/app/src/dev/layerRepro.tsx, packages/app/src/App.tsx</files>
  <read_first>
    - packages/app/src/live/mockLatest.ts (lines 1-30 and 95-105) — the exact URL-flag idiom to copy: header comment with `Safety:` paragraph, `typeof location === "undefined"` guard first, `URLSearchParams`, exact-value equality against a literal, inert `null` on the normal path
    - packages/app/src/App.tsx (lines 85-125) — the `#app-content` `display: contents` wrapper and the five toast siblings of `AppShell`; the new harness mounts as a sixth sibling
    - packages/app/src/config.ts §`ui.z` (lines ~240-297) — `toast: 20`, `content: 10`, `fabScrim: 25`, `fab: 30`, `sheet: 50`
    - packages/app/src/components/UpdateToast.tsx — the shipped toast geometry the repro band imitates
    - packages/app/src/dev/OrbFitHarness.tsx — the `src/dev/` dev-route precedent
  </read_first>
  <action>
    Create `packages/app/src/dev/layerRepro.tsx` exporting `isLayerReproEnabled(): boolean` and
    a `LayerReproToast` component.

    `isLayerReproEnabled` copies `mockLatest.ts`'s gate shape exactly, in this order: return
    `false` when `typeof location === "undefined"`; read the flag via
    `new URLSearchParams(location.search).get("layerRepro")`; return `flag === "1"` using exact
    string equality — never truthiness, never a regex over `location.search`, and never render
    any query-string content. Header doc comment states TEST HARNESS ONLY, cites D-29 and
    FOUND-03, explains that it force-shows a toast-tier band so a sheet or the FabMenu can be
    opened over it on demand, and carries a `Safety:` paragraph mirroring
    `mockLatest.ts:1-20`. Include the "read once per call — the flag can't change without a
    reload, so callers may cache at module/mount scope" note.

    `LayerReproToast` renders a persistent, non-dismissable band with the same geometry the
    shipped toasts use — `fixed inset-x-0 bottom-16 flex items-center border-t border-hairline
    bg-elevated px-4 py-4` — and inline `style={{ zIndex: config.ui.z.toast }}` (never a
    Tailwind `z-*` class). Its text is a fixed literal naming the harness, e.g. "layerRepro:
    toast tier 20". It is NOT `pointer-events-none` — the whole point is that it can eat taps
    from a surface nested inside the `content: 10` stacking context.

    In `App.tsx`, render `{isLayerReproEnabled() && <LayerReproToast />}` as a sibling of
    `AppShell`, immediately after `<WaveToast />`, so it lands in the root stacking context
    exactly as the real toasts do. Cache the flag result in a module-scope or `useMemo` const
    per the mockLatest note.

    Note for the D-12 guard author (plan 21-10): `src/dev/**` is excluded from the bottom-space
    source guard by name, precisely as `dev/OrbFitHarness.tsx` is already the one permitted
    Tailwind `z-*` site. Record that in this file's header comment so the exclusion is not read
    later as an oversight.
  </action>
  <verify>
    <automated>cd packages/app && npx vitest run --project @guezzer/app test/layerRepro.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `packages/app/src/dev/layerRepro.tsx` exports both `isLayerReproEnabled` and `LayerReproToast`
    - The file contains the literal `new URLSearchParams(location.search).get("layerRepro")` and the literal comparison `=== "1"`
    - The file contains `typeof location === "undefined"` on the line before the URLSearchParams read
    - The file contains no `innerHTML`, no `dangerouslySetInnerHTML`, and no interpolation of any `URLSearchParams` value into rendered output
    - `App.tsx` contains `isLayerReproEnabled()` guarding a `<LayerReproToast />` render, placed after `<WaveToast />`
    - `grep -n 'zIndex' packages/app/src/dev/layerRepro.tsx` shows `config.ui.z.toast`, and `grep -c 'z-\[' packages/app/src/dev/layerRepro.tsx` returns 0
    - `npm test` passes with no new failures against the 954-test baseline
  </acceptance_criteria>
  <done>A desktop load of `?layerRepro=1` shows the band; a load with no query string renders nothing new and the full suite is green.</done>
</task>

<task type="auto">
  <name>Task 2: ?layoutProbe=1 — the FOUND-01 diagnostic readout (D-14)</name>
  <files>packages/app/src/dev/LayoutProbe.tsx, packages/app/src/App.tsx, packages/app/test/layerRepro.test.tsx</files>
  <read_first>
    - packages/app/src/dev/layerRepro.tsx (created in Task 1) — reuse its gate shape verbatim
    - packages/app/src/styles.css (lines 204-224) — `body` padding-bottom/left/right and the UX-01 top-inset NOTE comment; the probe measures the consequence of line 220
    - packages/app/src/components/AppShell.tsx (lines 60-85) — the `<main>` element and its `paddingBottom` ternary the probe reads back
    - packages/app/src/components/BottomTabBar.tsx (lines 15-30) — the `<nav>` and its first button, whose `getBoundingClientRect().top` is one half of the gap subtraction
    - packages/app/src/live/mockLatest.ts (lines 95-105) — the gate idiom
  </read_first>
  <action>
    Create `packages/app/src/dev/LayoutProbe.tsx` exporting `isLayoutProbeEnabled(): boolean`,
    `readSafeAreaInsets(): { top: number; right: number; bottom: number; left: number }`, and a
    `LayoutProbe` component.

    `isLayoutProbeEnabled` copies the Task-1 gate exactly, keyed on `layoutProbe` and comparing
    `=== "1"`.

    `readSafeAreaInsets` uses the probe-element technique (RESEARCH §Measuring on device): create
    a detached `div`, set `style.paddingTop/Right/Bottom/Left` to the four
    `env(safe-area-inset-*)` strings, append it to `document.body`, read the four values back
    via `parseFloat(getComputedStyle(el).padding*)`, then `el.remove()`. Guard the whole
    function with `typeof document === "undefined"` returning zeroes.

    `LayoutProbe` renders a `fixed` overlay positioned at the TOP of the viewport (`top-0
    left-0 right-0`) so it never occludes the bottom gap being photographed, with
    `zIndex: config.ui.z.sheet`, `bg-surface/95`, `text-[11px]`, `leading-tight`,
    `font-mono`, `user-select: text` and `pointer-events-auto` so the numbers are selectable.
    It recomputes on mount, on `resize`, on `orientationchange`, and on
    `visualViewport` `resize` (all listeners removed on unmount), and renders one labelled line
    per field:

      - `sab` / `sat` / `sal` / `sar` — the four insets from `readSafeAreaInsets()`
      - `standalone` — `navigator.standalone === true` and
        `matchMedia("(display-mode: standalone)").matches`, printed as two separate booleans
      - `innerH` — `window.innerHeight`
      - `clientH` — `document.documentElement.clientHeight`
      - `vvH` — `window.visualViewport?.height ?? null`
      - `htmlH` / `bodyH` / `rootH` / `mainH` — `offsetHeight` of `document.documentElement`,
        `document.body`, `document.getElementById("root")`, `document.querySelector("main")`
      - `bodyH-rootH` — the single falsifiable equality D-15 predicts equals `sab`
      - `mainBottom` — `getBoundingClientRect().bottom` of `<main>`
      - `tabTop` — `getBoundingClientRect().top` of the first `<button>` inside
        `document.querySelector("nav")`
      - `GAP` — `tabTop - mainBottom`, rendered last and prefixed so it is unmissable. This is
        the number that goes in `21-HUMAN-UAT.md`.
      - `dpr` — `window.devicePixelRatio`
      - `htmlFont` — computed `font-size` of `document.documentElement`
      - `orient` — `screen.orientation?.type ?? (innerWidth > innerHeight ? "landscape" : "portrait")`
      - `ua` — `navigator.userAgent`, rendered as escaped React text (it is device-supplied, not
        query-supplied)

    Every DOM lookup must tolerate a missing element (render `n/a`, never throw).

    In `App.tsx`, render `{isLayoutProbeEnabled() && <LayoutProbe />}` as a sibling of
    `AppShell`, after the `LayerReproToast` line.

    Extend `packages/app/test/layerRepro.test.tsx` (created here, covering both flags) to assert
    the inert-unless-explicit contract for both gates.
  </action>
  <verify>
    <automated>cd packages/app && npx vitest run --project @guezzer/app test/layerRepro.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `packages/app/test/layerRepro.test.tsx` asserts, for BOTH flags: absent query string → gate returns `false`; `?layerRepro=0` / `?layoutProbe=0` → `false`; `?layerRepro=true` → `false` (exact-value equality, not truthiness); `?layerRepro=1` / `?layoutProbe=1` → `true`
    - The test asserts `readSafeAreaInsets()` returns four numbers and never throws under jsdom (where all four resolve to 0)
    - `LayoutProbe` rendered in jsdom with no `<main>`/`<nav>`/`#root` present renders without throwing and shows `n/a` for the missing measurements
    - `grep -c 'GAP' packages/app/src/dev/LayoutProbe.tsx` returns ≥ 1
    - `App.tsx` contains both `isLayerReproEnabled()` and `isLayoutProbeEnabled()` guards
    - `npm test` passes with no new failures
  </acceptance_criteria>
  <done>Both flags are inert on a normal load, proven by test; `?layoutProbe=1` prints every field the FOUND-01 diagnosis needs including `bodyH-rootH` and `GAP`.</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold 21-HUMAN-UAT.md with all 8 manual-only verifications</name>
  <files>.planning/phases/21-layout-layering-foundations/21-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/10-pre-show-validation-device-dry-run/10-HUMAN-UAT.md — the exact format: YAML frontmatter (`status`, `phase`, `source`, `started`, `updated`), a live `## Current Test` section, `### N. Title (REQ-ID)` per test with bare `expected: |` / `result: |` block scalars (NOT code fences), and a `## Harness` section
    - .planning/phases/21-layout-layering-foundations/21-VALIDATION.md §Manual-Only Verifications — the 8 rows, verbatim
    - .planning/phases/21-layout-layering-foundations/21-UI-SPEC.md §Device Verification — the 6 numbered device checks and their "why it cannot be a browser check" column
    - .planning/phases/21-layout-layering-foundations/21-CONTEXT.md D-18, D-19, D-42
  </read_first>
  <action>
    Create `.planning/phases/21-layout-layering-foundations/21-HUMAN-UAT.md` following the
    `10-HUMAN-UAT.md` format exactly.

    Frontmatter: `status: pending`, `phase: 21-layout-layering-foundations`,
    `source: [21-VALIDATION.md, 21-UI-SPEC.md]`, `started` = today's ISO timestamp,
    `updated` = same.

    `## Current Test` names the next open item: tests 1-before and 7, which gate plans 21-07
    and 21-11 respectively.

    `## Tests` carries eight entries, `### N. Title (REQ-ID)`, each with a bare `expected: |`
    block scalar and an empty `result: |` block scalar reading `PENDING`:

      1. FOUND-01 gap, **before** and **after**, portrait **and** landscape, installed instance
         (FOUND-01). `expected` states: install to the home screen over the HTTPS tunnel; load
         `?layoutProbe=1`; screenshot the readout in portrait and again in landscape; record
         `sab`, `bodyH-rootH`, and `GAP` for each. State the CONFIRMATION BRANCH (`bodyH-rootH
         === sab` and `GAP === sab` ⇒ D-15 confirmed, proceed to remove `styles.css:220`) and
         the FALSIFICATION BRANCH (`GAP === 0` ⇒ D-19 applies, the measurement IS the evidence,
         no code change required; `GAP != sab` and `GAP != 0` ⇒ D-15 falsified, fall back to the
         D-14 open investigation). Record device model, OS version and orientation (D-18).
      2. The `bottom-16` overlay overlap (FOUND-02, D-09). Trigger `BingoCelebration` and
         `WaveToast` on the installed instance and confirm each clears the tab-bar buttons;
         after the plan-21-10 conversion, confirm `InstallBanner`/`UpdateToast`/`BackupToast`
         gained no internal dead space.
      3. Tab strip at the largest Dynamic Type setting (NAV-01, D-04). iOS Settings → Display →
         Text Size to max; confirm all five labels fit on one line, no clip, no wrap.
      4. SearchSheet with the soft keyboard up (FOUND-03, D-17). Fix only if the reserved
         arithmetic misbehaves.
      5. Real share-card PNG at the widest realistic venue name (FOUND-05, D-36/D-37). Confirm
         the date is never truncated, the venue ellipsizes, and the footer baseline does not clip.
      6. Two devices on different builds, both directions (NAV-03, D-42). Pre-rename build on
         device A, post-rename on device B, two identities, over the HTTPS tunnel.
      7. Live paint order over a real toast (FOUND-03, D-29). Desktop browser, `?layerRepro=1`,
         open SearchSheet and the FabMenu over the band; record which surface the band paints
         over and whether it eats taps.
      8. SearchSheet gesture suppression after portaling (FOUND-03, D-23). On device, confirm
         double-tap zoom and long-press callout are still suppressed inside the portaled sheet.

    `## Harness` records the reproducible serve/tunnel steps: `npm run build --workspace
    packages/app`, `npx vite preview --workspace packages/app`, and the cloudflared tunnel with
    the **mandatory** `--http-host-header localhost` flag (vite preview 403s without it) — cite
    MEMORY `device-uat-hosting`. Add a sub-section for test 6 explaining how to serve two builds
    (check out the pre-rename commit into a second worktree, build, and serve on a second port
    with its own tunnel).
  </action>
  <verify>
    <automated>test -f .planning/phases/21-layout-layering-foundations/21-HUMAN-UAT.md &amp;&amp; grep -c '^### [1-8]\.' .planning/phases/21-layout-layering-foundations/21-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - The file exists and `grep -c '^### [1-8]\.'` returns exactly 8
    - `grep -c 'expected: |' file` returns 8 and `grep -c 'result: |' file` returns 8
    - No `expected`/`result` block is wrapped in a fenced code block (```)
    - Frontmatter contains `status: pending`, `phase: 21-layout-layering-foundations`, `source:`, `started:`, `updated:`
    - `grep -c '## Current Test' file` returns 1 and `grep -c '## Harness' file` returns 1
    - `grep -c 'http-host-header localhost' file` returns ≥ 1
    - Test 1's `expected` contains all three branch labels: CONFIRMATION BRANCH, FALSIFICATION BRANCH, and an explicit D-19 clause
  </acceptance_criteria>
  <done>All 8 manual-only verifications from 21-VALIDATION.md are carried as numbered tests in the Phase-10 format, with branch instructions and the tunnel harness recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL query string → app | Attacker-controllable text arrives via `location.search` on any load |
| Device/UA → probe readout | `navigator.userAgent` and computed styles are device-supplied strings rendered to the DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Elevation of Privilege | `dev/layerRepro.tsx`, `dev/LayoutProbe.tsx` | mitigate | Inert-unless-explicit gate copied from `mockLatest.ts:97-101`: `typeof location === "undefined"` guard, `URLSearchParams` (never regex), exact-value equality `=== "1"` (never truthiness). The flag toggles a boolean only (ASVS V12). |
| T-21-02 | Tampering | `dev/layerRepro.tsx`, `dev/LayoutProbe.tsx` | mitigate | No query-string value is ever rendered. Both components render fixed literals plus device-derived numbers; no `innerHTML`, no `dangerouslySetInnerHTML`. Asserted by the Task-1/2 acceptance criteria. |
| T-21-03 | Information Disclosure | `dev/LayoutProbe.tsx` | accept | The readout prints `navigator.userAgent` and viewport geometry. These are already available to any script on the page and the overlay is only reachable behind an explicit flag on a personal tool with no product users. |
| T-21-SC | Tampering | npm installs | mitigate | Zero packages added, removed or upgraded this plan (`21-RESEARCH.md` §Package Legitimacy Audit). No install task exists; `package.json` and lockfile are not in `files_modified`. |
</threat_model>

<verification>
- `npm test` green (baseline: 125 files / 954 tests / ~7.7s) plus the new `layerRepro.test.tsx` cases
- `npm run build --workspace packages/app` succeeds (the type gate — no `typecheck` script exists)
- A normal `npm run dev` load renders no band and no probe overlay
</verification>

<success_criteria>
- `?layerRepro=1` reproducibly shows a `toast: 20` band in a desktop browser
- `?layoutProbe=1` prints `sab`, `bodyH-rootH` and `GAP` as selectable text in one screenshot
- Both flags are proven inert on a normal load by automated test
- `21-HUMAN-UAT.md` exists with 8 scaffolded tests and the tunnel harness
</success_criteria>

<artifacts_this_phase_produces>
Symbols, files and flags newly created by Phase 21 (all plans). Nothing below exists on `master`
before this phase.

**CSS custom properties (new):** `--gz-safe-bottom`, `--gz-tab-bar-h`, `--gz-overlay-inset`,
`--gz-chrome-reserve`, `--gz-content-reserve`, `--gz-sheet-pad-bottom`, `--gz-fab-offset`

**Config keys (new):** `config.ui.bottomSpace`, `config.ui.bottomSpace.TAB_BAR_HEIGHT_REM`,
`config.ui.bottomSpace.FAB_CLEARANCE_PX`, `config.ui.bottomSpace.SHEET_PAD_BOTTOM_PX`,
`config.copy.tabs` (`show`/`explore`/`map`/`dex`/`games`), `config.copy.presence.activity`,
`config.copy.presence.activityUnknown`

**New modules and exports:** `packages/app/src/layout/bottomSpace.ts`
(`BOTTOM_SPACE_VAR_NAMES`, `bottomSpaceVarEntries`, `applyBottomSpaceVars`,
`useBottomSpaceVars`); `packages/app/src/dex/formatDate.ts` (`formatFullDate`, plus the re-homed
`formatMonYear`); `packages/app/src/dev/layerRepro.tsx` (`isLayerReproEnabled`,
`LayerReproToast`); `packages/app/src/dev/LayoutProbe.tsx` (`isLayoutProbeEnabled`,
`LayoutProbe`, `readSafeAreaInsets`); `packages/app/src/dex/FriendRow.tsx`
(`presenceActivityLabel`)

**Changed signatures:** `showBottomFabOffset(stripSlotReserved: boolean)` (was
`stripHasContent`); `FabMenu` / `OrbitStage` prop `stripSlotReserved` (was `stripHasContent`);
`data-strip-slot-reserved` DOM attribute (was `data-strip-has-content`); `PresenceActivitySlot`
gains an `online?: boolean` prop

**URL flags (new):** `?layerRepro=1`, `?layoutProbe=1`

**New test files:** `packages/app/test/layerRepro.test.tsx`,
`packages/app/test/bottomSpace.test.ts`, `packages/app/test/layerOrder.test.tsx`,
`packages/app/test/formatDate.test.ts`, `packages/app/test/presenceLabels.test.ts`,
`packages/app/test/setlistView.test.tsx`

**New docs:** `.planning/phases/21-layout-layering-foundations/21-HUMAN-UAT.md`,
`.planning/todos/pending/2026-07-24-simultaneous-bottom-overlay-stacking.md`

**Removed by this phase:** `packages/app/src/dex/formatMonYear.ts` (renamed to `formatDate.ts`);
the `padding-bottom: env(safe-area-inset-bottom)` declaration on `body` in `styles.css`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/21-layout-layering-foundations/21-01-SUMMARY.md` when done.
</output>
