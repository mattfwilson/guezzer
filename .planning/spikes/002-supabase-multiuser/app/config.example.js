// ── Spike config ────────────────────────────────────────────────────────────
// Copy this file to `config.local.js` (same folder) and paste your own values.
// Find both in the Supabase dashboard → Project Settings → API.
// `config.local.js` is gitignored — your keys never get committed.
//
// The anon key is safe to expose in a browser (that's its job); we keep it out
// of git only so the repo doesn't carry YOUR project's keys.
export const SUPABASE_URL = "https://YOUR-PROJECT.supabase.co";
export const SUPABASE_ANON_KEY = "your-anon-public-key-here";
