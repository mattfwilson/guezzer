---
phase: 05-live-sync-data-safety
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/app/src/db/db.ts
  - packages/app/src/config.ts
  - packages/app/test/migrationV3.test.ts
autonomous: true
requirements: [SYNC-01, SYNC-02, PWA-04]
must_haves:
  truths:
    - "The Dexie schema upgrades to version(3) additively — v1/v2 data survives untouched (D-03 additive-only rule)"
    - "Existing trackedEntries backfill source:'manual' on upgrade so no entry has an undefined source"
    - "trackedShows gains venueId/venueName/city binding columns and a showId index for reconciliation (D-07)"
    - "adoptSuggestion logs an editor song write-through stamping source:'editor' and the classified hit/miss outcome (D-03)"
    - "bindShow writes the show_id/venue binding onto the provisional row without altering status (D-07)"
    - "importSnapshot commits a merged snapshot in one rw transaction, never partially (D-12/Pitfall 5)"
    - "config.live, config.ui, config.dataSafety, and config.copy.live/settings exist as single-source constants (CLAUDE.md)"
  artifacts:
    - path: "packages/app/src/db/db.ts"
      provides: "version(3) migration + EntrySource type + adoptSuggestion/bindShow/importSnapshot helpers"
      contains: "this.version(3)"
    - path: "packages/app/src/config.ts"
      provides: "live/ui/dataSafety constant blocks + copy.live/copy.settings strings"
      contains: "POLL_INTERVAL_MS"
  key_links:
    - from: "packages/app/src/db/db.ts adoptSuggestion"
      to: "logSong write-through"
      via: "an add stamping source and derived outcome"
      pattern: "source"
    - from: "packages/app/src/db/db.ts importSnapshot"
      to: "Dexie rw transaction bulkPut"
      via: "atomic commit of the merged snapshot"
      pattern: "transaction"
---

<objective>
Extend the app persistence layer for Phase 5 with a purely additive Dexie `version(3)` migration (entry provenance + tracked-show venue/show_id binding), the write helpers the live-sync and data-safety UIs will call (`adoptSuggestion`, `bindShow`, `importSnapshot`), plus every Phase-5 tunable and copy string in the single app config file. This is the shared app substrate both Wave-2 UI slices depend on.

Purpose: D-03 (source provenance for an honest, decomposable tally), D-07 (binding seam), D-10/D-12 (atomic import write), and the single-config-file ethos (CLAUDE.md, SYNC-01 ≤1/60s floor + copy).
Output: modified `db.ts` (+ interfaces + helpers), modified `config.ts`, and a fake-indexeddb migration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-live-sync-data-safety/05-CONTEXT.md
@.planning/phases/05-live-sync-data-safety/05-RESEARCH.md
@.planning/phases/05-live-sync-data-safety/05-PATTERNS.md
@.planning/phases/05-live-sync-data-safety/05-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dexie version(3) additive migration + write helpers</name>
  <files>packages/app/src/db/db.ts, packages/app/test/migrationV3.test.ts</files>
  <read_first>
    - packages/app/src/db/db.ts (the WHOLE file: version(1)/version(2) blocks lines 111-124 — copy the additive pattern; TrackedEntry + TrackedShow interfaces; logSong lines 192-216, renameEntry lines 257-269, markSetBreak line 231, endShow — the transaction("rw",…) idiom the new helpers follow)
    - packages/app/test/db.test.ts (fake-indexeddb round-trip test shape; setup.ts imports fake-indexeddb/auto)
    - 05-RESEARCH.md §Migration (the additive stores block + backfill upgrade callback, lines 362-375) and §Runtime State Inventory (backfill source:'manual')
    - packages/app/src/show/scoring.ts (classifyOutcome signature — adoptSuggestion derives hit/miss the same way)
  </read_first>
  <behavior>
    - Opening the DB after seeding a version(2)-shape trackedEntries row backfills that row's `source` to "manual" (no undefined source).
    - version(1)/version(2) data (meta, attendedShows, a pre-existing trackedShow + entries) all survive the upgrade unchanged.
    - adoptSuggestion(sessionId, { songId, songName, shownFanSongIds }) adds a trackedEntry with source:"editor", isPlaceholder:false, and outcome derived by classifyOutcome against shownFanSongIds; position/setNumber are stamped by the same rule as logSong.
    - bindShow(sessionId, { showId, venueId, venueName, city }) updates the trackedShow's binding columns and leaves status/date/currentSetNumber untouched.
    - importSnapshot(snapshot) bulkPuts meta/attendedShows/trackedShows/trackedEntries inside ONE rw transaction; a thrown error mid-write rolls the whole transaction back (no partial state).
  </behavior>
  <action>
    In `packages/app/src/db/db.ts`: add `export type EntrySource = "manual" | "editor";` and extend `TrackedEntry` with `source: EntrySource;` and extend `TrackedShow` with `venueId: number | null; venueName: string | null; city: string | null;` (all default null pre-bind). Add a `this.version(3).stores({ trackedShows: "&sessionId, status, date, showId", trackedEntries: "++id, sessionId, [sessionId+position], source" }).upgrade(async (tx) => { … })` block that iterates `tx.table("trackedEntries").toCollection().modify((e) => { if (e.source === undefined) e.source = "manual"; })` for backfill and sets the new trackedShows binding columns to null where undefined. Do NOT modify the version(1)/version(2) blocks (additive-only, D-03). Update `startShow` so the created row initializes `venueId: null, venueName: null, city: null` and `logSong`'s default write path stamps `source: "manual"` (extend the Omit and the add). Add three helpers mirroring the existing transaction idiom: `adoptSuggestion(sessionId, entry)` — a logSong variant that imports `classifyOutcome` from `../show/scoring.ts`, computes `outcome = classifyOutcome(entry.songId, entry.shownFanSongIds)`, and adds the entry with `source: "editor"`, `isPlaceholder: false`, next position + snapshotted setNumber; `bindShow(sessionId, binding)` — `db.trackedShows.update(sessionId, binding)` (like markSetBreak); `importSnapshot(snapshot)` — `db.transaction("rw", db.meta, db.attendedShows, db.trackedShows, db.trackedEntries, async () => { bulkPut each array })` so the merged import commits atomically (Pitfall 5). Create `packages/app/test/migrationV3.test.ts` covering the behavior block with fake-indexeddb.
  </action>
  <verify>
    <automated>cd packages/app && npx vitest run test/migrationV3.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/app/src/db/db.ts` contains a `this.version(3)` block and does NOT alter the `this.version(1)`/`this.version(2)` blocks (source assertion).
    - A test seeds a source-less entry, reopens, and asserts `source === "manual"` (backfill).
    - A test asserts a pre-existing v2 trackedShow + entries survive the upgrade with values intact.
    - A test asserts `adoptSuggestion` writes `source === "editor"` and an outcome matching `classifyOutcome`.
    - A test asserts `importSnapshot` leaves NO rows written when the transaction throws mid-way (atomic rollback).
    - `npx vitest run test/migrationV3.test.ts` passes.
  </acceptance_criteria>
  <done>The schema is additively at version(3) with provenance + binding fields, existing data is preserved and backfilled, and the three write helpers the UIs need exist and are tested.</done>
</task>

<task type="auto">
  <name>Task 2: Phase-5 config constants + copy strings</name>
  <files>packages/app/src/config.ts</files>
  <read_first>
    - packages/app/src/config.ts (the WHOLE file: the `show` tunables block lines 33-58 and the `copy.show` block lines 103-152 — mirror their nesting and comment style; single-config-file ethos)
    - 05-UI-SPEC.md §Config surface (the exact keys + defaults) and §Copywriting Contract (the exact copy strings)
    - packages/app/src/pwa/persist.ts (PersistStatus union — the storage-status copy pairs with it)
  </read_first>
  <action>
    Add to the `config` object in `packages/app/src/config.ts`, following the existing nesting: a `live` block `{ POLL_INTERVAL_MS: 60000, POLL_MAX_INTERVAL_MS: 300000, SUGGESTION_COUNT: 2 }` with comments citing SYNC-01's ≤1/60s hard floor and D-06 adaptive ceiling; a `ui` block (or extend if present) `{ SUGGESTION_STRIP_HEIGHT: 56, SYNC_DOT_DIAMETER: 8 }`; a `dataSafety` block `{ SCHEMA_VERSION: 1 }` (feeds serializeExport, D-09/D-12). Under `copy`, add a `live` block with the verbatim UI-SPEC strings: suggestion eyebrow "kglw editor", suggestion adopt hint, fill-??? eyebrow "Fill in ???", fill-??? body template, offline reassurance "Offline — tracking still works. It'll resync when signal returns." and a `settings` block with: section heading "Backup & data", export CTA "Export backup", import CTA "Import backup", export description, import description, export success "Backup saved.", import success template "Backup merged." + counts, import error heading "That's not a Guezzer backup." + body "Nothing changed. Pick a valid export file and try again.", storage-protected "Storage is protected on this device.", storage-not-protected "Your device may clear Guezzer's data." + body, end-show auto-backup confirmation "Backup saved to your downloads.", and the AppMenu entry label "Settings". Use the exact wording from 05-UI-SPEC.md §Copywriting Contract. No component may hardcode these — they READ these keys.
  </action>
  <verify>
    <automated>cd packages/app && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `config.live.POLL_INTERVAL_MS === 60000` and `config.live.SUGGESTION_COUNT === 2`.
    - `config.dataSafety.SCHEMA_VERSION === 1`.
    - `config.copy.settings` contains keys for export CTA "Export backup", import CTA "Import backup", and the import-error heading "That's not a Guezzer backup.".
    - `config.copy.live` contains the offline reassurance string exactly as in the UI-SPEC.
    - Typecheck passes (`tsc --noEmit`).
  </acceptance_criteria>
  <done>Every Phase-5 numeric tunable and copy string lives in the single app config file with UI-SPEC-verbatim wording.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| merged import snapshot → IndexedDB | The committed result of a validated import crosses into local persistence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | Tampering (data loss) | version(3) migration | mitigate | Additive-only stores + backfill upgrade; v1/v2 blocks untouched — no destructive schema rewrite (D-03) |
| T-05-09 | Tampering (partial write) | importSnapshot | mitigate | Single rw transaction wrapping all bulkPuts — a mid-write throw rolls back entirely (Pitfall 5) |
| T-05-10 | Repudiation (tally integrity) | adoptSuggestion | mitigate | source:'editor' provenance stamped + hit/miss classified by the same Phase-4 rule so the tally stays honest and decomposable (D-03) |
| T-05-SC | Tampering (supply chain) | package installs | mitigate | No new dependencies this phase (RESEARCH §Package Legitimacy Audit — vacuously satisfied) |
</threat_model>

<verification>
- `cd packages/app && npx vitest run test/migrationV3.test.ts` green.
- `cd packages/app && npx vitest run test/db.test.ts` still green (v1/v2 preserved).
- `cd packages/app && npx tsc --noEmit -p tsconfig.json` clean.
</verification>

<success_criteria>
- Additive version(3) migration with provenance + binding fields, existing data preserved + backfilled.
- adoptSuggestion / bindShow / importSnapshot helpers ready for the Wave-2 UIs.
- All Phase-5 constants + copy in the single config file.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)

New symbols (exclude from source-drift verification — created here):
- `EntrySource` (type); `TrackedEntry.source`, `TrackedShow.venueId/venueName/city` (field additions); `this.version(3)` migration; helpers `adoptSuggestion`, `bindShow`, `importSnapshot` — `packages/app/src/db/db.ts`
- `config.live.{POLL_INTERVAL_MS,POLL_MAX_INTERVAL_MS,SUGGESTION_COUNT}`, `config.ui.{SUGGESTION_STRIP_HEIGHT,SYNC_DOT_DIAMETER}`, `config.dataSafety.SCHEMA_VERSION`, `config.copy.live.*`, `config.copy.settings.*` — `packages/app/src/config.ts`
- New test: `migrationV3.test.ts`
</artifacts_produced>

<output>
Create `.planning/phases/05-live-sync-data-safety/05-03-SUMMARY.md` when done
</output>
