---
phase: 16-gizz-bingo-build-live-marking-celebrations
plan: 03
type: execute
wave: 2
depends_on: [16-01, 16-02]
files_modified:
  - packages/app/src/games/GamesView.tsx
  - packages/app/src/games/DealScreen.tsx
  - packages/app/src/games/FillMeter.tsx
  - packages/app/src/games/SwapSheet.tsx
  - packages/app/test/games/SwapSheet.test.tsx
autonomous: true
requirements: [BINGO-01, BINGO-02, BINGO-04]

must_haves:
  truths:
    - "Tapping any of the three vibe buttons (Deal Chill/Balanced/Glory-hunter) immediately deals a complete 4×4 card — never a blank grid"
    - "The draft card renders through <BingoBoard> with a live fill/difficulty meter above it, fed by estimateFill"
    - "User can reshuffle the whole card and swap any individual square from a sectioned sheet (Events→Albums→Songs→Search) with fire-rate hints and cover art"
    - "Items already on the card are greyed/deduped so no dead duplicate square can be created; the vibe label flips to 'Custom' on deviation"
    - "When a locked card exists for the active session, GamesView shows the live-marking board (deriveLiveBoard), not the draft build controls"
  artifacts:
    - path: "packages/app/src/games/DealScreen.tsx"
      provides: "3 vibe buttons that ARE the deal action (D-01)"
      exports: ["DealScreen"]
    - path: "packages/app/src/games/FillMeter.tsx"
      provides: "bar + ~marks + line/blackout caption fed by estimateFill (D-11/D-12)"
      exports: ["FillMeter"]
    - path: "packages/app/src/games/SwapSheet.tsx"
      provides: "sectioned swap bottom-sheet with cover art + fuse search + dedup (D-02/D-04/D-05)"
      exports: ["SwapSheet"]
  key_links:
    - from: "packages/app/src/games/DealScreen.tsx"
      to: "@guezzer/core deal + db saveDraftCard"
      via: "vibe tap → deal() → saveDraftCard()"
      pattern: "deal\\(|saveDraftCard"
    - from: "packages/app/src/games/FillMeter.tsx"
      to: "@guezzer/core estimateFill"
      via: "renders FillEstimate"
      pattern: "estimateFill"
    - from: "packages/app/src/games/GamesView.tsx"
      to: "packages/app/src/components/BingoBoard.tsx"
      via: "draft + live board render"
      pattern: "BingoBoard"
---

<objective>
Replace the Phase-15 "Deal — coming soon" stub in `GamesView` with the real build surface: three vibe buttons that ARE the deal (D-01), a draft `<BingoBoard>`, a persistent live fill/difficulty meter (D-10/D-11/D-12), and a sectioned swap/reshuffle sheet (D-02/D-04/D-05). When a locked card exists for the active session, GamesView instead shows the live-marking board via `deriveLiveBoard`. Delivers the build-agency pillar (BINGO-01/02) and the GamesView half of live marking (BINGO-04).

Purpose: Fun lives in the build and the reshuffle. This is the one-thumb, in-the-dark card-building screen; the meter keeps it honest.
Output: DealScreen, FillMeter, SwapSheet, and the rewired GamesView state machine (consuming the Plan-02 `getBingoContext`/`<BingoBoard>`/`deriveLiveBoard` foundation).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-gizz-bingo-build-live-marking-celebrations/16-CONTEXT.md
@.planning/phases/16-gizz-bingo-build-live-marking-celebrations/16-RESEARCH.md
@.planning/phases/16-gizz-bingo-build-live-marking-celebrations/16-PATTERNS.md
@.planning/phases/16-gizz-bingo-build-live-marking-celebrations/16-UI-SPEC.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)
- **New components:** `DealScreen` (3 vibe buttons = deal, D-01), `FillMeter` (bar+~marks+caption, D-11/D-12), `SwapSheet` (sectioned swap sheet, D-02).
- **Changed:** `GamesView.tsx` state machine — deal screen (no card) → draft board+meter+swap (unlocked) → live board (locked+active) → replay list (unchanged).
- **Consumes (from Plan 02):** `getBingoContext()`, `<BingoBoard>`, `deriveLiveBoard`; (from Plan 01) `estimateFill`, `config.bingo.fireRates`, `config.copy.games.bingo`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: DealScreen + GamesView deal/draft-board wiring (BINGO-01)</name>
  <read_first>
    - packages/app/src/games/GamesView.tsx — the whole stub (the disabled teaser block :42-56 to delete; the `useLiveQuery(() => db.bingoCards.toArray())` source :32-34 and replay list :59-83 to KEEP; container classes :37).
    - packages/app/src/games/bingoContext.ts (Plan 02) — `getBingoContext()` memoized ctx + `corpusVersion` + `dexSnapshot` helper (do NOT rebuild ctx here).
    - packages/core/src/bingo/generate.ts — `deal(seed, vibe, ctx, dexSnapshot, corpusVersion, cfg=config)`.
    - packages/app/src/db/db.ts:463-510 — `DraftCardInput` + `saveDraftCard` (cardId==sessionId, overwrites in place, THROWS if finalized/locked); `getActiveShow()` for the active sessionId + showDate/venue/city.
    - packages/app/src/games/bingoReplay.ts (Plan 02) — `deriveLiveBoard(card, entries, ctx, dexSnapshot)` for the draft/live board.
    - packages/app/src/show/PreShowLauncher.tsx:32-37 — the single-action CTA button idiom to mirror for the vibe buttons.
    - .planning/phases/16-.../16-UI-SPEC.md §Copywriting/§Color — vibe labels + gamble hints from `config.copy.games.bingo`; accent reserved (use `bg-elevated` cards for buttons, not accent fill).
  </read_first>
  <action>
    Create `packages/app/src/games/DealScreen.tsx`: three `bg-elevated` buttons ("Deal Chill" / "Deal Balanced" / "Deal Glory-hunter" from `config.copy.games.bingo`) each with a `text-text-muted` gamble hint below (min-h-11). On tap: generate a fresh seed string, call `deal(seed, vibe, getBingoContext()!, dexSnapshot, corpusVersion)` (ctx/corpusVersion/dexSnapshot all from Plan-02 `getBingoContext`), then `saveDraftCard({ sessionId, card, showDate, venueName, city })` for the active show (a card can be dealt pre-show and locked at Start Show, D-07; reuse getActiveShow/startShow semantics for the session). In GamesView delete the disabled teaser block and add the state machine: no draft card for the active session → `<DealScreen>`; unlocked draft card → the draft `<BingoBoard marked=… onSquareTap=openSwap />` derived via `deriveLiveBoard(card, [], getBingoContext()!, dexSnapshot)` (all-unmarked pre-show); locked+active card → the live `<BingoBoard>` derived via `deriveLiveBoard(card, session.entries, …)`. Keep the replay list + empty state + container classes unchanged.
  </action>
  <verify>
    <automated>cd packages/app && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>Tapping a vibe button deals a complete card and persists it; GamesView renders the draft/live board; the coming-soon stub is gone; app typechecks.</done>
  <acceptance_criteria>
    - `grep -n "deal(\|saveDraftCard\|getBingoContext" packages/app/src/games/DealScreen.tsx` matches.
    - `grep -n "teaserAffordance" packages/app/src/games/GamesView.tsx` returns NOTHING (stub deleted).
    - `grep -n "BingoBoard\|DealScreen\|deriveLiveBoard" packages/app/src/games/GamesView.tsx` matches.
    - `cd packages/app && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: FillMeter fed by estimateFill; persistent above the draft board (BINGO-02, D-11/D-12)</name>
  <read_first>
    - packages/core/src/bingo/estimate.ts (Plan 01) — `estimateFill(card, ctx, caughtSnapshot, cfg) → FillEstimate` ({ expectedMarks, fillFraction, lineLikelihood, blackoutLikelihood }).
    - packages/app/src/dex/RecapView.tsx:322-339 — the win-badge chip presentation idiom (semantic-colored inline pills, escaped text) to mirror.
    - .planning/phases/16-.../16-UI-SPEC.md §Color/§Typography — bar `#22C55E`→amber `#F59E0B` when below `lineLikelyThreshold` (never red); caption `{line} · blackout: {blackout}`; "~11/15" `tabular-nums`; the amber warning string.
    - config.copy.games.bingo (Plan 01) — meter caption + amber warning + expected-marks format strings.
  </read_first>
  <action>
    Create `packages/app/src/games/FillMeter.tsx` taking a `FillEstimate` (GamesView owns the memo: `estimateFill(draftCard, getBingoContext()!, dexSnapshot)`). Render a `bg-elevated` (`#17171F`) track with a fill bar sized to `fillFraction`, colored `#22C55E` normally and `#F59E0B` when `lineLikelihood === "unlikely"` (below `config.bingo.fillMeter.lineLikelyThreshold`), the "~{expectedMarks}/15" figure in `tabular-nums`, and the caption "{line} · blackout: {blackout}" using the copy formatters. When amber, show the quiet non-blocking warning note. Mount `<FillMeter>` persistently ABOVE the draft `<BingoBoard>` in GamesView; recompute the estimate on every swap/reshuffle (memo keyed on the draft card). Guides, never blocks (D-12) — no disabling of Start Show.
  </action>
  <verify>
    <automated>cd packages/app && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>The meter renders above the board, reacts to card changes, and turns amber (never red) below the likely-line threshold with the honest both-odds caption.</done>
  <acceptance_criteria>
    - `grep -n "estimateFill\|FillEstimate" packages/app/src/games/FillMeter.tsx packages/app/src/games/GamesView.tsx` matches.
    - `grep -n "#F59E0B" packages/app/src/games/FillMeter.tsx` matches (amber warning state); no `#ef4444`/red in the meter.
    - `cd packages/app && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SwapSheet (sectioned, covers, fuse search, dedup) + reshuffle + custom-vibe flip (BINGO-02, D-02/D-04/D-05/D-06)</name>
  <read_first>
    - packages/app/src/show/SearchSheet.tsx:20-64 — the `<Sheet>` pattern + `makeCatalogSearcher(toCatalog(nodes))` from `@guezzer/core` (the fuse.js wrapper to reuse; do NOT build a new search).
    - packages/app/src/games/CatchUpSheet.tsx — the sectioned-sheet + reuse-SearchSheet precedent.
    - packages/app/src/dex/covers.ts:31 (`coverUrlFor(slug)`) + packages/app/src/dex/song-cover.ts:51 (`coverUrlForSong(songId)`) — cover art already bundled; degrade to a text chip only when `coverUrlForSong` returns null.
    - config.bingo.fireRates + config.bingo.albumSquarePool (Plan 01 + shipped) — the album/event fire-rate % hints; bust-out prefixed 🌟.
    - packages/core/src/bingo/types.ts — `BingoSquareDef` (kind: free|song|album|event) so a swapped square rebuilds a valid resolved def + the card stays 16 squares with one free.
    - packages/app/src/db/db.ts:485-510 — `saveDraftCard` overwrites in place (a swap = a new card object, same sessionId row).
    - .planning/phases/16-.../16-UI-SPEC.md §Copywriting — section labels, fire-rate hint format, custom-vibe "Custom", reshuffle-confirm strings.
  </read_first>
  <behavior>
    - Dedup: a menu item whose identity (albumUrl / songId / event) is already present on the card renders greyed/disabled and cannot be picked (no dead duplicate square — consume-once).
    - Custom flip: after any individual swap that deviates from the dealt vibe composition, the displayed vibe label reads "Custom".
    - Reshuffle: re-deals the whole card keeping the current vibe; if custom swaps exist, confirm first ("Reshuffle the whole card?" → destructive `#ef4444` "Reshuffle" / "Keep card"); with no custom swaps, re-deal silently.
  </behavior>
  <action>
    Create `packages/app/src/games/SwapSheet.tsx` using the app `<Sheet>` with fixed sections Events → Albums → Songs (sub-grouped "Likely"/"A stretch" by fire-rate) → Search. Album chips use `coverUrlFor(slug)` (slug = last path segment of albumUrl); Song chips use `coverUrlForSong(songId)` degrading to a text chip on null; Event chips use emoji/text. Each candidate shows ONLY its fire-rate % (D-05), bust-out prefixed 🌟. Already-on-card items greyed/deduped (D-04). Search reuses `makeCatalogSearcher`/`toCatalog`. Selecting a candidate replaces the tapped square's `BingoSquareDef` (rebuild a valid resolved def + label frozen at swap), keeps the card at 16 squares with the single free cell, and calls `saveDraftCard` (overwrites in place). Flip the GamesView vibe label to "Custom" on deviation. Wire a reshuffle control (keep vibe; confirm-on-custom per the behavior block; new seed → `deal` → `saveDraftCard`). Add `packages/app/test/games/SwapSheet.test.tsx` (jsdom) asserting: an on-card album is greyed/not selectable (dedup), a swap flips the vibe label to "Custom", and reshuffle with custom swaps shows the confirm before re-dealing.
  </action>
  <verify>
    <automated>npm test -- --run packages/app/test/games/SwapSheet.test.tsx</automated>
  </verify>
  <done>The swap sheet is sectioned with covers + fire-rate hints, dedups on-card items, flips to "Custom" on swap, and reshuffle confirms before discarding custom swaps; the test is green.</done>
  <acceptance_criteria>
    - `grep -n "coverUrlFor\|coverUrlForSong\|makeCatalogSearcher" packages/app/src/games/SwapSheet.tsx` matches all three.
    - `npm test -- --run packages/app/test/games/SwapSheet.test.tsx` exits 0 and covers dedup + custom-flip + reshuffle-confirm.
    - `cd packages/app && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| swap-sheet search input | client-only fuse.js query, no server, no eval |
| kglw-derived names → chips/board | untrusted corpus strings rendered as labels |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04 | Tampering (XSS) | swap chip / board labels | mitigate | Escaped React text for all song/album/venue names; cover `src` from the bundled glob only |
| T-16-05 | Tampering | swapped square produces malformed card | mitigate | Rebuild a valid `BingoSquareDef`; card stays 16 squares w/ one free (schema shape preserved); `saveDraftCard` throws if session finalized/locked |
| T-16-SC | Tampering | package installs | accept | No installs this phase — not applicable |
</threat_model>

<verification>
- `cd packages/app && npx tsc --noEmit` clean.
- `npm test -- --run packages/app/test/games/SwapSheet.test.tsx` green.
- `npm test -- --run` full suite green.
- Manual (device UAT, tracked separately): deal each vibe; swap squares; meter reacts + vibe flips to "Custom".
</verification>

<success_criteria>
- One tap deals a complete card per vibe (never blank) — BINGO-01.
- Reshuffle + per-square swap with dedup, fire-rate hints, cover art, and a live honest meter — BINGO-02.
- Locked-active session shows the live board via `deriveLiveBoard` — BINGO-04 (GamesView half).
</success_criteria>

<output>
Create `.planning/phases/16-gizz-bingo-build-live-marking-celebrations/16-03-SUMMARY.md` when done.
</output>
