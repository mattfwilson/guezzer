---
phase: 01-corpus-ingestion-schema-foundation
plan: 04
type: execute
wave: 4
depends_on: ["01-02", "01-03"]
files_modified:
  - packages/core/src/ingest/api-types.ts
  - packages/core/src/config.ts
  - docs/SCHEMA.md
  - packages/core/src/ingest/normalize.ts
  - packages/core/test/api-types.test.ts
  - packages/core/test/normalize.test.ts
  - packages/core/test/fixtures/
  - data/normalized/corpus.json
autonomous: true
requirements: [DATA-01, DATA-02]
tags: [ingestion, zod, normalizer, fixtures, enum-lock]

must_haves:
  truths:
    - "Every SCHEMA.md Open Unknown has a Resolution line citing census evidence (counts + example show IDs)"
    - "Locked zod schemas hard-fail on a novel setnumber/transition_id/settype value with an error naming the field, the value, and an example show (D-11)"
    - "data/normalized/corpus.json is regenerated from the FULL committed raw corpus with locked schemas and the D-08 header, and its counts cross-check against census.json"
    - "Era-spanning fixture tests (2012/2017/2022/2025-style) pass against the normalizer, proving set structure, segues, and sandwiches parse correctly (phase success criterion 5)"
  artifacts:
    - path: "packages/core/src/ingest/api-types.ts"
      provides: "Stage-2 locked schemas (transitionIdLocked, setnumberLocked, rawSetlistRowLocked)"
      exports: ["rawSetlistRowLocked", "transitionIdLocked", "setnumberLocked"]
    - path: "docs/SCHEMA.md"
      provides: "All unknowns resolved with census evidence; final enum vocabularies + settype allowlist rationale recorded"
      contains: "Resolution"
    - path: "data/normalized/corpus.json"
      provides: "The final Phase-1 versioned artifact — Phase 2's sole input"
      contains: "schemaVersion"
    - path: "packages/core/test/fixtures"
      provides: "Era-spanning real-show extracts (2012/2013/2017/2022/2025) for normalizer tests"
  key_links:
    - from: "packages/core/src/ingest/normalize.ts"
      to: "packages/core/src/ingest/api-types.ts"
      via: "normalizer validates rows with rawSetlistRowLocked (swapped from census-mode)"
      pattern: "rawSetlistRowLocked"
    - from: "docs/SCHEMA.md"
      to: "data/census.json"
      via: "each Resolution cites census counts/examples"
      pattern: "Resolution"
    - from: "packages/core/test/normalize.test.ts"
      to: "packages/core/test/fixtures"
      via: "fixture JSON imports"
      pattern: "fixtures/"
---

<objective>
Lock the schema: resolve every SCHEMA.md unknown with census evidence, upgrade zod to strict drift-failing enums, finalize the settype allowlist, regenerate the normalized artifact from the full committed corpus, and prove the normalizer against era-spanning real-show fixtures.

Purpose: Implements D-11 (strict enums fail loudly on drift) and finalizes D-16 (census-informed allowlist); completes DATA-01 and DATA-02. This is where "every schema assumption documented from real samples" becomes "every schema assumption enforced at runtime". Two-stage sequencing honored: enums lock ONLY now, after census evidence exists (Pitfall 2).
Output: Locked schemas, resolved SCHEMA.md, final full-corpus `data/normalized/corpus.json`, era fixture test suite (phase success criterion 5).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-corpus-ingestion-schema-foundation/01-03-SUMMARY.md
@.planning/phases/01-corpus-ingestion-schema-foundation/01-RESEARCH.md
@docs/SCHEMA.md
@data/census-report.md
@packages/core/src/ingest/api-types.ts
@packages/core/src/ingest/normalize.ts
@packages/core/src/config.ts
</context>

## Artifacts this phase produces (plan 04 subset)

- **Files:** stage-2 additions to `packages/core/src/ingest/api-types.ts`, resolved `docs/SCHEMA.md`, final `data/normalized/corpus.json`, `packages/core/test/fixtures/*.json` (era extracts + expected outputs), expanded `packages/core/test/{api-types,normalize}.test.ts`
- **Exported symbols:** `rawSetlistRowLocked`, `transitionIdLocked`, `setnumberLocked`, `settypeLocked`, `formatRowError(zodError, row)`, `SetNumber` (inferred literal-union type replacing plain string in domain types)
- **Config keys finalized:** `settypeAllowlist` (census-locked final value, D-16)
- **Data contract:** final `data/normalized/corpus.json` — Phase 2's sole input; header counts must equal census-derived counts

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock enums from census evidence + resolve SCHEMA.md unknowns</name>
  <files>packages/core/src/ingest/api-types.ts, packages/core/src/config.ts, docs/SCHEMA.md, packages/core/test/api-types.test.ts</files>
  <read_first>
    - data/census.json and data/census-report.md (the evidence — every locked value must appear here; do NOT lock values the census did not observe)
    - docs/SCHEMA.md (§13 Open Unknowns — fill every Resolution line)
    - .planning/phases/01-corpus-ingestion-schema-foundation/01-RESEARCH.md (Pattern 1 stage-2 example: transitionIdLocked literal union, setnumberLocked z.enum; D-11/D-16 wording)
    - packages/core/src/ingest/api-types.ts (stage-1 schemas — stage 2 is added alongside, not a replacement; census stays enum-loose forever)
  </read_first>
  <behavior>
    - Test 1: `rawSetlistRowLocked` parses every row of both samples AND a spot-checked recent year file (e.g. data/raw/setlists-2025.json) with zero errors
    - Test 2: a row with `setnumber: "x"` fails with an error message containing the string `setnumber`, the string `"x"`, and (via formatRowError) the row's show identifier (showdate or show_id) — D-11 drift UX
    - Test 3: a row with `transition_id: 99` fails with a message containing `transition_id` and `99`
    - Test 4: a `settype` value outside the census-observed vocabulary fails the locked schema (novel settype on a future refresh = loud failure, then an explicit allowlist decision)
    - Test 5: census-mode `rawSetlistRowCensus` still accepts all of the above novel values (the census must remain runnable on drifted data — the two schemas coexist permanently)
    - Test 6: every value in `setnumberLocked` and `settypeLocked` options appears in data/census.json (no invented vocabulary)
  </behavior>
  <action>
    Write failing tests, then:

    In `api-types.ts`, add stage-2 locked schemas beneath the census-mode ones: `transitionIdLocked` = union of z.literal for exactly the transition_id values census.json observed (working hypothesis 1–6 per RESEARCH; extend/trim per evidence); `setnumberLocked` = z.enum of exactly the observed setnumber vocabulary (hypothesis ["1","2","3","e"] — trim "3" if the census never saw it, extend if marathon shows produced more); `settypeLocked` = z.enum of ALL observed settype values (the full vocabulary — allowlist FILTERING is the normalizer's job; the locked schema's job is drift DETECTION); `rawSetlistRowLocked` = strictObject reusing the shared 41-key shape with the three locked fields swapped in. Attach zod custom error messages (zod 4 `error` param) so failures name field + offending value. Add helper `formatRowError(zodError, row)` (in api-types.ts or validate.ts) that appends the example-show context (show_id + showdate) per the project error convention.

    In `config.ts`, set the FINAL `settypeAllowlist` per D-16: proper live concerts in; radio sessions, soundchecks, and similar non-show variants out. Base membership strictly on census-report.md evidence; document each excluded settype with a one-line comment citing its census showCount. D-16's inclusion test: "would this set's song choices pollute rotation/transition signals?" — live festival/marathon sets in, "Live Session" (PBS radio) out.

    In `docs/SCHEMA.md`, fill every §13 "Resolution:" line with census evidence (value list, counts, example show IDs, report section reference). Record the final enum vocabularies, the allowlist decision with rationale, `transition_id: 4`'s corpus-wide count, and the confirmed terminal-unreliability numbers by era.
  </action>
  <verify>
    <automated>pnpm vitest run packages/core/test/api-types.test.ts && node -e "const s=require('fs').readFileSync('docs/SCHEMA.md','utf8'); const m=s.match(/Resolution:([^\n]*)/g)||[]; const empty=m.filter(x=>x.replace('Resolution:','').trim().length<10).length; if(m.length<5||empty>0){console.error('SCHEMA.md unknowns not all resolved: '+m.length+' found, '+empty+' empty');process.exit(1);} console.log('SCHEMA.md resolutions OK: '+m.length)"</automated>
  </verify>
  <acceptance_criteria>
    - All 6 behavior tests pass; `grep -cE "z\.enum|z\.literal" packages/core/src/ingest/api-types.ts` >= 3 (locked layer exists)
    - docs/SCHEMA.md has >= 5 non-empty Resolution lines, each citing a count or example show ID
    - config.ts settypeAllowlist has no "PROVISIONAL" comment remaining; each exclusion carries a census-count comment
    - rawSetlistRowCensus is byte-identical to its plan 01-01 form except comments (two-stage schemas coexist; git diff confirms)
  </acceptance_criteria>
  <done>Schema drift on any future refresh fails loudly naming field/value/example show; every documented unknown is resolved with corpus-wide evidence; the allowlist is final and evidence-based.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize normalizer on locked schemas + regenerate the full-corpus artifact</name>
  <files>packages/core/src/ingest/normalize.ts, data/normalized/corpus.json</files>
  <read_first>
    - packages/core/src/ingest/api-types.ts (locked schemas + formatRowError just added)
    - packages/core/src/ingest/normalize.ts (current census-mode validation call — swap to locked)
    - data/census.json (expected counts to cross-check the artifact header against)
  </read_first>
  <action>
    Swap the normalizer's row validation from `rawSetlistRowCensus` to `rawSetlistRowLocked` (D-11: normalization runs only on vocabulary-verified data; the census CLI keeps using census-mode). Tighten `SetSection.setNumber` to the literal union inferred from `setnumberLocked`. Remove plan 01-02's "any other transition_id → none" tolerance — the locked schema now guarantees the id set, so make the TransitionKind mapping exhaustive over locked values.

    Regenerate from the full corpus: `pnpm refresh --normalize-only` (default input data/raw). Commit the new `data/normalized/corpus.json` — it replaces the interim sample-derived artifact and is the final Phase-1 deliverable Phase 2 consumes.

    Cross-check header integrity against census.json (both derive from the same raw files through different code paths — disagreement means a bug): `showCount` must equal census shows-per-year sum minus settype-excluded shows; `songCount` must equal census distinct-KGLW-songs minus sentinels; `latestShowDate` must be <= today (shows without setlist rows never appear in setlists-YYYY files). Write these three cross-checks as assertions in the verify script below, using the actual census.json field names.
  </action>
  <verify>
    <automated>pnpm refresh --normalize-only && node -e "const fs=require('fs'); const c=JSON.parse(fs.readFileSync('data/normalized/corpus.json','utf8')); const assert=require('assert'); assert.strictEqual(c.schemaVersion,1); assert.ok(c.showCount>200,'expected full corpus, got '+c.showCount+' shows'); assert.ok(c.songCount>150&&c.songCount<600,'songCount implausible: '+c.songCount); assert.ok(c.latestShowDate>'2025-01-01'&&c.latestShowDate<=new Date().toISOString().slice(0,10),'latestShowDate bad: '+c.latestShowDate); assert.strictEqual(c.shows.length,c.showCount); console.log('final artifact OK: '+c.showCount+' shows, '+c.songCount+' songs, through '+c.latestShowDate)"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm refresh --normalize-only` exits 0 over the FULL data/raw corpus with locked schemas (zero drift failures — proving the lock matches reality)
    - Artifact header: schemaVersion 1, showCount > 200, songCount in 150–600, latestShowDate <= today, shows.length === showCount
    - showCount and songCount match census-derived expectations (assertions comparing corpus.json to census.json pass — add them to the verify script with the real census field names)
    - The committed artifact diff replaces the 25-show interim artifact with the full corpus
    - `grep -c "rawSetlistRowCensus" packages/core/src/ingest/normalize.ts` returns 0 (normalizer fully on locked schemas)
  </acceptance_criteria>
  <done>The final versioned full-corpus artifact is committed; its counts agree with the census through an independent code path; Phase 2 has its input.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Era-spanning fixture tests — 2012/2017/2022/2025 real shows prove structure, segues, sandwiches</name>
  <files>packages/core/test/fixtures/, packages/core/test/normalize.test.ts</files>
  <read_first>
    - data/raw/ year files for 2012, 2017, 2022, 2025 (fixture source — extract real shows)
    - data/census-report.md (find candidate shows: a 2025 sandwich show, a segue-chain show, an encore show — use the example show IDs the census recorded)
    - packages/core/test/normalize.test.ts (existing suite — extend, keep plan 01-02 tests passing)
    - .planning/phases/01-corpus-ingestion-schema-foundation/01-PATTERNS.md ("Test structure" convention: small real-show extracts, assertions against known expected outputs)
  </read_first>
  <behavior>
    - Fixture A (2012-style, early era): a real 2012 show whose final row has transition_id 1 (loose terminal) normalizes to correct set structure — no phantom set breaks, no truncation (Pitfall 1)
    - Fixture B (2013 Live Session): the PBS Studios show is excluded by the allowlist with stats recording it (D-16)
    - Fixture C (2013 encore): a show with setnumber "e" yields a final SetSection with setNumber "e" after the numbered sets
    - Fixture D (2013 Unknown sentinel): song_id 1 row → isPlaceholder true, excluded from songCount
    - Fixture E (2022 rr1010 marathon): 2 sets split exactly at position 13/14, transition_id 4 on the set-1 closer mapped to "terminal" and NOT used for the split
    - Fixture F (2017 show): mid-era show normalizes clean (SC-5 requires a 2017-style case; pick any real 2017 show with >= 2 segues)
    - Fixture G (2025 sandwich): the real 2025-12-13 Motor Spirit > Gila Monster > Motor Spirit sequence (or another census-confirmed sandwich) yields TWO distinct Performance entries for the sandwiched song at their real positions, duplicate songId detectable within the show, no reprise linking (D-14)
    - Fixture H (2025 segue chain): consecutive transition_id 2/3 rows all map to transitionKind "segue" in position order
  </behavior>
  <action>
    Extract each fixture as a JSON file under `packages/core/test/fixtures/` containing the raw rows of ONE real show (or one show + a comment-free `_meta` sibling file naming source year file + show_id + why it was chosen). Prefer real extracts from data/raw for every case; if a pattern genuinely does not exist in the corpus (census proves absence), create a synthetic fixture clearly named `synthetic-*.json` and note it in the test — expected: all 8 cases exist in real data. Name fixtures by era + trait: `2012-loose-terminal.json`, `2013-live-session.json`, `2013-encore.json`, `2013-unknown-sentinel.json`, `2022-rr1010-multiset.json`, `2017-segues.json`, `2025-sandwich.json`, `2025-segue-chain.json`.

    Extend `normalize.test.ts` with one describe block per fixture asserting the exact expected structure (set counts, set numbers, split positions, performance counts, specific songIds/flags) — explicit expected values, not snapshots (CLAUDE.md testing constraint: known expected outputs). Keep all plan 01-02 tests green.
  </action>
  <verify>
    <automated>pnpm vitest run packages/core/test/normalize.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 8 fixture cases pass with explicit expected-value assertions (no snapshot testing)
    - `ls packages/core/test/fixtures/*.json | wc -l` >= 8; fixture filenames cover years 2012, 2013, 2017, 2022, 2025
    - The sandwich test asserts the SAME songId appears at two different positions within one show and that both entries are ordinary Performances (no reprise linkage fields)
    - The rr1010 test asserts the set split derives from setnumber (assert set 1 length 13, set 2 length 14) and separately asserts position 13's transitionKind === "terminal"
    - Full `pnpm vitest run` green
  </acceptance_criteria>
  <done>Phase success criterion 5 is proven: era-spanning real fixtures from 2012/2013/2017/2022/2025 all parse with correct set structure, segue kinds, and sandwich handling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Committed raw corpus → locked schemas → artifact | Full untrusted corpus flows through strict validation into the committed artifact consumed by all later phases |
| Future refreshes → locked enums | API drift on the volunteer site meets the hard-fail wall for the first time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-11 | Tampering | rawSetlistRowLocked | mitigate | Strict enums locked to census-observed vocabulary; novel value → loud failure naming field/value/example show (D-11); silent corruption of the matrix input becomes impossible |
| T-01-12 | Integrity | data/normalized/corpus.json | mitigate | Independent cross-check: artifact header counts must equal census-derived counts computed by a separate code path; committed artifact reviewable via git diff |
| T-01-03 | Tampering (stored, future UI) | verbatim strings in final artifact | accept (this phase) | Unchanged: carried verbatim, flagged untrusted in SCHEMA.md §12 |
</threat_model>

<verification>
- `pnpm vitest run` fully green (api-types locked suite, normalize era suite, all prior)
- `docs/SCHEMA.md` has zero unresolved unknowns
- Final full-corpus artifact committed; header cross-checks against census.json pass
</verification>

<success_criteria>
- Phase success criteria 1, 2, and 5 fully satisfied: schema documented AND enforced; one command produces the versioned full-corpus artifact; era fixtures pass
- D-11 drift protection live; D-16 allowlist final and evidence-based
- Phase 2's input contract (NormalizedCorpus, schemaVersion 1) frozen and populated
</success_criteria>

<output>
Create `.planning/phases/01-corpus-ingestion-schema-foundation/01-04-SUMMARY.md` when done.
</output>
