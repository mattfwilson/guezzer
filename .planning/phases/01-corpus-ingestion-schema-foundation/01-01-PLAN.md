---
phase: 01-corpus-ingestion-schema-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pnpm-workspace.yaml
  - package.json
  - tsconfig.base.json
  - vitest.config.ts
  - .gitattributes
  - packages/core/package.json
  - packages/core/tsconfig.json
  - packages/app/package.json
  - packages/app/README.md
  - data/samples/rr1010.json
  - data/samples/showyear2013.json
  - docs/SCHEMA.md
  - packages/core/src/config.ts
  - packages/core/src/ingest/api-types.ts
  - packages/core/src/ingest/validate.ts
  - packages/core/test/smoke.test.ts
  - packages/core/test/api-types.test.ts
  - packages/core/test/validate.test.ts
autonomous: true
requirements: [DATA-01, DATA-03]
tags: [ingestion, zod, monorepo, schema]

must_haves:
  truths:
    - "Running `pnpm vitest run` from the repo root executes core tests in a node environment and passes"
    - "docs/SCHEMA.md documents every schema fact from the real API samples, and it exists before any extraction code (normalize.ts, fetch-corpus.ts) exists in the repo"
    - "The zod census-mode schema parses every row of both committed real samples with zero errors and rejects a row with an unknown extra key"
    - "assertFilterApplied throws a message naming endpoint, field, expected value, actual value, and an example row when any row violates a requested filter"
  artifacts:
    - path: "docs/SCHEMA.md"
      provides: "D-09 standalone empirical schema documentation (DATA-01)"
      min_lines: 80
    - path: "packages/core/src/ingest/api-types.ts"
      provides: "Census-mode zod strictObject schema for the 41-key setlist row + envelope schema"
      exports: ["rawSetlistRowCensus", "apiEnvelope"]
    - path: "packages/core/src/ingest/validate.ts"
      provides: "D-12 post-fetch filter assertion"
      exports: ["assertFilterApplied"]
    - path: "packages/core/src/config.ts"
      provides: "Single constants file (API base, pacing, year bounds, allowlist, sentinels)"
    - path: "data/samples/rr1010.json"
      provides: "Committed real API sample (moved from repo root)"
    - path: "vitest.config.ts"
      provides: "Vitest 4 test.projects config (NOT vitest.workspace.ts)"
  key_links:
    - from: "packages/core/test/api-types.test.ts"
      to: "data/samples/rr1010.json"
      via: "JSON import of real sample validated by rawSetlistRowCensus"
      pattern: "rr1010"
    - from: "packages/core/src/ingest/api-types.ts"
      to: "zod"
      via: "z.strictObject"
      pattern: "strictObject"
---

<objective>
Stand up the pnpm two-package workspace, write the empirical schema document BEFORE any extraction code exists (phase success criterion 1, D-09), and encode that document as census-mode (enum-loose, key-strict) zod schemas plus the D-12 filter assertion.

Purpose: This is the Walking Skeleton's foundation and the P6 gate — every later file in the project validates against schemas whose every claim traces to a real API response. Implements D-09, D-12; starts DATA-01 and DATA-03.
Output: Working workspace with passing vitest smoke test, `docs/SCHEMA.md`, `packages/core/src/config.ts`, census-mode zod schemas, `assertFilterApplied`, and committed sample data under `data/samples/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-corpus-ingestion-schema-foundation/01-RESEARCH.md
@.planning/phases/01-corpus-ingestion-schema-foundation/01-PATTERNS.md
@.planning/research/ARCHITECTURE.md
@CLAUDE.md
</context>

## Phase Goal

**As the** owner heading into the 2026 tour, **I want to** run one documented command that fetches, validates, and normalizes the full KGLW historical corpus into a versioned artifact, **so that** every downstream prediction is built on schema-verified, trustworthy data.

(Derived from the ROADMAP Phase 1 `**Goal:**` line; this phase is core-only data ingestion — "end-to-end" means CLI pipeline on real data with a passing test, per the walking-skeleton definition in SKELETON.md.)

## Artifacts this phase produces (plan 01 subset)

- **Files:** `pnpm-workspace.yaml`, root `package.json`, `tsconfig.base.json`, `vitest.config.ts`, `.gitattributes`, `packages/core/package.json`, `packages/core/tsconfig.json`, `packages/app/package.json`, `packages/app/README.md`, `docs/SCHEMA.md`, `packages/core/src/config.ts`, `packages/core/src/ingest/api-types.ts`, `packages/core/src/ingest/validate.ts`, `packages/core/test/{smoke,api-types,validate}.test.ts`, `data/samples/rr1010.json`, `data/samples/showyear2013.json`
- **Exported symbols:** `rawSetlistRowCensus`, `apiEnvelope`, `RawSetlistRow` (inferred type), `assertFilterApplied`, `config` (with keys `apiBase`, `fetchDelayMs`, `fetchTimeoutMs`, `userAgent`, `corpusYearStart`, `cliYearMin`, `cliYearMax`, `settypeAllowlist`, `sentinelSongIds`, `dataRawDir`, `dataSamplesDir`, `dataNormalizedDir`, `corpusArtifactPath`, `censusReportPath`, `censusJsonPath`, `tuningTagsPath`)
- **Package names:** `@guezzer/core`, `@guezzer/app` (stub)
- **npm scripts (root):** `test` → `vitest run`

<tasks>

<task type="auto">
  <name>Task 1: Scaffold pnpm workspace, vitest projects config, and commit sample data</name>
  <files>pnpm-workspace.yaml, package.json, tsconfig.base.json, vitest.config.ts, .gitattributes, packages/core/package.json, packages/core/tsconfig.json, packages/app/package.json, packages/app/README.md, packages/core/test/smoke.test.ts, data/samples/rr1010.json, data/samples/showyear2013.json</files>
  <read_first>
    - CLAUDE.md (Development Tools + Version Compatibility sections — locked versions and workspace rules)
    - .planning/phases/01-corpus-ingestion-schema-foundation/01-RESEARCH.md ("Recommended Project Structure" + "Environment Availability" — authoritative file layout; pnpm missing, corepack 0.34.6 present)
    - .planning/phases/01-corpus-ingestion-schema-foundation/01-PATTERNS.md ("Workspace scaffold (Wave 0)" table — exact config values per file)
  </read_first>
  <action>
    One-time setup: run `corepack enable pnpm` to activate pnpm (corepack 0.34.6 is present; Node is 24.15.0). If corepack activation fails on this Windows machine, fall back to npm workspaces (CLAUDE.md-approved: identical `packages/*` layout, `workspaces` key in root package.json instead of pnpm-workspace.yaml) and note the fallback in the summary.

    Create the workspace exactly per the RESEARCH.md Recommended Project Structure:
    - `pnpm-workspace.yaml` with `packages: ["packages/*"]`.
    - Root `package.json`: `"private": true`, `"type": "module"`, scripts `"test": "vitest run"`. Root devDependencies: `typescript@6.0.3` (NOT 7.x — typescript-eslint peer range `<6.1.0`), `vitest@4.1.10`. Do NOT add eslint this phase (deferrable per RESEARCH).
    - `tsconfig.base.json`: `strict: true`, `module: "nodenext"`, `moduleResolution: "nodenext"`, `target: "es2023"`, `noEmit: true`, `skipLibCheck: true`.
    - `vitest.config.ts` at root using `test.projects` (Vitest 4 — `vitest.workspace.ts` files were REMOVED in v4, do not create one). Configure the core project explicitly: name `@guezzer/core`, root `packages/core`, `environment: 'node'`, include `packages/core/test/**/*.test.ts`. Scope projects to core only this phase (the app stub has no tests; a glob over `packages/*` would fail project resolution) — widen to `packages/*` when app gains tests in Phase 3.
    - `packages/core/package.json`: name `@guezzer/core`, `"type": "module"`, `"private": true`. Dependencies: `zod@4.4.3`. devDependencies: `@types/node@^24.13.3`. NO react, NO DOM-touching deps — this absence IS the core-purity enforcement.
    - `packages/core/tsconfig.json`: extends `../../tsconfig.base.json`; `"lib": ["ES2023"]` (no DOM), `"erasableSyntaxOnly": true`, `"allowImportingTsExtensions": true`, `"noEmit": true`, `"module": "nodenext"`, `"types": ["node"]`. This combo supports Node ≥24.12 native type stripping (no enum/namespace anywhere in core, all relative imports carry `.ts` extensions — Pitfall 4).
    - `packages/app/package.json`: name `@guezzer/app`, `"private": true`, stub only — no dependencies this phase. `packages/app/README.md` one line: "Stub — UI arrives in Phase 3."
    - `.gitattributes`: `*.json text eol=lf` (Windows CRLF churn on committed corpus, Pitfall 9).
    - `git mv rr1010.json data/samples/rr1010.json` and `git mv showyear2013.json data/samples/showyear2013.json` — wait, both files are currently UNTRACKED (per git status), so plain `mkdir data/samples` + move + `git add` is correct; do not assume git mv works on untracked files.
    - `packages/core/test/smoke.test.ts`: one test importing `data/samples/rr1010.json` as JSON and asserting `data.length === 27` and top-level envelope keys `error`, `error_message`, `data` exist — proves JSON module imports work in the vitest node project and samples are wired.

    Run `pnpm install` then `pnpm vitest run`.
  </action>
  <verify>
    <automated>pnpm vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run` exits 0 with 1 test file passing (smoke)
    - `node --version` reports >= 24.12
    - Repo root contains NO `rr1010.json` / `showyear2013.json`; both exist under `data/samples/` and are staged/committed
    - `vitest.workspace.ts` does NOT exist anywhere in the repo
    - `packages/core/package.json` contains no `react` in any dependency block; `packages/core/tsconfig.json` contains `"erasableSyntaxOnly": true` and a `lib` array without "DOM"
    - `.gitattributes` contains the exact line `*.json text eol=lf`
    - Root `package.json` devDependencies pin `typescript` to `6.0.3` (not `^7`)
  </acceptance_criteria>
  <done>Workspace installs and tests green from the repo root; both real API samples are committed under data/samples/; core purity is mechanically enforced by tsconfig + absent deps.</done>
</task>

<task type="auto">
  <name>Task 2: Write docs/SCHEMA.md v1 — the empirical schema document, before any extraction code</name>
  <files>docs/SCHEMA.md</files>
  <read_first>
    - .planning/phases/01-corpus-ingestion-schema-foundation/01-RESEARCH.md ("Verified Schema Facts" §1–15, field-type census table, "Known facts carried forward" — transcribe these; they are ground truth)
    - .planning/phases/01-corpus-ingestion-schema-foundation/01-PATTERNS.md ("Data-Shape Analogs" — real row excerpts to embed verbatim)
    - .planning/research/ARCHITECTURE.md (Part 1 — envelope shape, silent filter-ignore, songs/albums/jamcharts schemas, sandwich evidence)
    - data/samples/rr1010.json and data/samples/showyear2013.json (spot-verify any claim you transcribe)
  </read_first>
  <action>
    Write `docs/SCHEMA.md` per D-09 — the standalone "why" document paired with the zod "what". ORDERING GATE: this task MUST complete before Task 3 and before ANY file in `ingest/` or `cli/` exists (phase success criterion 1: schema doc before extraction code; zod schemas in Task 3 are the executable pair of this doc, normalize/fetch code comes in later plans).

    Required sections (each claim tagged with its evidence source — sample file + example show_id/date, or "prior live fetch" per ARCHITECTURE.md):
    1. **Envelope** — `{ error, error_message, data }`; `data: []` with `error: false` is a VALID empty result, never an error.
    2. **Setlist row: all 41 fields** — the full field-type census table from RESEARCH.md (types, nullability, traps). Embed the real rr1010 `data[0]` row excerpt from PATTERNS.md verbatim.
    3. **Song ordering & set structure** — `position` is global and contiguous 1..N across sets; set membership comes ONLY from `setnumber` grouping (`"1"`, `"2"`, `"e"` observed; `"3"` unconfirmed pending census); `settype` is useless for structure (always "Set" on rr1010 set 2).
    4. **`transition_id` vocabulary** — 1 = normal break, 2/3 = notated hard segue, 4/5/6 = interchangeable terminal/boundary markers used inconsistently by editors (id 4 observed once: rr1010 position 13, last song of set 1, display "  "). CRITICAL RULE in bold: never infer set/show boundaries from transition_id — 14 of 26 shows in 2013 end with id 1; never string-parse the `transition` display field (whitespace garbage; ids 4 and 6 share the same display).
    5. **Set/encore delimiting** — `setnumber: "e"`; 2013-02-23 set→encore boundary carried `transition_id: 5`.
    6. **Covers & the Unknown sentinel** — `isoriginal: 0` + `original_artist` (9 rows in 2013); sentinel `song_id: 1`, songname "Unknown", slug `_custom_` (4 occurrences in 2013) — placeholder occupying a position, excluded from catalog and transition emission.
    7. **Multi-set representation** — rr1010: `setnumber` `"1"`/`"2"` string numerals, 27 contiguous positions, no encore.
    8. **Footnotes/teases** — `footnotes` is a DOUBLE-ENCODED JSON string or null (`"[\"first confirmed performance\"]"`); `footnote` is the same content as a plain string; guarded JSON.parse required (parse failure → keep raw, never throw); zero teases observed in any sample so far — footnote/shownotes are the only candidate locations; teases carried verbatim, never emit transitions (D-15).
    9. **Multi-artist database & silent filter-ignore** — `artist_id: 1` = KGLW; side projects share every table; invalid filter paths return the ENTIRE unfiltered table; the only defense is the post-fetch assertion (D-12). `songs.json` has NO artist_id — the KGLW catalog derives from `artist_id === 1` setlist rows only.
    10. **`settype` variants & corpus scope** — observed: "Set", "One Set", "Live Session" (PBS Studios radio sessions — the D-16 exclusion case). Working allowlist `{"Set","One Set"}`, FINAL LIST PENDING CENSUS.
    11. **Sibling endpoints** — `shows.json` (future shows with zero setlist rows exist), `songs.json`, `albums.json` (no song_id — join via slug from `song_url`; `releasedate` dirty; `album_notes` raw HTML — untrusted, never render), `jamcharts.json` (field-name drift: `showid`, `song_slug`), `latest.json` (smaller key subset — Phase 5 concern). `tour_id: 1` = "Not Part of a Tour" sentinel. Natural show key `(showdate, showorder, artist_id)` — two shows share 2013-09-11.
    12. **Security note** — `shownotes`, `album_notes`, footnotes are untrusted editor content (raw HTML observed); carry verbatim, never interpret or render without escaping.
    13. **Open Unknowns (census will resolve)** — numbered list with what-we-know/what's-unclear: (a) does `setnumber: "3"` exist; (b) full-corpus `transition_id` 4/5/6 distribution; (c) tease notation string conventions; (d) full `settype` variant list across 15 years; (e) footnotes rows that fail JSON.parse. Each unknown gets a "Resolution:" placeholder line to be filled with census evidence in plan 01-04.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('docs/SCHEMA.md','utf8'); for (const t of ['transition_id','setnumber','Open Unknowns','silent','artist_id','footnotes','Live Session','_custom_','41']) { if(!s.includes(t)) { console.error('MISSING: '+t); process.exit(1);} } console.log('SCHEMA.md sections OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/SCHEMA.md` exists with all 13 required sections; the verify script passes
    - The 41-field type table is present (all fields from the RESEARCH.md census table appear)
    - The bold never-infer-structure-from-transition_id rule is present
    - An "Open Unknowns" section lists at least the 5 enumerated unknowns, each with an empty "Resolution:" line
    - No file matching `packages/core/src/ingest/normalize.ts` or `packages/core/src/cli/*` exists yet at the commit that introduces SCHEMA.md (git log ordering proves success criterion 1)
  </acceptance_criteria>
  <done>SCHEMA.md is the committed single source of schema truth, written entirely from real endpoint evidence, before any extraction code exists in the repo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: config.ts + census-mode zod schemas + assertFilterApplied, tested against both real samples</name>
  <files>packages/core/src/config.ts, packages/core/src/ingest/api-types.ts, packages/core/src/ingest/validate.ts, packages/core/test/api-types.test.ts, packages/core/test/validate.test.ts</files>
  <read_first>
    - docs/SCHEMA.md (just written — the schemas encode THIS document; every zod field type must match its 41-field table)
    - .planning/phases/01-corpus-ingestion-schema-foundation/01-RESEARCH.md (Architecture Pattern 1 "Two-stage zod schemas" and Pattern 2 "Post-fetch filter assertion" — ready-to-copy code)
    - .planning/phases/01-corpus-ingestion-schema-foundation/01-PATTERNS.md ("Setlist row — all 41 keys, real values" + "Error message convention" + "Constants")
    - data/samples/rr1010.json, data/samples/showyear2013.json (the tests parse these whole files)
  </read_first>
  <behavior>
    - Test 1 (api-types): `apiEnvelope` parses both sample files' top level; `rawSetlistRowCensus` parses ALL 27 rr1010 rows and ALL 149 showyear2013 rows with zero errors
    - Test 2 (api-types): a valid row with one extra key `{"bogus_new_field": 1}` added is REJECTED by `rawSetlistRowCensus` (strictObject catches API drift)
    - Test 3 (api-types): a row with `setnumber: 2` (number instead of string) or `showyear: "2013"` (string instead of number) is rejected — the two known type traps
    - Test 4 (validate): `assertFilterApplied(rows, "setlists/showyear/2013", { field: "showyear", expected: 2013 })` passes on the real 2013 sample rows
    - Test 5 (validate): with one row mutated to `showyear: 2014`, it throws an Error whose message contains ALL of: `"setlists/showyear/2013"`, `"showyear"`, `"2013"`, `"2014"`, and a row excerpt (message.length > 100)
    - Test 6 (validate): empty rows array passes (empty result is valid, not an error)
  </behavior>
  <action>
    Write the failing tests above first, then implement:

    `packages/core/src/config.ts` — ALL pipeline constants (CLAUDE.md: single config file, zero magic numbers elsewhere). Export a single `config` object with: `apiBase` (kglw.net API v2 base URL — confirm the exact base from .planning/research/ARCHITECTURE.md Part 1 rather than guessing), `fetchDelayMs: 2000` (D-07), `fetchTimeoutMs: 30_000`, `userAgent: "Guezzer setlist tool (matt.f.wilson@gmail.com)"` (D-07 descriptive UA), `corpusYearStart: 2010`, `cliYearMin: 2010`, `cliYearMax: 2100` (CLI arg bound, security V5), `settypeAllowlist: ["Set", "One Set"]` (PROVISIONAL — comment: final list locked post-census per D-16), `sentinelSongIds: [1]` (Unknown sentinel, Pitfall 7), `maxRowsPerYearSanity: 5000` (Pitfall 3 row-count tripwire), and path constants: `dataRawDir: "data/raw"`, `dataSamplesDir: "data/samples"`, `dataNormalizedDir: "data/normalized"`, `corpusArtifactPath: "data/normalized/corpus.json"`, `censusReportPath: "data/census-report.md"`, `censusJsonPath: "data/census.json"`, `tuningTagsPath: "data/tuning-tags.json"`, plus `microtonalAlbums` placeholder comment (filled in plan 01-05).

    `packages/core/src/ingest/api-types.ts` — Stage-1 census-mode schemas per RESEARCH Pattern 1: `apiEnvelope` = z.strictObject({ error: z.boolean(), error_message: z.string(), data: z.array(z.unknown()) }); `rawSetlistRowCensus` = z.strictObject with ALL 41 keys typed per the SCHEMA.md field table — enum-ish fields stay structurally loose (`settype: z.string()`, `setnumber: z.string()`, `transition_id: z.number().int()`) because the census discovers vocabulary (locking now would be circular, Pitfall 2). Key traps: `showyear` is z.number().int(); flags (`isjamchart`, `isverified`, `isoriginal`, `isreprise`, `isjam`) are z.number() 0/1 not boolean; `footnotes` is z.string().nullable() (double-encoded JSON — do NOT transform here, the guarded parse lives in the normalizer); `tracktime`/`jamchart_notes`/`css_class` are z.string().nullable(); `showtime`/`timezone` are never-populated → z.null().or(z.unknown()) permissive nullable; `isrecommended` z.number().nullable(); `showdate` z.string().regex(/^\d{4}-\d{2}-\d{2}$/). Export inferred type `RawSetlistRow`. Also add a marked "Stage 2 — locked schemas (added post-census in plan 01-04)" placeholder comment.

    `packages/core/src/ingest/validate.ts` — copy `assertFilterApplied` from RESEARCH Pattern 2 essentially verbatim: generic over row type, takes rows/endpoint/{field, expected}, throws on first mismatching row with the error-message convention (endpoint, field, expected, actual, "The kglw.net API silently ignores invalid filters", first 200 chars of the offending row JSON). This error format is the project-wide failure-UX convention Phase 2+ copies.

    All relative imports carry `.ts` extensions (Node native type stripping, Pitfall 4). No enum/namespace anywhere.
  </action>
  <verify>
    <automated>pnpm vitest run packages/core/test/api-types.test.ts packages/core/test/validate.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 6 behavior tests pass; full `pnpm vitest run` green
    - `rawSetlistRowCensus` has exactly 41 keys (test asserts `Object.keys(rawSetlistRowCensus.shape).length === 41`)
    - `grep -c "z.enum" packages/core/src/ingest/api-types.ts` returns 0 (no enums locked before census — Pitfall 2)
    - `node packages/core/src/ingest/validate.ts` does not error on import (native type stripping works — proves the tsconfig/extension setup, A3)
    - No numeric literal for delay/timeout/year bounds appears outside config.ts in packages/core/src
  </acceptance_criteria>
  <done>The schema document is executable: both real samples parse clean, drift (new keys, wrong types) is rejected loudly, and the DATA-03 filter assertion exists as a tested pure function.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kglw.net API → repo | Untrusted volunteer-edited JSON crosses into committed data and zod schemas (samples now, live fetch in plan 01-03) |
| npm registry → node_modules | Supply chain: first dependency installation of the project |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | ingest/api-types.ts | mitigate | z.strictObject on every row: unknown keys and wrong types hard-fail; no eval/Function on fetched data anywhere |
| T-01-02 | Spoofing/Tampering | ingest/validate.ts | mitigate | assertFilterApplied: silent filter-ignore (verified API behavior) cannot poison the corpus undetected |
| T-01-03 | Tampering (stored, future UI) | shownotes/album_notes/footnotes strings | accept (this phase) | Carried verbatim, never parsed as HTML/never rendered; SCHEMA.md §12 flags them untrusted for later UI phases (React default escaping) |
| T-01-SC | Tampering | npm installs (typescript, zod, vitest, @types/node) | mitigate | RESEARCH.md Package Legitimacy Audit: all packages [OK] except vitest [SUS] — a name-proximity heuristic false positive, overruled with evidence (official vitest-dev/vitest repo verified via npm registry metadata, no postinstall scripts, version locked by user in CLAUDE.md). Pin exact versions; commit pnpm-lock.yaml |
</threat_model>

<verification>
- `pnpm vitest run` green from repo root (smoke + api-types + validate)
- `docs/SCHEMA.md` committed in a commit that precedes any `ingest/normalize.ts` / `cli/*` file (git history proves the P6 gate)
- Both samples committed under `data/samples/`, repo root clean
</verification>

<success_criteria>
- Phase success criterion 1 is structurally satisfied: schema document exists, built from real endpoint samples, before extraction code
- DATA-03's core primitive (`assertFilterApplied`) exists and is unit-tested with the exact error-message convention
- Workspace runs tests and native-TS scripts with zero build step
</success_criteria>

<output>
Create `.planning/phases/01-corpus-ingestion-schema-foundation/01-01-SUMMARY.md` when done.
</output>
