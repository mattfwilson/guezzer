---
phase: 08-on-device-ui-polish-accessibility
plan: 05
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - packages/app/src/show/ShowView.tsx
  - packages/app/src/components/InstallBanner.tsx
  - packages/app/src/components/UpdateToast.tsx
  - packages/app/src/show/FabMenu.tsx
  - packages/app/src/show/CometTrail.tsx
  - packages/app/src/show/SearchSheet.tsx
  - packages/app/src/dex/AlbumDetail.tsx
  - packages/app/src/dex/ArchiveBrowser.tsx
  - packages/app/src/dex/RecapView.tsx
  - packages/app/src/dex/SetlistView.tsx
  - .planning/todos/pending/2026-07-17-bottom-sheets-smooth-up-down-animation-always-on-top-layerin.md
autonomous: true
requirements: [A11Y-01, A11Y-02]
must_haves:
  truths:
    - "No raw `z-*` Tailwind literal remains anywhere in packages/app/src — every stacking value comes from config.ui.z"
    - "The FabMenu scrim sits below sheets (z.fabScrim) and its action rows sit at z.fab; toasts/banners at z.toast; fullscreen route-overlays at z.sheet — preserving every existing visual stacking relationship"
    - "The bottom-sheets todo is annotated: the z-index-scale half is DONE (D-04), the slide/scrim animation half stays deferred"
  artifacts:
    - path: "packages/app/src/show/FabMenu.tsx"
      provides: "FabMenu scrim + rows on config.ui.z tiers"
      contains: "config.ui.z"
  key_links:
    - from: "packages/app/src/dex/ArchiveBrowser.tsx"
      to: "config.ui.z.sheet"
      via: "inline zIndex on the fullscreen overlay"
      pattern: "config.ui.z"
---

<objective>
Complete D-04: migrate every remaining raw `z-*` Tailwind literal in the app (the overlays NOT covered by the sheet migrations in plans 02–04) onto the named `config.ui.z` tiers via inline `style={{ zIndex }}`, so no `z-NN` literal remains and the layering fix cannot relocate a collision. Then annotate the folded bottom-sheets todo.

Purpose: The point of a scale is that no stray z literal survives (D-04). These files carry no A11Y-01 sheet migration — only their z-literal moves — but they must move for the FAB-below-sheet invariant to hold app-wide.
Output: ~10 overlay files on the z scale + the bottom-sheets todo annotated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-on-device-ui-polish-accessibility/08-RESEARCH.md
@.planning/phases/08-on-device-ui-polish-accessibility/08-PATTERNS.md
@.planning/phases/08-on-device-ui-polish-accessibility/08-UI-SPEC.md
@.planning/phases/08-on-device-ui-polish-accessibility/08-01-PLAN.md
</context>

<artifacts_produced>
## Artifacts this phase produces (created/changed by THIS plan)

- Raw `z-*` classes replaced with `style={{ zIndex: config.ui.z.X }}` across: `ShowView` (content), `InstallBanner`/`UpdateToast` (toast), `FabMenu` (fabScrim + fab), `CometTrail`/`SearchSheet` (sheet/sheetScrim), `AlbumDetail`/`ArchiveBrowser`/`RecapView`/`SetlistView` (sheet/sheetScrim).
- Annotation on `2026-07-17-bottom-sheets-smooth-up-down-animation-always-on-top-layerin.md` (z-half done; animation half deferred).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Migrate content/toast/FAB tiers (ShowView, InstallBanner, UpdateToast, FabMenu)</name>
  <read_first>
    - .planning/phases/08-on-device-ui-polish-accessibility/08-PATTERNS.md §"Shared Patterns — Z-index tier migration (D-04)" (the grep-verified file:line→tier table)
    - packages/app/src/show/ShowView.tsx (`relative z-10` at ~:145 → content)
    - packages/app/src/components/InstallBanner.tsx (`z-10` at ~:90 → toast), packages/app/src/components/UpdateToast.tsx (`z-10` at ~:33 → toast)
    - packages/app/src/show/FabMenu.tsx (`z-20` scrim at ~:99 → fabScrim; `z-30` menu at ~:105 → fab)
    - packages/app/src/config.ts (`config.ui.z`)
  </read_first>
  <action>
    In each file, replace the raw Tailwind `z-NN` class on the fixed/relative overlay with an inline `style={{ zIndex: config.ui.z.X }}` per the PATTERNS tier map: ShowView `z-10`→`content`; InstallBanner + UpdateToast `z-10`→`toast`; FabMenu scrim `z-20`→`fabScrim`, FabMenu menu `z-30`→`fab`. Do NOT change any other class, geometry, or behavior. Where a `style` prop already exists, merge `zIndex` in. Import `config` if not already imported.
  </action>
  <verify>
    <automated>pnpm vitest run packages/app/test/fabMenu.test.tsx packages/app/test/bottomOverlayInset.test.tsx</automated>
    <automated>pnpm --filter @guezzer/app exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - No raw `z-[0-9]` class remains in ShowView.tsx, InstallBanner.tsx, UpdateToast.tsx, FabMenu.tsx (greppable).
    - FabMenu scrim uses `config.ui.z.fabScrim` and rows use `config.ui.z.fab` (scrim strictly below sheets).
    - fabMenu + bottomOverlayInset tests green; app `tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Content, toast, and FAB tiers are on the config scale with stacking relationships preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate dex fullscreen/backdrop overlays (AlbumDetail, ArchiveBrowser, RecapView, SetlistView)</name>
  <read_first>
    - .planning/phases/08-on-device-ui-polish-accessibility/08-PATTERNS.md §"Z-index tier migration (D-04)" table (AlbumDetail :47; ArchiveBrowser :250 fullscreen + :353 backdrop; RecapView :103,:143; SetlistView :119,:129)
    - packages/app/src/dex/AlbumDetail.tsx, packages/app/src/dex/ArchiveBrowser.tsx, packages/app/src/dex/RecapView.tsx, packages/app/src/dex/SetlistView.tsx
    - packages/app/src/config.ts (`config.ui.z`)
  </read_first>
  <action>
    Replace each raw `z-NN` with inline `style={{ zIndex: config.ui.z.X }}`: fullscreen `bg-surface` route-overlays (AlbumDetail :47, ArchiveBrowser :250, RecapView :103/:143, SetlistView :119/:129) → `sheet`; ArchiveBrowser's modal backdrop at :353 → `sheetScrim`. These are D-04 layering only — do NOT wrap them in `<Sheet>` (they are route-overlays, not audited dialogs; Open Q2 resolved: migrate the z-literal, keep the shell). No geometry/behavior change.
  </action>
  <verify>
    <automated>pnpm vitest run packages/app/test/archiveBrowser.test.tsx packages/app/test/recapView.test.tsx</automated>
    <automated>pnpm --filter @guezzer/app exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - No raw `z-[0-9]` class remains in AlbumDetail.tsx, ArchiveBrowser.tsx, RecapView.tsx, SetlistView.tsx.
    - Fullscreen overlays use `config.ui.z.sheet`; ArchiveBrowser backdrop uses `config.ui.z.sheetScrim`.
    - archiveBrowser + recapView tests green; app `tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>All dex overlays are on the z scale; no literal remains.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate show overlays (CometTrail, SearchSheet) + annotate the bottom-sheets todo</name>
  <read_first>
    - .planning/phases/08-on-device-ui-polish-accessibility/08-PATTERNS.md §"Z-index tier migration (D-04)" table (CometTrail :225 backdrop; SearchSheet :99 fullscreen)
    - packages/app/src/show/CometTrail.tsx, packages/app/src/show/SearchSheet.tsx
    - .planning/todos/pending/2026-07-17-bottom-sheets-smooth-up-down-animation-always-on-top-layerin.md (the folded todo — D-04 z-half done, animation half deferred)
    - packages/app/src/config.ts
  </read_first>
  <action>
    Replace CometTrail's `z-30` backdrop (:225) with `config.ui.z.sheetScrim` (or `sheet` if it is the content layer — match its current role) and SearchSheet's `z-30` fullscreen (:99) with `config.ui.z.sheet`. No behavior change; SearchSheet stays a route-overlay (NOT an audited dialog — no `<Sheet>` wrap, per Open Q2).
    Then append an annotation block to the bottom-sheets todo file noting: the centralized z-index scale half is COMPLETE in Phase 8 (D-04, `config.ui.z`, all 24 literals migrated); the slide-up/down + scrim cross-fade ANIMATION half remains deferred to a later polish pass; keep the todo `pending`.
  </action>
  <verify>
    <automated>pnpm vitest run packages/app/test/cometTrail.test.tsx</automated>
    <automated>pnpm --filter @guezzer/app exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - No raw `z-[0-9]` class remains in CometTrail.tsx or SearchSheet.tsx.
    - App-wide greppable invariant: `rg 'z-\[?[0-9]' packages/app/src` returns zero matches for stacking z classes (excluding unrelated tokens like `z-index` in comments).
    - The bottom-sheets todo file contains the D-04-done / animation-deferred annotation and remains `pending`.
    - cometTrail test green; app `tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Every remaining z literal is on the scale; the folded todo records the split status.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config value → inline style | z tiers move from static Tailwind classes to a JS-config-sourced inline style |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-13 | Tampering | z-tier miswiring (a toast over a modal / FAB under content) | mitigate | Follow the PATTERNS grep-verified file:line→tier map exactly; the FabMenu-scrim-below-sheet and toast-below-sheet invariants are asserted by the no-`z-[0-9]` grep + fabMenu/bottomOverlayInset tests. |
| T-08-14 | Error Handling (V7) | overlay guard states | mitigate | Pure className→inline-style swap; no render guard or early-return touched. |
| T-08-SC | Tampering (supply chain) | npm installs | accept | No new packages; style-only edits + one todo annotation. |
</threat_model>

<verification>
- `rg 'z-\[?[0-9]' packages/app/src` → zero stacking-z matches (D-04 complete).
- `pnpm vitest run packages/app` green.
- Bottom-sheets todo annotated, still `pending`.
</verification>

<success_criteria>
- No raw `z-*` literal remains in `packages/app/src`; every stacking value comes from `config.ui.z` with all prior visual relationships preserved.
- The folded todo records: z-scale done (D-04), animation deferred.
</success_criteria>

<output>
Create `.planning/phases/08-on-device-ui-polish-accessibility/08-05-SUMMARY.md` when done
</output>
