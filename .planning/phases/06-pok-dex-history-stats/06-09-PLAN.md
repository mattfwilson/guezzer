---
phase: 06-pok-dex-history-stats
plan: 09
type: execute
wave: 7
depends_on: ["06-06", "06-08"]
files_modified:
  - packages/core/src/dex/compare.ts
  - packages/core/src/dex/share-stats.ts
  - packages/core/src/index.ts
  - packages/app/src/settings/importPicker.ts
  - packages/app/src/settings/SettingsView.tsx
  - packages/app/src/dex/CompareView.tsx
  - packages/app/src/dex/shareCard.ts
  - packages/app/src/dex/ShareCardSheet.tsx
  - packages/app/src/dex/DexHeader.tsx
  - packages/app/src/dex/DexView.tsx
  - packages/app/src/dex/RecapView.tsx
  - packages/app/src/config.ts
  - packages/core/test/dex/compare.test.ts
  - packages/core/test/dex/share-stats.test.ts
  - packages/app/test/importFork.test.ts
  - packages/app/test/shareCard.test.tsx
autonomous: true
requirements: [SHAR-01, SHAR-02]
must_haves:
  truths:
    - "Importing a friend's backup opens a read-only compare view and writes NOTHING to the database — ever; importing my own file keeps the Phase-5 merge path verbatim (D-17)"
    - "A file with no owner name prompts 'Whose dex is this?' before anything else happens — never guess, never merge"
    - "The compare view shows You vs {name} stat columns and the two diff lists, with the friend's name escaped and length-clamped; no adopt/merge/copy affordance exists"
    - "Share card generates a 1080×1350 PNG from derived dex stats and shares via navigator.share (files) with an anchor-download fallback; the File is built BEFORE the share tap (iOS transient activation, Pitfall 7)"
    - "Share/render failures surface calm copy and never touch app state"
  artifacts:
    - path: "packages/core/src/dex/compare.ts"
      provides: "compareDexes(mine, theirs) pure diff — never merges"
      contains: "compareDexes"
    - path: "packages/core/src/dex/share-stats.ts"
      provides: "buildShareStats(dexStats) → ShareCardData"
      contains: "buildShareStats"
    - path: "packages/app/src/dex/shareCard.ts"
      provides: "Canvas draw fn (ctx, data) + share/download flow"
      contains: "canShare"
  key_links:
    - from: "packages/app/src/settings/importPicker.ts"
      to: "compare vs merge fork"
      via: "envelope.owner vs meta ownerName BEFORE parseAndMergeImport (Pattern 5)"
      pattern: "ownerName"
    - from: "packages/app/src/dex/CompareView.tsx"
      to: "@guezzer/core deriveDex"
      via: "run twice — local snapshot + friend envelope — zero DB writes"
      pattern: "deriveDex"
    - from: "packages/app/src/dex/shareCard.ts"
      to: "packages/app/src/settings/exportDownload.ts idiom"
      via: "anchor-download fallback + never-throw contract"
      pattern: "createObjectURL"
---

<objective>
The phase's social payoff (SHAR-01/SHAR-02, D-17/D-18/D-19): the friend-file compare fork (structurally incapable of merging — the compare path never calls the two functions that write), the read-only CompareView, and the canvas-rendered PNG share card with Web Share + download fallback.

User story slice: *As a friend group hitting multiple nights, we want to swap dex files and trade a brag card, so that the collection game is shared — without anyone's file ever inflating anyone else's counts.*

Purpose: D-17's guarantee is structural (RESEARCH Pattern 5 — fork BEFORE `parseAndMergeImport`); the share flow must pre-build the File before the tap (Pitfall 7, iOS `NotAllowedError`) and keep all stat assembly in core because jsdom has no canvas (Pitfall 8).
Output: two core modules, the import fork, CompareView, shareCard + preview sheet, Share CTAs in DexHeader + RecapView, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-pok-dex-history-stats/06-CONTEXT.md
@.planning/phases/06-pok-dex-history-stats/06-RESEARCH.md
@.planning/phases/06-pok-dex-history-stats/06-PATTERNS.md
@.planning/phases/06-pok-dex-history-stats/06-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Core compareDexes + buildShareStats</name>
  <files>packages/core/src/dex/compare.ts, packages/core/src/dex/share-stats.ts, packages/core/src/index.ts, packages/core/test/dex/compare.test.ts, packages/core/test/dex/share-stats.test.ts</files>
  <read_first>
    - packages/core/src/dex/derive-dex.ts (DexStats/DexSnapshotInput — compare runs deriveDex twice per RESEARCH Pattern 1; envelope v2 is structurally assignable to DexSnapshotInput)
    - packages/core/src/data-safety/merge.ts (identity-key discipline: songId only, NEVER names — "Bit"×3/"Jam"×2/"Ghost"×2 duplicates verified)
    - packages/core/src/dex/rarity.ts (tier data for diff lists + tier breakdown)
    - 06-UI-SPEC.md §Layout 4 (compare columns: completion %, songs caught, shows, per-tier counts) + §Layout 5 (card contents: wordmark, completion hero, {x}/{y}, show count, rarest catch + tier, tier breakdown, latest/best show)
  </read_first>
  <behavior>
    - compareDexes(mine, theirs) — both DexStats — returns { columns: { mine, theirs } (completion, caught, shows, per-tier counts), onlyMine: songId[], onlyTheirs: songId[], shared: songId[] } with diffs keyed strictly by songId.
    - The function is pure and read-only: inputs are not mutated (deep-equal check in test).
    - buildShareStats(dexStats, archive) returns ShareCardData: completionPct, caught, total, showCount, rarestCatch { songName, tier } | null, tierBreakdown (count per tier over CAUGHT songs), latestShow { date, venue } | null.
    - Zero-catch dex yields a valid card (0%, empty breakdown, null rarest) — no NaN.
  </behavior>
  <action>
    Create `packages/core/src/dex/compare.ts` exporting `CompareResult` and `compareDexes(mine: DexStats, theirs: DexStats): CompareResult` — set arithmetic over the perSong maps (caught = sightings > 0), tier counts via the stats' tier fields; sort diff lists by tier rarity (legendary first — trophy browsing) then name via a caller-supplied name resolver or songId (keep core name-free: return songIds; the view resolves names from archive.songs). Create `packages/core/src/dex/share-stats.ts` exporting `ShareCardData` and `buildShareStats(dex: DexStats, archive: ArchiveArtifact): ShareCardData` (song name for rarest resolved from archive.songs; latestShow from the newest attended show — accept a precomputed value in DexStats if 06-03 exposed one, else derive from the snapshot's attendance passed alongside; keep the signature minimal and consistent with what deriveDex already outputs — read it first). Barrel-export. Tests: two-fixture compare (hand-computed diff lists + columns; input immutability), empty-vs-populated compare, share stats happy path + zero-catch path.
  </action>
  <verify>
    <automated>npx vitest run --project @guezzer/core test/dex/compare.test.ts test/dex/share-stats.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - compare.test.ts pins onlyMine/onlyTheirs/shared for a hand-built pair and asserts inputs unmutated.
    - share-stats.test.ts pins every ShareCardData field for a fixture dex and the zero-catch case (no NaN — asserted).
    - grep confirms compare.ts contains no DB/table/import from data-safety merge paths (pure diff only).
    - Full core suite green.
  </acceptance_criteria>
  <done>Comparing and card-stat assembly are pure core — the friend path gets full stats with zero writes by construction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Import fork + CompareView (D-17, Pattern 5)</name>
  <files>packages/app/src/settings/importPicker.ts, packages/app/src/settings/SettingsView.tsx, packages/app/src/dex/CompareView.tsx, packages/app/src/dex/DexView.tsx, packages/app/src/config.ts, packages/app/test/importFork.test.ts</files>
  <read_first>
    - packages/app/src/settings/importPicker.ts (pickAndImport lines ~36-58 — the commit seam; openBackupFilePicker reused as-is; the fork goes AFTER zod validation, BEFORE parseAndMergeImport/importSnapshot)
    - packages/app/src/settings/SettingsView.tsx (import-result rendering lines ~99-123 — where the fork's outcomes surface)
    - packages/core/src/data-safety/export-schema.ts (exportEnvelope.safeParse — the validation gate; owner field from 06-06)
    - packages/app/src/db/db.ts (getMeta ownerName)
    - 06-UI-SPEC.md §Layout 4 + §Copywriting (exact strings: "Viewing {name}'s dex — nothing was added to yours.", "You" / "{name}", "Only {name} has caught ({n})" / "Only you have caught ({n})", "Whose dex is this?", "This is {name}'s dex — opening compare view.")
  </read_first>
  <behavior>
    - Import fork: (a) file fails envelope validation → existing rejection path unchanged; (b) envelope.owner matches local meta ownerName (case-insensitive trim) or local ownerName unset AND user confirms it's theirs → existing Phase-5 merge path VERBATIM; (c) owner names someone else → the PARSED envelope routes to CompareView with zero DB calls; (d) owner is null → "Whose dex is this?" prompt; the answer routes to (b) or (c).
    - Fake-indexeddb test proves the friend path writes nothing: snapshot all tables before/after → deep-equal.
    - CompareView: persistent banner, You vs {name} columns (tabular-nums), two collapsible diff lists with TierBadges + names resolved from archive.songs, Close — and NO adopt/merge/copy control exists.
    - The friend's name renders escaped and visually clamped everywhere it appears.
  </behavior>
  <action>
    Refactor importPicker: extract a `classifyImport(rawJson, localOwnerName): { kind: "invalid"; error } | { kind: "mine"; rawJson } | { kind: "friend"; envelope: ExportEnvelope } | { kind: "unowned"; envelope }` step running `exportEnvelope.safeParse` first (validation before ANY routing); `pickAndImport` handles "mine" exactly as today (parseAndMergeImport + importSnapshot untouched), returns the friend/unowned outcomes to the caller — the compare path structurally never reaches the two writing functions (Pattern 5; envelope v1 files arriving here have no owner → "unowned"). In SettingsView: on "friend"/"unowned" outcomes show the fork copy ("This is {name}'s dex — opening compare view." / the "Whose dex is this?" prompt with a text input) and hand the envelope up to open CompareView (mount it from DexView or App-level state — component state, no new routes; pick the simplest host consistent with DexView's drill-in pattern). Create `CompareView.tsx`: derive `theirs = deriveDex(envelope, archive, albums, rarity)` (envelope v2 is a DexSnapshotInput superset) and `mine` from useDexStats; render banner (muted, always visible), stat columns, diff sections (collapsible, tier-sorted, names from archive.songs as React text), Close. Clamp the displayed name (CSS truncate + the schema's 40-char cap already bounds it). Add `config.copy.compare` block. Tests in importFork.test.ts: classifyImport all four kinds; the zero-writes deep-equal proof for the friend path; owner match case-insensitivity; v1-file → unowned prompt path.
  </action>
  <verify>
    <automated>npx vitest run --project @guezzer/app test/importFork.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - importFork.test.ts: all four classify kinds pinned; friend-path before/after table snapshots deep-equal (the D-17 proof); merge path still green for "mine".
    - grep confirms CompareView.tsx imports neither parseAndMergeImport nor importSnapshot nor db write helpers.
    - CompareView renders the banner string from config and has no button whose handler writes (source assertion).
    - Full app suite green.
  </acceptance_criteria>
  <done>A friend's file is a read-only trophy case: full stats, zero writes, structurally guaranteed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Share card — canvas PNG + Web Share + CTAs (D-18/D-19, Pitfalls 7-8)</name>
  <files>packages/app/src/dex/shareCard.ts, packages/app/src/dex/ShareCardSheet.tsx, packages/app/src/dex/DexHeader.tsx, packages/app/src/dex/DexView.tsx, packages/app/src/dex/RecapView.tsx, packages/app/src/config.ts, packages/app/test/shareCard.test.tsx</files>
  <read_first>
    - packages/app/src/settings/exportDownload.ts (anchor-download + never-throw idiom lines ~35-76 — the fallback branch and the { ok } contract)
    - packages/core/src/dex/share-stats.ts (Task 1 ShareCardData — ALL numbers come from core; the draw fn only draws)
    - 06-RESEARCH.md §Code Examples "Share flow that survives iOS transient activation" (pre-build the File on preview open; canShare gate; user-cancel is normal) + Pitfall 8 (jsdom has no canvas — draw takes (ctx, data))
    - 06-UI-SPEC.md §Layout 5 (card contents/colors) + §Copywriting ("Share card", "Card saved to your downloads.", "Couldn't build the card." + body)
  </read_first>
  <behavior>
    - drawShareCard(ctx, data, opts) draws the fixed 1080×1350 layout (config share.CARD_WIDTH/CARD_HEIGHT) on #0C0C10: Guezzer wordmark, completion % hero, {caught}/{total}, show count, rarest catch + tier word (Legendary in gold), tier breakdown line, latest show — system font stack, contract colors only; testable against a recorded mock ctx (fillText/fillRect call log).
    - Opening the share preview builds the PNG File immediately (canvas.toBlob → File) — BEFORE any share tap.
    - Share tap: navigator.canShare({ files: [file] }) → navigator.share synchronously with the pre-built file; unsupported → anchor download + "Card saved to your downloads."; user-cancel (AbortError) is silent.
    - getContext returning null or toBlob failing → "Couldn't build the card." calm copy; { ok: false }; app state untouched.
    - The accent Share card CTA appears in the DexHeader and the RecapView footer, opening the preview sheet.
  </behavior>
  <action>
    Create `shareCard.ts` exporting `drawShareCard(ctx: CanvasRenderingContext2D, data: ShareCardData, opts: { width: number; height: number }): void` (pure draw — no canvas creation inside) and `buildShareCardFile(data): Promise<{ ok: true; file: File; previewUrl: string } | { ok: false; error: string }>` (creates the canvas, guards getContext null, draws, toBlob → File "guezzer-dex.png" image/png, objectURL for the <img> preview; never throws) and `shareOrDownload(file): Promise<{ ok: boolean; method: "share" | "download" | "cancelled" | "failed" }>` (canShare gate → share in try/catch with AbortError → "cancelled"; else the exportDownload anchor idiom + revokeObjectURL in finally). Create `ShareCardSheet.tsx` (bottom-sheet per EndShowDialog idiom): on open, immediately `buildShareCardFile(buildShareStats(dex, archive))`; render the preview <img>, a Share button calling shareOrDownload with the ALREADY-BUILT file (the Pitfall-7 contract — no async work between tap and share), the fallback/success/failure copy states. Add the accent `Share2` CTA to DexHeader (reserved accent use #1) and the RecapView footer ("Share card" accent · "Done" neutral per spec), both opening the sheet with current dex stats. Add `config.share.CARD_WIDTH: 1080`, `CARD_HEIGHT: 1350` and `config.copy.share` block. Tests in shareCard.test.tsx: drawShareCard against a recorded mock ctx asserts the hero percentage string and tier-breakdown text were drawn (fillText call args) and background fillRect uses #0C0C10; buildShareCardFile returns { ok: false } with the calm error when getContext is null (jsdom default — the natural test); shareOrDownload picks share when canShare true (mocked navigator), download when absent, "cancelled" on AbortError.
  </action>
  <verify>
    <automated>npx vitest run --project @guezzer/app test/shareCard.test.tsx</automated>
    <human-check>End-of-phase device gate: real navigator.share sheet opens on iPhone with the PNG attached; download fallback verified on desktop.</human-check>
  </verify>
  <acceptance_criteria>
    - shareCard.test.tsx passes: mock-ctx draw assertions, getContext-null calm failure, canShare-gated share vs download vs cancelled.
    - grep confirms shareCard.ts contains no stat arithmetic (numbers come from ShareCardData) and DexHeader/RecapView reference the sheet.
    - The Share button handler performs no await between the tap and navigator.share (source assertion: share is called with a variable captured from state, not a fresh toBlob).
    - Full app suite + `npx tsc --noEmit` green.
  </acceptance_criteria>
  <done>One tap turns the dex into a brag card in the OS share sheet — or a downloaded PNG — never an error state that touches data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Friend-supplied backup file → app | Hostile JSON crosses into parse + display (never into the DB on the friend path) |
| Canvas/share APIs → OS | Generated file handed to the OS share sheet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-24 | Tampering (attendance inflation) | import fork | mitigate | Structural D-17 fork: classifyImport routes friend files before parseAndMergeImport/importSnapshot are reachable; zero-writes deep-equal test pins it |
| T-06-25 | Tampering (prototype pollution / oversized fields) | friend envelope | mitigate | exportEnvelope strictObject whole-file gate BEFORE routing; owner .max(40) clamp (06-06) |
| T-06-26 | Injection (XSS) | friend owner name + song names in CompareView | mitigate | React text only + CSS clamp; never dangerouslySetInnerHTML (inherited rule, grep-verifiable) |
| T-06-27 | Denial of service (share failure) | canvas/share flow | mitigate | Never-throw contract: getContext/toBlob/share all guarded; { ok:false } + calm copy; app state untouched |
| T-06-SC | Tampering (supply chain) | package installs | mitigate | No new dependencies in this plan |
</threat_model>

<verification>
- `npx vitest run` (both projects) green; `npx tsc --noEmit` green.
- Dev-server spot check: import a friend fixture file → compare view, local counts unchanged; share preview renders.
</verification>

<success_criteria>
- SHAR-01: friend exchange complete — v2 files fork to compare; own files merge; v1 files prompt.
- SHAR-02/D-18/D-19: PNG card with completion %, rarest catch, show count, tier breakdown, total caught, latest show; Web Share + download fallback.
- D-17: zero-DB-writes on the friend path proven by test, guaranteed by structure.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 09)

New symbols (exclude from source-drift verification — created here):
- `CompareResult`, `compareDexes` — `packages/core/src/dex/compare.ts`
- `ShareCardData`, `buildShareStats` — `packages/core/src/dex/share-stats.ts`
- `classifyImport` — `packages/app/src/settings/importPicker.ts`
- Components `CompareView`, `ShareCardSheet` — `packages/app/src/dex/`
- `drawShareCard`, `buildShareCardFile`, `shareOrDownload` — `packages/app/src/dex/shareCard.ts`
- Config: `config.share.CARD_WIDTH/CARD_HEIGHT`, `config.copy.compare.*`, `config.copy.share.*` — `packages/app/src/config.ts`
- Barrel re-exports — `packages/core/src/index.ts`
- New tests: `compare.test.ts`, `share-stats.test.ts`, `importFork.test.ts`, `shareCard.test.tsx`
</artifacts_produced>

<output>
Create `.planning/phases/06-pok-dex-history-stats/06-09-SUMMARY.md` when done
</output>
