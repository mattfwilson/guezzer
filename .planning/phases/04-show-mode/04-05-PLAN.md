---
phase: 04-show-mode
plan: 05
type: execute
wave: 4
depends_on: [04-04, 04-02]
files_modified:
  - packages/app/src/show/ActionBar.tsx
  - packages/app/src/show/SearchSheet.tsx
  - packages/app/src/show/ShowView.tsx
autonomous: true
requirements: [SHOW-04, SHOW-05]
must_haves:
  truths:
    - "From the pre-opener 'Tap the opener' state, Search selects the opening song, seeds currentSongId, and the first prediction fan renders — closing the live loop the previous slice built (SHOW-04, opener seeding)"
    - "The opening song (nothing was predicting it) logs as a miss (empty shown-fan), keeping the denominator honest (D-06/D-08)"
    - "An always-visible Search opens fuzzy search over the full catalog; selecting a result logs a miss and recenters — as fast as tapping a hit (SHOW-04)"
    - "An always-visible ??? button logs a placeholder miss immediately with no confirm and recenters (SHOW-05/D-14)"
    - "Search with no result offers logging it as ??? inline (rename later from the trail)"
  artifacts:
    - path: "packages/app/src/show/ActionBar.tsx"
      provides: "fixed two-row bottom action bar; primary Search + ??? wired"
      contains: "env(safe-area-inset-bottom)"
    - path: "packages/app/src/show/SearchSheet.tsx"
      provides: "fuzzy catalog search sheet over core searchCatalog; seeds opener + logs misses"
      contains: "searchCatalog|makeCatalogSearcher"
  key_links:
    - from: "packages/app/src/show/SearchSheet.tsx"
      to: "packages/core searchCatalog"
      via: "makeCatalogSearcher(toCatalog(nodes))"
      pattern: "makeCatalogSearcher"
    - from: "packages/app/src/show/ActionBar.tsx"
      to: "packages/app/src/db/db.ts logSong"
      via: "??? → logSong(miss placeholder)"
      pattern: "logSong|onUnknown"
---

<objective>
Deliver the miss paths AND close the live loop the previous slice built: the persistent bottom ActionBar (D-13 two-row layout) with its primary row (Search + ???), and the SearchSheet wired to the core `searchCatalog` (fuse.js). Search doubles as the **opener-seeding affordance** — from the 04-04 "Tap the opener" state it selects the opening song, seeds `currentSongId`, and makes the first prediction fan render, so the full tap→log→persist→restore loop becomes interactively demonstrable here. Both routes log a miss and recenter with zero confirmation friction so tracking never stalls.

Purpose: SHOW-04 (search misses as fast as hits, and seeds the opener) and SHOW-05 (??? so tracking never stalls) — the honest-denominator half of the tally (D-08). Completes the slice-1 end-to-end user demonstration deferred from 04-04.
Output: ActionBar (primary row live; secondary row scaffolded for 04-06), SearchSheet (opener-seed + mid-show miss), and their mount in ShowView.
</objective>

## Phase Goal (MVP user story)

**As a** KGLW showgoer, **I want to** log the opener and any song the model missed by searching or tapping ??? just as fast as tapping a predicted orb, **so that** the loop starts, never stalls, and my hit-rate stays honest.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-show-mode/04-UI-SPEC.md
@.planning/phases/04-show-mode/04-PATTERNS.md
@.planning/phases/04-show-mode/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ActionBar (two-row D-13 layout) + ??? instant-miss log</name>
  <files>packages/app/src/show/ActionBar.tsx, packages/app/src/show/ShowView.tsx</files>
  <read_first>
    - .planning/phases/04-show-mode/04-UI-SPEC.md §Layout region 4 (the exact two-row mockup: `[Search][??? Unknown]` / `[Set break][Encore][Undo]`) + §Copywriting (??? / Unknown sublabel; no-confirm rule) + §Design System (icon set: Search, CircleHelp, Undo2, SkipForward/Minus, Star)
    - .planning/phases/04-show-mode/04-PATTERNS.md "ActionBar.tsx — best UI analog" (BottomTabBar copy-from excerpt: fixed bottom, env(safe-area-inset-bottom), min-h-11 min-w-11, lucide size 22, text-[14px] font-semibold, theme tokens)
    - packages/app/src/components/BottomTabBar.tsx (the fixed-bottom safe-area idiom to mirror)
    - packages/app/src/db/db.ts (logSong from 04-01 — placeholder entry shape: songId null, songName "???", isPlaceholder true, outcome "miss")
    - packages/app/src/show/ShowView.tsx (04-04 — mount point + current sessionId/currentSetNumber + recenter-by-write-through; the ActionBar placeholder slot to replace)
  </read_first>
  <action>
    Create `ActionBar.tsx` mirroring BottomTabBar: fixed bottom, `env(safe-area-inset-bottom)`, two rows per the D-13 mockup — primary row Search + ??? (both ≥44px, lucide `Search` + `CircleHelp`), secondary row Set break / Encore / Undo rendered as disabled/placeholder buttons wired in 04-06 (present now so layout is final; do not implement their handlers here). Use inherited theme tokens (bg-elevated, border-hairline, text-text-muted/primary) — Search/??? are NOT accent (accent reserved for Start Show). Sits above the safe-area inset; must not overlap the fan. The ActionBar (Search + ???) is visible in BOTH the pre-opener "Tap the opener" state and the active-fan state, so the opener is always enterable. Wire the ??? button to `onUnknown`: immediately `logSong(sessionId, { songId:null, songName:"???", setNumber:currentSetNumber, outcome:"miss", shownFanSongIds:currentFanIds, isPlaceholder:true, loggedAt })` — no confirm (D-14) — the write-through recenters via useLiveQuery. Mount ActionBar in ShowView (replace the 04-04 placeholder slot), passing the Search-open and onUnknown handlers.
  </action>
  <verify>
    <automated>npx tsc -p packages/app --noEmit && npx vitest run packages/app</automated>
    <human-check>On device: tap ??? → a "???" node appears in the trail immediately with a red (miss) ring and the orbit recenters, no dialog. (Perceptual per VALIDATION.)</human-check>
  </verify>
  <acceptance_criteria>
    - Source assertion: ActionBar uses `env(safe-area-inset-bottom)` and every button carries `min-h-11 min-w-11`.
    - Source assertion: the ??? handler calls `logSong` with `outcome:"miss"` and `isPlaceholder:true` and NO confirmation dialog (D-14).
    - Source assertion: the ActionBar is mounted in both the pre-opener and active-fan ShowView branches (opener always enterable).
    - `npx tsc -p packages/app --noEmit` passes; `npx vitest run packages/app` green.
  </acceptance_criteria>
  <done>The persistent two-row action bar is mounted (in pre-opener and active states); ??? logs an instant placeholder miss and recenters with zero friction.</done>
</task>

<task type="auto">
  <name>Task 2: SearchSheet over core searchCatalog — seeds the opener + logs mid-show misses</name>
  <files>packages/app/src/show/SearchSheet.tsx, packages/app/src/show/ShowView.tsx</files>
  <read_first>
    - .planning/phases/04-show-mode/04-UI-SPEC.md §Layout ("Tap Search → fuzzy over the full catalog via core searchCatalog; selecting a result logs a miss + recenters; must feel as fast as a hit") + §Copywriting (Center "Tap the opener" hint — seeds via Search; search placeholder "Search the catalog"; no-match copy offering ???) + §Typography (search field ≥16px Body to avoid iOS form-zoom)
    - .planning/phases/04-show-mode/04-PATTERNS.md "SearchSheet.tsx" (AppMenu overlay-sheet analog)
    - packages/app/src/components/AppMenu.tsx (overlay/sheet structure to mirror — read directly)
    - packages/core/src/index.ts (04-02 barrel: makeCatalogSearcher, toCatalog, SearchResult)
    - packages/app/src/show/matrix.ts (04-03 — the loaded matrix nodes to build the catalog from)
    - packages/app/src/show/useShowSession.ts (04-04 — currentSongId null-safe; shownFanSongIds is empty pre-opener) + packages/app/src/db/db.ts (logSong)
  </read_first>
  <action>
    Create `SearchSheet.tsx` (AppMenu overlay idiom): a full-width/large sheet with a ≥16px Body search input (placeholder from copy.show; avoids iOS zoom), memoizing `makeCatalogSearcher(toCatalog(matrix.nodes))` once and querying on input change (fuse.js is sub-ms on 264 items). Render results as ≥44px rows; selecting a row calls `onSelect(result)` which `logSong(sessionId, { songId, songName, setNumber:currentSetNumber, outcome:"miss", shownFanSongIds:currentFanIds, isPlaceholder:false, loggedAt })` and closes the sheet — the write-through recenters via useLiveQuery (no confirm; the miss path must feel as fast as a hit tap, D-06/SHOW-04). This is ALSO the opener-seeding path: from the 04-04 pre-opener state `currentFanIds` is empty, so the opening song is correctly classified a **miss** (nothing predicted it, D-06/D-08) AND becomes the new `currentSongId`, which makes `useShowSession` render the first prediction fan — closing the live loop. No-results state renders the copy.show no-match line and an inline "Log as ???" action reusing the ??? handler (D-14). Wire ShowView to open SearchSheet from the ActionBar Search button. All kglw-derived names render as text only (never dangerouslySetInnerHTML).
  </action>
  <verify>
    <automated>npx tsc -p packages/app --noEmit && npx vitest run packages/core -t "searchCatalog" && npx vitest run packages/app</automated>
    <human-check>On device — the full slice-1 loop is now demonstrable end-to-end: Start Show → "Tap the opener" → Search → select the opener → the first fan renders → tap a predicted orb → it becomes center and a fresh fan appears (live recenter, SHOW-03) → force-quit + relaunch restores the exact session (SHOW-11). Also: a no-match search shows the ??? offer. (SHOW-04 "as fast as a hit".)</human-check>
  </verify>
  <acceptance_criteria>
    - Source assertion: SearchSheet imports `makeCatalogSearcher`/`toCatalog` from `@guezzer/core` (search stays in core — never re-implemented in app).
    - Source assertion: the search input font-size is ≥16px (Body) to prevent iOS form-zoom.
    - Source assertion: selecting a result calls `logSong` with `outcome:"miss"` and closes immediately (no confirm step); pre-opener selection seeds `currentSongId` (empty shown-fan → miss).
    - `npx vitest run packages/core -t "searchCatalog"` and `npx vitest run packages/app` green; typecheck passes.
  </acceptance_criteria>
  <done>SearchSheet fuzzy-searches the full catalog via the core function, seeds the opener from the pre-opener state, and logs any selection as a miss that recenters instantly; no-match offers ???. The slice-1 tap→log→persist→restore loop is now fully demonstrable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user search query → fuse.js | Bounded string to core searchCatalog only; never DOM/eval/regex/network |
| kglw song names → DOM | Result rows render untrusted-origin text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | Denial of Service | SearchSheet input | mitigate | Query goes only to the bounded core `searchCatalog` (264 items, no regex-from-user, no network); memoized searcher (ASVS V5). |
| T-04-12 | Tampering (XSS) | SearchSheet result rows | mitigate | Song names render as React text; never `dangerouslySetInnerHTML` (ASVS V5, SCHEMA §12). |
</threat_model>

<verification>
- `npx vitest run packages/app` + `npx vitest run packages/core -t "searchCatalog"` green.
- `npx tsc -p packages/app --noEmit` passes.
- On-device: the full slice-1 loop (opener-seed → tap-orb recenter → restore) + SHOW-04/05 speed checks (manual per VALIDATION).
</verification>

<success_criteria>
Search seeds the opener from the pre-opener state and the first fan renders; both miss paths work with zero confirmation friction (Search + ??? log misses that recenter as fast as a hit); no-match offers ???. The slice-1 tap→log→persist→restore loop is now fully user-demonstrable, and the tally denominator stays honest.
</success_criteria>

## Artifacts this phase produces (this plan)

- **Components:** `ActionBar` (fixed two-row bottom bar; primary Search/??? live, secondary row scaffolded), `SearchSheet` (fuzzy catalog search over core `searchCatalog`; opener-seed + miss log)
- **Wiring:** ActionBar + SearchSheet mounted in `ShowView` (pre-opener + active); ??? and search-select both call `logSong` with `outcome:"miss"`; Search closes the slice-1 loop by seeding the opener

<output>
Create `.planning/phases/04-show-mode/04-05-SUMMARY.md` when done.
</output>
