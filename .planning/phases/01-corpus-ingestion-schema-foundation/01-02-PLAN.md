---
phase: 01-corpus-ingestion-schema-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - packages/core/src/domain/types.ts
  - packages/core/src/ingest/normalize.ts
  - packages/core/src/cli/normalize-corpus.ts
  - packages/core/src/cli/refresh.ts
  - packages/core/src/index.ts
  - packages/core/test/normalize.test.ts
  - package.json
  - data/normalized/corpus.json
autonomous: true
requirements: [DATA-02, DATA-03]
tags: [ingestion, normalizer, cli, walking-skeleton]

must_haves:
  truths:
    - "One documented command (`pnpm refresh --normalize-only --input data/samples`) runs validate → normalize → write-artifact on real committed sample data and exits 0"
    - "The written artifact carries the D-08 header: schemaVersion, generatedAt, latestShowDate, showCount, songCount"
    - "The normalizer derives set structure ONLY from setnumber grouping + position sort — never from transition_id"
    - "Only artist_id === 1 rows enter normalized output; Live Session shows are excluded; the Unknown sentinel is a flagged placeholder, not a catalog song"
    - "A vitest test proves the sample-data pipeline output (25 shows: 1 rr1010 + 26 2013 shows minus 2 Live Sessions)"
  artifacts:
    - path: "packages/core/src/domain/types.ts"
      provides: "Clean domain types with zero raw API field names"
      exports: ["NormalizedCorpus", "NormalizedShow", "SetSection", "Performance"]
    - path: "packages/core/src/ingest/normalize.ts"
      provides: "Pure raw-rows → NormalizedCorpus function"
      exports: ["normalizeCorpus"]
    - path: "packages/core/src/cli/refresh.ts"
      provides: "THE one documented command (skeleton: --normalize-only path)"
    - path: "data/normalized/corpus.json"
      provides: "Versioned artifact (interim, sample-derived; replaced by full corpus in plan 01-04)"
      contains: "schemaVersion"
    - path: "packages/core/src/index.ts"
      provides: "Public API barrel — raw API types NOT re-exported (anti-corruption boundary)"
  key_links:
    - from: "packages/core/src/cli/normalize-corpus.ts"
      to: "packages/core/src/ingest/normalize.ts"
      via: "thin CLI wrapper calling pure core function"
      pattern: "normalizeCorpus"
    - from: "packages/core/src/ingest/normalize.ts"
      to: "packages/core/src/ingest/api-types.ts"
      via: "rows validated with rawSetlistRowCensus before normalization"
      pattern: "rawSetlistRowCensus"
    - from: "package.json"
      to: "packages/core/src/cli/refresh.ts"
      via: "root refresh script"
      pattern: "cli/refresh.ts"
---

<objective>
Complete the Walking Skeleton: a provisional (census-mode) normalizer turns the committed real samples into a versioned `data/normalized/corpus.json` with the D-08 header, driven end-to-end by one documented CLI command, proven by tests.

Purpose: Proves the full pipeline shape (read raw JSON → zod-validate → filter artist_id===1 → group by setnumber → write artifact) on real data before any live API traffic. Implements D-08, D-13, D-14, D-15, D-16 (provisional allowlist); advances DATA-02 and DATA-03. The normalizer is finalized (locked schemas, census-confirmed allowlist, full corpus) in plan 01-04.
Output: Domain types, pure `normalizeCorpus`, `refresh.ts` skeleton CLI, interim sample-derived artifact, passing normalize tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-corpus-ingestion-schema-foundation/01-01-SUMMARY.md
@.planning/phases/01-corpus-ingestion-schema-foundation/01-RESEARCH.md
@docs/SCHEMA.md
@packages/core/src/ingest/api-types.ts
@packages/core/src/config.ts
</context>

## Artifacts this phase produces (plan 02 subset)

- **Files:** `packages/core/src/domain/types.ts`, `packages/core/src/ingest/normalize.ts`, `packages/core/src/cli/normalize-corpus.ts`, `packages/core/src/cli/refresh.ts`, `packages/core/src/index.ts`, `packages/core/test/normalize.test.ts`, `data/normalized/corpus.json`
- **Exported symbols:** `NormalizedCorpus`, `NormalizedShow`, `SetSection`, `Performance`, `TransitionKind`, `normalizeCorpus(rows, options)`, `parseFootnotesGuarded(raw)`
- **CLI flags (refresh.ts, this plan):** `--normalize-only`, `--input <dir>` (default `data/raw`), `--out <path>` (default `data/normalized/corpus.json`)
- **npm scripts (root):** `refresh` → `node packages/core/src/cli/refresh.ts`
- **Data contract:** `NormalizedCorpus` header fields `schemaVersion: 1`, `generatedAt`, `latestShowDate`, `showCount`, `songCount` — the first frozen contract in the system (Phase 2's sole input)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Domain types + pure normalizer (setnumber grouping, sentinel/cover/footnote semantics)</name>
  <files>packages/core/src/domain/types.ts, packages/core/src/ingest/normalize.ts, packages/core/test/normalize.test.ts</files>
  <read_first>
    - docs/SCHEMA.md (§3 set structure, §4 transition_id rules, §6 covers/sentinel, §8 footnotes, §10 settype — the normalizer implements THIS document)
    - .planning/phases/01-corpus-ingestion-schema-foundation/01-RESEARCH.md ("Anti-Patterns to Avoid" + Pitfalls 1/5/7 + "Code Examples" NormalizedCorpus header)
    - .planning/phases/01-corpus-ingestion-schema-foundation/01-PATTERNS.md ("Edge-case rows for fixtures" — the 4 real trap rows; "Error message convention")
    - packages/core/src/ingest/api-types.ts (rows are validated with rawSetlistRowCensus before normalization)
    - data/samples/rr1010.json, data/samples/showyear2013.json (test inputs)
  </read_first>
  <behavior>
    - Test 1: normalizing rr1010 rows yields 1 show (show_id 1678309429) with sets `["1","2"]` in order, 27 performances total, positions ascending within sets, set 1 ending at position 13
    - Test 2: normalizing showyear2013 rows yields 24 shows — the 2 `settype: "Live Session"` shows (PBS Studios) are EXCLUDED, with the exclusion counted in the returned stats
    - Test 3: the segue kind on rr1010 position 1 ("Mars For the Rich", transition_id 2) is `"segue"`; a transition_id 1 row maps to `"none"`; 4/5/6 map to `"terminal"` — and NO code path uses transition_id to determine set membership (Test 1's set split at position 13/14 comes from setnumber alone)
    - Test 4: the Unknown sentinel row (song_id 1, slug `_custom_`) becomes a Performance with `isPlaceholder: true`, still occupying its position; sentinel songs do NOT appear in the distinct-song count
    - Test 5: a 2013 cover row (`isoriginal: 0`, original_artist "The Reatards") gets `isCover: true` and `originalArtist` populated
    - Test 6: `footnotes: "[\"first confirmed performance\"]"` parses to `["first confirmed performance"]`; malformed footnotes string (e.g. `"[broken"`) is kept verbatim as `footnotesRaw` with `footnotesParsed: null` and does NOT throw (Pitfall 5)
    - Test 7: a synthetic row set with a position gap (1,2,4) throws an Error naming the show_id and showdate (contiguity assertion, RESEARCH fact 14)
    - Test 8: rows with `artist_id !== 1` are excluded and counted in returned stats (DATA-03 client-side artist filter)
  </behavior>
  <action>
    Write the failing tests first (import sample JSON directly from `data/samples/`), then implement.

    `packages/core/src/domain/types.ts` — clean domain model, ZERO raw API field names (anti-corruption boundary): `TransitionKind = "none" | "segue" | "terminal"` (union type, not enum — erasableSyntaxOnly); `Performance` { songId, songName, slug, position, transitionKind, transitionId (kept as provenance number), isCover, originalArtist (string | null), isPlaceholder, footnotesParsed (string[] | null), footnotesRaw (string | null), footnote (string) }; `SetSection` { setNumber (string: "1"|"2"|"3"|"e" — typed as string this plan, tightened after census), performances: Performance[] }; `NormalizedShow` { showId, date, showOrder, year, venue { venueId, name, city, state, country }, tourId, tourName, sets: SetSection[] }; `NormalizedCorpus` { schemaVersion: 1, generatedAt: string, latestShowDate: string, showCount: number, songCount: number, shows: NormalizedShow[] } exactly per the D-08 header in RESEARCH Code Examples.

    `packages/core/src/ingest/normalize.ts` — pure function `normalizeCorpus(rows: unknown[], options): { corpus: NormalizedCorpus; stats: NormalizeStats }`:
    1. Validate every row with `rawSetlistRowCensus` (locked schema swap happens in plan 01-04) — zod failure propagates with the error-message convention (field, value, example show).
    2. Filter to `artist_id === 1`, counting exclusions per D-12/DATA-03 (`stats.nonKglwRowsExcluded`).
    3. Group rows by `show_id`; sort shows by (showdate, showorder).
    4. Exclude shows whose settype is not in `config.settypeAllowlist` (D-16, provisional list; count as `stats.showsExcludedBySettype` with settype value). If a single show mixes settypes across rows, hard-fail naming the show.
    5. Per show: sort rows by `position`, assert contiguity 1..N (hard fail naming show_id + showdate on gaps), group into SetSections by `setnumber` in order of first appearance. NEVER branch on transition_id for structure (Pitfall 1).
    6. Map transition_id → TransitionKind: 2,3 → "segue"; 4,5,6 → "terminal"; 1 → "none"; any other value → "none" this plan (census-mode tolerance; locked schema in 01-04 will reject novel ids before this point).
    7. Sentinel handling: `config.sentinelSongIds` → `isPlaceholder: true`, excluded from the distinct-song count (Pitfall 7).
    8. Covers per D-13: `isoriginal === 0` → `isCover: true`, carry `original_artist` (empty string → null). Sandwiches per D-14: NO reprise detection — every appearance is its own positional Performance; ignore `isreprise` entirely.
    9. Footnotes per D-15: `parseFootnotesGuarded` — null → both fields null; parseable JSON array of strings → footnotesParsed; anything else → footnotesRaw verbatim, footnotesParsed null, never throw.
    10. Header: `schemaVersion: 1`, `generatedAt` ISO now, `latestShowDate` = max showdate of INCLUDED shows, `showCount` = included shows, `songCount` = distinct non-sentinel songIds across included shows.

    Expected sample-data outcomes for tests: rr1010 = 1 show/2 sets/27 performances; showyear2013 = 26 distinct shows − 2 Live Session = 24 included.
  </action>
  <verify>
    <automated>pnpm vitest run packages/core/test/normalize.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 8 behavior tests pass
    - `grep -n "transition_id" packages/core/src/ingest/normalize.ts` shows transition_id used only in the TransitionKind mapping, never in set-grouping logic; `grep -c "setnumber" packages/core/src/ingest/normalize.ts` >= 1 in the grouping code
    - `grep -c "isreprise" packages/core/src/ingest/normalize.ts` returns 0 (D-14: isreprise is unreliable, never read)
    - domain/types.ts contains none of the raw field names `showdate`, `songname`, `setnumber`, `venuename` (anti-corruption check): `grep -cE "showdate|songname|venuename" packages/core/src/domain/types.ts` returns 0
    - normalize.ts imports config for allowlist/sentinels — no inline settype or sentinel literals outside config.ts
  </acceptance_criteria>
  <done>A pure, fully-tested normalizer converts real raw rows into clean domain shows using only setnumber/position for structure, with all D-13/D-14/D-15/D-16 semantics implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Skeleton CLI — one documented command writes the versioned artifact from sample data</name>
  <files>packages/core/src/cli/normalize-corpus.ts, packages/core/src/cli/refresh.ts, packages/core/src/index.ts, package.json, data/normalized/corpus.json</files>
  <read_first>
    - packages/core/src/ingest/normalize.ts (the pure function this CLI wraps — signature and stats shape)
    - packages/core/src/config.ts (path constants: dataRawDir, dataSamplesDir, corpusArtifactPath)
    - .planning/phases/01-corpus-ingestion-schema-foundation/01-PATTERNS.md ("CLI structure" convention — thin orchestrators, arg validation, all orchestration in Node, no shell)
  </read_first>
  <action>
    `packages/core/src/cli/normalize-corpus.ts` — thin wrapper: parse `--input <dir>` (default `config.dataRawDir`) and `--out <path>` (default `config.corpusArtifactPath`); read every `*.json` file in the input dir; for each file accept either a raw API envelope (`{error, error_message, data}` — unwrap `data`) or a bare row array (tolerates both sample formats); concatenate rows; call `normalizeCorpus`; write the artifact with `JSON.stringify(corpus, null, 2)` + trailing newline (stable git diffs, LF via .gitattributes); print a summary line with showCount, songCount, latestShowDate, nonKglwRowsExcluded, showsExcludedBySettype. Exit non-zero on any validation failure, letting the zod/contiguity error message through untouched (failure-UX convention).

    `packages/core/src/cli/refresh.ts` — THE one documented command (skeleton form this plan): parse args; support `--normalize-only` which delegates to the normalize-corpus logic (import the shared function, don't shell out); print usage listing the flags that exist now (`--normalize-only`, `--input`, `--out`) and the ones arriving in plan 01-03 (`--all`, `--year`, `--census-only`, `--fetch-only`) marked "not yet implemented" so the command surface is stable from day one. Unknown flags → usage + exit 1.

    `packages/core/src/index.ts` — public API barrel: export domain types and `normalizeCorpus`; do NOT re-export anything from `ingest/api-types.ts` (raw API types stay behind the anti-corruption boundary; internal modules import them directly).

    Root `package.json`: add script `"refresh": "node packages/core/src/cli/refresh.ts"`.

    Run the skeleton end-to-end: `pnpm refresh --normalize-only --input data/samples` → writes `data/normalized/corpus.json` (interim artifact, sample-derived; plan 01-04 replaces it with the full corpus). Commit the artifact — it makes `git diff` the review mechanism from the first artifact onward (D-06 spirit).
  </action>
  <verify>
    <automated>pnpm refresh --normalize-only --input data/samples && node -e "const c=JSON.parse(require('fs').readFileSync('data/normalized/corpus.json','utf8')); const assert=require('assert'); assert.strictEqual(c.schemaVersion,1); assert.strictEqual(c.showCount,25); assert.ok(c.songCount>0); assert.ok(/^\d{4}-\d{2}-\d{2}$/.test(c.latestShowDate)); assert.ok(c.generatedAt); assert.strictEqual(c.shows.length,25); console.log('skeleton artifact OK: '+c.showCount+' shows, '+c.songCount+' songs')"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm refresh --normalize-only --input data/samples` exits 0 and writes `data/normalized/corpus.json`
    - Artifact header asserts pass: `schemaVersion === 1`, `showCount === 25` (1 rr1010 + 24 non-Live-Session 2013 shows), valid `latestShowDate`, `generatedAt` present
    - `pnpm refresh --bogus-flag` exits 1 and prints usage
    - `grep -c "api-types" packages/core/src/index.ts` returns 0 (raw types not re-exported)
    - Full `pnpm vitest run` still green
  </acceptance_criteria>
  <done>The Walking Skeleton is complete: one documented root command runs the validate → normalize → write-artifact pipeline on committed real data, the versioned artifact exists with its D-08 header, and tests prove the output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Committed raw JSON → normalizer | Untrusted editor-entered content (footnotes, shownotes, song names) flows through normalization into the committed artifact |
| CLI args → file paths | `--input`/`--out` values are interpolated into filesystem reads/writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering | ingest/normalize.ts | mitigate | Every row zod-validated before normalization; position-contiguity assertion; malformed footnotes handled by guarded parse (kept verbatim, never eval'd, never thrown) |
| T-01-05 | Tampering | cli args --input/--out | mitigate | Paths resolved relative to repo root only; no shell interpolation anywhere (pure Node fs); unknown flags rejected with usage |
| T-01-06 | Repudiation/Integrity | data/normalized/corpus.json | mitigate | Artifact committed with stable 2-space formatting + LF — git diff is the tamper-evidence and review mechanism (D-06) |
| T-01-03 | Tampering (stored, future UI) | verbatim footnote/shownotes strings in artifact | accept (this phase) | Strings carried verbatim, never interpreted; flagged untrusted in SCHEMA.md §12 for UI phases |
</threat_model>

<verification>
- `pnpm vitest run` green (normalize suite + all prior)
- `pnpm refresh --normalize-only --input data/samples` reproduces the committed artifact byte-for-byte except `generatedAt`
- Skeleton acceptance (SKELETON.md): scaffold ✔, one real pipeline read/write ✔, documented command ✔, passing test ✔
</verification>

<success_criteria>
- Phase success criterion 2 is half-met: the documented command and versioned artifact exist (full-corpus fetch lands in 01-03/01-04)
- Phase success criterion 3 advanced: artist_id===1 filtering enforced and tested in the normalize path
- D-08 header contract frozen for Phase 2
</success_criteria>

<output>
Create `.planning/phases/01-corpus-ingestion-schema-foundation/01-02-SUMMARY.md` when done.
</output>
