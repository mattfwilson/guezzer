---
phase: 02-transition-matrix-model-backtest
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/config.ts
  - packages/core/src/domain/types.ts
  - packages/core/src/model/decay.ts
  - packages/core/src/model/matrix.ts
  - packages/core/src/model/index-build.ts
  - packages/core/src/cli/build-model.ts
  - packages/core/src/index.ts
  - packages/core/test/model/matrix.test.ts
  - packages/core/test/fixtures/synthetic-ascutoff.json
  - packages/core/test/fixtures/synthetic-ascutoff.meta.json
  - data/normalized/transition-matrix.json
autonomous: true
requirements: [DATA-05, MODL-01, MODL-02, MODL-04, MODL-11, EVAL-05]
must_haves:
  truths:
    - "Running build-model emits data/normalized/transition-matrix.json — a versioned nodes+edge-list artifact with a frozen schemaVersion header"
    - "Set-boundary and encore transitions are never counted as edges (within-set adjacency only)"
    - "Matrix construction filters shows by an exclusive (date, showOrder) as-of bound before counting — no future leakage"
    - "Every edge carries both raw count and recency-decayed weightedCount, plus segueCount/firstDate/lastDate"
    - "All Phase 2 model constants live in packages/core/src/config.ts — no scattered magic numbers"
    - "The artifact is byte-stable across repeated rebuilds"
  artifacts:
    - path: "packages/core/src/model/matrix.ts"
      provides: "buildMatrix(corpus, asOf, config) → TransitionMatrix"
      exports: ["buildMatrix"]
    - path: "packages/core/src/model/decay.ts"
      provides: "decayedWeight exponential half-life helper relative to as-of cutoff"
      exports: ["decayedWeight"]
    - path: "packages/core/src/model/index-build.ts"
      provides: "from→edges + songId→node in-memory index over the frozen edge list"
      exports: ["buildMatrixIndex"]
    - path: "packages/core/src/cli/build-model.ts"
      provides: "corpus → transition-matrix.json thin CLI"
      exports: ["runBuildModel", "formatBuildModelSummary"]
    - path: "data/normalized/transition-matrix.json"
      provides: "The frozen D-08 model artifact"
      contains: "schemaVersion"
  key_links:
    - from: "packages/core/src/cli/build-model.ts"
      to: "packages/core/src/model/matrix.ts"
      via: "runBuildModel calls buildMatrix then writes JSON"
      pattern: "buildMatrix\\("
    - from: "packages/core/src/model/matrix.ts"
      to: "packages/core/src/model/decay.ts"
      via: "weightedCount accumulation"
      pattern: "decayedWeight\\("
---

<objective>
Deliver the frozen `TransitionMatrix` JSON artifact end-to-end: extend `config.ts` with every Phase 2 constant (D-16) and `domain/types.ts` with every new type contract (interface-first for all downstream plans), then build `buildMatrix` (D-07 boundary exclusion, D-08 nodes+edge-list, D-09 as-of cutoff, D-10 count+weightedCount decay) and ship a `build-model` CLI that emits `data/normalized/transition-matrix.json`.

This is MVP slice 1 (part A): the matrix leg of the build-matrix → predict → backtest pipeline. It is independently testable via fixtures and produces a real, inspectable artifact before any scoring exists.

Purpose: Freeze the single serializable structure consumed by BOTH the predictor (Phase 4) and the constellation renderer (Phase 7) — one artifact, one pipeline (CLAUDE.md constraint).
Output: Extended config + types, `model/decay.ts`, `model/matrix.ts`, `model/index-build.ts`, `cli/build-model.ts`, barrel exports, Wave-0 `matrix.test.ts`, one new synthetic fixture, and the committed artifact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-transition-matrix-model-backtest/02-CONTEXT.md
@.planning/phases/02-transition-matrix-model-backtest/02-RESEARCH.md
@.planning/phases/02-transition-matrix-model-backtest/02-PATTERNS.md
@.planning/phases/02-transition-matrix-model-backtest/02-VALIDATION.md
</context>

<artifacts_produced>
Symbols this plan creates (exclude from drift verification — they are new):
- **Types** (`domain/types.ts`): `TransitionMatrix`, `MatrixNode`, `MatrixEdge`, `PredictionCandidate`, `PredictionFactors`, `BackoffTier`, `ShowContext`, `SignalToggles`, `BacktestResult`, `BacktestSplit`, `AblationEntry`, `AsOfBound` (all new types for the whole phase are defined here, interface-first; only matrix-related ones are consumed in THIS plan).
- **Config keys** (`config.ts`): `matrixArtifactPath`, `backtestReportPath`, `backtestJsonPath`, `decayHalfLifeDays`, `transitionAddAlpha`, `backoffWeights`, `rotationWindowShows`, `rotationPenaltyPerShow`, `alreadyPlayedFactor`, `hardSegueConsistencyThreshold`, `hardSegueMinSupport`, `hardSegueOverrideCeiling`, `hardSegueBoost`, `eraWindowShows`, `eraPriorSmoothingK`, `eraPriorFloor`, `eraPriorCeil`, `candidateListSize`.
- **Functions**: `decayedWeight`, `buildMatrix`, `buildMatrixIndex`, `runBuildModel`, `formatBuildModelSummary`.
- **CLI**: `packages/core/src/cli/build-model.ts` (default input `config.corpusArtifactPath`, default output `config.matrixArtifactPath`, optional `--cutoff <YYYY-MM-DD>` and `--out <path>` flags).
- **Files**: `data/normalized/transition-matrix.json`, `test/fixtures/synthetic-ascutoff.json` (+ `.meta.json`).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Extend config.ts + domain/types.ts with all Phase 2 constants and type contracts</name>
  <files>packages/core/src/config.ts, packages/core/src/domain/types.ts</files>
  <read_first>
    - packages/core/src/config.ts (the exact `as const` object to extend; doc-comment-per-constant convention at lines 30-81; REUSE `sentinelSongIds`, `tourIdSentinel`, `settypeAllowlist`, `corpusArtifactPath`, `dataNormalizedDir`, `censusReportPath`/`censusJsonPath`)
    - packages/core/src/domain/types.ts (union-not-enum convention lines 9-11/46-48; frozen-header convention lines 75-87 for `NormalizedCorpus`; REUSE `Performance`, `NormalizedShow`, `TransitionKind`, `SetNumber`)
    - packages/core/src/ingest/tuning-tags.ts (the `TuningFamily` type to import for `MatrixNode.tuningFamily`)
    - .planning/phases/02-transition-matrix-model-backtest/02-PATTERNS.md (§ config.ts EXTEND lines 72-106 — exact key names + seed values; § domain/types.ts EXTEND lines 35-68 — header shape)
    - .planning/phases/02-transition-matrix-model-backtest/02-RESEARCH.md (M1-M8 — seed justifications; every numeric is `[ASSUMED]`/backtest-tunable per D-16)
  </read_first>
  <action>
    Extend the single `config` object (D-16 / MODL-11 — no numeric literal for any model constant may live outside this file) with these keys, each carrying a doc comment naming its deciding decision/research section and flagged `[ASSUMED]` (backtest-tunable, "starting default, not authoritative", matching the existing `microtonalAlbums` comment style): `matrixArtifactPath: "data/normalized/transition-matrix.json"` (D-08 output, sibling of `corpusArtifactPath`); `backtestReportPath: "data/backtest-report.md"` + `backtestJsonPath: "data/backtest.json"` (D-15, mirror `censusReportPath`/`censusJsonPath` naming); `decayHalfLifeDays: 365` (M2); `transitionAddAlpha: 0.0` (M1); `backoffWeights: { w1: 0.6, w2: 0.2, w3: 0.15, w4: 0.05 }` (M1, sums to 1.0); `rotationWindowShows: 3`, `rotationPenaltyPerShow: 0.5` (M3); `alreadyPlayedFactor: 0.02` (M3, near-zero per D-05); `hardSegueConsistencyThreshold: 0.70`, `hardSegueMinSupport: 3`, `hardSegueOverrideCeiling: 0.97`, `hardSegueBoost: 3.0` (M4/D-04); `eraWindowShows: 40`, `eraPriorSmoothingK: 1`, `eraPriorFloor: 0.3`, `eraPriorCeil: 2.0` (M8); `candidateListSize: 15` (M7, must be ≥10 for top-10). Keep the object `as const`.

    Extend `domain/types.ts` with the full phase type vocabulary (interface-first: define ALL new types here even though later plans consume most). Use string-literal unions NOT enums (`erasableSyntaxOnly`). Add: `MatrixNode` with `songId: number; songName: string; playCount: number; eraPlayCount: number; tuningFamily: TuningFamily` (eraPlayCount = plays within the trailing `eraWindowShows` shows before the cutoff, baked at build time so the predictor's era-prior stays pure and leak-safe); `MatrixEdge` with `from: number; to: number; count: number; weightedCount: number; segueCount: number; firstDate: string; lastDate: string`; `TransitionMatrix` with `schemaVersion: 1; generatedAt: string; asOfDate: string; showCount: number; nodeCount: number; edgeCount: number; nodes: MatrixNode[]; edges: MatrixEdge[]` (mirror the `NormalizedCorpus` frozen-header shape — this is the second frozen contract, D-08/D-10); `AsOfBound` with `date: string; showOrder: number; inclusive: boolean` (D-09 refined by M5 — exclusive tuple bound for walk-forward); `BackoffTier = "transition" | "tuning" | "albumEra" | "basePlayRate"`; `PredictionFactors` with `transitionProb: number; decay: number; rotation: number; alreadyPlayed: number; eraPrior: number; backoffTier: BackoffTier; hardSegueFlag: boolean`; `PredictionCandidate` with `songId: number; songName: string; score: number; factors: PredictionFactors; reason: string` (D-06 rich breakdown); `ShowContext` with `currentSongId: number; trail: number[]; recentShowSongSets: number[][]` (already-played trail + last-N-tour-shows for rotation); `SignalToggles` with boolean `decay; rotation; alreadyPlayed; eraPrior; hardSegue; tuning; albumEra` (all default true; ablation flips one false); `BacktestSplit` with `n: number; top1: number; top5: number; top10: number`; `AblationEntry` with `signal: string; overall: BacktestSplit; hardSegue: BacktestSplit; freeChoice: BacktestSplit; deltaVsFull: { top1: number; top5: number; top10: number }`; `BacktestResult` with `schemaVersion: 1; generatedAt: string; holdoutTourId: number; holdoutTourName: string; holdoutShowCount: number; evalTransitionCount: number; overall: BacktestSplit; hardSegue: BacktestSplit; freeChoice: BacktestSplit; ablation: AblationEntry[]`. Import `TuningFamily` from `../ingest/tuning-tags.ts`. Do NOT redefine `Performance`/`NormalizedShow`/`TransitionKind` — reuse them.
  </action>
  <verify>
    <automated>npx tsc -p packages/core --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `packages/core/src/config.ts` typechecks and every one of the 18 new keys listed in the action is present with a doc comment citing its decision/research section.
    - `grep -v '^\s*\*' packages/core/src/domain/types.ts | grep -c "enum "` returns 0 (no enums — union types only, `erasableSyntaxOnly`).
    - `TransitionMatrix`, `MatrixNode`, `MatrixEdge`, `PredictionCandidate`, `ShowContext`, `SignalToggles`, `BacktestResult`, `AsOfBound` all exist as exported types and `npx tsc -p packages/core --noEmit` passes.
    - `backoffWeights` values sum to exactly 1.0.
  </acceptance_criteria>
  <done>All Phase 2 constants and type contracts exist in the two extended files; core typechecks clean; no enums introduced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: buildMatrix + decay helper + Wave-0 matrix.test.ts</name>
  <files>packages/core/src/model/decay.ts, packages/core/src/model/matrix.ts, packages/core/test/model/matrix.test.ts, packages/core/test/fixtures/synthetic-ascutoff.json, packages/core/test/fixtures/synthetic-ascutoff.meta.json</files>
  <read_first>
    - packages/core/src/ingest/census.ts (the pure-derivation analog: module doc-comment + `.ts` imports; `Map`-keyed `byValue` accumulation ~lines 109-123; explicit `.sort` comparators ~lines 132/174-175 — the determinism discipline to copy verbatim; sentinel filter `config.sentinelSongIds as readonly number[]` ~line 232)
    - packages/core/test/normalize.test.ts (fixture-normalize pattern lines 1-16: `import ... with { type: "json" }`, `normalizeCorpus(fixture)` first, then feed the corpus; `describe`/`it "Test N:"` numbered-assertion style)
    - packages/core/test/fixtures/2013-encore.json + 2013-encore.meta.json (encore boundary fixture — reuse for D-07); packages/core/test/fixtures/2022-rr1010-multiset.json (multiset boundary); packages/core/test/fixtures/2017-segues.meta.json (the `.meta.json` sidecar template: sourceFile/showId/showdate/why)
    - packages/core/src/config.ts (the config keys added in Task 1)
    - packages/core/src/domain/types.ts (TransitionMatrix/MatrixNode/MatrixEdge/AsOfBound from Task 1)
    - .planning/phases/02-transition-matrix-model-backtest/02-RESEARCH.md (M2 decay lines 196-199; within-set edge emission lines 291-306; decay body lines 267-277; Pitfall 1 segue direction; Pitfall 2 determinism; Pitfall 3 same-date leakage)
    - .planning/phases/02-transition-matrix-model-backtest/02-PATTERNS.md (§ matrix.ts lines 110-146; § decay.ts lines 150-160)
  </read_first>
  <behavior>
    - Test "boundary exclusion" (DATA-05, D-07): normalizing `2013-encore.json` then buildMatrix emits ZERO edge from the last main-set song into the first encore song, and ZERO edge across a set-1→set-2 boundary in `2022-rr1010-multiset.json`. Only within-set adjacent pairs become edges.
    - Test "matrix schema" (MODL-01): output has `schemaVersion: 1`, `asOfDate`, `nodes` sorted by `songId`, `edges` sorted by `(from, to)`; no node/edge references `config.sentinelSongIds` (song_id 1); no edge touches an `isPlaceholder` performance.
    - Test "as-of cutoff" (MODL-02): against the new `synthetic-ascutoff` fixture (two shows on the same date disambiguated by `showOrder`, plus a later show), an exclusive `(date, showOrder)` bound excludes the same-date-later show and all future shows from edge counts — a `date`-only cutoff would leak them.
    - Test "decay half-life" (MODL-04): an edge observed exactly `decayHalfLifeDays` before `asOfDate` contributes `weightedCount ≈ 0.5` per instance; `count` is the raw integer; decay is measured from `asOfDate`, never `Date.now()`.
    - Test "determinism": `JSON.stringify(buildMatrix(...))` is byte-identical across two calls with the same inputs (`weightedCount` rounded to fixed precision, stable sorts).
  </behavior>
  <action>
    Create `model/decay.ts` exporting `decayedWeight(showDate, asOfDate, halfLifeDays)` = `Math.exp((-Math.LN2 * ageDays) / halfLifeDays)` where `ageDays = (Date.parse(asOfDate) - Date.parse(showDate)) / 86_400_000` — half-life relative to the matrix's own `asOf` cutoff, NEVER `Date.now()` (D-10). Pure, no I/O.

    Create `model/matrix.ts` exporting `buildMatrix(corpus, asOf, cfg = config)` returning `TransitionMatrix`. Algorithm (D-07/D-08/D-10): (1) filter `corpus.shows` to those satisfying the exclusive `(date, showOrder)` tuple bound — a show passes when `show.date < asOf.date`, or `show.date === asOf.date && show.showOrder < asOf.showOrder` (when `inclusive` is false); this refines D-09's `date ≤ cutoff` because same-date shows exist (Pitfall 3). (2) For each passing show, iterate `show.sets`, and within EACH set walk position-adjacent performance pairs ONLY — never across sets or into the encore (D-07 boundary exclusion). (3) Skip any pair where `a.isPlaceholder || b.isPlaceholder` (do not bridge Unknown; do not emit A→placeholder→C). (4) Skip sentinel song ids via `config.sentinelSongIds as readonly number[]`. (5) Per surviving edge A→B accumulate: `count += 1`; `weightedCount += decayedWeight(show.date, asOf.date, cfg.decayHalfLifeDays)`; `if (a.transitionKind === "segue") segueCount += 1` — the segue lives on A's OUT-transition (Pitfall 1, D-11: source is `Performance.transitionKind`, jamcharts deferred); set `firstDate`/`lastDate` (M4 needs them for the reason string). Accumulate instances in fixed `(date, showOrder, position)` iteration order, then round `weightedCount` to 1e-9 before returning (Pitfall 2). Build `nodes` (one `MatrixNode` per non-sentinel non-placeholder catalog song with `playCount`, `eraPlayCount` = plays within the last `cfg.eraWindowShows` shows before `asOf`, `tuningFamily` looked up from the tuning-tags data — take the tuning-tags map as a parameter to keep the function pure), sort nodes by `songId` and edges by `(from, to)`. Populate the `TransitionMatrix` frozen header. Use `Map`-keyed accumulation like `census.ts` `byValue`.

    Create the `synthetic-ascutoff.json` raw-row fixture (same raw kglw.net setlist-row shape as existing fixtures — it will be normalized through `normalizeCorpus`): two shows sharing one date distinguished by show order, plus one later-dated show, with a known within-set A→B pair whose inclusion flips depending on the cutoff tuple. Pair it with a `synthetic-ascutoff.meta.json` sidecar (sourceFile/showId/showdate/why, noting it is hand-authored for as-of leak-safety and giving the expected pre/post-cutoff edge counts).

    Create `test/model/matrix.test.ts` implementing the five `behavior` tests (D-17 / EVAL-05 — fixture setlists with known expected outputs). Each test normalizes a raw fixture via `normalizeCorpus(...)` first, then calls `buildMatrix`. Use numbered `it("Test N: ...")` assertions with concrete expected counts.
  </action>
  <verify>
    <automated>npx vitest run packages/core test/model/matrix.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run packages/core -t "boundary exclusion"` passes — no cross-set/encore edge emitted.
    - `npx vitest run packages/core -t "as-of cutoff"` passes — exclusive `(date, showOrder)` bound excludes the same-date-later and future shows.
    - `npx vitest run packages/core -t "decay half-life"` passes — instance at exactly one half-life contributes `weightedCount ≈ 0.5`.
    - `npx vitest run packages/core -t "matrix schema"` passes — schemaVersion 1, sorted nodes/edges, no sentinel/placeholder.
    - `npx vitest run packages/core -t "determinism"` passes — byte-identical serialization across two builds.
  </acceptance_criteria>
  <done>buildMatrix + decayedWeight exist and are pure; all five Wave-0 matrix tests green; the synthetic as-of fixture + sidecar exist.</done>
</task>

<task type="auto">
  <name>Task 3: index-build + build-model CLI + barrel export + emit the artifact</name>
  <files>packages/core/src/model/index-build.ts, packages/core/src/cli/build-model.ts, packages/core/src/index.ts, data/normalized/transition-matrix.json</files>
  <read_first>
    - packages/core/src/ingest/tuning-tags.ts (deriveCatalogFromCorpus lines 104-125 — the "walk once, Map-keyed index, return sorted" analog for index-build.ts; also how the tuning-tags file is loaded/parsed for the CLI)
    - packages/core/src/cli/normalize-corpus.ts (full thin-CLI skeleton: `mkdir`+`writeFile` with `JSON.stringify(x, null, 2) + "\n"`; `parseArgs` with `throw new Error(\`Unknown flag\`)`; `isMain` guard via `pathToFileURL`; try/catch/`process.exit(1)`; `runNormalizeCorpus` + `formatNormalizeSummary` split — mirror exactly)
    - packages/core/src/cli/generate-tuning-tags.ts (line ~69 corpus-load-from-disk: `JSON.parse(await readFile(path,"utf8")) as NormalizedCorpus`)
    - packages/core/src/index.ts (barrel grouping + anti-corruption boundary comment — add new domain types + pure fns, NOT CLI internals)
    - packages/core/src/config.ts (`corpusArtifactPath`, `matrixArtifactPath`, `tuningTagsPath`, `cliYearMin/Max` for arg-validation convention)
    - .planning/phases/02-transition-matrix-model-backtest/02-PATTERNS.md (§ index-build.ts lines 193-208; § build-model.ts lines 244-278; § barrel lines 313-317)
  </read_first>
  <action>
    Create `model/index-build.ts` exporting `buildMatrixIndex(matrix)` returning `{ edgesFrom: Map<number, MatrixEdge[]>; nodeById: Map<number, MatrixNode> }` — walk the frozen edge list once building `Map<fromSongId, MatrixEdge[]>` for O(1) successor access plus a `songId → MatrixNode` lookup, following the `deriveCatalogFromCorpus` Map-index pattern. Pure, no I/O.

    Create `cli/build-model.ts` mirroring `normalize-corpus.ts` verbatim in structure: export `runBuildModel(options)` that loads the corpus from `options.corpusPath ?? config.corpusArtifactPath` via `JSON.parse(await readFile(...)) as NormalizedCorpus`, loads the tuning-tags map from `config.tuningTagsPath`, derives the default as-of bound from the corpus's latest `(date, showOrder)` with `inclusive: true` (ship-time artifact uses `cutoff = latest`, D-09), calls `buildMatrix`, and writes `options.outPath ?? config.matrixArtifactPath` with `JSON.stringify(matrix, null, 2) + "\n"` (diffable, Pitfall 2) after `mkdir(dirname, { recursive: true })`. Export `formatBuildModelSummary(matrix)` returning a one-line summary (node/edge counts, asOfDate). Support optional flags `--out <path>` and `--cutoff <YYYY-MM-DD>`; validate `--cutoff` as a bounded date (year within `cliYearMin`/`cliYearMax`) before use and reject unknown flags with `throw new Error(\`Unknown flag: ${arg}\`)` (Security V5 — validate before path/use). Include the `isMain` guard + try/catch/`console.error((err as Error).message)`/`process.exit(1)`.

    Extend `index.ts` (barrel): add `export type { TransitionMatrix, MatrixNode, MatrixEdge, PredictionCandidate, PredictionFactors, BackoffTier, ShowContext, SignalToggles, BacktestResult, BacktestSplit, AblationEntry, AsOfBound } from "./domain/types.ts"` and `export { buildMatrix } from "./model/matrix.ts"`, `export { decayedWeight } from "./model/decay.ts"`, `export { buildMatrixIndex } from "./model/index-build.ts"`. Do NOT export CLI internals (boundary discipline).

    Run the CLI to emit `data/normalized/transition-matrix.json` and commit it.
  </action>
  <verify>
    <automated>node packages/core/src/cli/build-model.ts && node -e "const m=require('./data/normalized/transition-matrix.json'); if(m.schemaVersion!==1||!Array.isArray(m.nodes)||!Array.isArray(m.edges)) process.exit(1); console.log('nodes',m.nodes.length,'edges',m.edges.length)"</automated>
  </verify>
  <acceptance_criteria>
    - `node packages/core/src/cli/build-model.ts` exits 0 and writes `data/normalized/transition-matrix.json`.
    - The emitted artifact parses as JSON with `schemaVersion === 1`, non-empty `nodes` and `edges` arrays, and each edge has both `count` and `weightedCount`.
    - Running the CLI twice produces a byte-identical file (`git diff --quiet data/normalized/transition-matrix.json` after a second run).
    - `buildMatrixIndex` and the new types/fns are exported from `index.ts` and `npx tsc -p packages/core --noEmit` passes.
  </acceptance_criteria>
  <done>The build-model CLI emits a byte-stable frozen matrix artifact; index-build + barrel exports exist; core typechecks clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv → build-model | `--cutoff` / `--out` flags are the only external input; corpus is a committed, Phase-1 zod-validated artifact |
| corpus.json → buildMatrix | Already normalized behind the Phase 1 anti-corruption boundary; read as `NormalizedCorpus` only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `--out` output path from CLI argv | mitigate | Default output comes from `config.matrixArtifactPath`; if `--out` is supplied, write only under the intended data dir; reject unknown flags (Phase 1 `parseArgs` convention) |
| T-02-02 | Tampering | `--cutoff` date arg | mitigate | Validate as a bounded date (year within `cliYearMin`/`cliYearMax`) before constructing the `AsOfBound` — Security V5, Phase 1 bounded-integer convention |
| T-02-03 | Denial of Service | Malformed corpus crashing the build | accept | Corpus is a committed, zod-validated Phase 1 artifact; builder still fails loudly (throw + `process.exit(1)`) rather than emit a corrupt matrix |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase (RESEARCH § Package Legitimacy Audit: zero new deps); no `[ASSUMED]`/`[SUS]` packages exist |
</threat_model>

<verification>
- `npx vitest run packages/core` — full core suite green (including new `matrix.test.ts`).
- `node packages/core/src/cli/build-model.ts` emits a byte-stable `data/normalized/transition-matrix.json`.
- `npx tsc -p packages/core --noEmit` clean (no enums; all new types resolve).
</verification>

<success_criteria>
- `TransitionMatrix` artifact exists, versioned, nodes+edge-list, with an as-of cutoff and boundary transitions excluded (Phase success criterion 1 — matrix leg).
- All model constants live in `config.ts` (success criterion 5 — config leg).
- Wave-0 `matrix.test.ts` covers DATA-05, MODL-01, MODL-02, MODL-04 with known fixture outputs.
</success_criteria>

<output>
Create `.planning/phases/02-transition-matrix-model-backtest/02-01-SUMMARY.md` when done.
</output>
