---
phase: quick-260718-1no
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/dex/openers.ts
  - packages/core/src/index.ts
  - packages/core/test/dex/openers.test.ts
  - packages/app/src/config.ts
  - packages/app/src/show/openerSuggestions.ts
  - packages/app/src/show/SearchSheet.tsx
  - packages/app/src/show/ShowView.tsx
autonomous: true
requirements: [QUICK-260718-1no]

must_haves:
  truths:
    - "Before the opener is logged (session.currentSongId == null), tapping the 'Search for the opener' orb opens the SearchSheet already showing the top 5 recency-weighted King Gizzard openers under a heading, before any typing"
    - "Typing a query replaces the opener suggestions with normal fuzzy catalog results; clearing the query returns to the opener suggestions"
    - "Selecting an opener suggestion logs it through the exact same path as a normal search result (onSelect → handleSearchSelect → logSong) — no new selection wiring"
    - "Mid-show (currentSongId != null), an empty search query still renders a blank list exactly as today — opener suggestions are pre-opener only"
    - "The opener ranking is a pure, deterministic core derivation (recency-weighted via decayedWeight, asOf = archive.latestShowDate, deterministic tie-break) with zero React/DOM/Date.now"
  artifacts:
    - path: "packages/core/src/dex/openers.ts"
      provides: "Pure deriveTopOpeners(shows, songs, opts) → ranked opener list"
      contains: "export function deriveTopOpeners"
      min_lines: 30
    - path: "packages/core/test/dex/openers.test.ts"
      provides: "Fixture test: recency order, tie-break, limit, no-Set-1 + name-fallback edge cases"
      contains: "deriveTopOpeners"
    - path: "packages/app/src/show/openerSuggestions.ts"
      provides: "Memoized app wrapper: loadArchive() + deriveTopOpeners with config values"
      contains: "deriveTopOpeners"
    - path: "packages/app/src/show/SearchSheet.tsx"
      provides: "openerSuggestions prop + empty-query suggestion render branch"
      contains: "openerSuggestions"
  key_links:
    - from: "packages/core/src/index.ts"
      to: "packages/core/src/dex/openers.ts"
      via: "barrel export"
      pattern: "deriveTopOpeners"
    - from: "packages/app/src/show/openerSuggestions.ts"
      to: "packages/app/src/dex/archive-loader.ts"
      via: "loadArchive()"
      pattern: "loadArchive"
    - from: "packages/app/src/show/ShowView.tsx"
      to: "packages/app/src/show/SearchSheet.tsx"
      via: "openerSuggestions prop passed only when session.currentSongId == null"
      pattern: "openerSuggestions"
---

<objective>
When tracking a show, tapping the pre-opener "Search for the opener" orb should open the SearchSheet already populated with the top 5 most popular King Gizzard show openers (recency-weighted), before the user types anything. Typing runs the normal fuzzy catalog search; clearing the query returns to the opener suggestions. Mid-show, an empty search stays blank exactly as today.

Purpose: give the owner a credible one-thumb head-start on logging the opener — the one moment predict() cannot help (no current song yet).
Output: one new pure core derivation (+ its fixture test), a memoized app wrapper, a config count + heading, and the SearchSheet/ShowView wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# Core data source + reuse (READ these before Task 1)
@packages/core/src/dex/archive-types.ts
@packages/core/src/model/decay.ts
@packages/core/src/index.ts
@packages/core/test/dex/search-archive.test.ts

# App wiring surfaces (READ these before Tasks 2-3)
@packages/app/src/dex/archive-loader.ts
@packages/app/src/show/showContext.ts
@packages/app/src/show/SearchSheet.tsx
@packages/app/src/show/ShowView.tsx

CRITICAL project constraints (CLAUDE.md):
- Strict core/UI separation: all domain/derivation logic lives in packages/core with ZERO React/DOM/browser deps and NO Date.now. The opener ranking is CORE.
- Single config file per tier, no scattered magic numbers: the count + heading go in packages/app/src/config.ts.
- Core derivations REQUIRE a unit test with a small fixture setlist and known expected outputs.
- kglw-derived song names render as React text only — never dangerouslySetInnerHTML.

Key data facts (from investigation):
- The opener = first songId of the set whose n === "1": `show.sets.find(s => s.n === "1")?.songs[0]`. Guard shows with no Set 1 or an empty songs array (skip them).
- Song names resolve via the archive `songs` Record<string,string>, keyed by String(songId). Fall back to a stable placeholder (e.g. `Song {songId}`) when a name is absent.
- Recency anchor asOfDate = archive.latestShowDate (matches the matrix build's asOf convention; keeps core pure/deterministic).
- Rank = sum of decayedWeight(showDate, asOfDate, halfLifeDays) over every show that song opened. Tie-break: raw opener count desc, then songId asc.
- predict() CANNOT produce openers (requires non-null currentSongId). Do NOT use it.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure core deriveTopOpeners derivation + fixture test</name>
  <files>packages/core/src/dex/openers.ts, packages/core/src/index.ts, packages/core/test/dex/openers.test.ts</files>
  <behavior>
    Fixture: a small ArchiveShow[] (mirror the `show()` factory idiom in search-archive.test.ts) plus a songs Record, with known openers + dates, asserting:
    - Recency order: two songs that each opened once, the one whose opening show is closer to asOfDate ranks higher (decayedWeight larger).
    - A frequently-but-long-ago opener vs a recent opener: order follows the summed decayed weight, not raw count.
    - Deterministic tie-break: two songs with equal summed score fall back to raw opener count desc, then songId asc.
    - limit is honored (e.g. limit 2 returns exactly the top 2).
    - Edge: a show with NO Set 1 (or an empty Set 1 songs array) is skipped, never throws, never contributes a NaN/undefined opener.
    - Edge: an opener songId absent from the songs record gets the placeholder name fallback (e.g. "Song {songId}"), not undefined.
    - Purity: the function reads only its args (no Date.now); calling twice with the same args returns equal output.
  </behavior>
  <action>Create packages/core/src/dex/openers.ts exporting a pure `deriveTopOpeners(shows: ArchiveShow[], songs: Record<string,string>, opts: { asOfDate: string; halfLifeDays: number; limit: number }) => { songId: number; songName: string; count: number; score: number }[]`. For each show, take `show.sets.find(s => s.n === "1")?.songs[0]` as the opener; skip shows with no Set 1 or an empty songs array. Accumulate per opener songId: `count` (raw opener occurrences) and `score` (sum of `decayedWeight(show.date, opts.asOfDate, opts.halfLifeDays)`). Import decayedWeight from ../model/decay.ts. Round/quantize score consistently before comparison if needed for float-summation determinism (mirror config.weightedCountPrecision idiom if you round). Resolve songName via `songs[String(songId)] ?? \`Song ${songId}\``. Sort by score desc, then count desc, then songId asc; slice to opts.limit. Zero React/DOM/Date.now — core purity ("lib": ["ES2023"]). Add a barrel export block in packages/core/src/index.ts (mirror the existing dex export comment style) exporting `deriveTopOpeners` and its return-row type. Then write the fixture test at packages/core/test/dex/openers.test.ts per the behavior block above, mirroring search-archive.test.ts's factory + describe/it style.</action>
  <verify>
    <automated>cd packages/core && npx vitest run test/dex/openers.test.ts</automated>
  </verify>
  <done>deriveTopOpeners is exported from @guezzer/core, pure, and its fixture test passes covering recency order, tie-break, limit, and both edge cases.</done>
</task>

<task type="auto">
  <name>Task 2: App config count/heading + memoized openerSuggestions wrapper</name>
  <files>packages/app/src/config.ts, packages/app/src/show/openerSuggestions.ts</files>
  <action>In packages/app/src/config.ts, add `OPENER_SUGGESTION_COUNT: 5` to `config.show` (with a comment: pre-opener SearchSheet auto-populates this many recency-weighted openers) — this is app-only config, no core mirror, so configMirror.test.ts needs no change. Add `openerSuggestionsHeading: "Popular openers"` under `config.copy.show`. Then create packages/app/src/show/openerSuggestions.ts (mirror the showContext.ts / archive-loader.ts wrapper idiom): a module-level memoized `getOpenerSuggestions(): { songId: number; songName: string }[]` that calls `loadArchive()` (from ../dex/archive-loader.ts); if `!result.ok` return `[]`; otherwise call `deriveTopOpeners(archive.shows, archive.songs, { asOfDate: archive.latestShowDate, halfLifeDays: coreConfig.decayHalfLifeDays, limit: config.show.OPENER_SUGGESTION_COUNT })` and map to the `{ songId, songName }` shape SearchSheet needs. Import halfLifeDays from @guezzer/core/config (coreConfig.decayHalfLifeDays) — do NOT hardcode 365. Memoize the result at module level (archive is static).</action>
  <verify>
    <automated>cd C:/Users/mattf/git/guezzer && npx tsc -p packages/app/tsconfig.json --noEmit</automated>
  </verify>
  <done>config.show.OPENER_SUGGESTION_COUNT and config.copy.show.openerSuggestionsHeading exist; getOpenerSuggestions() returns the mapped top-5 opener list (or [] on archive-load failure), memoized, and the app typechecks clean.</done>
</task>

<task type="auto">
  <name>Task 3: SearchSheet opener-suggestions branch + ShowView pre-opener wiring</name>
  <files>packages/app/src/show/SearchSheet.tsx, packages/app/src/show/ShowView.tsx</files>
  <action>In SearchSheet.tsx: add an optional prop `openerSuggestions?: { songId: number; songName: string }[]` to SearchSheetProps. In the results body, when `query.trim() === "" && openerSuggestions && openerSuggestions.length > 0`, render a heading (`config.copy.show.openerSuggestionsHeading`) followed by suggestion rows that REUSE the exact existing result-row button markup/classes (same min-h-11, same touch targets, song name as React text only) — each row calls `handleSelect({ songId, songName })` so selection flows through the unchanged onSelect path. Keep the current behavior otherwise: a non-empty query shows fuzzy `results`; clearing back to empty returns to the suggestions; the noMatch block is unchanged (it only shows when query.trim() !== ""). Do not render suggestions when the prop is absent/empty (preserves today's blank empty-query state). In ShowView.tsx: import getOpenerSuggestions from ./openerSuggestions.ts (call it once, e.g. via useMemo or module scope) and pass `openerSuggestions={session.currentSongId === null ? openerSuggestions : undefined}` to the existing <SearchSheet> at ~line 451 — so suggestions appear ONLY pre-opener and mid-show empty search stays blank. No other SearchSheet props change.</action>
  <verify>
    <automated>cd C:/Users/mattf/git/guezzer && npx tsc -p packages/app/tsconfig.json --noEmit && npm test</automated>
  </verify>
  <done>Pre-opener, opening Search shows the "Popular openers" heading + 5 opener rows before typing; typing shows fuzzy results; clearing returns to suggestions; selecting a suggestion logs via the existing path; mid-show empty search is blank. App typechecks and the full test suite (including the new core test) is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bundled archive.json → app render | kglw-derived song names (untrusted text) reach the SearchSheet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1no-01 | Tampering (XSS) | SearchSheet opener rows | mitigate | Song names render as React text children only (never dangerouslySetInnerHTML) — identical to the existing result-row markup being reused. |
| T-1no-02 | Denial of Service | deriveTopOpeners over ~740 shows | accept | O(shows) single pass, memoized once at module load; negligible cost, no external input. |

No package installs in this plan — package-legitimacy gate not applicable.
</threat_model>

<verification>
From repo root:
- `npx tsc -p packages/app/tsconfig.json --noEmit` (app clean)
- `cd packages/core && npx vitest run test/dex/openers.test.ts` (new core test green)
- `npm test` (full vitest run) — all green, including configMirror and the new core opener test
</verification>

<success_criteria>
- deriveTopOpeners is a pure, exported, fixture-tested core derivation (recency-weighted, deterministic tie-break, limit, edge-safe).
- Pre-opener SearchSheet auto-populates the top 5 openers under a config-driven heading; typing → fuzzy; clearing → suggestions.
- Selection flows through the unchanged onSelect → handleSearchSelect → logSong path.
- Mid-show empty search stays blank (suggestions only when currentSongId == null).
- No scattered magic numbers: count + heading live in packages/app/src/config.ts; halfLifeDays sourced from core config.
- Full typecheck + test suite green.
</success_criteria>

<output>
Create `.planning/quick/260718-1no-auto-populate-opener-search-with-top-5-r/260718-1no-SUMMARY.md` when done
</output>
