---
phase: 21-layout-layering-foundations
plan: 12
type: execute
wave: 7
depends_on: ["21-11"]
files_modified:
  - packages/app/src/dex/AlbumDetail.tsx
  - packages/app/src/dex/ArchiveBrowser.tsx
  - packages/app/src/dex/SetlistView.tsx
  - packages/app/src/explore/NodeSheet.tsx
  - packages/app/test/layerOrder.test.tsx
  - packages/app/test/sheet.a11y.test.tsx
autonomous: true
requirements: [FOUND-03]

must_haves:
  truths:
    - "Every surface rendering at the sheet tier is portaled to document.body, so its z-index competes at the top level"
    - "A source scan fails if a file uses config.ui.z.sheet without createPortal"
    - "All five hand-rolled surfaces keep their shipped VoiceOver contract: role, aria-modal, accessible name, focus and Escape"
    - "The NodeSheet stays non-modal and the focusedFab lift above it still works"
  artifacts:
    - path: "packages/app/test/layerOrder.test.tsx"
      provides: "the source-scan portal assertion completing the D-24 invariant"
      contains: "createPortal"
  key_links:
    - from: "packages/app/src/dex/AlbumDetail.tsx"
      to: "document.body"
      via: "createPortal, removing the latent trap that a future z-index on DexView's wrapper would spring"
      pattern: "createPortal\\(.*document\\.body"
---

<objective>
Portal the four remaining hand-rolled sheet-tier surfaces — `AlbumDetail`, `ArchiveBrowser`,
`SetlistView` and `NodeSheet` — and complete the FOUND-03 invariant with the source-scan
assertion that every sheet-tier file also portals.

Purpose: RESEARCH verified these four are **already at root level** today (`DexView.tsx:99` has
no z-index and no transform; `NodeSheet` is a fragment child of `<main>`), so unlike plan 21-11's
two, these are **prophylactic**. Their value is that they make the D-24 structural invariant
uniformly true and remove a latent trap: adding a `z-index` to `DexView`'s wrapper later would
silently break three sheets at once. Sequenced last and kept independently revertible because,
per RESEARCH, **if schedule pressure appears these four are the droppable part — the live fix
and the invariant test are not.**

Output: four portaled surfaces, the source-scan portal assertion, and a11y re-verification of
all five.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-layout-layering-foundations/21-CONTEXT.md
@.planning/phases/21-layout-layering-foundations/21-RESEARCH.md
@.planning/phases/21-layout-layering-foundations/21-UI-SPEC.md
@.planning/phases/21-layout-layering-foundations/21-PATTERNS.md
@.planning/phases/21-layout-layering-foundations/21-HUMAN-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Portal AlbumDetail, ArchiveBrowser, SetlistView and NodeSheet (D-21, D-22, D-23)</name>
  <files>packages/app/src/dex/AlbumDetail.tsx, packages/app/src/dex/ArchiveBrowser.tsx, packages/app/src/dex/SetlistView.tsx, packages/app/src/explore/NodeSheet.tsx</files>
  <read_first>
    - packages/app/src/show/SearchSheet.tsx — the portal shape landed in plan 21-11; copy it exactly
    - packages/app/src/dex/AlbumDetail.tsx around line 48 — the `role="dialog"` root with its own `zIndex: config.ui.z.sheet`
    - packages/app/src/dex/ArchiveBrowser.tsx around lines 265-275 and 370-382 — the dialog root and the scroll container (whose padding plan 21-09 already converted to `var(--gz-sheet-pad-bottom)`)
    - packages/app/src/dex/SetlistView.tsx lines 113-135 — **two** return paths: the loading early-return dialog at ~115 and the resolved dialog at ~125. Both are `role="dialog" aria-modal="true"` at `config.ui.z.sheet`, so **both** must be portaled
    - packages/app/src/explore/NodeSheet.tsx lines 143-160 — `aria-modal={false}`, `touchAction: "none"`, the drag/snap `height` + `transition`, and the `onClick={(e) => e.stopPropagation()}`
    - packages/app/src/dex/DexView.tsx around line 99 — the wrapper (`mx-auto flex w-full max-w-md … flex-col`) that today has no z-index and no transform, which is exactly the assumption portaling makes unnecessary
    - packages/app/src/explore/ExploreFilterFab.tsx — the `focusedFab: 60` lift that must still sit above the portaled non-modal `NodeSheet`
  </read_first>
  <action>
    For each of the four surfaces, apply the same minimal transformation plan 21-11 applied to
    `SearchSheet`: import `createPortal` from `react-dom`, add
    `if (typeof document === "undefined") return null;` immediately before the return, and make
    `createPortal(<existing tree>, document.body)` the **return value** rather than an extra
    wrapper element. `SetlistView` has **two** return paths — portal both, including the loading
    early-return dialog.

    Keep every surface **hand-rolled** (D-22): do not migrate any of them onto the shared
    `<Sheet>` primitive and do not adopt its focus-trap or dismiss hooks. Keep `role`,
    `aria-modal` (including `NodeSheet`'s `false`), `aria-label`, every `className`, every
    `zIndex` expression, `NodeSheet`'s `touchAction: "none"` / drag geometry / `height` /
    `transition`, and every handler byte-identical.

    **D-23 audit per surface.** For each, record in the SUMMARY: whether it currently renders
    inside an element carrying `.orbit-stage` / `.action-bar` / `.fab-menu` (none of these four is
    expected to); whether its Escape handling depends on DOM tree position; whether focus enters
    correctly on open and returns on close; and whether anything relied on being inside
    `#app-content`. Re-apply only what is actually lost. `NodeSheet` already owns its gestures via
    inline `touchAction: "none"`, which travels with the node — note that explicitly rather than
    adding a class it does not need.

    Commit the four as one independently revertible change, separate from plan 21-11's live fix,
    so the prophylactic group can be backed out on its own if a device regression appears.

    **Do not renumber any tier.** `config.ui.z` is untouched by this plan.
  </action>
  <verify>
    <automated>cd packages/app &amp;&amp; npx vitest run --project @guezzer/app test/layerOrder.test.tsx test/archiveBrowser.test.tsx test/setlistView.test.tsx test/dexView.test.tsx test/explore</automated>
  </verify>
  <acceptance_criteria>
    - All four files import `createPortal` from `react-dom` and return `createPortal(..., document.body)`
    - `SetlistView.tsx` portals **both** return paths (`grep -c 'createPortal' packages/app/src/dex/SetlistView.tsx` returns ≥ 3 counting the import)
    - All four contain `if (typeof document === "undefined") return null;`
    - `NodeSheet.tsx` still has `aria-modal={false}` and `touchAction: "none"`
    - `git diff packages/app/src/config.ts` is empty — no tier is renumbered
    - `grep -rc 'components/Sheet' packages/app/src/dex/AlbumDetail.tsx packages/app/src/dex/ArchiveBrowser.tsx packages/app/src/dex/SetlistView.tsx packages/app/src/explore/NodeSheet.tsx` returns 0 for each
    - `npm run build --workspace packages/app` succeeds
  </acceptance_criteria>
  <done>All five hand-rolled sheet surfaces now compete at the top level; the latent DexView trap is removed without renumbering anything.</done>
</task>

<task type="auto">
  <name>Task 2: Complete the invariant with the source-scan portal assertion, and re-verify a11y</name>
  <files>packages/app/test/layerOrder.test.tsx, packages/app/test/sheet.a11y.test.tsx</files>
  <read_first>
    - packages/app/test/layerOrder.test.tsx — the ancestor-walk invariant, `createsStackingContext`, `ancestorsUpToBody` and the two numeric guards landed in plan 21-11
    - packages/app/test/rebrand.test.ts (lines 1-25) — the `readFileSync` + `fileURLToPath(import.meta.url)` source-scan idiom
    - packages/app/test/sheet.a11y.test.tsx (whole file) — the 8 shipped cases including stacked modals, the `mountAppContent()` fixture and the jsdom-limits disclaimer
    - packages/app/src/config.ts §`ui.z` — `sheet: 50`, `focusedFab: 60` and the D-03 exception comment
  </read_first>
  <action>
    **In `layerOrder.test.tsx`**, add a `describe("FOUND-03 portal assertion (D-24, form a)")`
    block. Walk `join(testDir, "..", "src")` recursively (never `dist/`) for `.ts`/`.tsx` files,
    and for every file whose source contains `config.ui.z.sheet`, assert it also contains
    `createPortal(`. Expected members after this plan: `components/Sheet.tsx`,
    `show/SearchSheet.tsx`, `dex/AlbumDetail.tsx`, `dex/ArchiveBrowser.tsx`,
    `dex/SetlistView.tsx`, `explore/NodeSheet.tsx`. Also assert the discovered set is **non-empty**
    (so a refactor that renames the tier cannot make the guard vacuous) and print the file list in
    the failure message.

    Exclude `src/dev/**` by name with a comment (the dev harnesses are the established exception,
    as `dev/OrbFitHarness.tsx` already is for Tailwind `z-*`), and exclude
    `src/layout/bottomSpace.ts` and `src/config.ts` if they mention the tier.

    Extend the ancestor-walk invariant to cover the four newly portaled modal surfaces
    (`AlbumDetail`, `ArchiveBrowser`, `SetlistView`), each rendered open in a realistic tree
    (inside a `DexView`-shaped wrapper). Keep `NodeSheet` **exempt by name from the modal
    assertion** — `aria-modal={false}`, and FOUND-03 says "open **modal** sheet" — but include it
    in the portal source-scan assertion, and add an explicit `it` asserting
    `config.ui.z.focusedFab > config.ui.z.sheet` with the D-03/D-26 reason quoted: the FilterFab
    lifts above the non-modal NodeSheet so a keyboard/VoiceOver user can still reach the filter
    while a node is focused; renumbering it would be an a11y regression.

    **In `sheet.a11y.test.tsx`**, add a portal-parity block mirroring the shipped cases across the
    five hand-rolled surfaces: for each, assert after portaling that `role="dialog"` is present,
    `aria-modal` has its expected value (`"true"` for the four modals, `"false"` for `NodeSheet`),
    the accessible name is non-empty, and the dialog root is a child of `document.body` rather
    than of its trigger's subtree. Leave the 8 shipped `<Sheet>` cases untouched
    (`git diff` must show additions only). Extend the file's jsdom-limits disclaimer to say what
    still cannot be covered here: real double-tap-zoom / long-press-callout suppression and real
    VoiceOver focus order — device UAT test 8.
  </action>
  <verify>
    <automated>cd packages/app &amp;&amp; npx vitest run --project @guezzer/app test/layerOrder.test.tsx test/sheet.a11y.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The portal source-scan block exists, passes, and asserts the discovered file set is non-empty
    - It lists all six expected files in its failure message and never references `dist`
    - The ancestor-walk invariant now covers `SearchSheet`, `FabMenu`, `AlbumDetail`, `ArchiveBrowser` and `SetlistView`
    - `NodeSheet` is exempt from the modal assertion with a named comment, and the `focusedFab > sheet` assertion exists with its D-03 reason
    - `test/sheet.a11y.test.tsx`'s 8 shipped cases are unmodified; the new parity block covers all five hand-rolled surfaces
    - `npm test` passes with no new failures against the 954-test baseline
  </acceptance_criteria>
  <done>FOUND-03's invariant is complete in both forms — structural ancestor walk and portal source scan — with both deliberate exceptions carried by name.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DOM tree position → a11y guarantees | Portaling moves five VoiceOver-verified surfaces out of their trigger's subtree |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-34 | Tampering | a11y contract of four VoiceOver-verified dex/explore surfaces | mitigate | Minimal diff — portal wrapper plus an SSR guard only; `role`, `aria-modal`, accessible name, classes, `zIndex` and handlers pinned unchanged by `git diff` criteria; a per-surface parity block added to `sheet.a11y.test.tsx`; the four land as one independently revertible commit. |
| T-21-35 | Denial of Service (usability) | `NodeSheet` drag/peek gestures after portaling | mitigate | `NodeSheet` owns its gestures via the inline `touchAction: "none"` on its own root, which travels with the portaled node — no class-scoped inheritance is lost. Asserted unchanged by the Task-1 criteria. |
| T-21-36 | Elevation of Privilege (a11y) | `focusedFab: 60` above the portaled non-modal `NodeSheet` | mitigate | Carried as a named, commented exemption with its own assertion. No tier is renumbered (`config.ts` diff must be empty), so the D-03 keyboard/VoiceOver escape route survives. |
| T-21-37 | Repudiation | a vacuous portal source scan | mitigate | The block asserts the discovered file set is non-empty, so renaming or removing the tier cannot silently disable the guard. |
| T-21-SC | Tampering | npm installs | mitigate | Zero packages added, removed or upgraded (`21-RESEARCH.md` §Package Legitimacy Audit). `createPortal` comes from the already-installed `react-dom`. No install task; `package.json` is not in `files_modified`. |
</threat_model>

<verification>
- `npm test` green (baseline 125 files / 954 tests)
- `npm run build --workspace packages/app` succeeds
- `?layerRepro=1`: the band paints over none of the five hand-rolled surfaces
- `git diff packages/app/src/config.ts` is empty — no tier renumbered anywhere in the phase
</verification>

<success_criteria>
- All five hand-rolled sheet-tier surfaces are portaled and remain hand-rolled
- The invariant holds in both forms (ancestor walk + portal source scan) and neither is vacuous
- The two deliberate exceptions survive by name with their reasons
- The prophylactic group is a separate, revertible commit from the live fix
</success_criteria>

<artifacts_this_phase_produces>
Symbols, files and flags newly created by Phase 21 (all plans). Nothing below exists on `master`
before this phase.

**CSS custom properties (new):** `--gz-safe-bottom`, `--gz-tab-bar-h`, `--gz-overlay-inset`,
`--gz-chrome-reserve`, `--gz-content-reserve`, `--gz-sheet-pad-bottom`, `--gz-fab-offset`

**New CSS class (new):** `.gesture-guard` (added to the `styles.css` gesture-suppression
selector list for portaled roots, D-23)

**Config keys (new):** `config.ui.bottomSpace`, `config.ui.bottomSpace.TAB_BAR_HEIGHT_REM`,
`config.ui.bottomSpace.FAB_CLEARANCE_PX`, `config.ui.bottomSpace.SHEET_PAD_BOTTOM_PX`,
`config.copy.tabs` (`show`/`explore`/`map`/`dex`/`games`), `config.copy.presence.activity`,
`config.copy.presence.activityUnknown`

**New modules and exports:** `packages/app/src/layout/bottomSpace.ts`
(`BOTTOM_SPACE_VAR_NAMES`, `bottomSpaceVarEntries`, `applyBottomSpaceVars`,
`useBottomSpaceVars`); `packages/app/src/dex/formatDate.ts` (`formatFullDate`, plus the re-homed
`formatMonYear`); `packages/app/src/dev/layerRepro.tsx` (`isLayerReproEnabled`,
`LayerReproToast`); `packages/app/src/dev/LayoutProbe.tsx` (`isLayoutProbeEnabled`,
`LayoutProbe`, `readSafeAreaInsets`); `packages/app/src/dex/FriendRow.tsx`
(`presenceActivityLabel`); `packages/app/src/dex/shareCard.ts` (module-private
`composeFooterLine`)

**New test-local helpers:** `packages/app/test/layerOrder.test.tsx` (`createsStackingContext`,
`ancestorsUpToBody`)

**Changed signatures:** `showBottomFabOffset(stripSlotReserved: boolean)` (was
`stripHasContent`); `FabMenu` / `OrbitStage` prop `stripSlotReserved` (was `stripHasContent`);
`data-strip-slot-reserved` DOM attribute (was `data-strip-has-content`); `PresenceActivitySlot`
gains an `online?: boolean` prop

**URL flags (new):** `?layerRepro=1`, `?layoutProbe=1`

**New test files:** `packages/app/test/layerRepro.test.tsx`,
`packages/app/test/bottomSpace.test.ts`, `packages/app/test/layerOrder.test.tsx`,
`packages/app/test/formatDate.test.ts`, `packages/app/test/presenceLabels.test.ts`,
`packages/app/test/setlistView.test.tsx`

**New docs:** `.planning/phases/21-layout-layering-foundations/21-HUMAN-UAT.md`,
`.planning/todos/pending/2026-07-24-simultaneous-bottom-overlay-stacking.md`

**Removed by this phase:** `packages/app/src/dex/formatMonYear.ts` (renamed to `formatDate.ts`);
the `padding-bottom: env(safe-area-inset-bottom)` declaration on `body` in `styles.css`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/21-layout-layering-foundations/21-12-SUMMARY.md` when done.
</output>
