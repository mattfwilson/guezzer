---
phase: 10-pre-show-validation-device-dry-run
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - .planning/phases/10-pre-show-validation-device-dry-run/10-HUMAN-UAT.md
autonomous: false
requirements: [VALID-02]

must_haves:
  truths:
    - "The full show loop (start -> predictions -> log hits/misses -> set break -> encore -> End Show -> recap -> dex credit) completes on a real iPhone against a vite build + preview served over the cloudflared HTTPS tunnel (VALID-02, D-07)"
    - "Predictions, logging, and the constellation keep working in airplane mode from precache/IndexedDB (D-05 offline leg)"
    - "A JSON export -> re-import round-trip succeeds on device with no local data loss (VALID-02)"
    - "Android is formally recorded as no-device-available / waived (D-06)"
  artifacts:
    - path: ".planning/phases/10-pre-show-validation-device-dry-run/10-HUMAN-UAT.md"
      provides: "VALID-02 enumerated device-loop checklist + graded result + D-07 harness commands"
      contains: "offline"
  key_links:
    - from: "npm run build + preview -w @guezzer/app"
      to: "cloudflared trycloudflare HTTPS tunnel"
      via: "--http-host-header localhost (device-uat-hosting memory)"
      pattern: "http-host-header localhost"
    - from: "settings/exportDownload.ts"
      to: "settings/importPicker.ts + ownerMatch.ts"
      via: "on-device export -> re-import merge round-trip"
      pattern: "export|import"
---

<objective>
Deliver VALID-02: prove the full show loop survives a real-device rehearsal before show #1. Author the enumerated `10-HUMAN-UAT.md` VALID-02 checklist (start -> predictions -> log hits/misses -> set break -> encore -> End Show -> recap -> dex credit -> offline airplane-mode leg -> JSON export/import round-trip), document the D-07 production-build + cloudflared-tunnel harness, then the owner runs the graded rehearsal on iPhone. Blockers that break the loop are fixed inside this phase (D-09); cosmetic issues are logged to backlog. Android is waived (D-06).

Purpose: Hands-on proof the one-thumb show loop works reliably on the real target device, including the offline venue condition.
Output: The VALID-02 section of 10-HUMAN-UAT.md with a graded pass and any inline blocker fixes recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-pre-show-validation-device-dry-run/10-CONTEXT.md
@.planning/phases/10-pre-show-validation-device-dry-run/10-PATTERNS.md
@.planning/phases/05-live-sync-data-safety/05-HUMAN-UAT.md
@packages/app/src/live/mockLatest.ts
@packages/app/src/show/ShowView.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the VALID-02 device-loop checklist + D-07 harness in 10-HUMAN-UAT.md</name>
  <files>.planning/phases/10-pre-show-validation-device-dry-run/10-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/10-pre-show-validation-device-dry-run/10-HUMAN-UAT.md (the doc created by plan 10-01 — EXTEND it, do not overwrite the VALID-01 test #1)
    - .planning/phases/05-live-sync-data-safety/05-HUMAN-UAT.md (result-line evidence style: on-device / iPhone / over trycloudflare HTTPS tunnel / found+fixed notes inline)
    - packages/app/src/live/mockLatest.ts (the ?mockLatest=1 sync-leg fixture: Rattlesnake 168, Robot Stop 172, Gaia 81, Mars for the Rich 133 — used ONCE for the sync leg only, D-04)
    - packages/app/src/live/useLatestPoll.ts (the poll path the mock swaps into)
    - packages/app/src/show/ShowView.tsx, packages/app/src/show/useShowSession.ts, packages/app/src/show/EndShowDialog.tsx (the real Show Mode controls the rest of the loop is driven through with manual song entry)
    - packages/app/src/settings/exportDownload.ts, packages/app/src/settings/importPicker.ts, packages/app/src/settings/ownerMatch.ts (the export/import round-trip leg)
    - MEMORY device-uat-hosting (build/preview/tunnel commands) and sw-clientsclaim-offline (offline-leg SW note)
  </read_first>
  <action>
    Extend `10-HUMAN-UAT.md` (created in plan 10-01) by appending the VALID-02 tests AFTER the existing VALID-01 test #1 — do not touch test #1. Add a "## Harness (D-07)" note block documenting the exact graded-run setup: `npm run build -w @guezzer/app`, then `npm run preview -w @guezzer/app -- --port 4173 --strictPort` (background), then `cloudflared tunnel --url http://localhost:4173 --http-host-header localhost` (the `--http-host-header localhost` flag is mandatory or vite preview returns 403), grab the `https://<random>.trycloudflare.com` URL, smoke-test that `/`, `/sw.js`, `/manifest.webmanifest` return 200, then install the PWA to the iOS home screen. State explicitly that the graded pass MUST be against this production build — the vite dev server over the tunnel is allowed only for informal bug-shaking, never the recorded run.

    Then enumerate the loop as discrete numbered tests (D-08), each with an `expected:` (steps + expected result) and `result: [pending]`:
      - "### 2. Live-sync leg (?mockLatest=1)" — load the installed app with `?mockLatest=1`, confirm the SuggestionStrip surfaces the fixture song, adopt it, and the orbit auto-binds/recenters with a real prediction fan (D-04: mock used ONCE, sync leg only).
      - "### 3. Start + predictions + log hits/misses" — leave the mock behind; Start Show, pick an opener via manual search entry, confirm the prediction fan renders, log several songs via the real controls, confirm the hit/miss tally + comet trail update correctly.
      - "### 4. Set break + encore" — advance a set boundary and log an encore through the real Show Mode controls (manual entry), confirm set structure is captured.
      - "### 5. End Show + recap + dex credit" — End Show (confirm-gated), confirm the recap renders show-scoped stats and the attended show credits the GizzDex.
      - "### 6. Offline airplane-mode leg (D-05)" — mid-rehearsal enable airplane mode; confirm predictions, logging, and the GizzVerse constellation all still work from precache/IndexedDB with no error banner; re-enable and confirm silent resume.
      - "### 7. JSON export/import round-trip" — export the backup JSON on device, then re-import it via the picker; confirm the owner-match path merges with zero local data loss.
      - "### 8. Android (VALID-02 criterion 3)" — `expected:` no Android device available; `result:` will record "waived / no device available" per D-06.
    Update the "## Summary" counts to 8 tests (1 VALID-01 + 7 VALID-02) and keep the "## Gaps" section.
  </action>
  <verify>
    <automated>grep -qi "offline" .planning/phases/10-pre-show-validation-device-dry-run/10-HUMAN-UAT.md && grep -q "http-host-header localhost" .planning/phases/10-pre-show-validation-device-dry-run/10-HUMAN-UAT.md && grep -qi "export" .planning/phases/10-pre-show-validation-device-dry-run/10-HUMAN-UAT.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - 10-HUMAN-UAT.md contains a "## Harness (D-07)" block with the `--http-host-header localhost` cloudflared command and the vite build+preview commands.
    - The doc contains discrete numbered tests for: mockLatest sync leg, start+predictions+log, set break+encore, End Show+recap+dex credit, the offline airplane-mode leg, and the JSON export/import round-trip.
    - The doc contains a step recording Android as waived / no device available (D-06).
    - The existing VALID-01 test #1 (from plan 10-01) is unchanged.
  </acceptance_criteria>
  <done>The VALID-02 device-loop checklist and D-07 harness are authored in 10-HUMAN-UAT.md, ready for the owner's graded run.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Owner runs the graded device dry-run, records results, fixes blockers inline (D-09)</name>
  <files>.planning/phases/10-pre-show-validation-device-dry-run/10-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/10-pre-show-validation-device-dry-run/10-HUMAN-UAT.md (the checklist authored in Task 1)
    - MEMORY device-uat-hosting (exact build/preview/tunnel steps) and sw-clientsclaim-offline (offline SW note)
  </read_first>
  <action>Owner-executed device rehearsal (no autonomous code unless a blocker is found): the owner builds + serves + tunnels per the "## Harness (D-07)" block, installs the PWA on iPhone, and runs tests 2–7 on-device against the production build. The executor records each `result:` line with an on-device evidence note. Loop-breaking blockers (data loss, broken logging, failed import, offline breakage) are fixed inside Phase 10 (D-09) and re-verified; cosmetic issues are logged to Gaps as deferred. Test 8 is marked Android waived / no device available (D-06); the executor then flips the doc status to complete and updates Summary counts.</action>
  <what-built>
    A production-build show-loop rehearsal checklist covering the live-sync leg (?mockLatest=1), manual-entry start/log/set-break/encore, End Show/recap/dex credit, an airplane-mode offline leg, and a JSON export/import round-trip — to be run on your iPhone over the cloudflared HTTPS tunnel.
  </what-built>
  <how-to-verify>
    1. Build + serve + tunnel per the "## Harness (D-07)" block: `npm run build -w @guezzer/app`, `npm run preview -w @guezzer/app -- --port 4173 --strictPort`, `cloudflared tunnel --url http://localhost:4173 --http-host-header localhost`. Smoke-test `/`, `/sw.js`, `/manifest.webmanifest` return 200, then install the PWA to your iPhone home screen. This MUST be the production build, not the dev server (D-07).
    2. Work through tests 2–7 in order on-device, one thumb: sync leg with `?mockLatest=1` (adopt once), then Start Show and log the loop through real controls with manual song entry, set break, encore, End Show, recap, dex credit.
    3. Run the offline leg (test 6): mid-show, enable airplane mode; confirm predictions, logging, and the GizzVerse constellation still work; re-enable and confirm silent resume.
    4. Run the export/import round-trip (test 7) on device; confirm no local data loss.
    5. For each test, record `result:` as pass/fail with an on-device evidence note. If a step BREAKS the loop (data loss, broken logging, failed import, offline breakage), that is a blocker — fix it inside Phase 10 (D-09) and re-run the affected step; note the found+fixed inline. Log cosmetic/minor issues to the backlog as deferred ideas, not blockers.
    6. Mark test 8 (Android) "waived / no device available" (D-06).
  </how-to-verify>
  <verify>
    <human-check>Owner confirms tests 2–7 pass on-device against the production build; executor fills every result line and flips the doc status. Any blocker fixed inline per D-09 has a found+fixed note.</human-check>
  </verify>
  <resume-signal>Reply "passed" once tests 2–7 pass on-device (blockers fixed inline) and Android is marked waived — or describe the blocker so it can be fixed before the gate closes. The executor then fills every `result:` line, sets the doc status to complete/resolved, updates the Summary counts, and lists any deferred cosmetic issues under Gaps.</resume-signal>
  <acceptance_criteria>
    - Tests 2–7 in 10-HUMAN-UAT.md each have a pass result with an on-device evidence note, run against the production build over the tunnel.
    - The offline airplane-mode leg (test 6) and the export/import round-trip (test 7) are both recorded as passing.
    - Any loop-breaking blocker was fixed inside Phase 10 (D-09) and its found+fixed note is recorded; cosmetic issues are listed under Gaps as deferred.
    - Test 8 records Android as waived / no device available (D-06).
  </acceptance_criteria>
  <done>VALID-02 is closed: the full show loop (incl. offline leg + export/import round-trip) passed a real-device rehearsal, recorded in 10-HUMAN-UAT.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cloudflared tunnel -> public internet | The local vite-preview production build is briefly exposed at an ephemeral trycloudflare HTTPS URL for iOS testing |
| exported JSON file -> re-import | An owner-authored backup file crosses back into the app's merge path on device |
| live poll (mockLatest fixture) | The sync leg swaps a local fixture into the poll path; no real network call to the volunteer API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Information disclosure | exported backup JSON on device | accept | Owner-local backup; contains only setlist/attendance data, no credentials or PII of others; owner-controlled |
| T-10-05 | Spoofing / Tampering | import round-trip merge path | mitigate | Reuses the Phase 5/9 zod envelope validation + ownerMatch/classifyImport fork on `envelope.owner` before any merge — a foreign or malformed file cannot silently overwrite local data |
| T-10-06 | Information disclosure | cloudflared HTTPS tunnel exposes the preview build publicly | mitigate | Ephemeral trycloudflare URL, no secrets in the static bundle (no backend, no accounts), processes/tunnel torn down when UAT ends; residual exposure accepted for the short test window |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan; cloudflared is a downloaded release binary run manually, not a dependency added to the tree; legitimacy gate N/A |
</threat_model>

<verification>
- `grep` confirms 10-HUMAN-UAT.md contains the offline leg, the `--http-host-header localhost` harness command, and the export/import step.
- Owner-run: tests 2–7 recorded as passing on iPhone against the production build over the tunnel; test 8 waived.
- Any blocker found was fixed inside Phase 10 and recorded (D-09).
</verification>

<success_criteria>
VALID-02 complete: the full show loop — including the offline airplane-mode leg and the JSON export/import round-trip — passed a real-device rehearsal on iPhone against the production build over the cloudflared tunnel, with Android formally waived, all recorded in 10-HUMAN-UAT.md.
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 10-02)

- `.planning/phases/10-pre-show-validation-device-dry-run/10-HUMAN-UAT.md` (EXTENDED) — adds the "## Harness (D-07)" block and VALID-02 tests 2–8 (sync leg, start+log, set break+encore, End Show+recap+dex, offline leg, export/import round-trip, Android waiver). No new symbols or CLI files. The owner-executed device steps (Task 2) are a human task, not autonomous code.
- Conditional (D-09 blocker branch only): inline source fixes to whichever Show Mode / live / settings file breaks the loop — scope determined at UAT time, recorded as found+fixed notes.
</artifacts>

<output>
Create `.planning/phases/10-pre-show-validation-device-dry-run/10-02-SUMMARY.md` when done.
</output>
