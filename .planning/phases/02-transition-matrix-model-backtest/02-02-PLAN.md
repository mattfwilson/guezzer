---
phase: 02-transition-matrix-model-backtest
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - packages/core/src/model/predict.ts
  - packages/core/src/index.ts
  - packages/core/test/model/predict.test.ts
autonomous: true
requirements: [MODL-03, MODL-08, MODL-09, EVAL-05]
must_haves:
  truths:
    - "Given a current song and a TransitionMatrix, predict returns ranked next-song candidates"
    - "The base factor is a weighted interpolated blend of four tiers, each normalized over the candidate universe (Jelinek-Mercer)"
    - "A plausible unseen A→B pair still receives a positive score (never a hard zero) via the basePlayRate floor"
    - "Tuning-family affinity contributes ONLY inside the backoff blend, never as a top-level multiplier"
    - "Each candidate carries a rich factor breakdown and a one-line human-readable reason string"
    - "Ranking is deterministic with a stable tie-break (playCount desc, songId asc)"
  artifacts:
    - path: "packages/core/src/model/predict.ts"
      provides: "predict + baseFactor + tier functions + scoreCandidate skeleton"
      exports: ["predict", "baseFactor", "scoreCandidate"]
    - path: "packages/core/test/model/predict.test.ts"
      provides: "Wave-0 predict scaffold (backoff floor, tuning-backoff-only, transitionProb)"
  key_links:
    - from: "packages/core/src/model/predict.ts"
      to: "packages/core/src/model/index-build.ts"
      via: "predict builds/consumes the from→edges index"
      pattern: "buildMatrixIndex\\("
    - from: "packages/core/src/model/predict.ts"
      to: "packages/core/src/model/matrix.ts"
      via: "tuningAffinity tier reads MatrixNode.tuningFamily baked at build time — predict.ts must stay zero-I/O and never read data/tuning-tags.json directly"
      pattern: "tuningFamily"
---

<objective>
Deliver the predictor's core: the interpolated backoff base (D-02 Jelinek-Mercer blend of transition / tuning / album-era / base-play-rate tiers, MODL-03/MODL-08/MODL-09) and the `predict` entrypoint that ranks candidates with the rich per-candidate breakdown + reason string (D-06). The multiplicative modifier pipeline is stubbed here (all downstream multipliers default to 1.0) so this plan produces a real first ranked prediction end-to-end; Plan 03 layers the signals in.

This is MVP slice 1 (part B): the predict leg. Independently testable against fixtures — a current song yields plausible ranked next-songs with a nonzero floor for every candidate.

Purpose: Establish the D-01 multiplicative pipeline skeleton and the D-02 smoothing that makes the sparse corpus (65% singleton edges) predict without hard zeros.
Output: `model/predict.ts` (tiers + baseFactor + scoreCandidate + predict), barrel exports, Wave-0 `predict.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-transition-matrix-model-backtest/02-CONTEXT.md
@.planning/phases/02-transition-matrix-model-backtest/02-RESEARCH.md
@.planning/phases/02-transition-matrix-model-backtest/02-PATTERNS.md
@.planning/phases/02-transition-matrix-model-backtest/02-01-SUMMARY.md
</context>

<artifacts_produced>
Symbols this plan creates (exclude from drift verification — they are new):
- **Functions** (`model/predict.ts`): `predict(matrix, context, cfg, toggles) → PredictionCandidate[]`, `baseFactor`, tier helpers `transitionProb` / `tuningAffinity` / `albumEraAffinity` / `basePlayRate`, `scoreCandidate` (skeleton — downstream multipliers stubbed to 1.0 pending Plan 03).
- Barrel re-exports of `predict` from `index.ts`.
- **Test**: `packages/core/test/model/predict.test.ts` (extended further in Plan 03).
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Four backoff tiers + Jelinek-Mercer baseFactor + Wave-0 predict.test.ts scaffold</name>
  <files>packages/core/src/model/predict.ts, packages/core/test/model/predict.test.ts</files>
  <read_first>
    - packages/core/src/ingest/tuning-tags.ts (the staged-helper decomposition analog lines 104-249: several small named pure fns composed; `config.microtonalAlbums as readonly string[]` cast pattern line ~177; how `TuningFamily` is derived)
    - packages/core/src/model/index-build.ts (from Plan 01 — `buildMatrixIndex` shape the tiers read)
    - packages/core/src/model/matrix.ts (from Plan 01 — `MatrixEdge`/`MatrixNode` fields the tiers consume: weightedCount, count, playCount, tuningFamily)
    - packages/core/src/domain/types.ts (PredictionCandidate/PredictionFactors/BackoffTier/ShowContext/SignalToggles from Plan 01)
    - packages/core/test/normalize.test.ts (fixture-normalize + numbered-assertion pattern)
    - packages/core/test/fixtures/2017-segues.json (a small dense fixture to build a matrix from for tier tests)
    - .planning/phases/02-transition-matrix-model-backtest/02-RESEARCH.md (M1 tier definitions + weight seeds lines 186-194; Pattern 1 baseFactor lines 127-142; Pitfall 4 uncalibrated blend lines 255-258)
    - .planning/phases/02-transition-matrix-model-backtest/02-PATTERNS.md (§ predict.ts lines 164-189)
  </read_first>
  <behavior>
    - Test "transitionProb" (MODL-03): for a matrix built from `2017-segues`, `transitionProb(A, B)` equals `weightedCount(A→B) / Σ_x weightedCount(A→x)` and sums to ~1 over A's observed successors.
    - Test "backoff floor" (MODL-08): for a plausible A→B pair that has NO observed edge, `baseFactor(A, B)` is strictly greater than 0 (the `basePlayRate` tier supplies a nonzero floor) — assert `> 0`, never `=== 0`.
    - Test "tuning backoff only" (MODL-09): `tuningAffinity` contributes to `baseFactor` (its weight `w2 > 0`) but there is NO top-level multiplication by tuning family anywhere in `scoreCandidate` — verified by asserting that zeroing `w2` in a config clone changes `baseFactor` while the pipeline exposes no separate tuning multiplier field in `PredictionFactors`.
    - Test "tier normalization" (Pitfall 4): each tier function summed over the candidate universe C is ~1.0 (within float tolerance) before blending.
  </behavior>
  <action>
    Create `model/predict.ts` decomposed into small named pure fns (mirror the `tuning-tags.ts` staged-helper style). Define the candidate universe `C` = all non-placeholder catalog `MatrixNode` song ids. Define four tier functions, EACH normalized to sum to 1 over `C` before blending (Pitfall 4 — uncalibrated blend is the failure mode): `transitionProb(A, B, index)` = `weightedCount(A→B) / Σ_x weightedCount(A→x)` (use raw `count` when the `decay` toggle is off, hooked in Plan 03; add-α from `cfg.transitionAddAlpha` to the numerator, seed 0.0) — MODL-03 first-order signal; `tuningAffinity(A, B, index)` = successor frequency of `B` among within-set successors of all songs sharing A's tuning family, normalized — D-03/MODL-09, contributes ONLY here; `albumEraAffinity(A, B, index)` = same-album/era-cluster pair affinity, normalized (resolve the operational "era cluster" per RESEARCH M8 — co-activity within the era window is acceptable given album metadata is not in the normalized corpus; keep it distinct from `eraPrior`); `basePlayRate(B, index)` = `playCount(B) / Σ playCount` — the all-time marginal, the nonzero floor guaranteeing MODL-08. Then `baseFactor(A, B, index, cfg, toggles)` = `w1·transitionProb + w2·tuningAffinity + w3·albumEraAffinity + w4·basePlayRate` with weights from `cfg.backoffWeights` (D-02 Jelinek-Mercer). When a backoff tier is toggled off (Plan 03 ablation), DROP that tier and re-normalize the remaining weights to sum to 1 (M6) — implement the re-normalization helper now so Plan 03 just flips flags. Record which tier carried the most mass into `PredictionFactors.backoffTier`.

    Create `test/model/predict.test.ts` (D-17 / EVAL-05 — known-output fixture setlists) implementing the four `behavior` tests: normalize `2017-segues.json`, `buildMatrix`, `buildMatrixIndex`, then assert tier values and the nonzero floor. Numbered `it("Test N: ...")` assertions with concrete expected values.
  </action>
  <verify>
    <automated>npx vitest run packages/core test/model/predict.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run packages/core -t "transitionProb"` passes — MLE ratio over weightedCount edges, sums ~1 over A's successors.
    - `npx vitest run packages/core -t "backoff floor"` passes — an unobserved plausible pair scores strictly `> 0`.
    - `npx vitest run packages/core -t "tuning backoff only"` passes — tuning affects only the blend, no top-level tuning multiplier exists.
    - Each tier summed over C is within 1e-6 of 1.0 (tier-normalization assertion).
  </acceptance_criteria>
  <done>Four normalized tiers + Jelinek-Mercer baseFactor exist; unseen pairs never hard-zero; tuning is backoff-only; Wave-0 predict scaffold green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: predict entrypoint — scoreCandidate skeleton, deterministic ranking, reason string, barrel export</name>
  <files>packages/core/src/model/predict.ts, packages/core/src/index.ts, packages/core/test/model/predict.test.ts</files>
  <read_first>
    - packages/core/src/model/predict.ts (the tiers + baseFactor from Task 1)
    - packages/core/src/ingest/census.ts (explicit-comparator sort discipline ~lines 132/174-175 — never rely on Map order; deterministic tie-break)
    - packages/core/src/index.ts (barrel — add `predict`; keep CLI internals out)
    - packages/core/src/domain/types.ts (PredictionCandidate/PredictionFactors/SignalToggles/ShowContext)
    - .planning/phases/02-transition-matrix-model-backtest/02-RESEARCH.md (Pattern 2 multiplicative pipeline lines 144-162; D-06 rich breakdown; M7 candidateListSize lines 221-225; Pitfall 2 deterministic tie-break lines 244-247)
    - .planning/phases/02-transition-matrix-model-backtest/02-CONTEXT.md (D-06 reason-string example "notated segue 14/15 times since 2024"; D-01 multiplicative pipeline)
  </read_first>
  <behavior>
    - Test "ranked prediction" (MODL-03 end-to-end): `predict(matrix, context, cfg, defaultToggles)` returns at least `min(cfg.candidateListSize, |C|)` candidates sorted by `score` desc; the strongest observed successor of the current song ranks above an unobserved one.
    - Test "deterministic ranking" (Pitfall 2): two candidates with equal score are ordered by `playCount` desc then `songId` asc; `predict` output is identical across repeated calls.
    - Test "candidate breakdown" (D-06): every returned `PredictionCandidate` has a populated `factors` object (transitionProb, decay, rotation, alreadyPlayed, eraPrior, backoffTier, hardSegueFlag) and a non-empty `reason` string.
  </behavior>
  <action>
    Add `scoreCandidate(A, B, index, cfg, toggles, ctx)` to `predict.ts` implementing the D-01 multiplicative pipeline SKELETON: `let s = baseFactor(...)`, then `s *= toggles.rotation ? rotationSuppression(...) : 1`, `s *= toggles.alreadyPlayed ? alreadyPlayedFactor(...) : 1`, `s *= toggles.eraPrior ? eraPrior(...) : 1`, and a hard-segue override hook — but in THIS plan every downstream modifier is a stub returning the neutral `1.0` (and hard-segue returns null). Plan 03 fills the real bodies; the toggle-gated structure is finalized now so ablation is a pure flag-flip (M6). Every downstream signal defaults to `1.0` when its toggle is off. Assemble `PredictionFactors` (record the neutral 1.0 stubs honestly) and a `reason` string built from the edge's stored `segueCount`/`count`/`firstDate` when an observed edge exists (e.g. "seen 8× since 2024"), else a backoff-tier reason (e.g. "backoff: base play rate") — concrete counts, not vague labels (D-06, powers SHOW-10 later).

    Add `predict(matrix, context, cfg = config, toggles = allTrue)` that builds the index (or accepts a prebuilt one), scores every candidate in `C` (264 songs is trivial to rank fully), sorts by `score` desc with tie-break `playCount` desc then `songId` asc (Pitfall 2), and returns the top `cfg.candidateListSize` as `PredictionCandidate[]`. Export `predict` from the barrel `index.ts`.

    Extend `test/model/predict.test.ts` with the three `behavior` tests above.
  </action>
  <verify>
    <automated>npx vitest run packages/core test/model/predict.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run packages/core -t "ranked prediction"` passes — returns ranked candidates; observed successor outranks unobserved.
    - `npx vitest run packages/core -t "deterministic ranking"` passes — stable tie-break, identical across calls.
    - `npx vitest run packages/core -t "candidate breakdown"` passes — every candidate has full factors + non-empty reason.
    - `predict` is exported from `index.ts`; `npx tsc -p packages/core --noEmit` passes.
  </acceptance_criteria>
  <done>predict ranks candidates deterministically with rich D-06 breakdowns; the toggle-gated multiplicative skeleton is in place (stubs neutral); barrel exports predict.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | `predict` is a pure in-memory function over the already-validated matrix + caller-supplied `ShowContext`; no I/O, no network, no argv |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | Malformed `ShowContext` (e.g. songId not in matrix) | mitigate | `predict` treats unknown current songs via the backoff floor (never throws on plausible input) and ignores trail ids not in `C`; no hard-zero, no crash |
| T-02-05 | Information Disclosure | reason strings embedding song names | accept | Song names are catalog data (not user secrets); markdown escaping is applied downstream at report-write time (Plan 05), not here (pure fn returns plain strings) |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase (RESEARCH § Package Legitimacy Audit: zero new deps) |
</threat_model>

<verification>
- `npx vitest run packages/core` — full core suite green (matrix + predict).
- `npx tsc -p packages/core --noEmit` clean.
- `predict` produces ranked candidates with nonzero floors for all plausible songs.
</verification>

<success_criteria>
- Predictor returns ranked candidates combining transition frequency + interpolated backoff, with no plausible song hard-zeroed and tuning-family used only in backoff (Phase success criterion 2 — base leg; hard-segue/recency/rotation layered in Plan 03).
- Fixture unit tests with known expected outputs pass (success criterion 5).
</success_criteria>

<output>
Create `.planning/phases/02-transition-matrix-model-backtest/02-02-SUMMARY.md` when done.
</output>
