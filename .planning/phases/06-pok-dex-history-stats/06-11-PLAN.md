---
phase: 06-pok-dex-history-stats
plan: 11
type: execute
wave: 9
depends_on: ["06-06", "06-09", "06-10"]
files_modified:
  - packages/core/src/dex/share-stats.ts
  - packages/core/src/index.ts
  - packages/app/src/dex/shareCard.ts
  - packages/app/src/dex/ShareCardSheet.tsx
  - packages/app/src/dex/DexHeader.tsx
  - packages/app/src/dex/DexView.tsx
  - packages/app/src/dex/RecapView.tsx
  - packages/app/src/config.ts
  - packages/core/test/dex/share-stats.test.ts
  - packages/app/test/shareCard.test.tsx
autonomous: true
requirements: [SHAR-02]
must_haves:
  truths:
    - "Share card generates a 1080×1350 PNG from derived dex stats and shares via navigator.share (files) with an anchor-download fallback; the File is built BEFORE the share tap (iOS transient activation, Pitfall 7)"
    - "Share/render failures surface calm copy and never touch app state"
    - "The accent Share card CTA lives in the DexHeader and the RecapView footer — no other accent use is introduced"
  artifacts:
    - path: "packages/core/src/dex/share-stats.ts"
      provides: "buildShareStats(dexStats) → ShareCardData"
      contains: "buildShareStats"
    - path: "packages/app/src/dex/shareCard.ts"
      provides: "Canvas draw fn (ctx, data) + share/download flow"
      contains: "canShare"
  key_links:
    - from: "packages/app/src/dex/shareCard.ts"
      to: "packages/app/src/settings/exportDownload.ts idiom"
      via: "anchor-download fallback + never-throw contract"
      pattern: "createObjectURL"
    - from: "packages/app/src/dex/ShareCardSheet.tsx"
      to: "@guezzer/core buildShareStats"
      via: "all card numbers assembled in core (jsdom has no canvas — Pitfall 8)"
      pattern: "buildShareStats"
---

<objective>
The brag-card half of the phase's social payoff (SHAR-02, D-18/D-19): pure core `buildShareStats`, the canvas-rendered 1080×1350 PNG share card, the Web Share + anchor-download flow, and the accent Share CTAs in DexHeader and RecapView.

User story slice: *As a collector walking out of night three, I want to trade a brag card with my friends, so that the collection game has a trophy to show off.*

Purpose: the share flow must pre-build the File before the tap (Pitfall 7, iOS `NotAllowedError` on transient activation) and keep all stat assembly in core because jsdom has no canvas (Pitfall 8 — the draw fn takes (ctx, data) so tests use a recorded mock ctx). Builds on the 06-10 compare slice's core patterns; the two slices are sequenced because they share config.ts, the core barrel, and DexView.
Output: one core module, shareCard + preview sheet, Share CTAs in DexHeader + RecapView, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-pok-dex-history-stats/06-CONTEXT.md
@.planning/phases/06-pok-dex-history-stats/06-RESEARCH.md
@.planning/phases/06-pok-dex-history-stats/06-PATTERNS.md
@.planning/phases/06-pok-dex-history-stats/06-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Core buildShareStats</name>
  <files>packages/core/src/dex/share-stats.ts, packages/core/src/index.ts, packages/core/test/dex/share-stats.test.ts</files>
  <read_first>
    - packages/core/src/dex/derive-dex.ts (DexStats — the input shape; read what deriveDex already outputs before choosing the signature)
    - packages/core/src/dex/rarity.ts (tier data for the tier breakdown)
    - 06-UI-SPEC.md §Layout 5 (card contents: wordmark, completion hero, {x}/{y}, show count, rarest catch + tier, tier breakdown, latest/best show)
  </read_first>
  <behavior>
    - buildShareStats(dexStats, archive) returns ShareCardData: completionPct, caught, total, showCount, rarestCatch { songName, tier } | null, tierBreakdown (count per tier over CAUGHT songs), latestShow { date, venue } | null.
    - Zero-catch dex yields a valid card (0%, empty breakdown, null rarest) — no NaN.
  </behavior>
  <action>
    Create `packages/core/src/dex/share-stats.ts` exporting `ShareCardData` and `buildShareStats(dex: DexStats, archive: ArchiveArtifact): ShareCardData` (song name for rarest resolved from archive.songs; latestShow from the newest attended show — accept a precomputed value in DexStats if 06-03 exposed one, else derive from the snapshot's attendance passed alongside; keep the signature minimal and consistent with what deriveDex already outputs — read it first). Barrel-export. Tests: share stats happy path (every ShareCardData field pinned for a fixture dex) + zero-catch path (no NaN — asserted).
  </action>
  <verify>
    <automated>npx vitest run --project @guezzer/core test/dex/share-stats.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - share-stats.test.ts pins every ShareCardData field for a fixture dex and the zero-catch case (no NaN — asserted).
    - grep confirms share-stats.ts is pure (no I/O, no DB imports).
    - Full core suite green.
  </acceptance_criteria>
  <done>Card-stat assembly is pure core — the draw layer only draws, and jsdom tests never need a canvas for the numbers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Share card — canvas PNG + Web Share + CTAs (D-18/D-19, Pitfalls 7-8)</name>
  <files>packages/app/src/dex/shareCard.ts, packages/app/src/dex/ShareCardSheet.tsx, packages/app/src/dex/DexHeader.tsx, packages/app/src/dex/DexView.tsx, packages/app/src/dex/RecapView.tsx, packages/app/src/config.ts, packages/app/test/shareCard.test.tsx</files>
  <read_first>
    - packages/app/src/settings/exportDownload.ts (anchor-download + never-throw idiom lines ~35-76 — the fallback branch and the { ok } contract)
    - packages/core/src/dex/share-stats.ts (Task 1 ShareCardData — ALL numbers come from core; the draw fn only draws)
    - 06-RESEARCH.md §Code Examples "Share flow that survives iOS transient activation" (pre-build the File on preview open; canShare gate; user-cancel is normal) + Pitfall 8 (jsdom has no canvas — draw takes (ctx, data))
    - 06-UI-SPEC.md §Layout 5 (card contents/colors) + §Copywriting ("Share card", "Card saved to your downloads.", "Couldn't build the card." + body)
  </read_first>
  <behavior>
    - drawShareCard(ctx, data, opts) draws the fixed 1080×1350 layout (config share.CARD_WIDTH/CARD_HEIGHT) on #0C0C10: Guezzer wordmark, completion % hero, {caught}/{total}, show count, rarest catch + tier word (Legendary in gold), tier breakdown line, latest show — system font stack, contract colors only; testable against a recorded mock ctx (fillText/fillRect call log).
    - Opening the share preview builds the PNG File immediately (canvas.toBlob → File) — BEFORE any share tap.
    - Share tap: navigator.canShare({ files: [file] }) → navigator.share synchronously with the pre-built file; unsupported → anchor download + "Card saved to your downloads."; user-cancel (AbortError) is silent.
    - getContext returning null or toBlob failing → "Couldn't build the card." calm copy; { ok: false }; app state untouched.
    - The accent Share card CTA appears in the DexHeader and the RecapView footer, opening the preview sheet.
  </behavior>
  <action>
    Create `shareCard.ts` exporting `drawShareCard(ctx: CanvasRenderingContext2D, data: ShareCardData, opts: { width: number; height: number }): void` (pure draw — no canvas creation inside) and `buildShareCardFile(data): Promise<{ ok: true; file: File; previewUrl: string } | { ok: false; error: string }>` (creates the canvas, guards getContext null, draws, toBlob → File "guezzer-dex.png" image/png, objectURL for the <img> preview; never throws) and `shareOrDownload(file): Promise<{ ok: boolean; method: "share" | "download" | "cancelled" | "failed" }>` (canShare gate → share in try/catch with AbortError → "cancelled"; else the exportDownload anchor idiom + revokeObjectURL in finally). Create `ShareCardSheet.tsx` (bottom-sheet per EndShowDialog idiom): on open, immediately `buildShareCardFile(buildShareStats(dex, archive))`; render the preview <img>, a Share button calling shareOrDownload with the ALREADY-BUILT file (the Pitfall-7 contract — no async work between tap and share), the fallback/success/failure copy states. Add the accent `Share2` CTA to DexHeader (reserved accent use #1) and the RecapView footer ("Share card" accent · "Done" neutral per spec), both opening the sheet with current dex stats. Add `config.share.CARD_WIDTH: 1080`, `CARD_HEIGHT: 1350` and `config.copy.share` block. Tests in shareCard.test.tsx: drawShareCard against a recorded mock ctx asserts the hero percentage string and tier-breakdown text were drawn (fillText call args) and background fillRect uses #0C0C10; buildShareCardFile returns { ok: false } with the calm error when getContext is null (jsdom default — the natural test); shareOrDownload picks share when canShare true (mocked navigator), download when absent, "cancelled" on AbortError.
  </action>
  <verify>
    <automated>npx vitest run --project @guezzer/app test/shareCard.test.tsx</automated>
    <human-check>End-of-phase device gate: real navigator.share sheet opens on iPhone with the PNG attached; download fallback verified on desktop.</human-check>
  </verify>
  <acceptance_criteria>
    - shareCard.test.tsx passes: mock-ctx draw assertions, getContext-null calm failure, canShare-gated share vs download vs cancelled.
    - grep confirms shareCard.ts contains no stat arithmetic (numbers come from ShareCardData) and DexHeader/RecapView reference the sheet.
    - The Share button handler performs no await between the tap and navigator.share (source assertion: share is called with a variable captured from state, not a fresh toBlob).
    - Full app suite + `npx tsc --noEmit` green.
  </acceptance_criteria>
  <done>One tap turns the dex into a brag card in the OS share sheet — or a downloaded PNG — never an error state that touches data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Canvas/share APIs → OS | Generated file handed to the OS share sheet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-27 | Denial of service (share failure) | canvas/share flow | mitigate | Never-throw contract: getContext/toBlob/share all guarded; { ok:false } + calm copy; app state untouched |
| T-06-SC | Tampering (supply chain) | package installs | mitigate | No new dependencies in this plan |
</threat_model>

<verification>
- `npx vitest run` (both projects) green; `npx tsc --noEmit` green.
- Dev-server spot check: share preview renders; desktop fallback downloads the PNG.
</verification>

<success_criteria>
- SHAR-02/D-18/D-19: PNG card with completion %, rarest catch, show count, tier breakdown, total caught, latest show; Web Share + download fallback.
- Pitfall 7 honored: the File exists before the tap; no async work between tap and navigator.share.
- Pitfall 8 honored: all numbers from core; draw fn testable with a mock ctx.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 11)

New symbols (exclude from source-drift verification — created here):
- `ShareCardData`, `buildShareStats` — `packages/core/src/dex/share-stats.ts`
- `drawShareCard`, `buildShareCardFile`, `shareOrDownload` — `packages/app/src/dex/shareCard.ts`
- Component `ShareCardSheet` — `packages/app/src/dex/ShareCardSheet.tsx`
- Config: `config.share.CARD_WIDTH/CARD_HEIGHT`, `config.copy.share.*` — `packages/app/src/config.ts`
- Barrel re-exports — `packages/core/src/index.ts`
- New tests: `share-stats.test.ts`, `shareCard.test.tsx`
</artifacts_produced>

<output>
Create `.planning/phases/06-pok-dex-history-stats/06-11-SUMMARY.md` when done
</output>
