---
phase: 21-layout-layering-foundations
plan: 04
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - .planning/phases/21-layout-layering-foundations/21-HUMAN-UAT.md
autonomous: false
requirements: [FOUND-01, FOUND-03]

must_haves:
  truths:
    - "21-HUMAN-UAT.md test 7 carries a recorded desktop result naming which surfaces the toast band paints over and whether it eats taps"
    - "21-HUMAN-UAT.md test 1 carries recorded before-numbers for sab, bodyH-rootH and GAP in BOTH portrait and landscape on an installed instance, with device model and OS version"
    - "The recorded numbers select one of three named branches (D-15 confirmed / D-19 already-flush / D-15 falsified) so plan 21-07 and plan 21-11 know what to build"
  artifacts:
    - path: ".planning/phases/21-layout-layering-foundations/21-HUMAN-UAT.md"
      provides: "recorded before-measurement and repro evidence"
      contains: "PASS"
  key_links:
    - from: ".planning/phases/21-layout-layering-foundations/21-HUMAN-UAT.md"
      to: "21-07-PLAN.md styles.css:220 removal"
      via: "the branch selected by the recorded GAP value"
      pattern: "CONFIRMATION BRANCH|FALSIFICATION BRANCH"
---

<objective>
Device session #1: run the two gating manual verifications and record their results in
`21-HUMAN-UAT.md`. This is not an end-of-phase check — D-30 and D-14 make these two
measurements **sequencing dependencies** for the work in waves 3 and 6.

Purpose: D-30 orders the phase repro → bottom-space → portals, because the repro's result sizes
the layering work and the FOUND-01 before-measurement is what licenses the `styles.css:220`
removal. D-19 makes a non-reproduction a valid outcome: if the numbers show flush, the
measurement IS the evidence and no code change is needed to close FOUND-01. Running these later
would mean editing the same files twice.

Output: `21-HUMAN-UAT.md` tests 1 (before half) and 7 filled in with numbers, screenshots and a
named branch decision.

**Why this plan is not deferred to the end-of-phase human-verify gate** (`workflow.human_verify_mode`
is `end-of-phase`): these two items are not verification of work already done — they are inputs
that determine what plans 21-07 and 21-11 build. The *after*-verification half, and the other six
manual items, are correctly batched at phase end in plan 21-13.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-layout-layering-foundations/21-CONTEXT.md
@.planning/phases/21-layout-layering-foundations/21-RESEARCH.md
@.planning/phases/21-layout-layering-foundations/21-HUMAN-UAT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Desktop repro of the FOUND-03 stacking-context defect (D-20, D-29, UAT test 7)</name>
  <files>.planning/phases/21-layout-layering-foundations/21-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/21-layout-layering-foundations/21-HUMAN-UAT.md §"### 7." — the expected block written in plan 21-01
    - .planning/phases/21-layout-layering-foundations/21-RESEARCH.md §The Stacking-Context Defect — the predicted result table (SearchSheet, CometTrail scrim and FabMenu lose; AlbumDetail / ArchiveBrowser / SetlistView / NodeSheet win)
    - .planning/phases/21-layout-layering-foundations/21-CONTEXT.md D-20 (the structural lead hypothesis this repro tests, with its four pieces of scouting evidence), D-27, D-28, D-29
  </read_first>
  <action>
    Run the `?layerRepro=1` desktop repro exactly as scripted in `<how-to-verify>` below. This is
    the test of **D-20**'s structural lead hypothesis — a `z-index: 50` nested inside a stacking
    context of `10` loses to a top-level `20`, so the tier numbers are consistent and the
    *nesting* is not — and D-20 explicitly requires producing the repro and naming the offending
    surface before acting. Write the outcome into `21-HUMAN-UAT.md` test 7's `result: |` block as a PASS/FAIL plus a
    per-surface table with one row each for `FabMenu` (scrim and rows), `SearchSheet`,
    `CometTrail`, `AlbumDetail`, `ArchiveBrowser`, `SetlistView` and `NodeSheet`, and two columns:
    does the toast band paint over it, and are taps eaten. Name any transform-created stacking
    context that actually reproduced with its file:line, or state explicitly that none did (D-28:
    fix only what reproduces). End the result by naming which surfaces plan 21-11 must portal and
    which, if any, plan 21-12 may skip. Update `## Current Test`. Change no production source in
    this task — the fixes are plans 21-11 and 21-12.
  </action>
  <what-built>
    `?layerRepro=1` (plan 21-01) renders a persistent band at `config.ui.z.toast` (20) as a
    sibling of `AppShell`, exactly where the five shipped toasts render. `ShowView.tsx:173-183`
    wraps the show column in `position: relative` + `zIndex: config.ui.z.content` (10), which
    creates a stacking context — so anything inside it paints at effective level 10, below the
    band, regardless of its own z-index.
  </what-built>
  <how-to-verify>
    1. `npm run dev --workspace packages/app` and open the app in a desktop browser at
       `http://localhost:5173/?layerRepro=1`. Confirm a band reading "layerRepro: toast tier 20"
       is visible above the tab bar.
    2. Go to the **Live** tab. Start or resume a show so the FAB is present. Tap the FAB to open
       the speed-dial. **Record:** does the band paint over the FAB scrim and/or the action rows?
       Try to tap an action row where the band overlaps it — **record whether the tap is eaten.**
       (D-27: this is the worse symptom because it hits the live-logging loop.)
    3. Open **SearchSheet** from the Live tab (the centre orb / the FAB's search action).
       **Record:** does the band paint over the sheet?
    4. Open the **comet trail** sheet (`CometTrail`) from the Live tab. **Record** the same.
    5. Go to **Me** (the dex tab). Open an album (`AlbumDetail`), the archive browser
       (`ArchiveBrowser`) and a setlist (`SetlistView`). **Record:** does the band paint over any
       of them? RESEARCH predicts **no** — they are not inside a stacking context.
    6. Go to **GizzVerse**, tap a node to open `NodeSheet`. **Record** the same. RESEARCH predicts
       **no**.
    7. Screenshot each case where the band wins.
    8. Also grep for transform-created ancestors and check the ones RESEARCH lists
       (`MapView.tsx:213`, `ExploreBackground.tsx:120/157`, `ShowBackground`,
       `ExploreFilterFab.tsx:97`) for any **mispositioned** `fixed` descendant — a transform
       ancestor makes `fixed` children position relative to it, which is worse than mispainting.
       **Record only what actually reproduces** (D-28).
  </how-to-verify>
  <verify>
    <human-check>
      `21-HUMAN-UAT.md` test 7's `result:` block is filled in with a seven-row per-surface table
      and an explicit statement of which surfaces plans 21-11 and 21-12 must portal.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `21-HUMAN-UAT.md` test 7's `result:` block is no longer `PENDING` and contains a row for each of: FabMenu (scrim + rows), SearchSheet, CometTrail, AlbumDetail, ArchiveBrowser, SetlistView, NodeSheet
    - The FabMenu row explicitly records whether taps were eaten
    - Any transform-created stacking context that actually reproduced is named with its file:line; if none reproduced, the result says so explicitly
    - The result names which surfaces plan 21-11 must portal and which (if any) plan 21-12 may skip
    - `git diff packages/app/src` is empty for this task
  </acceptance_criteria>
  <done>The FOUND-03 defect is reproduced (or ruled out) per surface, in writing, so the portal work is sized by evidence rather than by hypothesis.</done>
  <resume-signal>Type "approved" with the per-surface outcome, or describe what differed from the prediction</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: FOUND-01 before-measurement, installed instance, portrait and landscape (D-14/D-18/D-19, UAT test 1)</name>
  <files>.planning/phases/21-layout-layering-foundations/21-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/21-layout-layering-foundations/21-HUMAN-UAT.md §"### 1." and §"## Harness" — the expected block and the tunnel commands written in plan 21-01
    - .planning/phases/21-layout-layering-foundations/21-RESEARCH.md §The Installed-PWA Dead Gap — the derivation predicting `gap ≡ env(safe-area-inset-bottom)` and the falsifiable equality `body.offsetHeight − #root.offsetHeight === S`
    - .planning/phases/21-layout-layering-foundations/21-CONTEXT.md D-11, D-15, D-17, D-18, D-19
  </read_first>
  <action>
    Install the current build to the home screen over the HTTPS tunnel, run `?layoutProbe=1` in
    the installed instance, and record the readout in **both** orientations into
    `21-HUMAN-UAT.md` test 1's `result: |` block — two labelled tables carrying `sab`, `bodyH`,
    `rootH`, `bodyH-rootH`, `mainBottom`, `tabTop` and `GAP`, plus device model, iOS version and
    `htmlFont` (D-18). Then select and name exactly one of three branches, which is what plan
    21-07 reads before touching `styles.css`:

      - **CONFIRMATION BRANCH** — `bodyH-rootH === sab` and `GAP === sab`, both non-zero. D-15 is
        confirmed; plan 21-07 removes `padding-bottom: env(safe-area-inset-bottom)` from `body`
        and keeps the left/right gutters.
      - **ALREADY-FLUSH BRANCH (D-19)** — `GAP === 0`. The measurement IS the evidence; FOUND-01
        closes with no code change, and plan 21-10's D-12 guard whitelists that one `styles.css`
        line with this measurement quoted as its justification.
      - **FALSIFIED BRANCH** — `GAP` is non-zero but `!== sab`. D-15 is falsified; plan 21-07 must
        not remove the line and falls back to the D-14 open investigation.

    Also record the D-17 keyboard observation as an observation only, and update
    `## Current Test`. Change no production source in this task.
  </action>
  <what-built>
    `?layoutProbe=1` (plan 21-01) prints, as selectable text in a top-anchored overlay: the four
    `env(safe-area-inset-*)` values via a probe element, both standalone-detection signals,
    `window.innerHeight` / `documentElement.clientHeight` / `visualViewport.height`, the
    `offsetHeight` of `html`/`body`/`#root`/`main`, the derived `bodyH-rootH`, `mainBottom`,
    `tabTop`, and `GAP = tabTop - mainBottom`, plus device pixel ratio, root font size,
    orientation and user agent. Nothing has been changed in `styles.css` yet — this is the
    baseline.
  </what-built>
  <how-to-verify>
    1. `npm run build --workspace packages/app`, then serve the built output and expose it over
       the HTTPS cloudflared tunnel. The `--http-host-header localhost` flag is **mandatory** —
       vite preview 403s without it (MEMORY `device-uat-hosting`).
    2. On the iPhone, open the tunnel URL in Safari and **Add to Home Screen**. Launch from the
       home-screen icon — this is the only context in which the bug is visible.
    3. Navigate to `?layoutProbe=1` inside the installed instance.
    4. **Portrait:** screenshot the readout. Record `sab`, `bodyH`, `rootH`, `bodyH-rootH`,
       `mainBottom`, `tabTop`, `GAP`, plus device model, iOS version and `htmlFont`.
    5. **Landscape:** rotate, screenshot, record the same fields (D-11: the phase guarantees the
       arithmetic holds when the inset shrinks; no landscape-specific layout is expected).
    6. Repeat on the **Me** tab (the longest scroller) with the page scrolled to the bottom, to
       confirm nothing already lands under the home indicator.
    7. Opportunistically, while installed: open **SearchSheet** and focus the input so the soft
       keyboard appears. Record whether the FAB or the suggestion strip rides the keyboard and
       whether the search input is covered (D-17 — this seeds UAT test 4; **do not fix anything
       here**).
    8. Select one of the three branches named in `<action>` and record the numbers plan 21-13 will
       compare against for the after-measurement.
  </how-to-verify>
  <verify>
    <human-check>
      `21-HUMAN-UAT.md` test 1's `result:` block contains portrait and landscape measurement
      tables with all seven fields, device model and OS version, and names exactly one of the
      three branches.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `21-HUMAN-UAT.md` test 1's `result:` block contains two labelled measurement tables (portrait, landscape), each with `sab`, `bodyH`, `rootH`, `bodyH-rootH`, `mainBottom`, `tabTop`, `GAP`
    - Device model, iOS version and orientation are recorded per D-18
    - Exactly one of CONFIRMATION BRANCH / ALREADY-FLUSH BRANCH / FALSIFIED BRANCH is named as the selected outcome
    - Screenshots are referenced (path or attachment note) for both orientations
    - The D-17 keyboard observation is recorded as an observation only — no fix was applied
    - `## Current Test` is updated to name the next open item
    - `git diff packages/app/src` is empty for this task
  </acceptance_criteria>
  <done>FOUND-01 has a recorded, orientation-complete baseline on an installed instance, and plan 21-07 has an unambiguous instruction for `styles.css`.</done>
  <resume-signal>Type "approved" with the selected branch name and the portrait/landscape GAP values</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public HTTPS tunnel → local preview server | The device UAT harness briefly exposes a local build over a public cloudflared URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-09 | Information Disclosure | cloudflared tunnel to `vite preview` | accept | Ephemeral, unlisted, random-subdomain tunnel torn down at the end of the session; the served artifact is a public-by-design static PWA whose only secrets are the already-public `VITE_SUPABASE_ANON_KEY` (protected by RLS, unchanged this phase). Same practice as the Phase-10 VALID-02 rehearsal. |
| T-21-10 | Elevation of Privilege | `?layoutProbe=1` / `?layerRepro=1` reachable on the tunnelled build | accept | Both flags are inert-unless-explicit and boolean-only (T-21-01, asserted by `test/layerRepro.test.tsx`); they toggle a dev overlay and render no query-string content. |
| T-21-SC | Tampering | npm installs | mitigate | No packages installed. This plan runs a build and a browser session; `package.json` is not in `files_modified`. |
</threat_model>

<verification>
- `21-HUMAN-UAT.md` tests 1 and 7 have non-`PENDING` results
- One of the three named FOUND-01 branches is selected, with numbers backing it
- The per-surface repro table names the portal targets for plans 21-11 and 21-12
- `git diff packages/app/src` is empty — this plan records evidence, it does not change code
</verification>

<success_criteria>
- Plans 21-07 and 21-11 can proceed with evidence, not hypothesis
- The FOUND-01 baseline is recorded for the plan-21-13 after-comparison
- D-19's escape hatch is a selectable, documented outcome rather than an assumption
</success_criteria>

<artifacts_this_phase_produces>
Symbols, files and flags newly created by Phase 21 (all plans). Nothing below exists on `master`
before this phase.

**CSS custom properties (new):** `--gz-safe-bottom`, `--gz-tab-bar-h`, `--gz-overlay-inset`,
`--gz-chrome-reserve`, `--gz-content-reserve`, `--gz-sheet-pad-bottom`, `--gz-fab-offset`

**New CSS class (new):** `.gesture-guard` (added to the `styles.css` gesture-suppression
selector list for portaled roots, D-23)

**Config keys (new):** `config.ui.bottomSpace`, `config.ui.bottomSpace.TAB_BAR_HEIGHT_REM`,
`config.ui.bottomSpace.FAB_CLEARANCE_PX`, `config.ui.bottomSpace.SHEET_PAD_BOTTOM_PX`,
`config.copy.tabs` (`show`/`explore`/`map`/`dex`/`games`), `config.copy.presence.activity`,
`config.copy.presence.activityUnknown`

**New modules and exports:** `packages/app/src/layout/bottomSpace.ts`
(`BOTTOM_SPACE_VAR_NAMES`, `bottomSpaceVarEntries`, `applyBottomSpaceVars`,
`useBottomSpaceVars`); `packages/app/src/dex/formatDate.ts` (`formatFullDate`, plus the re-homed
`formatMonYear`); `packages/app/src/dev/layerRepro.tsx` (`isLayerReproEnabled`,
`LayerReproToast`); `packages/app/src/dev/LayoutProbe.tsx` (`isLayoutProbeEnabled`,
`LayoutProbe`, `readSafeAreaInsets`); `packages/app/src/dex/FriendRow.tsx`
(`presenceActivityLabel`); `packages/app/src/dex/shareCard.ts` (module-private
`composeFooterLine`)

**New test-local helpers:** `packages/app/test/layerOrder.test.tsx` (`createsStackingContext`,
`ancestorsUpToBody`)

**Changed signatures:** `showBottomFabOffset(stripSlotReserved: boolean)` (was
`stripHasContent`); `FabMenu` / `OrbitStage` prop `stripSlotReserved` (was `stripHasContent`);
`data-strip-slot-reserved` DOM attribute (was `data-strip-has-content`); `PresenceActivitySlot`
gains an `online?: boolean` prop

**URL flags (new):** `?layerRepro=1`, `?layoutProbe=1`

**New test files:** `packages/app/test/layerRepro.test.tsx`,
`packages/app/test/bottomSpace.test.ts`, `packages/app/test/layerOrder.test.tsx`,
`packages/app/test/formatDate.test.ts`, `packages/app/test/presenceLabels.test.ts`,
`packages/app/test/setlistView.test.tsx`

**New docs:** `.planning/phases/21-layout-layering-foundations/21-HUMAN-UAT.md`,
`.planning/todos/pending/2026-07-24-simultaneous-bottom-overlay-stacking.md`

**Removed by this phase:** `packages/app/src/dex/formatMonYear.ts` (renamed to `formatDate.ts`);
the `padding-bottom: env(safe-area-inset-bottom)` declaration on `body` in `styles.css`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/21-layout-layering-foundations/21-04-SUMMARY.md` when done.
</output>
