---
phase: 21-layout-layering-foundations
plan: 13
type: execute
wave: 8
depends_on: ["21-03", "21-05", "21-06", "21-12"]
files_modified:
  - .planning/phases/21-layout-layering-foundations/21-HUMAN-UAT.md
  - packages/app/src/dex/shareCard.ts
autonomous: false
requirements: [FOUND-01, FOUND-02, FOUND-03, FOUND-05, NAV-01, NAV-03]

must_haves:
  truths:
    - "21-HUMAN-UAT.md carries a recorded result for all 8 manual verifications and its status is resolved"
    - "The FOUND-01 after-measurement is recorded in portrait and landscape on the same installed device as the before-measurement, with the same fields"
    - "The five bottom overlays are confirmed clear of the tab-bar buttons, and the three previously self-padded toasts gained no internal dead space"
    - "The share-card PNG at the widest realistic venue name shows the full date, an ellipsized venue and no clipped footer baseline"
    - "Two devices on different builds each show the other a correct readable activity label, in both directions"
  artifacts:
    - path: ".planning/phases/21-layout-layering-foundations/21-HUMAN-UAT.md"
      provides: "the phase's complete device-verification record"
      contains: "status: resolved"
  key_links:
    - from: ".planning/phases/21-layout-layering-foundations/21-HUMAN-UAT.md test 1 result"
      to: "the plan-21-04 before-measurement"
      via: "same device, same fields, before vs after comparison"
      pattern: "GAP"
---

<objective>
Device session #2 — the end-of-phase human-verify gate. Close the six remaining manual
verifications plus the after-half of the FOUND-01 measurement, and mark `21-HUMAN-UAT.md`
resolved.

Purpose: `21-VALIDATION.md` enumerates 8 genuinely un-automatable behaviors, and
`REQUIREMENTS.md` budgets FOUND-01, FOUND-05 and NAV-03 device verification **inside** this
phase, never as a trailing cleanup. `env(safe-area-inset-bottom)` reports `0` in every
headless/jsdom/desktop context, so the whole FOUND-01/FOUND-02 bug class is invisible outside an
installed instance by construction. NAV-03's failure mode is silent, and this project has learned
twice (`260724-hqu`, `260724-lgo`) that a unit-proven realtime path is not a verified one.

Output: `21-HUMAN-UAT.md` with all 8 results filled in, `status: resolved`, and any residual gap
recorded explicitly rather than assumed closed — plus, **only if** UAT test 5 records a clipped
footer baseline, the one-line `height * 0.99` → `height * 0.97` nudge in `dex/shareCard.ts`. That
conditional edit is the only production code this plan can touch, and it is regression-gated by
Task 2's `npm test`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-layout-layering-foundations/21-CONTEXT.md
@.planning/phases/21-layout-layering-foundations/21-UI-SPEC.md
@.planning/phases/21-layout-layering-foundations/21-VALIDATION.md
@.planning/phases/21-layout-layering-foundations/21-HUMAN-UAT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Installed-instance layout pass — UAT tests 1 (after), 2, 3, 4</name>
  <files>.planning/phases/21-layout-layering-foundations/21-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/21-layout-layering-foundations/21-HUMAN-UAT.md — test 1's recorded before-numbers (the comparison baseline), tests 2/3/4's expected blocks, and the `## Harness` tunnel commands with the mandatory `--http-host-header localhost`
    - .planning/phases/21-layout-layering-foundations/21-CONTEXT.md D-04, D-09, D-11, D-17, D-18, D-19
    - The plan-21-07 SUMMARY — which `styles.css` branch was applied and why
    - The plan-21-10 SUMMARY — the recorded direction of the overlay-reserve change (it got smaller by one inset)
  </read_first>
  <action>
    Rebuild, re-serve over the HTTPS tunnel and reinstall to the home screen on the **same device**
    used in plan 21-04, then run the four checks scripted in `<how-to-verify>` and record each
    into `21-HUMAN-UAT.md`:

      - **Test 1 (after):** two labelled tables (portrait, landscape) with the same seven fields
        as the before-run — `sab`, `bodyH`, `rootH`, `bodyH-rootH`, `mainBottom`, `tabTop`, `GAP`
        — plus device model and iOS version (D-18), and an explicit before→after comparison of
        `GAP`. Expected `GAP === 0`.
      - **Test 2:** a per-overlay PASS/FAIL for `BingoCelebration`, `WaveToast`, `UpdateToast`,
        `BackupToast` and `InstallBanner`, and an explicit statement of whether the three formerly
        self-padded toasts show internal dead space (RESEARCH Pitfall 2).
      - **Test 3:** whether all five tab labels fit on one line at maximum Dynamic Type, with a
        screenshot reference (D-04 — this is what `rem` buys).
      - **Test 4:** the soft-keyboard behavior of `SearchSheet`, and whether any fix was required
        (D-17 — fix only if the reserved arithmetic misbehaves).

    Attach or reference screenshots for tests 1 and 3. Update `## Current Test`.
  </action>
  <what-built>
    The bottom-space arithmetic now has one owner: `config.ui.bottomSpace` holds the numbers,
    `layout/bottomSpace.ts` composes the `--gz-*` ladder onto `document.documentElement`, and
    `styles.css :root` declares the single `--gz-safe-bottom: env(safe-area-inset-bottom)`.
    `<main>`, the tab bar, both FABs, three sheet paddings, the RecapView page footer, the
    NodeSheet gutter and all five bottom overlays read `var(...)`. The body-level bottom inset was
    resolved per the branch recorded in plan 21-04. The three previously self-padded toasts had
    that padding deleted in the same edit as their offset change.
  </what-built>
  <how-to-verify>
    Rebuild and re-serve on the **same device** used in plan 21-04:
    `npm run build --workspace packages/app`, serve the built output, expose it over the HTTPS
    cloudflared tunnel with `--http-host-header localhost` (mandatory — MEMORY
    `device-uat-hosting`), reinstall to the home screen, and launch from the icon.

    **Test 1 (after).** Load `?layoutProbe=1`. Record the seven fields in **portrait and
    landscape**, with device model and iOS version. Screenshot both orientations. Also scroll the
    **Me** tab to the very bottom and confirm no content sits under the home indicator.

    **Test 2.** Trigger each bottom overlay on the installed instance and confirm it clears the
    tab-bar buttons with no overlap: `BingoCelebration` (complete a bingo, or use the shipped
    trigger path), `WaveToast` (have a friend send a reaction, or self-send), `UpdateToast`,
    `BackupToast` (run an export) and `InstallBanner` if still reachable. For the three previously
    self-padded ones — `InstallBanner`, `UpdateToast`, `BackupToast` — specifically confirm there
    is **no dead space inside** the toast below its text. Also confirm that with a toast visible
    on a **scrolling** route, no content is covered — the reserve got smaller by one inset in
    plan 21-10, and an under-reserve would cover a control.

    **Test 3.** iOS Settings → Display & Brightness → Text Size to **maximum**. Return to the app
    and confirm all five tab labels (Live · GizzVerse · Map · Me · Games) fit on one line with no
    clipping and no wrap, and that the bar grew with them. Screenshot. Reset the text size
    afterwards.

    **Test 4.** Open SearchSheet on the Live tab and focus the input so the soft keyboard appears.
    Confirm the search input is not covered and that neither the FAB nor the suggestion strip
    rides the keyboard. Compare against the observation recorded during plan 21-04.

    Also confirm **landscape** correctness generally (D-11): the bar stays bottom-anchored
    full-width and the arithmetic holds when the inset shrinks. No landscape-specific layout is
    expected or wanted.
  </how-to-verify>
  <verify>
    <human-check>
      `21-HUMAN-UAT.md` tests 1, 2, 3 and 4 all carry non-`PENDING` results, with the after-GAP
      recorded for both orientations and a per-overlay verdict for all five bottom overlays.
    </human-check>
  </verify>
  <acceptance_criteria>
    - Test 1's `result:` block contains an after-table for portrait and landscape with the same seven fields as the before-table, and an explicit before→after comparison of `GAP`
    - Device model, iOS version and orientation are recorded (D-18)
    - Test 2's result names each of the five overlays with a per-overlay PASS/FAIL, and explicitly states whether the three formerly padded toasts show internal dead space
    - Test 3's result states whether all five labels fit at maximum Dynamic Type, with a screenshot reference
    - Test 4's result records the keyboard behavior and whether any fix was required
    - No result block among tests 1-4 is left as `PENDING`
  </acceptance_criteria>
  <done>FOUND-01 is closed by a before/after comparison in both orientations, and the overlay, Dynamic Type and keyboard checks are recorded with per-item verdicts.</done>
  <resume-signal>Type "approved" with the after-GAP values for both orientations, or describe the failure</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Share card and portaled-sheet gestures — UAT tests 5 and 8</name>
  <files>.planning/phases/21-layout-layering-foundations/21-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/21-layout-layering-foundations/21-HUMAN-UAT.md tests 5 and 8 expected blocks
    - .planning/phases/21-layout-layering-foundations/21-CONTEXT.md D-23, D-36, D-37
    - The plan-21-06 SUMMARY — the `composeFooterLine` behavior and the `width * 0.9` budget
    - The plan-21-11 SUMMARY — the four recorded D-23 audit outcomes for `SearchSheet`
  </read_first>
  <action>
    Run the two checks scripted in `<how-to-verify>` on the installed instance and record each into
    `21-HUMAN-UAT.md`:

      - **Test 5:** name the specific venue used (the widest realistic name in the actual corpus),
        state the full date as rendered, state whether the venue ellipsized, and answer the D-37
        descender question explicitly. Attach or reference the PNG. If the baseline clipped, apply
        the recorded cheap fix — nudge both `centerText` footer draws in `dex/shareCard.ts` from
        `height * 0.99` to `height * 0.97` — re-shoot, and record both the fix and the
        confirmation. **Fix only if it clips.** That nudge is the only production-code edit this
        plan can make, so it is regression-gated: run this task's `<automated>` gate (`npm test`)
        after applying it and record the green result beside the fix. If nothing clips, no edit is
        made and the gate is a cheap confirmation that the tree is still green at the commit under
        test.
      - **Test 8:** answer double-tap zoom, long-press callout, pull-to-refresh, focus-on-open and
        VoiceOver focus **individually** for the portaled `SearchSheet`, and double-tap plus
        long-press for the portaled `FabMenu`. Record any regression with the surface and the
        behavior named, never summarised as "fine".

    Update `## Current Test`.
  </action>
  <what-built>
    Both share-card footers draw the date via `formatFullDate` and compose through
    `composeFooterLine`, which measures the date plus separator at the real 44px footer font and
    ellipsizes only the venue remainder against a `width * 0.9` = 972px budget using the shipped
    `truncateToWidth`. `SearchSheet` (and `FabMenu`) are now portaled to `document.body`, and
    `SearchSheet`'s portaled root carries the `gesture-guard` class so the `styles.css`
    suppression rules (`touch-action: manipulation`, `overscroll-behavior: none`,
    `-webkit-touch-callout: none`, `user-select: none`) still apply.
  </what-built>
  <how-to-verify>
    **Test 5.** On the installed instance, pick the **widest realistic venue name in the actual
    corpus** (search the archive for the longest festival/amphitheatre name) and generate the
    share card for a show at that venue — do both card variants if reachable: the per-show recap
    card and the bingo-trophy card. Save the PNG and inspect at full resolution. Confirm: the date
    reads `Mon D, YYYY` in full and is **never truncated**; the venue is ellipsized with `…` if it
    does not fit; nothing overflows the card edges; and — the D-37 check — the footer baseline at
    `height * 0.99` does not clip descenders (look at the `g` in `Aug` and any `p`/`g`/`y` in the
    venue).

    **Test 8.** On the installed instance, open SearchSheet on the Live tab and confirm the
    gesture behaviors are unchanged from before the portal: **double-tap** inside the sheet does
    not zoom the page; **long-press** on text does not raise the iOS callout/selection menu;
    **drag** inside the sheet does not pull-to-refresh or rubber-band the page behind it. Then
    confirm the sheet still opens with the input focused, closes cleanly, and that VoiceOver reads
    the dialog correctly (swipe into it and confirm focus lands inside, not on the background).
    Repeat the double-tap/long-press check with the **FabMenu** open, since it was portaled too.
  </how-to-verify>
  <verify>
    <automated>npm test</automated>
    <human-check>
      `21-HUMAN-UAT.md` tests 5 and 8 carry non-`PENDING` results, with the venue named, the
      rendered date quoted, the descender question answered, and each gesture behavior answered
      individually per surface.
    </human-check>
  </verify>
  <acceptance_criteria>
    - Test 5's `result:` block names the specific venue used, states the full date as rendered, states whether the venue ellipsized, and answers the D-37 descender question explicitly; a PNG is attached or referenced
    - If the baseline clipped, the result records the applied `height * 0.97` fix and the re-shot confirmation, and `npm test` is re-run green
    - Test 8's result answers double-tap, long-press, pull-to-refresh, focus-on-open and VoiceOver focus individually for `SearchSheet`, and double-tap/long-press for `FabMenu`
    - Any regression found is recorded with the surface and the behavior, not summarised as "fine"
  </acceptance_criteria>
  <done>FOUND-05 is confirmed against real font metrics at the worst realistic case, and the portaled surfaces are confirmed to have kept their venue gesture behavior.</done>
  <resume-signal>Type "approved" with the venue used and the per-behavior gesture results, or describe the regression</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: NAV-03 cross-build presence, both directions — UAT test 6; close the document</name>
  <files>.planning/phases/21-layout-layering-foundations/21-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/21-layout-layering-foundations/21-HUMAN-UAT.md test 6 expected block and the `## Harness` two-build sub-section
    - .planning/phases/21-layout-layering-foundations/21-CONTEXT.md D-39, D-40, D-41, D-42
    - .claude/skills/spike-findings-guezzer/SKILL.md — presence rides Realtime and is never persisted, so there is no stored payload to migrate and no cleanup after the test
    - The plan-21-03 SUMMARY — the chosen render-path fallback (option ii) and the frozen wire vocabulary
  </read_first>
  <action>
    Run the two-device, two-build presence check scripted in `<how-to-verify>` and record test 6's
    `result: |` block with: both directions separately and the **exact label text observed** in
    each; the `At a show 🎸` precedence check; the background→`idle`→foreground recovery check;
    an explicit statement that no blank activity slot was observed for an online friend; and both
    device models plus both build commit SHAs.

    Then close the document: set `## Current Test` to `None — …` (or name any residual gap), set
    frontmatter `status: resolved`, and refresh `updated`. If any test failed or any residual gap
    remains, record it explicitly in `## Current Test` and leave `status: partial` rather than
    marking the phase clean. Also close, or explicitly restate as outstanding, the two-device
    realtime recheck that quick tasks `260724-hqu` and `260724-lgo` left open.
  </action>
  <what-built>
    Tab display labels moved to `config.copy.tabs` and read Live · GizzVerse · Map · Me · Games.
    The `gizz-room` presence wire vocabulary (`LiveGizz`, `GizzVerse`, `GizzMap`, `GizzDex`,
    `GizzGames`, `idle`) is **frozen and unchanged**, and `reduceActivity`'s allow-list is
    byte-identical. A second label voice in `config.copy.presence.activity` renders "on GizzDex"
    rather than the raw token, and an online friend whose activity cannot be resolved renders the
    constant `in the app` instead of a blank slot.
  </what-built>
  <how-to-verify>
    1. Build and serve **two** versions: check out the pre-Phase-21 commit into a second worktree
       and build it, and build the current head. Serve each on its own port with its own
       cloudflared tunnel (`--http-host-header localhost` on both).
    2. Device A loads the **pre-rename** build and signs in as identity 1. Device B loads the
       **post-rename** build and signs in as identity 2.
    3. Move each device between tabs (Live, GizzVerse, Map, Me, Games) and, on each device, read
       the other's row in the friends list.
       - **B looking at A (old build → new build):** expect the correct new-voice label, e.g.
         `on GizzDex` when A is on its "GizzDex"-labelled tab. Never blank, never a raw token.
       - **A looking at B (new build → old build):** expect A's own shipped rendering of the
         token — i.e. the brand token as A's code renders it. Record exactly what appears. This is
         the honest expected result: labels are chosen **receiver-side**, so an old build renders
         whatever its own code says. Because the wire tokens are frozen, nothing breaks.
    4. Put one device at a tracked show and confirm `At a show 🎸` still wins over the tab label,
       in both directions.
    5. Background one device and confirm it goes `idle` on the other, and that foregrounding
       recovers the label (the `visibleEpoch` rejoin from `260724-hqu`/`260724-lgo` — this is the
       still-outstanding two-device recheck those quick tasks flagged).
    6. Confirm neither device ever shows a blank activity slot for an online friend.
  </how-to-verify>
  <verify>
    <human-check>
      `21-HUMAN-UAT.md` test 6 carries a non-`PENDING` result with both directions and exact
      observed labels, and the document frontmatter reads `status: resolved` (or `status: partial`
      with the residual gap named in `## Current Test`).
    </human-check>
  </verify>
  <acceptance_criteria>
    - Test 6's `result:` block records both directions separately, with the exact label text observed in each
    - It records the `At a show 🎸` precedence check and the background→`idle`→foreground recovery check
    - It states explicitly that no blank activity slot was observed for an online friend
    - Both device models and both build identifiers (commit SHAs) are recorded
    - `21-HUMAN-UAT.md` frontmatter is `status: resolved` (or `status: partial` with the residual gap named in `## Current Test`), `updated` is refreshed, and no `result:` block remains `PENDING`
    - The still-open `260724-hqu` / `260724-lgo` two-device realtime recheck is either closed here with its result, or explicitly restated as outstanding
  </acceptance_criteria>
  <done>NAV-03 is verified against real Realtime with a genuinely mixed build vocabulary, in both directions, and the phase's device record is closed.</done>
  <resume-signal>Type "approved" with the observed label in each direction, or describe the failure</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Two live devices on different builds → `gizz-room` Realtime | The one place the mixed-vocabulary input-validation control is exercised for real |
| Public HTTPS tunnels → two local preview servers | Two ephemeral tunnels are open simultaneously for the cross-build test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-38 | Tampering / Spoofing | `reduceActivity` allow-list under real mixed-build traffic | mitigate | This session is the only verification of the phase's one real security control (T-21-06) against live untrusted peer payloads. The test asserts in both directions that no raw token and no blank ever reaches the screen. Unit proof is not accepted as verification — precedent `260724-hqu` / `260724-lgo`. |
| T-21-39 | Information Disclosure | two simultaneous cloudflared tunnels | accept | Ephemeral, unlisted, random-subdomain, torn down at session end; the served artifact is a public-by-design static PWA and the only key present is the RLS-protected `VITE_SUPABASE_ANON_KEY`, unchanged this phase. |
| T-21-40 | Repudiation | an unrecorded or partially-recorded device pass | mitigate | Every acceptance criterion demands a written result with numbers, exact observed strings, device models, OS versions and build SHAs; an incomplete pass must leave `status: partial` with the gap named in `## Current Test` rather than being marked clean. |
| T-21-SC | Tampering | npm installs | mitigate | No packages installed. This plan runs two builds and a device session; `package.json` is not in `files_modified`. |
</threat_model>

<verification>
- `21-HUMAN-UAT.md` has non-`PENDING` results for all 8 tests
- Frontmatter `status` is `resolved`, or `partial` with the residual gap named
- The FOUND-01 after-measurement is directly comparable to the plan-21-04 before-measurement
- `npm test` is green and `npm run build --workspace packages/app` succeeds at the commit under test
</verification>

<success_criteria>
- FOUND-01 is closed by a before/after measurement in both orientations on an installed instance
- FOUND-02's overlay overlap is confirmed fixed with no new internal dead space
- FOUND-05's real-font render is confirmed at the widest realistic venue name
- NAV-01's labels survive maximum Dynamic Type
- NAV-03 is verified across two devices on different builds, in both directions
- FOUND-03's portaled SearchSheet keeps its venue gesture behavior
</success_criteria>

<artifacts_this_phase_produces>
Symbols, files and flags newly created by Phase 21 (all plans). Nothing below exists on `master`
before this phase.

**CSS custom properties (new):** `--gz-safe-bottom`, `--gz-tab-bar-h`, `--gz-overlay-inset`,
`--gz-chrome-reserve`, `--gz-content-reserve`, `--gz-sheet-pad-bottom`, `--gz-fab-offset`

**New CSS class (new):** `.gesture-guard` (added to the `styles.css` gesture-suppression
selector list for portaled roots, D-23)

**Config keys (new):** `config.ui.bottomSpace`, `config.ui.bottomSpace.TAB_BAR_HEIGHT_REM`,
`config.ui.bottomSpace.FAB_CLEARANCE_PX`, `config.ui.bottomSpace.SHEET_PAD_BOTTOM_PX`,
`config.copy.tabs` (`show`/`explore`/`map`/`dex`/`games`), `config.copy.presence.activity`,
`config.copy.presence.activityUnknown`

**New modules and exports:** `packages/app/src/layout/bottomSpace.ts`
(`BOTTOM_SPACE_VAR_NAMES`, `bottomSpaceVarEntries`, `applyBottomSpaceVars`,
`useBottomSpaceVars`); `packages/app/src/dex/formatDate.ts` (`formatFullDate`, plus the re-homed
`formatMonYear`); `packages/app/src/dev/layerRepro.tsx` (`isLayerReproEnabled`,
`LayerReproToast`); `packages/app/src/dev/LayoutProbe.tsx` (`isLayoutProbeEnabled`,
`LayoutProbe`, `readSafeAreaInsets`); `packages/app/src/dex/FriendRow.tsx`
(`presenceActivityLabel`); `packages/app/src/dex/shareCard.ts` (module-private
`composeFooterLine`)

**New test-local helpers:** `packages/app/test/layerOrder.test.tsx` (`createsStackingContext`,
`ancestorsUpToBody`)

**Changed signatures:** `showBottomFabOffset(stripSlotReserved: boolean)` (was
`stripHasContent`); `FabMenu` / `OrbitStage` prop `stripSlotReserved` (was `stripHasContent`);
`data-strip-slot-reserved` DOM attribute (was `data-strip-has-content`); `PresenceActivitySlot`
gains an `online?: boolean` prop

**URL flags (new):** `?layerRepro=1`, `?layoutProbe=1`

**New test files:** `packages/app/test/layerRepro.test.tsx`,
`packages/app/test/bottomSpace.test.ts`, `packages/app/test/layerOrder.test.tsx`,
`packages/app/test/formatDate.test.ts`, `packages/app/test/presenceLabels.test.ts`,
`packages/app/test/setlistView.test.tsx`

**New docs:** `.planning/phases/21-layout-layering-foundations/21-HUMAN-UAT.md`,
`.planning/todos/pending/2026-07-24-simultaneous-bottom-overlay-stacking.md`

**Removed by this phase:** `packages/app/src/dex/formatMonYear.ts` (renamed to `formatDate.ts`);
the `padding-bottom: env(safe-area-inset-bottom)` declaration on `body` in `styles.css`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/21-layout-layering-foundations/21-13-SUMMARY.md` when done.
</output>
