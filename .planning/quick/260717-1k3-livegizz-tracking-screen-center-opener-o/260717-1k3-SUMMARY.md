---
quick_id: 260717-1k3
title: LiveGizz tracking screen — 4 tweaks
status: complete
date: 2026-07-17
commit: 8049e0a
---

# Summary — Quick Task 260717-1k3

Implements + resolves todo
`2026-07-17-livegizz-tracking-screen-opener-orb-search-center-strip-ends`.
All four owner tweaks to the LiveGizz (Show) tracking screen.

## Changed (6 src + 2 test files)

- **`config.ts`** — `centerPrompt` "Tap the opener" → **"Search for the opener"**.
- **`show/CenterNode.tsx`** — pre-opener branch is now a `<button>` (dashed pill,
  ≥44px, `aria-label`) calling a new `onOpenSearch` prop; seeded branch unchanged.
- **`show/OrbitStage.tsx`** — threads `onOpenSearch` to `CenterNode`.
- **`live/SuggestionStrip.tsx`** — new `reserveSpace` prop. Height =
  `(reserveSpace || hasContent) ? 56px : 0`; border/bg chrome only when
  `hasContent`. Pre-opener (not reserved, empty) → 0 height, invisible.
- **`show/FabMenu.tsx`** — new `onEndShow` + `stripReserved` props. **End Show
  appended as the LAST action** (`CircleStop`, `copy.endCta`); bottom offset drops
  the strip term when `!stripReserved`.
- **`show/ShowView.tsx`** — `openerSeeded = session.currentSongId !== null` drives
  `reserveSpace`/`stripReserved`; passes `onOpenSearch`; **removed the header End
  Show button** (header now = SyncDot + tally) + its `CircleStop` import; wires
  `onEndShow={() => setEndOpen(true)}`. `endOpen`/`EndShowDialog`/`endShow`
  unchanged.
- Tests: `fabMenu.test.tsx` (6th action + `stripReserved`), `recapView.test.tsx`
  (open the FAB before tapping End Show).

## How the 4 asks map

1. **Center the opener orb** — comes from Task 2's strip collapse: pre-opener the
   strip is 0, the stage fills, and `CenterNode` (already `absolute inset-0 flex
   items-center justify-center`) sits at true center. Verified: orb `cy` = stage
   center `cy` (462 = 462).
2. **"Search for the opener" + tappable** — copy renamed; tapping opens the Search
   sheet. Verified `SEARCH_OPENED_ON_ORB_TAP = true`.
3. **Kill the blank bar** — empty strip renders 0 height, no border/bg. Verified
   strip-after-stage `height:0px, borderTop:0px, bg:transparent`. SHOW-02 kept:
   once the opener is seeded the 56px is reserved so the fan never re-lays-out
   when a suggestion toggles.
4. **End Show as last FAB item** — verified FAB order
   `[Encore, Set break, Search, ???, Undo, End Show]`; confirm dialog still gates
   the finalize (functionality identical).

## Verification

- `tsc` exit 0; `vitest run` → **481 passed / 66 files**.
- Ran the app (dev + Playwright, pre-opener): centered "Search for the opener"
  pill, no bar, FAB just above tab bar, End Show last in the FAB, no End Show in
  the header; **zero console errors**. Screenshots captured.

## Out of scope

Tier colors, dates, GizzVerse. Routes/storage untouched.
