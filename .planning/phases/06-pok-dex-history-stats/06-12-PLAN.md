---
phase: 06-pok-dex-history-stats
plan: 12
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/app/vite.config.ts
  - packages/app/src/dex/CoverThumb.tsx
  - packages/app/src/dex/AlbumGrid.tsx
  - packages/app/src/dex/AlbumDetail.tsx
  - packages/app/test/dexView.test.tsx
  - packages/app/scripts/fetch-covers.ts
  - packages/app/test/fetchCovers.test.ts
  - packages/app/src/assets/covers/phantom-island.webp
  - packages/app/src/assets/covers/covers-manifest.json
autonomous: true
gap_closure: true
requirements: [DEX-04, DEX-02]

must_haves:
  truths:
    - "In airplane mode after one online load of the built app, album covers render in the dex shelf and album drill-in (no broken-image '?')"
    - "Any cover <img> that fails to load degrades to the existing initials placeholder, never a broken-image icon"
    - "The Phantom Island card shows the 2025 studio-album cover (release-group 716f0986-f131-4e3c-a140-55845bbded3c), not the 2024 Single art"
    - "Full test suite stays green, including the 25 KB per-file / 350 KB total cover budget guards"
  artifacts:
    - path: "packages/app/vite.config.ts"
      provides: "webp in workbox globPatterns so the 29 hashed cover assets are precached"
      contains: "webp"
    - path: "packages/app/src/dex/CoverThumb.tsx"
      provides: "Shared cover-or-initials component with onError fallback"
      min_lines: 30
    - path: "packages/app/scripts/fetch-covers.ts"
      provides: "primary-type Album preference in release-group selection"
      contains: "primary-type"
    - path: "packages/app/src/assets/covers/covers-manifest.json"
      provides: "phantom-island provenance pointing at the Album release group"
      contains: "716f0986-f131-4e3c-a140-55845bbded3c"
  key_links:
    - from: "packages/app/src/dex/AlbumGrid.tsx"
      to: "packages/app/src/dex/CoverThumb.tsx"
      via: "CoverThumb import replaces inline img/placeholder markup"
      pattern: "CoverThumb"
    - from: "packages/app/src/dex/AlbumDetail.tsx"
      to: "packages/app/src/dex/CoverThumb.tsx"
      via: "CoverThumb import replaces inline img/placeholder markup"
      pattern: "CoverThumb"
    - from: "packages/app/vite.config.ts"
      to: "packages/app/dist/sw.js"
      via: "workbox globPatterns → precache manifest includes .webp entries"
      pattern: "webp"
---

<objective>
Close the two diagnosed UAT gaps from 06-HUMAN-UAT.md:

1. **[major, UAT test 2]** Album covers break offline — `webp` is missing from the workbox
   `globPatterns` so the 29 hashed cover assets are never precached, and cover `<img>`
   elements have no error fallback to the existing initials placeholder.
2. **[minor, UAT test 5]** Phantom Island shows the 2024 Single art —
   `findReleaseGroupMbid` in the cover fetch script takes `groups[0].id` with no
   primary-type filter, and MusicBrainz returned the Single first (both score 100).

Purpose: Airplane-mode dex browsing is a core-value behavior ("fully offline once
loaded") and the discography shelf must show correct studio art.
Output: Updated PWA precache config, a shared `CoverThumb` fallback component, an
Album-preferring fetch script with a regression test, and the corrected
phantom-island.webp + manifest entry — with the full 465-test suite (plus new tests)
green, including the 25 KB / 350 KB cover budget guards.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-pok-dex-history-stats/06-HUMAN-UAT.md

# Gap 1 surfaces
@packages/app/vite.config.ts
@packages/app/src/dex/covers.ts
@packages/app/src/dex/AlbumGrid.tsx
@packages/app/src/dex/AlbumDetail.tsx
@packages/app/test/dexView.test.tsx

# Gap 2 surfaces
@packages/app/scripts/fetch-covers.ts
@packages/app/src/assets/covers/covers-manifest.json
@packages/app/test/coversManifest.test.ts
</context>

<interface_contracts>
Existing contracts the executor builds against (read once, do not rediscover):

- `coverUrlFor(slug: string): string | null` (packages/app/src/dex/covers.ts) —
  null means "render initials placeholder". Unchanged by this plan.
- Both AlbumGrid and AlbumDetail currently duplicate `initialsFor(title)` (up-to-2-word
  uppercase initials) and an identical img-or-initials block sized by
  `config.dex.ALBUM_ART_DISPLAY_PX`. The dexView test locates the cover element via
  `data-testid="album-cover"` and asserts dim classes (`opacity-40 grayscale`) on it —
  whichever element renders (img or placeholder div) MUST keep that testid and receive
  the caller's dim classes.
- `runFetchCovers(options, sleep)` (fetch-covers.ts) skips any slug whose `.webp`
  already exists on disk BEFORE making any network request (the `fileExists` check
  precedes `paceNextRequest`). Deleting exactly one `.webp` and running WITHOUT
  `--force` therefore re-fetches only that cover: 2 total HTTP requests (MB search +
  CAA thumbnail). NEVER pass `--force` in this plan (volunteer-API etiquette).
- coversManifest.test.ts asserts: webp↔manifest bijection, ≤25 KB per file,
  ≤350 KB total, manifest slugs ⊆ dex-albums card slugs, coverartarchive.org
  provenance sourceUrl per entry. The re-fetched phantom-island.webp and updated
  manifest entry must keep all five assertions green.
</interface_contracts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Precache webp covers + graceful img→initials fallback (gap 1, UAT test 2)</name>
  <files>packages/app/vite.config.ts, packages/app/src/dex/CoverThumb.tsx, packages/app/src/dex/AlbumGrid.tsx, packages/app/src/dex/AlbumDetail.tsx, packages/app/test/dexView.test.tsx</files>
  <behavior>
    - Test 1 (new, dexView.test.tsx): rendering the shelf with a slug that has a
      committed cover produces an img with data-testid="album-cover"; firing the
      `error` event on that img re-renders the SAME testid as the initials
      placeholder div showing `initialsFor(title)` — no broken img remains.
    - Test 2 (new): the error-fallback placeholder for a zero-catch album still
      carries the dim classes (`opacity-40`, `grayscale`) — fallback preserves the
      §B4 dimming contract.
    - Test 3 (existing, must stay green): the current dimming assertion on
      data-testid="album-cover" in dexView.test.tsx passes unchanged.
  </behavior>
  <action>
    Fixes the UAT gap "airplane mode → covers show '?'" (06-HUMAN-UAT.md gap 1,
    major). Reason: covers are hashed Vite assets (`import.meta.glob` with `?url`)
    emitted to dist/assets/*.webp, but workbox `globPatterns` omits `webp`, so the SW
    never precaches them; offline, the img 404s with no fallback.

    1. **vite.config.ts:** change `globPatterns` to
       `["**/*.{js,css,html,ico,png,svg,woff2,webp}"]`. Update the adjacent NOTE
       comment: covers (29 files, ~195 KB total WebP) are now precached — within the
       documented 350 KB cover budget guarded by test/coversManifest.test.ts; `json`
       is still intentionally excluded (matrix/archive/dex artifacts ride the JS
       bundle). Keep `registerType: "prompt"` untouched (CLAUDE.md: never autoUpdate).

    2. **CoverThumb.tsx (new):** presentational component with props
       `{ slug: string | null; title: string; px: number; dimClass?: string }`.
       Resolves `coverUrlFor(slug)` (null slug → null url). Holds a
       `useState(false)` failed flag; the `<img>` gets
       `onError={() => setFailed(true)}`. When url is null OR failed, render the
       initials placeholder div (move `initialsFor` here — delete the duplicate from
       BOTH AlbumGrid.tsx and AlbumDetail.tsx). Both branches: keep
       `data-testid="album-cover"`, apply `dimClass`, size via inline
       `style={{ width: px, height: px }}` + rounded/border classes exactly matching
       the current markup in AlbumGrid (img: `alt=""`, `loading="lazy"`; placeholder:
       `aria-hidden="true"`). No new config constants — sizing stays sourced from
       `config.dex.ALBUM_ART_DISPLAY_PX` at the call sites (config single-source rule).

    3. **AlbumGrid.tsx / AlbumDetail.tsx:** replace the duplicated
       img-or-initials blocks with `<CoverThumb slug={...} title={...} px={px}
       dimClass={dimClass} />` (AlbumDetail passes no dimClass — drill-in header never
       dims). Presentational contract unchanged: names rendered as React text only.

    4. **dexView.test.tsx:** add the two behavior tests above using
       `fireEvent.error(...)` on the cover img (jsdom). Do not modify existing
       assertions.

    This is UI-package-only work; core/ is untouched (core/UI separation holds).
  </action>
  <verify>
    <automated>npm test -- --run 2>&1 | tail -5  # full suite green (465 existing + new), then: npm run build -w @guezzer/app && grep -c "\.webp" packages/app/dist/sw.js</automated>
  </verify>
  <done>
    `grep -c "\.webp" packages/app/dist/sw.js` returns ≥ 1 (covers listed in the SW
    precache manifest); new fallback tests pass; existing dimming test unchanged and
    green; no remaining `initialsFor` duplication in AlbumGrid/AlbumDetail.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prefer primary-type Album in cover fetch + re-fetch phantom-island only (gap 2, UAT test 5)</name>
  <files>packages/app/scripts/fetch-covers.ts, packages/app/test/fetchCovers.test.ts, packages/app/src/assets/covers/phantom-island.webp, packages/app/src/assets/covers/covers-manifest.json</files>
  <behavior>
    - Test 1 (new, fetchCovers.test.ts): given a stubbed MB search response listing a
      Single first (`primary-type: "Single"`) and an Album second (`primary-type:
      "Album"`), `findReleaseGroupMbid` returns the Album's id.
    - Test 2 (new): given a response with NO Album-typed group (e.g. only an EP),
      it falls back to `groups[0].id` — many committed covers are intentionally
      non-Album release groups (EPs etc.), so the fallback is load-bearing.
    - Test 3 (new): empty `release-groups` still returns null (existing placeholder
      contract preserved).
  </behavior>
  <action>
    Fixes the UAT gap "Phantom Island shows the 2024 Single art" (06-HUMAN-UAT.md
    gap 2, minor). Confirmed diagnosis: covers-manifest.json records mbid
    `e2e5b06e-e0e7-4362-ab3d-353f5f6a4455` (MusicBrainz: Single, 2024-10-29); the
    correct studio release group is `716f0986-f131-4e3c-a140-55845bbded3c` (Album,
    2025-06-13). Root cause: `findReleaseGroupMbid` returns `groups[0].id` unfiltered.

    1. **fetch-covers.ts:** add `"primary-type"?: string` to the `MbReleaseGroup`
       interface. In `findReleaseGroupMbid`, after the empty-check: return the id of
       the FIRST group whose `"primary-type" === "Album"`, falling back to
       `groups[0].id` when none exists (keep the descending-score comment; document
       the Album preference + fallback rationale in the doc comment). Add `export` to
       `findReleaseGroupMbid` so the regression test can import it. No other behavior
       changes — pacing, D-07 no-auto-retry, 25 KB abort guard, and manifest format
       all stay verbatim.

    2. **fetchCovers.test.ts (new):** unit-test `findReleaseGroupMbid` with
       `vi.stubGlobal("fetch", ...)` returning canned `MbSearchResponse` JSON — the
       three behavior cases above. No real network, no sharp invocation (importing
       the module is safe: the `isMain` guard prevents execution and top-level code
       only computes paths). Restore the stub in afterEach (`vi.unstubAllGlobals()`).

    3. **Re-fetch ONLY phantom-island (2 network requests total):** delete
       `packages/app/src/assets/covers/phantom-island.webp`, then run
       `npm run fetch:covers -w @guezzer/app` WITHOUT `--force`. The fileExists skip
       fires before any request for the other 28 covers, so this makes exactly one MB
       search + one CAA thumbnail request — volunteer-API etiquette holds. If the run
       fails (rate limit/outage), diagnose and re-run manually per D-07; do NOT add
       retry logic.

    4. **Confirm the committed artifacts:** covers-manifest.json `phantom-island`
       entry now has `mbid: "716f0986-f131-4e3c-a140-55845bbded3c"` and a matching
       coverartarchive.org sourceUrl, provenance format (title/sourceUrl/mbid/
       fetchedAt) unchanged, key-sorted manifest with trailing newline preserved. The
       new webp must satisfy the existing budget guards (≤25 KB file, ≤350 KB total)
       in test/coversManifest.test.ts. If MusicBrainz somehow still resolves a
       non-Album group for this title, STOP and report — do not hand-edit the
       manifest to fake provenance.
  </action>
  <verify>
    <automated>grep -c "716f0986-f131-4e3c-a140-55845bbded3c" packages/app/src/assets/covers/covers-manifest.json && npm test -- --run 2>&1 | tail -5</automated>
  </verify>
  <done>
    Manifest phantom-island entry points at the Album release group
    716f0986-f131-4e3c-a140-55845bbded3c; new findReleaseGroupMbid regression tests
    pass; coversManifest budget/provenance guards pass against the re-fetched webp;
    full suite green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build script → musicbrainz.org / coverartarchive.org | untrusted external JSON + image bytes enter at build time (never at runtime) |
| SW precache → app runtime | precached assets served offline to the installed PWA |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-12-01 | Tampering | fetch-covers.ts external image bytes | mitigate | existing T-06-09 control preserved: fetched bytes are always re-encoded through sharp (resize + WebP), never committed verbatim; 25 KB abort guard unchanged |
| T-06-12-02 | Spoofing | MB search returning a wrong release group | mitigate | provenance manifest (mbid + sourceUrl) makes `git diff` the review mechanism; verify gate greps the exact expected MBID |
| T-06-12-03 | DoS | volunteer APIs (MB/CAA) | mitigate | single-cover re-fetch without `--force` = 2 requests total; existing 2 s sequential pacing; D-07 no auto-retry |
| T-06-12-SC | Tampering | npm/pip/cargo installs | accept | no new packages installed in this plan (CoverThumb uses existing React; tests use existing vitest) |
</threat_model>

<verification>
1. `npm test -- --run` — full suite green (465 existing tests + new fallback and
   release-group tests), including coversManifest budget guards against the
   re-fetched phantom-island.webp.
2. `npm run build -w @guezzer/app` succeeds; `grep -c "\.webp" packages/app/dist/sw.js`
   ≥ 1 — the 29 cover assets appear in the SW precache manifest.
3. `grep -c "716f0986-f131-4e3c-a140-55845bbded3c" packages/app/src/assets/covers/covers-manifest.json`
   returns 2 (mbid field + sourceUrl) or ≥ 1.
4. Re-UAT on device (next UAT round, not this plan): airplane-mode dex browse shows
   covers; Phantom Island card shows the 2025 studio art.
</verification>

<success_criteria>
- Both 06-HUMAN-UAT.md gaps have closing artifacts: webp precache + onError fallback
  (gap 1), Album-preferring fetch + corrected phantom-island asset/manifest (gap 2)
- Zero regressions: existing dimming/placeholder/budget assertions untouched and green
- No `--force` re-fetch of the other 28 covers; exactly 2 external requests made
- Core/UI separation and config single-source rules intact (no core changes, no new
  magic numbers)
</success_criteria>

<output>
Create `.planning/phases/06-pok-dex-history-stats/06-12-SUMMARY.md` when done.
</output>
