---
phase: 08-on-device-ui-polish-accessibility
plan: 04
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - packages/app/src/explore/useVisibleViewportHeight.ts
  - packages/app/src/explore/NodeSheet.tsx
  - packages/app/src/explore/ExploreFilterFab.tsx
  - packages/app/src/explore/ExploreView.tsx
  - packages/app/src/explore/ConstellationCanvas.tsx
  - packages/app/test/explore/filterFabLift.test.tsx
autonomous: true
requirements: [A11Y-02, A11Y-03]
must_haves:
  truths:
    - "While a constellation node is focused, the Explore FilterFab lifts above the NodeSheet's top edge and is fully visible/tappable (no occlusion), returning to its resting position when the sheet closes"
    - "The lifted FilterFab renders above the NodeSheet (z.focusedFab); resting it sits at z.fab, below the sheet"
    - "NodeSheet gets Escape-to-dismiss + focus-restore but stays non-modal (no scrim, no focus-trap, no inert) — the graph and FilterFab remain reachable"
    - "NodeSheet peek, the FAB lift, and the camera reframe all read ONE shared visible-viewport height source (not window.innerHeight)"
    - "Resizing the viewport with a node focused keeps the camera framed on that node (no snap-off)"
    - "The FAB lift transition is instant under prefers-reduced-motion"
  artifacts:
    - path: "packages/app/src/explore/useVisibleViewportHeight.ts"
      provides: "Shared visible-viewport height hook (visualViewport?.height ?? innerHeight)"
      exports: ["useVisibleViewportHeight"]
    - path: "packages/app/src/explore/ExploreFilterFab.tsx"
      provides: "FilterFab with a `lifted` prop that translateY's above the NodeSheet"
      contains: "lifted"
  key_links:
    - from: "packages/app/src/explore/ExploreView.tsx"
      to: "packages/app/src/explore/ExploreFilterFab.tsx"
      via: "lifted={focusId != null}"
      pattern: "lifted"
    - from: "packages/app/src/explore/ExploreFilterFab.tsx"
      to: "config.ui.z.focusedFab"
      via: "zIndex when lifted"
      pattern: "focusedFab"
---

<objective>
Deliver the Explore-mode accessibility behaviors: (A11Y-02) lift the FilterFab above the NodeSheet's top edge while a node is focused so both stay usable with no occlusion (D-03); give NodeSheet Escape + focus-restore while keeping it non-modal (D-02); standardize NodeSheet peek, the FAB lift, and the camera reframe on ONE shared visible-viewport height source (fixing the latent `window.innerHeight` mismatch at `NodeSheet.tsx:82`); and (A11Y-03) verify + harden the resize-keeps-camera-framed behavior.

Purpose: A constellation-focused user can reach both the sheet and the filter control, and the camera never snaps off the focused node on orientation change / keyboard show-hide / address-bar collapse.
Output: A shared viewport hook, a lifted FilterFab, NodeSheet a11y + viewport fix, a hardened reframe, and automated coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-on-device-ui-polish-accessibility/08-RESEARCH.md
@.planning/phases/08-on-device-ui-polish-accessibility/08-PATTERNS.md
@.planning/phases/08-on-device-ui-polish-accessibility/08-UI-SPEC.md
@.planning/phases/07-explore-mode-constellation/07-CONTEXT.md
@.planning/phases/08-on-device-ui-polish-accessibility/08-01-PLAN.md
</context>

<artifacts_produced>
## Artifacts this phase produces (created/changed by THIS plan)

- `useVisibleViewportHeight()` — shared hook returning `window.visualViewport?.height ?? window.innerHeight`, updating on `resize` + `visualViewport` `resize`/`scroll`.
- `ExploreFilterFab` gains a `lifted` prop: `translateY(-liftPx)`, `zIndex: lifted ? z.focusedFab : z.fab`, reduced-motion-gated transition.
- `NodeSheet` consumes `useDialogDismiss` + focus-restore (D-02, non-modal), reads the shared viewport hook for peek, and its `z-30` becomes `z.sheet`.
- `ExploreView` passes `lifted={focusId != null}` to the FilterFab.
- `packages/app/test/explore/filterFabLift.test.tsx`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Shared visible-viewport hook + NodeSheet a11y and peek fix (D-02, Pitfall 3)</name>
  <read_first>
    - packages/app/src/explore/NodeSheet.tsx (peek from `window.innerHeight` at ~:81-88; the reduced-motion helper at ~:62-67; `aria-modal={false}` at ~:133; `z-30` at ~:135; SNAP drag shell at ~:104-159 — KEEP bespoke, do NOT route through <Sheet>)
    - packages/app/src/components/a11y/useDialogDismiss.ts (from plan 01 — Escape only)
    - packages/app/src/components/a11y/useFocusTrap.ts (plan 01 — reuse ONLY the focus-restore idea; NodeSheet must NOT trap/inert)
    - .planning/phases/08-on-device-ui-polish-accessibility/08-RESEARCH.md §"Pitfall 3" + §"Don't Hand-Roll" (visualViewport) + §"Open Questions" Q3
    - .planning/phases/08-on-device-ui-polish-accessibility/08-PATTERNS.md §"explore/NodeSheet.tsx"
  </read_first>
  <action>
    Create `explore/useVisibleViewportHeight.ts` exporting `useVisibleViewportHeight()` — returns `window.visualViewport?.height ?? window.innerHeight`, subscribing to `window` `resize` and `window.visualViewport` `resize`+`scroll` (feature-detected, cleaned up on unmount; the local listener idiom mirrors `NodeSheet.tsx:84-88`).
    In `NodeSheet.tsx`: (a) replace the `window.innerHeight` peek computation with `useVisibleViewportHeight()`; (b) add `useDialogDismiss(true, onClose)` for Escape (rely on the shared LIFO — do NOT add a bespoke keydown listener); (c) add focus-restore ONLY (capture `document.activeElement` on mount, restore in cleanup) WITHOUT any focus-trap, inert, or scrim (D-02 — the graph + FilterFab must stay reachable); (d) migrate the `z-30` to `style={{ zIndex: config.ui.z.sheet }}`; keep `aria-modal={false}` and the bespoke drag/peek shell intact.
  </action>
  <verify>
    <automated>pnpm vitest run packages/app/test/explore/filterFabLift.test.tsx</automated>
    <automated>pnpm --filter @guezzer/app exec tsc --noEmit</automated>
    <human-check>End-of-phase device sweep: with a node focused, Escape dismisses NodeSheet and the graph stays interactive above it (non-modal, no scrim).</human-check>
  </verify>
  <acceptance_criteria>
    - `useVisibleViewportHeight.ts` exists and reads `window.visualViewport?.height ?? window.innerHeight`.
    - `NodeSheet.tsx` no longer references `window.innerHeight` for peek; renders `style={{ zIndex: config.ui.z.sheet }}` (no raw `z-[0-9]`); keeps `aria-modal={false}`; adds NO focus-trap/inert/scrim; has NO bespoke Escape `document` listener.
    - App `tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>NodeSheet is Escape-dismissible and focus-restoring while staying a live, non-modal window on the graph, sized from the shared visible-viewport source.</done>
</task>

<task type="auto">
  <name>Task 2: Lift the FilterFab above the NodeSheet when a node is focused (A11Y-02 / D-03)</name>
  <read_first>
    - packages/app/src/explore/ExploreFilterFab.tsx (fixed wrapper at ~:56 with `z-30`; resting bottom offset `calc(env(safe-area-inset-bottom) + 64px + 8px)` at ~:50; the `motion-safe:` transition idiom at ~:81-85)
    - packages/app/src/explore/ExploreView.tsx (focus state `focusId` at ~:41; `handleFocus` at ~:175 — where `lifted` is driven from)
    - packages/app/src/explore/useVisibleViewportHeight.ts (Task 1)
    - packages/app/src/config.ts (`config.ui.z.fab`/`focusedFab`, `config.ui.FAB_SHEET_GAP_PX`; `config.explore.SHEET_PEEK_FRACTION`, `config.explore.FOCUS_CAMERA_DURATION_MS`)
    - .planning/phases/08-on-device-ui-polish-accessibility/08-RESEARCH.md §"Pattern 5: FAB lift above the sheet" + §"Pitfall 6"
    - packages/app/src/explore/ConstellationCanvas.tsx (reduced-motion gate at ~:323-326 to mirror)
  </read_first>
  <action>
    Add a `lifted: boolean` prop to `ExploreFilterFab`. Compute `peekHeightPx = visibleViewportHeight * config.explore.SHEET_PEEK_FRACTION` from `useVisibleViewportHeight()`; compute `liftPx = lifted ? Math.max(0, peekHeightPx - RESTING_BOTTOM_PX + config.ui.FAB_SHEET_GAP_PX) : 0`, where `RESTING_BOTTOM_PX` is derived from the existing resting bottom offset. Apply `transform: translateY(${-liftPx}px)`; set `zIndex: lifted ? config.ui.z.focusedFab : config.ui.z.fab` (replacing the raw `z-30`); gate the transition: `transition: reduced ? "none" : \`transform ${config.explore.FOCUS_CAMERA_DURATION_MS}ms\`` using the same `matchMedia("(prefers-reduced-motion: reduce)")` read as ConstellationCanvas:323 (Pitfall 6). In `ExploreView.tsx`, pass `lifted={focusId != null}` to the FilterFab. Preserve the 44px tap floor and the filter-panel-collapse behavior on focus.
  </action>
  <verify>
    <automated>pnpm vitest run packages/app/test/explore/filterFabLift.test.tsx</automated>
    <automated>pnpm --filter @guezzer/app exec tsc --noEmit</automated>
    <human-check>End-of-phase device sweep (A11Y-02): focus a node on a real phone — the FilterFab rests ~12px above the NodeSheet top edge, fully visible and tappable, no overlap with sheet content; it returns to bottom-right when the sheet closes.</human-check>
  </verify>
  <acceptance_criteria>
    - `ExploreFilterFab.tsx` renders `zIndex: lifted ? config.ui.z.focusedFab : config.ui.z.fab` — no raw `z-30`/`z-[0-9]` remains.
    - `filterFabLift.test.tsx` asserts: focused → FAB has a non-zero negative `translateY` AND `zIndex === config.ui.z.focusedFab`; unfocused → `translateY(0)` AND `zIndex === config.ui.z.fab`; reduced-motion → `transition: none`.
    - `ExploreView.tsx` passes `lifted={focusId != null}`.
    - App `tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The FilterFab lifts above the focused-node NodeSheet (z.focusedFab) and returns on close, honoring reduced-motion — A11Y-02 occlusion resolved.</done>
</task>

<task type="auto">
  <name>Task 3: Harden the resize reframe on the shared viewport source (A11Y-03)</name>
  <read_first>
    - packages/app/src/explore/ConstellationCanvas.tsx (ResizeObserver → `size` at ~:192; the focus-camera effect at ~:315-334 that already lists `size.height` in deps and re-fires `fg.zoom()`+`fg.centerAt()`)
    - packages/app/src/explore/useVisibleViewportHeight.ts (Task 1)
    - .planning/phases/08-on-device-ui-polish-accessibility/08-RESEARCH.md §"Resize Reframe" + §Claude's Discretion (A11Y-03 = verify + fix edge cases, not from-scratch)
  </read_first>
  <action>
    Verify the focus-camera effect re-frames the focused node when the visible viewport changes. Ensure the effect's height dependency reflects the SAME shared visible-viewport source used by NodeSheet/FAB (so keyboard show/hide and address-bar collapse — not just element resize — retrigger the reframe). If the container ResizeObserver already tracks the visible box, confirm and document that; only add a `visualViewport` resize subscription if a keyboard/orientation edge case is not already covered. Do not change the settle-and-freeze / frozen fx/fy behavior (EXPL-06). Add an automated assertion that a change in the shared viewport height with a node focused re-invokes `fg.zoom`/`fg.centerAt`.
  </action>
  <verify>
    <automated>pnpm vitest run packages/app/test/explore/filterFabLift.test.tsx</automated>
    <automated>pnpm vitest run packages/app</automated>
    <human-check>End-of-phase device sweep (A11Y-03): focus a node, then rotate the phone and toggle the on-screen keyboard — the camera stays framed on that node (no snap-off).</human-check>
  </verify>
  <acceptance_criteria>
    - An automated test asserts `fg.zoom`/`fg.centerAt` re-fire when the shared viewport height changes with a node focused.
    - The settle-and-freeze frozen `fx`/`fy` behavior (EXPL-06) is unchanged (no d3 reheat introduced).
    - Full app suite `pnpm vitest run packages/app` green.
  </acceptance_criteria>
  <done>Viewport resize with a focused node keeps the camera framed, driven by the same shared visible-viewport source as the sheet and FAB.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kglw-derived data → NodeSheet chrome | Focused song name renders as React text only |
| keyboard/AT user → non-modal NodeSheet | Escape must dismiss; focus must not be trapped (graph stays reachable) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-10 | Denial of Service (a11y) | NodeSheet focus | mitigate | NodeSheet is deliberately non-modal — no trap/inert — so focus always reaches the graph + FilterFab; Escape + focus-restore prevent stranding (D-02). |
| T-08-11 | Tampering/Elevation | focused song name in NodeSheet | mitigate | Rendered as React text; no `dangerouslySetInnerHTML`. |
| T-08-12 | Error Handling (V7) | NodeSheet honest-zero state | mitigate | The honest-zero render branch (~:161-170) is preserved; migration adds hooks only, no guard removed. |
| T-08-SC | Tampering (supply chain) | npm installs | accept | No new packages; hooks + transforms only. |
</threat_model>

<verification>
- `pnpm vitest run packages/app` green including `explore/filterFabLift.test.tsx`.
- Grep: no `z-[0-9]` literal remains in `NodeSheet.tsx` or `ExploreFilterFab.tsx`; `window.innerHeight` no longer sources NodeSheet peek.
</verification>

<success_criteria>
- FilterFab lifts above the focused-node NodeSheet with no occlusion and returns on close (A11Y-02).
- Resize keeps the camera framed on the focused node (A11Y-03).
- NodeSheet is Escape-dismissible + focus-restoring while staying non-modal; all three consumers share one visible-viewport source.
</success_criteria>

<output>
Create `.planning/phases/08-on-device-ui-polish-accessibility/08-04-SUMMARY.md` when done
</output>
