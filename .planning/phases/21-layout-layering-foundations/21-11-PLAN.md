---
phase: 21-layout-layering-foundations
plan: 11
type: execute
wave: 6
depends_on: ["21-04", "21-10"]
files_modified:
  - packages/app/test/layerOrder.test.tsx
  - packages/app/src/show/SearchSheet.tsx
  - packages/app/src/show/FabMenu.tsx
  - packages/app/src/styles.css
autonomous: true
requirements: [FOUND-03]

must_haves:
  truths:
    - "A toast can no longer paint over the open SearchSheet or over the FabMenu's scrim and action rows"
    - "An automated test fails if any ancestor of an open modal sheet, up to document.body, creates a stacking context"
    - "The deliberate non-modal NodeSheet and the focusedFab: 60 lift survive as named, commented exemptions"
    - "page < sheetScrim and fabScrim < fab are locked by named assertions rather than by comments"
    - "The portaled SearchSheet still suppresses double-tap zoom and the iOS long-press callout"
  artifacts:
    - path: "packages/app/test/layerOrder.test.tsx"
      provides: "the FOUND-03 structural invariant + the WR-01/CR-01 numeric guards"
      exports: ["createsStackingContext", "ancestorsUpToBody"]
    - path: "packages/app/src/show/SearchSheet.tsx"
      provides: "the portaled one-thumb search surface with its gesture suppression re-applied"
      contains: "createPortal"
  key_links:
    - from: "packages/app/src/show/SearchSheet.tsx"
      to: "document.body"
      via: "createPortal escaping the ShowView content:10 stacking context"
      pattern: "createPortal\\(.*document\\.body"
    - from: "packages/app/src/show/FabMenu.tsx"
      to: "document.body"
      via: "createPortal on both the scrim and the speed-dial container"
      pattern: "createPortal\\(.*document\\.body"
---

<objective>
Write the structural layer-ordering invariant FOUND-03 asks for, then fix the two surfaces the
plan-21-04 repro proved defective: `SearchSheet` and `FabMenu`. Both escape the `ShowView`
`content: 10` stacking context by portaling to `document.body` — no tier is renumbered.

Purpose: `ShowView.tsx:173-183` wraps the show column in `position: relative` + `zIndex:
config.ui.z.content`, which creates a stacking context by spec. Everything inside paints at
effective level 10, losing to the `toast: 20` siblings of `AppShell` — so a `sheet: 50` loses to
a `20` at any number, which is exactly why the standing constraint is "write the invariant test,
renumber nothing". `FabMenu` is the worse symptom (D-27): its `fabScrim: 25` scrim and `fab: 30`
rows are also at effective level 10, so a toast can eat speed-dial taps **mid-show**, in the
live-logging loop.

Output: `packages/app/test/layerOrder.test.tsx` with the ancestor-walk invariant and the two
named numeric guards, plus the two live portal fixes with their lost ancestor CSS re-applied.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-layout-layering-foundations/21-CONTEXT.md
@.planning/phases/21-layout-layering-foundations/21-RESEARCH.md
@.planning/phases/21-layout-layering-foundations/21-UI-SPEC.md
@.planning/phases/21-layout-layering-foundations/21-PATTERNS.md
@.planning/phases/21-layout-layering-foundations/21-HUMAN-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: layerOrder.test.tsx — the structural invariant and the two named numeric guards (D-24, D-25, D-26)</name>
  <files>packages/app/test/layerOrder.test.tsx</files>
  <read_first>
    - .planning/phases/21-layout-layering-foundations/21-HUMAN-UAT.md §"### 7." `result:` — the recorded per-surface repro outcome; the invariant must fail for exactly the surfaces it names
    - packages/app/test/sheet.a11y.test.tsx (lines 1-45 and 120-200) — the top-level `await import(...)` idiom, the `mountAppContent()` fixture, `afterEach(cleanup)` plus manual DOM teardown, the raw-DOM query style (`screen.getByRole("dialog")`, `dialog.getAttribute("aria-modal")`, `dialog.className`), and the jsdom-limits disclaimer comment
    - packages/app/src/show/ShowView.tsx lines 172-185 — `withBackground`: the outer `relative flex h-full …` div and the inner `relative …` div with `style={{ zIndex: config.ui.z.content }}` — the mixed idiom (position from a Tailwind class, `zIndex` from inline style) the walk must detect
    - packages/app/src/config.ts §`ui.z` (lines ~240-297) — the tier values and the two prose invariants at ~265 ("WR-01 regression guard: page < sheetScrim") and ~284 ("CR-01 regression guard: fabScrim < fab"), plus the `focusedFab` D-03 exception comment
    - packages/app/src/explore/NodeSheet.tsx line ~147 — `aria-modal={false}`, the attribute that drives the modal/non-modal split
    - packages/app/src/dev/OrbFitHarness.tsx line ~147 — the repo's single Tailwind `z-10` class, dev-only, which the test's completeness argument must exclude by name
  </read_first>
  <action>
    Create `packages/app/test/layerOrder.test.tsx`.

    **File doc comment** must state FOUND-03, D-24/D-25/D-26, and the completeness argument
    verbatim in substance: an inline-style-only stacking-context detector is a *complete* detector
    **in this codebase specifically**, because all production z-index goes through
    `style={{ zIndex: config.ui.z.X }}` and the only Tailwind `z-*` utility in the repo is
    `dev/OrbFitHarness.tsx:147` (dev-only route). jsdom does not cascade the Tailwind stylesheet,
    so class-derived computed styles are invisible there — but there are none to miss. **The
    moment someone adds a `z-*` class, this test silently loses coverage.** Say so.

    Export two helpers so they can be reused by plan 21-12:

      `createsStackingContext(el: HTMLElement): boolean` — returns true when any of:
        - `el.style.zIndex` is set and not `"auto"` **and** the element is positioned, where
          positioned means `el.style.position` is one of `relative`/`absolute`/`fixed`/`sticky`
          **or** `el.className` contains one of `relative`/`absolute`/`fixed`/`sticky` (the mixed
          idiom `ShowView.tsx:177-179` uses)
        - `el.style.transform` is set and not `"none"`
        - `el.style.filter` is set and not `"none"`
        - `el.style.backdropFilter` is set and not `"none"`
        - `el.style.willChange` contains `transform` or `opacity`
        - `el.style.opacity` is set and numerically `< 1`
        - `el.style.isolation === "isolate"`

      `ancestorsUpToBody(el: HTMLElement): HTMLElement[]` — walks `parentElement` until (and
      excluding) `document.body`.

    **The invariant.** For each modal sheet-tier surface, render it **open, inside a realistic
    tree** — for `SearchSheet` and `FabMenu` that means inside the `withBackground` wrapper shape
    from `ShowView.tsx:173-183` (reproduce the two nested divs with the same classes and the same
    inline `zIndex: config.ui.z.content`), because rendering in isolation makes the walk vacuous.
    Then find the surface root (`screen.getByRole("dialog")` for sheets; the
    `data-strip-slot-reserved` container and the `fab-scrim` testid for `FabMenu`) and assert
    `ancestorsUpToBody(root).every((el) => !createsStackingContext(el))`. On failure the message
    must name the offending ancestor's `className` and its inline `zIndex`.

    **Drive the modal/non-modal split off `aria-modal`**, not a hand-kept list — the DOM already
    carries the distinction and it cannot drift. Add the two exemptions **by name, with comments**
    (D-26), copying the reasons:
      - `NodeSheet` — `aria-modal={false}` (`NodeSheet.tsx:147`). Non-modal peek; FOUND-03 says
        "open **modal** sheet". Its coexistence with a lifted FAB is the point.
      - `focusedFab: 60 > sheet: 50` — deliberate (D-03): the FilterFab lifts ABOVE the non-modal
        NodeSheet so a keyboard/VoiceOver user can still reach the filter while a node is focused.
        Renumbering it would be an a11y regression.

    **The two named numeric guards (D-25)**, each preceded by the corresponding `config.ts`
    comment sentence quoted verbatim:
      - `expect(config.ui.z.page).toBeLessThan(config.ui.z.sheetScrim);` // WR-01
      - `expect(config.ui.z.fabScrim).toBeLessThan(config.ui.z.fab);` // CR-01
    Do **not** pin the full ladder — a full pin would fight D-26's deliberate `focusedFab`
    inversion.

    **Self-test the detector** so it cannot rot: assert `createsStackingContext` returns `true`
    for a div with `className="relative"` + inline `zIndex: 10` (the exact `ShowView` idiom),
    `true` for a div with inline `transform: "scale(1.2)"`, and `false` for a plain
    `className="relative"` div with no inline `zIndex`.

    Expect this file to be **RED for `SearchSheet` and `FabMenu`** until Tasks 2 and 3 land —
    that is the point (RESEARCH Pitfall 4: a test that is green before the portal fix cannot
    satisfy FOUND-03). Note in the doc comment that the *portal* source-scan assertion covering
    the four already-root-level surfaces lands in plan 21-12.
  </action>
  <verify>
    <automated>cd packages/app &amp;&amp; npx vitest run --project @guezzer/app test/layerOrder.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `packages/app/test/layerOrder.test.tsx` exports `createsStackingContext` and `ancestorsUpToBody`
    - The file contains the two exemption comments naming `NodeSheet` (`aria-modal`) and `focusedFab: 60` (D-03)
    - It contains both numeric guards with the `config.ts` sentences quoted above them
    - The three detector self-tests exist and pass
    - Before Tasks 2 and 3, running the file shows failures for `SearchSheet` and `FabMenu` and passes for the other surfaces — record that RED output in the plan SUMMARY
    - `grep -c 'OrbFitHarness' packages/app/test/layerOrder.test.tsx` returns ≥ 1 (the completeness caveat names it)
  </acceptance_criteria>
  <done>The invariant is structural, demonstrably RED against the live defect, and the two real numeric regressions are locked by name.</done>
</task>

<task type="auto">
  <name>Task 2: Portal SearchSheet and re-apply what it loses (D-21, D-22, D-23)</name>
  <files>packages/app/src/show/SearchSheet.tsx, packages/app/src/styles.css</files>
  <read_first>
    - packages/app/src/show/SearchSheet.tsx (whole file) — the current return: `<div role="dialog" aria-modal="true" aria-label={copy.searchPlaceholder} className="fixed inset-0 flex flex-col bg-surface" style={{ zIndex: config.ui.z.sheet }}>`, the `autoFocus` input, the `paddingTop: "calc(env(safe-area-inset-top) + 12px)"` header (top insets are out of scope for the bottom guard), the reset/select/unknown handlers, and any Escape or focus handling
    - packages/app/src/components/Sheet.tsx lines 60-115 — the target pattern: `if (!open) return null; if (typeof document === "undefined") return null;` then `createPortal(<div …>, document.body)` as the **return** rather than a wrapper
    - packages/app/src/styles.css lines 21-37 — the class-scoped gesture suppression on `.orbit-stage, .action-bar, .fab-menu`: `touch-action: manipulation`, `overscroll-behavior: none`, `user-select`/`-webkit-user-select: none`, `-webkit-touch-callout: none`
    - packages/app/src/App.tsx lines 85-95 — the `#app-content` `display: contents` inert wrapper and the comment noting that portaled sheets land OUTSIDE it
    - .planning/phases/21-layout-layering-foundations/21-HUMAN-UAT.md §"### 7." — the recorded repro result confirming SearchSheet is captured
  </read_first>
  <action>
    Wrap `SearchSheet`'s returned tree in `createPortal(…, document.body)`, imported from
    `react-dom`. Copy `Sheet.tsx`'s shape exactly: add the `if (typeof document === "undefined")
    return null;` SSR/jsdom guard immediately before the return, and make `createPortal` the
    **return value**, not an extra wrapper element. Keep the surface **hand-rolled** — do **not**
    migrate it onto the shared `<Sheet>` primitive and do **not** adopt `Sheet`'s focus-trap hooks
    (D-22: this deliberately keeps Phase 22's sheet-animation blast radius at 11 surfaces, not 16).
    Keep `role="dialog"`, `aria-modal="true"`, `aria-label`, `className="fixed inset-0 flex
    flex-col bg-surface"`, the `zIndex: config.ui.z.sheet` inline style, the `autoFocus` input and
    every handler byte-identical.

    **D-23 — audit and re-apply what the portaled node loses.** Perform and record each of these:

    1. **Gesture suppression.** Determine whether `SearchSheet` currently renders inside an
       element carrying `.orbit-stage`, `.action-bar` or `.fab-menu`. Either way, add a
       `gesture-guard` class to the portaled root and extend the selector list in `styles.css`
       from `.orbit-stage, .action-bar, .fab-menu` to
       `.orbit-stage, .action-bar, .fab-menu, .gesture-guard`, with a comment citing D-23 and
       SHOW-13: this is *the* one-thumb, in-the-dark surface, and losing double-tap-zoom or
       long-press-callout suppression there is a real venue regression. Record in the SUMMARY
       whether the surface previously had those ancestors — if it did not, the class is a
       deliberate improvement, and say so rather than implying a regression was averted.
    2. **Escape handling.** Record whether the surface handles Escape today and whether that
       handling depends on DOM tree position (a `keydown` listener on `document` or `window` is
       position-independent; one on a React ancestor is not). If it becomes unreachable, restore
       it on the portaled root. Do not introduce `useDialogDismiss` — that is `Sheet`'s
       machinery and D-22 keeps this surface hand-rolled.
    3. **Focus.** The `autoFocus` input must still receive focus on open, and focus must still
       return sensibly on close. Record the observed behavior in the jsdom test.
    4. **Inert.** `App.tsx` wraps the app in `#app-content` (`display: contents`) and
       `Sheet.tsx`'s focus trap sets `inert` on it. After portaling, `SearchSheet` is outside that
       wrapper. Record what changes: previously the sheet would have gone inert alongside the
       background if a `<Sheet>` were open; now it would not. Note whether that combination is
       reachable. **Make no behavior change here** — this is an audit item, and adding inert
       management would be adopting `Sheet`'s machinery.

    Add the corresponding assertions to `packages/app/test/layerOrder.test.tsx` in the same
    commit: the portaled root is a child of `document.body` (not of the `withBackground` fixture),
    its `className` contains `gesture-guard`, and `aria-modal` is still `"true"`.
  </action>
  <verify>
    <automated>cd packages/app &amp;&amp; npx vitest run --project @guezzer/app test/layerOrder.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `packages/app/src/show/SearchSheet.tsx` imports `createPortal` from `react-dom` and returns `createPortal(..., document.body)`
    - It contains `if (typeof document === "undefined") return null;` before the return
    - Its root `className` contains `gesture-guard`, and `styles.css`'s gesture-suppression selector list contains `.gesture-guard`
    - `grep -c 'from "../components/Sheet' packages/app/src/show/SearchSheet.tsx` returns 0 — the surface stays hand-rolled
    - `role`, `aria-modal`, `aria-label`, the `zIndex` expression and `autoFocus` are unchanged (`git diff` shows only the portal wrapper, the guard line, the class addition and comments)
    - `test/layerOrder.test.tsx`'s SearchSheet invariant case now passes
    - The plan SUMMARY records the outcome of all four D-23 audit items
  </acceptance_criteria>
  <done>SearchSheet competes at the top level, keeps its accessible contract, and carries its gesture suppression explicitly rather than by inheritance.</done>
</task>

<task type="auto">
  <name>Task 3: Portal FabMenu — the mid-show tap-eater (D-27)</name>
  <files>packages/app/src/show/FabMenu.tsx</files>
  <read_first>
    - packages/app/src/show/FabMenu.tsx lines 95-140 — the returned fragment: the conditional `fab-scrim` div (`className="fab-menu fixed inset-0"`, `zIndex: config.ui.z.fabScrim`) and the speed-dial container (`className="fab-menu fixed flex flex-col items-end gap-2 motion-safe:transition-[bottom] …"`, `zIndex: config.ui.z.fab`, `bottom: bottomOffset`, `right: rightOffset`, `data-strip-slot-reserved`)
    - packages/app/src/components/Sheet.tsx lines 60-115 — the portal shape and the `typeof document === "undefined"` guard
    - packages/app/src/styles.css lines 21-37 — `.fab-menu` is already in the gesture-suppression selector list, and both FabMenu roots already carry that class
    - .planning/phases/21-layout-layering-foundations/21-HUMAN-UAT.md §"### 7." — the recorded result for FabMenu, including whether taps were eaten
    - packages/app/test/fabMenu.test.tsx — the shipped regression net, which queries by testid and dataset
  </read_first>
  <action>
    **Gate on the recorded repro.** If `21-HUMAN-UAT.md` test 7 recorded that the band paints over
    the FabMenu and/or eats its taps, portal it. If it recorded that FabMenu was unaffected, skip
    this task, record why in the SUMMARY, and leave the code unchanged — D-27 and D-28 both say
    "fix only what reproduces". RESEARCH predicts it reproduces.

    Wrap the returned fragment (scrim + speed-dial container) in a single
    `createPortal(<>…</>, document.body)`, imported from `react-dom`, with the
    `if (typeof document === "undefined") return null;` guard immediately before the return.
    Portal **both** roots together so the scrim and the rows keep their relative order and their
    `fabScrim < fab` relationship at the top level. Change nothing else: both `className`s (which
    already carry `.fab-menu`, so the gesture suppression travels with them — no `styles.css`
    change is needed here), both `zIndex` expressions, `bottom`/`right`, the
    `data-strip-slot-reserved` attribute, the `data-testid="fab-scrim"`, the `onClick` handlers
    and the `motion-safe:transition-[bottom]` classes all stay byte-identical.

    Note in a comment that portaling the FAB is a **layering** fix, not a positioning one: both
    roots are already `position: fixed` and therefore viewport-anchored, so their offsets —
    including `var(--gz-fab-offset)` from plan 21-08 — resolve identically outside `#app-content`.
    The vars resolve because the ladder is set on `document.documentElement` (plan 21-07), which
    `document.body` inherits from.

    Add assertions to `packages/app/test/layerOrder.test.tsx` in the same commit: rendered inside
    the `withBackground` fixture with the menu open, both the `fab-scrim` element and the
    speed-dial container are children of `document.body`; `ancestorsUpToBody` of each creates no
    stacking context; and `config.ui.z.fabScrim < config.ui.z.fab` still holds for the rendered
    inline values (not just the config values).
  </action>
  <verify>
    <automated>cd packages/app &amp;&amp; npx vitest run --project @guezzer/app test/layerOrder.test.tsx test/fabMenu.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Either: `FabMenu.tsx` imports `createPortal` and returns `createPortal(<>…</>, document.body)` with the `typeof document` guard — or the SUMMARY records that the repro showed FabMenu unaffected and the file is unchanged
    - If portaled: both roots still carry the `fab-menu` class, both `zIndex` expressions are unchanged, `data-testid="fab-scrim"` and `data-strip-slot-reserved` are unchanged
    - `test/fabMenu.test.tsx` passes unmodified — its testid and dataset queries are portal-agnostic
    - `test/layerOrder.test.tsx`'s FabMenu invariant case passes
    - `npm test` passes with no new failures against the 954-test baseline
  </acceptance_criteria>
  <done>The speed-dial and its scrim compete at the top level, so a toast can no longer eat a tap in the live-logging loop.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DOM tree position → a11y guarantees | A portaled node leaves its trigger's subtree; focus order, Escape reachability and `inert` scoping are all tree-position properties |
| Rendered overlay → tap target | A surface that paints over a control silently intercepts input; there is no error, only a lost song |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-30 | Denial of Service (usability) | `show/FabMenu.tsx` under a `toast: 20` | mitigate | Portal both FAB roots to `document.body` so `fabScrim: 25` / `fab: 30` compete at the top level instead of at effective level 10. This is the live-logging loop — a toast eating a speed-dial tap mid-show costs a logged song. Verified live in the plan-21-04 repro and re-checked in plan 21-13. |
| T-21-31 | Denial of Service (usability) | `show/SearchSheet.tsx` gesture suppression after portaling | mitigate | D-23: the `gesture-guard` class is applied to the portaled root and added to the `styles.css` selector list, restoring `touch-action: manipulation`, `overscroll-behavior: none` and `-webkit-touch-callout: none`. No unit test can catch this — device UAT test 8 is budgeted for it. |
| T-21-32 | Tampering | a11y contract of the portaled surfaces | mitigate | Four named D-23 audit items (gesture, Escape, focus, inert) with recorded outcomes; `role`, `aria-modal` and `aria-label` pinned unchanged by the `git diff` acceptance criteria; the shipped `fabMenu.test.tsx` must pass unmodified. |
| T-21-33 | Repudiation | a layer test that passes vacuously | mitigate | The invariant is RED for `SearchSheet` and `FabMenu` before the fixes land, and that RED output is recorded in the SUMMARY. Three detector self-tests prove `createsStackingContext` actually fires on the `ShowView` idiom. |
| T-21-SC | Tampering | npm installs | mitigate | Zero packages added, removed or upgraded (`21-RESEARCH.md` §Package Legitimacy Audit). `createPortal` comes from the already-installed `react-dom`. No install task; `package.json` is not in `files_modified`. |
</threat_model>

<verification>
- `npm test` green (baseline 125 files / 954 tests) including the new `layerOrder.test.tsx`
- `npm run build --workspace packages/app` succeeds
- `?layerRepro=1` in a desktop browser: the band no longer paints over the open SearchSheet or the open speed-dial
</verification>

<success_criteria>
- The FOUND-03 invariant is structural, self-tested, and was demonstrably RED before the fix
- `SearchSheet` and `FabMenu` are portaled and stay hand-rolled
- WR-01 and CR-01 are locked by named assertions
- The `NodeSheet` and `focusedFab` exemptions survive by name with their reasons
- SearchSheet's gesture suppression is explicit on the portaled root
</success_criteria>

<artifacts_this_phase_produces>
Symbols, files and flags newly created by Phase 21 (all plans). Nothing below exists on `master`
before this phase.

**CSS custom properties (new):** `--gz-safe-bottom`, `--gz-tab-bar-h`, `--gz-overlay-inset`,
`--gz-chrome-reserve`, `--gz-content-reserve`, `--gz-sheet-pad-bottom`, `--gz-fab-offset`

**New CSS class (new):** `.gesture-guard` (added to the `styles.css` gesture-suppression
selector list for portaled roots, D-23)

**Config keys (new):** `config.ui.bottomSpace`, `config.ui.bottomSpace.TAB_BAR_HEIGHT_REM`,
`config.ui.bottomSpace.FAB_CLEARANCE_PX`, `config.ui.bottomSpace.SHEET_PAD_BOTTOM_PX`,
`config.copy.tabs` (`show`/`explore`/`map`/`dex`/`games`), `config.copy.presence.activity`,
`config.copy.presence.activityUnknown`

**New modules and exports:** `packages/app/src/layout/bottomSpace.ts`
(`BOTTOM_SPACE_VAR_NAMES`, `bottomSpaceVarEntries`, `applyBottomSpaceVars`,
`useBottomSpaceVars`); `packages/app/src/dex/formatDate.ts` (`formatFullDate`, plus the re-homed
`formatMonYear`); `packages/app/src/dev/layerRepro.tsx` (`isLayerReproEnabled`,
`LayerReproToast`); `packages/app/src/dev/LayoutProbe.tsx` (`isLayoutProbeEnabled`,
`LayoutProbe`, `readSafeAreaInsets`); `packages/app/src/dex/FriendRow.tsx`
(`presenceActivityLabel`); `packages/app/src/dex/shareCard.ts` (module-private
`composeFooterLine`)

**New test-local helpers:** `packages/app/test/layerOrder.test.tsx` (`createsStackingContext`,
`ancestorsUpToBody`)

**Changed signatures:** `showBottomFabOffset(stripSlotReserved: boolean)` (was
`stripHasContent`); `FabMenu` / `OrbitStage` prop `stripSlotReserved` (was `stripHasContent`);
`data-strip-slot-reserved` DOM attribute (was `data-strip-has-content`); `PresenceActivitySlot`
gains an `online?: boolean` prop

**URL flags (new):** `?layerRepro=1`, `?layoutProbe=1`

**New test files:** `packages/app/test/layerRepro.test.tsx`,
`packages/app/test/bottomSpace.test.ts`, `packages/app/test/layerOrder.test.tsx`,
`packages/app/test/formatDate.test.ts`, `packages/app/test/presenceLabels.test.ts`,
`packages/app/test/setlistView.test.tsx`

**New docs:** `.planning/phases/21-layout-layering-foundations/21-HUMAN-UAT.md`,
`.planning/todos/pending/2026-07-24-simultaneous-bottom-overlay-stacking.md`

**Removed by this phase:** `packages/app/src/dex/formatMonYear.ts` (renamed to `formatDate.ts`);
the `padding-bottom: env(safe-area-inset-bottom)` declaration on `body` in `styles.css`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/21-layout-layering-foundations/21-11-SUMMARY.md` when done.
</output>
