---
phase: 07-explore-mode-constellation
plan: 05
type: execute
wave: 4
depends_on: [07-04]
files_modified:
  - packages/app/src/explore/ExploreFilterFab.tsx
  - packages/app/src/explore/ExploreFilterPanel.tsx
  - packages/app/src/explore/ExploreView.tsx
  - packages/app/src/explore/ConstellationCanvas.tsx
autonomous: true
requirements: [EXPL-03, EXPL-04]
must_haves:
  truths:
    - "The default view is the current-era active rotation (last N=5 shows → 56 songs via rotationSongIds), with a Rotation | Full catalog toggle in the filter panel (EXPL-03/D-05)"
    - "An edge-count slider (default ≥2, range 1–10) hides links below the threshold live, with no simulation reheat (EXPL-04)"
    - "Hiding all of a node's edges leaves the node visible as a free-floating star — the node population never churns with the slider (D-08)"
    - "All controls live in a collapsed bottom-right filter FAB that expands a no-scrim panel; the graph stays live while sliding (D-09)"
  artifacts:
    - path: "packages/app/src/explore/ExploreFilterFab.tsx"
      provides: "collapsed filter FAB (56px, SlidersHorizontal, no scrim)"
      min_lines: 25
    - path: "packages/app/src/explore/ExploreFilterPanel.tsx"
      provides: "Rotation|Full toggle + edge slider (overlay switch seam for Slice 4)"
      min_lines: 35
  key_links:
    - from: "packages/app/src/explore/ExploreView.tsx"
      to: "rotationSongIds"
      via: "Rotation view node population"
      pattern: "rotationSongIds"
    - from: "packages/app/src/explore/ConstellationCanvas.tsx"
      to: "link.count >= threshold"
      via: "render-pass edge filter (nodes unchanged)"
      pattern: "count\\s*>=|EDGE_COUNT_THRESHOLD"
---

<objective>
Slice 3 — shape the sky. Add the collapsed filter FAB (bottom-right, Phase-6 FabMenu idiom, no scrim) expanding a panel with the Rotation | Full catalog toggle and the edge-count slider. Make Rotation the opening default (last-5-shows → 56 legible nodes via `rotationSongIds`), and hide sub-threshold edges live (default ≥2) while nodes stay put as free-floating stars.

Purpose: This closes EXPL-03 (rotation default + full toggle) and EXPL-04 (edge slider). The filters are VISUAL-ONLY per D-03/D-08: the slider filters drawn edges without churning the node population, and a tapped node's bars still report its complete history. Positions are preserved on toggle (the sky must feel stable); only Rotation→Full spawns new nodes with a low-alpha reheat of unfixed nodes.
Output: A working filter FAB + rotation-default view + edge slider.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-explore-mode-constellation/07-RESEARCH.md
@.planning/phases/07-explore-mode-constellation/07-PATTERNS.md
@.planning/phases/07-explore-mode-constellation/07-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ExploreFilterFab + ExploreFilterPanel (rotation toggle + edge slider)</name>
  <files>packages/app/src/explore/ExploreFilterFab.tsx, packages/app/src/explore/ExploreFilterPanel.tsx</files>
  <read_first>
    - packages/app/src/show/FabMenu.tsx lines 52 + 75-132 (fixed-anchor + collapse-state + glyph-rotate idiom; the `fab-scrim` at 82-90 is Show-Mode-only — DO NOT copy it)
    - 07-PATTERNS.md lines 181-194 (FAB divergences: SlidersHorizontal glyph not Plus; NO scrim; bottom offset omits SUGGESTION_STRIP_HEIGHT; rows ≥44px; accent gold only on active toggle half; slider applies immediately)
    - 07-UI-SPEC.md §"Layout" region 3 (three controls top→bottom: Rotation|Full, edge slider, overlay switch), §"Spacing" FAB anchor (bottom = safe-area + tab bar + sm; right = md + safe-area), §"Color" accent reserved list, §"Copywriting Contract" (aria-label "Explore filters", rotationHelper, edgeSliderLabel, overlay switch label)
    - packages/app/src/config.ts explore + copy.explore (from 07-01); ui.FAB_DIAMETER (56)
  </read_first>
  <action>
    Create `packages/app/src/explore/ExploreFilterFab.tsx`: copy FabMenu's fixed bottom-right anchor + `open` collapse state + glyph-rotate-45°-on-open, but use the `SlidersHorizontal` lucide glyph, render NO scrim (the graph stays live while sliding, D-09), and OMIT the `SUGGESTION_STRIP_HEIGHT` term from the bottom offset (Show-Mode-only chrome). 56px circle, `bg-elevated border-hairline` (NOT accent — Phase-6 precedent). aria-label `config.copy.explore` "Explore filters". Tapping the FAB or the canvas collapses the panel.
    Create `packages/app/src/explore/ExploreFilterPanel.tsx`: a compact secondary-surface panel opening upward (md padding, hairline border) with three ≥44px control rows: (1) Rotation | Full catalog segmented toggle — accent gold ONLY on the active half; helper line `config.copy.explore.rotationHelper(N)` under Rotation. (2) Edge-count slider, range `EDGE_SLIDER_MIN`..`EDGE_SLIDER_MAX` (1–10), default `EDGE_COUNT_THRESHOLD_DEFAULT` (2), label `config.copy.explore.edgeSliderLabel(x)` with a tabular-nums value readout; changes fire immediately. (3) A placeholder/seam for the dex-overlay switch (Slice 4 fills it) — render a disabled-or-stubbed row now or leave a clearly-marked prop, executor's choice, but reserve the slot. Controls are DOM + keyboard/AT operable with a gold focus-visible ring.
  </action>
  <verify>
    <automated>pnpm --filter @guezzer/app exec tsc --noEmit 2>&1 | tail -8; pnpm -w test --run 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - FAB is 56px, bottom-right, `SlidersHorizontal` glyph rotating 45° when open, NO scrim, no SUGGESTION_STRIP_HEIGHT term
    - Panel has Rotation|Full toggle (accent only on active half) + edge slider (range 1–10, default 2, live) + a reserved overlay-switch slot
    - Slider/toggle rows ≥44px; aria-label "Explore filters"; slider readout uses tabular-nums
    - app typecheck + full suite green
  </acceptance_criteria>
  <done>The filter FAB expands a no-scrim panel with a live rotation toggle and edge slider.</done>
</task>

<task type="auto">
  <name>Task 2: Wire rotation-default population + render-pass edge filter</name>
  <files>packages/app/src/explore/ExploreView.tsx, packages/app/src/explore/ConstellationCanvas.tsx</files>
  <read_first>
    - packages/app/src/explore/ExploreView.tsx (from 07-04: focus state, deriveConstellation memo)
    - packages/app/src/explore/ConstellationCanvas.tsx (from 07-04: graphData, focus-dim, settle-freeze)
    - packages/app/src/dex/archive-loader.ts lines 31-45 (`loadArchive()` guarded sentinel — reuse VERBATIM for the rotation source)
    - packages/core barrel: `rotationSongIds` (from 07-01)
    - 07-RESEARCH.md §"Anti-Patterns" (slider filters links only, never nodes; don't reheat on slider/overlay), §"Pitfall 6" (focused node exempt from filters), §"Interaction rules" (filter/overlay changes preserve positions; Rotation→Full low-alpha reheat of unfixed nodes only)
    - 07-UI-SPEC.md §"Interaction rules" (slider hides all of a node's edges → node stays a free-floating star, D-08)
  </read_first>
  <action>
    In `ExploreView.tsx`: add `view: "rotation" | "full"` (default "rotation") and `edgeThreshold` (default `EDGE_COUNT_THRESHOLD_DEFAULT`) state, driven by the panel. Load the archive via `loadArchive()` (guarded, reuse verbatim) and `useMemo(() => rotationSongIds(archive, config.explore.ROTATION_WINDOW_SHOWS), [archive])`. Build the drawn node set: Rotation → nodes whose songId ∈ rotation Set; Full → all nodes. ALWAYS union in the focused node + its neighbors (Pitfall 6 filter exemption from 07-04). Rebuild `graphData` ONLY when the node population actually changes (view toggle), not on slider/overlay ticks (RESEARCH Pitfall 1 — preserve fx/fy on survivors so the sky stays stable; entering nodes get a low-alpha reheat, then freeze on onEngineStop). Handle the rotation-empty corpus edge case with the `config.copy.explore` "Nothing in rotation" state.
    In `ConstellationCanvas.tsx`: accept an `edgeThreshold` prop and filter EDGES only in the render pass — draw a link iff `link.count >= edgeThreshold`. The node array is unaffected: a node whose every edge is hidden still draws as a free-floating star and stays tappable (D-08). Slider changes are pure render-pass — no `graphData` rebuild, no reheat.
    Pass `view`, `edgeThreshold`, and their setters between ExploreView ↔ ExploreFilterPanel/FAB.
  </action>
  <verify>
    <automated>pnpm --filter @guezzer/app exec tsc --noEmit 2>&1 | tail -8; pnpm -w test --run 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - Default view is Rotation (56 nodes from `rotationSongIds`, N=5); Full toggle shows all matrix nodes
    - The edge slider hides links with `count < threshold` in the render pass; the node count is identical before/after sliding (free-floating stars remain)
    - Toggling the slider does NOT rebuild graphData or reheat the simulation; toggling Rotation↔Full preserves surviving node positions (fx/fy) and only reheats entering nodes
    - `loadArchive()` reused verbatim; rotation-empty edge case shows the honest "Nothing in rotation" state
    - app typecheck + full suite green
  </acceptance_criteria>
  <done>Rotation is the default sky; the slider hides sub-threshold edges without churning nodes or reheating.</done>
</task>

</tasks>

<artifacts_produced>
- `ExploreFilterFab` component — `packages/app/src/explore/ExploreFilterFab.tsx`
- `ExploreFilterPanel` component — `packages/app/src/explore/ExploreFilterPanel.tsx`
- `view`/`edgeThreshold` state + rotation/threshold wiring added to `ExploreView` and `ConstellationCanvas` (extends prior symbols)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bundled archive → app loader | `archive.json` loaded via the schemaVersion-guarded `loadArchive()` sentinel |
| user slider/toggle input → render | bounded int (1–10) + binary toggle — the only user input in the phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Tampering/DoS | archive load | mitigate | `loadArchive()` schemaVersion===1 guard → calm error/empty state, never a crash |
| T-07-07 | Tampering | slider/toggle input | accept | Slider is a bounded integer (EDGE_SLIDER_MIN..MAX) and the toggle is binary; neither reaches derivation as free text. No injection surface. |

No writes, no network. No high-severity threat.
</threat_model>

<verification>
- `pnpm -w test --run` + `pnpm --filter @guezzer/app exec tsc --noEmit` green
- Manual (executor, dev server): Explore opens on Rotation (~56 nodes); Full shows the whole catalog; sliding the edge slider thins edges while node count holds; sliding does not restart the sim
</verification>

<success_criteria>
- Rotation-default + Full toggle (EXPL-03) and the live edge slider (EXPL-04) work
- D-08 free-floating stars hold; positions stay stable across filter changes
</success_criteria>

<output>
Create `.planning/phases/07-explore-mode-constellation/07-05-SUMMARY.md` when done.
</output>
