---
phase: quick-260717-k1v
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/app/src/dex/DexView.tsx
  - packages/app/src/dex/AlbumGrid.tsx
autonomous: true
requirements: [QUICK-260717-k1v]

must_haves:
  truths:
    - "On phones (<=414px) the GizzDex album shelf renders exactly 2 columns at ~448px width — visually identical to today"
    - "On tablet/desktop the album shelf widens past 448px and fills with additional columns instead of stretching 2 cards"
    - "The three DexView width surfaces (body, error hold-frame, loading hold-frame) share the same responsive max-width — no width jump between states"
  artifacts:
    - path: "packages/app/src/dex/DexView.tsx"
      provides: "Responsive max-width on the dex body container and both hold-frames"
    - path: "packages/app/src/dex/AlbumGrid.tsx"
      provides: "Fluid auto-fill album grid that adds columns as width grows"
  key_links:
    - from: "packages/app/src/dex/DexView.tsx"
      to: "packages/app/src/dex/AlbumGrid.tsx"
      via: "AlbumGrid rendered inside the widened body container"
      pattern: "max-w-md sm:max-w-3xl"
---

<objective>
Make the GizzDex (`#/dex`) album-shelf body responsive to screen width. Today the body is capped at phone width (~448px / `max-w-md`) even on desktop; it should widen progressively on larger screens while keeping the phone layout byte-for-byte identical.

Purpose: The dex doubles as a browsable "Pokédex" of songs caught live; on tablet/desktop it currently wastes the screen and shows a narrow phone column. This is a purely responsive layout change — NO functionality, logic, data, handler, or config change.

Output: Responsive Tailwind classNames on two files (`DexView.tsx` container + two hold-frames, `AlbumGrid.tsx` grid).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

# Target files (only these two are touched)
@packages/app/src/dex/DexView.tsx
@packages/app/src/dex/AlbumGrid.tsx
</context>

<scouted_facts>
Pre-traced by the orchestrator — do NOT re-derive:

1. **Root cause:** `DexView.tsx:91` wraps the entire dex body in `<div className="mx-auto flex w-full max-w-md flex-col">`. `max-w-md` = 28rem (~448px) is the phone-width cap. AppShell's `<main>` is already full-width and does NOT constrain — the cap is entirely this `max-w-md`. Widening this container also widens the header, segment toggle, and Shows list because they are all its children.

2. **Same `max-w-md` on both hold-frames:** `DexView.tsx:76` (error state) and `DexView.tsx:84` (loading placeholder). Apply the SAME responsive max-width so there is no width jump between states.

3. **Grid must go responsive too:** `AlbumGrid.tsx:60` is `<div className="grid grid-cols-2 gap-2 px-4 pb-16">`. `grid-cols-2` is FIXED at 2 columns — if only the container widens, the 2 columns stretch into huge cards. The album cover is fixed at `config.dex.ALBUM_ART_DISPLAY_PX = 80` (CoverThumb renders explicit px). Correct behavior = MORE columns as width grows, keeping cards ~phone-sized. Do NOT scale covers (WebP thumbs are 160×160; upscaling looks blurry).

4. **Tests are safe:** `packages/app/test/dexView.test.tsx` asserts only on `data-testid="album-card"` (counts + content), never on `max-w-md`/`grid-cols-2`/layout classes. No test edits needed.

5. **Tooling:** Tailwind CSS v4 — arbitrary values (`grid-cols-[repeat(auto-fill,minmax(9.5rem,1fr))]`) and breakpoint prefixes (`sm:`/`md:`/`lg:`/`xl:`) are available.

6. **Out of scope (do NOT touch):** `AlbumDetail.tsx`, `ArchiveBrowser.tsx` (already `fixed inset-0` full-screen, no max-width), `RecapView.tsx:146`, `ShowsList.tsx:170` (their own `max-w-md` belongs to the Shows/recap surfaces, not the album-shelf body), `AppShell.tsx`, and `core`.
</scouted_facts>

<tasks>

<task type="auto">
  <name>Task 1: Widen the DexView body + hold-frames responsively</name>
  <files>packages/app/src/dex/DexView.tsx</files>
  <action>
Replace the fixed `max-w-md` on the three width surfaces with a progressive responsive max-width, keeping `mx-auto` (stays centered) and every other class unchanged:

- Line 91 (body container): `mx-auto flex w-full max-w-md flex-col` → `mx-auto flex w-full max-w-md sm:max-w-3xl lg:max-w-5xl xl:max-w-6xl flex-col`
- Line 76 (error hold-frame): `mx-auto flex w-full max-w-md flex-col px-4 pt-16 text-center` → add the same `sm:max-w-3xl lg:max-w-5xl xl:max-w-6xl` right after `max-w-md`
- Line 84 (loading hold-frame): `mx-auto w-full max-w-md` → add the same `sm:max-w-3xl lg:max-w-5xl xl:max-w-6xl` right after `max-w-md`

All three must carry the IDENTICAL responsive max-width chain so there is no width jump between error/loading/ready states. Phone (<640px) stays at `max-w-md` (~448px) — byte-for-byte unchanged. Do NOT add, remove, or reorder any other class. Do NOT change any JSX structure, props, handlers, or logic. Touch only these three className strings.
  </action>
  <verify>
    <automated>cd packages/app && npx tsc --noEmit</automated>
  </verify>
  <done>All three DexView width surfaces carry `max-w-md sm:max-w-3xl lg:max-w-5xl xl:max-w-6xl`; typecheck passes; no other lines changed.</done>
</task>

<task type="auto">
  <name>Task 2: Make the AlbumGrid fluid (auto-fill columns)</name>
  <files>packages/app/src/dex/AlbumGrid.tsx</files>
  <action>
Replace the fixed 2-column grid at line 60 with a fluid auto-fill grid, keeping `gap-2 px-4 pb-16` unchanged:

- `grid grid-cols-2 gap-2 px-4 pb-16` → `grid grid-cols-[repeat(auto-fill,minmax(9.5rem,1fr))] gap-2 px-4 pb-16`

HARD CONSTRAINT: must still render exactly **2 columns at <=414px viewport** (iPhone Pro Max). `minmax(9.5rem≈152px)` satisfies this: with `gap-2` (0.5rem) and `px-4` padding, 2 columns fit in a 360–414px phone and 3 do not — so the phone layout is identical to today, and columns only increase on wider screens as the DexView container (Task 1) widens. Do NOT scale the covers (they stay at the fixed 80px `ALBUM_ART_DISPLAY_PX`). Do NOT change any JSX structure, the `shelf.map`, `AlbumCard` props, or any logic. Touch only this one className string.
  </action>
  <verify>
    <automated>cd packages/app && npx tsc --noEmit</automated>
  </verify>
  <done>Grid uses `grid-cols-[repeat(auto-fill,minmax(9.5rem,1fr))]` with unchanged `gap-2 px-4 pb-16`; still 2 columns at <=414px; typecheck passes.</done>
</task>

</tasks>

<verification>
Run from the REPO ROOT (do NOT `cd packages/app` for vitest — that bypasses the root `projects` config: jsdom env + `@dexAlbums` alias won't load):

- `npx vitest run` — full suite passes (dexView.test.tsx unaffected; it asserts only on `album-card` testids, never layout classes).
- `cd packages/app && npx tsc --noEmit` — typecheck clean.
- Manual visual check on the running dev server (http://localhost:5175, route `#/dex`):
  - Narrow the window to a phone width (<=414px): still 2 columns, ~448px shelf, unchanged.
  - Widen to tablet/desktop: shelf grows past 448px, fills with 3+ columns of phone-sized cards (covers NOT enlarged); header, segment toggle, and Shows list widen with it.
</verification>

<success_criteria>
- Phone (<=414px): GizzDex album shelf renders exactly 2 columns at ~448px — visually identical to before this change.
- Tablet/desktop: shelf widens progressively (sm/lg/xl breakpoints) and fills with additional auto-fill columns rather than stretching 2 cards; covers stay 80px.
- All three DexView width surfaces (body, error, loading) share the same responsive max-width — no width jump.
- Only two files changed; only className strings changed; no functionality/logic/data/config change.
- `npx vitest run` (repo root) and `cd packages/app && npx tsc --noEmit` both pass.
</success_criteria>

<output>
Create `.planning/quick/260717-k1v-make-gizzdex-layout-responsive-widen-alb/260717-k1v-SUMMARY.md` when done.

In the SUMMARY, note as a possible FUTURE follow-up (NOT part of this task): the drill-in overlays `AlbumDetail.tsx`/`ArchiveBrowser.tsx` are already full-screen, and `RecapView.tsx`/`ShowsList.tsx` retain their own `max-w-md` on the Shows/recap surfaces — a future pass could evaluate whether those recap surfaces should also widen.
</output>
